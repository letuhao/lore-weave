// Hoisted to the shared @/hooks/useIsMobile so composition + knowledge use one source
// (LOOM composition mobile pass). Re-exported here to keep existing knowledge importers
// working unchanged.
export { useIsMobile } from '@/hooks/useIsMobile';
