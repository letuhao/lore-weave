{
  "scenario": "S07",
  "title": "Build a book end-to-end — import → translate → world info → connections → notes",
  "maps_to": "W7 build-a-book rail",
  "persona": "P1/P2 — has a manuscript, wants the whole pipeline run for them",
  "model": "gemma-4-26b-a4b-qat",
  "permission_mode": "write",
  "fixture": "⚠️ a book WITH source chapters (prose present, untranslated) — SKILL_BOOK_ID. The rail chains over real content; on an empty book every step no-ops and the scenario silently passes without testing the chain.",
  "context": {"book_context": {"book_id": "<BOOK_ID>"}},
  "enabled_skills": [],
  "jargon_denylist": [
    "workflow", "rail", "pipeline", "glossary", "ontology", "kind", "entity",
    "knowledge graph", "KG", "wiki", "job", "async", "tool"
  ],
  "canon_facts": [
    "the chain actually advanced (each stage's artifact is in the DB, not just claimed)",
    "the agent never asked the user to name a tool or a stage",
    "any long-running stage was POLLED to a terminal state before it was called done",
    "no stage was claimed complete while its artifact was absent"
  ],
  "movements": [
    {"id": "A", "label": "Hand it the book"},
    {"id": "B", "label": "Let it run the chain"},
    {"id": "C", "label": "What is actually done?"}
  ],
  "_note": "S07 exercises the SEEDED build-a-book rail (W7 = 'turn the idea into a book plan' → plan_propose_spec, done_when plan>0). The _INDEX's broader 'import→translate→KG' framing is aspirational; the built rail is planning. The prompt asks for a PLAN so the planning rail drives (an earlier 'get it set up' prompt triggered world-setup instead — my scenario bug, fixed 2026-07-14).",
  "turns": [
    {"movement": "A", "user": "I've got the whole story in my head. Before I start writing, help me lay out the plan for the entire book — the overall arc and the major beats."},
    {"movement": "B", "user": "Yes, build me the full plan."},
    {"movement": "C", "user": "What's the plan you laid out? Walk me through the shape of it."}
  ]
}
