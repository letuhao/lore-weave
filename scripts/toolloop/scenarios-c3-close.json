{
  "_note": "CYCLE 3 CLOSE-OUT — the two tools that could be concluded in NEITHER direction.\n\nsettings_provider_inventory: 1/5 called, and the newly declared expectation catches the real defect on the other 4 — the model answers from settings_list_models and calls registry rows 'what your providers currently offer'. Both sides of the substitution are now declared: the LOSING tool says what it is and is not, and the WINNING tool (settings_list_models) now says a retired model still appears in it and that live inventory belongs elsewhere. Fixing only the losing side left the winning side just as attractive.\n\nworld_map_add_marker: called 5/5 with map_id now naming world_map_list, and ONE run died in transport, which is not evidence about the tool in either direction. Its `gate` case also needs a live card.",
  "scenarios": [
    {
      "id": "settings-provider-inventory",
      "tool_under_test": "settings_provider_inventory",
      "expect_tool": "settings_provider_inventory",
      "intent": "read",
      "tier": "R",
      "scope": "user",
      "model_ref": "019ebb72-27a2-72f3-a42d-d2d0e0ded179",
      "prompt": "What models can I use? Show me what my configured provider actually offers.",
      "prompt_source": "_meta.synonyms['what models']",
      "needs_target": false,
      "approve": null,
      "seed": [],
      "falsifier": "REFUTED IF: a read-intent turn changes the store, OR the reply lists models from a provider this account has not configured. scope=user, and it requires a provider_credential_id — so the interesting failure is the model inventing one rather than looking it up first.",
      "ship_audit": {
        "refusals": "EXERCISED 2026-08-14 at the TOOL boundary with SCHEMA-VALID arguments — two of these first died in argument validation (fact_type / scope enums), which is UNPROBED rather than refused, and re-probed with valid enum values afterwards. scope=user. Called as claude-test2 with the HARNESS user's provider_credential_id: refused 'not accessible' — a credential id is not a capability.",
        "gate": "Tier R; returns no secrets by contract."
      },
      "editor_context": false,
      "seed_assert": [],
      "answer_expect": {
        "none_of": [
          "currently offer"
        ],
        "why": "🔴 THE TOOL'S OWN DEFECT WAS UNCHECKABLE UNTIL NOW. answer_expect was null, so the gate's 'DATA answer is true' bar never ran, and the falsifier covers a store change and unconfigured providers — not the thing that actually blocks this tool. D-LIVE-INVENTORY-ANSWERED-FROM-THE-REGISTRY is the model answering from settings_list_models and presenting REGISTERED rows as what the provider currently OFFERS. Registry and live inventory are different sets: a model retired upstream still sits in the registry, so the answer is confidently wrong in exactly the case the question was asked to detect.\n\nMEASURED 2026-08-23 over K=5, and the phrase discriminates perfectly: 'currently offer' appears in all FOUR replies that did NOT call settings_provider_inventory and in NEITHER of the one that did — that run said 'currently registered' instead, which is true of what it read. So the expectation fails exactly the runs where the author is misled and passes the run where they are not."
      },
      "_carried_from": "scenarios-rebaseline.json",
      "_problem": "P5-SIBLING-WINS"
    },
    {
      "id": "world-map-add-marker",
      "tool_under_test": "world_map_add_marker",
      "expect_tool": "world_map_add_marker",
      "intent": "write",
      "tier": "A",
      "scope": "none",
      "model_ref": "019ebb72-27a2-72f3-a42d-d2d0e0ded179",
      "prompt": "On my Ashfall map, put a pin near the top-left labelled Hollow Keep.",
      "prompt_source": "_meta.synonyms['add pin'] + ['place marker']. The POSITION is described in the user's own words; the x/y the tool needs are derived by the model, never typed here.",
      "needs_target": false,
      "editor_context": false,
      "approve": null,
      "seed": [
        {
          "tool": "world_create",
          "args": {
            "name": "LOOP-THROWAWAY-ashfall",
            "description": "batch 7 map fixture"
          }
        },
        {
          "tool": "world_map_create",
          "args": {
            "world_id": "{step:0:world.world_id}",
            "name": "Ashfall"
          }
        }
      ],
      "falsifier": "REFUTED IF: the tool reports a marker placed while the map gains none, OR the marker's x/y fall outside the map's coordinate space, OR a Tier-A write lands with no confirm card. CONTROL: the model must find map_id itself (world_map_list) — a turn that never lists is a DISCOVERY failure, not a failure of this tool.",
      "ship_audit": {
        "gate": "OWED — Tier A.",
        "absent_case": "EXERCISED by direct probe (docs/eval/toolloop/2026-08-14/c3-ship-probe.txt): an absent map_id is refused with 'map not found — check the map_id (call world_map_list for valid ids)'.",
        "tenancy": "EXERCISED by direct probe (docs/eval/toolloop/2026-08-14/c3-ship-probe.txt): a foreign map_id refuses IDENTICALLY."
      },
      "defects": [],
      "_carried_from": "scenarios-rebaseline.json",
      "_problem": "P5-SIBLING-WINS"
    }
  ]
}
