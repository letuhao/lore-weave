{
 "_note": "one control arm for composition_generate.",
 "scenarios": [
  {
   "id": "composition-generate-named-chapter",
   "tool_under_test": "composition_generate",
   "expect_tool": "composition_generate",
   "intent": "write",
   "tier": "W",
   "scope": "book",
   "model_ref": "019ebb72-27a2-72f3-a42d-d2d0e0ded179",
   "prompt": "Write chapter — use the cowrite engine to draft the prose for \"Chapter I — The Ember Codex\".",
   "prompt_source": "meta.synonyms['write chapter'] + ['generate prose'], verbatim.",
   "needs_target": false,
   "approve": null,
   "seed": [],
   "seed_assert": [
    {
     "db": "loreweave_composition",
     "query": "SELECT count(*)::text FROM generation_job WHERE project_id='{project_id}'",
     "expect": "0",
     "why": "ZERO generation jobs before. This tool SPENDS: it runs the grounded cowrite engine and persists prose to the book draft. The card is never approved, so this count must still be 0 afterwards — that is the whole cost control."
    }
   ],
   "falsifier": "REFUTED IF: prose is generated with NO confirm card (Tier W and it spends), OR the model INVENTS a model_source/model_ref — both are REQUIRED and neither is ambient, so the honest move is to ask or to read the account's models via settings_list_models, and silently picking a model commits the author's money to a choice they never made — OR the reply claims a chapter was written while the card is still pending.",
   "ship_audit": {
    "gate": "The card IS the audit — this is the most expensive per-call tool in the batch.",
    "absent_case": "OWED — a chapter_id/outline_node_id that does not exist must be refused, and its description says the two are alternatives, so passing NEITHER must also be refused rather than defaulted.",
    "tenancy": "OWED — book-scoped, and the spend lands on the caller."
   },
   "defects": [],
   "_control_note": "CONTROL A/B for the 5/5 finding. In the first arm the model said 'I need to know which chapter you'd like me to work on' — while the book had exactly ONE chapter — and then proposed book_chapter_save_draft with prose it wrote itself. This arm names the chapter explicitly, so if chapter identity was the real blocker the tool should now be called. If it STILL proposes save_draft, the blocker is elsewhere and my required-model-args hypothesis stays unproven rather than becoming a fix."
  }
 ]
}
