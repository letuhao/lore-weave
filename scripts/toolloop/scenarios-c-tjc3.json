{
  "_note": "translation_job_control asked its own domain question — the previous prompt front-loaded 'Cancel job', the phrase that ties it with jobs_cancel.",
  "scenarios": [
    {
      "id": "translation-job-control-domain-question",
      "tool_under_test": "translation_job_control",
      "expect_tool": "translation_job_control",
      "intent": "write",
      "tier": "W",
      "scope": "book",
      "model_ref": "019ebb72-27a2-72f3-a42d-d2d0e0ded179",
      "prompt": "Stop the translation for this book.",
      "follow_ups": [
        "Cancel that job."
      ],
      "prompt_source": "THE TOOL'S OWN QUESTION, and the previous prompt was the one that made the tie.\n\nIt read 'Cancel job — stop translation for this book's running translation job.' Measured against the deployed matcher, that phrasing makes translation_job_control match BOTH 'cancel job' (10) and 'stop translation' (16) — and the matcher BREAKS on the first synonym that hits, so the tool contributes 10 and ties jobs_cancel exactly, while its own longer, exclusive match is discarded. The prompt front-loaded the generic tie-maker.\n\n'Stop the translation for this book.' is what a person asking this tool's question types, and R1 forces translation_job_control ALONE on it — jobs_cancel matches nothing, because it declares no translation vocabulary (checked: ['cancel job', 'stop job', 'abort job', 'kill job', 'stop a running job', 'cancel my task']).\n\n🔴 THIS IS NOT THE TIE BEING HIDDEN. The tie is real for 'Cancel that job.', where both tools declare the IDENTICAL string, and it stays recorded as DQ-T41's genuine instance. What this arm removes is a tie that the PROMPT manufactured, so the run measures whether the tool can be reached and used — which is what the gate is for. jobs_cancel remains advertised via its own domain, so the model can still choose it; nothing is being suppressed.",
      "needs_target": false,
      "approve": null,
      "seed": [
        {
          "sql": {
            "db": "loreweave_translation",
            "statement": "INSERT INTO translation_jobs (book_id, owner_user_id, status, target_language, model_source, model_ref, system_prompt, user_prompt_tpl, chapter_ids, total_chapters) VALUES ('{book_id}', '019d5e3c-7cc5-7e6a-8b27-1344e148bf7c', 'pending', 'vi', 'user_model', '019ebb72-27a2-72f3-a42d-d2d0e0ded179', 'sys', 'tpl', ARRAY['{chapter_id}']::uuid[], 1)",
            "why": "A PENDING translation job for the throwaway book. Seeded in SQL because the only tool that creates one is translation_start_job, which SPENDS — and a cost-bearing card is never approved. status='pending' rather than a terminal state so that CANCEL is a meaningful action rather than a no-op."
          }
        }
      ],
      "seed_assert": [
        {
          "db": "loreweave_translation",
          "query": "SELECT count(*)::text FROM translation_jobs WHERE book_id='{book_id}' AND status='pending'",
          "expect": "1",
          "why": "EXACTLY ONE pending job — the SELECT that proves the SQL seed did what it claims. The card is never approved, so it must still be 'pending' afterwards; a status of 'cancelled' would mean the cancel landed without approval."
        }
      ],
      "falsifier": "REFUTED IF: the job is cancelled with NO confirm card (Tier W), OR the action chosen is `resume` or `retry` — both RE-SPEND money by the tool's own description and neither is what the author asked for — OR the model invents a job_id rather than reading one from turn 1. CONTROL: cancel and pause 'apply immediately' while resume/retry return a cost estimate + confirm token, so the two halves of this enum have DIFFERENT gates and a model that treats them alike is wrong about its own tool.",
      "ship_audit": {
        "gate": "OWED — Tier W.",
        "absent_case": "OWED — a job_id that does not exist must be refused.",
        "idempotency": "OWED — cancelling an already-cancelled job must refuse or be a clear no-op.",
        "tenancy": "OWED — book-scoped."
      },
      "defects": [],
      "_carried_from": "scenarios-rebaseline.json",
      "_problem": "P11-DISTRIBUTION",
      "_original_prompt": "What is the status of the translation job for this book?"
    }
  ]
}
