{
  "_note": "composition_motif_link_edit with the Tier-A approval its own evidence shows it needs. Run WARM - a cold provider measures the loader, not the tool.",
  "scenarios": [
    {
      "id": "composition-motif-link-edit-approved",
      "tool_under_test": "composition_motif_link_edit",
      "expect_tool": "composition_motif_link_edit",
      "intent": "write",
      "tier": "A",
      "scope": "user",
      "model_ref": "019ebb72-27a2-72f3-a42d-d2d0e0ded179",
      "prompt": "Link motifs — mark Throwaway Loop Alpha {run_word} as coming before Throwaway Loop Beta {run_word}.",
      "prompt_source": "meta.synonyms['link motifs'] + ['set succession'], verbatim, plus both motif names so the endpoints are unambiguous.",
      "needs_target": false,
      "editor_context": false,
      "approve": "approved_once",
      "seed": [
        {
          "tool": "composition_motif_edit",
          "args": {
            "op": "create",
            "code": "throwaway-loop-alpha-b19-{run_id}",
            "name": "Throwaway Loop Alpha {run_word}",
            "kind": "trope"
          }
        },
        {
          "tool": "composition_motif_edit",
          "args": {
            "op": "create",
            "code": "throwaway-loop-beta-b19-{run_id}",
            "name": "Throwaway Loop Beta {run_word}",
            "kind": "trope"
          }
        }
      ],
      "seed_assert": [
        {
          "db": "loreweave_composition",
          "query": "SELECT count(*)::text FROM motif WHERE status='active' AND code IN ('throwaway-loop-alpha-b19-{run_id}','throwaway-loop-beta-b19-{run_id}')",
          "expect": "2",
          "why": "BOTH endpoints must exist and both must be the caller's own — the tool's description requires it ('both endpoints must be YOUR own motifs'). The model must resolve two NAMES to two ids, which is the resolution step that has failed repeatedly in this loop, and it must get them the right way round: `precedes` is directional.\n\n🔴 status='active' ADDED 2026-08-23. Without it this assertion counted ARCHIVED rows and passed while the tool could not see them: get_by_codes filters status='active', so the two names the prompt asks the model to link resolved to nothing, for fifteen runs. A fixture assertion that cannot see the same rows the tool sees is not an assertion."
        },
        {
          "db": "loreweave_composition",
          "query": "SELECT count(*)::text FROM motif_link WHERE from_motif_id IN (SELECT id FROM motif WHERE code LIKE 'throwaway-loop-%-b19-{run_id}') OR to_motif_id IN (SELECT id FROM motif WHERE code LIKE 'throwaway-loop-%-b19-{run_id}')",
          "expect": "0",
          "why": "SCOPED TO THE THROWAWAYS, not to the account's 63 existing links. My first draft asserted the global motif_link count and that is a fixture that fails for the wrong reason: motifs are ACCOUNT-scoped, every scenario in this batch seeds into the SAME library, and the runs are sequential — so any account-wide total drifts as the batch proceeds. Scoping to the seeded codes makes the assertion true regardless of what ran before it. The store diff the harness takes before/after each run is what catches an unexpected global change.\n\n🔴 status='active' ADDED 2026-08-23. Without it this assertion counted ARCHIVED rows and passed while the tool could not see them: get_by_codes filters status='active', so the two names the prompt asks the model to link resolved to nothing, for fifteen runs. A fixture assertion that cannot see the same rows the tool sees is not an assertion.\n\n🔴 THE LIKE WAS NONCED TOO, 2026-08-23. When the seed codes gained {run_id} this pattern stopped matching them, and an assertion that matches nothing and expects 0 passes for the wrong reason — it can never go red. A gate that cannot fail is not measuring the fixture, it is describing it."
        }
      ],
      "falsifier": "REFUTED IF: an edge lands with NO confirm card, OR the model invents a from_motif_id/to_motif_id rather than resolving the two names, OR `kind` is not `precedes` when the user said 'coming before' (its enum spans composed_of|precedes|variant_of, and variant_of would assert the two motifs are versions of each other, which is a different claim), OR the DIRECTION is reversed — Alpha precedes Beta, not the other way round, and a reversed edge is a wrong fact quietly stored.",
      "ship_audit": {
        "gate": "EXERCISED live (docs/eval/toolloop/2026-08-14/c2-arc.json): called 4/5, left SUSPENDED on a Tier-A approval card on 2 of 5 runs, store unchanged.",
        "absent_case": "EXERCISED by targeted probe (docs/eval/toolloop/2026-08-14/c2-ship-probe-opdispatch.txt), for the same op-dispatch reason: op=create with absent from/to motif ids is refused 'not found or not accessible'.",
        "tenancy": "EXERCISED by targeted probe (docs/eval/toolloop/2026-08-14/c2-ship-probe-opdispatch.txt): foreign ids refuse IDENTICALLY."
      },
      "defects": [],
      "_rerun_of": "batch19-v5",
      "approve_why": "TIER A, AND THE PREVIOUS ARM COULD NOT MOVE ITS STORE BECAUSE NOBODY APPROVED THE WRITE. Measured c-motiflink4: 3 of 3 complete runs resolved both motif NAMES to real UUIDs via composition_motif_search and called this tool correctly, and every one was recorded {kind: tool_approval, tier: A, pending: true, call_outcome: deferred} with the run ending left_suspended. `approve: null` cannot produce a write.\n\nSAFETY: the target is this run's OWN throwaway book, provisioned by the fixture seconds earlier and torn down after. Both motifs are the fixture's own. ONE approval, for this tool, for this cycle - nothing pre-existing is touched and no cost is incurred beyond the turn itself.",
      "seed_why_book_id_2026_08_24": "🔴 I ADDED `book_id` TO BOTH SEED STEPS AND TOOK IT BACK OUT — IT IS SILENTLY IGNORED, and a seed argument that does nothing is worse than none.\n\nThe intent was to give store_snapshot a per-run key for the new motif_link sweep. composition_motif_edit ADVERTISES book_id, so it looked available. It is only read inside the `target == 'book_shared'` branch (composition_motif_create: 'book_id is required when target=book_shared'); passing it alone is dropped. Measured after the change: 2 motifs created, both with book_id NULL.\n\nWHAT THAT MEANS, and it is a real limit rather than a bug to fix here: `motif.book_id` is set ONLY for book-shared motifs. A motif the caller merely OWNS is attached to the USER, not to any book. So this scenario's own store cannot be attributed to a run by any key store_snapshot sweeps, and the _motif_link_counts join through motif.book_id cannot see it.\n\nSetting target='book_shared' would make it observable and would test a DIFFERENT MODE — the one where passing book_id is correct. That is a legitimate second arm, not a substitute for this one, and choosing it because it is the arm that can go green would be picking the measurement to fit the answer."
    }
  ]
}
