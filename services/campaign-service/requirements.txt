fastapi>=0.141.1
uvicorn[standard]>=0.52.4
asyncpg>=0.31.0
pydantic>=2.13.4
pydantic-settings>=2.15.0
httpx>=0.27
PyJWT>=2.13.0
python-dotenv>=1.2.2
redis[hiredis]>=5.0

# loreweave_obs is installed via Dockerfile COPY of sdks/python (build
# context is the repo root). For host-side pytest:
#   pip install -e ../../sdks/python   (from services/campaign-service)

# test
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.15.1
