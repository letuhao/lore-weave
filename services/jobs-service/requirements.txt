fastapi>=0.139.0
uvicorn[standard]>=0.51.0
asyncpg>=0.29
pydantic>=2.13.4
pydantic-settings>=2.14.2
httpx>=0.27
PyJWT>=2.13.0
python-dotenv>=1.0
redis[hiredis]>=5.0

# loreweave_jobs + loreweave_obs + loreweave_mcp (the shared MCP kit backing the
# /mcp facade — pulls in `mcp>=1.27` via its own deps) are installed via the
# Dockerfile COPY+pip-install of sdks/python (build context is the repo root). For
# host-side pytest they resolve via pytest.ini `pythonpath = ../../sdks/python`.

# test
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
