fastapi>=0.111
uvicorn[standard]>=0.29
asyncpg>=0.29
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
PyJWT>=2.8
python-dotenv>=1.0
redis[hiredis]>=5.0

# loreweave_jobs + loreweave_obs are installed via the Dockerfile COPY of
# sdks/python (build context is the repo root). For host-side pytest the SDK
# is resolved via pytest.ini `pythonpath = ../../sdks/python`.

# test
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
