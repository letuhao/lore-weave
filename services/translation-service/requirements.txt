fastapi>=0.111
uvicorn[standard]>=0.29
asyncpg>=0.31.0
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
PyJWT>=2.8
python-dotenv>=1.0
aio-pika>=9.4
redis[hiredis]>=5.0
# CACHE/M6 raw-output offload (D-RAWCACHE-MINIO-OFFLOAD) — S3/MinIO cold archive of the
# bulky verbatim raw_response column. Mirrors chat-service's boto3 storage client.
boto3>=1.34

# Phase 4c-α: loreweave_llm + loreweave_extraction SDKs are installed
# via Dockerfile COPY of sdks/python (build context is repo root).
# For host-side pytest:
#   pip install -e ../../sdks/python   (from services/translation-service)

# test
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
