HTTP_ADDR=:8081
DATABASE_URL=postgres://loreweave:loreweave_dev@localhost:5432/loreweave?sslmode=disable
JWT_SECRET=change_me_dev_only_min_32_chars_long_secret
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=604800
PASSWORD_MIN_LENGTH=8
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_REQUESTS=60
DEV_LOG_EMAIL_TOKENS=1

# Optional: send verification / password-reset mail via SMTP (Mailhog: host localhost, port 1025)
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_FROM=LoreWeave <noreply@loreweave.local>
# SMTP_USER=
# SMTP_PASSWORD=
# Links in emails (browser URL of the frontend)
# PUBLIC_APP_URL=http://localhost:5173
