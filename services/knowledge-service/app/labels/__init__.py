"""KG-ML M5 (Layer 2) — localization labels.

Backend-served label resolution for KG output (predicate edge labels now;
kind/entity-name labels join in via the timeline/graph resolvers). Backend-served
(not frontend-only, R5) so chat / MCP / agent consumers get localized labels too.
"""
