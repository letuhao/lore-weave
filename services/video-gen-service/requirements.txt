fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.7
pydantic-settings==2.6.1
httpx==0.28.1
minio==7.2.15
PyJWT==2.10.1
# LLM re-arch Phase 3 M5 — decoupled video-gen job-row + terminal-event worker.
# asyncpg backs the video_gen_jobs store (imported by the router → hard dep even
# flag-off); redis.asyncio is the terminal-event consumer (worker process only).
asyncpg>=0.31.0
redis[hiredis]>=5.0
# loreweave_llm SDK installed via `pip install /sdk` from repo root
# (see Dockerfile — Phase 5e-α). Pinned to the version in sdks/python.
