asyncpg>=0.29
httpx>=0.27
pydantic>=2.13.4
pydantic-settings>=2.14.2
python-json-logger>=2.0
# P3 D-P3-WORKER-AI-CONSUMER-WIRING — Redis Stream consumer for the
# extraction.summarize job feed. Async client only (sync API unused).
redis>=5.0
# Cycle 73h D-WORKER-AI-METRICS-INFRA — Prometheus client for the
# worker_ai_filter_reload_total counter + /metrics scrape endpoint
# (built-in WSGI server via start_http_server; runs alongside asyncio).
prometheus-client>=0.20

# Phase 4b-γ: loreweave_llm + loreweave_extraction SDKs are installed
# via Dockerfile COPY of sdks/python (build context is repo root).
# For host-side pytest:
#   pip install -e ../../sdks/python   (from services/worker-ai)
