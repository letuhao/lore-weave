# RUN-STATE — Writing Studio Tool↔GUI build (plan 30, waves 0–8 + T)

> **This file is the anti-drift anchor mandated by CLAUDE.md → "Long autonomous runs — the GOAL
> COMMITMENT". Context is lossy. Anything living only in the conversation evaporates at compaction.
> This file does not.**
>
> **RE-READ THIS FILE FIRST AFTER EVERY COMPACTION, BEFORE DOING ANYTHING ELSE.**
> Keep its path in the TODO list — the harness re-surfaces todos, so it is the anchor that survives.
>
> - **Started:** 2026-07-13 · **Branch:** `feat/context-budget-law` · **Base HEAD:** `9262ed53e`
> - **Umbrella plan:** `docs/specs/2026-07-01-writing-studio/30_TOOL_GUI_GAP_AUDIT_AND_PLAN.md`
> - **Wave plans:** `docs/plans/2026-07-13-studio-wave-{0,1,2,3,4,5,6,7,8,T}-*.md`
> - **Slices on the board:** **188** across **10 waves** *(W0 20 · W1 20 · W2 14 · W3 15 · W4 17 · W5 19 · W6 22 · W7 17 · W8 21 · WT 23 — every wave carries an explicit CLOSE slice with `/review-impl` in its DoD)*
> - **Panel ids:** **57 → 76** (+19). **Migrations:** 7 additive, 0 destructive.
>
> 🔴 **RECONCILED 2026-07-13 (the executability gate).** The board previously listed **143** and was
> **STALE against the wave plans** — 46 slices existed in the plans and on **no board row**, and the resume
> protocol (§9) walks the **BOARD**. *A slice that is not a board row is a slice that gets silently skipped
> after a compaction.* The additions are the **contract-first slices** (9 waves each gained one — CLAUDE.md's
> *"API contract frozen before frontend flow"* was being violated by every wave), the **homeless-legacy-sub-tab
> panels** (`scene-compose`, `chapter-assemble` @ W6; `cast`, `character-arc`, `canon-growth` @ W8), and the
> **defer rows that were refuted against code and became slices** (I-14). One row was **DELETED**: the old
> `W2-S0` (a Go `my-access` route) — the plan **refutes it**; the grant is already on the wire.

---

## 1 · THE COMMITMENT

> **DONE means:** every slice on the board in §5 is marked **DONE with a pasted EVIDENCE STRING**
> (a test count, a live-smoke one-liner, or a commit sha) — or carries a tracked defer-row ID in the
> PARKED register (§6.2) — **and** each of the 10 waves has closed with `/review-impl` run on its diff
> with every finding FIXED, its live browser smoke green against a **rebuilt** image, and its
> SESSION_HANDOFF update landed **in the same commit** as its code.

**Falsifiable.** A slice is NOT done because it "looks done", because a unit suite is green, or
because a checkbox is ticked. **A checkbox is self-report. Evidence is proof.** Claiming a check
passed without pasting its output does **not** satisfy this commitment.

**The law this run enforces (plan 30 GG-1):** *every backend capability a user owns must have a
human surface.*

---

## 2 · THE INVARIANTS — true for EVERY slice. Self-check against this list when you feel drift.

| # | Invariant | The self-check |
|---|---|---|
| **I-1** | **Provider-gateway (ENFORCED)** | No service imports a provider SDK or calls a provider API. Every LLM/embedding/rerank/STT/TTS call goes through `provider-registry-service`. A **local** backend (lm_studio, ollama, local-rerank) is **not an exception** — it is a **BYOK credential** (`user_models` JOIN `provider_credentials`), never a per-service `*_URL`/`*_MODEL` env var. |
| **I-2** | **No hardcoded model names** | Model names *and pricing* resolve from provider-registry. Pre-commit `scripts/ai-provider-gate.py` will red you. |
| **I-3** | **MCP-first (agent logic)** | Any capability where an LLM decides/reasons over tools is an **MCP tool through `ai-gateway`**, never a bespoke HTTP endpoint driven by a raw prompt. **If the tool doesn't exist, WRITE it** on the owning domain service. Non-agentic pipelines are exempt. |
| **I-4** | **Gateway invariant** | All external traffic through `api-gateway-bff`. (Sole sanctioned exception: `game-server` Colyseus WS — irrelevant to this run.) |
| **I-5** | **Tenancy scope keys** | Every user-customizable row carries `owner_user_id` and/or `book_id`, and **every query filters by it**. Tiers: System (admin-write, read-only to users) → Per-user → Per-book; higher shadows lower by `code`. A `UNIQUE(code)` on a shared table is the smell. A regular user **must not** mutate a System row. Cross-tenant access is **grant-gated**, never implicit. ⚠ **W1-07 exists precisely because a "daily goal" was book-global.** |
| **I-6** | **SET-1..8 (settings boundary)** | "Would two users want different values?" → **yes ⇒ user setting**, not an env flag. A setting must: expose **effective value + source tier** (SET-1); be **CONSUMED, proven by effect** (SET-4 — a stored-but-unread blob is a bug); **enum-validate closed sets** on write; live **server-side**; have **one home / one name**. A spend-causing toggle **fails CLOSED**. |
| **I-7** | **Frontend-Tool Contract (LOCKED)** | **Closed-set arg ⇒ `enum`** (register in `CLOSED_SET_ARGS`). **The resolver NEVER silently no-ops** — return `result.error`. **One name for one concept.** Machine-checked both sides via `contracts/frontend-tools.contract.json`: change a schema ⇒ `WRITE_FRONTEND_CONTRACT=1 pytest` + update the resolver, or a test reds. **Verify by EFFECT** (live browser), not by raw stream. Panel registration is three-way: **py enum == contract enum == OPENABLE_STUDIO_PANELS**, and each wave asserts the **DELTA** (+1), never a literal count. |
| **I-8** | **React MVC** | hooks = controllers (logic, no JSX) · context = services · components = views (render only). **Never conditionally unmount stateful components** — CSS `hidden`, never a ternary. **No `useEffect` for event handling.** Split context by update frequency. Hooks self-contained. ~100 lines/component, ~200/hook. |
| **I-9** | **Test parallelization** | Python suites: `python -m pytest tests -q -n auto --dist loadgroup`. **Any NEW test file touching a real DB/port MUST carry `pytestmark = pytest.mark.xdist_group("pg")`** — or parallel workers interleave and the counts lie. |
| **I-10** | **NEVER `git add -A`** | Other tracks are dirty in this shared checkout (see the git status: Track C eval runs, book-package, work-assistant). **Enumerate files by name.** Also: the index may already carry pre-staged unrelated changes — check `git diff --cached --stat` before every commit. And `git commit -- <path>` commits the **WORKING TREE**, not the index. |
| **I-11** | **`/review-impl` at EVERY wave close** | Non-negotiable, baked into each wave's DoD as a literal step. Every finding it raises is **FIXED before the wave closes**. |
| **I-12** | **Cross-service ⇒ live smoke** | When a slice touches ≥2 services, a green unit suite is **insufficient** evidence. Live-smoke it, or write `LIVE-SMOKE deferred to D-<NAME>-LIVE-SMOKE`. Agent→GUI loops need a **live BROWSER** smoke (raw-stream smoke misses "the FE couldn't execute it"). **Rebuild the image first** — `:5174` is the BAKED nginx prod build; a host `vite dev` SHADOWS it and stale images give false greens. |
| **I-13** | **Defer ≠ silent drop** | Every deferral is a row: ID · wave/slice of origin · what · which of CLAUDE.md's 5 gates · target wave/trigger. Fix-now is the default; a defer row must EARN its place. |
| **I-14** | **Missing infrastructure is NOT "blocked"** | A route that does not exist is a route you **WRITE**. Only a dependency genuinely external to this repo clears gate #4. Saying "blocked" when you mean "I'd have to build it" is the lazy tell. |

---

## 3 · THE SEALED DECISIONS — plan 30 §0, PO-1..4

**Never re-litigate a sealed decision from memory. RE-READ IT.**
Source: `docs/specs/2026-07-01-writing-studio/30_TOOL_GUI_GAP_AUDIT_AND_PLAN.md` §0 (SEALED 2026-07-12).

- **PO-1 — AMEND spec 28's AN-12.** The "No new GUI surface" clause is **lifted** for
  `composition_diagnostics` / `composition_package_tree` / `composition_find_references`.
  **Wave 7 proceeds.** AN-12's architecture is honoured exactly: wire the **existing**
  `StudioBottomPanel` Issues tab (a stub string since day one), and make `find_references` a
  **right-click lens on an entity badge** — **no new dock panel**, so the DOCK-2/DOCK-8 fork AN-12
  was protecting against still cannot happen. *Rationale on record:* AN-12's premise was "the human
  equivalents already exist"; the audit found **~2.5 of 5** diagnostic sources have **no** human
  surface and **none** is ranked — the premise was **false**. The amendment is written INTO
  `28_agent_native_studio.md` (do not fork it).

- **PO-2 — G-WORKFLOWS is DROPPED from this plan; Track C owns it** (its P-5 claims "workflow rack,
  binding UI"). **Wave 8 loses 8c** → Wave 8 = KG write holes + world maps only. The gap stays in the
  register as **OWNED-BY-TRACK-C**, not as a hole. ⚠ The defect is real and Track C's to close:
  `registry_propose_workflow` says the user approves "in the UI" and there is no UI — an agent calling
  it today writes a row **no human can ever approve**.

- **PO-3 — RETIRE `ui_show_panel`**, folding it into `ui_open_studio_panel` (one name for one
  concept). **X-5 is a retirement, not an enum-add.** ⚠ Cross-surface, not a delete: `ui_show_panel`
  is used **outside** the studio too, so retirement must (a) keep non-studio call sites working
  (migrate to `ui_open_studio_panel` + the studio interceptor, or give them an explicit non-studio
  path) and (b) land with `py enum == contract enum == openable` green. **`ui_watch_job` is separately
  fixed:** add it to `STUDIO_UI_TOOLS` + the interceptor → open `job-detail`. Today it route-navigates
  and **tears down the dock**.

- **PO-4 — ALL detail specs (31–38) + ALL HTML drafts were written FIRST.** No design checkpoints
  remain. The build proceeds as **one piece, with full information**. This is why §7's policy holds:
  *anything left vague becomes a stall or a guess at 3am* — so it wasn't left vague; go read the wave plan.

---

## 4 · THE POLICY THE PO SET FOR THIS RUN (binding — quote it in every plan you write)

1. **This plan is written ONCE, in full, at BUILD DETAIL.** After the QC gate, implementation proceeds
   **autonomously with no further design checkpoints.** So anything left vague becomes a stall or a
   guess at 3am. A slice that says "wire the panel" is a FAILURE; a slice says WHICH FILE, WHAT CHANGE,
   WHICH TEST.
2. **`/review-impl` runs at the completion of EVERY wave**, and any bug it finds is fixed before the
   wave closes. It is a literal step in each wave's Definition of Done.
3. **DEFERRAL POLICY — "blocked ≠ stopped".** When the build hits a blocker: write a tracked defer row
   and **KEEP GOING**. Do **not** stop, do **not** ask. A blocker is a **DEFER by default**. Stop and
   ask ONLY for a CRITICAL blocker (§8).
4. **Every defer row carries:** ID, wave/slice of origin, what, the gate reason (CLAUDE.md's 5 gates),
   target wave/trigger. **A defer row is never a silent drop.**
5. **CLAUDE.md's anti-laziness rule is in force:** *"missing infrastructure is NOT blocked — it is
   unbuilt work to implement."* A route that does not exist is a route you WRITE. Do not defer it as
   "blocked" unless the dependency is genuinely external to this repo.

---

## 5 · THE SLICE BOARD — 188 slices · 10 waves

**Status ∈ {TODO, DOING, DONE}. `DONE` REQUIRES AN EVIDENCE STRING** — a pasted test count, a
live-smoke one-liner, or a commit sha. Not a checkmark. A checkbox is self-report; evidence is proof.
The `EVIDENCE` column starts as the slice's Definition of Done (what will prove it); on completion,
**overwrite it with what actually happened.**

### Wave 0 — Foundations / cross-cutting (20 slices) · `docs/plans/2026-07-13-studio-wave-0-foundations.md`

> **Board order == the plan's §11 build sequence == a valid topological order.** The only edges that bind:
> `W0-C1` ← `W0-BE1`+`W0-BE2` (a contract describes what exists) · `W0-S7` ← `W0-BE1`+`W0-C1`+`W0-S6` ·
> `W0-S5` → `W0-S5b` → `W0-S5c` (**contract-regen serialization — NEVER two concurrent regens**) ·
> `W0-S11` **LAST**. Everything else is independent: a stall in one **never** blocks the wave — park it and
> take the next.

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 0 | **W0-BE1** [BE] 🔴 BE-7c **IN FULL** — a Work-LESS job, **CREATABLE then readable**: the DDL (`DROP NOT NULL` on `project_id`/`book_id` + the both-or-neither shape CHECK + the owner-unbound index) · `create_unbound()` · the `GenerationJob` model · `_enqueue_motif_job`'s branch (**delete the synthetic `uuid4()`**) · `GET /v1/composition/motif-jobs/{job_id}` · the MCP arg change. **A READ ROUTE ALONE FIXES NOTHING** — at HEAD `/actions/confirm` **500s** (`ReferenceViolationError`) and the job row is **NEVER CREATED** (C-1). Canonical build detail = Wave 3's `3a-1`; build from it, do not fork it. | TODO | *DoD:* `pytest -q tests/integration/db/test_motif_job_read.py -n auto --dist loadgroup` → **8 passed**, with `test_an_unbound_job_can_be_CREATED_at_all` and `test_the_confirm_path_no_longer_500s` **observed RED before / GREEN after (both runs pasted)**. **Fixtures go through `create_unbound()` — NEVER a raw INSERT** (`fixtures-can-seed-a-field-the-writer-never-sets`). 🔴 **`W0-S7` HARD-DEPENDS ON THIS — its live smoke cannot pass until this lands.** |
| 0 | **W0-BE2** [BE] 🆕 BE-11 — `canon_rule` **restore** (the undo the DELETE already promises): `CanonRulesRepo.restore()` + `list_all(include_archived)` + `POST /canon-rules/{id}/restore` + `GET …?include_archived`. Pulled into Wave 0 so `W0-C1` can freeze both routes in one contract pass. Plan §3.4 | TODO | *DoD:* composition-service `pytest tests -q -n auto --dist loadgroup` → `<baseline>+N` passed incl. `test_canon_rule_restore_roundtrip` + `test_restore_not_archived_404`. ⚠ **If this lands here, `W1-02` is a SKIP** — do not double-implement (pre-flight: `grep -n 'def restore' services/composition-service/app/db/repositories/canon_rules.py`) |
| 0 | **W0-C1** [CONTRACT] 🔴 🆕 **THE CONTRACT SLICE** — CLAUDE.md law: *"contract-first — API contract frozen before frontend flow."* Both new routes into `contracts/api/composition/v1/openapi.yaml`: `GET /motif-jobs/{job_id}` (after `/jobs/{job_id}`) · `POST /canon-rules/{id}/restore` (after `/canon-rules/{rule_id}`'s delete) · **`GenerationJob.project_id`/`.book_id` become `nullable: true`** (amend the schema, do NOT fork it). 🔴 **dependsOn `W0-BE1` + `W0-BE2`** — a contract describes what exists. Plan §3.5 | TODO | *DoD:* the 2 paths + the amended `GenerationJob` schema are in the openapi.yaml; `redocly lint` clean; the contract-parity test passes. **BEFORE `W0-S7` (the first FE consumer).** |
| 0 | **W0-S1** [FE] X-2 — add `quality` to `CATEGORY_ORDER` + the category∈CATEGORY_ORDER membership assertion | TODO | *DoD:* `npx vitest run …/panelCatalogContract.test.ts …/useStudioCommands.test.ts` → all passed; palette no longer sorts quality above editor |
| 0 | **W0-S2** [FE] X-3 — `guideBodyKey` must RESOLVE (not just be declared): the effect assertion + backfill 5 blank bodies × 18 locales | TODO | *DoD:* `npx vitest run src/features/studio/panels/__tests__/` → all passed; 5 guide bodies backfilled × 18 locales; user-guide panel renders the 4 quality bodies live (blank at HEAD) |
| 0 | **W0-S3** [FE] X-1 — `AddModelCta` DOCK-7: branch on `useOptionalStudioHost()` at the SHARED component, never at the ~8 call sites | TODO | *DoD:* `npx vitest run src/components/shared/__tests__/AddModelCta.test.tsx` → 2 new passed; **LIVE BROWSER:** clicked "Add a model" inside a dock panel — the dock is still mounted and settings opened on Providers |
| 0 | **W0-S4** [FE] X-4 — Lane-B registry: delete the FALSE comment, ship `agentModeEffects.ts` (the one stale domain), + 2 mechanical guards (no-metachar-string, no-double-fire) | TODO | *DoD:* `npx vitest run src/features/studio/agent/` → all passed incl. 2 new guards; the false comment at `useStudioEffectReconciler.ts:10` is deleted; **LIVE BROWSER:** agent `composition_authoring_run_pause` updates Mission Control with no manual refetch |
| 0 | **W0-S5** [CONTRACT] X-5 / **PO-3** — RETIRE `ui_show_panel` (zero working consumers); intercept `ui_watch_job` → job-detail dock panel instead of tearing down the dock | TODO | *DoD:* the **runtime** references are gone — `grep -rnE "['\"]ui_show_panel['\"]" frontend/src services/ --include=*.ts --include=*.tsx --include=*.py \| grep -v __tests__ \| grep -v '/tests/'` → **ZERO** lines. ⚠ **NOT a bare repo-wide grep** — explanatory comments naming the retired tool, and the rewritten negative tests (`isUiTool('ui_show_panel') === false`), are CORRECT and STAY (`hygiene-grep-literal-token-in-comment-false-positive`); chat-service `pytest test_frontend_tools*.py test_tool_discovery.py test_agent_surface.py` → all passed; contract regenerated (panel_id enum STILL 57); **LIVE BROWSER:** agent `ui_watch_job` opened a job-detail tab and the dock survived |
| 0 | **W0-S5b** [CONTRACT] 🆕 **X-12** — `ui_open_studio_panel` gains an **OPTIONAL `params` object** (the carrier every deep-link slice downstream needs: `focusRuleId`, `focusNodeId`, `entityId`, `mapId`, `search`…). 🔴 **dependsOn `W0-S5`** — same file, same contract REGEN. **NEVER two concurrent regens** (00B:203). Plan §`W0-S5b` | TODO | *DoD:* chat-service `pytest test_frontend_tools*.py -q` → all passed; the contract JSON **REGENERATED** (`WRITE_FRONTEND_CONTRACT=1`), panel_id enum **STILL 57**; the FE resolver reads `params` and **never silently no-ops** on an unknown key (`result.error`); `params` is **not** in `CLOSED_SET_ARGS` (it is an open object, not a closed set) |
| 0 | **W0-S5c** [CONTRACT] 🆕 **X-13** — **DELETE the 2 dead fields** (`consumer_capabilities` / `contributeContext`) **+ fix the SILENT SUCCESS they pretended to guard**. ⚖ **This is the E-2 / `D-W7-X13-REHOME` escalation, RESOLVED BY BUILDING IT HERE** — Wave 0 is *earlier* than plan 30 §8.2's "Wave 7 at the latest" deadline, so the deadline **tightens** and nothing lapses. `ConsumerCapabilities` is an **empty model (`pass`, zero fields)** that `messages.py` never forwards ⇒ **no consumer AND no producer** ⇒ deleting it costs nothing. *(Also dead: `StudioContext.active_panel_ids`.)* 🔴 **dependsOn `W0-S5b`** (regen serialization). Plan §`W0-S5c` | TODO | *DoD:* the 4 dead symbols are **GONE** (`grep -rn 'consumer_capabilities\|contributeContext\|active_panel_ids' services/ frontend/src --include=*.py --include=*.ts --include=*.tsx \| grep -v __tests__` → **ZERO**); the silent-success path now returns `result.error`; contract REGENERATED (enum still 57); chat-service + frontend suites green. **Then: amend plan 30 §8.2's X-13 bullet + mark spec 37 OQ-5 CLOSED — E-2 is DISCHARGED, not deferred.** |
| 0 | **W0-S6** [BE] X-7 — the `gather_motifs` packer lens + a BA12-style real-DB EFFECT test, wired at **ALL FOUR** `pack()` call sites — **THE HARD GATE ON WAVE 3** | TODO | *DoD:* `grep -rn 'motif' services/composition-service/app/packer/*.py` → NON-EMPTY; `pytest tests/integration/db/test_pack_motifs_wired.py -q -n auto --dist loadgroup` → 6 passed incl. `test_the_prompt_CHANGES_when_the_binding_changes` + `test_every_pack_call_site_passes_the_motif_repos` |
| 0 | **W0-S7** [FE] Kill all 4 live 404s — 2 invented `/actions/*` URLs → the generic preview/confirm spine; **DELETE** `regenerateToBeat`; re-point the mine/拆文 poll at `/motif-jobs/{id}`. 🔴 **dependsOn `W0-BE1` (the WRITER, not just the read route) + `W0-S6`** | TODO | *DoD:* `npx vitest run src/features/composition/motif` → all passed; **LIVE cross-service smoke:** clicked ⛏ Mine, confirmed the cost, the poll returned 200 and the panel reached a terminal state. ⚠ **This smoke is IMPOSSIBLE until `W0-BE1`'s `create_unbound()` lands** — at HEAD `/actions/confirm` **500s** and no job row is ever written, so there is nothing to poll (C-1). If the confirm still 500s, **`W0-BE1` is incomplete — go finish it**; do NOT "fix" it by minting a phantom `composition_work` row. |
| 0 | **W0-S8** [DOC] X-6 — write spec 28's **AN-12 `resource_ref` addressing contract** (a HARD PREREQUISITE for Waves 2/3/7 by spec 28's own words) | TODO | *DoD:* `28_agent_native_studio.md` gains the AN-12 `resource_ref` section naming all 5 kinds + the 6 rules; OQ-8 marked CLOSED → §AN-12; no code changed |
| 0 | **W0-S9** [DOC] X-9 — close the 2 un-audited MCP sweeps (provider-registry 14 tools, catalog 2) + adjudicate the unprefixed `web_search` | TODO | *DoD:* plan 30 §3.4's "UNVERIFIED" block REPLACED by a closed 16-tool table; §3.1's total corrected from 173; `web_search` recorded as a conscious won't-fix with the `mcp_web_search_tool.go:9-12` supersession evidence |
| 0 | **W0-S10** [DOC] X-8 — doc hygiene: renumber 14a/14b + 15a/15b with zero dead links; fix 29's stale H1, 00_OVERVIEW's index, 00C Q-2, spec 20's header | TODO | *DoD:* `git mv` ×4; `grep -rn '14_kg_panels\|14_utility_panels\|15_chapter_browser\|15_wiki_panels' docs/` → 0 hits; 29's H1 fixed; 00_OVERVIEW index refreshed; 00C Q-2 cleared; spec 20 header un-staled |
| 0 | **W0-S12** [FE] 🆕 **X-GG4-GATE** — the legacy-editor retirement gate as a **MACHINE CHECK** (today it is an 18-line prose banner; *nothing reds*) 🔴 **+ the `LEGACY_SUBTAB_HOME` LEDGER**: **7 legacy `CompositionPanel` sub-tabs have no home in any wave plan**, and the naive map **FALSE-HOMES two of them** ⇒ the gate would go **GREEN on a feature being deleted**. Wave 0 owns the gate test ⇒ Wave 0 owns the ledger it reads. **This ledger is what Waves 6 + 8 consume** (`scene-compose`/`chapter-assemble` @ W6-M6/M7; `cast`/`character-arc`/`canon-growth` @ W8-16/17/18; `worldmap` = a reviewed **won't-port**). Plan §`W0-S12` | TODO | *DoD:* `npx vitest run …/legacyEditorRetirementGate.test.ts` → passed (clones `dockablePanelHygiene.test.ts`'s `readFileSync`-over-source pattern — **~40 lines, not new infrastructure**); the ledger names, for **all 25** sub-tabs, the wave + slice that homes it or the reviewed retirement reason; **`place-graph` appears NOWHERE as a panel id** (no wave builds it) |
| 0 | **W0-S13** [FE] 🆕 **X-DIRTY-CLOSE** — closing a dirty dock tab **SILENTLY DISCARDS THE EDIT** (live at HEAD). 🔴 **Sequence BEFORE any new editing panel lands** — Waves 1–8 add 19 panels, several of them editors, and every one inherits this. Plan §`W0-S13` | TODO | *DoD:* frontend `<N>` passed incl. "closing a dirty tab prompts and **does not discard**"; **LIVE BROWSER:** typed into a panel, closed the tab, got the guard — the edit survived (at HEAD it vanishes) |
| 0 | **W0-S14** [FE] 🆕 **X-NEW-CHAPTER** — the manuscript **`+` button is DEAD** (renders, clicks, does nothing). A silent no-op on a primary affordance (`silent-success-is-a-bug-not-environment`). Plan §`W0-S14` | TODO | *DoD:* frontend `<N>` passed incl. "clicking + calls createChapter and the new chapter appears"; **LIVE BROWSER:** clicked `+`, a chapter was created and rendered |
| 0 | **W0-S11** [BE] X-10 / AN-C2 — the discovery scent in `book_context_note` (**SEQUENCE LAST**; re-verify the Track-C gate, DEFER not stop if dirty) | TODO | *DoD:* `pytest services/chat-service/tests -q -n auto --dist loadgroup` → ≥baseline passed, 0 failed; `grep 'composition_diagnostics' stream_service.py` → 1 hit; **LIVE LLM smoke** (local lm_studio, $0): asked "what's wrong with my book?" and the agent called `composition_diagnostics` instead of foraging per-domain list tools |
| 0 | **W0-CLOSE** [DOC] 🔴 **WAVE CLOSE** — run the wave-0 plan's DoD checklist (`…-wave-0-foundations.md` § "`/review-impl` — MANDATORY"): full suites green · **live browser smoke** on a REBUILT image · **`/review-impl` with every finding FIXED** · SESSION_HANDOFF · commit by enumerated path | TODO | *DoD:* `/review-impl studio-wave-0` run on the wave diff; `<K>` findings, **ALL FIXED** (listed); suites re-green AFTER the fixes (composition `<N>`, chat-service `<N>`, frontend `<N>`); the drift-locks green with the panel enum **STILL 57** (Wave 0 adds no panel); SESSION_HANDOFF ▶ NEXT rewritten **in the same commit as the code** |

### Wave 1 — Quality completion (20 slices) · `docs/plans/2026-07-13-studio-wave-1-quality.md`

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 1 | **W1-00** [FE] GATE X-2 + X-3 — **CONDITIONAL**, only if Wave 0 has not landed | TODO | *DoD:* `panelCatalogContract.test.ts`: `<N>` passed incl. "every palette-openable panel category is a member of CATEGORY_ORDER" — observed **RED before** the fix (both runs pasted) |
| 1 | **W1-01** [FE] GATE X-1 — AddModelCta DOCK-7 (**CONDITIONAL**; hard gate on M3) | TODO | *DoD:* `AddModelCta.test.tsx`: `<N>` passed incl. "inside a StudioHost, clicking calls host.openPanel and does NOT navigate" |
| 1 | **W1-02** [BE] BE-11a/b — `CanonRulesRepo.restore()` + `list_all(include_archived)` + `POST /canon-rules/{id}/restore` + `GET …?include_archived`. ⚠ **CONDITIONAL — `W0-BE2` builds this.** Pre-flight: `grep -n 'def restore' …/repositories/canon_rules.py` non-empty ⇒ **SKIP**, paste the suite output, go to `W1-CONTRACT`. **Do NOT fork a second `restore()`.** | TODO | *DoD:* composition-service `<N>` passed (`-n auto --dist loadgroup`) incl. `test_canon_rule_restore_roundtrip` + `test_restore_not_archived_404` |
| 1 | **W1-CONTRACT** [CONTRACT] 🔴 🆕 **FREEZE THE API CONTRACT** — CLAUDE.md law: *"contract-first: API contract frozen before frontend flow."* Wave 1's routes (canon restore + `include_archived` **if `W0-C1` did not already carry them**, `PUT /progress/goal`, the `by_chapter` widening, the correction-capture surface) enter `contracts/api/composition/v1/openapi.yaml`. 🔴 **RUNS BEFORE `W1-04`** (the first FE data-layer slice). Plan §7 `W1-CONTRACT` | TODO | *DoD:* every route Wave 1's FE calls is in the openapi.yaml with request + response + error codes; `redocly lint` clean; contract-parity test green. **A contract that omits the routes the frontend calls is not a contract.** |
| 1 | **W1-03** [BE] BE-11c — MCP `composition_canon_rule_restore` (Tier A) + the restore `undo_hint` on `composition_canon_rule_delete` (**MCP-first parity: never ship the human half alone**) | TODO | *DoD:* composition-service `<N>` passed incl. `test_canon_rule_delete_now_returns_restore_undo_hint` + `test_canon_rule_restore_is_registered_with_both_args` (FastMCP 3-schema-source guard); `tools/list` fragment pasted |
| 1 | **W1-04** [FE] FE data layer — `CanonRule.kind` + `.is_archived`; `restoreCanonRule` + `listCanonRules(includeArchived)`; `useCanonRules` restore + **SERIALIZED OCC writes**; `CanonRuleForm` kind; NEW `CanonRuleRow` with the 412 banner that **KEEPS the user's draft** | TODO | *DoD:* frontend `<N>` passed incl. "a 412 renders the conflict banner with current AND KEEPS the user's draft" + "two rapid active-toggles both land (writes serialized)"; the 6 legacy `CanonRulesPanel` tests green **unedited** |
| 1 | **W1-05** [FS] Panel `quality-canon-rules` + GG-8 registration (enum N→N+1) + the two-way `focusRuleId` deep-link + QC-9 Dismiss | TODO | *DoD:* enum N→N+1; **py enum == contract enum == openable** (three-way green); frontend `<N>` passed; chat-service contract `<N>` passed |
| 1 | **W1-06** [FE] Lane-B X-4 — CREATE `handlers/compositionEffects.ts` (RegExp `^composition_canon_rule_` + `^composition_record_correction$`), register it, DELETE the now-false comment at `useStudioEffectReconciler.ts:8-9` | TODO | *DoD:* frontend `<N>` passed incl. the RegExp-not-string assertion + the WIRING test (`matchEffectHandlers('composition_canon_rule_create').length === 1` after the reconciler mounts) |
| 1 | **W1-07** [BE] BE-P2 (`composition_progress_goal` **per-user** table + `PUT /progress/goal` + read-through fallback + `daily_goal_source`) + BE-P1 (`by_chapter`) — **THE TENANCY FIX (I-5)** | TODO | *DoD:* composition-service `<N>` passed (`-n auto --dist loadgroup`) with **`test_two_users_on_one_book_have_independent_goals` NAMED** in the output (real SQL — a mock would encode the bug) |
| 1 | **W1-08** [FS] Panel `progress` — REWRITE `useSetDailyGoal` (DELETE the `patchWork` full-blob caller), props + **SET-1 source tier** + `by_chapter` + cold start, register (N+1→N+2) | TODO | *DoD:* enum N+1→N+2 three-way green; frontend `<N>` passed incl. "setting a goal calls setDailyGoal and **NEVER** patchWork" + the SET-4 goal-bar-effect test |
| 1 | **W1-09** [FE] QC-4 hoist chokepoint — `applyHealedDocument` on `ManuscriptUnitApi`: 3 stale guards + the **F18 fix** (`setContent` SUPPRESSES `onUpdate`, so it MUST call `setBody` itself or the heal silently vanishes on ⌘S) | TODO | *DoD:* frontend `<N>` passed incl. "an applied heal marks the hoist DIRTY (setContent alone does not — this is the bug)" + the 3 stale-reason tests + no-editor |
| 1 | **W1-10** [FE] QC-8 — `usePolishProposals` storage moves `useState` → the react-query cache `['composition','self-heal',projectId,chapterId]` (staleTime Infinity, no queryFn). **RETURN SHAPE UNCHANGED** | TODO | *DoD:* frontend `<N>` passed incl. "a second hook instance on the SAME chapter reads from the cache" + "a DIFFERENT chapter reads [] (no false badge)"; `PolishPanel.test.tsx` green and **UNEDITED** (`git diff --stat` proves it) |
| 1 | **W1-11** [FS] Panel `quality-heal` — chapter picker (QC-10 verbatim) + ModelPicker + PolishPanel with the widened `onApply(healedText, draftVersion)` + the 3 stale banners that **DISABLE Apply**; register (N+2→N+3) | TODO | *DoD:* frontend `<N>` passed, with the three stale tests (chapter / version / dirty) EACH asserting the banner renders, Apply is DISABLED, and the document is unchanged; enum N+2→N+3 three-way green |
| 1 | **W1-12** [FE] D-QUALITY-CRITIC-HEAL-LINK — `QualityCriticPanel` reads `polishKey` from the shared cache and passes `proposals` to `QualityReportSection`, so the violation-has-fix badge can finally fire | TODO | *DoD:* frontend `<N>` passed incl. "the violation-has-fix badge APPEARS after quality-heal ran for the SAME chapter" + "…does NOT appear for a DIFFERENT chapter" |
| 1 | **W1-13** [BE] BE-9c + BE-9c′ — **THE DENOMINATOR**: `CORRECTABLE_OPERATIONS=('draft_scene','draft_chapter','stitch_chapter')` **ADDED to** (never replacing) the NOT-selection_edit predicate; close `GenerateBody`/`GenerateChapterBody.operation` to a `Literal`. **SHIPS FIRST, ALONE.** | TODO | *DoD:* composition-service `<N>` passed with `test_correction_stats_ignores_non_draft_operations`, `test_correction_stats_still_excludes_selection_edits_after_the_allowlist`, `test_correction_stats_draft_scene_cowrite_lands_in_the_stream_column` ALL NAMED |
| 1 | **W1-14** [BE] BE-9a — `ALTER TABLE authoring_run_units ADD COLUMN IF NOT EXISTS job_id UUID` (nullable, **NEVER backfill a guess**) + `DraftOutcome.job_id` + the seam returns the id it already reads + `mark_drafted` persists it + the Run Report says when it is NULL | TODO | *DoD:* composition-service `<N>` passed incl. `test_draft_outcome_job_id_is_persisted_by_the_driver` + `test_mark_drafted_coalesces_and_never_nulls_an_existing_job_id`; migrate test asserts `job_id` is nullable |
| 1 | **W1-15** [BE] BE-9b — **THE CAPTURE SEAM**: `CorrectionSink` + `ChapterText` protocols; `reject_unit` → kind=`reject`; `accept_unit` AFTER-EDIT → kind=`edit` (accept-as-is records **NOTHING**, H2); fire-and-forget; `revert_all` captures nothing; + MCP `composition_record_correction` | TODO | *DoD:* composition-service `<N>` passed with `test_accept_as_is_records_NOTHING`, `test_a_capture_failure_NEVER_fails_the_reject`, `test_reject_writes_a_row_AND_emits_GENERATION_CORRECTED_in_the_SAME_transaction` NAMED; **live smoke:** rejected a real authoring-run unit → book-service restored the revision, a `generation_correction` row landed, the outbox relayed `GENERATION_CORRECTED` |
| 1 | **W1-16** [FS] Panel `quality-corrections` — **EXTRACT** `CorrectionStatsTable` out of `QualityPanel` (never mount QualityPanel: it would put a **paid LLM action on screen twice**) + the BE-9c denominator note; register (N+3→N+4) | TODO | *DoD:* enum N+3→N+4 (61 if the baseline was 57); three-way green; frontend `<N>` passed incl. "does NOT render BookPromiseCoverageSection" (the F-Q11 no-paid-button-twice lock); `QualityPanel.test.tsx` green and unedited |
| 1 | **W1-17** [TEST] **THE LIVE BROWSER SMOKE** — `frontend/tests/e2e/specs/studio-quality.spec.ts` against a **REBUILT** image: 4 panels mount via a real agent `ui_open_studio_panel` turn; canon create→archive→restore; the deep-link chain; the heal loop Apply→⌘S→reload→violation-has-fix; the stale-dirty guard; the DOCK-7 regression; Lane-B live sync | TODO | *DoD:* **live smoke:** `studio-quality.spec.ts` green on a rebuilt image — all 4 dock tabs mounted from real agent turns; canon restore round-tripped; Apply→⌘S→reload persisted the healed text and `[data-testid=violation-has-fix]` rendered; the stale-dirty banner blocked Apply and the doc was unchanged; the Add-a-model CTA did NOT unmount the dock |
| 1 | **W1-18** [DOC] Wave close — `/review-impl` (every bug FIXED before close) + SESSION_HANDOFF (clear D-QUALITY-CRITIC-HEAL-LINK + 00C Q-3(a)(b), file the 9 defer rows) + the wave COMMIT (**files enumerated, never `git add -A`**) | TODO | *DoD:* `/review-impl` run on the wave diff; `<K>` findings, ALL FIXED (listed); full suites re-green after the fixes: composition `<N>`, chat-service `<N>`, frontend `<N>`; SESSION_HANDOFF updated; summary presented, human approved |

### Wave 2 — Arc inspector (14 slices) · `docs/plans/2026-07-13-studio-wave-2-arc-inspector.md`

> 🔴 **THE OLD `W2-S0` (a new Go `GET /v1/books/{book_id}/my-access`) IS DELETED — DO NOT BUILD IT.**
> The plan's §0.1 **LA-1** refutes it against code: `GET /v1/books/{book_id}` **already computes and returns**
> the caller's own `access_level ∈ owner|manage|edit|view|none` (`book-service/internal/api/server.go:957`
> computes it, `:987` emits it; the list route does the same at `:857`/`:885`) — and
> `grep -rn "access_level" frontend/src` returns **ZERO hits**. **The grant is on the wire and the FE throws
> it away** (`rest-write-mirror-drops-fields`, in read form). The fix is `useBookGrant.ts` reading it off the
> `['book', bookId]` cache the studio **already holds** — a cache hit, not a request. 🔴 **THERE IS NO GO
> WORK AND NO NEW ROUTE IN THIS WAVE.** Pre-flight **G8** checks this before you type.

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 2 | **W2-S0** [CONTRACT] 🔴 🆕 **CONTRACT-FIRST — the eight arc routes enter `contracts/api/composition/v1/openapi.yaml`.** **Not one `/arcs` path is in the contract today**, and this wave changes 5 route contracts and gives 8 of them their first FE consumer. 🔴 **THIS SLICE COMES FIRST. No FE slice may precede it.** Plan §3 `W2-S0` | TODO | *DoD:* the 8 `/arcs` paths are in the openapi.yaml with request + response + error codes (incl. BE-A2's **428**, BE-A3's nullable `structure_node_id`); `redocly lint` clean; contract-parity test green |
| 2 | **W2-S1** [BE] BE-A1 — span shape+unit fixed at BOTH detail doors (REST `arc.py:455` + MCP `server.py:4255`) via `derived_blocks()`; `span()` itself UNTOUCHED (the packer needs its raw strided keys) + an anti-cleanup pin test | TODO | *DoD:* `pytest tests -q -n auto --dist loadgroup` → `<baseline>+4` passed; AND the pasted output of `pytest tests/integration/db/test_structure_repo.py -q -k raw_strided` → 1 passed (**NOT skipped** — `TEST_COMPOSITION_DB_URL` set) |
| 2 | **W2-S1b** [BE] 🆕 the **AI-2 PROVENANCE SIDECAR** + the **OQ-3 archive BLAST RADIUS** — an archive must state what it takes with it (`D-ARC-DELETE-BLAST-COUNT`'s human half). Plan §3 `W2-S1b` | TODO | *DoD:* `pytest tests -q -n auto --dist loadgroup` → `<baseline>+N` passed; the archive response carries the subtree count the FE renders as the blast radius (**not a silent OUT-5**) |
| 2 | **W2-S2** [BE] BE-A2 — `PATCH /arcs/{node_id}`: `If-Match` becomes **REQUIRED**, absent ⇒ **428** (a blind write also never bumped version) | TODO | *DoD:* `pytest tests -q -n auto --dist loadgroup` → `<baseline>+8` passed; `test_patch_arc_without_if_match_is_428` asserts `structures.update.await_count == 0` |
| 2 | **W2-S3** [BE] BE-A3 — **UNASSIGN**: `structure_node_id` nullable at repo + REST + MCP (there was no unassign at any layer, yet `?unassigned=true` is already READ) | TODO | *DoD:* `pytest tests -q -n auto --dist loadgroup` → `<baseline>+12` passed; AND `pytest -k unassign` integration → 2 passed (**not skipped**), incl. the real-SQL proof that `?unassigned=true`'s predicate (`structure_node_id IS NULL`) now sees what the writer produced |
| 2 | **W2-S3b** [BE] 🆕 **AI-3 + OQ-2 — THE KEY INVARIANT**, enforced at the doors **AND in the merge**. `tracks`/`roster` are free-form blobs at both doors and **the server accepts a keyless entry that corrupts the cascade** (`D-ARC-TRACKS-ROSTER-SCHEMA`'s enforceable half). ⚠ The read model stays **permissive** (a legacy bad row must still round-trip); the **WRITE doors** tighten. Plan §3 `W2-S3b` | TODO | *DoD:* `pytest tests -q -n auto --dist loadgroup` → `<baseline>+N` passed; a keyless `tracks`/`roster` entry is **422 at the write door**; a legacy bad row still **reads** clean; the migration audit is **0 non-empty rows** (pasted) |
| 2 | **W2-S4** [CONTRACT] Panel skeleton + the FULL GG-8 registration (catalog · en+17 i18n · frontend_tools enum+gloss · contract REGEN · the arc bus slice) — one commit, the drift-locks | TODO | *DoD:* the 4 drift-locks green (panelCatalogContract · UserGuidePanel · useStudioCommands · frontendToolContract + chat-service `test_frontend_tools_contract`) with **py enum == contract enum == OPENABLE_STUDIO_PANELS** and count == pre-flight-G4 baseline **+1** (assert the DELTA, never a literal) |
| 2 | **W2-S4b** [CONTRACT] 🆕 **`resource_ref` — the agent's deep-link leg (X-6 / AN-12)**. Consumes `W0-S8`'s addressing contract + `W0-S5b`'s `params`. **Without it the arc deep-link has no address the agent can name.** Plan §3 `W2-S4b` | TODO | *DoD:* the arc `resource_ref` kind resolves both ways (agent → panel, panel → agent) per spec 28 §AN-12.1's 6 rules; frontend `<N>` passed; contract REGENERATED. *(If `W0-S8` did not land ⇒ open `D-MOTIF-DEEPLINK-NO-RESOURCE-REF`'s arc twin and **CONTINUE** — the panel ships, the deep-link waits.)* |
| 2 | **W2-S5** [FE] The panel READ-ONLY — widen `ArcListNode` (the type that lies), 5 new fetches in `plan-hub/api.ts`, 🔴 **`useBookGrant` reading `access_level` OFF THE EXISTING `['book', bookId]` CACHE** (LA-1 — **not** a new route, not a new fetch), the arcCascade own-vs-inherited split, all 6 sections + every state in §3.4 | TODO | *DoD:* `npx vitest run …/ArcInspector` → N passed incl. "an ARCHIVED arc renders — not 0" AND its inverse "a LIVE arc with chapter_count 0 renders 0, not —" (**the falsiness trap, both directions**), and "VIEW-only unmounts every control"; AND `git diff --stat services/book-service` is **EMPTY** (zero Go changed — the LA-1 lock) |
| 2 | **W2-S6** [FE] The WRITES — create (kind is a closed set of TWO, no `sub_arc`) · patch with a **SERIALIZED OCC chain** · 412 seed-and-say · archive with a client-derived blast radius · restore · AI-2 Override · AI-3 key validation | TODO | *DoD:* `npx vitest run …` → N passed with the pasted line for "two rapid edits SERIALIZE — the second sends If-Match: 8, not 7, and no 412" and "the retry after a 412 SUCCEEDS" |
| 2 | **W2-S7** [FE] Membership — the Chapters section: assign from the unassigned pool + REMOVE (`assignChapters` with a NULL `structure_node_id` — the BE-A3 consumer), virtualized >200, warn-only gap rows | TODO | *DoD:* `npx vitest run … ArcInspectorChapters` → N passed incl. "remove calls assignChapters with a NULL structure_node_id" |
| 2 | **W2-S8** [FE] `arcEffects.ts` — the Lane-B effect handler (X-4, **MANDATORY**). ONE home for `composition_arc_*` (Wave 4 EXTENDS this file's body; a second registration would **DOUBLE-FIRE**, not shadow) | TODO | *DoD:* `npx vitest run src/features/studio/agent/handlers/__tests__` → N passed incl. `matchEffectHandlers('composition_arc_update')` `toHaveLength(1)` (the double-fire guard) + the `{ok,result}` JSON-STRING envelope unwrap test |
| 2 | **W2-S9** [FE] The PlanDrawer embed (24-H3.1) — DELETE `ArcFacets`/`rosterKeysOf`/the gap note + the wrong-type cast, thread `bookId` into `DrawerBody`, mount the SAME `ArcInspectorBody` (DOCK-2), publish the arc bus slice from plan-hub | TODO | *DoD:* `npx vitest run src/features/plan-hub` → N passed; AND the pasted output of `grep -rn "plan-drawer-arc-gap" frontend/src` showing **NOTHING** |
| 2 | **W2-S10** [TEST] Wave close — LIVE BROWSER SMOKE (9 steps, rebuilt image, Playwright via evaluate+data-testid) + the two prompt-bearing effect proofs + `/review-impl` + SESSION/DBT-06/plan-30 close-out | TODO | *DoD:* **live smoke:** signed in as claude-test@loreweave.dev on a REBUILT `:5174` image — palette-opened arc-inspector, created a saga (appeared in plan-hub with no reload), added a track on the saga and saw it render as INHERITED on the sub-arc, edited twice back-to-back with NO 412, forced an agent `composition_arc_update` and the panel said "changed elsewhere — reloaded" and the retry SUCCEEDED, removed a chapter and it landed in the Hub's unassigned tray, archived+restored the subtree, and `ui_open_studio_panel {panel_id:'arc-inspector'}` mounted a DOCK TAB (verified by effect); AND the prompt CHANGED for both tracks and roster_bindings; AND `/review-impl` run with every finding fixed |

### Wave 3 — Motif studio (15 slices) · `docs/plans/2026-07-13-studio-wave-3-motif.md`

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 3a | **3a-0** [CONTRACT] 🔴 🆕 **CONTRACT-FIRST — freeze the 6 new REST routes** in `contracts/api/composition/v1/openapi.yaml`: (1) `GET /motif-jobs/{job_id}` *(amend `GenerationJob`'s `project_id`/`book_id` to `nullable: true` — **do NOT fork the schema**; if `W0-C1` landed this is already done)* · (2) `GET /motifs/{motif_id}/links` · (3) `POST /motifs/{motif_id}/links` · (4) `DELETE /motif-links/{link_id}` · (5) `GET /works/{project_id}/motifs/suggest` · (6) `POST /works/{project_id}/outline/{node_id}/motif/repin`. 🔴 **RUNS FIRST — before 3a-4 / 3b-1 / 3b-3 implement them, and long before any FE slice.** Plan §2.2 | TODO | *DoD:* all 6 paths in the openapi.yaml with request + response + error codes (incl. the H13 **uniform-404**, the `200 {links:[],count:0}`-not-404 rule for an invisible anchor, and `additionalProperties:false` on `MotifLinkCreate`); `redocly lint` clean; contract-parity test green |
| 3a | **3a-1** [BE] BE-7c — **VERIFY-OR-BUILD** (Wave 0's `W0-BE1` owns it; this slice only re-verifies). Pre-flight G4: `grep create_unbound generation_jobs.py` + `grep generation_job_scope_shape migrate.py` + `grep motif-jobs engine.py` — **all 3 NON-EMPTY ⇒ SKIP the build**, paste the suite output, go to `3a-2`. **Do NOT re-apply the DDL or fork a 2nd `create_unbound()`.** Any EMPTY ⇒ **BUILD IT HERE IN FULL** (paid-action class — never defer it) | TODO | *DoD:* the 3 pre-flight greps **pasted**; `pytest -q tests/integration/db/test_motif_job_read.py -n auto --dist loadgroup` → **8 passed**, **RAN not skipped** (pasted) AND **live smoke:** confirmed `composition.motif_mine` scope=corpus through `/actions/confirm` on a stack-up → 202 `{job_id}` → `GET /v1/composition/motif-jobs/{id}` → 200 (was: **500 ReferenceViolationError at confirm**, job row never created) |
| 3a | **3a-3** [BE] BE-M1 — enforce the one-row-per-node invariant; collapse 4 predicates to 1 | TODO | *DoD:* 2355+N passed + pasted back-fill probe output (**EMPTY**) + pasted `test_motif_application_invariant.py` run showing 5 passed, **not skipped** |
| 3a | **3a-2** [BE] BE-M2 / X-7 — the `gather_motifs` packer lens (**THE HARD GATE**) | TODO | *DoD:* pasted output of `TEST_COMPOSITION_DB_URL=… pytest tests/integration/db/test_pack_motifs_wired.py -q` showing **3 passed (NOT 3 skipped)** — a bound motif CHANGES the packed prompt |
| 3a | **3a-4** [BE] BE-M3 — motif-link REST (the graph) | TODO | *DoD:* 2355+N passed — incl. `test_list_links_404s_for_a_foreign_anchor_not_an_empty_200` + `test_create_link_409s_with_the_trigger_reason_on_a_cycle` |
| 3a | **3a-4b** [BE+FE] 🆕 🔴 **THE TENANCY GATE LIES — ship `tier` + `can_edit` ON THE WIRE.** The FE cannot tell a **System** motif from the user's own, so it renders an Edit button that the server then 403s (**I-5**: a System row must be read-only to a regular user — the FE must *say so*, not discover it). ⚠ **Not a CRITICAL stop:** it is a false-**negative** gate (a legitimate editor is locked *out*), **not** a cross-user data exposure. Plan §3a-4b | TODO | *DoD:* composition `<N>` passed + frontend `<N>` passed; every motif row carries `tier` + `can_edit`; **a System motif renders NO Edit button** (asserted) — the FE never offers a write the server will refuse |
| 3a | **3a-5** [FE] The `motif-library` dock panel — the SPLIT from `ArcTemplateLibraryView` + 6 scope tabs + the graph section + `promote_target` | TODO | *DoD:* 152+N passed (frontend motif) + **live smoke:** opened motif-library from the command palette, created a motif, adopted one from the catalog, added a `precedes` link, saw the 409 inline on a self-link; a System motif has **NO Edit button**; the dock never unmounted |
| 3a | **3a-6** [FE] `motifEffects.ts` — the Lane-B effect handlers (X-4) | TODO | *DoD:* 152+N passed — incl. `test_a_RegExp_pattern_actually_matches` (`matchEffectHandlers('composition_motif_unbind')` returns ≥1 handler) + `test_bind_invalidates_the_plan_hub_OVERLAY_key` |
| 3b | **3b-1** [BE] BE-M4 — motif-suggest REST + fix the half-dead retriever call (PL-4) | TODO | *DoD:* 2355+N passed — incl. `test_suggest_passes_the_nodes_beat_role_and_tension_to_the_retriever` (**REDS on old code**: `getattr(node,'tension_target')` was always None and `beat_role` was hardcoded None) |
| 3b | **3b-3** [BE+FE] 🆕 🔴 **BE-M6 · RE-PIN — a LEDGER-ONLY route, NOT a re-bind.** ⚖ `Q-33-STALE-PIN-REPIN`: the original *"re-pin = re-bind to the live version"* **WOULD DESTROY DATA.** The drift is real and unactionable today (`_MOTIF_CHIPS_SQL` ships `pinned_version` + `live_version`; `NodeBadges.tsx:124-145` renders amber + `↑` on a **non-interactive `<span>` with no onClick** — **nothing anywhere can bump the pin**). 🔴 **The obvious fix is WRONG:** `PATCH …/outline/{node}/motif` with the same `motif_id` **(a)** re-runs `bind_motif()` role resolution and **silently CLOBBERS a hand-rebound role**, and **(b)** on a **CHAPTER** node runs `apply_motif_swap` — **it ARCHIVES SCENES AND REGENERATES PROSE** (`plan.py:867-880`) — **and chips DO render on chapter/arc nodes.** ⇒ a blind re-bind **destroys a chapter's scenes on a "Re-pin" click**. The route is a **single ledger UPDATE** (`motif_version` + `role_bindings` + `annotations`), no re-resolution, no swap. Plan §3b-3 | TODO | *DoD:* composition `<N>` passed incl. `test_repin_does_NOT_call_bind_motif` + `test_repin_on_a_CHAPTER_node_does_NOT_call_apply_motif_swap` (**spies at `call_count == 0`** — a green suite that never exercised the chapter path is not evidence) + `test_repin_PRESERVES_a_hand_rebound_role`; frontend: the amber `↑` chip is now **interactive** and re-pins |
| 3b | **3b-2** [FE] The binding lens — scene-inspector Motifs section + PlanDrawer affordance + clickable chip + Suggest button | TODO | *DoD:* 152+N passed + **live smoke:** bound a motif to a scene from scene-inspector, clicked Suggest and got RANKED rows with `match_reason`, clicked a plan-hub motif chip and scene-inspector opened focused on that node; no Undo button on a scene node; the dock never unmounted |
| 3c | **3c-1** [FE] Delete the 3 invented routes; fix M-BUG-4 (`arc_template_id` → `arc_id`); re-point Regenerate at the existing scene-generate | TODO | *DoD:* 152+N passed + 2355+N passed — incl. arcConformance sends `arc_id` never `arc_template_id` (**REDS on old code** — the ONLY one of the four claimed bugs that genuinely reds), and no request to `/actions/conformance_run/*` or `/regenerate-to-beat` |
| 3c | **3c-2** [FE] PL-2 — migrate `useConformanceStatus` to react-query (the spec's Option A cannot work) | TODO | *DoD:* 152+N passed + `npx vitest run src/features/studio/panels` green (SceneBrowserPanel + SceneInspectorPanel re-smoked, **ZERO edits needed**) — incl. `test_invalidating_the_conformance_status_key_REFETCHES` |
| 3c | **3c-3** [FE] The `quality-conformance` dock panel + the QualityHub 8th card | TODO | *DoD:* 152+N passed + the 4 contract guards green with all three counts moving **+1 in lockstep** (py enum == contract enum == openable) + **live smoke:** `ui_open_studio_panel {panel_id:'quality-conformance'}` mounted a dock tab, the trace showed the bound beat, "Advisory — unverified" stamped when `calibrated:false` |
| 3 | **W3-CLOSE** [DOC] 🔴 **WAVE CLOSE** — run the wave-3 plan's §9 WAVE DEFINITION OF DONE: full suites green · **live browser smoke** on a REBUILT image · **`/review-impl` with every finding FIXED** · SESSION_HANDOFF · commit by enumerated path | TODO | *DoD:* `/review-impl` run on the Wave-3 diff; `<K>` findings, **ALL FIXED** (listed); suites re-green after the fixes (composition `<N>`, frontend `<N>`); three-way enum green at `N_before+2` (`motif-library` + `quality-conformance`) asserted as a **DELTA**; the G5 probe result **pasted** (empty ⇒ 3a-3 built; non-empty ⇒ `D-MOTIF-APPLICATION-MULTI-ROW` parked and the run CONTINUED); SESSION_HANDOFF in the same commit |

### Wave 4 — Arc templates & deconstruct (17 slices) · `docs/plans/2026-07-13-studio-wave-4-arc-templates.md`

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 4 | **W4-BE0** [CONTRACT] 🔴 🆕 **CONTRACT-FIRST — freeze the OpenAPI for BE-7a + BE-7b BEFORE any FE flow.** Plan §4 `W4-BE0` | TODO | *DoD:* `POST /arcs/{node_id}/extract-template` + `POST /arc-templates/suggest` are in `contracts/api/composition/v1/openapi.yaml` with request + response + error codes (incl. extract's **409 on the `code` field** and suggest's `detail` enum + `match_reason` on **every** candidate); `redocly lint` clean |
| 4 | **W4-BE1** [BE] `POST /arcs/{node_id}/extract-template` (BE-7a) — thin wrapper over the SHIPPED `extract_template_from_arc`; `UniqueViolationError` → 409 **on the code field**; VIEW gate derived from the ROW | TODO | *DoD:* composition suite `<N>` passed (was `<N-6>`); 6 new in `tests/unit/test_arc_extract_route.py` incl. duplicate-code→409-on-code-field and the missing-node/foreign-book **uniform-404 byte-equality**; route added to `contracts/api/composition/v1/openapi.yaml` |
| 4 | **W4-BE2** [BE] `POST /arc-templates/suggest` (BE-7b) — REST twin of `composition_arc_suggest`; keeps `detail` (Context Budget Law §6b) + `match_reason`; **move** `_ARC_REF_FIELDS`/`_arc_public_projection` to a shared module rather than copy them | TODO | *DoD:* composition suite `<N>` passed; 7 new in `tests/unit/test_arc_suggest_route.py`; `match_reason` asserted present on **EVERY** candidate; `detail=summary` strips threads/layout/pacing but keeps score+match_reason; route in openapi.yaml |
| 4 | **W4-BE3** [BE] **CONDITIONAL** — BE-7c / AT-11: the **OWNER-scoped** job read (gate on `generation_job.created_by`, NOT a book grant) + `create_unbound()` + the DDL + `composition_get_mine_job`'s `project_id` optional/ignored. 🔴 **BE-7c is built in WAVE 0 (`W0-BE1`) and re-verified at Wave 3 (`3a-1`) ⇒ by the time Wave 4 runs this should ALREADY BE PRESENT. SKIP it** (pre-flight cmd 4: `grep create_unbound generation_jobs.py` non-empty ⇒ skip). Build it here **only** if both earlier waves somehow did not — it is the paid-action class, never deferrable | TODO | *DoD:* composition suite `<N>` passed; 6 new in `tests/unit/test_motif_job_read.py` — a synthetic-pid `analyze_reference` job is 200 for its owner and a **uniform 404 (byte-identical body)** for anyone else and for a missing row; `mine_motifs` readable too; `GET /jobs/{id}` STILL 404s on it (**regression lock** proving we did not weaken the Work gate) |
| 4 | **W4-BE4** [BE] 🆕 🔴 **AT-6's PROVENANCE STAMP — SERVER-SIDE** (adjudication D). The stamp belongs in the **materialize transaction**, not in the browser. 🔴 **This RETIRES `D-AT6-FE-PROVENANCE-STAMP-DUPLICATE-WRITER` before it is ever opened** — `W4-FE4` must then **CONSUME** the server stamp, never write a second one. **Two writers for one field is the defect; do not ship one and plan to delete it later.** Plan §5b | TODO | *DoD:* composition `<N>` passed; `materialize` stamps `structure_node.arc_template_id` + `template_version` **inside the txn**; `test_the_stamp_lands_even_when_the_FE_never_calls_PATCH` green; **`W4-FE4` asserts it READS the stamp and issues NO stamping write** |
| 4 | **W4-BE5** [BE] 🆕 🔴 **AT-8: THE PLATFORM MODEL MAY NEVER SILENTLY PAY.** A 拆文 import with no `model_ref` must be **REJECTED BEFORE SPEND** in `_execute_arc_import` (`actions.py:~669-712`) — not silently charged to a platform default. **I-1/I-6: a spend-causing setting fails CLOSED.** Plan §5c | TODO | *DoD:* composition `<N>` passed incl. `test_import_with_no_model_ref_402s_BEFORE_the_llm_runs` (**LLM spy `call_count == 0`** — a rejection after the call is not a rejection); no platform model is ever resolved on this path |
| 4 | **W4-FE1** [FS] The `arc-templates` panel + full GG-8 registration (catalog row, 3 i18n keys × 18 locales, panel_id enum + description prose, contract REGEN, studioLinks deep-link, `arcEffects.ts` handler-body EXTENSION) + the 3-tier browse + the FIXED empty state (two wired buttons, not a dangling sentence). `ArcConformancePanel` **DROPPED** (AT-7) | TODO | *DoD:* panelCatalogContract + UserGuidePanel + useStudioCommands + frontendToolContract green; chat-service `test_frontend_tools_contract` + `test_frontend_tools` green; `ArcTemplatesPanel.test.tsx` 7 passed (incl. the **dangling-CTA lock** and the **all-three-tabs-stay-MOUNTED lock**); `arcEffects.test.ts` green incl. `matchEffectHandlers('composition_arc_create').length === 1` (the double-fire lock); three-way +1 (**DELTA asserted, never a literal**) |
| 4 | **W4-FE2** [FE] The Catalog tab — first FE consumer of the SHIPPED `GET /arc-templates/catalog` (paged `{items,total,limit,offset}`); `arcApi.catalog()` + `useArcCatalog` + `ArcCatalogList` with an Adopt-only write | TODO | *DoD:* `useArcCatalog.test.tsx` 3 passed; the catalog route has its FIRST FE consumer; the **shape-drift lock** is green — the hook reads `.items` (paged) and yields ZERO when handed the list route's `{arc_templates}` shape |
| 4 | **W4-FE3** [FE] CRUD + adopt + the **412 RECONCILE** (never clobber; keep the user's edits) + the System-row read-only rule (`Adopt to edit`, **NEVER a disabled input**) + the AT-9 publish-strip disclosure BEFORE the flip + serialized/entity-bound OCC writes on the timeline grid | TODO | *DoD:* `useArcTemplateEditor` + `ArcTemplateDetail` + `ArcTemplateCreateForm` + `ArcVisibilityControl` — `<N>` passed; a forced 412 replaces the baseline, **KEEPS the user's unsaved edits** and re-enables Save; rapid cell edits are SERIALIZED (no self-412) and each debounced write is **bound to the template it started for**; a System row renders "Adopt to edit" with NO edit affordance; a 409 code-exists renders ON the code field, not a toast |
| 4 | **W4-FE4** [FS] Apply-preview → Materialize → **AT-6's PROVENANCE STAMP** (find-or-create the spec arc via POST/PATCH `/books/{bid}/arcs` with `arc_template_id`+`template_version`, link chapters via plan-hub's `assignChapters`, and **ASSERT `assigned == len(chapter_ids)`** — an `assigned:0` is an ERROR, never a checkmark). Adds `createArc` to `plan-hub/api.ts`; **NO rival `arcSpecApi`** | TODO | *DoD:* `useArcProvenanceStamp.test.tsx` 7 passed incl. **THE SILENT-NO-OP LOCK** (`{assigned:0}` with HTTP 200 makes the hook **THROW** `ArcStampError`, not render a checkmark), the partial-link lock (`assigned:4` of 5 also throws), the OQ-2 lock (a second materialize PATCHes the existing spec arc, does NOT create a second), the `replay:true`-still-stamps lock; `ArcMaterializeErrors.test.tsx` 4 passed; grep for a second assign-chapters caller outside `plan-hub/api.ts` → **EMPTY** |
| 4 | **W4-FE5** [FE] Used-by + DRIFT — **BE-NONE** (the audit correction: `GET /works/{pid}/conformance?scope=arc_template_drift&arc_id=…` ALREADY SHIPS with zero FE consumers). Thread coverage, pacing curve, succession flags, §12.6 folded-away placements — plus **THREE DISTINCT empty states** that must not collapse into one | TODO | *DoD:* `useArcTemplateDrift.test.tsx` 4 passed — the 422 `NO_TEMPLATE_PROVENANCE`, the 404 template-gone and the zero-usages cases are **THREE DIFFERENT renders** (not one generic "no data", not a spinner, not an empty chart), and zero-usages makes **NO network call at all**; `ArcUsedByList` 1 passed; **ZERO backend written** |
| 4 | **W4-FE6** [FE] Extract + Suggest UIs — consumers of BE-7a/BE-7b. Extract ("save this arc as a template" — the half that makes the library GROW from the user's own work) with its 409 on the code field; Suggest rendering `match_reason` via the SHARED `MatchReasonChip`. OQ-3: premise = free-text seeded from the book's summary, **NOT a new settings key** | TODO | *DoD:* `useArcExtract.test.tsx` 2 + `ArcSuggestPanel.test.tsx` 3 passed; extract's 409 renders ON the code field (not a toast) and success deep-links the new `template_id` into the detail view; EVERY suggest candidate renders its `match_reason` via the SHARED chip (the "don't throw the explanation away" lock) |
| 4 | **W4-FE7** [FS] Import & Deconstruct (拆文) — Sources CRUD (20 000-char client-side block; hard delete said out loud) · Configure (**REQUIRED BYOK `model_ref`** via ModelRolePicker — AT-8, no silent hidden payer) · the AT-5 cost gate through the **GENERIC spine ONLY** (`mcpExecute` with fields NESTED under `args` → `POST /actions/confirm?token=`) · the AT-11 poll against `/motif-jobs/{id}` where **a 404 is an ERROR, never an eternal spinner** · the AT-9 copyright/strip disclosure | TODO | *DoD:* `useDeconstruct.test.tsx` 9 + `useImportSources.test.tsx` 2 passed — the **args-NESTING lock**, the `_ArcImportArgs` ForbidExtra key-subset lock, the **submit-is-IMPOSSIBLE-with-no-model_ref** lock (disabled, not warned), the GENERIC `/actions/confirm?token=` lock (asserts the URL does **NOT** match `/actions/arc_import/`), the poll-hits-`/motif-jobs`-NOT-`/jobs` lock, and the **404-from-the-poll-is-an-ERROR-and-polling-STOPS** lock; a 20 001-char paste is blocked client-side with `fetch` NEVER called |
| 4 | **W4-BE8a** [BE] BE-8 agent parity, half 1 (S) — un-stub `composition_arc_template_drift` by **DELEGATING** to the shipped `compute_arc_report(..., by_structure=False)`. Deliberately **NOT a second engine** | TODO | *DoD:* composition suite `<N>` passed; the tool no longer returns `pending_dependency`; **THE PARITY ASSERTION** is green — the tool's report is IDENTICAL to the REST route's report for the same arc |
| 4 | **W4-BE8b** [BE] BE-8 agent parity, half 2 (M) — write `apply_arc_to_spec` (rescale placements + bind the roster once + write pacing into scene tension + emit the `motif_application` ledger, ONTO `structure_node` with a **server-side provenance stamp**) and un-stub `composition_arc_apply`. **Compose the SHIPPED pieces — do not write a fourth arc engine.** ESCAPE HATCH: if it needs a schema change or a `plan.py` refactor → write `D-BE8-APPLY-ARC-TO-SPEC` (gate #2) and **CONTINUE** | TODO | *DoD:* composition suite `<N>` passed with `-n auto --dist loadgroup` (the new integration file carries `pytestmark = pytest.mark.xdist_group('pg')`); the tool no longer returns `pending_dependency`; `apply_arc_to_spec` creates a `structure_node` WITH `arc_template_id`+`template_version`, links its chapters (`assigned == n`), writes the ledger, and is **idempotent on re-apply**; the M5 PARITY test asserts an agent-driven apply + drift MATCHES the panel's output |
| 4 | **W4-BE8c** [BE] 🆕 **KILL THE SEAM (XS)** — after BE8a/BE8b land, the temporary wrapper seam is dead code. **Delete it.** A seam kept "just in case" is the `D-EDITORBRIDGE-SINGLETON` class: alive only because the thing it was going to die with was kept. Plan §7 `W4-BE8c` | TODO | *DoD:* composition `<N>` passed; the seam symbol is **GONE** (`grep` → ZERO non-test hits); no behavior change (the same tests that were green before are green after, **unedited**) |
| 4 | **W4-CLOSE** [TEST] Wave close — `/review-impl` (every bug FIXED) + the LIVE BROWSER Playwright smoke (`studio-arc-templates.spec.ts`) + SESSION_HANDOFF + **plan 30 §5.2's "BE: NONE" row for G-IMPORT-DECONSTRUCT AMENDED** (it is false — the poll leg did not exist) | TODO | *DoD:* **live smoke:** Playwright on the baked `:5174` nginx build (**rebuilt first**) as claude-test@loreweave.dev — (1) `ui_open_studio_panel {panel_id:'arc-templates'}` from the agent chat MOUNTED a dock tab; (2) pasted a reference text, picked a LOCAL lm_studio model by explicit `user_model_id`, Proposed → real cost estimate → Confirmed → polled to TERMINAL `completed` → a new arc template appeared in the Library ($0 spend); (3) opened it → Apply-preview → Materialize → **DRIFT RENDERED A REPORT** (which transitively proves AT-6's provenance stamp landed — a `NO_TEMPLATE_PROVENANCE` here would have meant a silent no-op); (4) NO dock teardown at any point incl. the model picker's zero-model state (the X-1 regression guard). `/review-impl` `<N>` findings, ALL FIXED. Full composition + frontend suites green. SESSION_HANDOFF updated; plan 30 §5.2 amended |

### Wave 5 — PlanForge (19 slices) · `docs/plans/2026-07-13-studio-wave-5-planforge.md`

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 5 | **W5-S0** [FE] 🆕 🔴 **X-1 — `AddModelCta` must not tear the dock down. ~30 LINES. THE FIRST SLICE.** ⚖ The plan's own C-3: an earlier cut said *"X-1 is an external hard gate; if it has not landed, STOP the panel slices."* 🔴 **"STOP and ask" for UNBUILT WORK VIOLATES THE RUN POLICY (§4 rule 5 / I-14) — a missing component is work you WRITE.** It is ~30 lines in **ONE** file: `frontend/src/components/shared/AddModelCta.tsx` (⚠ **the spec's path is WRONG**). ⚠ **CONDITIONAL — `W0-S3` builds this.** Pre-flight green ⇒ **SKIP**. Plan §`W5-S0` | TODO | *DoD:* `AddModelCta.test.tsx` `<N>` passed incl. "inside a StudioHost, clicking calls host.openPanel and does NOT navigate"; **every paid PlanForge action's ModelPicker empty state no longer unmounts the dockview layout** |
| 5 | **W5-S1** [BE] BE-20 + BE-21 + BE-22 — the ledger tells the truth about itself (**LOAD-BEARING**) | TODO | *DoD:* composition **2360** passed (2355 baseline + 5) under `-n auto --dist loadgroup`; `grep -c plan_bootstrap_seed services/` == 0 |
| 5 | **W5-S2** [BE] BE-3 — the artifact **BODY** read route (exists in NO transport today) | TODO | *DoD:* composition 2362 passed; `curl GET /…/plan/runs/{r}/artifacts/{a}` returns a real content body; a cross-book id returns **404 (not 403)** |
| 5 | **W5-S3** [BE] BE-3b — the run detail returns its own `source_markdown` | TODO | *DoD:* composition 2363 passed; `test_run_detail_round_trips_source_markdown` green |
| 5 | **W5-S4** [BE] BE-2 — autofix REST mirror (**200 `{rounds, run}` — NOT a 202 ack; the spec is wrong**) | TODO | *DoD:* composition 2365 passed; `test_autofix_returns_rounds_and_run_not_an_ack` green (asserts `'job_id' not in body`) |
| 5 | **W5-S5** [BE] BE-4 + BE-4b — archive a plan run **AND restore it** (PS-13: they ship together) | TODO | *DoD:* composition 2370 passed; `psql \d plan_run` shows `is_archived \| boolean \| not null default false`; `test_archive_refuses_while_a_job_is_in_flight` green |
| 5 | **W5-S5b** [BE] 🆕 🔴 **OQ-6 — the pass ledger CAN LOSE a concurrent sibling write. FIX-NOW.** A lost-update window on the ledger the whole wave reads. Plan §`W5-S5b` | TODO | *DoD:* composition `<N>` passed incl. `test_two_concurrent_sibling_pass_writes_BOTH_survive` (**RED before, GREEN after — both runs pasted**; a mock would encode the bug — this needs real SQL, `xdist_group("pg")`) |
| 5 | **W5-S5c** [BE+CONTRACT+FE] 🆕 🔴 **`self_heal` SHADOWS `scenes` — give the artifact list PROVENANCE.** Two artifact kinds collide in the list and the later one **silently hides** the earlier. Plan §`W5-S5c` | TODO | *DoD:* composition `<N>` passed incl. `test_a_self_heal_artifact_does_NOT_shadow_the_scenes_artifact`; the artifact list carries its provenance on the wire; the contract + the FE type are updated **in the same slice** (this is the one BE slice that carries its own contract edit — `W5-S6c` freezes the rest) |
| 5 | **W5-S6** [CONTRACT] 27-B1/B1b/B2 — contract hygiene: the schema that contradicts its own emitter | TODO | *DoD:* composition 2373 passed incl. `test_planner_state_validates_a_book_that_is_not_the_POC` (**RED today on BOTH clauses**) + `test_every_pass_output_kind_has_a_schema_definition` (proves **SIX** kinds, not seven) |
| 5 | **W5-S6c** [CONTRACT] 🔴 🆕 **CONTRACT-FIRST — freeze the API spec BEFORE any FE slice consumes it.** 🔴 **FIND THE FILE — DO NOT GUESS. It is `contracts/api/composition-service/plan-forge.v1.yaml`** (455 lines, 8 paths, 14 schemas) — **NOT** `contracts/api/composition/v1/openapi.yaml` (that is Waves 0/1/2/3/4/6's file), and **NOT** `contracts/api/book-service/` (**which DOES NOT EXIST** — another wave's plan names it 3×; the real path is `contracts/api/books/v1/openapi.yaml`). Freezes **5 NEW** routes (S2 artifact body · S4 autofix · S5 archive/restore/`?include_archived`) **+ the 4 routes the Pass Rail consumes for the FIRST time and the contract NEVER had** (`…/passes`, `…/passes/{id}/run`, `…/checkpoint`, `…/link`). 🔴 **RUNS BEFORE `W5-S7`.** Plan §`W5-S6c` | TODO | *DoD:* 9 routes in `plan-forge.v1.yaml` with request + response + error codes (incl. S4's **200 `{rounds,run}` — NOT a 202 ack**, C-5; the H13 uniform-404 on artifact body; `409 UPSTREAM_STALE`/`CHECKPOINT_REFUSED`/`LINK_REFUSED`); `redocly lint` clean. **A contract that omits the routes the frontend calls is not a contract.** |
| 5 | **W5-S7** [FE] FE-1 — `readOnly` on the JSON-document standard (**BUILD it**; zero hits today) | TODO | *DoD:* frontend vitest green incl. `a_readOnly_provider_renders_with_no_save_affordance_and_cmdS_does_not_call_save` — asserts **NO save button** (not "save() is a no-op"; a no-op save IS the bug) |
| 5 | **W5-S8** [FE] PS-14 — plan-forge react-query migration + api/type widening (PS-5 revision type fix) | TODO | *DoD:* frontend vitest green incl. `an_agent_invalidation_of_plan_run_refetches_the_detail` (proves Lane-B can reach the hook) + `refine_body_sends_revision_as_an_object` (pins PS-5) |
| 5 | **W5-S9** [FE] R1 + PS-9 — the artifact viewer + the plan-artifact JSON document provider (composite `{runId}:{artifactId}`) | TODO | *DoD:* frontend vitest green incl. `test_the_provider_is_registered_at_planner_mount`; **live smoke leg (e):** an artifact row opens json-editor with a real body, read-only, ⌘S does nothing visibly and deliberately |
| 5 | **W5-S10** [FE] R2 + R3 — source-markdown resume + the Repair strip (interpret / refine / autofix — the free win) | TODO | *DoD:* frontend vitest green incl. `test_reopening_a_run_seeds_the_source_textarea` (BE-3b's **effect**, observed) + `test_the_repair_strip_is_hidden_when_there_are_no_gaps` |
| 5 | **W5-S11** [FE] R4 — archive a failed run + restore it from the runs list (**the toast must NOT say "deleted"**) | TODO | *DoD:* frontend vitest green incl. `test_archiving_a_run_removes_it_from_the_list_and_offers_undo`; **live:** archive → row goes → Undo → it is back |
| 5 | **W5-S12** [FS] **THE PASS RAIL** — the `plan-passes` panel + `usePassRail` (the headline) | TODO | *DoD:* frontend vitest green incl. `test_the_stuck_banner_survives_a_rejected_blocking_pass` + `test_world_is_disabled_and_beats_is_enabled_on_a_cast_pending_run`; live smoke legs (a)(b)(c)(d)(f) |
| 5 | **W5-S13** [FS] GG-8 registration for `plan-passes` (catalog + 18 locales + chat-service enum + contract REGEN) | TODO | *DoD:* 4 drift-lock suites green (`test_frontend_tools_contract`, `test_frontend_tools`, panelCatalogContract, UserGuidePanel, useStudioCommands, frontendToolContract); **py enum == contract enum == openable, EACH == pre-flight baseline + 1 (asserted as a DELTA, never the literal 66)** |
| 5 | **W5-S14** [FE] X-4 Lane-B — `planEffects.ts` (there is **NO** `plan_*` handler today; the agent runs a pass and the Rail sits stale) | TODO | *DoD:* frontend vitest green incl. `test_the_run_id_is_read_through_the_tool_result_ENVELOPE` (the wrapped `{ok,result}` shape the live stream actually sends); **live smoke leg (b):** the agent runs a pass and the Rail updates with NO manual refresh |
| 5 | **W5-S15** [TEST] WAVE CLOSE — live browser smoke (6 legs) · `/review-impl` + fix every bug · SESSION_HANDOFF · commit | TODO | *DoD:* **live smoke:** on a `blocked_at:'cast'` run, a human clicked through the seed proposal (review→approve→apply) then Approve, and **the ledger ADVANCED** — `blocked_at` moved off cast, `pass_cursor` incremented; `/review-impl` run, every finding FIXED; `D-PLANFORGE-GUI-AUDIT` amended then cleared |

### Wave 6 — Editor craft (22 slices) · `docs/plans/2026-07-13-studio-wave-6-editor-craft.md`

> 🔴 **WAVE 6 ADDS FIVE PANELS, NOT THREE** (`style-voice` · `reference-shelf` · `divergence` ·
> **`scene-compose`** · **`chapter-assemble`**) — the last two were added when the plan homed the orphaned
> `compose`/`assemble` legacy sub-tabs. **Assert `N_before + 5`, as a DELTA, never a literal.**
> 🔴 **Four defer rows are DELETED, not carried** — `D-DIVERGENCE-SPEC-EDIT`, `D-REFERENCES-MCP-TOOLS`,
> `D-DIVERGENCE-MCP-TOOLS`, `D-WAVE6-DISCOVERY-SCENT` were each **refuted against code** (I-14: *missing
> infrastructure is unbuilt work, not a blocker*). They are BUILT here as `W6-BE6/BE4b/BE8/BE9`.

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 6 | **W6-M0** [FE] 🆕 **The Wave-0 gate (X-1..X-5) — RUNS BEFORE EVERYTHING.** **CONDITIONAL**: each is a SKIP if Wave 0 landed it. Plan §`W6-M0` | TODO | *DoD:* the 5 pre-flight greps **pasted**; each already-green item is **SKIPPED, not re-implemented** (a second `AddModelCta` fix / a second `CATEGORY_ORDER` entry is a defect, not insurance) |
| 6 | **W6-C0** [CONTRACT] 🔴 🆕 **CONTRACT FIRST — freeze the OpenAPI before any FE flow.** 🔴 **THE FILE IS `contracts/api/composition/v1/openapi.yaml`** (498 lines, 17 paths) — ⚠ **`contracts/api/composition-service/plan-forge.v1.yaml` is a DIFFERENT file: that is Wave 5's. Do not write into it.** ⚠ **There is no `contracts/api/book-service/` — do not create one.** **15 routes**, each consumed by an FE slice in this wave: style-profiles (3) · voice-profiles (3) · references GET/POST/**PATCH (NEW, BE-17b)**/DELETE/search (5) · `PATCH /works/{pid}` with **`settings_unset` (NEW, BE-18)** + the **shallow-merge** semantics · `POST …/derive` with a **REQUIRED `name`** (BE-13a) · `GET …/derivative-context` + `derivative_name` · **`PATCH …/divergence-spec` (NEW, BE-13)** · **`PUT/DELETE …/overrides/{id}` (NEW, BE-13)** · `POST …/outline/decompose` (`structure_template_id` becomes nullable). Plan §`W6-C0` | TODO | *DoD:* all 15 in the openapi.yaml with request + response + error codes; `redocly lint` clean; `test_openapi_contract.py` green — **BEFORE any FE slice consumes them** |
| 6 | **W6-BE1** [BE] BE-18 — close the settings **lost-update window** (server-side shallow merge + `settings_unset` + per-call-site `ifMatch`) | TODO | *DoD:* `test_works_repo_settings_merge.py`: 6 passed incl. `test_two_patches_to_different_keys_both_survive` (**RED before the merge, GREEN after**) + `test_whole_subkey_replace_still_deletes_subkeys` |
| 6 | **W6-BE2** [BE] BE-21 — make composition's engine **READ `model_roles`** (`resolve_model_role` + all 7 critic sites). **HARD PREREQ of M4a** | TODO | *DoD:* `test_model_roles_resolver.py`: 5 passed · `test_engine_critic_reads_model_roles.py`: 3 passed incl. `test_critic_set_via_model_roles_is_NOT_skipped` (**RED at HEAD 9262ed53e, GREEN after**) · `grep -c 'sdict.get("critic_model_ref")' routers/engine.py` == 0 |
| 6 | **W6-BE3** [BE] BE-13a — a derivative gets a NAME (`settings.derivative_name`; no DDL). **HARD PREREQ of M3** | TODO | *DoD:* `test_derive_name.py`: 5 passed incl. `test_work_resolution_candidates_carry_the_name` · `useDivergenceWizard.test.ts`: `buildBody` includes the trimmed name (**RED at HEAD** — buildBody dropped it) |
| 6 | **W6-BE4** [BE] BE-10 — 6 style/voice MCP tools (Tier-A, `undo_hint`) + the 2 AN-8 rows in spec 28. **The INVERSE gap** | TODO | *DoD:* `test_mcp_style_voice_tools.py`: 8 passed incl. `test_the_emitted_schemas_carry_the_scope_type_enum`; spec 28 AN-8 table has `style_profile` + `voice_profile` rows |
| 6 | **W6-BE4b** [BE] 🆕 **BE-10b — the 4 reference MCP tools (C15, un-deferred).** `D-REFERENCES-MCP-TOOLS` said *"the agent can `pack()` with references and cannot curate them"* — **the routes ALL exist; it is cheap.** GG-2 inverse gap. Plan §`W6-BE4b` | TODO | *DoD:* composition `<N>` passed incl. the **FastMCP 3-schema-source guard** (pydantic model · JSON schema · **function signature** — FastMCP generates from the SIGNATURE and STRIPS the model); `tools/list` fragment pasted showing all 4; ✅ **`D-REFERENCES-MCP-TOOLS` moves to Recently-cleared — it was BUILT, not deferred** |
| 6 | **W6-BE5** [BE] BE-20 — cost-gate Decompose: `composition_decompose` (Tier-W) + descriptor + `_execute_decompose` + the C2 dispatch-fallthrough fix. Gates M4b only | TODO | *DoD:* `test_decompose_action_gate.py`: 7 passed incl. `test_decompose_confirm_402s_before_the_llm_runs_when_over_budget` (**LLM spy `call_count == 0`**) + `test_an_unknown_work_scoped_descriptor_400s_and_does_NOT_run_conformance` |
| 6 | **W6-BE6** [BE] 🆕 🔴 **BE-13 — the divergence spec + overrides become EDITABLE (C13, un-deferred).** ⚖ `D-DIVERGENCE-SPEC-EDIT` claimed *"UNBUILDABLE on today's backend — needs new write semantics."* **FALSE** (`Q-36-DEFERRED-BE13`): `packer/pack.py:153-157`'s own docstring says the override is **read FRESH on every pack** (*"an edited override takes effect on the next pack"*); the tables, columns, the `uq_entity_override_work_target` UNIQUE index (`migrate.py:175-176`), the repo write pattern and the EDIT gate **ALL EXIST.** *"New write semantics"* means, literally, **an UPDATE statement.** 🔴 **This is I-14 (missing infrastructure is NOT blocked) caught in the act.** Without it, a user who mistypes a POV anchor must **abandon the derivative and re-derive — losing every drafted chapter** (derive mints a *fresh* knowledge project, `works.py:371`). Plan §`W6-BE6` | TODO | *DoD:* composition `<N>` passed; `PATCH /divergence-spec` + `PUT/DELETE /overrides/{id}` land (an explicit **`null` CLEARS**, an **omitted key PRESERVES**; `DELETE` → **404 when 0 rows matched, never a silent 204**); the `ON CONFLICT` **repeats `uq_entity_override_work_target`'s predicate exactly**; ✅ **`D-DIVERGENCE-SPEC-EDIT` DELETED — it was never defer-eligible** |
| 6 | **W6-BE7** [BE] 🆕 🔴 **BE-17 — reference LIST-widen + metadata PATCH (C14, un-deferred).** `D-REF-UPDATE-AND-MODEL-SURFACE`: today, fixing a **typo in a reference title** means delete + re-add — **which RE-EMBEDS: a paid provider call to fix a typo.** Plan §`W6-BE7` | TODO | *DoD:* composition `<N>` passed; `PATCH /references/{id}` accepts `{title?,author?,source_url?}` with **`extra: forbid`** and **422 on `content`** (`rest-write-mirror-drops-fields`: the write model must not silently drop what it won't take); `test_a_title_edit_does_NOT_re_embed` — **the embed spy is at `call_count == 0`**; the LIST returns `reference_embed_model_ref` + `_source` (SET-1: **effective value + source tier**, no silent hidden default) |
| 6 | **W6-BE8** [BE] 🆕 **M6-D — the 3 divergence MCP tools (C15, un-deferred).** ⚖ `D-DIVERGENCE-MCP-TOOLS` claimed *"deriving forks a knowledge partition — it deserves its own confirm design."* **FALSE:** the **AN-8 spine is GENERIC (11 descriptors)** and an MCP path **already mints a knowledge project**. Inventing a second confirm convention here would be **the AN-8 defect** (*"a reviewer finding a new confirmation convention here has found a defect"*). Plan §`W6-BE8` | TODO | *DoD:* composition `<N>` passed; the 3 tools ride the **EXISTING generic descriptor spine** (asserted: **no new confirm convention** — `grep` for a bespoke `/actions/derive_*` route → **ZERO**); FastMCP 3-schema-source guard green; ✅ **`D-DIVERGENCE-MCP-TOOLS` DELETED** |
| 6 | **W6-BE9** [BE] 🆕 **X-10 / AN-C2 — the discovery scent (C16 — the hold is RELEASED).** ⚖ `D-WAVE6-DISCOVERY-SCENT` said *"`stream_service.py` is uncommitted and mid-edit"* — **FALSE at HEAD: Track C's edits are COMMITTED and the tree is CLEAN.** 🔴 **A dirty file is a wait of minutes, never a defer row.** The 7 new MCP tools this wave adds are otherwise **never mentioned to the model** (declared-and-undiscoverable). ⚠ **Re-verify the Track-C gate at build time**; if genuinely dirty ⇒ defer and **CONTINUE**. Plan §`W6-BE9` | TODO | *DoD:* chat-service `pytest tests -q -n auto --dist loadgroup` → ≥baseline, 0 failed; the scent clause names the wave's new tools; **LIVE LLM smoke** (local lm_studio, $0): the agent reaches for a new style/voice/reference tool **unprompted**; ✅ **`D-WAVE6-DISCOVERY-SCENT` DELETED** |
| 6 | **W6-FE0** [FS] EC-3c/EC-3d — **`source_work_id` is the predicate**: `workSelect.ts` + bus `work:switch` + `useActiveWork` + all 12 positional sites + the scoped hygiene guard | TODO | *DoD:* `workSelect.test.ts`: 4 passed · `useActiveWork.test.tsx`: 3 passed · `noPositionalWorkIdentity.test.ts`: 1 passed (0 hits across 4 scoped dirs, 1 allowlisted) · `test_book_model_settings_picks_canonical.py`: 2 passed · full vitest + composition pytest green at the pre-flight baselines |
| 6 | **W6-M1** [FS] `style-voice` panel — **the CASCADE, not a slider pair**: TierChip +4 tiers, ClearOverride wired, neutral ≠ 50/50, + registration (+1 enum) + the Lane-B RegExp handler | TODO | *DoD:* `TierChip.test.tsx`: 4 new tiers render their LABEL · `StyleVoicePanel.test.tsx`: 6 passed incl. `test_an_inherited_scope_renders_the_INHERITED_numbers_and_the_source_chip` · `matchEffectHandlers('composition_style_upsert').length === 1` · three-way at N_before+1 |
| 6 | **W6-M2** [FE] `reference-shelf` panel (**NOT `references`** — it collides with `composition_find_references`) + the honest read-only embed-model header (+1 enum) | TODO | *DoD:* `ReferenceShelfPanel.test.tsx`: 5 passed incl. `test_the_empty_model_state_is_NOT_a_router_Link` (**the DOCK-7 guard**) · registration green at N_before+2 three-way |
| 6 | **W6-M3** [FS] `divergence` panel — a **MANAGE** panel (list/switch/archive), not a wizard launcher; wizard port + DerivativeBanner + the persisted per-user active Work (+1 enum) | TODO | *DoD:* `DivergencePanel.test.tsx`: 6 passed incl. `test_switch_to_persists_the_pref_AND_publishes_the_bus_event` · registration green at N_before+3 · **LIVE:** derived a named dị bản, switched to it, **RELOADED** — Editor, navigator, scene-inspector and quality panels are ALL still on the derivative |
| 6 | **W6-M4a** [FS] Story structure, the free half — the Composition section in SettingsTab (**Book-tier writer of `model_roles`**) + `active_template_id` as the ONE home + the Beats facet in PlanDrawer | TODO | *DoD:* `useCompositionSettings.test.tsx`: 6 passed incl. `test_a_world_map_drag_does_NOT_412` · `test_capture_correction_prose_effect.py`: 2 passed (raw_before/raw_after stored ON, NULL OFF) · `test_critic_set_via_model_roles_is_NOT_skipped` GREEN · `PlanDrawerBeats.test.tsx`: 4 passed · **LIVE:** set the critic model in book-settings → a critique run no longer returns "critique skipped" |
| 6 | **W6-M4b** [FS] The **Decompose** action in PlanDrawer — through the GENERIC `/actions/preview` → `/actions/confirm` spine → `ui_watch_job` → job-detail (cross-service) | TODO | *DoD:* `PlanDrawerDecompose.test.tsx` + `useDecomposeAction.test.ts`: 6 passed incl. `test_the_estimate_comes_from_the_GENERIC_actions_preview` · **LIVE (cross-service):** decomposed a chapter from plan-hub — `/actions/preview` returned a real estimate, confirm enqueued, **job-detail opened IN THE DOCK** (no route-nav), the preview tree committed |
| 6 | **W6-M6** [FS] 🆕 🔴 **`scene-compose` — the home for the legacy `compose` + `cowriter` sub-tabs (+1 panel).** ⚠ **NOT the Chat panel.** Without it, GG-4 **deletes the compose loop.** Plan §`W6-M6` | TODO | *DoD:* `SceneComposePanel.test.tsx` 4 passed — `ComposeView` mounts as a **LEAF** (no `CompositionPanel` import; `no-composition-shell.test.ts` green), `onAccept` → `applyProposedEdit` **with provenance**, a reject **POSTs `/jobs/{id}/correction`**, `useAdaptFromSource` renders on a derivative; registration green at **`N_before+1`** three-way |
| 6 | **W6-M7** [FS] 🆕 🔴 **`chapter-assemble` — the home for the legacy `assemble` sub-tab (+1 panel).** ⚠ **NOT `agent-mode`.** **The SECOND correction producer** — it closes G-CORRECTION-FLYWHEEL's real surface. Plan §`W6-M7` | TODO | *DoD:* `ChapterAssemblePanel.test.tsx` 6 passed — preview uses `persist=false` and **leaves the editor draft untouched**, `scenesAllDone=false` disables the stitch **WITH A REASON** (not a dead button), Accept → `applyProposedEdit`, a correction **POSTs `/jobs/{id}/correction`**; registration green at **`N_before+1`** three-way |
| 6 | **W6-M5** [TEST] The GG-4 gate, **MECHANIZED** — `legacyParityContract.test.ts` (a route/hygiene assertion, NOT the 18-line prose banner). Does **NOT** delete `ChapterEditorPage` (OQ-2). 🔴 **dependsOn M1, M2, M3, M4a, M6, M7** | TODO | *DoD:* `legacyParityContract.test.ts` passed — the map is **exhaustive over `ALL_TABS`** (25/25; a missing key is a **TS error**) and style-voice / reference-shelf / divergence / **scene-compose** / **chapter-assemble** are all openable. 🔴 **THREE rows ship as `{pending:'Wave 8 / W8-1x'}` — `cast`, `arc`, `flywheel` — because WAVE 8 RUNS AFTER WAVE 6 and its panels do not exist yet.** ⇒ **`it.fails('GG-4: zero pending rows')` is RED at this wave's close, BY DESIGN — that is the gate HOLDING.** File `D-GG4-PARITY-ROWS-PENDING`; **W8-16/17/18 flip all three** and W8-18 converts `it.fails`→`it`. ⚠ **`worldmap` is a `{retired:…}` won't-port — there is NO `place-graph` panel and no wave builds one. DO NOT invent the id, DO NOT re-point a row at a panel that is not the thing, DO NOT demote a `pending` to a `retired` to get green** (that is the "gate goes GREEN on a feature being deleted" failure — master C-8) |
| 6 | **W6-CLOSE** [DOC] 🔴 **WAVE CLOSE** — run the wave-6 plan's DoD checklist: full suites green · **live browser smoke** on a REBUILT image · **`/review-impl` with every finding FIXED** · SESSION_HANDOFF · commit by enumerated path. **Raise E-3 (`D-STUDIO-MOBILE-SHELL`) to the PO at this close-out — GG-4 stays SHUT until they rule.** | TODO | *DoD:* `/review-impl` run on the Wave-6 diff; `<K>` findings, **ALL FIXED** (listed); suites re-green after the fixes (composition `<N>`, chat-service `<N>`, frontend `<N>`); 🔴 **three-way enum green at `N_before+5`** (`style-voice` · `reference-shelf` · `divergence` · `scene-compose` · `chapter-assemble`) asserted as a **DELTA, never a literal**; the `legacyParityContract` PENDING rows for **Waves 1–5 flipped**, and **exactly 3 pending rows remain** (`cast`/`arc`/`flywheel` → Wave 8) with `D-GG4-PARITY-ROWS-PENDING` filed; **`ChapterEditorPage` is NOT deleted** (E-3 open — **raise it to the PO at this close-out; GG-4 stays SHUT until they rule**); the 4 refuted defer rows moved to **Recently cleared (BUILT, not deferred)**; SESSION_HANDOFF in the same commit |

### Wave 7 — Issues feed (17 slices) · `docs/plans/2026-07-13-studio-wave-7-issues-feed.md`

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 7 | **W7-S0** [FE] 🆕 **X-1 — `AddModelCta` stops destroying the dock.** ⚖ **ADJ: this was a defer row (`D-W7-RUN-BUTTON-X1`); it is now a SLICE** — I-14, a missing component is work you WRITE. ⚠ **CONDITIONAL — `W0-S3` builds it.** Pre-flight green ⇒ **SKIP.** Plan §`W7-S0` | TODO | *DoD:* `AddModelCta.test.tsx` `<N>` passed; **the W7-S7 Run-conformance button can now ship** (it no longer destroys the dock on the empty-model path) ⇒ `D-W7-RUN-BUTTON-X1` **never opens** |
| 7 | **W7-S1** [BE] BE-1a — extract `build_diagnostics()` into `agent_native.py` + widen the payload (`ref_kind`/`chapter_id`/`rule_id`); the MCP handler becomes a 10-line shell | TODO | *DoD:* `pytest tests/unit/test_agent_native.py -q` all pass, AND `grep -c "compute_coverage" services/composition-service/app/mcp/server.py` → **0** (the fanout MOVED; there is no second copy) |
| 7 | **W7-S2** [CONTRACT] BE-1 — `GET /books/{book_id}/diagnostics` (read-only, VIEW-gated, the user's own JWT, server-side filter with **UNFILTERED counts**) | TODO | *DoD:* `pytest tests/unit/test_diagnostics_router.py tests/unit/test_agent_native.py -q` all pass; the **degraded-source** test asserts NO `unplanned_chapter` items + a `warnings[]` entry (**never 0**) |
| 7 | **W7-S3** [CONTRACT] BE-1d — `GET /books/{book_id}/entity-references` (8 closed-set sources, per-source degrade, **422 on an unknown source — never `(0,[])`**) | TODO | *DoD:* `pytest tests/unit/test_entity_references_router.py -q` all pass; `test_all_eight_sources_are_returned_by_default` green |
| 7 | **W7-S3b** [CONTRACT] 🔴 🆕 **CONTRACT-FIRST — freeze the two new routes in the OpenAPI spec BEFORE any FE consumes them.** 🔴 **RUNS BEFORE `W7-S4`.** Plan §`W7-S3b` | TODO | *DoD:* `GET /books/{book_id}/diagnostics` + `GET /books/{book_id}/entity-references` are in `contracts/api/composition/v1/openapi.yaml` with request + response + error codes (incl. the **8 closed-set sources**, the **422 on an unknown source — never `(0,[])`**, and the `warnings[]` degrade shape); `redocly lint` clean |
| 7 | **W7-S4** [FE] `StudioBottomPanel` — 3 **ALWAYS-MOUNTED** bodies (CSS hidden, never a ternary — I-8) + drag-resize grip (localStorage, per-device) + the HONEST stub copy + the inverted guard test | TODO | *DoD:* `npx vitest run …/StudioBottomPanel.test.tsx` green — asserts on **VISIBILITY** (`.not.toBeVisible()`), not `textContent`; 18 locale files touched by `scripts/i18n_translate.py` |
| 7 | **W7-S5** [FS] The **Issues tab** — `useDiagnostics` (React-Query, staleTime 60s, no polling) + `useIssueRouting` (the 7-row routing table, 3 rows INERT per FE-1) + `IssueRow` + the warnings strip + every state rendered | TODO | *DoD:* `npx vitest run src/features/studio/components/bottom` green incl. `test_warnings_and_no_items_never_renders_no_issues_found` + `test_the_three_FE1_rows_render_with_no_chevron_and_are_not_clickable` |
| 7 | **W7-S6** [FE] Lane-B — `handlers/diagnosticsEffects.ts` (**a RegExp, never a string** — the string branch is `startsWith` and would silently no-op) + register it + delete the now-FALSE comment at `:7-9` | TODO | *DoD:* vitest green (`DIAGNOSTICS_STALING instanceof RegExp`; invalidates the filter-suffixed query key **by prefix**) AND `grep -n registerDiagnosticsEffectHandlers …/useStudioEffectReconciler.ts` is **non-empty** (the wiring, proven) |
| 7 | **W7-S7** [FS] The **Run-conformance** button — the GENERIC actions spine (`mcpExecute` propose → `/actions/confirm`), `arc_id` **NOT** `arc_template_id`. SKIPPED + deferred if X-1 has not landed | TODO | *DoD:* vitest green + `grep -rn "conformance_run/estimate\|conformance_run/confirm\|arc_template_id" frontend/src/features/studio/` → **EMPTY** (the two live-404 paths and the ForbidExtra-rejected arg were not copied) |
| 7 | **W7-S8** [FS] M2 — the **find-references LENS** (a popover, not a panel — PO-1): 8 rows, one per `REFERENCE_SOURCES` member; right-click + a keyboard `⋯` affordance on NodeBadges cast chips and EntityRefField | TODO | *DoD:* vitest green — `test_all_eight_source_rows_render` (length === 8) + `test_a_failed_source_renders_could_not_be_read_not_zero`; **live smoke** right-clicks a cast badge and counts 8 source rows |
| 7 | **W7-S9** [FE] M1b / FE-1 — `PlanHubPanel` reads `props.params.{focusNodeId,focusArcId}` and `ChapterBrowserPanel` reads `.focusChapterId`; both **HOIST+REVEAL** (mirror `useQualityCanon`'s `hoist()`, never filter); `PARAM_BLIND_TARGETS` empties and the 3 rows light up | TODO | *DoD:* vitest green + a Playwright click that **lands on the FOCUSED node** — asserting the tab mounted is NOT the assertion; asserting the right row is focused IS |
| 7 | **W7-S10** [BE] M3 / BE-1b — stamp `book_id` into the jobs projection params at **BOTH** composition producer call sites, taken from the **ROW** (never a caller arg) + an additive expression index | TODO | *DoD:* `cd services/composition-service && python -m pytest tests -q -n auto --dist loadgroup` green; `test_the_guarded_create_path_also_emits_book_id` proves the **SECOND** call site was patched |
| 7 | **W7-S11** [BE] M3 / BE-1c — `GET /v1/jobs?book_id=…`: one clause in `_build_filters` (`params->>'book_id'`), threaded through `list_jobs` + `list_jobs_paged` + the router. A **NARROWING** filter on top of the unconditional owner gate | TODO | *DoD:* `cd services/jobs-service && python -m pytest tests -q -n auto --dist loadgroup` green; `test_book_id_does_not_widen_past_the_owner_gate` green (**user B's job never leaks to user A** — I-5) |
| 7 | **W7-S12** [FE] M3 — the **Jobs + Generation** tabs over `JobsStreamProvider` (book-scoped), row-click → job-detail via the shipped `host.openPanel` seam; dead `bottomStub.*` keys deleted | TODO | *DoD:* vitest green + **live smoke:** a real composition generate job appears in the Jobs tab, book-scoped, on a stack-up (cross-service ⇒ a green unit suite is insufficient — I-12) |
| 7 | **W7-S12b** [BE] 🆕 **X-10 — the agent's discovery scent.** ⚖ **ADJ: the no-go zone is LIFTED** (Track C's `stream_service.py` edits are **COMMITTED**; the tree is clean at HEAD). Without it, `composition_diagnostics` is **never mentioned to the model** and the agent forages per-domain list tools instead. ⚠ **Re-verify the Track-C gate at build time**; genuinely dirty ⇒ defer and **CONTINUE**. Plan §`W7-S12b` | TODO | *DoD:* chat-service `pytest tests -q -n auto --dist loadgroup` → ≥baseline, 0 failed; `grep 'composition_diagnostics' stream_service.py` → ≥1 hit; **LIVE LLM smoke** (local lm_studio, $0): "what's wrong with my book?" → the agent calls `composition_diagnostics` |
| 7 | **W7-S12c** [TEST] 🆕 **The live-smoke SEED + the two Playwright specs** (DoD-7's actual recipe — *a smoke with no seeded data proves nothing*). Plan §`W7-S12c` | TODO | *DoD:* the seed script produces a book with **≥1 of each routable issue kind**; the 2 Playwright specs run **against a REBUILT image** and are green |
| 7 | **W7-S13** [DOC] Wave close-out — LIVE BROWSER SMOKE (Playwright, rebuilt images, evaluate+data-testid), `/review-impl` with every bug FIXED, drift-lock proof, SESSION_HANDOFF, commit by file name | TODO | *DoD:* Playwright: clicked the `broken_canon_rule` row → quality-canon mounted **AND the focused rule hoisted to the top**; the 3 FE-1 rows are inert (a click mounts no tab); the lens shows all 8 source rows. `git diff --exit-code contracts/frontend-tools.contract.json` exits 0. `/review-impl` run, 0 open findings |

### Wave 8 — KG & world (21 slices) · `docs/plans/2026-07-13-studio-wave-8-kg-world.md`

> 🔴 **WAVE 8 ADDS FIVE PANELS, NOT TWO** — `world` · `world-map` · **`cast`** · **`character-arc`** ·
> **`canon-growth`**. The last three **home the orphaned legacy sub-tabs** and **FLIP Wave 6's three
> `{pending}` GG-4 rows**. **`N_BEFORE` is 71 (not 69) — Wave 6 ships 5 panels, not 3.** Assert the
> **DELTA (+5)**, never a literal. 🔴 **Wave 8 is the LAST wave: there is no next wave to defer a bug
> into. `/review-impl` findings are FIXED, full stop.**

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| 8 | **W8-0C** [CONTRACT] 🔴 🆕 **CONTRACT-FIRST — freeze the route contracts BEFORE any consumer.** ⚠ **`contracts/api/book-service/` DOES NOT EXIST** — this plan names it 3×. **The real path is `contracts/api/books/v1/openapi.yaml`.** **Do not create the wrong directory.** Covers the **18 new REST routes** (knowledge-service entity-kinds/projection/fact-invalidate + book-service worlds/maps/markers/regions/image). Plan §5 `W8-0C` | TODO | *DoD:* all 18 routes in the **correct** contract files with request + response + error codes (incl. PATCH's **428 without `If-Match` / 412 + the current row on stale**, the **byte-identical uniform 404**, and `{invalidated:false}` **200-on-a-miss, NOT 404**); `redocly lint` clean |
| 8 | **W8-00** [DOC] Track C ownership write-down — the 8b gate (**RESOLVED, not a stall**) | TODO | *DoD:* ownership write-down committed — SESSION_HANDOFF + Track C RUN-STATE both name **Wave 8** as the owner of the world container; Track C keeps workflow rack + binding UI + onboarding fork + reader. **Also: spec 38 `:465` OQ-9 is CORRECTED** (it says the legacy place-graph *"belongs to plan 30's Wave 6"* — **it does not; Wave 6 contains no place-graph row**) → replace with *"RESOLVED — won't-port; `D-WORLDMAP-PLACE-GRAPH-WONTPORT`"* |
| 8a | **W8-01** [BE] BE-14c + BE-14d — one authorable-kind set, **THREE schema sources** | TODO | *DoD:* knowledge pytest `<N>` passed. `test_authorable_kinds_one_home::test_rest_and_pydantic_and_json_schema_and_mcp_signature_all_agree` PASSED — all 4 surfaces expose `{character,concept,faction,location}`. `GET /v1/knowledge/entity-kinds` → 200. `KgCreateNodeArgs(kind='item')` now raises `ValidationError` |
| 8a | **W8-02** [FE] A1 — create an entity in `kg-entities` (BE-14c-fed `<select>`, anchor warning) | TODO | *DoD:* vitest `<N>` passed. `CreateEntityDialog` renders a 4-option `<select>` fed from the route (**no free text**, `KIND_OPTIONS` NOT reused); the glossary-anchor warning renders; an idempotent 201 does **not** claim a create; `useCreateEntity` invalidates knowledge-entities + -subgraph + -projects |
| 8a | **W8-03** [FE] A2 — create a relation: the **THIRD VERB** on `RelationEditDialog` + the unified `PredicateControl` | TODO | *DoD:* vitest `<N>` passed. 3 verbs on ONE dialog; `PredicateControl` used by create AND correct (**the free-text `<input>` at `RelationEditDialog.tsx:127` is GONE — asserted null**); empty-ontology + `!allow_free_edges` renders the schema-panel escape, not a dead `<select>`; `useCreateRelation` invalidates knowledge-subgraph; kg-graph edges open the dialog on right-click |
| 8a | **W8-04** [FE] A5 — kill the three **dead buttons** (`onArchive`/`onRestore`/`onDelete` = noop) | TODO | *DoD:* vitest `<N>` passed. `ProjectRow` renders 0 of 3 destructive buttons when its handlers are absent (4 cases); `OverviewSection`'s `const noop` is **gone** (asserted); restore goes through `updateProject` with `expectedVersion` (D-K8-03 asserted) |
| 8a | **W8-05** [BE] BE-14a + BE-14b — the two thin route mirrors (projection + fact-invalidate) | TODO | *DoD:* knowledge pytest `<N>` passed. `POST /projects/{id}/project-entities` returns all **SIX** counters ALWAYS-PRESENT (`nodes_conflicted:0`, `truncated:false` asserted on a zero result), 409 on a book-less project, gated on `require_project_grant(EDIT)`. `POST /facts/{id}/invalidate` returns **200 `{invalidated:false}` on a miss (NOT 404)** |
| 8a | **W8-06** [FE] A3 seed-from-glossary + A4 forget-a-fact + the `memory_*` Lane-B handler | TODO | *DoD:* vitest `<N>` passed. `SeedFromGlossaryCard` renders ALL SIX wire-named counters and hides itself when the project has no `book_id`; `FactRow`'s `{invalidated:false}` shows "already gone" and **NOT success**; `matchEffectHandlers('memory_forget').length > 0` (RegExp, not string); `kg_create_node` still matches (X-4 refutation pinned) |
| 8a | **W8-07** [TEST] `studio-kg-write.spec.ts` — the 8a **LIVE BROWSER** smoke | TODO | *DoD:* **live smoke:** 5 passed against the BAKED FE (**image rebuilt**) with claude-test@loreweave.dev — created an entity in kg-entities and it **survived a page reload**; drew an edge in kg-graph, reloaded, Mark-wrong'd it away from the canvas; seed-from-glossary rendered its counters; kg-overview's Delete opened a **real confirm dialog** (it was a silent noop before) |
| 8b | **W8-08** [BE] book-service: the migration + maps REST reads/create/delete (BE-15a/b/c/e) | TODO | *DoD:* `go test ./...` ok (`<N>` tests). Migration applied: `world_maps.version` + `map_markers.updated_at` + `map_regions.updated_at`. `GET /v1/worlds/maps/<uuid>` reaches `getWorldMap` and **NOT** `getWorld{world_id=maps}`. `marker_count`/`region_count` are `COUNT(DISTINCT)` — 2 markers + 3 regions reports **2 and 3, not 6 and 6**. A foreign map 404s with a body **byte-identical** to a missing one |
| 8b | **W8-09** [BE] book-service: **THE UPDATE SEMANTICS** (PATCH map/marker/region) + marker/region create+delete | TODO | *DoD:* `go test ./...` ok. UPDATE now exists: PATCH map (`If-Match` → **428 without / 412 + the current row on stale**, version bumps 1→2→3), PATCH marker + region (partial: absent=unchanged, **explicit null=clear**, `{}` = idempotent 200; `updated_at` strictly advances). Scope gate proven: a marker from map B addressed under map A returns 404 **AND leaves M-B unchanged in the DB** |
| 8b | **W8-10** [BE] BE-15f — the **PUBLIC (JWT)** image upload + retire the dead internal route | TODO | *DoD:* `go test ./...` ok. `POST /v1/worlds/maps/{id}/image` is PUBLIC and **JWT-gated** (a `?user_id` without a JWT is **401** — asserted); the dead `/internal/…/image` route is DELETED (`TestInternalMapImageRouteIsGone` asserts 404); webp uploads succeed with NULL dims (pinned) |
| 8b | **W8-11** [BE] BE-15m — 3 new MCP update tools (**agent parity**, GG-2 inverse gap #4) | TODO | *DoD:* `go test ./...` ok. `registerMapTools` registers **11** tools (8 + `world_map_update`/`_update_marker`/`_update_region`), all `world_`-prefixed (`mcp_prefix_contract_test` green). **The inverse gap is closed IN THE SAME WAVE that created it**: the human can move a pin and so can the agent. `x=0.0` is stored as 0.0, not treated as absent (pointer fields) |
| 8b | **W8-12** [FE] the `world` container panel + `studioLinks` worldId rule + GG-8 registration (+1) | TODO | *DoD:* three-way at N_BEFORE+1 (70). vitest `<N>` passed incl. panelCatalogContract + dockablePanelHygiene + studioLinks (**3 cases**: matching world ⇒ studio, different world ⇒ external, worldId ABSENT ⇒ external). `KgOverviewPanel` passes `worldId` into `followStudioLink` — the World backlink opens the dock tab instead of popping a browser tab. `WorldPanel` renders all 5 states incl. the foreign-world no-access card |
| 8b | **W8-13** [FE] the `world-map` panel (rail+canvas+inspector+drag/PATCH+image+entity picker) + `worldEffects` Lane-B + GG-8 (+1) | TODO | *DoD:* three-way at N_BEFORE+2 (71). vitest `<N>` passed. `WorldMapPanel` renders all FOUR bare-open degraded states (no-world **reuses** `LinkBookToWorldEmptyState` — not a fork; a 404 mapId falls back to the rail and does NOT show a different map). `useWorldMapDetail` **serializes** per-marker writes and **binds the debounce to its target marker** (both asserted). `worldEffects` matches the write verbs and NOT `world_map_get`/`_list`; `WORLD_WRITE_PATTERN` is a RegExp |
| 8b | **W8-14** [TEST] `studio-world-map.spec.ts` — the 8b **LIVE BROWSER** smoke (page.mouse drag + reload + bare-open degraded leg) | TODO | *DoD:* **live smoke:** 5 passed against the BAKED FE — created a map, uploaded a base image, placed a pin, **DRAGGED it with `page.mouse` (CDP-trusted)**, **RELOADED**, and the pin is at the new coords (**the PATCH landed, not a repaint**); a stale rename got a 412 with the current name; a chat turn's `ui_open_studio_panel {panel_id:'world-map'}` mounted the dock tab; the SAME tool on a book with NO world rendered the Link-this-book-to-a-world state, **not a blank panel** |
| 8b | **W8-16** [FE] 🆕 🔴 **the `cast` panel — `CastCodexPanel`, HOMED (+1).** ⚠ **NOT `kg-entities`** — that is a thin wrapper over a **FLAT, cross-project entity LIST with NO kind-grouping and NO spoiler-safe story-state join.** 🔴 **FLIPS Wave 6's `cast` pending row.** Plan §`W8-16` | TODO | *DoD:* `CastPanel.test.tsx` passed — rows **GROUPED BY KIND** with the spoiler-safe story-state join; `search` is a panel **PARAM** so the canon-growth deep-links land; `onViewArc` → `host.openPanel('character-arc',{entityId})`; **no book ⇒ an empty state, not a blank panel**; three-way at `N_BEFORE+3`. 🔴 **`legacyParityContract`: the `cast` PENDING row is FLIPPED to a string home and resolves — 2 pending remain** |
| 8b | **W8-17** [FE] 🆕 🔴 **the `character-arc` panel — `CharacterArcView`, HOMED (+1).** ⚠ **NOT `arc-templates`** (Wave 4 = structure-TEMPLATE library) and **NOT `arc-inspector`** (Wave 2 = the narrative-arc SPEC tree; `32_arc_inspector.md:75` says verbatim *"there is no legacy component to move"*). 🔴 **FLIPS Wave 6's `arc` pending row.** Plan §`W8-17` | TODO | *DoD:* vitest passed — `CharacterArcView`'s react-router `useNavigate` is **REPLACED by an injected `onOpenChapter`**, and **the router mock is asserted NOT called from the dock** (it would have **unmounted the whole studio** — DOCK-7); bare open renders pick-a-character + a link to `cast`, **not a blank panel**; three-way at `N_BEFORE+4`. 🔴 **`legacyParityContract`: the `arc` row is FLIPPED — 1 pending remains** |
| 8b | **W8-18** [FE] 🆕 🔴 **the `canon-growth` panel — `FlywheelPanel`, HOMED (+1).** ⚠ **NOT `quality-corrections`** — that is `CorrectionStatsTable` (**composition** correction *rates*); `FlywheelPanel` is **knowledge-graph growth** (`knowledgeApi.getFlywheel`, knowledge-service). **Two services, two datasets. The name collides; the thing does not.** 🔴 **FLIPS THE LAST pending row AND CLOSES THE GG-4 GATE OUT.** Plan §`W8-18` | TODO | *DoD:* vitest passed; three-way at `N_BEFORE+5` (**the FINAL count**); its 3 deep-links go through `host.openPanel` (an entity chip lands in `cast` **WITH the search param**). 🔴 **`legacyParityContract`: the LAST pending row is FLIPPED; `it.fails('GG-4: zero pending rows')` is CONVERTED to `it(…)` and PASSES — 25/25 sub-tabs have a real home or a reviewed retirement.** ⚠ **Green ≠ authorized: `ChapterEditorPage` is NOT deleted** (E-3 / `D-STUDIO-MOBILE-SHELL` + spec 16's Phase-4b *"kept indefinitely"*) |
| 8b | **W8-19** [TEST] 🆕 `studio-kg-codex.spec.ts` — the codex-trio **LIVE BROWSER** smoke | TODO | *DoD:* **live smoke:** 4 passed against the **BAKED, REBUILT** FE — `cast` mounted from a real agent `ui_open_studio_panel` turn and grouped by kind; a Cast row → `character-arc` opened **in the dock** (the router was NOT used); a `canon-growth` entity chip deep-linked into `cast` **with the search prefilled**; **the dock survived every hop** |
| 8 | **W8-15** [DOC] Wave close — `/review-impl` + fix every bug + SESSION_HANDOFF + plan-30 amendment | TODO | *DoD:* `/review-impl` on the Wave-8 diff — `<N>` findings, **ALL FIXED** (listed). Suites: knowledge pytest `<N>` · book-service `go test` ok · frontend vitest `<N>` · **playwright 14 passed** (studio-kg-write 5 + studio-world-map 5 + **studio-kg-codex 4**). Panels: **`N_BEFORE+5`, three-way** (the run's final count: **76**). 🔴 **`legacyParityContract`: ZERO pending rows — the GG-4 gate is mechanically GREEN for the first time** (deletion still blocked on E-3). SESSION_HANDOFF updated in the same commit, with the refuted defer rows moved to **Recently-cleared (they were BUILT)**. Plan 30 §5.3: G-KG-WRITE-HOLES + G-WORLD-MAPS marked **CLOSED**; §7 carries `D-WORLDMAP-PLACE-GRAPH-WONTPORT` as an explicit won't-fix |

### Wave T — Translation repair (23 slices) · `docs/plans/2026-07-13-studio-wave-T-translation-repair.md`

| wave | slice | status | EVIDENCE |
|---|---|---|---|
| T | **T-A1** [FE] Typed errors + the always-rendered shell (T4, T10, D9) | TODO | *DoD:* `TranslationTab.errors.test.tsx` 5 passed, `apiError.test.ts` 4 passed; full FE suite `N_fe_before+9` passed |
| T | **T-A2** [FE] The header **Translate…** CTA (T1, D1, D2) | TODO | *DoD:* `TranslationTab.cta.test.tsx` 4 passed; lint clean (Plus/AlertCircle removed) |
| T | **T-A3** [FE] One row per **CHAPTER**, left-joined onto coverage + pagination + orphan footnote (T2, D3, D4, D5) | TODO | *DoD:* `TranslationTab.matrix.test.tsx` 5 passed; `TranslationTab.badge.test.tsx` green after the row-shape update; `grep 'coverage.coverage' TranslationTab.tsx` → 0 hits outside `coverageByChapter`/`orphanCount` |
| T | **T-A4** [FE] The modal receives the **selection AND the language** (T8, D6) | TODO | *DoD:* `TranslationTab.preselect.test.tsx` 4 passed — the footer reads "Translate 2 selected" **ENABLED**; a fully-translated book yields an enabled force-retranslate |
| T | **T-A5** [FE] The modal stops wedging: pickers render immediately, AbortController + 15s timeout + inline retry, seed-once (T5, D7, D8) | TODO | *DoD:* `TranslateModal.degraded.test.tsx` 7 passed — pickers render **during** load; a 15s timeout → inline Retry; the language **survives a late settings GET** |
| T | **T-A6** [FE] The EDIT-grant **403 becomes a readable toast** (D10, first half) | TODO | *DoD:* 2 new assertions green — a 403 renders "view-only access", **never the raw message** |
| T | **T-A7** [FE] 🆕 🔴 **THE EDITOR STOPS BEING UNMOUNTED BY TRANSLATE WORKMODE** (closes **X1** — *SILENT DATA LOSS* — and **X3**). Switching to Translate workmode **conditionally unmounts a stateful component** ⇒ **the user's unsaved draft is destroyed** (**I-8**: never conditionally unmount — CSS `hidden`, never a ternary; and `editor-workmode-and-compose-must-keep-editor-mounted`). Plan §6 `T-A7` | TODO | *DoD:* frontend `<N>` passed incl. `test_switching_to_Translate_KEEPS_the_editor_MOUNTED_and_the_draft_INTACT` (**RED before, GREEN after — both runs pasted**); the switch is CSS `hidden`, not a ternary (asserted); **LIVE BROWSER:** typed an unsaved draft, switched to Translate, switched back — **the draft is still there** (at HEAD it is gone) |
| T | **T-B1** [FE] `ChapterTranslationsPanel` stops degrading silently (T6) | TODO | *DoD:* `ChapterTranslationsPanel.error.test.tsx` 3 passed — service-down renders a **banner**, not a blank panel with "??" language |
| T | **T-B2** [FE] The version list refreshes after a human edit (S4) | TODO | *DoD:* S4 — a saved human edit **renders in the sidebar** without a manual reload (assert the **rendered list**, not that a callback fired) |
| T | **T-B3** [FE] Add-a-language affordance + **ONE** `translation-versions` dock id (T7, D11, S10) | TODO | *DoD:* VersionSidebar 2 passed, EditorPanel D11 1 passed (**the openPanel arg contains no `:`**); `panelCatalogContract.test.ts` green with **DELTA 0** |
| T | **T-B4** [FE] The **paid-job poll** can no longer spin forever (S1) | TODO | *DoD:* `useGlossaryTranslatePolling` 3 passed, `StepProgress` 2 passed — the paid-job spinner **terminates** and shows Retry; `onComplete` is **NOT** called on a poll error |
| T | **T-B5** [FE] The remaining swallowed errors (S2, S3, S5, S6, S8) | TODO | *DoD:* 7 new tests passed; **no `.catch(()=>null)` without a rendered consequence remains** in `features/translation/**` |
| T | **T-B6** [FE] 🆕 **The agent's confirmed `resume`/`retry` refreshes the SEGMENT grids too** (closes **S11** — 🔴 **ONE LINE, not a defer row**). `translationEffects.ts`'s pattern doesn't match the agent's `resume`/`retry` (they dispatch via `confirm_action`). ⚠ **This RETIRES `D-TRANSL-S11-JOBCONTROL-EFFECTS`** — a one-line fix is cheaper than its defer row (CLAUDE.md: *"if fixing the bug is cheaper than writing + carrying its defer row, just fix it"*). Plan §6 `T-B6` | TODO | *DoD:* frontend `<N>` passed incl. `matchEffectHandlers` matching the `confirm_action` envelope for `resume`/`retry`; the segment grid refreshes with **no manual refetch**; ✅ `D-TRANSL-S11-JOBCONTROL-EFFECTS` **cleared, not carried** |
| T | **T-C1** [CONTRACT] The **language registry** becomes a real, machine-checked contract (D13 part 1) | TODO | *DoD:* `test_languages_parity.py` 5 passed ("Vietnamese" rejected, "VI"→"vi", "zh_CN"→"zh-CN"); `languages.contract.test.ts` 1 passed; `contracts/languages.json` committed (18 codes) |
| T | **T-C2** [BE] Normalize → validate on all **SEVEN** REST write edges (D13 part 2) | TODO | *DoD:* `test_language_validation.py` 5 passed — "Vietnamese" → 422/400, "VI" → "vi", **all 7 write models guarded by the iterating drift test** |
| T | **T-C3** [BE] The MCP `target_language` arg becomes a real **enum** (D13 part 3 — closes the closed-set⇒enum breach, I-7) | TODO | *DoD:* `test_mcp_language_enum.py` 4 passed — the **LIVE `inputSchema`** for `translation_start_job` advertises an 18-value enum; `get_args(TargetLanguage) == TRANSLATION_TARGET_CODES`; the 2 row-anchored tools stay free-string |
| T | **T-C4** [FE] All **FIVE** language inputs consolidate onto `LanguagePicker` + `TRANSLATION_TARGETS` (D13 frontend half; closes S7) | TODO | *DoD:* 5 language inputs consolidated; `LanguagePicker` 2 passed; `TranslationTab.legacy.test.tsx` 1 passed (**a legacy "Vietnamese" column still renders** — D13 constrains writes, never reads) |
| T | **T-C5** [FE] `access_level` gates the translate affordance **WITH A REASON** (T9, D10 second half) — **ZERO backend work** | TODO | *DoD:* `TranslationTab.grants.test.tsx` 6 passed, `grants.test.ts` 6 passed — "view" **disables-with-reason**; edit/manage/owner ALL enable; `git diff --stat services/book-service` is **EMPTY** (zero Go changed) |
| T | **T-C6** [FE] The last DOCK-9 leftover + the dead button (S9, S12) | TODO | *DoD:* `ConfirmNameDialog` 1 passed, `GlossaryTranslateWizard` S12 1 passed, `dockablePanelHygiene.test.ts` GREEN |
| T | **T-C7** [FE] 🆕 **The unbounded coverage payload gets an opt-in projection** (closes **X5**; the Phase-C half of `UC-29-COVERAGE-IS-UNPAGED`). Plan §6 `T-C7` | TODO | *DoD:* frontend `<N>` passed; the coverage read is **projected/paged**, and the projection is **opt-in** (no behavior change for existing callers — asserted) |
| T | **T-C8** [FE] 🆕 🔴 **The legacy language's THREE dead ends become LEGIBLE** (closes **X4** — 🔴 **A PAID-ACTION DEFECT**: a legacy free-text language (`Vietnamese`) drives a **retranslate that can never succeed** — *the user pays and gets nothing*). ⚠ **This is the §8 CRITICAL class #4 — but it is being FIXED here, so it is a BUILD, not a stop-and-ask.** Plan §6 `T-C8` | TODO | *DoD:* frontend `<N>` passed; each of the 3 dead ends renders **why it cannot proceed** and offers the migration path; **no paid job can be launched against a legacy free-text language** (asserted: `fetch` is **never called**) |
| T | **T-C9** [FE] 🆕 **The i18n backfill + the parity test that keeps it from rotting.** ⚠ **`scripts/i18n_translate.py` GAP-FILLS ONLY** — it **adds** keys, never **edits** them, so an edited `en` string leaves 17 stale locales **forever**, and a SOFT untranslated flag **does not self-heal**. Plan §6 `T-C9` | TODO | *DoD:* 18 locale files carry every new key; the **parity test** reds on a missing key in any locale; no key was hand-written |
| T | **WT-CLOSE** [DOC] 🔴 **WAVE CLOSE** — run the wave-T plan's §8 WAVE DEFINITION OF DONE (its "PHASE C: LIVE-SMOKE GATE + /review-impl + WAVE CLOSE"): full suites green · **live browser smoke in BOTH states** on a REBUILT image · **`/review-impl` with every finding FIXED** · SESSION_HANDOFF · commit by enumerated path | TODO | *DoD:* `/review-impl wave-T-translation-repair` run on the wave diff; `<K>` findings, **ALL FIXED** (listed); frontend `<N>` + translation-service `<N>` re-green after the fixes; **`panelCatalogContract` green with DELTA 0** (Wave T adds NO panel and must never touch `catalog.ts` / `frontend_tools.py` / the contract JSON — `git diff --exit-code contracts/frontend-tools.contract.json` exits 0); SESSION_HANDOFF in the same commit |

---

## 6 · THE REGISTERS — append as you go. They make the final audit a byproduct.

### 6.1 · DECISIONS — every non-obvious call made mid-run, with its why

| # | Wave/slice | Decision | Why |
|---|---|---|---|
| D-001 | — | *(append here)* | |

### 6.2 · PARKED — blockers hit and DEFERRED (the run CONTINUES; only a §8 CRITICAL stops it)

Every row: **ID · wave/slice of origin · what · the gate reason (CLAUDE.md gates 1–5) · target wave/trigger.**
A defer row is **never a silent drop.** Mirror every row into `docs/sessions/SESSION_HANDOFF.md`
Deferred Items at the wave's close.

| ID | Origin | What | Gate (1 out-of-scope · 2 large/structural · 3 next-phase · 4 blocked-external · 5 won't-fix) | Target |
|---|---|---|---|---|
| *(none yet)* | | | | |

> Pre-declared escape hatches from the wave plans (write the row and **CONTINUE**, do not stop):
> - `D-BE8-APPLY-ARC-TO-SPEC` — W4-BE8b, if it needs a schema change or a `plan.py` refactor (gate #2).
> - 🔴 `D-MOTIF-APPLICATION-MULTI-ROW` — **Wave 3 / `3a-3`, if the §2 G5 back-fill probe returns ROWS.**
>   The one-row-per-node invariant is then FALSE, the UNIQUE index cannot build, and M-BUG-2/M-BUG-3
>   are real. **SKIP ALL of `3a-3`** (both the index *and* the collapse-4-predicates-to-1 — the collapse
>   assumes the invariant) and **CONTINUE to `3a-2`.** Gate #2 (large/structural: it needs a de-dup
>   migration + a product rule for which row wins). **This is NOT a stop-and-ask** — a false *spec
>   invariant* is not a PO-1..4 *sealed decision* (§8). Nothing else in Wave 3 depends on `3a-3`.
> - `D-X10-ANC2-SCENT` — W0-S11, if the build-time re-verify finds Track C's 4 files dirty (gate #4).
> - `D-<NAME>-LIVE-SMOKE` — any slice where the full stack genuinely will not boot (I-12).

### 6.3 · DEBT — shortcuts consciously taken

| ID | Wave/slice | The shortcut | The cost if never paid | Trigger to pay |
|---|---|---|---|---|
| *(none yet)* | | | | |

### 6.4 · DRIFT — near-misses, bars nearly lowered, tests nearly skipped

> ⚠ **CLAUDE.md: "A run that ends with an empty drift log is not clean — it is DISHONEST."**
>
> Record the near-misses. Every one. Examples of what belongs here: *"almost accepted a green unit
> suite as proof of a behavior it never exercised"* · *"almost marked a slice DONE whose live-smoke
> never ran"* · *"almost skipped `/review-impl` because the wave felt small"* · *"almost asserted the
> literal panel count instead of the DELTA"* · *"almost wrote `git add -A`"* · *"almost deferred a
> route as 'blocked' when I would just have had to write it (I-14)"* · *"almost let a `pytest`
> integration test count as passing when it was SKIPPED"*.

| # | Wave/slice | The near-miss | What caught it | What I did instead |
|---|---|---|---|---|
| *(none yet — and if this table is still empty at the end of the run, that is a RED FLAG, not a gold star)* | | | | |

---

## 7 · WAVE CLOSE-OUT CHECKLIST (run this at the end of EVERY wave — all 10)

1. Full suites green for every service the wave touched (`-n auto --dist loadgroup` for Python).
2. **LIVE BROWSER / cross-service smoke** on a **REBUILT** image (I-12). Verify by **EFFECT**.
3. **`/review-impl` on the wave diff** — every finding **FIXED before the wave closes** (I-11).
4. Re-run the suites after the review fixes. (A green suite proves the **working tree**, not the commit.)
5. Update **this file's slice board** — status DONE + the **actual** evidence string (overwrite the DoD).
6. Append to the **registers** (§6) — decisions, parked, debt, **drift**.
7. Update `docs/sessions/SESSION_HANDOFF.md` (NEXT block + Deferred Items) — **in the same commit as the code**.
8. **COMMIT by enumerated file name — NEVER `git add -A`** (I-10). Check `git diff --cached --stat` first.
9. Continue **immediately** to the next wave. A commit is a checkpoint, **not** a session boundary.
   Do **not** suggest restarting, compacting-as-a-stopping-point, or "wrapping up".

---

## 8 · THE CRITICAL-STOP LIST — the ONLY 4 things that stop the run and ask the human

**Everything else — a missing route, an awkward refactor, a failing third-party thing, an ugly seam,
a test that needs a new fixture, an unbuilt module — is a DEFER ROW + CONTINUE (§4.3).**

1. **A destructive / irreversible action** — data loss, or a migration that drops or rewrites user rows.
2. **A sealed decision proven WRONG by the code** — **plan 30 §0, PO-1..4, and NOTHING ELSE** (§3 of this
   file). ⚠ **"Sealed decision" means EXACTLY those four PO rulings.** A *spec's internal invariant*, a
   plan's assumption, an audit's claim, or a slice's premise turning out to be false is **NOT** a sealed
   decision — it is a **wrong plan**, which is a **DEFER + CONTINUE**, not a stop. *(This exact
   mis-mapping was caught in QC: Wave 3's `3a-3` back-fill probe was labelled "critical category #2" and
   would have STALLED THE UNATTENDED RUN at 3am over a UNIQUE index that simply cannot build. The
   correct move is: park `D-MOTIF-APPLICATION-MULTI-ROW`, skip the slice, keep going.)*
3. **A tenancy / security breach** — cross-user data exposure (I-5).
4. **A paid-action defect that would charge the user for nothing** — this repo just shipped one: the
   motif-mine confirm **500s after burning the token and reserving the hold**, so the user pays and the
   job is never even created (that is `W0-BE1` / `W0-S7`; C-1). ⚠ **Note this class is about SHIPPING
   one, not about ENCOUNTERING the known one** — `W0-BE1` is the *fix* for the known one. **Build it.
   Do not stop and ask about it.**

If it is not one of these four: **write the defer row and KEEP GOING.** Do not stop. Do not ask.
**A plan being wrong is not a stop. A test needing a fixture is not a stop. A missing route is not a
stop — it is a route you WRITE (I-14).**

---

## 9 · RESUME PROTOCOL — after ANY compaction, in this exact order

1. **Re-read THIS file** — `docs/plans/2026-07-13-studio-tool-gui-RUN-STATE.md`. First. Before anything.
2. `git log --oneline -15` — what actually landed.
3. The **current wave's plan file** — `docs/plans/2026-07-13-studio-wave-<N>-*.md`.
4. **Continue at the first non-DONE slice on the board (§5).**

**Never re-derive state from memory. Never re-litigate a sealed decision from memory. Re-read it.**
Context is lossy; this file is not.
