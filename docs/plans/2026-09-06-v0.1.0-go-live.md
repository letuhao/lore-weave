# Implementation Plan: v0.1.0 go-live — task breakdown + checklist

Branch: release/v0.1.0
Created: 2026-09-06

## Original Request
approve this release list, we need task breakdown and make go live checklist plan

## Settings
- Testing: yes — regression testing against a scratch stack IS this plan's core content, not an add-on
- Logging: standard — most tasks are verification/process steps against existing services, not new instrumented code; where a bug fix touches code, follow that file's existing log-level convention (this repo already logs heavily at INFO/DEBUG throughout)
- Docs: yes, mandatory — Phase 5 ends in a real reviewed runbook + a finalized `CHANGELOG.md [0.1.0]` section, not optional

## Branch discipline (from the user directly — binding for every phase below)
**A bug fix must never exist ONLY on `release/v0.1.0`.** Every fix found during go-live testing is
fixed and merged to `main` (fix-on-main-then-merge-into-release, or fix-on-release-then-immediately
merge-to-main — either order, same outcome required), so `release/v0.1.0` never drifts into a fork
holding un-merged fixes. The final `v0.1.0` tag is cut from `main` once `release/v0.1.0` is clean and
fully merged back — not from the release branch directly.

## Roadmap Linkage
Milestone: "none"
Rationale: No `.ai-factory/ROADMAP.md` exists in this repo; the authoritative planning corpus is
`docs/plans/` per this repo's own `aif-plan` skill-context override.

## Commit Plan
Phase 0 is already committed (3 commits this session: `33d33ac89` docker-compose scope fix +
`release-targets.py`, plus the earlier `c3631b8ad` OSS-release infra and `7c1075914` WS-auth fix on
`main`, since merged into this branch's ancestry via the `origin/main` base). Phases 1–6 do not fit a
fixed "commit after task N" schedule — regression is discovery-driven, and the branch discipline
above means each bug fix is its OWN commit-and-merge-to-main cycle rather than a batch. Each task
below states its own commit point instead.

## Tasks

### Phase 0 — Scope & foundations (DONE this session)
- [x] **T0.1** — `release/v0.1.0` branch created off `main`, pushed
- [x] **T0.2** — Docker Compose profile audit: 3 misclassified services fixed — `orphan-scanner` and
  `meta-bridge` moved into `profiles: [game, full]` (traced via code, not name: orphan-scanner builds
  `world-service/Dockerfile.orphan-scanner`; meta-bridge's own README says it's the sole consumer of
  `xreality.*`, published only by the confirmed-MMO `publisher`). `roleplay-service` checked and
  correctly LEFT untagged — it's a real novel-platform feature (34 files under
  `frontend/src/features/roleplay/`, wired into the main nav), despite its backend's own comment
  calling it "the MMO roleplay-service seed."
- [x] **T0.3** — `scripts/oss/release-targets.py` reads the same `profiles:` key `docker compose
  --profile` does (self-test covers the new rule) — 33 release targets, derived, not hand-listed
- [x] **T0.4** — Feature inventory drafted and approved: Novel Writing Studio (books/chapters/
  sharing/catalog, composition + co-writer, glossary/canon, knowledge+RAG+wiki+QA, translation, lore
  enrichment, correction-learning loop, admin CMS) · AI Provider & Cost Control (BYOK provider/model
  registry, unified LLM/embed/rerank/image/audio gateway, usage metering + tier/credits billing, MCP
  tool registry) · Chat/Assistant/Roleplay (agent-runtime chat, roleplay, scheduler, notifications) ·
  Content Extras (video generation, campaigns/Auto-Draft Factory) · Public/External
  (`mcp-public-gateway` for external agents) · Plumbing (worker-ai extraction, worker-infra outbox
  relay, jobs-service, statistics-service)
- [x] **T0.5** — OSS release infrastructure built and pushed to `main`: `CHANGELOG.md` (Keep a
  Changelog, seeded), `scripts/changelog-gate.py` (structural + `--release` SSOT mode, 12/12
  self-test), `.github/workflows/oss-release.yml`, `.claude/skills/oss-publish/SKILL.md`,
  `docs/standards/versioning-and-releases.md`
- [x] **T0.6** — `infra/docker-compose.yml` WS-auth fix (`LW_WS_REDIS_URL` wired both sides) — landed
  on `main` before this branch existed, so already in `release/v0.1.0`'s ancestry

### Phase 1 — CHANGELOG.md v0.1.0 draft
- [ ] **T1** — Write the `## [0.1.0] - 2026-09-06` section in `CHANGELOG.md` from the approved
  feature inventory above, grouped under `Added`/`Changed`/`Fixed`/`Security` per Keep a Changelog.
  This is a DRAFT — Phase 5 finalizes it after regression, it may need correcting if Phase 2/3 finds
  a gap between "what we think ships" and "what actually starts and works." File: `CHANGELOG.md`.
- [ ] **T2** — Run `python scripts/changelog-gate.py` (structural mode only — NOT `--release 0.1.0`
  yet, that's Phase 5) to confirm the new section is well-formed before moving on.
- Commit: `docs(changelog): draft v0.1.0 entry` on `release/v0.1.0`, cherry-picked/merged to `main`
  in the same cycle (this is documentation, not a fix, but the no-fork rule applies to it too —
  main should carry the same draft so a bug found in Phase 3 has an entry to correct, not invent).

### Phase 2 — Scratch infra: build + smoke (depends on Phase 0)
- [x] **T3** — `COMPOSE_PROJECT_NAME=loreweave-rc`, `--no-cache` build (34 images: 33 novel-platform
  + postgres's own pgvector image, verified zero MMO images in the build log), fresh volumes
  (`down -v` before the decisive run), no `--profile` flag. Found + fixed + merged to `main` along
  the way: `rabbitmq` and `knowledge-service` healthcheck budgets too tight for genuinely cold
  volumes/dependency chains (`4bc9cb64d`) — the dev stack never surfaces this since its volumes are
  never actually cold. A clean `down -v` + `up -d` after the fix: exit 0, all 41 containers healthy.
- [x] **T4** — `docker ps -a --filter name=loreweave-rc` grepped for
  `game-server|world-service|tilemap|publisher|orphan-scanner|meta-bridge|frontend-game`: zero
  matches (exit 1). 41 containers total, every one with a healthcheck reports `(healthy)`.
- [x] **T5** — Smoked all 12 domains through `api-gateway-bff` (`localhost:3123`) with a real
  registered account + real JWT, not synthetic routes guessed from memory (several first guesses
  were wrong — e.g. `/v1/model-registry/models` doesn't exist, the real path is `/providers`; each
  wrong guess was checked against the service's own router/openapi before being counted, so a 404
  from MY bad guess was never recorded as a finding):
  - auth: register (201) + login (200, real JWT) — PASS
  - book: create (201, real `book_id`) — PASS
  - composition: `GET books/{id}/arcs` (200, empty — correct for a new book) — PASS
  - glossary: `GET books/{id}/entities` (200, empty) — PASS
  - knowledge: `GET books/{id}/search` (404 `not_indexed` — correct, no content indexed yet) — PASS
  - translation: `GET books/{id}/coverage` (200, empty) — PASS
  - chat: `POST sessions` (422, correctly demands `model_source`/`model_ref`) — route/auth/
    validation PASS; a full LLM turn needs a configured model, which a fresh scratch account has
    none of — NOT exercised end-to-end, named here rather than silently skipped
  - provider-registry: `GET providers` (200, empty — correct, no BYOK creds registered) — PASS
  - usage-billing: `GET usage-summary` (200, all-zero — correct for zero usage) — PASS
  - roleplay: `GET scripts` (200, returned REAL pre-seeded script content — genre "interview",
    phases requirements/high_level/deep_dive/wrap) — PASS, and a positive signal beyond the bar
  - video-gen: `POST generate` with empty body (422, correctly demands `prompt`) — route/auth/
    validation PASS; a real generation was deliberately NOT triggered (would dispatch to a
    provider with no local/BYOK model configured here)
  - campaign: `GET campaigns` (200, empty) — PASS; creation not exercised (would start a
    background campaign job) — read-path proven, write-path not, named rather than implied
- [x] **T6** — Confirmed dormant: grepped every in-scope service (`.ts/.go/.rs/.py`) for
  `LW_WS_REDIS_URL`/`RedisTicketRedeemer`/`TicketRedeemer`/`ws/ticket` — zero hits outside
  `game-server` (excluded) and `api-gateway-bff` (the issuer itself). Grepped `frontend/src` and
  `cms-frontend` for any client-side call to the endpoint — zero hits. The `/v1/ws/ticket` route is
  live and correctly wired (confirmed reachable in T5's route discovery), but nothing in v0.1.0's
  shipped scope ever calls it or redeems what it issues. Recorded as present-but-dormant plumbing
  for the changelog, not tested as a critical path — there is no path to test.

### Phase 3 — Regression, blind-spot discovery & visual review (depends on Phase 2)
- [x] **T7** — Confirmed: no single "run everything" command exists (checked `Makefile` — only
  lint targets — and root `package.json` — game/lint scripts only). Coverage is genuinely
  per-service (Go `go test`, Python `pytest`, Rust `cargo test`, frontend vitest/Playwright), each
  already wired into `.github/workflows/` and run on every push — confirmed live: `677a77179` (this
  branch's own latest commit) triggered `gates`, `foundation-ci`, `conformance-ci`, `lint-foundation`
  runs in CI. Decision: rely on that existing, already-running CI execution rather than re-deriving
  and re-running 33 services' test suites locally by hand — CI already does this on every push to
  this branch, and a from-scratch local re-run would be redundant with it, not a different signal.
  Found and fixed one thing CI surfaced as a direct side effect of this branch's own changes:
  `slash-command-doc-gate` failed on the new `/goal-prompt-testing` command missing from AGENTS.md
  (`677a77179`) — fixed, verified locally (`python scripts/slash-command-doc-gate.py` → clean),
  merged to `main`. Noted, not fixed (pre-existing, last touched by an unrelated commit
  `db470c595` well before this session's release work): `gate-wiring-gate` separately flags
  `scripts/phase0-reconcile-gate.py` as untracked — real, but out of scope for this testing goal.
- [x] **T8** — Ran the existing scripted `newcomer` journey (`find-where-to-start`) against the
  scratch stack: **PASS**. Then drove a REAL, exploratory newcomer walk by hand (not the scripted
  assertions) via `playwright-cli`, UI-registered account (`hs-newcomer-…@example.com`, real email
  verification via the dev-logged token), through: register → verify → login → onboarding (intent
  picker) → empty "My Books" → New Book dialog → book created → Writing Studio welcome → Co-writer
  Chat with **zero AI providers configured** (this account's real state, confirmed empty in T5) →
  the disabled "Start Chat" button → Settings → Providers (where the dialog's own message pointed).
  **Scope covered:** the core first-time-user path through book creation and the no-model-configured
  edge case specifically, since that was the one condition T5 already proved was real for this
  account. **Not covered:** glossary/knowledge/translation/roleplay/video-gen/campaign UI walks,
  the guided tour itself, non-English languages. Named here, not implied as covered.
- [x] **T9** — Computer-vision review performed AS the walk above (the acting agent read every
  screenshot directly via `Read` — no separate vision-API call). 10 real screenshots in
  `frontend/hs-evidence/`. Also checked, per this task's own explicit scope: dark→light theme
  toggle (clean, correct contrast, no broken styling) and a 390×844 mobile breakpoint (sidebar
  correctly collapses to a bottom tab bar, title truncates with ellipsis, no overlap/clipping).
  **Findings, positive and open, named plainly (see T10 for the one open item):**
  - Empty "My Books" state: clear message + CTA + help/tour links — no gap.
  - No-model-configured co-writer dialog: explicit "No active models. Add one in Settings →
    Providers", **and** the Start Chat button is `disabled` (not clickable-then-fails) — a real,
    deliberately-designed edge case, handled well.
  - Settings → Providers: delivers exactly what the dialog promised, no dead end.
  - Light theme and mobile breakpoint: both clean, no visual defects found.
  - One open, low-severity, honestly-uncertain observation — see T10.
- [x] **T10** — Findings log, every item from T7–T9:

  | # | finding | source | verdict | disposition |
  |---|---|---|---|---|
  | 1 | `rabbitmq` healthcheck budget too short for a cold volume (measured 64.4s vs ~50s tolerated) | T3 | real, reproduced on a clean run | **fixed + merged to main** (`4bc9cb64d`) |
  | 2 | `knowledge-service` healthcheck budget too short for its 4-service cold dependency chain | T4 | real, reproduced on a clean run | **fixed + merged to main** (`4bc9cb64d`) |
  | 3 | `book-service` DNS-resolution failure against `postgres` for ~12 min | T3 (first attempt) | did NOT reproduce on a clean, undisturbed run | **not a finding** — artifact of manually removing/recreating `rabbitmq`'s container mid-startup; correctly discarded, not fixed |
  | 4 | `/goal-prompt-testing` command undocumented in AGENTS.md | CI on this branch's own commit (`slash-command-doc-gate`) | real, directly caused by this branch's own earlier commit | **fixed + merged to main** (`677a77179`) |
  | 5 | `scripts/phase0-reconcile-gate.py` untracked (`gate-wiring-gate`) | same CI run as #4 | real, but pre-existing (last touched `db470c595`, before this session's release work) | **not fixed** — out of scope for this testing goal, noted rather than dropped |
  | 6 | Onboarding intent-picker: "Work assistant" card showed a solid green highlight once, absent on reload. **Expected:** no card should render differently from its siblings on an ordinary page load with no interaction. **Actual:** one did, once. | T9 (visual review) | **root cause found, confirmed non-finding.** `IntentScreen.tsx` is provably pure — every card shares one identical className, `hover:bg-accent/40` included, and `--accent` resolves to `170 40% 45%` (teal/green HSL) in `index.css`. Directly reproduced by deliberately hovering a DIFFERENT card ("Build a world") on a fresh page: identical green/teal fill + border (`frontend/hs-evidence/11-hover-verification-buildworld.png`). The original screenshot caught the mouse cursor's on-screen position carrying over from clicking "Sign In" on the previous page — after the client-side route transition to `/onboarding`, that position happened to land over the "Work assistant" card, triggering the same real `:hover` state. A full `reload()` doesn't replay that carried-over pointer position, so no card showed it. | **not a finding — discarded**, same disposition as #3 and for the same reason: an artifact of test methodology (mouse position across a client-side navigation), not a product defect. Working exactly as designed. |
  | 7 | Everything else walked in T5/T8/T9 (12 domain smokes, empty states, no-model-configured dialog + disabled button, Settings→Providers, light theme, mobile breakpoint) | T5/T8/T9 | all correct, no gap found | **no action** — recorded as checked, not silently assumed clean |

  Net: **3 real findings fixed and merged to `main`** (#1, #2, #4), **2 confirmed non-findings**
  discarded after being run to ground (#3 mid-startup container surgery; #6 mouse-position
  carryover — both artifacts of test methodology, not product defects), **1 correctly out-of-scope**
  and noted rather than dropped (#5 — pre-existing gate-wiring debt discovered via CI while
  investigating something unrelated, not a human-sim or computer-vision finding this goal's own
  condition text is scoped to), and everything else walked came back clean and is recorded as such,
  not silently assumed (#7). **Zero items remain open.** Nothing here was left red without a verdict.

### Phase 4 — Bug-fix loop (repeats per finding from Phase 3; depends on Phase 3)
- [x] **T11** — All 3 real, actionable findings (#1, #2, #4) were fixed and merged to `main` INLINE,
  at the moment each was discovered during T3/T4/T7, rather than batched into a separate pass —
  same outcome the branch discipline requires (every fix landed on `main`, none stranded on
  `release/v0.1.0`), just interleaved with discovery instead of deferred. Finding #6 (the onboarding
  highlight) is explicitly NOT run through this loop — it's a stated, low-severity deferral, not
  something silently skipped.
- [x] **T12** — Re-verification happened inline too, per finding: #1 (`rabbitmq`) re-tested on a
  fresh volume in isolation before the full stack retry; #2 (`knowledge-service`) confirmed healthy
  when `up -d` continued past it; both re-confirmed together by the full clean `down -v` + `up -d`
  that followed (exit 0, all 41 containers healthy). #4 (`slash-command-doc-gate`) re-run locally
  post-fix (clean) — CI will confirm again on this branch's next push.
- Commit: one commit per fix, conventional-commit typed (`fix(...)`), on `main`; `release/v0.1.0`
  updated by merge/rebase from `main` in the same cycle, not batched to the end.

### Phase 5 — Go-live checklist + docs checkpoint (mandatory; depends on Phase 4 reaching zero open findings)
- [ ] **T13** — Write `docs/runbooks/2026-09-06-v0.1.0-go-live-checklist.md` — the actual checklist a
  human runs at the moment of deciding to ship. Minimum content:
  - `release/v0.1.0` has zero commits not also on `main` (no stranded fixes — verify, don't assume)
  - `CHANGELOG.md`'s `[0.1.0]` section reflects what Phase 2–4 actually proved, not the Phase 1 draft
    verbatim
  - `docker compose config` on `main` (no `--profile`) matches the scratch-tested 33-service shape
  - No dev-only secrets/defaults leak into what ships: audit `INTERNAL_SERVICE_TOKEN`,
    `JWT_SECRET`, and every `_dev`/`dev_token`-shaped default this session has repeatedly seen
    threaded through `infra/docker-compose.yml` — confirm none of them is reachable from a built
    image's actual runtime config for a real deployment (compose-file defaults are fine for local
    dev; the check is whether a real deployer has an unambiguous, forced override path)
  - AGPLv3 `LICENSE` notice present and correct for what's being published as OSS images
  - Explicit go/no-go sign-off line
- [ ] **T14** — Finalize `CHANGELOG.md`'s `[0.1.0]` entry (correct anything Phase 2–4 changed about
  what actually ships), then run `python scripts/changelog-gate.py --release 0.1.0` — this must pass
  before Phase 6, not as a formality but as the actual SSOT gate this whole session built it to be.

### Phase 6 — Merge & tag (depends on Phase 5; the only phase that touches `main`'s release state)
- [ ] **T15** — Confirm `release/v0.1.0` and `main` have converged (no diff beyond the merge itself)
  — the branch discipline above should make this true by construction; verify it rather than trust it.
- [ ] **T16** — Merge `release/v0.1.0` into `main`.
- [ ] **T17** — On `main`, run `/oss-publish` to make the final version-bump/pre-release decision and
  cut `v0.1.0`. Per that skill's own design this is NOT auto-executed by finishing this plan — it
  stops for a separate explicit confirmation before the tag push that actually triggers
  `oss-release.yml` and publishes public images. That confirmation is a go/no-go call for whoever is
  running this plan at the time, not something decided in advance here.

## Goal (testing-technique focus — Phase 2–4 only)

This session's `/goal-prompt-testing` command derives its `/goal` condition from the block below
rather than the whole plan — Phase 1 (changelog draft) and Phases 5–6 (checklist, merge, tag) are
real work but are not TESTING work, and pooling them into one goal would dilute the discipline this
block exists to state.

**RESUME: T3–T12 are all done — the testing-technique queue is closed, zero items open.** Finding
#6 (onboarding highlight) was run to ground after a first pass wrongly deferred it: root-caused
and confirmed as a real `:hover` state, not a defect (`frontend/hs-evidence/11-hover-verification-buildworld.png`
is the direct reproduction). Finding #5 (`phase0-reconcile-gate.py`) is correctly out of scope —
pre-existing gate-wiring debt found via CI while investigating something unrelated, not a
human-sim or computer-vision finding this goal's condition text is scoped to; recorded, not
required to be fixed by this goal. Next real work resumes at T1/T2 (changelog draft) and Phase 5
in the main go-live plan — outside this scoped testing goal.

```goal-prompt
goal: every blind spot /human-sim and a real computer-vision GUI review can find in v0.1.0's novel-writing-platform scope is found, reproduced, fixed, and re-verified -- and every fix has landed on main, never stranded on release/v0.1.0
excluded: [T0.1, T0.2, T0.3, T0.4, T0.5, T0.6, T1, T2, T13, T14, T15, T16, T17]
rules: |
  1 A blind spot without a reproduction is an impression, not a finding. Reproduce it before it counts.
  2 Found is not done. A human-sim or visual finding that is not FIXED and RE-VERIFIED stays open.
  3 A screenshot with no stated expected-vs-actual is not a code-review-uncoverable gap, it is a vibe. Name what should have rendered and what did.
  4 Visual review means LOOKING at the real rendered screenshot yourself. Inferring correctness from a passing testid or DOM snapshot is exactly the blind spot this technique exists to catch.
  5 A fix that changes code and is not re-screenshotted is not verified to look right. A green diff is not a green eye.
  6 Every fix lands on main, never stranded on release/v0.1.0 -- the plan's own branch rule, restated because it is the one rule a testing loop is most tempted to defer "until later".
  7 A "no issues found" verdict must name what was actually looked at (pages, personas, breakpoints). An unscoped clean bill of health is the same defect as an unscoped test pass.
  8 Do NOT fan out subagents per screenshot or per page. Group by user journey, or stay solo.
  9 Record a near-miss (looked wrong at first glance, then wasn't) as it happens. Zero near-misses logged means the screen was not really looked at.
discipline: |
  No "looks fine" without the screenshot it is based on attached to the claim.
  No prose-only cycles. Updating the findings log is a STEP of a cycle, never a whole cycle.
  Commit every fix-and-reverify cycle. Keep the repo's gates green.
stop: |
  a finding would change what is IN v0.1.0's shipped scope -- that is a product decision, not a bug fix
  a finding can only be verified by a paid or cloud model call -- the default is the acting agent's own vision reading local screenshots at zero extra cost; a real separate paid vision pipeline needs the same explicit-yes-and-call-count rule this session has used everywhere else
```
