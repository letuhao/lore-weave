# RUN-STATE — gate teeth: 41 gates that assert coverage nobody has proven

**Reconciles:** A gate, lint, test, `const` assertion, validator, or an axiom that constrains code ·
MCP Tool I/O Standard · Settings & Configuration Boundary — the first is the governing row; the
others because the recurring defect below is theirs in a different costume (a closed set that is not
enum-locked, a value stored and never consumed). Phase 0 is §1 and was measured, not assumed.

---

## 0 · HOW TO WORK

**Binding execution contract: [`§0.6d` of the reality-layer run-state](2026-08-08-reality-layer-RUN-STATE.md)**, adopted verbatim.

Hazards, each earned in this repo:

* **`--run-all` DETACHED, read the process's REAL exit code** (`BDR-89`/`BDR-90`) — a task
  notification said *"exit code 0"* over a run whose process returned **1**, on 2026-08-11.
* **Edit nothing while a sweep or bite harness runs** (`WSD-8`).
* **Byte-level I/O** for anything a shell executes or any document (`BDR-86`).
* **Never restore a bite with `git checkout`** (`TLD-10`) — it restores from the index and deletes
  unstaged work.
* **A bite harness is an unverified check** (`TLD-11`); its `want` strings are **case-sensitive**.
* **Never hand bash an absolute Windows path** — backslashes are eaten as escapes and every arm
  reports `rc=127`. Use a relative path under `cwd`.
* **Backticks in `git commit -m` trigger command substitution** — use `-F`.

**Not stop conditions:** a commit · a green sweep · a batch finishing · a POST-REVIEW · wanting a
decision this file can make.

---

## 1 · PHASE 0 — measured 2026-08-12

| fact | value | command |
|---|---|---|
| CI-invoked gates | **98** | `gate-teeth-gate.py --list` |
| carrying a red-ability proof | **57** | same |
| carrying none | **41** | same |
| …of those, shell | **26** | `--list \| grep 'no red-ability'` |
| …of those, Python | **15** | same |
| size range | **38 – 386 lines** | `wc -l` |

**Every one of the 98 is already proven able to return non-zero.** What the 41 lack is a proof that
they return non-zero *for their own reason* — that the rule bites rather than the script merely
being capable of exiting 1.

### 1.1 · ⚠️ THE TRAP — why this number sat still

**Proof detection is STRUCTURAL.** `gate-teeth-gate` searches executable text for
`def self_test` / `selftest() {`, or a `test_<name>.py`. **It cannot distinguish a real self-test
from an empty one.** So 41 stub functions would take this baseline to **zero** and prove nothing.

That outcome is **strictly worse than 41**: a gap invites work, a false pass silences review. It is
also exactly `NV-1` — a check that cannot fail is a claim in the costume of evidence — committed
inside the mechanism built to count `NV-1` violations.

**Therefore the unit of progress on this board is not "a `selftest()` exists". It is "a mutation of
the rule makes the self-test red, and the output is pasted."**

### 1.2 · The house pattern, already in the tree

`scripts/emit-migration-0013-lint.sh` is the precedent and is copied rather than reinvented:

1. extract the rule into a **predicate function** a case can drive;
2. `selftest()` asserts **both directions** — the bad sample is flagged, the good sample is not;
3. `case "${1:-}"` runs **`selftest; run_lint`** on a bare invocation, so the proof executes on
   every CI run rather than on request;
4. selftest failure exits **2**, distinct from a lint violation's 1.

### 1.3 · What the first batch found, which generalises

Both gates in batch 1 **counted nothing**. A walk that reached zero services printed `PASS` —
byte-identical to compliance (`BDR-82`). **Assume every one of the 41 has this defect until its
counter is read**; a reach floor is part of the work, not an extra.

---

## 2 · SEALED FORKS

**`GT-F1` · A `selftest()` is not counted until it is BITTEN.** Six steps, output pasted, restore
byte-exact. **Reversal trigger: none.** This is the board's reason to exist (§1.1).

**`GT-F2` · Batch by SHAPE, not by size.** Gates sharing a predicate shape (*"X must appear in
registry Y"*) share their case design, so the second costs a fraction of the first. Ordering by line
count would scatter the shapes and pay the design cost every time.

**`GT-F3` · Every gate gains a REACH FLOOR in the same edit.** `0 < floor < measured`, printed. A
gate that cannot say what it scanned cannot be believed when it says it found nothing (`BDR-82`,
and §1.3 measured it twice out of two).

**`GT-F5` · Every EXEMPTION LIST gains a SHRINK ARM in the same edit** (sealed 2026-08-12, from
evidence rather than principle). Three allowlists in a row needed one and none had it:
`projection-coverage`'s 12 rows, `runbook-drift-check`'s 23 (**91% dead**), and by extension any
list to come. A row dies two ways — its **subject disappears**, or its **reason expires** — and both
must red. An exemption with no expiry is permanent by default, and the `runbook-drift` case shows
that is not merely untidy: each of its 21 dead names would have silently satisfied a stale runbook
reference, i.e. the allowlist could hide the very drift the gate detects. **Reversal trigger:** none.
The repo already had the pattern twice (`deferral-gate`'s `PROSE_ONLY`, `dp-oracle-coverage`'s
`NO_PRODUCER`); this fork stops it being rediscovered a fourth time.

**`GT-F4` · The baseline moves only DOWN, and only after the bite.** The ratchet already refuses to
pass until the number follows a drop; it must never be raised to accommodate a gate that lost its
proof. **Reversal trigger:** a scope widening that legitimately adds unproven gates — the 2026-08-10
`45 → 55` precedent, which is recorded as the one honest exception.

---

## 3 · THE BOARD

`done =` for every batch row: **each gate carries a predicate-driven `selftest()` asserting both
directions, a reach floor, and a pasted six-step bite; `gate-teeth-gate` green at the lowered
baseline; the batch committed.**

| # | batch | gates | state |
|---|---|---|---|
| `GT1` | registry membership — the smallest two | `capacity-budget-lint.sh` · `service-acl-matrix-lint.sh` | ✅ **43 → 41**, `baed59cad`. 5 bite arms, all byte-exact |
| `GT2e` | ecosystem pinning | `dep-pinning-lint.sh` | ✅ **38 → 37.** 5 bite arms. Two findings that dwarf the proof — `GTD-8` (89% foreign trees, and `-not -path` does not prune) and `GT-DOCKER-WARN-100PCT` |
| `GT2d` | handler-registry membership | `admin-command-registry-lint.sh` | ✅ **39 → 38.** 5 bite arms (marker parser · receiver narrowing · the case fold · membership · the exempt pragma). **Its scans have ZERO subjects** — see `GT-ADMIN-NO-SUBJECT` |
| `GT2c` | config-vs-tree conformance — moved here from `GT2` | `language-rule-lint.sh` (the LOCKED I3 amendment) | ✅ **40 → 39.** `run_lint` parameterised on config + services root, so 5 bite arms drive the WHOLE gate end-to-end: the outer `Cargo.toml` marker, the parse block-end rule, the I3 comparison, PRR-21 completeness, and the reach floor. Two self-inflicted findings — `GTD-6`, `GTD-7` |
| `GT2b` | forbidden-shape scanners — moved here from `GT2` | `dependency-registry-lint.sh` | ✅ **41 → 40.** 4 bite arms (one Go shape · one Rust shape · the exemption widened · the walk pointed at nothing), all byte-exact. Two defects fixed in passing — see `GTD-4`, `GTD-5` |
| `GT3c` | runbook ↔ service drift | `runbook-drift-check.sh` | ✅ **34 → 33.** 4 bite arms. Its allowlist was **91% dead** — see `GTD-12`. `GT3` is now CLOSED |
| `GT3b` | registry coverage + exemption hygiene | `projection-coverage-lint.sh` | ✅ **35 → 34.** 3 bite arms. Its allowlist had **no shrink arm** — see `GTD-11` |
| `GT3a` | drift / mirror checks — done | `read-audit-query-type-drift-lint.sh` · `transitions-validation-lint.sh` | ✅ **37 → 35.** 6 bite arms. Each shipped a DIFFERENT way of passing over nothing — `GTD-9`, `GTD-10` |
| `GT4a` | handler instrumentation | `tracing-completeness-lint.sh` | ✅ **33 → 32.** 5 bite arms incl. BOTH ratchet directions. Second DISARMED gate — see `GTD-13` |
| `GT4b` | metric inventory conformance | `observability-inventory-lint.sh` | ✅ **32 → 31.** 4 bite arms. Same missing-file hole as `transitions-validation` — see `GTD-14` |
| `GT4c` | SLO contract shape | `slo-latency-lint.py` | ✅ **31 → 30.** 14 self-test cases + 4 bite arms. **The first gate whose reach was already sound** — see `GTD-15` |
| `GT4d` | alert envelope conformance | `alert-rule-validator.sh` | ✅ **30 → 29.** 4 bite arms. **Third dead exemption list** — 14 of 38 rows — see `GTD-16` |
| `GT4e` | dashboard standards | `dashboard-validator.sh` | ✅ **29 → 28.** 4 bite arms; **2 failed first** because rules added minutes earlier had no case — see `GTD-17`, `GTD-18`. **`GT4` CLOSED** |
| `GT5a` | outbound-call timeouts | `timeout-discipline-lint.sh` | ✅ **28 → 27.** 8 bite arms. **Second disarmed gate** — see `GTD-20` |
| `GT5b` | blocking calls on the event loop | `blocking-in-async-lint.py` | ✅ **27 → 26.** 18 self-test cases + 8 bite arms. A THIRD way to pass over nothing — see `GTD-21` |
| `GT5c` | LLM-SDK containment + body-never-stored | `prompt-assembly-discipline-lint.sh` | ✅ **26 → 25.** 18 self-test cases + 10 bite arms. **Half of it could not fire** — see `GTD-22`, the worst finding on this board |
| `GT5d` | SQL injection defense (SEC-4) | `raw-sql-lint.py` | ✅ **25 → 24.** 19 self-test cases + 10 bite arms. A scope row that scanned nothing, and the FIRST pre-existing mechanism on this board that held — see `GTD-23` |
| `GT5e` | bounded results by construction (PERF-3) | `pagination-cap-lint.py` | ✅ **24 → 23.** 17 self-test cases + 10 bite arms. **A row the file's own comment said had been removed** — see `GTD-24` |
| `GT5f` | structured-logging discipline | `logging-discipline-lint.sh` | ✅ **23 → 22.** 21 self-test cases + 12 bite arms. **A second exemption that could not fire** — see `GTD-25`. **`GT5` CLOSED** |
| `GT6a` | MCP list-tool defaults (OUT-2) | `context-budget-defaults-lint.py` | ✅ **22 → 21.** 19 self-test cases + 10 bite arms. **The exemption was un-auditable by construction** — see `GTD-26` |
| `GT6b` | concise tool-result wire (L3) | `context-budget-l3-lint.py` | ✅ **21 → 20.** 13 self-test cases + 8 bite arms. **It counted names in a list as files checked** — see `GTD-27` |
| `GT6c` | §11a coverage manifest (§13c) | `context-inspector-checklist-gate.py` | ✅ **20 → 19.** 14 self-test cases + 10 bite arms. **The dead code was the one guarding the escape hatch** — see `GTD-28` |
| `GT6d` | live trace contract (§13b) | `context-inspector-trace-gate.py` | ✅ **19 → 18.** 13 self-test cases + 8 bite arms. **A "live" gate whose entire rule set is a pure function** — see `GTD-29`. **`GT6` CLOSED** |
| `GT7a` | deploy classification (§12AH.2) | `deploy-class-check.sh` | ✅ **18 → 17.** 25 self-test cases + 10 bite arms. **"One authoritative ruleset, two consumers" with nothing comparing them** — see `GTD-30` |
| `GT7b` | deploy freeze (§12AH.3) | `deploy-freeze-check.sh` | ✅ **17 → 16.** 19 self-test cases + 10 bite arms. **The break-glass hatch was wider than the sentence justifying it** — see `GTD-31` |
| `GT7c` | burn-rate feature freeze (§12AD.4) | `feature-freeze-enforcer.sh` | ✅ **16 → 15.** 17 self-test cases + 9 bite arms. **Live mode could not produce a non-zero burn rate** — see `GTD-32` |
| `GT7d` | B5 prod isolation (§13.5) | `raid/prod-isolation-lint.sh` | ✅ **15 → 14.** 21 self-test cases + 9 bite arms. **A detector that reds on its own source, then on the record of that** — see `GTD-33`. **`GT7` CLOSED** |
| `GT8a` | outbox emit discipline (I13) | `outbox-event-emit-lint.sh` | ✅ **14 → 13.** 15 self-test cases + 12 bite arms. **One of its two advertised rules did not exist** — see `GTD-34` |
| `GT8e` | INV-KAL table reads | `knowledge-access-gate.py` | ✅ **10 → 9.** 13 self-test cases + 8 bite arms. A SUBJECT floor, not a file floor — see `GTD-38` |
| `GT8d` | runbook verification (SR1-D6) | `runbook-verification-lint.sh` → `.py` | ✅ **11 → 10.** 17 self-test cases + 14 bite arms. **Every runbook it verifies is a stub** — see `GTD-37` |
| `GT8c` | service-ACL grants (S04-D6) | `role-grant-validator.sh` | ✅ **12 → 11.** 12 self-test cases + 9 bite arms. **A third header advertising a rule it did not have** — see `GTD-36` |
| `GT8b` | prompt-template skeletons | `template-fixture-validator.sh` | ✅ **13 → 12.** 15 self-test cases + 7 bite arms. **It could not do the job its first sentence describes** — see `GTD-35` |
| `GT8` | the remainder, one at a time | `knowledge-http-surface-gate.py` · `boundaries-lock-gate.py` · `ingress-admission-gate.py` · `i18n-completeness-gate.py` · `design-draft-token-lint.py` · `game-wire-lint.py` · `sdk-duplication-gate.py` · `phantom-route-scan.py` · `perf/bench-gate.sh` | ⬜ |

**The batch list is a WORKLIST, not the scope.** The scope is the predicate *"CI-invoked and no
red-ability proof"*, which `gate-teeth-gate --list` recomputes. A gate added tomorrow joins it
without anyone editing this table — and if this table and the gate ever disagree, **the gate is
right**.

---

## 4 · OPEN ROWS

| id | what | why not here |
|---|---|---|
| `GT-STRUCTURAL-DETECTION` | the ratchet counts a `selftest()` it cannot evaluate (§1.1) | fixing it means executing every gate's self-test and judging it, which is `gate-bite-harness`'s job — the two mechanisms already exist and the gap is that nothing joins them. A real row, deliberately not solved by this board |
| `GT-OBS-UNEMITTED` | **54 of the 100 metrics declared in `contracts/observability/inventory.yaml` are emitted nowhere in code** | planned-but-unbuilt and rot look identical from here, and this gate cannot tell them apart — declaring a metric ahead of its emitter is legitimate. Reported as a number on every run rather than enforced, so the ratio is visible to whoever can judge it. A shrink arm would need a producer/planned distinction the inventory does not carry |
| `GT-TRACING-WARN-OVERDUE` | `tracing-completeness-lint` runs in warn mode with **49** untraced handlers; the error-mode flip its header promises for *"cycle 33+"* is in no deferral row and no handoff line | the flip is a MIGRATION (49 call sites), not a lint change, so it is not this board's to make. The gate is now armed by a ratchet that reds on any NEW untraced handler, which is the part that can regress today. Whoever owns the tracing migration owns the flip |
| `GT-DOCKER-WARN-100PCT` | `dep-pinning`'s Docker arm warns on **36 of 36** Dockerfiles; **zero** digest-pinned `FROM`s exist anywhere in the tree | a warn that fires on 100% of its subjects has no discriminating power — it was 36 identical lines every CI run, which is how a gate teaches people to skip its output. Collapsed to a ratio, which still moves the day someone digest-pins one. Whether to adopt digest pinning at all is a platform decision, not a teeth one |
| `GT-ADMIN-NO-SUBJECT` | `admin-command-registry-lint` walks `services/` and matches **nothing**: `// ADMIN-SQL:` and `// ADMIN-RPC:` occur **0** times, `func (… *AdminHandler)` **0** times. Its rule is now proven to bite, but it has nothing to bite in this tree, and its old message read as verified admin coverage | **not resolved by making it red** — zero markers is the TRUE state and failing on the truth is cry-wolf. The gate now prints its subject count and says nothing was compared. The real question is whether the marker convention should be adopted (the live admin surface is the 10 registry YAMLs + the Go dispatcher) or the gate retired; that is a design call, not a teeth call |
| `GT-OUTBOX-SERVICEMAP` | `outbox-event-emit-lint`'s header (and `events_allowlist.yaml`'s own) describe auto-deriving the allowlist from *"the service map's Emits events column"* | there is no machine-readable emits column to derive from, and the allowlist's `owner:` field is not a substitute: measured 2026-08-12, **13 of its 18 distinct values are prose** (*"admin-cli (cycle 36)"*, *"shard health agent (per-shard sidecar; cycle TBD L7)"*), so an owner→service arm would report documentation as drift. The YAML-level integrity that IS checkable — table exists, event_name unique, op closed-set — is now built and bitten; the derivation needs a data source someone has to create first |
| `GT-FREEZE-LIVE-UNWIRED` | `feature-freeze-enforcer`'s live burn-rate fetch does not exist; `slo-budget-calculator`'s `/slo/targets` is config-only. Every tier above `normal` is reachable only through `--burn-rate` or a fixture | the fetch needs the calculator to actually compute and expose a burn rate, which is `D-SLO-CALC-LIVE-WIRING`'s job, not a teeth one. What this board CAN do is stop the gate lying about it: live mode now returns **CANNOT RUN (2)** instead of manufacturing `0.0`, and the self-test case *"live mode is CANNOT RUN, not a manufactured 0.0"* is the mechanism that deferral never had — implement the fetch and that case must change. Measured 2026-08-12, `D-SLO-CALC-LIVE-WIRING` appears in exactly two places: one `cycle_done` line in `AUDIT_LOG.jsonl` dated **2026-05-30**, and a comment in the script. No Deferred Items row, no handoff line — prose only, for 2.5 months |
| `GT-RAWSQL-RUST-UNSCANNED` | `raw-sql-lint`'s `SCAN_EXTS` is `.py`/`.go`; **63 `.rs` files under `crates/` and `services/` contain SQL keywords and none of them is scanned** | writing a Rust detector is a RULE change, not a proof. The two live signals do not transfer cleanly: `'{x}'` inside a Rust SQL literal is ambiguous between a `format!` interpolation and a Postgres array literal — exactly the ambiguity that made the brace detector Python-only in the first place. Rust here uses `sqlx`, whose compile-time-checked macros bind by construction, so the risk surface is the non-macro paths; sizing that is a security-review question, not a teeth one. Recorded rather than faked: the `crates` scope row that implied this coverage is gone, so the gate no longer claims it |
| `GT-TIMEOUT-PY-ADVISORY` | `timeout-discipline-lint`'s Python leg is a RATCHET at 3, not a blocking rule; its header has promised a flip to blocking since the leg was added | the flip reds on 13 pre-existing untimed calls (10 baselined + 3), and the correct asyncpg fix is a judgement call — `command_timeout=` set too low fails long queries, so this is a migration with a per-call decision, not a lint change. The ratchet is the part that can regress today. Whoever owns the PERF-1 backlog owns the flip |
| `GT-PERF-BENCH` | `perf/bench-gate.sh` SKIPs in `--run-all` (*"needs a live stack"*) | a gate that cannot run here cannot be bitten here; it needs the stack, and a self-test for it would be testing the skip |

---

## 5 · DRIFT REGISTER

**An empty drift log is not evidence of a clean run.**

| id | what happened |
|---|---|
| `GTD-1` | **The first bite arm certified nothing and looked like a pass.** Blanking the whole meta-write alternation red the self-test — but on its FIRST case, so it proved `contracts/meta` was live and said nothing about the other two. A bite that reds for the wrong reason is a bite that certifies coverage it never exercised. Split into one arm per alternative. |
| `GTD-38` | **The floor this gate needed was not a file count.** `knowledge-access-gate` enforces INV-KAL by looking for two names — the EAV table `entity_attribute_values` and the Neo4j driver — so **renaming either leaves it matching nothing anywhere, including inside the owning service, and exiting 0**. A walk-reached-N floor would not have noticed: the walk is fine, it is the detector that has gone blind. The floor counts SUBJECTS instead (measured: 362 EAV mentions, 359 of them owner-side; 10 Neo4j uses, 9 owner-side), so a rename reds. Its one `ALLOWLIST_PREFIXES` row had no shrink arm — live, as it happens, and now proven so. **And the general rule, after writing the same mistake five times on this board: where a subject floor exists, a file-count floor is ALWAYS shadowed by it** — zero files implies zero subjects. I wrote one here anyway, the bite went green on the sibling, and it is deleted with the reason recorded in the source so the next person does not re-add it. |
| `GTD-37` | **A gate called runbook-VERIFICATION whose entire corpus is unverified.** Measured 2026-08-12: **27 runbooks, 27 stubs** — every one a placeholder carrying `verification_method: stub` and `last_verified: 1970-01-01`. That is faithful to Q-L7B-1 (stubs are PRESENT and count), and the gate printed the number — but nothing changed colour when it moved, in either direction. `STUB_CEIL` is now a ratchet: a new placeholder reds, and draining one reds until the constant follows, so the day someone writes a real runbook the gate insists on being told. Its alert leg was **advisory** — 5 of 33 alerts with no runbook, printed as WARN *"until cycle 36+ alert-rule-validator backfill"*; cycle 36 came and went. `GTD-13`'s shape, fifth occurrence, now a ratchet. A renamed alerts directory scanned zero rules and reported zero unlinked, which is the best possible result and means nothing — floored. **It had no self-test because there was nothing to call:** the whole checker was a `python3 - <<'PY'` heredoc inside the shell wrapper. Extracted to a real module and delegated, the shape `migration-idempotency-validator.sh` already used. **Three cases had to be isolated before they could speak**, which is the tax of a gate with several ratchets: a non-stub runbook trips the STUB ratchet, zero runbooks empties the reverse lookup and trips the UNLINKED ratchet, and both had to be silenced before the closed-set rule and the README exclusion had a fixture of their own. |
| `GTD-36` | **The third gate in one batch whose header advertises a rule it does not have.** `role-grant-validator` has said *"declare permissions from {SELECT,INSERT,UPDATE,DELETE} only"* since it was written, and the file contained the audit-table check and the unknown-table check and nothing else — a typo'd `- SELCT` or an invented `- TRUNCATE` sailed through. After `outbox-event-emit` (a whole second leg) and `template-fixture-validator` (the version it hardcoded past), that is three in a row, which makes it a habit rather than an accident: **the header is where the rule gets written, and then nobody diffs the header against the code.** Built and measured clean. A missing matrix was a silent `WARN … exit 0` — the **fourth** gate on this board with that line. No floors on three independent walks (9 audit tables · 55 declared · 45 matrix refs, any of which going quiet used to mean *nothing to complain about*). No shrink arm on the `deploy_audit` UPDATE exception. **The new rule cried wolf on its first production run**, which is the part worth keeping: `^[[:space:]]+- WORD` also matched the ten-space `allowed_callers:` lists and reported `publisher` as an invalid permission. Grants sit at exactly eight spaces (74 of them); callers at ten. Narrowed, and the caller list is now a fixture so the false-positive twin is a case rather than a memory. **Two arms needed a more careful fixture:** the shrink arm's two deaths shadow each other — deleting `deploy_audit` makes death #2 fire (it then grants no UPDATE) and deleting its migration makes the unknown-table rule fire, so only RENAMING the migration file isolates death #1. |
| `GTD-35` | **A gate that could not do the job its first sentence describes.** `template-fixture-validator` opens *"blocks PRs that bump a template version without also adding the matching fixture directory"* — and hardcoded `v1` everywhere, under the comment *"Pick active_version (default 1 for skeleton)"*. Bump `active_version` to 2 and it goes on checking v1, which still exists, and passes. The registry's own schema comment states the contract it was not enforcing: *"`active_version` MUST match a v<N>.tmpl + v<N>.meta.yaml pair on disk."* Two documents describing a check, one implementation ignoring both — the same shape as `GTD-34`, in the next gate along. Now read per intent from the registry, with the bump case as the headline arm. Its 7-intent list was also a hardcoded **mirror** of `AllIntents()` (the header says so) with nothing comparing them; the set is now DERIVED from the Go string constants and compared, so a change on either side reds, and the literal `7` is gone from the logic. **Its reach was already sound** — a hardcoded expected list cannot pass over nothing — which makes it the third gate on this board that needed no floor. **A pattern worth naming, now that it has happened eight times:** an early-exit guard whose finding the check downstream makes anyway. Three here (missing `intent.go`, non-numeric `active_version`, missing intent dir) all came back GREEN under a bite. They are kept — *"intent.go is missing"* and *"an extraction matched nothing"* send a reader to different places — but each is now marked *message refinement, not detection*, so the next person to bite them knows before they start. |
| `GTD-34` | **One of its two advertised rules did not exist, and BOTH sides said it did.** `outbox-event-emit-lint`'s header claimed *"Also enforces the events_allowlist.yaml ↔ service-map cross-check at the YAML level (every emitted event MUST appear in allowlist)"* — and the file's only reference to that YAML was that sentence. The allowlist agrees from the other end: *"The L1.K lint that cross-references this file with the service-map ships in cycle 11 (L1.K)."* The lint shipped; the cross-check did not. **Two documents agreeing about a mechanism neither of them has** — which is worse than one stale comment, because the corroboration is what stops anyone checking. Built as far as the data allows (table exists, event_name unique, op closed-set, no duplicate rows, non-empty entries) with the limit written down rather than glossed; the rest is `GT-OUTBOX-SERVICEMAP`. Its XADD leg had 12 exclusion rows and no shrink arm (`services/learning-service/` was dead — removed, and the arm now proves the other ten live), no reach floor, and **16457 files scanned of which 12356 were vendored `node_modules`** — `GTD-8`, third occurrence. **The nicest thing the bite found:** killing the XADD detector does not red the detector's own case, it reds the SHRINK ARM — with no hits, every sanctioned path excuses nothing. The shrink arm doubles as the detector's reach floor, which is not a property I designed and is worth knowing. Two guards were shadowed and are kept as diagnostics with a comment. |
| `GTD-33` | **An enumerated hostname list, and a gate that reds on its own output.** `prod-isolation-lint` forbade four literal prod hostnames, so a fifth was default-uncovered (`NV-3`); replaced with the structural `prod*.loreweave.*`, which subsumes all four. Its range handling swallowed an unresolvable ref — `git diff -U0 "$RANGE" || true` turned *"I could not read that commit"* into *"that commit is fine"*. **Then it caught me twice in a row, which is the finding:** the rewrite's first run flagged **its own source** (the new cases quote the hostnames they exist to detect), and the second flagged **the audit record the first had just written** (the gate quotes the offending line into `AUDIT_LOG.jsonl`). A detector cannot be barred from naming its subject, and a gate that reds on its own output reds once and then forever. The fix needed per-file attribution the gate did not have — it flattened the diff, so it could not tell whose line it was reading, which is *why* it had no way to exempt anything narrowly. Two exemptions now, both with the shrink arm, and I removed the two spurious `prod_isolation_violation` records my own run had appended to the repo's evidence. **What this gate cannot have, stated rather than papered over:** it is diff-scoped, so in `--run-all` on a clean tree it has no subject and its green means nothing — and it must stay that way, because `infra/foundation-dev/docker-compose.yml:12` reads *"Foundation cycles target THIS env, NEVER existing prod.loreweave.app"*: the invariant being obeyed, written in the forbidden words. A whole-tree scan would red on the comment documenting the rule. Neither guarded infra path exists either — also the invariant holding, not a dead arm. |
| `GTD-32` | **A four-tier freeze policy that could not leave tier one.** `feature-freeze-enforcer` maps an SLO burn rate to five outcomes, and in LIVE mode **both** branches of the fetch assigned `burn_rate="0.0"` — the unreachable one as a documented fail-open, and the REACHABLE one under the comment *"Real burn-rate fetch will land in a follow-up cycle; for now we assume normal operation when the calculator is up."* So the `curl /healthz` probe decided nothing: its two outcomes produced the same number, and every tier above `normal` was unreachable in production. `NV-1` with a comment explaining itself, shipped since cycle 34. Live mode now returns CANNOT RUN rather than a reading nobody took — this repo's own `context-inspector-trace-gate` says why two files over: *"conflating [cannot-run] into 0 is the skip-reads-as-pass bug this repo has now shipped twice."* CI invokes this with `--dry-run`, so nothing regressed. A comment also claimed the ≥90% tier requires BOTH labels while the code requires only the override — the code matches the policy table, so the comment was the wrong one, and a reader trusting it would have believed in a check that does not exist. `--help`'s fixed line window was the **sixth** occurrence of that shape. Tracked as `GT-FREEZE-LIVE-UNWIRED`. |
| `GTD-31` | **The break-glass hatch was wider than the sentence justifying it, on the control that stops deploys during an incident.** `deploy-freeze-check`'s header says the label lifts a freeze *"for an EMERGENCY-class deploy"*, and its own blocked message says *"add 'break-glass-deploy' label (emergency class + tech-lead) to override"* — while the code lifted it for **any** class. A `patch` PR carrying the label walked through a **security** freeze. That is `GTD-18`'s shape (`dashboard-validator`'s `2>/dev/null`) on something that matters considerably more than a template's timezone. Narrowed to the stated reason. Two more in the same file: **"no burn data" and "burn = 0.0" were the same observable** — `resolve_burn` returns `0.0` when nothing is available, so a broken slo-budget-calculator reads as a perfectly healthy SLO and the slo_burn freeze silently stops existing. The fail-open is the spec's operational call and is kept, but it now says so on every run, because a fail-open nobody can see is indistinguishable from a rule that works. And an **unparseable `--burn-rate` was silently ignored** — the numeric guard skipped the whole detection, so `--burn-rate abc` meant *no freeze*: the input that cannot be understood became the input that means everything is fine. Now exit 2, as is an unknown `--class` (previously accepted, then compared against `emergency` forever after — a typo turning the exemption off silently). `--help`'s fixed line window was the **fifth** occurrence of that shape. **One arm needed a better case:** widening the label match to a substring left the `docs-only` probe green, because that string does not contain the label either way; the boundary needs a label that CONTAINS the break-glass name (`needs-break-glass-deploy-approval`). |
| `GTD-30` | **A mirror with no drift check, announced in its own header.** `deploy-class-check` opens with *"this is the shell sibling of `services/canary-controller/internal/deployclass` (one authoritative ruleset, two consumers)"* — and nothing compared them. The Go side carries a test table; the shell side carried nothing; neither knew the other existed. A rule added to either would have shipped a silent disagreement about how a PR is classified, which is what decides whether a canary is required. A MIRROR CHECK now compares the decision ORDER and each branch's OR-operand COUNT against the Go source on every invocation. It is not semantic equivalence — it is the drift that actually happens, made mechanical. An EMPTY changed-file list also classified as `patch` and exited 0: *"the diff failed to resolve"* and *"this is a small change"* were the same word, and only one of them is a fact. And `--help` printed a **fixed line window** (`sed -n '2,38p'`) into its own header — the brittle-window shape this repo has now shipped four times; the window is computed. **The mirror check needed two rounds of GT-F1 to be worth anything.** Its zero-extraction guard (*two failed greps compare equal*, `GTD-9`) came back GREEN, because rewording only the Go side leaves the shell at 3 and the ordinary comparison catches it; the guard's real case is BOTH sides going quiet at once, which needed `mirror_check` to take the self file as a parameter before it could be built. Its missing-file guard is strictly shadowed by that zero guard and is kept for the message, documented as diagnostics — the third time on this board that distinction has had to be written down. |
| `GTD-29` | **A gate filed in my own head as un-bitable, that was nothing of the sort.** `context-inspector-trace-gate` drives real turns through a running chat-service, so the first read was *"needs a stack — record it in §4 like `GT-PERF-BENCH`"*. That is the **"missing infrastructure is not blocked"** trap: only the TRANSPORT needs a stack. `_assert_contract` is a pure function over the decoded response, and it holds the entire rule set — every frame field present AND non-null, every trace span wire-standard, at least one turn carrying a status flag, and `raw == used + savings`. All eight are now driven over synthetic frames, in CI, beside the existing `--selfcheck`. Filing it as blocked would have been calling buildable work impossible. Two `GTD-17`s found on the way, both mine: `CONTRACT` was read at module load, so every probe would have tested the LIVE contract instead of its own fixture; and the `CANNOT_RUN` probe called `main()` while `sys.argv` still held `--selfcheck` — the form CI uses — so `main()` short-circuited to the static half and never reached the branch under test. **It came back rc=0 and the arm said so.** Worth recording what this gate already had right, because it is the exception on this board: three distinct exit codes with the reasoning written down (*"conflating 2 into 0 is the skip-reads-as-pass bug this repo has now shipped twice"*), and a `--selfcheck` whose stated purpose is that *"a live gate nobody can execute rots exactly like an unwired lint, and rots invisibly: the failure looks like 'no stack today', every day."* Somebody had already thought about vacuity here. What they had not done was prove the rules bite. |
| `GTD-28` | **The dead code was the one guarding the escape hatch.** `context-inspector-checklist-gate` makes the §11a manifest un-gameable — every item needs a `✓test:<path>::<needle>` or a `⊘manual:<reason>` — and `⊘manual` is the ONE way to satisfy it without writing a test. The check on that hatch was `if not it.reason:`, and `PROOF_MANUAL` requires a non-space character, so `reason` was **non-empty by construction**: the branch could never fire. Third could-not-fire rule on this board, after `prompt-assembly`'s body check and `logging-discipline`'s `cfg(test)` carve-out, and the only one sitting on the gate's own bypass. `⊘manual:x` was a valid reason. Replaced with a substantive bar (20+ chars, no `TODO`/`TBD` placeholder — both live reasons run ~100) plus a **RATCHET on the manual COUNT** at 2 of 85, which is the spec's *"small minority"* made mechanical: there is no exemption LIST here to shrink, because `⊘manual` is an inline marker that cannot outlive its subject, so the population is what can rot. **A hook with no user, and a trap for whoever wrote the first one:** `--spec` was documented *"for gate self-tests"*, but `resolve_test_ref` read the module-level `REPO`, so a probe supplying a synthetic checklist would have resolved its `✓test` paths against the LIVE tree — `GTD-17` lying in wait behind an affordance that had never been used. **And this gate was largely right already**, which is worth saying: it exits 2 on a missing spec, exits 2 on zero items parsed (a reach floor, before this board existed), and had already closed the needle-matches-a-comment hole. Second pre-existing mechanism on this board that held. One arm was removed rather than kept: `needle not in text` is strictly shadowed by the declaration-line check, but unlike a shadowed FLOOR it earns its keep as a distinct message, so the line stays with a comment saying it is diagnostics and not detection. |
| `GTD-27` | **It printed the length of a hardcoded list as the number of files it had read.** `context-budget-l3-lint` looped over three `SCAN_FILES` names with `if not os.path.exists(path): continue`, then announced *"L3 wire lint clean (3 turn-loop files checked)"* — `len(SCAN_FILES)`. Rename one and it reported three checked having read two; rename all three and it reported three checked having read none. Shape 4 and shape 1 in the same statement. A missing entry is now exit 2 and the count is files actually READ. **Its scope was `NV-3` by construction** — an enumerated list, with the hole written out in prose (*"a new assembly path MUST be added here — a missing file is how a bypass ships silently"*) and nothing enforcing it. A DISCOVERY ARM now reds when a chat-service module uses the `tool_result_content` funnel without being watched, which is the signal a genuine new assembly path emits. **What I deliberately did NOT build, and why it matters:** the obvious arm — *any file matching the violation regex must be listed* — would cry wolf. Measured, two chat-service files match it and neither is in scope: `routers/outputs.py` serialises a downloadable `chat_export.json`, and `services/stream_events.py` builds the AG-UI `TOOL_CALL_RESULT` envelope the BROWSER reads. L3 governs the bytes the MODEL reads. Also surfaced and now printed rather than hidden: one of the three watched files (`subagent_runtime.py`, 182 lines) assembles no content at all — a scope row watching nothing, the `crates` shape from `GTD-23`. **Fourth shadowed-rule finding, and this time the rule was right and the CASE was degenerate:** the shrink-arm probe used a one-entry list, so a missing entry tripped the floor too and the probe passed whichever rule was live. Two entries — one present, one absent — separates them. |
| `GTD-26` | **An exemption list that could not be audited, because the rule was never evaluated for the rows it exempted.** `context-budget-defaults-lint`'s `_check_pair` opened with `if key in ALLOW: return` — so for an allowlisted tool the check simply did not run, and *"this row suppresses a real violation"* and *"this row suppresses nothing"* were indistinguishable **by construction**, not by oversight. No shrink arm could have been bolted on; the evaluation had to be inverted first (judge, then exempt the RESULT). Both deaths now red. `ALLOW` is empty today, so the arms are driven by cases rather than by a measurement — which is the point: the previous three lists were 33–91% dead and each was discovered only when someone looked. Its floors were missing too, and the one that matters here is not a file count but a **SUBJECT** count: the rule's subject is the `detail`+`limit` pair, so renaming that convention leaves 14 files scanned, 0 tools judged, and a clean exit. Measured: 16 subjects across 14 files. **Caught before writing any case:** `LIMIT_CEIL` was read as a module constant *inside* the checker, so a probe varying the ceiling would never have reached its own fixture — `GTD-17`'s exact shape, spotted this time by looking rather than by a bite. Three self-inflicted: the probe helper seeded its clean fixture whenever a body was passed (so the no-subject case silently got a subject); the `Field(default=…)` arm needed the COMPLIANT twin to isolate it, because an un-unwrapped `Field(...)` Call is not the string `"summary"` either and reds for the wrong reason; and I wrote a **third** strictly-shadowed floor (`not files` under `not subjects`), again found by an arm reding on its sibling. |
| `GTD-25` | **A second exemption that could not fire, in the same batch.** `logging-discipline`'s Rust `#[cfg(test)]` carve-out read `grep -q '#[cfg(test)]' && ! grep -E 'println!' | grep -v 'cfg(test)'` — the `grep -v` runs over the **macro lines**, which never contain the string `cfg(test)`, so the pipeline always succeeded and the `!` always made the condition false. Measured: **4 files carry both markers, the exemption fired for 0 of them, and 2 of the 4 have every `println!` below the marker** — genuinely test-only code, warned about for as long as the rule has existed. Replaced with a position test that can distinguish the two cases, bitten in both directions; the soft count fell 87 → 85 as a result. Its SOFT leg was DISARMED (warn-mode exit 0, so *"it passed"* and *"87 bare log calls"* were one observable — `GTD-13`, third occurrence) and its header said **~67** while the tree held **87**: a number nothing measured, in the same sentence that justified leaving the leg unarmed. Now a ratchet, both directions. No floors on any of the four target lists — and these come from `git ls-files`, so an untracked tree reads as clean. No shrink arm on either hardcoded exemption path. **Self-inflicted, and again found by a bite arm reding without its message:** my `EMPTY TypeScript list` probe deleted the exemption file along with the ordinary one, so the SHRINK ARM fired and the probe passed for the wrong rule, leaving the floor uncovered. Two rules, one fixture. |
| `GTD-24` | **The file documented the removal of a row it still carried.** `pagination-cap-lint`'s BASELINE said, in prose three lines under the rows: *"the fourth finding was NOT baselined — `mcp_worlds.go` was fixed, because its inline clamp DISAGREED with the helper beside it"*. The row was still in the set, and had been for two weeks. A second row (`pipeline_read_tools.go`) had also stopped matching. Both dead, both now red under a shrink arm. Three further rows were written **twice**, under two comment blocks giving different accounts of the same finding; `frozenset` deduplicated them silently, so **39 literal rows were 36** and nobody counting either number was right. Now 34, each once. Two more, both self-inflicted and both caught by the bite rather than by review: `Path.write_text` rewrote the whole file to CRLF (`BDR-86`, and it cost four arms their anchors before I looked at the bytes); and the reach floor I wrote had a clause (`py_files == 0`) **strictly shadowed** by its sibling — zero python files implies zero `limit=Query` declarations — so the arm disabling it still went red, on the other rule. That is `GTD-7` exactly, and the second time on this board I have written a floor that could not produce a finding another did not. Deleted rather than kept as decoration. |
| `GTD-23` | **A scope row that scanned nothing, and a third dead exemption list.** `raw-sql-lint`'s `SEARCH_DIRS` named `crates` — which is Rust, while `SCAN_EXTS` is `.py`/`.go`, so the entry contributed **0 files** and had done since it was written. Decorative scope reads as coverage: three directories listed, two scanned. A per-directory arm now reds on any row contributing nothing, and the Rust surface it implied is an open row instead of a silent gap. Its `BASELINE` was **2 of 6 dead** (33%) — both rows named `_smoke_a2s1b2*.py` files that no longer exist. For THIS gate a dead row is not merely untidy: it is a standing waiver on a path, so the day anything reappears under that name its SQL is pre-approved. No reach floor either. **And the one genuinely good thing found on this board so far, worth recording as loudly as the defects:** the docstring's `NV-4` claim — that the `package_rekey` exemption rests on six module-level constant tuples staying literal, and that `test_package_rekey_constants.py` parses the module with `ast` and reds if any stops being one — **is true**. The test exists and still asserts it. That is a previous author writing a mechanism instead of a promise, and it is the first one on this board that did not need replacing. |
| `GTD-22` | **A rule that could not fire, in production, guarding a privacy invariant, under a header calling itself "defense-in-depth".** `prompt-assembly`'s body-never-stored check (S09 §12Y — the prompt_audit table must never hold prompt text) piped `grep -nvE '^\s*--' file` into `grep -niE '^\s*(body|…)\s+(TEXT|…)'`. **`grep -n` prefixes each line with `LINENO:`, so the second pattern's `^` anchor never reached the SQL.** A migration declaring `body TEXT` was waved through. Demonstrated on a three-line fixture before touching the gate. This is `NV-1` exactly — a check wearing the costume of evidence — and the costume was convincing enough that the header advertised redundancy. Four more, all in the same file: the migration's ABSENCE was a silent pass (`if [[ -f … ]]`, the **third** gate on this board with that line, after `GTD-10` and `GTD-14`); no reach floor; **75% of the corpus was foreign** — 12358 of 16459 files were vendored `node_modules`, i.e. it was judging other people's packages by this repo's invariant, `GTD-8`'s second outing; and the exemption list held **five rows that exempted nothing**, one of which (`services/knowledge-service/`) **was never in the documented allowlist at all** and was the only row that could have hidden a genuine Provider-gateway breach. The stated reason for another (`chat-service` *"uses litellm"*) is false in this tree: measured, there is no provider SDK dependency anywhere — provider-registry-service is Go and speaks raw HTTP. **And `GT-F1` earned its keep again**: the arm deleting the new TypeScript alternative came back GREEN, because the case had been written as `import OpenAI from "openai"` and the scan is case-insensitive, so the PYTHON alternative matched the BINDING name by coincidence. `import Foo from "openai"` was invisible. The case was wrong, not the arm — and nothing but a bite would have said so. |
| `GTD-21` | **A third way to pass over nothing: read the file, fail to parse it, count it clean.** `blocking-in-async-lint` swallows a `SyntaxError` and returns no hits, so a file it could not read was indistinguishable from a file with no violation — the walk reached the file and the *parse* reached nothing, which no reach floor on file count would have caught. Now counted, printed, and ratcheted at zero (measured: 0 of 958). Deliberately no downward arm on that ratchet: zero is the floor and a branch that cannot fire is what this board removes (`GTD-7`). It also had the ordinary floor missing — `iter_files` opens with `if not os.path.isdir(SERVICES): return`, so a renamed tree yielded 0 files, 0 hits and the same `OK` line. **And its docstring described a mechanism doing nothing**: *"passes clean on the CURRENT tree via a BASELINE of today's known offenders"* — the baseline is EMPTY and the tree has zero offenders, so it passes because the rule holds. Shape 4 in a gate header, one file after `projection-coverage`'s stale ratio. **Self-inflicted, and caught by reading my own output:** the *missing* and *empty* services-tree probes were the same fixture, one wearing the other's name — a duplicate that reports coverage of an input it never read (`GTD-17` again, in the self-test I was writing to prevent it). |
| `GTD-20` | **A second gate whose findings and its success were the same observable, and its re-arming promise was again tracked nowhere.** `timeout-discipline-lint`'s Python leg printed **3** unguarded outbound calls and exited **0** — the `GTD-13` shape exactly, one batch later — under a comment promising a flip to blocking *"once the asyncpg-timeout debt is addressed"*, which appears in no deferral row and no handoff line. Armed with a RATCHET (`GT-F5`'s sibling discipline): it reds on a NEW offender today and reds again when the count falls without the constant following, and both directions are bitten. Its three walks had no floor either — Go, Rust and Python are each a `grep -r` whose empty result is byte-identical to compliance, with `2>/dev/null` over the top, so a renamed `services/` printed `PASS` forever. **The baseline was the first exemption list on this board to land clean**: 10 rows, 0 dead — worth recording because the previous three measured 36–91% dead, and it is the shrink arm that now says so rather than a one-off measurement. Foreign-tree scope was measured too (`GTD-8`'s shape) and is clean here: identical .go/.rs/.py counts with and without pruning `node_modules`/`target`, because those trees hold none of the three. |
| `GTD-19` | **I reported three board updates that never happened.** The `GT4c`/`GT4d`/`GT4e` rows were written by `str.replace` calls whose anchors did not match — Python's `replace` silently returns the string unchanged — and the script printed *"board updated"* unconditionally afterwards. Three commits therefore claimed a board state that did not exist, and the stale `GT4` row went on listing three gates as unproven **after they were proven and pushed**. Found only because the human asked whether the goal covered all 28; the check that answered it (gate's unproven set vs the board's open rows) is the mechanism this should have had from the start. **An unverified edit is a claim** — the same defect this entire board is about, committed in the file that records it. Every board edit now asserts its anchor count. |
| `GTD-17` | **Two bite arms failed on their first run, and both were right to — the rules they targeted had NO CASE.** `dashboard-validator`'s narrowed template exemption and its new shrink arm could each be disabled with the self-test still green: the probe tree contained no template entry, and the hardcoded exemption path resolved against the real repo rather than the fixture. **Rules added ten minutes earlier, already deletable with the suite passing.** Root cause underneath: `DASH_ROOT` was read at script LOAD, so every probe ran against the real `dashboards/` tree and passed by accident — a self-test that never reaches its own fixture is worse than none, because it reports coverage of inputs it never read. Fixed by parameterising, using `+x` not `-n` (a probe wanting zero exemptions passes an empty string, and `-n` hands it the production default). **This is `GT-F1` earning its keep against me twice in one gate.** |
| `GTD-16` | **Third dead exemption list, and this one could disarm the checks it exempts from.** `alert-rule-validator`'s `PRE_SLI_GRANDFATHER` held 38 names; **14 (36%) referred to alerts that no longer exist on disk**. Dead weight — but worse than the previous two, because a dead row silently RE-grandfathers its alert the day the name comes back, waiving `sli_ref`, registry membership and the runbook requirement without anyone deciding to. Trimmed to 24 + `GT-F5` arm. Its reach floor was missing too: `os.walk` on a renamed alerts dir yields nothing, so it printed *"0 alerts validated"* and exited 0 — the count sitting in the success line, exactly like `read-audit-drift`'s *"(0 ids)"*. |
| `GTD-15` | **The first gate that was already right, and it is worth recording as loudly as the broken ones.** `slo-latency-lint.py` exits 2 on a missing config, exits 2 on an empty `endpoints:` list, and prints its count — reach sound before I touched it, unlike the twelve before it. It needed only cases and a `GT-F5` shrink arm. **The case worth keeping:** `p95_ms: true`. `True` IS an `int` in Python, so without the explicit `isinstance(p95, bool)` guard a boolean passes as the number 1 — a positive number, and a green SLO. The guard was already there; nothing proved it load-bearing until a bite arm removed it and watched the case go red. A correct guard with no case is one refactor from silently disappearing. |
| `GTD-14` | **The same missing-file hole, in a gate that had already survived two `NV-4` findings.** `observability-inventory-lint` opened with `if [[ ! -f "$inventory" ]]; then echo "skipping"; exit 0; fi` — one `git mv` from permanently green, and the SECOND gate on this board with that exact line. Its header is unusually careful (it documents two hard-won false-positive fixes), which is the point: a gate can be well-maintained on the axis someone was looking at and still pass over nothing on the axis nobody was. Its EMITTED side had no floor either — the declared side fails loudly when it empties, so the asymmetry hid it. |
| `GTD-13` | **A second disarmed gate, and its re-arming promise was tracked nowhere.** `tracing-completeness-lint` defaults to `warn`: it printed **49** violations and exited 0, so *"it passed"* and *"49 handlers have no tracing"* were the same observable. Its header promised the error-mode flip *"in cycle 33+"* — measured 2026-08-12, that appears in no deferral row and no handoff line. Prose, several cycles overdue, nothing to wake it. **Armed with a RATCHET instead of a flip**: flipping reds the build on 49 pre-existing violations (a migration), while a falling baseline reds a NEW untraced handler today. Both directions bitten — a ratchet that only reds upward never falls. Tracked as `GT-TRACING-WARN-OVERDUE`. |
| `GTD-12` | **A second allowlist, 91% dead, and this one could hide the drift it guards against.** `runbook-drift-check`'s `KNOWN_LOGICAL` held 23 names described as *"canonical platform names that aren't yet `services/` dirs"* — **19 of them ARE `services/` dirs** (they shipped) and 2 more are cited by no runbook. **Two** were load-bearing. Worse than dead weight: each of the 21 would have silently satisfied a stale runbook reference, which is exactly the drift this gate detects. Trimmed to two with both shrink arms. Third allowlist in this repo to need them — `GT-F2` should probably become *"every exemption list gets a shrink arm in the same edit"*. |
| `GTD-18` | **An exemption WIDER than its stated reason.** `dashboard-validator` excused its template file with `check_dashboard "$f" 2>/dev/null` — swallowing all NINE rules — under a comment justifying exactly one (*"uid `_template` is intentionally non-kebab-case"*). A template that lost its `timezone` or stopped being valid JSON was waved through by a justification that did not cover it. A fifth shape for §1: **the exemption and its reason are different sizes**, and only the reason is written down. Measured before narrowing: the file fails the uid rule and nothing else, so it reds nothing today. |
| `GTD-11` | **A 12-row allowlist with no shrink arm — an exemption permanent by default.** `projection-coverage-lint` reds when a registered event has no projection, but nothing red when an allowlisted event was **deregistered** (the row outlives its subject) or when one **gained a projection** (the reason expired). The `npc.said` row literally documents its own retirement trigger in prose — *"when the actor/NPC track ships an emitter, this row must be replaced"* — and no mechanism could notice. This repo already solved it twice (`deferral-gate`'s `PROSE_ONLY` shrink, `dp-oracle-coverage`'s `NO_PRODUCER`); this gate just never got the arm. Measured clean on landing: 0 stale, 0 expired. **Its docstring was also stale** — *"5/14 registered events are projected"* against a measured **4/16**, i.e. claim rot in the header of a gate. |
| `GTD-10` | **`transitions-validation-lint` was one `git mv` from permanently green.** Its first statement was `if [[ ! -f "$target" ]]; then echo "nothing to lint"; exit 0; fi`. Rename or move `contracts/meta/transitions.yaml` and the gate reports success forever, with a cheerful message explaining why. The file existing today is the only reason it never bit. An EMPTY file was the same story by a second road — all three heuristics are trivially satisfied by no content. Both are now exit 2. |
| `GTD-9` | **`read-audit-query-type-drift-lint` called two empty sets agreement.** The entire gate is one string comparison between two grep outputs; if either pattern stops matching, both sides become `""`, the equality holds, and it prints *"PASS — CHECK == YAML SSOT (**0 ids**)"*. A drift detector that reports agreement because it parsed nothing is the defect it exists to catch, wearing its own success message — and the `(0 ids)` was printed on every run for anyone who looked. Floor added BEFORE the comparison, because after it the answer is already wrong. |
| `GTD-8` | **`-not -path` does not prune, and I had assumed it did.** `dep-pinning-lint` excluded `node_modules`/`.venv` by RESULT filter, so `find` still descended into every `.claude/worktrees/*/target/` — two complete Rust build trees, four times per run. 51–76s for a gate whose real subject is 131 files; `-prune` took it to **13s**. The scope error underneath it was worse than the latency: **89% of what it scanned was foreign** — agent worktree copies, `site-packages`, `vendor` — so it was judging third-party packages by this repo's pinning convention, one bare `requests` away from a false finding nobody could act on. |
| `GTD-7` | **I wrote a second reach floor that could never fire.** `language-rule-lint` got floors on both *directories walked* and *comparisons made* — but a comparison requires the directory to EXIST under the root, so `scanned == 0` implies `compared == 0` and the walk floor was strictly shadowed by its sibling. A rule that cannot produce a finding another does not is **deletable with the suite green**, which is the precise defect this whole board exists to remove. Deleted rather than kept as decoration; its count folded into the surviving message. Ten minutes between writing it and its own gate's discipline catching it.
| `GTD-6` | **A floor placed above the violation report turned a real finding into a misuse code.** The PRR-16 case (a service declared `missing` but present on disk) returned **2** instead of **1**, because the zero-comparisons floor fired before violations were counted. Floors exist for the SILENT case; if the gate found something it demonstrably had a subject. Ordering fixed — and the end-to-end self-test is what caught it, which a unit test of `detect_lang` never could have.
| `GTD-5` | **The reach floor I added to satisfy `GT-F3` could not reach its own reason.** Under `set -euo pipefail` a `find` on a missing directory fails, the command substitution fails with it, and the script dies at that line with rc=1 — *before* the floor can report anything. So the guard against "the walk reached nothing" was inert in exactly the case it was written for. **Found by the bite, not by review**, and it is the fourth non-vacuity shape by name: an adjacent decision (a shell option set at the top of the file) defeating a guard added at the bottom. Fixed with `|| true` inside the braces. |
| `GTD-4` | **`dependency-registry-lint` reported `2` violations against a real `49`, for months.** The counter incremented once per GREP BLOCK, not per hit, so two categories read as two call-sites. Anyone tracking the ClientFactory migration off that line — and `DEFERRED 082` is exactly that — would think it was nearly done. Nobody noticed because the gate runs in WARN mode and exits 0 either way: **"it passed" and "it found 49 problems" were the same observable.** |
| `GTD-3` | **`GT2`'s batch was grouped by a shape I had not read.** I filed `dependency-registry-lint.sh` under *registry membership* on the strength of its NAME; it is a forbidden-shape scanner and shares nothing with `capacity-budget`. `GT-F2` says batch by shape precisely so the design cost is paid once — and I grouped by filename, which is the same error one level up. Split into `GT2b`; the remaining `GT2` entries are still unread and may move too. |
| `GTD-2` | **Handed bash an absolute Windows path and all four arms died at `rc=127`.** `D:\Works\…` arrives as `D:Works…` — backslashes eaten as escapes. Cost one full harness run to diagnose something the tree had already taught me twice this session. |
