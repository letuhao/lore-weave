# Plan — K-detect (mui #1c phase 4)

- **Date:** 2026-06-07 · **Branch:** `glossary/ai-pipeline-v2` · **Size:** L
- **Spec:** `docs/specs/2026-06-07-entity-resolution-merge.md` §3.1 + §7c (DESIGN locked)
- **Workflow:** v2.2 human-in-loop (no /amaw — epic L4); `/review-impl` at POST-REVIEW.

## Acceptance (from spec §4)
- **AC1:** co-occurring same-character names (姜子牙/太公望/子牙) surface as ONE cluster; two distinct 李靖 (no shared structure) do NOT (or surface low → dismissable).
- **AC7:** runs without entity-embeddings (name + KG-structural only) and is bounded (blocking, no O(n²)).
- Plumbing AC: a detect pass proposes clusters to glossary via `propose_merge_candidates`; LLM-verify gates above-floor pairs when configured, degrades to score-only otherwise.

## Build order (TDD where the logic is pure)
1. **config.py** — add the 11 `coref_*` settings (defaults per §7c).
2. **coref_detect.py — pure scorer first** (`CorefEntity`, `_name_signal`, `_structural_signal`, `score_pair`, `block_and_score`, `cluster_pairs`). Unit-test these in isolation (AC1 pos+neg, blocking bound, clustering transitivity).
3. **coref_detect.py — orchestrator** (`detect_from_records`) + `_verify_pair` (LLM) + `_load_coref_entities` (Neo4j `run_read`). Unit-test `detect_from_records` with fake llm + fake glossary (score-only path, verify-rejects, verify-fails-degrades, propose-shape).
4. **routers/coref.py** — `POST /internal/coref/detect` (internal-token; `ProjectsRepo.get` → book_id; calls orchestrator).
5. **main.py** — register the coref router.
6. **internal_extraction.py** — gated auto-hook at end-of-book (default OFF).

## VERIFY
- `pytest tests/unit/test_coref_detect.py` green in-container (docker cp pattern).
- Full knowledge unit suite net-zero new failures.
- Cross-service (≥2 services at runtime via propose): **LIVE-SMOKE deferred to D-GLOSSARY-MERGE-LIVE-SMOKE (062)** — needs a real duplicate-bearing project; the propose contract is unit-covered + G-cand DB-verified.

## Risks
- **Blocking recall** (L2): name+structural only; purely-semantic coref with no name/structural overlap is missed — accepted v1, embedding signal is the documented upgrade.
- **LLM cost**: bounded by `coref_max_pairs` + only above-floor pairs verified + config-gate.
- **Loader is the only Neo4j-coupled part** — kept thin, deferred to live-smoke; all decision logic is in the pure/fake-tested layer.
