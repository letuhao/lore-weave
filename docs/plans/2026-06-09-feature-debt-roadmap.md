# Feature-Debt Roadmap — composition/LOOM branch (`feat/composition-service`)

> **Created:** 2026-06-09 (LOOM-62 audit) · **HEAD:** `1edb777f` · **PR #19** open → `main`.
> **Status:** PLAN ONLY (PO 2026-06-09 — write the roadmap, build nothing yet; PO greenlights each cycle).
> **Scope:** FEATURE incompleteness **inside this branch's domain** (composition / knowledge / learning / worker-ai / FE / sdks). Other-track debt (tilemap, geo, game-server, enrichment P2/P3, provider-registry, glossary, tooling) is explicitly OUT — those have their own tracks.
> **Method:** grounded by a 3-agent audit (spec-vs-code, deferred+markers, FE). Every row cites file:line evidence.

---

## How to read this

"Incomplete" splits into three *kinds*, and they are NOT equally urgent:

- **🔴 Phase 1 — Half-built (feature LIES).** Code exists + looks wired, but is a no-op / inert / dormant-prompt. These masquerade as complete; they are the genuinely bad debt. Fix regardless of strategy.
- **🟡 Phase 2 — Long-form reasoning engine (Phase B).** Designed in the spec but **deliberately gated** behind "only build if A wins" (reasoning-engine spec §8 validate-first). A's coherence validation **saturated the metric** (lesson: eval-metric-saturation), so building this blind is the exact trap. EVAL-GATED, lower confidence. Prioritized internally per the lesson *"the real lever is F2/narrative_thread state-reinjection, NOT Phase-B recipes"*.
- **🟢 Phase 3 — Polish / UX / hardening.** Real but LOW/MED; usability + latent-bug cleanup.

> **Production-ready principle (PO 2026-06-09):** this is a production product, not a toy build. There is **no "intentional seam we just skip"** — every designed-but-unbuilt escape valve, every dormant flag, every "deferred to a later session" marker gets an explicit **decision before go-live: prove-it-is-needed (measure), build-it, or consciously remove-it**. "Benign-by-design" is only acceptable once it's a *recorded decision*, not a silent gap. The audit's borderline/intentional items are therefore tracked as real rows (FD-21…FD-26), not excluded.

**Spec SSOT:** `docs/specs/2026-06-05-composition-v1-reasoning-engine.md`. **Sub-plans:** `docs/plans/2026-06-05-composition-v1-phase-a.md`, `docs/plans/2026-06-06-comp-longform-state-reinjection.md`.

**Merge relationship:** PR #19's close-out roadmap is COMPLETE; the branch has zero *tracked-as-blocking* debt. This roadmap is the *next* body of work. Per-phase merge guidance is noted below — the PO decides whether any Phase-1 item is a merge-blocker or a fast-follow.

---

## 🔴 Phase 1 — Half-built honesty fixes (feature appears done but is a no-op)

| ID | Feature gap | Evidence | Size | Eval-gated? | Notes / dependency |
|---|---|---|---|---|---|
| ~~**FD-1**~~ 🟢 S2 DONE | ~~narrative_thread is INERT~~ — **PO reshaped (2026-06-09): build the FULL feature, sliced (S2→S3→S4), not a "minimal" sliver.** ✅ **S2 (producer) built (LOOM-64):** `app/engine/narrative_thread.py` `detect_and_update_threads` — a gated, best-effort LLM pass on the auto-generate commit (scene+chapter) opens promises/foreshadows/MICE-opens + pays resolved open threads. The ledger now has a real WRITER (no longer inert). Opt-in per-Work (`narrative_thread_enabled`, default off); never fails generate (F1); dedup-by-fold + bounded + reasoning-disabled (FD-4 lesson); regen-idempotent. 11 unit tests (detector + wiring). /review-impl: MED (wiring untested→helper+3 tests) + LOW (context cap 50) fixed, 1 LOW (concurrent-dup TOCTOU) recorded. **S3 = FD-7 (re-inject list_open → loop closes), S4 = FD-8 (debt+eval).** | composition `engine/narrative_thread.py` + engine wiring + deps/config | L | YES (S4) | S2 done; S3/S4 next (= FD-7/FD-8). |
| ~~**FD-2**~~ ✅ DONE | ~~Chat→KG extraction is a no-op~~ — **BUILT FULLY (LOOM-76, option A).** Was dead at 2 points: knowledge `handle_chat_turn` enqueued `aggregate_type='chat_session'` (worker drains `'chat'`) + worker passed `text=""`. Fixed: type-align + new chat-service internal `GET /internal/chat/turns/{id}/text` (joins user Q + assistant A, role-guarded) + worker-ai `ChatClient` threaded through the turn loop. **/review-impl also surfaced no auto-drain** → built `_ensure_chat_pending_jobs` (mirror of the chapter drainer) so chat→KG is near-real-time per turn (not manual-only). **Full-chain live-smoke ✓** (auto scope='chat' job → endpoint 200 → extract → persist, $0.004). | knowledge `handlers.py`, chat-service `routers/internal.py`, worker-ai `clients.py`+`runner.py` | L (cross-3-svc) | NO | Done — auto-drain + endpoint + ChatClient; 12 tests + live-smoke. |
| ~~**FD-3**~~ ✅ DONE | **Scene-leaf summary uses placeholder text** — was a stub returning Neo4j `path` strings → summaries built from path noise. **FIXED (LOOM-75):** `_load_scene_leaf_texts` rewired to `book_client.list_scenes_by_chapter` (real ordered `leaf_text`) + `get_chapter_draft_text` fallback for legacy chapters (mirrors pass2 D8); `None`→transient re-enqueue. 5 unit tests; **live-smoke: book-service /scenes returns real leaf_text**. The summary md5 naturally re-keys (paths→prose) → chapters re-summarize correctly. | DONE | quality restored | — |
| ~~**FD-4**~~ ✅ DONE | ~~status_effect prompt is DORMANT~~ — **PREMISE WRONG: the prompt was ALREADY active (fe0dae9e). The canonical 067 run found the REAL bug:** `event_order` was derived only from the part-gated `hierarchy_paths.chapter_index`, so a **flat book (no parts)** got `event_order=None` → every `status_effect` skipped + the dense timeline axis collapsed. **Fixed** (5-layer `chapter_index` fallback) + **live-proven** (part-less death-chapter → fresh `:EntityStatus` with chapter-sort `from_order`; was 0). Also fixed the stale event.py dormancy comments + confirmed the reasoning-model-emits-empty caveat. 5 regression tests. Resolves DEFERRED 066/067. | sdk event.py + knowledge pass2_writer/internal_extraction + worker clients/runner | L (was M) | live-proven | Done LOOM-63. **Follow-up (filed): a zero-event / reasoning-model extraction warning guard.** |
| ~~**FD-5**~~ ✅ DONE | ~~full context mode may be unbuilt~~ — **VERIFIED STALE DOCSTRING (LOOM-63):** `builder.py:63-64` dispatches `build_full_mode` on `extraction_enabled`, no `NotImplementedError` remains, full-mode is built + reached in prod. Fixed the stale "Commit 1 scaffold" docstring. No gap. | knowledge `app/context/modes/full.py` | XS | NO | Done — docstring corrected. |
| ~~**FD-21**~~ ✅ DONE | ~~MCP tool-call path unproven live~~ — **PROVEN LIVE (LOOM-77).** Ran the real chat-service → knowledge-service `/mcp` `memory_recall_entity` call on the live stack → `success=true` + `found=true` (recalled the `Kael` entity FD-2 wrote, confidence 1.0). Exercises real Streamable HTTP + JSON-RPC init + `call_tool` + header-only scope. **Decision: prove → keep the `use_mcp_tools=True` default** (now live-justified); no code change. DEFERRED 056 cleared. | DEFERRED 056 ✅ | M (cross-svc) | NO | Done — live-smoke gate passed (transport + data round-trip). |
| ~~**FD-22**~~ ✅ DONE | ~~Worker-notification "deferred to K16.6"~~ — verified: worker already **polls** (K16.6 landed as polling, works). **PO chose to BUILD the push-notify** (not just close the seam): knowledge XADDs a best-effort Redis wake (`extraction.wake`) on job-start; worker-ai blocks on it (`XREAD BLOCK`) instead of a blind sleep → ~immediate pickup. Wake-over-poll (poll stays source-of-truth → no double-process); degrades to polling on any Redis fault. Plan: [2026-06-09-fd22-extraction-wake.md](2026-06-09-fd22-extraction-wake.md). /review-impl: 2 MED fixed (block=0 hang floor, cross-service stream-name pin), 2 LOW documented. Live-smoke PASS (real Redis :6399). | knowledge + worker-ai | L (reclassified from XS) | live-smoke | Done LOOM-63. |

**Phase-1 order:** FD-5 + FD-22 (cheap verifies, may be non-issues) → FD-4 (067 diagnostic then activate) → FD-1 minimal-runnable → FD-3 → FD-2 → FD-21 (MCP live-smoke gate). FD-2/FD-3/FD-21 are cross-service and depend on upstream endpoints/stack-up — may slip if chat/book endpoints or a bootable stack aren't ready.

**Merge guidance:** FD-1 (narrative_thread not-lying) and FD-4 (status producer) are the strongest "should this be in PR #19" candidates since the PO chose to build the ledger and the canon-death arc. FD-2/3/5 are platform gaps not specific to the composition V1 arc → fast-follow is fine.

---

## 🟡 Phase 2 — Long-form reasoning engine (Phase B) — EVAL-GATED, validate-first

> ⚠️ **Strategy gate (spec §8):** the spec says build Phase B *only if Phase A wins coherence*. A's validation **saturated** (coherence 5/5 both arms on short scenes — lesson `eval-metric-saturation`). So this whole phase is a **bet**, not a known-good. Each item MUST ship behind a discriminating eval (correction-rate / dropped-promise-rate / contradiction-rate — NOT a ceiling-prone coherence median). Prioritized per lesson: **F2/narrative_thread state-reinjection is the real lever; craft_recipe + recipe-checks are speculative and ranked last.**

| ID | Feature (spec §) | Status | Size | Priority | Eval signal |
|---|---|---|---|---|---|
| ~~**FD-6**~~ ✅ DONE | narrative_thread **S2 — OPEN detection producer** (§5.2/§7) — built as the FD-1 reshape (LOOM-64): `detect_and_update_threads` opens promises + pays resolved threads on the auto-generate commit. Gated opt-in, best-effort, 11 unit tests. (Eval of dropped-promise-rate lands with S4/FD-8.) | DONE | — | (FD-8) |
| ~~**FD-7**~~ 🟢 S3 CORE DONE | **S3 — re-injection** (§3/§6/§10.3) — built (LOOM-65): `gather_open_promises` lens → `<open_promises>` pack block (protected, capped, sanitized) + a `build_messages` steer so the model honors/pays them. **The loop is closed** (S2 writes → S3 re-injects + steers). Gated opt-in, degrade-safe. 14 unit tests; /review-impl folded (sanitize MED + steer + pack-gate + position-filter recorded). **Remaining (deferred, optional):** the formal `ReasoningState` object + `generation_job.state` persistence (resumable runs) — the core F2 lever is the pack re-injection, which is done; persistence is a separate resumability concern. | composition pack/engine | L | (S4 eval) | core done; ReasoningState-persist optional follow-up. |
| ~~**FD-8**~~ ✅ DONE | **S4 — arc-end DEBT check + eval** (§7). ✅ **S4a (debt-check BE, LOOM-66):** `GET …/narrative-threads` (`open`=unpaid debt \| `all`=full ledger; `open_count`=a true COUNT) + advisory `open_promise_count`; read-only, D4. 7 unit tests. ✅ **S4b (eval + live-smoke, LOOM-67):** `reinjected_promise_count` (deterministic S3-fired signal) + a **ledger-BLIND** `promise_audit` judge + `/promise-audit` route + the `eval_narrative_thread.py` ON/OFF harness. **★ FULL-LOOP LIVE-SMOKE PASSED** (real stack, n=1 & n=3): S2 opens → S3 re-injects → control OFF=0 → S2 pays (5/7/7). **⚠️ EVAL NULL:** dropped-rate OFF 0.454 vs ON 0.429 (Δ=+0.025, noise; 1/3 wins) — **no measured lift.** 381 unit; /review-impl MED (rate-clamp) + 2 LOW. **The ledger is BUILT + live-proven + measured** (ships ADVISORY/opt-in; no quality-lift claim). | **DONE** (built+proven; eval null) | M | **3** | ✅ FD-6/7 proven functional; lift NOT shown |
| **FD-9** | `search`/backtrack combinator (§3b/§6/§10.1): on reflect-exhaust, re-enter `diverge` at this/prior scene with the violation as a new constraint (instead of stop-and-keep-last). | NOT-BUILT (`reflect_revise` caps at max_iters then stops — `canon_check.py:280,302`) | L | 4 | contradiction-resolution-rate |
| **FD-10** | Autonomous arc/book loop (§6): drive the decompose plan scene-by-scene unattended, ReasoningState carried between scenes, capped by `generation_run`, per-chapter human checkpoints. | PARTIAL (single scene/chapter only; no multi-scene driver) | L-XL | 5 | end-to-end long-form defect count |
| **FD-11** | Canon-check breadth (§5.1): broaden beyond "gone entity present" → ConStory-taxonomy judge rubric (timeline/world/factual/memory classes), multi-status `{active,lost,destroyed}`. | PARTIAL (only `gone` literal — `canon_check.py:96,108,133`) | M-L | 6 | contradiction-recall |
| **FD-12** | `craft_recipe` library + compiler (§4/§10.3): table + recipe→primitive compiler so a Work composes 1 template + N recipes. | NOT-BUILT (0 code) | L | 7 (speculative) | only if FD-6/7 show lift first |
| **FD-13** | Craft-method checks (§4b-e): But/Therefore, Scene-Sequel, try-fail escalation, MICE LIFO, monotone tension. | NOT-BUILT | L | 8 (speculative) | per-check ablation |
| **FD-14** | Extra structure templates Freytag / 7-point / Fichtean (§4a). | NOT-BUILT (only 6 builtins — `db/migrate.py:248-324`) | S | 9 (cheap, low-risk) | none (additive seed) |
| **FD-23** | **`llm_judge` reasoning-effort engine** (§3 auto-reasoning) — a small pre-call LLM rating difficulty, as an alternative to the rule-based scorer. The `resolve` seam exists; only `rule_based` is implemented. | NOT-BUILT (intentional seam — `reasoning/policy.py:9-12`) | M | 10 (conditional) | rule-based scorer underperformance |
| | **Production decision (not "skip"):** measure whether the rule-based scorer is good enough on production traffic. If yes → **consciously close** the seam (document "rule-based is sufficient", remove the unbuilt-path framing). If no → build `llm_judge`. Do NOT leave it as an unmeasured "maybe later". | | | | |

**Dependency:** FD-6 → FD-7 → FD-8 is the spine (and is the FD-1 continuation). FD-9/10 build on the ReasoningState from FD-7. FD-12/13 are the *recipe* surface — **do not start until FD-6/7/8 demonstrate a measured lift** (else it's the recipe-trap the lesson warns about). FD-14 is a cheap standalone.

> 🔴 **EVAL GATE RESULT (LOOM-67 v1; RE-CONFIRMED LOOM-68 v2):** the FD-8 ledger-lift gate is **NOT met**, now under a *methodologically sound* eval. **v1 (LOOM-67):** dropped-promise-rate NULL (OFF 0.454 vs ON 0.429) — but confounded (unstable denominator, cutoff-as-dropped, coarse judge). **v2 (LOOM-68):** rebuilt the eval — FIXED spec-derived promise set (kills introduced-inflation) + paid/progressing/abandoned/absent verdicts + **chapter-mode generation so arcs reach the payoff** (the fix that makes pay-rate discriminate; per_scene truncated → pay-rate stuck at 0). v2 NOW discriminates (pay-rate spans 0–1). Result: n=1 hinted a lift (ON 1.00 vs 0.33) but **powered n=3 did NOT replicate** — pay OFF=0.500 vs **ON=0.292** (ON worse; paid/tracked OFF 0.477 vs ON 0.292), abandon ON 0.000 vs OFF 0.056. **No lift even under the good metric.** The ledger biases toward sustain-more/abandon-less but **pay-less**. Therefore **FD-9, FD-10, FD-12, FD-13 stay DEFERRED/speculative** with STRONGER evidence — do not build on this. The eval v2 (`eval_narrative_thread.py --mode=chapter` + `/promise-extract` + `/promise-coverage`) is now the permanent discriminating harness. FD-14 (cheap template seed) remains a free standalone. **Separately (LOOM-68 quality diagnostic):** the architecture is sound (chapter mode → coherent arc); the prose ceiling is the **local model** (qwen3.6-35b tics + CJK code-switch leak) → the quality levers are (i) author-selectable stronger drafter, (ii) anti-repetition, NOT more ledger.

**Merge guidance:** entirely post-merge (validate-first; not blocking PR #19).

---

## 🟢 Phase 3 — Polish / UX / hardening (in-branch, LOW-MED)

| ID | Item | Evidence | Size |
|---|---|---|---|
| ~~**FD-15**~~ ✅ DONE | ~~Planner FE affordances~~ — **BUILT (LOOM-85), all 4.** `beat_role` editable input (PlannerTree → `editChapter`); cast add/remove chips + roster `<select>` (PlannerSceneRow → `editScene`, reuses `useGlossaryRoster`); resolve `present_entity_names_unresolved` (name→roster-id one-click, non-matches stay hints); planner-local model picker (PlannerView, overrides the panel model for preview). Pure-FE (hook+BE pre-existing). /review-impl verified the commit body accepts beat_role + present_entity_ids (no silent drop). 8 vitest + 96 composition suite + tsc + i18n parity (plan=31×4). | composition FE (PlannerView/Tree/SceneRow) | L (FE) |
| ~~**FD-16**~~ ✅ DONE | ~~Canon-rule FE: edit-in-place + set entity_id/from_order/until_order/active~~ — **BUILT (LOOM-84).** New `useGlossaryRoster` (shared with FD-15) + `CanonRuleForm` (scope-conditional entity picker + reveal window + active toggle) + `CanonRulesPanel` rewrite (full-payload create + edit-in-place wiring the previously-unused `patch`). BE was already complete. /review-impl verified the PATCH null-clearing round-trip (entity_id/from/until are nullable-update cols → scope→world clears bounds); added an edit-clears-bounds regression lock. 7 CanonRulesPanel vitest + 90 composition suite + tsc + i18n parity (121×4). | composition FE (`CanonRuleForm`/`CanonRulesPanel`/`useGlossaryRoster`) | M (FE) |
| ~~**FD-17**~~ ✅ DONE | ~~069 — A3 replace re-plan leaves orphan arc/chapter nodes~~ — **FIXED (LOOM-78).** `commit_decomposed_tree(replace)` now archives `scene`+`chapter` for all `affected` chapter_ids + a scoped childless-arc sweep (parent arcs of the archived chapters only). /review-impl HIGH folded (project-wide→scoped, no bystander-arc collateral). 3 real-PG tests; 47 integration pass. DEFERRED 069 cleared. | DEFERRED 069 ✅; `db/repositories/outline.py` | S (BE) |
| ~~**FD-18**~~ ✅ DONE | ~~051 — knowledge consumer.py has the dead-retry-in-PEL bug~~ — **PORTED (LOOM-80).** Added the learning-service F-A1 `XAUTOCLAIM` reclaim to knowledge `consumer.py` (consts + loop trigger + `_reclaim_stale_pending`, tombstone-ack + NOGROUP-tolerant) so a failed chapter/chat/glossary event's retry→DLQ advances in steady state, not just on restart. 13 tests (5 new incl. the fail→DLQ invariant, /review-impl LOW). DEFERRED 051 cleared. | DEFERRED 051 ✅ | S |
| **FD-19** | Correction-feedback loop (learning Phase-B/C/E): **048** Tier-1 anchor consume, **050** raw-content opt-in (Tier-3 gold), **049** ReplayCorrections admin task, **052** desc-diff granularity, **053** emit permanent-failure surface, **046** KS↔PG reconcile sweep. | DEFERRED 048/050/049/052/053/046 | each S-M; batch by area |
| ~~**FD-20**~~ ✅ DONE | ~~Dead-code / minor~~ — **TRIAGED (LOOM-82).** Only 1 of 4 was genuinely dead: **`composition/api.ts` `getJob`** — no callers, no tests → **deleted** (`GenerationJob` import retained; tsc clean). The other 3 are NOT dead code but **documented intentional deferrals**, kept as-is: (a) pass2 `scene_id=chapter_id` (knowledge `pass2_orchestrator.py`) is an active legacy-single-scene path tagged `D-P2-PER-SCENE-FANOUT`; (b) events.py timeline axes (`entity_id`/date-range/chronological) are tagged `D-K19e-α-01/02/03` with rationale (participants store names not ids; :Event has no date field); (c) packer `lenses.py` "L5 deferred" is a future long-term-summary lens (docstring-noted). Roadmap paths were stale (pass2 is knowledge not composition; lenses is composition not knowledge). | composition `api.ts` (+ 3 documented-deferral confirmations) | XS |
| ~~**FD-24**~~ ✅ DECIDED | ~~M7d-3 learning worker-feed dormant~~ — **DECISION: keep as a documented opt-in (LOOM-81, PO 2026-06-10).** Investigation corrected the framing: it is NOT a half-feature — the M7d online *translation fidelity judge* is **fully built and cleanly dual-gated off**: translation-service `_emit_translation_quality` ([chapter_worker.py](../../services/translation-service/app/workers/chapter_worker.py)) ships truncated `source_text`+`translated_text` (cap `n_feed_max_chars=2000`) into `translation.quality` only when `n_feed_enabled`; learning `_maybe_judge_translation` ([handlers.py](../../services/learning-service/app/events/handlers.py)) runs an LLM fidelity judge → persists the `n_fidelity` 0–1 metric only when `online_translation_judge_enabled` + `online_judge_model_ref` are set **and** the text is present. Off-by-default is **correct** (per-translation online LLM judging has ongoing token cost) and nothing consumes `n_fidelity` yet, so it is a cost-justified opt-in, not a silent gap. **To enable:** translation `N_FEED_ENABLED=true` + learning `ONLINE_TRANSLATION_JUDGE_ENABLED=true`, `ONLINE_JUDGE_MODEL_REF=<user_model uuid>`, `ONLINE_JUDGE_USER_ID=<owner>`. Revisit only if/when a consumer of `n_fidelity` is built (then enable+eval). | learning + translation (opt-in, documented) | S (decide) ✅ |
| ~~**FD-25**~~ ✅ DECIDED | ~~Extraction throughput / parallelism~~ — **DECISION: measured-deferral with a concrete recipe + trigger (LOOM-83, DEFERRED 072).** Per CLAUDE.md ("Perf items — fix when profiling shows pain") + this roadmap's own merge-guidance ("before a real-load deploy, not the composition arc"), building speculative concurrency on the consumer hot path (just modified for FD-18) with zero load evidence is premature. Recorded the gap + the cheap fix path so it's tracked, not silent: **(1)** consumer batch-concurrency — `asyncio.gather` over each XREADGROUP batch (≤`count`=10, msg_ids independent → safe) instead of the sequential for-loop; **(2)** D-CM3C — move the heavy `chapter.published` inline passage embed behind the `extraction_pending` drain (the real per-message cost); **(3)** worker-ai job consumer — scale replicas (consumer_name is host-based → multi-instance ready). **Trigger:** publish→extract latency or Redis consumer-group lag (`XINFO GROUPS` pending) exceeding threshold under real load. The heavy LLM R+E+F is ALREADY parallel in worker-ai (separate process). | DEFERRED 072 (perf); consumer batch path | M (when triggered) |
| **FD-26** | Minor cleanups: `mining.py:176` node→run provenance link deferred; richer truncation surfacing (cy16 shipped `finish_reason`; the char-estimate richer signal stays deferred — confirm it's wanted or drop). | learning `app/db/mining.py:176`; composition `app/routers/engine.py:640` | XS each |
| ~~**FD-27**~~ ✅ DONE | ~~Zero-event / reasoning-model extraction guard~~ — **BUILT BOTH (LOOM-79, PO choice).** (1) **Symptom guard:** worker-ai post-`extract_pass2`, substantive input + `candidates.is_empty()` → `EXTRACTION_ZERO_OUTPUT` warn + `worker_ai_extraction_zero_output_total{source_type}` (cause-agnostic). (2) **Capability pre-check:** new pr `GET /internal/models/{src}/{ref}/info` (name+kind, no secrets) → worker-ai `ProviderRegistryClient` + `_is_likely_reasoning_model` name-heuristic → once-per-job advisory + counter (scope-gated to extracting scopes). /review-impl MED folded (glossary_sync false advisory). 24 worker-ai tests + cross-svc live-smoke (gemma→False, qwen3.6→True, gpt-4o-mini→False). **Caveat:** heuristic best-effort (name-based). | worker-ai runner + pr endpoint | L (cross-svc) |

**Merge guidance:** all post-merge (none blocking). FD-15/16 improve the just-shipped planner/canon FE. FD-25 (throughput) matters for go-live scale, not for the composition arc — schedule before a real-load deploy.

---

## Recommended sequencing (when the PO greenlights building)

1. **Cheap verifies first** — FD-5 (full-mode reachable?) + FD-22 (K16.6 stale?). Both may collapse to "delete a stale marker"; do them before committing build effort.
2. **Phase 1 honesty** — FD-4 (067 diagnostic → activate status prompt) + FD-1 minimal-runnable (ledger stops lying) + FD-3 + FD-2 + FD-21 (MCP live-smoke gate). Removes every "feature that looks done but is a no-op".
3. **Decide merge timing** — merge PR #19 either after Phase 1's in-arc items (FD-1, FD-4) or now-with-fast-follow; FD-2/3/21 (cross-service) and all of Phase 2/3 are post-merge.
4. **Phase 2 only behind eval** — run FD-6→7→8 (the F2/ledger spine), gate on dropped-promise-rate; **only** proceed to FD-9/10/11 then FD-12/13 if a lift is measured. FD-14 is a free cheap add anytime. **FD-23 (`llm_judge`) = a production decision** (measure rule-based → close the seam or build), not a "maybe later".
5. **Phase 3** — batch FD-19 by area; FD-15/16 when the planner/canon FE is next touched; **FD-24 (M7d-3) = decide enable-or-remove**, don't ship dormant; **FD-25 (throughput) before any real-load deploy**.

**Production-ready close-out condition:** the branch's feature ledger is "done" only when every FD-1…FD-26 is either BUILT, or a **recorded conscious decision** (removed / proven-not-needed / scheduled to a named track) — no silent dormant code, no unproven-live cross-service path, no unmeasured seam.

**Won't-do here (separate tracks, not silent — recorded):** all other-track items (tilemap/geo/game/enrichment P2/P3/provider-registry/glossary/tooling) own their tracks; 063 grounding-compose (own post-merge track); 064 (deferred-on-trigger, re-confirmed cy15).
