CYCLE 4 — glossary_ontology_delete SHIP boundary, exercised 2026-08-23

The generic ship_probe could not reach this tool's boundary at all: `items` is an array with
minItems:1, so absent/tenancy/empty all failed SCHEMA VALIDATION before touching anything —
"validating /properties/items: minItems: array length 0 is less than 1", three times, which reads
like a boundary result and is not one. Third instance of that blind spot in this loop.

TENANCY + BOOK ACCESS (well-formed items, direct MCP)
  absent   refused   book not accessible
  tenancy  refused   book not accessible
  empty    refused   book_id must be a UUID when scope=book
  oracle   IDENTICAL (absent == tenancy: no existence oracle)

ABSENT CASE, as this tool's OWN ship_audit defines it — "a kind or genre that the book has NOT
adopted must be refused". My first probe used a nonexistent BOOK, which tests book access rather
than the item and conflated the two. Re-run on a REAL throwaway book the fixture owns, with a code
it never adopted, THROUGH THE CONFIRM GATE:
  mint    ok, confirm_token issued
  redeem  200 {"deleted": [], "deleted_count": 0, "skipped": ["no-such-kind-xyz"], "skipped_count": 1}
  -> the effect is HONEST: nothing deleted, the unknown code named as skipped. It is NOT the
     "archived:true over 0 rows" defect this loop found on arc templates.

IDEMPOTENCY, through the real gate, on the probe's own throwaway book (destructive, permitted
because the target is the run's own fixture and is torn down after):
  delete #1  200 {"deleted": ["character"], "deleted_count": 1, "skipped": [],          "skipped_count": 0}
  delete #2  200 {"deleted": [],            "deleted_count": 0, "skipped": ["character"], "skipped_count": 1}
  -> a real delete, then a CLEAR NO-OP that names the skip. Exactly what the audit demanded.

🔴 ONE LIMIT ON THIS EVIDENCE, stated rather than glossed: my independent
glossary_book_ontology_read helper returned [] both before and after, so the reader's shape is
wrong and the store was NOT independently confirmed. The discriminating evidence is the tool's own
two DIFFERENT responses — a tool reporting success over zero rows would have said deleted_count:1
twice, and this one did not.
