ARC-TEMPLATE IDEMPOTENCY — the run behind D-DATA-BAR-BLIND-TO-ACCOUNT-SCOPED-ARC-TEMPLATE
(2026-08-23; the probe crashed on the glossary entry that followed, before writing its JSON, so
this is the terminal output preserved verbatim rather than a file it produced.)

=== composition_arc_template_edit ===
  args     {"op": "archive", "arc_id": "01a0214a-b003-7bc5-a412-b7da8981da3a"}
  first    ok: {"id": "...", "archived": true, "already_archived": false, "_meta": {"undo_hint": ...
  second   ok: {"id": "...", "archived": true, "already_archived": true,  "_meta": {"undo_hint": ...
  diff 1   {}
  diff 2   {}
  STRICTLY IDEMPOTENT — the second call touched nothing at all
  ⚠ BUT THE FIRST CALL CHANGED NOTHING EITHER, so this probe measured two no-ops and proves
    nothing about idempotency.

READ IT THIS WAY. The tool's two RESPONSES are correct and are not the problem: already_archived
goes false -> true, so the second call is truthfully reported as a no-op. That is the earlier
"archived:true over 0 rows, with an undo hint" defect staying fixed.

The STORE claim is what cannot be made. Verified by direct SQL before and after, the row really did
go active -> archived, and store_diff was {} on both calls — because arc_template.book_id is NULL on
51 of 57 rows and store_snapshot sweeps BY book_id. So this is NOT an idempotency pass; it is the
DATA bar being blind to the table the tool writes.

The shared fixture row was RESTORED to 'active' immediately afterwards, as the probe spec committed
to doing: leaving it archived is what provision-failed five runs earlier the same day.
