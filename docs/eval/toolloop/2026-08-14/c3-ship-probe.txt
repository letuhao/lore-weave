
composition_arc_edit   required=['op']
  absent   refused    MCP composition_arc_edit error: Error executing tool composition_arc_edit: op=create requires book_id
  tenancy  refused    MCP composition_arc_edit error: Error executing tool composition_arc_edit: op=create requires book_id
  empty    refused    MCP composition_arc_edit error: Error executing tool composition_arc_edit: op=create requires book_id
  oracle   identical

glossary_deep_research   required=['entity_id', 'query']
  absent   refused    MCP glossary_deep_research error: book not accessible
  tenancy  refused    MCP glossary_deep_research error: book not accessible
  empty    refused    MCP glossary_deep_research error: book_id must be a UUID
  oracle   identical

world_map_add_marker   required=['map_id', 'label', 'x', 'y']
  absent   refused    MCP world_map_add_marker error: map not found — check the map_id (call world_map_list for valid ids)
  tenancy  refused    MCP world_map_add_marker error: map not found — check the map_id (call world_map_list for valid ids)
  empty    refused    MCP world_map_add_marker error: map_id must be a UUID
  oracle   identical

registry_list_workflows   required=[]
  absent   SUCCEEDED  {"workflows": [{"description": "Set the book drafting itself chapter by chapter, on its own, pausing where the user wants to review — for a user who wants the draft to move forward without steering every line. Needs a st  🔴 SUCCEEDED — a boundary that does not hold
  tenancy  SUCCEEDED  {"workflows": [{"description": "Set the book drafting itself chapter by chapter, on its own, pausing where the user wants to review — for a user who wants the draft to move forward without steering every line. Needs a st  🔴 SUCCEEDED — a boundary that does not hold
  empty    SUCCEEDED  {"workflows": [{"description": "Set the book drafting itself chapter by chapter, on its own, pausing where the user wants to review — for a user who wants the draft to move forward without steering every line. Needs a st  🔴 SUCCEEDED — a boundary that does not hold
  oracle   identical

settings_provider_inventory   required=['provider_credential_id']
  absent   refused    MCP settings_provider_inventory error: not accessible
  tenancy  refused    MCP settings_provider_inventory error: not accessible
  empty    refused    MCP settings_provider_inventory error: provider_credential_id must be a UUID
  oracle   identical

── TARGETED FOLLOW-UPS (2026-08-23), because the generic sweep could not reach two of these ──

composition_arc_edit  (op-dispatch: only `op` is required, so all three generic cases landed on
"op=create requires book_id" and never touched an id. Re-probed with a VALID op read from the
schema — my first attempt used op=archive, which is not in this tool's enum
[create|update|delete|restore|move|assign_chapters]; guessing an op is the same error as guessing
an argument name.)
  absent   refused   not found or not accessible
  tenancy  refused   not found or not accessible
  empty    refused   op=delete requires node_id
  oracle   IDENTICAL (absent == tenancy: no existence oracle)

registry_list_workflows  — the probe's 🔴 "SUCCEEDED — a boundary that does not hold" is a FALSE
ALARM here, and the probe cannot know it. The tool has required=[], so there is no id to make
absent, no id to make foreign, and no field to empty: all three cases degenerate to the same
argument-less call, and SUCCEEDING is the correct behaviour for a zero-argument list read. Its real
boundary is TENANCY OF THE RESULT — whether it returns another account's workflows — which needs a
second account and is not what this probe measures.
