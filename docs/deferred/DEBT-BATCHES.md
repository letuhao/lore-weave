# Debt-Clearing Batches — `feat/auto-draft-factory-gaps`

**Created:** 2026-06-16 · **Source:** full-branch sweep of `docs/sessions/SESSION_HANDOFF.md` (open deferrals) + production-code debt markers (`services/*/app|internal|src`, `frontend/src`, `sdks`). Items already `✅/CLEARED` are excluded.

**Purpose:** turn the scattered deferral backlog into **coherent long-run batches**, each drivable as one continuous `/loom <batch>` flow. Batches are sequenced top-down by priority. A separate **Park bucket** lists items that are *not* debt to clear (R&D / perf-when-pain / HA-scale / wontfix) — do NOT spend long-runs there.

**Legend:** severity `high|med|low` · type `correctness|telemetry|feature-gap|live-smoke|perf|cleanup` · source `[H]`=handoff `[C]`=code.

**How to use:** pick the top open batch → `/loom <batch goal>` → clear its items → tick them here in the same commit. Live-smoke batches: rebuild touched images first (stale = false-green), bring the relevant stack up once, run all smokes in the batch.

---

## Sequencing overview

| # | Batch | Type | ~Items | Size | Status |
|---|---|---|---:|---|---|
| **B0** | Correctness sweep (cross-service, small + high-value) | correctness | 7 | M | ✅ 2026-06-16 |
| **B1** | Jobs GUI telemetry completeness (P4) | telemetry | 9 | L | ✅ 2026-06-17 |
| **B2** | Jobs control completeness (P3) | feature-gap | 3 | M | ✅ 2026-06-17 |
| **B3** | Live-smoke sweep — Job Control Plane + P5 | live-smoke | 7 | M | ✅ 2026-06-17 (all 7 proven; FULL-E2E belt-suspenders remains low) |
| **B4a** | Live-smoke sweep — Auto-Draft Factory (S1–S6) | live-smoke | ~17 | L | ☐ (needs 4-svc campaign stack + browser) |
| **B4b** | Auto-Draft Factory functional/correctness gaps | feature/correctness | ~10 | L | ☐ (not yet scoped) |
| **B5** | Live-smoke sweep — Wiki + Glossary + E0 | live-smoke | ~9 | L | ☐ (needs stack + browser) |
| **B6** | Translation V3 functional gaps | feature/correctness | ~13 | L | ✅ 2026-06-17 (3 fixed + 5 verified-resolved; rest low) |
| **B7** | Knowledge Projects FE (K19) — mobile + filters + polish | feature-gap (FE) | ~20 | L | ◑ reconciled — ~5 already-shipped; rest need design/backend/browser |
| **B8** | Search/Rawsearch + cosmetic cleanup + misc code gaps | mixed/low | ~18 | M→XL | ✅ 2026-06-18 — M1 active-job + M2a grant-SDK + M2b instant-revoke + M3 canon-wiring; rest reconciled/low |

Recommended order: ~~B0~~ ✅ → ~~B1~~ ✅ → ~~B2~~ ✅ → ~~B3~~ ✅ → ~~B6~~ ✅ → ~~B8~~ ✅ → (B7 reconciled). **Remaining open batches: B4a, B4b, B5 — almost entirely live-smokes that need the multi-service stack up + a browser; plus residual low rows in B7/B8 (design-call / backend-metadata / low-polish) and the B6 perf-when-pain items.** Per the 2026-06-17/18 reconciliations, ~40-50% of "open" rows in B6/B7/B8 were already-shipped-but-unticked → **the autonomous-safe code surface is now essentially exhausted**: what genuinely remains needs a running stack/browser (live-smoke), a design call, backend metadata, or a larger refactor.

---

## B0 — Correctness sweep — ✅ CLEARED 2026-06-16
Small, well-defined fixes that reduce latent risk. Cross-service but each is independent.
CLARIFY refined 7 items → **3 real fixes** + **4 no-ops** (handoff-confirmed won't-fix / confirmed-intended / already-resolved).

| ID / location | Description | sev | resolution |
|---|---|---|---|
| `D-JOBS-EMIT-STATUS-PASSTHROUGH-ROLLBACK` | `JobStatus()` raised in-tx on an unmapped native status → rolled back a legit transition | high | ✅ **FIXED** — `emit.py` tolerant `_coerce_status` (canonical/case-insensitive/alias-map) → **map-or-skip** (skip logs `[EMIT_STATUS_SKIPPED]` + `skipped_emit_total()` counter, never raises); reconcile sweep backstops |
| `provider-registry server.go` (embed) | validate model is embedding-capable before dispatch (K12.1 TODO) | high | ✅ **FIXED** — `canEmbed()` rejects only **affirmatively-detected** non-embedding caps (rerank/stt/tts/image_gen/video_gen), fail-OPEN on empty/`chat`-default; `[]byte`+Unmarshal scan (review-impl HIGH×2) |
| `D-TRANSL-VERSION-NUM-RACE` | `version_num = MAX+1` collided on `idx_ct_version` → 500 | med | ✅ **FIXED** — `pg_advisory_xact_lock` at the 2 unguarded insert sites (create-loop sorted for deadlock-safety + save-edited), same key as `patch_translation_block`; wiring pinned by tests (review-impl MED) |
| `D-S5B-EMBED-CREATE-ATOMICITY` | project patch precedes campaign insert (ordering) | med | ⊘ **no-op** — handoff (SESSION_HANDOFF L408) classifies won't-fix (benign post-patch mutation) |
| `D-REDIS8-CONSUMERS` | port `TimeoutError` catch + pin redis across consumers | med | ⊘ **no-op** — already resolved: every blocking consumer (campaign/worker-ai raw + knowledge/learning/video-gen via shared `BaseProjectionConsumer`/`BaseTerminalConsumer`) catches the redis-py-8 idle `TimeoutError`; `redis>=5.0` is version-tolerant given the catch |
| `D-S4A-MIGRATION-ORDERING` | knowledge migration before worker-ai starts | low | ⊘ **no-op** — handoff (L408) won't-fix (inert: running jobs exist only post-migration) |
| `worker-infra outbox_relay.go:108` | confirm quiet-retry on a missing `outbox_events` table | med | ⊘ **no-op** — confirmed intended: video-gen creates `outbox_events` unconditionally at migration → missing-table is transient cold-start, already logged once-per-transition via `noteTableState` |

**Verify:** SDK emit 9/9 (+ skip-counter) · SDK jobs suite 47 passed · provider-registry `canEmbed` 13/13 + pkg compiles · translation versions+jobs 74/74 · provider-gate OK.
**Live-smoke:** deferred to **B3** (Job-Control + P5 sweep, rebuild-stale-first) — tracked as `D-B0-LIVE-SMOKE`. Fixes are unit-proven + low-risk.

---

## B1 — Jobs GUI telemetry completeness (P4) — ✅ CLEARED 2026-06-17
Make the unified jobs dashboard show complete cost/model/tokens for **every** kind + wire retry.
Shipped as 4 milestones (plan `docs/plans/2026-06-16-b1-jobs-telemetry.md`): M1 model-names+spend
(`92850509`/`2e2c8977`) · M2 translation cost (`7354ce5b`) · M3 summary+overlay (`f3fe9430`) ·
M4 retry=re-submit (this commit). **All 9 items done.** Retry shipped for **translation** only;
the other kinds were tracked deferrals; status after the 2026-06-17 retry-extension run:
- `D-JOBS-P4-RETRY-KNOWLEDGE` — ✅ **DONE 2026-06-17**. `extraction` ∈ `_RETRYABLE_KINDS`; knowledge `control_extraction_job` routes `retry` → `_retry_extraction_job_core` (owner-scoped 404, 409 unless `failed`, 409 if campaign-managed) which reconstructs `StartJobRequest` from the failed row (carries every field) and re-runs `_start_extraction_job_core` — RE-VALIDATES the K17.9 benchmark + budget gates, emits `running`, wakes worker. Tests: jobs-service caps + knowledge 4 (happy/404/409-not-failed/409-campaign). Live-smoke → `D-B1-LIVE-SMOKE` (B3; the forward path was proven by `D-SECONDARY-KIND-CONTROL`, the start core by the P5 smokes).
- `D-JOBS-P4-RETRY-VIDEOGEN` — ✅ **DONE 2026-06-17** (NO migration — the earlier "params not persisted" triage was WRONG: `video_gen_jobs.request_json` JSONB already stores prompt/model_source/model_ref/duration/aspect/style). `video_gen` ∈ `_RETRYABLE_KINDS`; `control_video_gen_job` routes `retry` → `_retry_video_gen_job` (owner-scoped 404, 409 unless `failed`, 409 on empty request_json, 404 when stateless/decouple-off) which reconstructs `GenerateRequest` from `request_json` and re-runs the existing `_submit_decoupled` (new pending row + emits `running`, zero submit-logic duplication). No campaign guard (video-gen isn't campaign-dispatched). Tests: jobs-service caps + video-gen 5 (happy/404/409-not-failed/409-missing-params/404-stateless).
- `D-JOBS-P4-RETRY-COMPOSITION` — ✅ **DONE 2026-06-17** (/loom L + /review-impl; design [`…composition-retry-design.md`](../specs/2026-06-17-composition-retry-design.md)). The "bifurcated → not clean" re-scope was REFINED into a clean win: retryability is **per-job** (a job is server-reconstructable iff it is **worker-drivable** — the server stamped a recognized `input.worker_op`, so the row carries the full bearer-resolved context the worker re-runs from), NOT per-kind (composition `kind` = the free-form `operation`, identical for the worker + inline-streamed paths). NO migration — uses the existing `job_projection.params` JSONB: composition `create` emits `params.retryable = is_worker_drivable(operation, input)`; jobs-service `derive_control_caps(status, kind, *, retryable=None)` offers RETRY when `kind ∈ _RETRYABLE_KINDS OR retryable is True` (read by `_with_caps` + the `control_job` gate + `sse.py`); composition `internal_job_control` routes `retry` → `_retry_generation_job_core` (404-not-owned → 409-unless-failed → 409-not-worker-drivable re-checked on the row → 409-worker-disabled → re-submit a NEW pending job, `idempotency_key=None`, guarded create for BOTH chapter-draft writers `chapter_generate`+`stitch_chapter`, then enqueue). **/review-impl 2 MED fixed:** tightened drivability to the server-set `worker_op` (was over-broad via an `operation` fallback → inline stitch / user `operation="generate"` falsely retryable) + stamped `worker_op` on the stitch/decompose worker inputs; guarded stitch retry. FE/SDK zero change. Tests: jobs-service 29 + composition 95. **Deferred:** `D-JOBS-P4-RETRY-COMPOSITION-SSE-CAPS` (LOW — failed SSE frame omits the retry cap, `params=None` on the failed emit; read API authoritative) + `D-JOBS-P4-RETRY-COMPOSITION-LIVE-SMOKE` (worker stack + a deterministically-failing job).
- `D-JOBS-P4-RETRY-LORE` — ✅ **DONE 2026-06-18** (/loom M, spec+plan [`…lore-enrichment-async-decouple.md`](../specs/2026-06-18-lore-enrichment-async-decouple.md)). The "BLOCKED on an XL async-decouple" tag was WRONG: a RISK INVESTIGATION found the production create path (`auto-enrich`+`compose`) is ALREADY async/worker-driven — only a vestigial no-caller `POST /jobs` is synchronous. So retry was just unwired control, not an async refactor. Shipped as 4 milestones: **M1** per-job advisory-lock claim in `redrive_one` (status-agnostic `pg_try_advisory_lock` on a 64-bit job-id key — a status CAS would reject a legit resume; HIGH-2 double-spend guard); **M2** cancel/pause-mid-run honoring (pre-flight + between-gap status reads) + guarded terminal writes (`mark_job_status` `only_if_status` CAS + control endpoint `SELECT … FOR UPDATE`) so the runner can't clobber an external cancel (HIGH-1 "ran for days despite cancel" class); **M3** `enrichment_job ∈ _RETRYABLE_KINDS` + lore `POST /{job_id}/retry` (failed→running CAS + emit + re-drive enqueue; idempotent via M1 claim + skip-done); **M4** sync endpoint marked demo-only + retry route added to the OpenAPI spec. FE control caps already data-driven (no change). Tests: 926 lore non-DB + 73 jobs-service. **Live-smoke (real PG + rebuilt images, this session):** M1 advisory-key SQL valid + `pg_try_advisory_lock` ok; M3 retry flips failed→running + emits + XADD (+1) + worker consumed the re-drive via `redrive_one` (M1 claim → clean no-request drop); M2 cancel running→cancelled (FOR-UPDATE) + `only_if_status` CAS applies/no-ops correctly; retry-non-failed→409. **/review-impl** (post-commit, all findings fixed in-loop): **MED-1** enqueue-failure left a resume/retry'd job stuck `running` + not re-triggerable (no stranded-running sweeper) → added `_compensate_stuck_running` (roll the flip back to paused/failed on XADD failure, guarded) + fixed resume's false "re-triggerable" comment; **LOW-2** the real CAS/key SQL had no CI test → added `tests/db/test_job_status_cas.py` (3 real-PG round-trips); **LOW-3** cancel-mid-run under-reported cost → `_finalize_interrupted` now persists spend via a status-preserving no-emit `record_actual_cost`; cosmetic unlock-comment corrected. **✅ `D-LORE-ASYNC-DECOUPLE-LIVE-SMOKE` PROVEN (2026-06-18, real Qwen + rebuilt lore/worker images, [`tests/live_smoke_async_decouple.py`](../../services/lore-enrichment-service/tests/live_smoke_async_decouple.py)).** Drove the REAL background worker (not in-process) over a throwaway project with real bge-m3 grounding + real qwen3.6-35b generation: **ST1** seed job_request + XADD → worker `run_job` → `completed` + 1 quarantined proposal; **ST2 (M2/HIGH-1)** a 2-gap job cancelled mid-run (after gap 1) → ends `cancelled` (NOT completed), 1<2 proposals — never clobbered; **ST3 (M1)** held the per-job advisory lock in a side session → the worker's `redrive_one` could NOT claim → did NO work (job stayed pending, 0 proposals) → released → re-XADD → ran to `completed` (the claim deterministically gates the call site); **ST4 (M3)** forced ST2's partial job `failed` → retry via the lore internal endpoint → re-drive SKIPPED the done gap (蓬萊 proposal id unchanged, no re-spend) + ran the remaining gap (玉虛宮) → `completed`. Fully cleaned up (3 jobs + project corpora, 0 residue). NOTE: needed `max_spend_usd=None` — the worker uses the real token-denominated GapCostModel (a small USD-looking cap pauses before gap 1; the cost-cap pause is tested separately).
`D-JOBS-P4-RETRY-CAMPAIGN-GATE` (low) — a campaign-dispatched translation job's Retry button
still renders (the cap-gate can't see `campaign_id` — not on the projection); clicking it safely
409s (`TRANSL_CAMPAIGN_MANAGED`). Hiding the button needs the projection to carry campaign
membership. Safe today (server refuses); cosmetic UX wart.

| ID | Description | sev |
|---|---|---|
| `D-JOBS-P4-TRANSLATION-COST` | translation has no job-level cost column → cost shows `—` | med |
| `D-JOBS-P4-LORE-MODEL` | lore-enrichment model NAME not emitted (ref in separate `enrichment_job_request`) | med |
| `D-JOBS-P4-CAMPAIGN-MODEL-NAMES` | per-stage campaign model names not emitted (HTTP-in-tx concern) | med |
| `D-JOBS-P4-COMPOSITION-GUARDED-MODEL` | guarded auto-draft path emits `model=None`+ref-in-params | low |
| `D-JOBS-CAMPAIGN-SPEND-EMIT` | campaign cost updates only on status transition, not per SpendConsumer write | low |
| `D-JOBS-P4-SUMMARY-TOPLEVEL` | active count is top-level-only → completed parent w/ running child undercounts | low |
| `D-JOBS-P4-TRANSL-TOKENS-PG` | translation tokens-SUM SQL only FakeConn-tested → add real-PG coverage | low |
| `D-JOBS-P4-RETRY` | failed-job "Retry" — BE retry action + FE button (mockup shows it) | med |
| `D-JOBS-P4-OVERLAY-EVICT` | SSE overlay Map never evicts terminal jobs (slow growth) | low |

---

## B2 — Jobs control completeness (P3) — ✅ CLEARED 2026-06-17
Finished the control surface gaps. Plan: `docs/plans/2026-06-17-b2-jobs-control-completeness.md`.
All 3 closed, no new deferrals (live-smoke → B3). `/review-impl` caught 1 HIGH (resume re-driving a
`failed` chapter over-counted completed+failed past total → stuck job; fixed: resume re-drives
`pending` only) + 1 MED (best-effort abort broadened to `except Exception`).

| ID | Description | sev | resolution |
|---|---|---|---|
| `D-JOBS-P3-TRANSLATION-PAUSE` | translation stop-dispatch pause/resume + re-add to `_MULTI_UNIT_KINDS` | med | ✅ contract + pause/resume cores (running↔paused, resume re-drives pending-only from stored row) + worker paused-drop + stale-aware guarded claim (dup-safety vs parked WFQ units) |
| `D-JOBS-P3-VIDEOGEN-PROVIDER-ABORT` | video-gen cancel doesn't abort the in-flight provider job (reclaim slot/cost) | med | ✅ best-effort `Client.cancel_job(provider_job_id)` after the local CAS (reclaims slot+reservation; local row canonical) |
| `D-JOBS-P3-LORE-COMPOSE-TASK-CONTROL` | one-shot `enrichment_compose_task` not control-wired | low | ✅ status-only cancel (option a) — endpoint + claim-skip + `_mark` guard + status-CHECK widen migration |

**Verify:** jobs 67p · translation 770p · video-gen 53p · lore 773p · provider-gate OK. Live-smoke → B3.

### Producer-emit backfill (2026-06-17) — systematic P1 gap, spec `docs/specs/2026-06-17-producer-emit-backfill.md`
Audit confirmed **4 un-wired producers** (glossary-extract, glossary-translate, wiki-gen, book-import). Slices A→C→B→D.

| ID | Description | sev | status |
|---|---|---|---|
| `D-JOBS-GLOSSARY-EXTRACT-UNWIRED` | glossary-extract (translation `extraction_jobs`) emitted nothing → invisible in Jobs screen; + FE pick inherited 'all'; + create sync freeze | high | ✅ **Slice A** — emit pending(create,in-tx)/running/terminal/cancelled(worker) + reconcile UNION (kind `glossary_extraction`) + FE clear-on-pick + bulk-insert/atomic create + kind label×4. Live-smoke → `D-PRODUCER-EMIT-GLOSSARY-EXTRACT-LIVE-SMOKE` (B3). |
| `D-JOBS-WIKI-GEN-UNWIRED` | `wiki_gen_jobs` (knowledge) emitted nothing → invisible in Jobs screen | high | ✅ **Slice C** — emit pending(create,in-tx)/running(claim)/paused/pending(resume)/cancelled/completed/failed at all 7 repo mutations (each UPDATE+emit in one tx, RETURNING user_id+cost; guarded emits only fire on a real transition) + reconcile UNION into `/internal/knowledge/jobs` (kind `wiki_gen`, `complete`→`completed`, merged oldest-first w/ extraction) + FE kind label×4. Live-smoke → `D-PRODUCER-EMIT-WIKI-GEN-LIVE-SMOKE` (B3). |
| `D-JOBS-GLOSSARY-TRANSLATE-UNWIRED` | `glossary_translation_jobs` (translation) emitted nothing → invisible in Jobs screen | high | ✅ **Slice B** — create emits pending(in-tx)+cancelling; worker emits running(claim)/cancelled(guarded settle + mid-loop)/completed(terminal, summed tokens)/failed; reconcile 3rd UNION arm (kind `glossary_translation`, `completed_with_errors`→completed); FE kind label×4; view-only gate extended (`_VIEW_ONLY_KINDS`). Live-smoke → `D-PRODUCER-EMIT-GLOSSARY-TRANSLATE-LIVE-SMOKE` (B3). |
| `D-JOBS-BOOK-IMPORT-UNWIRED` | `import_jobs` (book-service, Go) emitted nothing → invisible in Jobs screen | med | ✅ **Slice D** — Go `emitJobEvent` (outbox `aggregate_type='jobs'` → relay → `loreweave:events:jobs`; canonicalizes native status `processing`→running, skips unmappable like the Py SDK); `startImport` emits pending(in-tx), `updateImportJobStatus` wrapped in tx + RETURNING user_id → emits the transition (404 on missing, was silent 204); new `GET /internal/book/jobs` reconcile source + jobs-service `_RECONCILE["book"]` + `book_service_internal_url` config; FE kind label×4; view-only gate (`book_import` — book-service has NO control endpoint). Live-smoke → `D-PRODUCER-EMIT-BOOK-IMPORT-LIVE-SMOKE` (B3). |
| `D-PRODUCER-EMIT-GLOSSARY-EXTRACT-COST` | glossary-extract emits tokens but not actual $cost (estimate only); price summed tokens via the billing oracle (like translation B1-M2) | low | ☐ later |
| `D-JOBS-SECONDARY-KIND-CONTROL` | Slice A/B/C secondary kinds were view-only (control would 404). | med | ✅ **DONE 2026-06-17** — `kind` now rides the control forward body (`forward_control` + `jobs.py`; other services ignore it, Pydantic `extra='ignore'`). `derive_control_caps` gives each secondary kind its NATIVE caps: glossary_extraction/glossary_translation = cancel-only (pending\|running), wiki_gen = cancel(pending\|paused)+resume(paused) (no running-cancel, D-WIKI-M7B); `book_import` stays view-only (no control endpoint). translation `control_job` dispatches glossary kinds → `_cancel_secondary_core` (owner-scoped, 404/409, UPDATE→cancelling+emit on the right table); knowledge `control_extraction_job` dispatches `wiki_gen` → `_control_wiki_gen_job` (owner re-check via repo.get, repo.cancel/resume which emit). Tests: jobs-service caps 70 · translation dispatch 15 · knowledge wiki-dispatch 16. Live-smoke → `D-SECONDARY-KIND-CONTROL-LIVE-SMOKE`. |
| `D-JOBS-WIKI-GEN-RECONCILE-INDEX` | wiki-gen reconcile `list_since` filters `wiki_gen_jobs.updated_at` with no index (extraction_jobs has one). Table is tiny (1 active job/book) → seq-scan fine for now; add `idx_wiki_gen_jobs_updated_at` if the sweep shows pain. | perf | ☐ later |

---

## B3 — Live-smoke sweep — Job Control Plane + P5
Stack up with `P5_SCHED_ENABLED=true`, seed one extraction-ready project. Run all in one session.

| ID | Description | sev |
|---|---|---|
| `D-P5-M3-EXTRACTION-LIVE-SMOKE` | ✅ **DONE 2026-06-17** — REAL decoupled extraction proven with the per-owner cap engaging. Setup: 2 knowledge projects on a published 5-chapter book (`019eb60e`), embedding=bge-m3, LLM=Qwen2.5-7B, started via the gateway API (passing-benchmark row inserted as test fixturing for the orthogonal K17.9 gate); `P5_OWNER_CAP` lowered to 1 so the cap bites with 2 concurrent jobs. Observed for 24s: `ZCARD p5:knowledge:extraction:inflight:{owner}` pinned at **1 (= cap)**, `inflight_total=1`; job A held the slot (resume_state NOT NULL, cost $0.004), job B continuously logged "at P5 cap — deferring next chunk to a later poll" with **cost $0** (the gate runs BEFORE try_spend → a deferred chunk never inflates cost). A finding the unit tests can't show: one decoupled job submits exactly ONE chunk per poll then waits ([runner.py:1948](../../services/worker-ai/app/runner.py#L1948)), so the per-user cap only bites across MULTIPLE concurrent jobs — that's the configuration the smoke must (and did) create. | high |
| `D-P5-M2-MULTI-OWNER-LIVE-SMOKE` | ✅ **DONE 2026-06-17** — per-owner isolation / anti-starvation proven live. With owner1 already at cap (ZCARD=1), seeded 2 running jobs for a SECOND owner (`019d4966…`, own BYOK models + book `019ebb6c`, rows direct-inserted as `status='running'` — the worker's poll + P5 acquire path is identical regardless of how the row reached running). Observed simultaneously: `inflight:{owner1}=1`, `inflight:{owner2}=1`, `inflight_total=2` — owner2 acquired its OWN slot despite owner1 being saturated, and owner2's 2nd job was independently deferred by owner2's own cap. The per-owner ZSET key (`…:inflight:{owner}`) makes the isolation structural; there is no shared counter that could cross-starve. | med |
| `D-JOBS-P2-SSE-LIVE-SMOKE` | ✅ **DONE 2026-06-17** — subscribed to `loreweave:jobs:user:<uid>`, inserted a `jobs` event → relay → consumer upsert → pub/sub publish RECEIVED by the subscriber with the full JobEvent payload (incl. derived `control_caps:[]` for book_import). (Manual-publish isolation confirmed plumbing; the real proof needed a window > the variable relay latency.) | med |
| `D-JOBS-P3-KNOWLEDGE-CANCEL-SUCCESS-LIVE-SMOKE` | ✅ **DONE 2026-06-17** — cancelled the actively-inflight extraction job A (above) via `POST /v1/knowledge/projects/{id}/extraction/cancel` → 200 `status=cancelled`; `extraction_jobs` row → cancelled; knowledge `outbox_events` carried `job.cancelled` (H1 same-tx emit), relayed → `loreweave_jobs.job_projection` = `knowledge\|extraction\|cancelled`. Re-proves the cancel-path producer-emit on a REAL running row. (The held P5 token releases at the chunk terminal/TTL, not synchronously by cancel — as designed.) | med |
| `D-SECONDARY-KIND-CONTROL-LIVE-SMOKE` | ✅ **DONE 2026-06-17** — unified control dispatch PROVEN on the stack (after rebuild): glossary_extraction cancel → `extraction_jobs`→cancelling + emit landed in `job_projection`; wiki_gen resume → pending (re-enqueue ran, no 500) + emit; wiki_gen cancel → cancelled. glossary_translation shares the identical `_cancel_secondary_core` dispatch. | high |
| `D-PRODUCER-EMIT-BOOK-IMPORT-LIVE-SMOKE` | ✅ **DONE 2026-06-17** — Slice D cross-language path PROVEN on the running stack: (1) a `book_import` `jobs` outbox event inserted into `loreweave_book` → worker-infra relay → landed in `loreweave_jobs.job_projection` (service=book, kind=book_import, status=pending, title); (2) `GET /internal/book/jobs` returns the canonical JobEvent with native `processing`→`running` mapping + `progress.done=7`. Synthetic data cleaned. | high |
| `D-PRODUCER-EMIT-RECONCILE-UNIONS-LIVE-SMOKE` | ✅ **DONE 2026-06-17** — the reconcile UNION SQL added in A/B/C validated live (200 + valid JobEvent JSON, not 500): translation 3-way UNION (translation + glossary_extraction + glossary_translation) returns rows; knowledge UNION (extraction + wiki_gen) returns valid `{"jobs":[]}`. Confirms the new tables' columns resolve against the real schema. | high |
| `D-PRODUCER-EMIT-{GLOSSARY-EXTRACT,WIKI-GEN,GLOSSARY-TRANSLATE}-FULL-E2E` | Remaining: trigger a REAL job of each kind via the gateway (JWT + real book/project + LLM) and observe pending→running→terminal land in `job_projection`. LOWER priority — the Python emit lib is proven (B1), the relay `jobs` routing is established + re-proven (book), and each worker's emit wiring is unit-tested; this is the belt-and-suspenders full path. | med |

---

## B4a — Live-smoke sweep — Auto-Draft Factory (S1–S6)
The campaign 4-service stack: bring up once, run the chain. The single biggest live-smoke cluster.

**◑ PARTIAL — retroactive harvest from completed campaigns (2026-06-17, stack up).** The test account has **already run campaigns to `completed`** on the current stack (`019ebe09` "D-WX ch3" stages `{knowledge,translation,eval}`; `019eb684` 5ch full) with **local BYOK models (lm_studio/bge-m3/local-rerank → $0 spend)**, so several smokes are evidenced by real artifacts without a fresh run:
- ✅ `D-CAMPAIGN-S1-LIVE-SMOKE` (dispatch) — `campaign_activity` shows `knowledge dispatched→done` + `translation dispatched→done` on `019ebe09`.
- ✅ `D-CAMPAIGN-CLAIM-LIVE-SMOKE` — those dispatched stages reached `done` (jobs were claimed + ran).
- ✅ `D-S4A-THREADING-LIVE-SMOKE` (the high-value structural one) — `provider_registry.llm_jobs.job_meta->>'campaign_id'` is populated on the campaign's child jobs (386 jobs tagged for `019eb684`, 26 for `019ebe09`, …). The campaign_id contextvar→job_meta threading works live e2e ([[contextvar-attribution-merge-pattern]]).
- ✅ `D-S5BEVAL-LIVE-SMOKE` (eval) — `eval done` in `019ebe09`'s activity (stages included `eval`).
- ✅ `D-S4B-RELAY-LIVE-SMOKE` / `D-S4C-CONSUMER-LIVE-SMOKE` — **PROVEN 2026-06-18** (stack up). The full spend path observed end-to-end with a **non-zero** spend landing: (a) **mechanism ($0 credit)** — seeded 2 parked campaigns (`driver_leased_until` future → real driver skips them; status=`running`) + injected synthetic `usage_outbox` rows with `campaign_id` + non-zero `cost_usd` → the **real relay** (`usage_relay.go`, poll 500ms) stamped `published_at` + XADD'd to `loreweave:events:campaign_usage` → the **real `SpendConsumer`** accumulated: ACCUM campaign `spent_usd 0→0.05`; PAUSE campaign (budget 0.01) `spent_usd→0.02` AND `status running→paused` + `error_message='budget cap reached'` (auto-pause-at-cap, which local-$0 runs could never show); **idempotency** — redelivered the same `request_id` (2 published `usage_outbox` rows) → `spent_usd` stayed 0.05 (the `campaign_usage_seen` PK dedup blocks double-count, at-least-once safe). (b) **real provider leg ($0.00004 credit)** — submitted ONE minimal real `gpt-4o` chat job (`POST /internal/llm/jobs`, `job_meta.campaign_id`, max_tokens=5) → completed → real `cost_usd=0.00004000` written to `usage_outbox` tagged with the campaign → relay → consumer → ACCUM `spent_usd 0.05→0.05004000`. Confirms a REAL priced model's cost flows the identical path. The relay/consumer are provably cost-source-agnostic. Fully cleaned up (campaigns + usage_seen + usage_outbox + llm_jobs/job_event_outbox all deleted, verified 0 residue). **Finding (LOW):** the registered OpenAI chat models mostly have empty `pricing {}` (gpt-4o-mini + one gpt-4o) → a submit 402s `LLM_QUOTA_EXCEEDED model pricing not configured`; only the `019d5e3c` gpt-4o is priced ({2.5,10}/Mtok). Per-model pricing is a required submit gate — populate `user_models.pricing` for any model intended for priced runs.

**Fresh live run (2026-06-17) — created a throwaway 1-ch campaign (book `019eb60e`, local models, eval+verifier+rerank configured), started + cancelled it, then fully cleaned up (8 rows + the throwaway project deleted; 0 tagged jobs, 0 Neo4j nodes since knowledge never passed the gate):**
- ✅ `D-S3B-BACKOFF-LIVE-SMOKE` — the knowledge stage failed (`benchmark_missing` 409 — the K17.9 gate, expected without a benchmark fixture) and the saga **re-dispatched at exactly 5s intervals** (16:07:55 → :08:00 → :08:05, `knowledge_attempts=3`): the retry-with-backoff dispatch loop + error propagation (dispatch 409 → stage failed → re-dispatch) is live-proven.
- ✅ `D-CAMPAIGN-CANCEL-LIVE-SMOKE` — `POST /v1/campaigns/{id}/cancel` → campaign reached **terminal `cancelled`** (proven on the fixtured run below, not just `cancelling`).
- ✅ `D-CAMPAIGN-CANCEL-PROP` **PROVEN LIVE** (2026-06-17 fixtured run) — to get a *running* child to propagate to, inserted a passing K17.9 benchmark fixture for the throwaway project's embedding model (`project_embedding_benchmark_runs`, `passed=true`; **gotcha**: the `embedding_model` value must be TRIMMED — a stored trailing space silently fails the gate's exact-match). With the gate passing, the campaign dispatched a REAL extraction job (`extraction_jobs` row `running`, `campaign_id` stamped → threading at the extraction-job level too); cancelling the campaign → `_propagate_cancel` posted cancel to that running child → the extraction job transitioned `running`→**`cancelled`** (`items_processed=0`, a real `job.running`→`job.cancelled` outbox emit landed) and the campaign finalized `cancelled`. Fully cleaned up (0 Neo4j nodes built since cancelled at items_processed=0; campaigns/jobs/project/benchmark all deleted, verified 0). Complements the B4b unit-verification of `_propagate_cancel`.
- ✅ `D-CAMPAIGN-S1-LIVE-SMOKE` re-confirmed live (fresh `dispatched` events).
- ✅ `D-S5A-ESTIMATE-LIVE-SMOKE` — `POST /v1/campaigns/estimate` (read-only, no campaign) sized 3 chapters → all 5 LLM/embedding stages priced `status=ok`, `is_local=true`, `provider_kind=lm_studio`, real per-stage tokens (extraction/embedding/translation 9000, verify/eval 22500); rerank `not_estimated` by design (no per-token price); band `$0–$0` (local) + minutes + notes. **Resolves the est_usd-NULL question: NOT a gap** — the endpoint works; completed campaigns show NULL only because they were launched without the estimating wizard passing the band to create. (Role keys must be `extractor`/`translator`/`verifier`/`eval_judge`/`embedding`/`reranker` — mis-keys silently read as "no model selected".)

- ✅ `D-S2-IDEMPOTENCY-LIVE-SMOKE` + `D-S3A-GOVERNOR-LIVE-SMOKE` + `D-CAMPAIGN-BREAKER-PAUSE-LIVE-SMOKE` **PROVEN LIVE (2026-06-18)** at the dispatch seam — drove the **real campaign-service saga driver** (live process + real Postgres + real Redis event-bus) against a directly-seeded campaign (`DRIVER_MAX_INFLIGHT_PER_CAMPAIGN=1`, 1 chapter `dispatched` + 4 `pending`; bypassing the create-API gives full control of the in-flight state — driver/gating/`count_inflight`/CAS are all real). **S3a governor-hold:** across 3 ticks (~14s) state held at `dispatched=1, pending=4` — the driver leased + renewed the campaign every tick (`leased=t, lease_live=t`) yet dispatched nothing (`budget = max_inflight − count_inflight = 0`). **S3a governor-release:** draining the in-flight chapter (→`done`) freed `budget=1` → exactly **ONE** pending chapter dispatched per tick (`pending 4→3`), never all 4 — the cap is a *moving* ceiling, not a freeze (the NULL-`knowledge_project_id` seed makes the released chapter fail cleanly with no external dep, and confirms a 1-dispatch/tick rate). **S2 idempotency:** the cleanest isolation is the release phase — with `budget=1` AVAILABLE, the `done` chapter was NOT re-dispatched while a `pending` chapter WAS (the gate only returns `pending|failed`; the per-row `pending|failed→dispatched` CAS no-ops on a `done`/`dispatched` row). (Note: "in-flight chapter's `knowledge_attempts` stayed 0" during the budget=0 hold is a WEAK proxy — it'd be 0 regardless since the gate never selects a non-dispatchable row; the done-skip under available budget is the real proof.) **Breaker-pause:** XADD'd a real-shaped `knowledge.chapter_failed` + `{error_code:"LLM_CIRCUIT_OPEN", user_id, book_id, chapter_id}` onto `loreweave:events:knowledge` → consumer → `_handle_circuit_failure` → `pause_campaigns_for_breaker` → campaign `running`→**`paused`** (`error_message="auto-paused: provider circuit open"`, log `circuit-open auto-paused 1 campaign(s) … stage=knowledge`). PROVEN: the POSITIVE — the matching campaign pauses; the (chapter,stage)-scoping (`EXISTS cc.{col}='dispatched'`) that EXCLUDES unrelated campaigns is from the SQL + unit tests, NOT a live negative control (only one campaign existed). **NOTE:** the worker's actual provider circuit-*trip* (N consecutive failures → emit `LLM_CIRCUIT_OPEN`) is the upstream, genuinely non-deterministic half — out of scope here; the campaign-pause path (what B4a owns) is fully proven. Fully cleaned up (synthetic campaign + 5 chapters deleted, stream entry `XDEL`'d, `DRIVER_MAX_INFLIGHT_PER_CAMPAIGN` compose override reverted, no Neo4j/PG/jobs residue — the dispatch-seam method builds no graph).
  - **`D-CAMPAIGN-NULL-KPROJECT-NONPROGRESS` (LOW, latent — found by the smoke above, 2026-06-18):** [`driver.py`](../../services/campaign-service/app/saga/driver.py#L137) marks a knowledge chapter `failed` ("no knowledge_project_id configured") via `mark_stage_failed` *directly*, bypassing `mark_stage_dispatched`, so `knowledge_attempts` never increments → the `failed` row stays dispatchable (`attempts < max`) → the gate re-selects the same chapter every tick forever (never exhausts → campaign never completes, burns `budget`/tick). **NOT reachable via the API** (create 400s `CAMPAIGN_NO_KNOWLEDGE_PROJECT` if `knowledge_project_id is None`), so purely latent (only via DB drift / a direct seed). One-line fix if ever wanted: bump attempts on the no-project failure (so it exhausts→settles), or treat NULL-project as a campaign-level error. Deferred (unreachable today).

**Still genuinely open:** ~~`D-RERANK-BYOK-LIVE-SMOKE`~~ ✅ **PROVEN 2026-06-18** (reranker backend came up → built a throwaway graph: book + published canon chapter + draft chapter + project w/ BYOK bge-m3 embed + bge-reranker; semantic search `rerank=true` rel=0.9976 vs `rerank=false` rel=0.8529 = rerank rescored, logs `POST provider-registry:8085/internal/rerank?user_id=… 200` only when on; cleaned up via project-delete → Neo4j 0). Same e2e also FULLY proved `D-RAWSEARCH-CANON-WIRING-LIVE-SMOKE` (surface=canon→0 draft hits, surface=all→draft hit surface="draft" rel=0.989, real bge-m3). The earlier "no graph'd project" blocker is gone — a throwaway graph is cheap to build now. · ✅ relay/consumer (`D-S4B`/`D-S4C`) **PROVEN 2026-06-18** (synthetic non-zero spend → real relay+consumer accumulate/pause/dedup; + 1 real $0.00004 gpt-4o call confirming a real priced cost flows the identical path — see the ✅ row above). · ✅ `D-S5C-LIVE-SMOKE` + `D-S6-LIVE-SMOKE` **PROVEN 2026-06-18 (Playwright, test account, frontend :5174)** — the last 2 browser smokes, closing B4a. **S5c wizard:** logged in → `/campaigns/new` → 4-step stepper rendered; book picker (selected 万古神帝 5ch) + knowledge-project select + target-lang; Chapters step (pacing selector + from/to + published-count validation); Models step (Extractor+Translator required, **Advanced collapsible** revealing all 6 roles — verifier/eval/embedding-override(bge-m3)/reranker-override(bge-reranker)); Review step → **"Estimate cost & time" called the S5a endpoint and rendered the full per-stage table** (Cost $0.0000–$0.0000 local, Time 1–2 min, Chapters 5; rows extraction/embedding(not_estimated)/translation/verify/eval/rerank w/ tokens + lm_studio·free badges + notes); **Launch → navigated to `/campaigns/{id}` detail** (create+start round-trip). **S6 monitor:** live `running` status, metrics (elapsed/throughput/ETA/in-progress), **budget bar $0/$5**, per-stage progress (Ingest 5/5, Knowledge 0/5·5 failed `KNOW_NO_EMBEDDING_MODEL` — expected, no embedding override), **chapter table** (paginated, per-chapter Knowledge/Translation/Eval/Fidelity/Last-error, select checkboxes, "Show all" filter, "Re-run all failed"/"Re-run selected"), **live activity feed** (ch.1-5 dispatched→failed, auto-refreshing). **All 4 control round-trips:** budget-PATCH ($5→$7.50, bar updated), **Pause** (running→paused + explainer + "Switch model & resume" appeared), **Resume** (paused→running), **Cancel** (confirm dialog Yes → running→cancelled terminal). Fully cleaned up (30 activity + 5 chapter + 1 campaign rows deleted; 0 usage_outbox / 0 extraction_jobs / 0 Neo4j — knowledge 422'd at dispatch before any job/graph). **B4a now 17/17 — COMPLETE.** *(Net: of the ~17, **ALL evidenced**: dispatch, claim (transitive), S4a threading, eval, S3b backoff, cancel + cancel-prop, S5a estimate, S2 idempotency, S3a governor, breaker-pause, rerank, S4b relay, S4c consumer, **S5c wizard, S6 monitor**.)*

> **✅ PRODUCT BUG FIXED (2026-06-18, /loom M + test) — `D-KNOWLEDGE-PROJECT-DELETE-NEO4J-ORPHAN` (was MED).** Found during the B4a live run: `delete_project` (`projects.py`) called only `ProjectsRepo.delete` (Postgres row) — no Neo4j cleanup → deleting a project ORPHANED its entire graph in Neo4j forever (observed ≥7 orphaned graphs: `019ec73e`=279 nodes, `019eb683`=55, …). **Fix:** new `neo4j_helpers.purge_project(session, project_id)` — `MATCH (n {project_id:$pid}) DETACH DELETE n` (every project node carries `project_id`; verified no connected node lacks it → complete) + drops ONLY this project's `<level>_summary_emb_p<id>_e<model>` indexes (reuses the name-validated `list_summary_vector_indexes`/`drop_summary_index`; the SHARED `entity_embeddings_*`/`passage_embeddings_*` dimension indexes are never touched). Wired **best-effort** into `delete_project` AFTER the owner-gated Postgres delete (a Neo4j fault logs + leaves a re-sweepable orphan, never fails the delete). OWNER-gated + `project_id`-bound (no cross-project risk). Tests: neo4j_helpers 31 (purge: scoped delete + index-filter + skip-on-0) + public_projects 87 (router import + delete path). **✅ LIVE-SMOKE PROVEN (2026-06-18, `3dfb2199` image rebuilt):** created a `general` project via the gateway → injected a synthetic graph (3 nodes tagged `project_id` + a per-project summary index) → `DELETE /v1/knowledge/projects/{id}` → **204**; verified Neo4j nodes 3→**0**, summary index **dropped**, all shared dim indexes **survived**, Postgres row **0**, service log `purged neo4j … 3 nodes, 1 indexes` (exact) → `D-KNOWLEDGE-PROJECT-DELETE-NEO4J-ORPHAN-LIVE-SMOKE` DONE. **✅ ORPHAN SWEEP DONE (2026-06-18):** the existing orphans were **106** (not ~7 — the earlier figure was a sample); only 11 of 108 Neo4j graphs had a live Postgres row. Swept by the live-set predicate (`MATCH (n) WHERE NOT n.project_id IN <11 live> DETACH DELETE n`): **2342 nodes / 106 graphs** deleted + the 1 orphan's 3 summary indexes dropped; the 2 live graphs + all 9 shared dim indexes untouched (verified). Audit trail: `docs/raid/evidence/neo4j-orphan-sweep/2026-06-18-orphan-audit.txt`. LOW follow-up: batch the `DETACH DELETE` (`CALL{}IN TRANSACTIONS`) for very large graphs.

---

## B4b — Auto-Draft Factory functional / correctness gaps

**Reconciliation 2026-06-17 (verified vs code):** 3 of the top rows are ALREADY DONE; the 2 remaining mediums are **deferred-by-design** (re-confirmed 2026-06-17): `D-S4-SUMMARY-ATTRIBUTION` is NOT the "S, no migration" one-liner it reads as — `SummarizeMessage` carries no `campaign_id`, so attributing summary-gen spend needs message-schema plumbing for a metric that is inert until S4d cost accounting matters; `D-S5BEVAL-LEARNING-OUTBOX` needs an outbox-table migration (→ /amaw + approval) for a telemetry-only durability gain. The lows are genuinely low. **No clean autonomous build remains in B4b** — its high-value rows already shipped.

| ID | Description | sev |
|---|---|---|
| `D-CAMPAIGN-CANCEL-PROP` | ✅ **DONE (verified 2026-06-17)** — `saga/driver.py` `_propagate_cancel` posts cancel to the stamped translation + knowledge child jobs on `cancelling` (best-effort, idempotent). | med |
| `D-CAMPAIGN-BREAKER-PAUSE` | ✅ **DONE (verified 2026-06-17)** — campaign consumer watches worker `*.failed` events carrying a circuit-open error_code → auto-pauses the campaign. | med |
| `D-S4-SUMMARY-ATTRIBUTION` / `D-S4A-SUMMARY-COST` | thread summary-generation LLM spend attribution — **OPEN but perf-later**: knowledge summary-gen runs in its own llm_client without the campaign contextvar → spend is UNDER-counted (not mis-attributed), inert until S4d cost accounting matters. S, no migration. | med |
| `D-S5BEVAL-LEARNING-OUTBOX` | ✅ **DONE 2026-06-18 (/loom L M2 + /review-impl).** transactional outbox for `eval_judged` emit. learning-service got its first `outbox_events` table (additive/idempotent DDL, mirrors knowledge); the decoupled fidelity judge's `_finalize` now writes the `translation.eval_judged` row in the SAME tx as the completion CAS (not swallowed → a failed insert rolls back the completion → the sweeper re-drives; `persist_*` idempotent). `learning` added to worker-infra `OUTBOX_SOURCES` → relay ships `aggregate_type='translation_eval'` to `loreweave:events:translation_eval` (campaign projection `set_eval_fidelity_by_chapter` is idempotent → at-least-once safe). Replaces the prior best-effort XADD that silently dropped the score (+ the old XADD was unbounded; the relay caps it). **/review-impl: 1 MED fixed in-loop** (the rollback invariant was untested + the test fake's tx was a no-op → faithful snapshot/restore + a fault-injection rollback test). VERIFY: learning 186 passed; provider-gate OK. Live-smoke → `D-S5BEVAL-LEARNING-OUTBOX-LIVE-SMOKE` (stack down). LOW deferred: `D-S5BEVAL-OUTBOX-PERSISTENT-FAIL-NO-CAP` (a permanently-failing insert sweeper-loops, no attempt cap — loud > the old silent drop). | med |
| `D-CAMPAIGN-KPROJECT-OWNERSHIP` | ✅ **DONE (verified 2026-06-17)** — campaign `create` re-checks `ProjectsRepo.get(user_id, project_id)` → fail-closed if not owned. | low |
| `D-CAMPAIGN-CONSUMER-NO-DLQ` | projection consumer drops on error, no DLQ | low |
| `D-S3A-INTERACTIVE-GOVERNANCE` | wrap interactive stream + media workers in the governor | low |
| `D-RERANK-COHERE-SHAPE` / `D-RERANK-LOCAL-NOKEY` | non-Cohere rerank adapter shapes + local empty-secret parity | low |
| `D-S5A-RERANK-COST` / `D-S5A-TARGET-LANG-RATIO` | estimator: rerank cost dim + per-language expansion ratio | low |
| `D-S4C-STREAMING-REALCOST` | real per-model cost for the streaming `/record` path | low |

---

## B5 — Live-smoke sweep — Wiki + Glossary + E0
Wiki + glossary + collaborator (E0) cross-service round-trips on a real stack.

`D-WIKI-M1-LIVE-SMOKE` · `D-WIKI-M3-LIVE-SMOKE` · `D-WIKI-M4-LIVE-SMOKE` · `D-WIKI-M5-LIVE-SMOKE` · `D-GLOSSARY-LIVE-SMOKE-BROWSER` (diff-card Apply + schema-confirm) · `D-E0-3-P2B-LIVE-SMOKE` (summary billing E2E). Plus the small Wiki correctness gaps that pair naturally: ✅ `D-WIKI-M6-CONSUMER-GROUP` **DONE 2026-06-18 (/loom S)** — the wiki-GEN consumer (`run_wiki_gen_consumer`) was a bare `XREAD '$'`; converted to a Redis consumer group (`wiki-gen-workers`, per-process consumer name `hostname-pid`, ack-after-process) → exclusive per-replica delivery, killing the double-LLM-spend on multi-replica. BUSYGROUP-safe MKSTREAM create; `_consume_batch` factored for testability (+4 unit tests); `drain_resumable_jobs` stays the row-based crash-durability backstop (no DLQ/reclaim scaffold needed). **/review-impl: 1 MED + 1 LOW fixed** — (MED) the one-shot group-create would spin forever on `NOGROUP` if the boot create failed transiently or the group was deleted; now `_ensure_group` is called at startup AND on a `NOGROUP` read to self-heal (matches the events-consumer convention); (LOW) the consumer name dropped `os.getpid()` → hostname-only (stable per replica, like `staleness_consumer`) so restarts don't orphan PEL entries. +1 test (NOGROUP→recreate). provider-gate OK. Deferred `D-WIKI-M6-CONSUMER-GROUP-LIVE-SMOKE` (needs 2 replicas + flag-on; correct by XREADGROUP construction + unit-tested). · `D-WIKI-M4-NEUTRALIZED` (med), `D-WIKI-M6-PRECISE-COST` (med, blocked on the provider-registry per-call cost seam).

- ✅ **`D-WIKI-P2-SWEEP-DISMISS-RESWEEP` DONE (2026-06-18, /loom S + /review-impl, design call: dismiss durable per drift-signature).** The sweep dedup is the partial-unique `uq_wiki_staleness_open … WHERE status='pending'`, so a DISMISSED sweep-drift didn't block re-insertion → re-nagged the same drift every sweep. **Fix** ([wiki_staleness.go](../../services/glossary-service/internal/api/wiki_staleness.go), no migration): `sweepRecipeDrift` + `sweepKgDrift` now suppress re-insert when a `dismissed` row exists for the same **drift-signature** — recipe_drift signature = the article's STORED from-version (`source_id`; a regen changes it → re-surfaces); kg_drift folds `current_hash` into the guard (`source_id`=storedHash keys too coarsely; a new current hash re-surfaces). The `is_knowledge_stale` badge is guarded in BOTH spots (recipe bulk-UPDATE NOT EXISTS; kg `is_knowledge_stale=true` moved into the `RowsAffected==1` block) so a dismissed-suppressed article isn't re-flagged with no feed row. **Push reasons** (`entity_changed`/`merged`/`chapter_regrounded`/`citation_broken`) intentionally UNCHANGED — they key on `aggregate_id`, so a re-surface there is a genuinely new change = correct re-notify. **/review-impl: no HIGH/MED; 1 LOW fixed** (added `DismissedSurvivesCurrentBump` — pins "signature=stored, NOT current": a further current bump on the same stored build stays suppressed; a future refactor folding current into recipe's source_id would otherwise silently flip this with both prior tests still passing). VERIFY: glossary real-PG (:5555) 12 sweep tests (5 recipe + 7 kg) + full Wiki/Staleness/Dismiss suite green; gofmt+build+vet clean. **LOW deferred (`D-WIKI-KG-DRIFT-STALE-CURRENT-HASH`, pre-existing):** a pending kg_drift row keeps its original `current_hash` when `cur` changes while still pending (ON CONFLICT DO NOTHING) — the displayed drifted-to hash can be stale; not introduced here.

---

## B6 — Translation V3 functional gaps

**Sweep 2026-06-17 (one /loom, S):** 3 genuinely-open items FIXED + 5 found ALREADY-RESOLVED by recent T1/T2 work (verified, not trusted). translation 790 pytest (+11). Remaining rows are low polish.

| ID | Description | sev |
|---|---|---|
| `D-TRANSL-M7D-INLINE-JUDGE` | ✅ **ALREADY DONE (verified 2026-06-17)** — the fidelity judge is NOT inline: `chapter_worker._emit_chapter_done` emits a post-commit `translation.quality` transactional-outbox event (the actual judge runs downstream in learning-service M7d-2), and the text feed is already **capped + fraction-sampled** (`translation_judge_feed_max_chars`, [chapter_worker.py:964-973](../../services/translation-service/app/workers/chapter_worker.py)). Out-of-band AND sampled = the deferral's ask. No latency on the translate path. | high |
| `D-TRANSL-RESUME` | ✅ **ALREADY DONE (verified 2026-06-17)** — `jobs.py` skip-gate skips a chapter iff `status='completed' AND NOT is_glossary_stale` (fresh-completed-version EXISTS, not the active version — the correct idempotency scope), and emits `chapter.translation_skipped` so resumed campaigns converge. Covered by `test_idempotency.py`. | med |
| `D-TRANSL-VERIFY-WHOLEWORD` | ✅ **DONE 2026-06-17** — `verifier._name_present`: non-CJK glossary source names now require unicode word boundaries (kills the "King" ⊂ "Kingdom" false positive that churned the corrector); CJK keeps substring behind the `len>=2` guard (no in-service segmenter). `tgt_name not in draft` stays substring on purpose (safe direction). +3 tests. | med |
| `D-TRANSL-M4B-RESIDUALS` | ✅ **DONE 2026-06-17** — `knowledge_context._fetch_all_neighborhoods`: each entity fetch is now `asyncio.wait_for`-bounded (`_FETCH_TIMEOUT_S=5s`) + failure-isolated (`except Exception` → `WikiNeighborhood.empty()`, entity keeps its bio line) so one slow/failing entity can't abort the whole brief; `CancelledError` (BaseException) still propagates = the "abort" half. Injection neutralization was already complete (`_sanitize`). +2 tests. | med |
| `D-TRANSL-M2-VERIFY-BATCHING` | batch LLM verifier (40-block cap) | low |
| `D-TRANSL-M2-LLM-ADVISORY` | ✅ **ALREADY DONE (verified 2026-06-17)** — advisory-by-design: LLM-flagged issues are capped high→med (`decoupled_v3_verify._cap_llm_issues` + `orchestrator.py:290`), so an LLM flag never ALONE triggers the destructive corrector re-translate (only rule-tier high does). | low |
| `D-TRANSL-M3-DIALOGUE-HEURISTIC` | ✅ **ALREADY DONE (verified 2026-06-17)** — `semantic_chunker._has_dialogue` groups on any CJK bracket / smart-quote / em-dash lead; grouping is pre-batch so unbalanced quotes are harmless (no validation gate). Pragmatic + fit-for-purpose. | low |
| `D-TRANSL-M4B-FANOUT-CACHE` | per-book/project TTL cache for entity fetches | low |
| `D-TRANSL-M4C-HARVEST-LATIN` | false-positive guard for name harvesting | low |
| `D-TRANSL-CORRECTOR-LIMITS` | ✅ **DONE 2026-06-17** — `corrector.build_corrector_submit_kwargs` now sets `max_tokens = min(16384, max(2048, est_tokens(src)×3))` (source-scaled, generous — bounds runaway/looping generation, never truncates a legit one-block correction) + a system-prompt line to preserve list/line structure. +5 tests. | low |
| `D-TRANSL-M7C2-DIRTY` | Tiptap dirty-check + onSaved + unsaved warning | low |
| `D-TRANSL-VERIFY-COARSE` (`D-TRANSL-VERIFY-2ND-FETCH` ✅ done — glossary fetched ONCE at `orchestrator.py:320`, reused across verify/correct rounds) | refined number/CJK-leak detection (COARSE still open; number/CJK rules are intentionally conservative — low value) | low |
| `D-TRANSL-M6B2-PERLANG-JOB` | one-click per-language re-translate UI | low |

---

## B7 — Knowledge Projects FE (K19) — mobile + filters + polish
Coherent FE feature batch on the knowledge-projects surface.

**Reconciliation 2026-06-17 (verified each row against the FE):** ~5 rows found ALREADY-SHIPPED but never ticked. The remaining open ones need a design call, backend metadata, or are marginal — NOT clean autonomous wins.

- **Mobile-responsive:** `D-K19d-β-01` ✅ **DONE (verified)** — `EntitiesTable` has the `hidden md:block` desktop grid + `md:hidden` mobile card tree ([EntitiesTable.tsx:104-223](../../frontend/src/features/knowledge/components/EntitiesTable.tsx)). · `D-K19d-β-02` (Timeline grid) — **effectively a no-op:** `TimelineEventRow` uses `grid-cols-[56px_1fr_auto]` with a `1fr` middle + `truncate` + a flex-**wrapping** meta line, i.e. responsive-by-construction (no fixed-wide grid like EntitiesTable had). A mobile-card rewrite is marginal + needs a browser to verify — left open, low value. Reclassify perf/polish.
- **Filters:** `D-K19e-α-01` ✅ **DONE (verified)** — entity_id picker shipped (`TimelineFilters` search→chip→`entity_id` param). · `D-K19e-α-02` (ISO date range) ✅ **DONE 2026-06-18** — FE-only (the BE `event_date_from`/`event_date_to` inclusive ISO bounds were already supported, [timeline.py:118-141](../../services/knowledge-service/app/routers/public/timeline.py)); added a date-range fieldset to [TimelineFilters.tsx](../../frontend/src/features/knowledge/components/TimelineFilters.tsx) (two truncated-ISO text inputs YYYY/YYYY-MM/YYYY-MM-DD, debounced commit, client format + reversed-range validation that withholds a 422-bound value) threaded through useTimeline + 4 locales. · `D-K19e-α-03` (sort direction) ✅ **DONE 2026-06-18** (PO chose "both axes"). Added a `sort_dir` (asc|desc) BE param to the timeline endpoint, flipping the ORDER BY for BOTH narrative+chronological axes with a direction-aware null sentinel so undated/unordered events sink LAST in both directions (closed allowlist, no user text in Cypher; [events.py](../../services/knowledge-service/app/db/neo4j_repos/events.py)) + a FE direction toggle next to the axis toggle. Default `asc` = back-compat. Tests: BE `_order_fragment` both-axes + forward + 422, 2634 knowledge unit; FE 26 timeline + tsc clean. · `D-K19e-γa-01` ✅ **DONE (verified)** — source_type radio-pills shipped (`DrawerSearchFilters`, `source_type` param threaded). · `D-K19e-γa-02` (drawer-search embed cost) ✅ **DONE 2026-06-18** — VERIFY-FIRST found the cost data was already on the wire (provider-registry `/internal/embed` returns `prompt_tokens`, knowledge-service just discarded it). Captured it on `EmbeddingResult.prompt_tokens`, and the drawer-search response now carries `embedding_prompt_tokens` + `embedding_cost_usd` (cost = `prompt_tokens × pricing.cost_per_token(embed_result.model)`, the resolved model NAME not the user_model UUID). tokens==0 (provider omitted usage, e.g. Ollama) → both null ("unknown", NOT a misleading $0); a free self-hosted model (rate 0) reports tokens with a "0.00000000" cost. FE: hook surfaces both, `RawDrawersTab` shows a "Query embedded: N tokens · $X" footer line (omitted when unknown) + 4 locales. Tests: 3 BE cases (priced/free-local/unreported) + 31 drawers+user-entities unit green.
- **Job-history UX:** `D-K19b.1-01` (cursor pagination ~150 jobs), `D-K19b.2-01` ✅ **DONE (verified)** — "load more" wired (`ExtractionJobsTab` + `useExtractionJobs.fetchMoreHistory`, `next_cursor`). · `D-K19b.3-01/02` ✅ **DONE (verified)** — `JobDetailPanel` shows `current_chapter_title` + ETA via `useJobProgressRate` (EMA over poll deltas). · `D-K19b.8-02/03` (orchestrator pipeline logs + tail-follow).
- **Scope/build:** `D-K19a.5-04` (chapter_range picker + runner enforcement) ✅ **DONE/verified 2026-06-18** — VERIFY-FIRST found it was already shipped end-to-end: BE `scope_range.chapter_range` accepted+validated+persisted ([extraction.py](../../services/knowledge-service/app/routers/public/extraction.py)), FE picker ([BuildGraphDialog.tsx:465-503](../../frontend/src/features/knowledge/components/BuildGraphDialog.tsx)), estimate honours it, AND the runner enforces it (`D-K16.2-02b` cleared in S2 — worker-ai `_enumerate_chapters` filters `lo<=sort_order<=hi`, [runner.py:1031-1040](../../services/worker-ai/app/runner.py)). The stale "runner doesn't honour it" comment in extraction.py was corrected. The ONE real gap closed: an out-of-bounds range (matches 0 published chapters) now 422s at start instead of silently creating a 0-item job (`_start_extraction_job_core` guard via injected book_client; 2 unit tests; 2627 knowledge unit green). · `D-K19a.5-06` (glossary_sync scope) ✅ **ALREADY-DONE (verified 2026-06-18)** — shipped C12c-b end-to-end: FE radio option ([BuildGraphDialog.tsx:74](../../frontend/src/features/knowledge/components/BuildGraphDialog.tsx) `glossary_sync` in `ALL_SCOPES` + `book_id` availability gate) + BE (`POST /internal/extraction/glossary-sync-entity`, worker-ai branch). Never ticked. · `D-K19a.5-07` (run-benchmark CTA) ✅ **ALREADY-DONE (verified 2026-06-18)** — shipped C12b-b: `RunBenchmarkButton` ([EmbeddingModelPicker.tsx:255](../../frontend/src/features/knowledge/components/EmbeddingModelPicker.tsx)) wired (visible when `!passed`) + `useRunBenchmark` + locales + tests. Never ticked. · `D-K19c.4-01` (archive variant keeping `glossary_entity_id`) ✅ **DONE 2026-06-18** — user-"delete" archive was clearing the glossary FK (the §3.4.F glossary-deletion path's `archive_entity` NULLs it), so a later `restore_entity` brought the entity back unanchored (score 0, no glossary link). Added an FK-preserving `user_archive_entity` variant (`_USER_ARCHIVE_CYPHER` — same soft-archive + edge preservation, but keeps `glossary_entity_id`); the user-delete handler now calls it. Tests: unit (handler calls the FK-preserving fn) + a **real-Neo4j integration test (RUN live, passing)** proving user-archive KEEPS the FK+score / glossary-deletion CLEARS the FK+zeroes the score / restore stays fully anchored. 28 user-entities+drawers unit green. **/review-impl MED-1 fixed in-loop:** `_USER_ARCHIVE_CYPHER` also PRESERVES `anchor_score` (was copied from `archive_entity` which zeroes it) — since `restore_entity` doesn't recompute, zeroing would leave a restored FK-anchored entity ranking as unanchored (`weighted_score = raw × 0`); archived rows are filtered by `archived_at IS NULL` so the kept score is inert while hidden. Integration test now asserts score preserved through archive→restore.
- **Test cov:** `D-K19a.7-01` (useProjectState action smokes) ✅ **ALREADY-DONE (verified 2026-06-18)** — cleared session 51 C4, 20/20 tests in `useProjectState.actions.test.tsx`. · `D-K19a.8-01` (MSW dialog stories) ✅ **ALREADY-DONE (verified 2026-06-18)** — cleared session 51 C13, 3 dialog stories + full `.storybook/msw-handlers.ts` harness. Both were shipped but never ticked here (this doc predated session 51).

---

## B8 — Search/Rawsearch + cosmetic cleanup + misc code gaps

**Reconciliation 2026-06-17 (verified each row against code):** `D-RERANK-I18N` FIXED; 3 rows found ALREADY-RESOLVED (`D-S4B-RELAY-SHUTDOWN`, `D-GLOSSARY-SORT-BE`, `provider-registry worker.go 501`); 2 confirmed legitimately-blocked (`D-S4C-ACCOUNTBALANCES-DROP` needs a migration → /amaw L+; `D-PHASE5E` blocked on a gateway SDK field).

- **Search (verify-swept 2026-06-18, /loom XL B8):**
  - `D-RAWSEARCH-P3C-ROBUST-MAP` — ✅ **ALREADY-RESOLVED (verified)**: `chunkIndex` is the canonical hit key ([hybrid_fusion.go _hit_key]), `blockIndex` is display-only behind a safe-null guard. The robustness concern is mitigated; no code owed.
  - `D-RAWSEARCH-EVAL-CI-PLUS` — ✅ **MOSTLY-RESOLVED (verified)**: the CI gate + per-band metrics + Postgres persist ([benchmark/persist.py]) are live; the only remainder (a 2nd-book golden) needs a corpus ingest, not code. Reclassify → not-autonomous (needs an ingest).
  - `D-RAWSEARCH-SEED-PAGINATION` — ☐ **OPEN, low** (a seed/eval *script* `scripts/seed_rawsearch_eval.py` assumes a single-page chapter GET; tooling, not product). Deferred (low value).
  - `D-RAWSEARCH-CANON-WIRING` — 🔨 **PO chose FULL FEATURE (2026-06-18)** → M3 of this XL: index non-canon/draft passages + retriever `surface=canon|all` filter + FE control. The passage index is canon-only today ([passage_ingester.py:213] "canonizes only author-published content"), so this is a real multi-layer feature, not a param toggle. (BE+FE.)
  - low cosmetic (`E5/E5B/E6/TITLE-FORMAT`) — untouched, low.
- **Cleanup:**
  - `D-S4C-ACCOUNTBALANCES-DROP` — ✅ **DONE 2026-06-17** (decision: REMOVE the endpoint, not re-map — no FE/gateway consumer found). `migrate.go` drops the `CREATE TABLE account_balances` + appends an idempotent `DROP TABLE IF EXISTS account_balances` (rollback = restore the CREATE, re-creates an EMPTY table — the wallet was never populated post-S4c-retirement). Removed the dead `GET /v1/model-billing/account-balance` handler + route + its OpenAPI path & orphaned `AccountBalance` schema (kept the `account-balance` tag — it groups the wallet endpoints). Rewrote `TestRecordInvocation_Idempotent_NoDoubleWrite` (was `…NoDoubleDeduct`) to assert idempotency on the `usage_logs` request_id UNIQUE (the real artifact; the token-quota check was vestigial). VERIFY: build+vet clean; **real-PG test PASS** (migrate.Up runs clean with the table removed, DROP removes an existing one, `/record` writes exactly 1 audit row on a duplicate request_id). Dev DB drops the table on next usage-billing restart. **/review-impl: 1 MED fixed** — the OpenAPI spec still defined the path (contract-first drift); also DISPROVED a feared FE breakage (the FE already retired the call at `usage/api.ts:46` → USD guardrail/platform-balance endpoints; the "no consumer" claim was right, the earlier wrong-pattern grep was the risk).
  - `D-S4B-RELAY-SHUTDOWN` — ✅ **ALREADY DONE (verified 2026-06-17)**: the worker-infra outbox relay exits cleanly on `ctx.Done()` ([outbox_relay.go:125-127](../../services/worker-infra/internal/tasks/outbox_relay.go)), and `cmd/worker-infra/main.go` binds the ctx to `SIGINT`/`SIGTERM`; in-flight relays are idempotent (`published_at` guard).
  - `D-RERANK-I18N` — ✅ **DONE 2026-06-17**: `ModelMatrixStep`'s `label()` was already i18n-wrapped (`t('matrix.role.{role}', {defaultValue})`), but the `matrix.role.*` keys (all 6 roles) + `matrix.projectEmbedding` were absent from ALL 4 locale bundles → non-English users saw English. Added translated `role` (extractor/translator/verifier/eval_judge/embedding/reranker) + `projectEmbedding` to en/vi/ja/zh-TW campaigns.json. (Closed the whole role-label set, not just reranker.)
  - `D-WIKI-M5-SUGGESTION-DEDUP` — ✅ **DONE 2026-06-18 (/loom S).** Repeated AI regen over a human-edited article filed a NEW pending `wiki_suggestion` each time (plain INSERT in `writeWikiSuggestion`) → review-queue pileup. Fixed (glossary-service, no migration): "latest AI suggestion wins" — DELETE prior PENDING ai-regen suggestion(s) for the article before inserting the new one (collapses to exactly one + self-heals any pre-fix pileup), scoped by a new `aiRegenSuggestionReason` const so human-filed feedback suggestions are never touched; DELETE+INSERT in the same tx (atomic). +1 real-PG test (3 regens → 1 pending ai-regen, human feedback preserved). Resolution was pre-specified in the deferred note (no design call needed). VERIFY: glossary :5555 Writeback/Suggestion/Wiki/Feedback suite green.
  - `D-WIKI-SEED-ROBUSTNESS` — ✅ **DONE 2026-06-18 (/loom XS).** `migrate.Seed` guarded on `count(entity_kinds) > 0 → skip`, but the system `unknown` kind is inserted in `Up()` BEFORE Seed runs, so on an already-Up'd / shared test DB the 12 default kinds (incl. `character`) never seeded → merge/wiki fixtures lost `character`. Fixed: dropped the count-guard; each default kind now `INSERT … ON CONFLICT (code) DO NOTHING` then re-reads its id for the attr defs (`ON CONFLICT (kind_id, code) DO NOTHING`) → per-kind idempotent, reconciles a partial catalogue, never clobbers an author-customized default. Self-heals prod too. **Rode a latent test bug:** `schema_confirm_test.go` created a kind with code `power_system` — which is actually a DEFAULT kind — and only passed because the seed bug left it unseeded; switched those tests to a non-default code `qa_new_kind`. +1 migrate reconcile/idempotency test. VERIFY: glossary :5555 — migrate pkg + FULL api pkg green (-p1).
  - `D-WIKI-DELETEKIND-CONTRACT` — ✅ **CLEARED (verified doc-only, resolved).** Kind-delete already returns `200 {deleted_wiki_articles}` and the FE `apiJson` tolerates 200+body (previously verified). No code owed.
  - `D-TRANSL-M5D-SYNC-NOTES` — ✅ **CLEARED (accepted, won't-fix).** The three sub-notes are net-improvements/conscious choices (retry-then-fail-fast vs skip-batch; per-request context-window call vs hardcoded 8192; dropped per-batch log number) — no action.
  - `D-GLOSSARY-SCHEMA-TOKEN-TAMPER-TEST-FLAKY` (LOW, test-quality) — ✅ **DONE 2026-06-18 (same session it was found).** `TestSchemaToken_TamperedSignatureRejected` flipped the LAST base64url char of the JWT signature, but that char carries "don't-care" low bits → the flip can decode to the SAME signature bytes → the test flakily passed a "tampered" token. Fixed: tamper the PAYLOAD segment instead (the signature is computed over header.payload, so any payload change invalidates deterministically). Verified 30× green (was intermittently failing in the full run).
  - `D-WIKI-M7B-RUNNING-CANCEL` — ✅ **DONE 2026-06-18** (/loom, mirrors the lore-M2 cancel-parity pattern). A *running* wiki-gen job is now cancellable: `WikiGenJobsRepo.cancel()` widened to `pending|paused|running → cancelled` + a new `read_status()`; the orchestrator ([wiki/orchestrator.py](../../services/knowledge-service/app/wiki/orchestrator.py)) polls status BETWEEN entities and stops promptly on a non-running status (the real fix — before this the loop ran to the end wasting tokens; only the final `complete()` no-op'd on its `NOT IN (cancelled)` guard). jobs-service `derive_control_caps` now offers `cancel` for a running wiki_gen. The control endpoint defers to `repo.cancel` (no separate guard). No migration ('cancelled' already in the CHECK; `items_done` is the existing per-entity checkpoint, cost persisted per entity). Tests: orchestrator mid-run cancel/pause stops + token-safe (no further gen), repo widening, endpoint running→cancellable + terminal→409, control-caps; 2631 knowledge unit + 73 jobs-service green. **LOW noted:** cancel frees the per-book lock immediately, so the one in-flight entity can briefly overlap a newly-started job (eventually consistent; a `cancelling` hold-state was judged not worth it here) — `D-WIKI-M7B-CANCEL-LOCK-OVERLAP`.
  - `D-WIKI-M7B-CANCEL-LOCK-OVERLAP` ✅ **ACCEPTED / won't-fix (verified 2026-06-18)** — the overlap window is real but ~milliseconds (orchestrator polls per-entity), last-write-wins on the upsert is non-corrupting, and re-run is idempotent (`items_done` checkpoint); a `cancelling` hold-state isn't worth the machinery. Documented design trade-off, not a tracked bug.
  - `D-WIKI-M7B-GEN-LIMIT` ✅ **DONE 2026-06-18** — wiki-gen silently capped at `genLimit` (default 50); a book with >50 entities of a kind generated only the first 50 and the banner read "50/50 = done" (silent drop). Honest-count fix: `resolveWikiGenEntities` now also returns `totalMatched` (same predicate, no LIMIT); the glossary delegate handler injects `total_matched` + `selected` into the 202 body (`injectGenSelectionCounts`, best-effort/non-fatal); the FE `trigger` shows a distinct `gen.startedTruncated` warning toast ("Generating N of M — raise the limit") instead of the bland "started" when `total_matched > selected`. 4 locales. Tests (full wiring proven, no defer): BE injection unit (object/no-trunc/non-object passthrough) + **real-PG `resolveWikiGenEntities`** (limit<eligible → 3 ids + total_matched 5, deleted/inactive excluded; limit≥eligible → 5 ids + total 5) + **FE `useWikiGenJob`** truncation toast (total_matched>selected → `gen.startedTruncated`; equal → plain `gen.started`; back-compat no-counts → `gen.started`); glossary api pkg green, 9 wiki hook tests green, tsc clean.
  - `D-TRANSL-M6A-NOTES` ✅ **CLEARED (verified 2026-06-18)** — (a) subtag-drift dup risk: no real bug — the server always uses the authoritative DB `language_code`, not a client subtag (M6b-1 consumer also tolerates drift); (c) full-propagate: DONE in M6b-1 + M6b-2. Only (b) "dialog inputs not cleared on cancel/reopen" remains — pure FE cosmetic, reclassified to perf/polish (no backend, low value).
  - `D-FACTORY-ACTIVITY-TRIM` ✅ **PARKED (verified 2026-06-18)** — `campaign_activity` is append-only but bounded per-campaign (~75 rows for a 5-chapter run; <1 MB even at 100 chapters). MVP keep-all is intentional; a retention/trim job is a ~S add-if-pain (no schema change). Moved to perf-when-pain — not autonomous debt.
- **Glossary/versioning:** `D-GLOSSARY-VERSIONING` (VG-2/3) — ✅ **ALREADY-RESOLVED (verified 2026-06-18)**: VG-1/2/3 fully shipped — `entity_revisions` table ([migrate/entity_revisions.go]) + list/fetch/restore endpoints ([api/entity_revisions_handler.go]) + async revision consumer ([events/revision_consumer.go], actor-granular, idempotent). Stale row. · `D-E0-4-PY-GRANT-SDK-EXTRACT` — ✅ **DONE 2026-06-18 (/loom XL M2a).** Extracted the 3 byte-identical Python copies (composition/knowledge/translation) into shared `sdks/python/loreweave_grants` (GrantLevel + parse_grant_level + GrantClient; per-service knobs at construction incl. an optional `trace_id_provider` for knowledge's X-Trace-Id; added `invalidate()` for the M2b instant-revoke). Each service's `grant_client.py` is now a thin shim re-exporting the shared class + keeping its singleton/settings wiring → zero call-site churn, single GrantLevel identity. Added to `sdks/python/pyproject.toml` packages. Tests: shared 8 + knowledge 22 + composition 10 + translation 25 green. · `D-GLOSSARY-SORT-BE` — ✅ **ALREADY DONE (verified 2026-06-17)**: server-side counts-sort exists (`entityOrderBy` `links`→`cached_chapter_link_count DESC`, `evidence`→`cached_evidence_count DESC`, [entity_search.go:204-208](../../services/glossary-service/internal/api/entity_search.go)) and is wired to the list handler ([entity_handler.go:630](../../services/glossary-service/internal/api/entity_handler.go)). *(Perf note, NOT the deferral: the list handler still computes the displayed counts via inline correlated subqueries instead of the cached columns — a perf-when-pain item, untracked.)* · `D-E0-4-PY-GRANT-SDK-EXTRACT` (extract shared grant SDK, 4 copies, med).
- **Misc code gaps:** `provider-registry worker.go:397-403` — ✅ **WORKING-AS-INTENDED (verified 2026-06-17)**: a not-yet-implemented async operation fails fast with a user-facing `LLM_OPERATION_NOT_SUPPORTED` ("not yet implemented in async-job mode"); the `streamableOperations` whitelist gates support and a new op is added with its adapter when built — no code change owed (reclassify won't-fix). · `book-service media.go:527` (`D-PHASE5E` expose provider_model_name + provider_kind analytics) — ✅ **DONE 2026-06-18 (/loom L M1 + /review-impl).** UNBLOCKED self-owned: the gateway image worker already resolved both values — threaded `providerKind` into `runImageGenJob` + added `provider_kind`/`provider_model_name` to the result map → OpenAPI `ImageGenResult` (additive/optional) → Go + Python SDK structs (mirror-honest) → book-service consumes `result.ProviderModelName` (→ `block_media_versions.ai_model` + response) + `result.ProviderKind` (→ billing; audit-only so NO double-bill, also powers the usage-log `provider_kind` filter). Tests: Go SDK decode ×2 + Py ×1 + producer/consumer source-locks. **/review-impl:** double-bill CLEARED; 1 MED fixed in-loop (Python SDK contract drift); LOW deferred `D-PHASE5E-CONSUMER-SWAP-COVERAGE`. Live-smoke → `D-PHASE5E-LIVE-SMOKE` (folds into B5/B4a). · **SDK low gaps (verify-swept 2026-06-18):** `grantclient` instant-revoke v1.1 → ✅ **DONE 2026-06-18 (/loom XL M2b + /review-impl).** book-service emits a `grant_revoke` event (aggregate_type → `loreweave:events:grant_revoke`) in the SAME tx as a collaborator delete/role-change ([collaborators.go] `insertGrantRevokeOutbox`); the relay (book already a source) ships it. Each grant client TAILS the stream (no consumer-group → fan-out to every instance) and drops the cached (user,book) grant on the spot vs the 45s TTL: shared `loreweave_grants` gained `invalidate()` + a `start_revoke_consumer` background tail (lazy redis), wired into knowledge/translation/composition lifespans; the Go SDK gained `Invalidate()` + a glossary `GrantRevokeConsumer`. **/review-impl: fail-safe by construction** (invalidate→re-fetch from the authority → can only return the CURRENT grant; NO escalation path; forged/missed event → TTL fallback). No HIGH/MED. Tests: shared 10 + grantclient (Invalidate ×2) + knowledge/composition/translation grant suites + book-service api green; builds+vet+provider-gate clean. **✅ LIVE-SMOKE 2026-06-18 (producer→relay→stream→consumer):** PUT collaborator → book `outbox_events` grant_revoke row (published=t) → relay routed to `loreweave:events:grant_revoke` (XLEN=1, source=book) → glossary + knowledge consumers started+tailing. **Found+fixed a LOW in-smoke:** the Python `_revoke_loop` logged the redis-py-8 idle BLOCK timeout as a WARNING+traceback every 5s; now caught silently (`except RedisTimeoutError: continue`, D-REDIS8-CONSUMERS) — re-smoked 0 spam over 25s idle. Cache-eviction leaf deferred (`D-GRANT-INSTANT-REVOKE-CACHE-EVICT-LIVE-SMOKE`, needs a pre-cached grant). M3 canon-wiring smoke ◑ wiring-proven (route/param/owner-gate live; draft-retrieval differentiation needs an embed backend). `llmgw` batch partial-success → ✅ won't-fix (atomic-batch is the design). `loreweave_llm` multi-video → ⊘ **SKIP** (additive but no caller needs it + needs gateway/adapter support, not just an SDK param). `pass2_filter` entity-FK canonical form → ✅ won't-fix (canonical-name anchoring IS the design, [pass2_writer.py]). · `knowledge extraction.py:843` (project-scoped active-job lookup) — ✅ **DONE 2026-06-18 (/loom XL M1):** `_get_active_job_for_project` now uses the existing SQL-scoped `list_active_for_project(user_id, project_id)` instead of `list_active(user_id)` + in-memory filter (the cross-project fetch the TODO flagged). Behavior-preserving (unique index → ≤1 active/project). Tests: extraction lifecycle unit+integration 15 + start/rebuild/disable/delete/caller-pays/embedding 55 green.

---

## 🅿️ Park bucket — NOT debt to clear (acknowledge only)

**Knowledge eval / extraction R&D** (fix when eval shows pain, not a task to "clear"):
`D-PASS2-FILTER-*` (NEO4J-REALIZED-F1, FACTS-SUPPORT, RELATION-ONLY-OPT, CLOUD-CALIBRATION, RUNTIME-FLAG, CACHE, PER-USER-UI, CATEGORIES-AB-TUNE, PER-JOB-OVERRIDE), `D-PASS2-WRITER-*`, `D-PASS2-CASCADE-*`, `D-EVENT-RULE-*`, `D-CYCLE71/1-*`, `D-EVENT-AGGREGATOR-FUZZY-MATCH`, `D-CLAUDE-JUDGE-VS-GEMMA-*`, `D-EVAL-FRAMEWORK-*`, `D-LM-STUDIO-RESPONSE-FORMAT-*`, `D-JUDGE-EVAL-ASYNCIO-TEARDOWN`, `D-EXTRACTION-PARALLEL-CONCURRENCY-FLAKE`, `D-AGGREGATOR-REASONING-CONTAMINATION-GUARD`, `D-MCP-TASKS-MIGRATION`, `D-MCP-DIRECT-RETURN`.

**Perf-when-pain:** `D-S3A-GOVERNOR-FAIRNESS`, `D-S4C-CONSUMER-PEL`, `D-S4D-CONSUMER-PEL`, `D-TRANSL-M6B-USAGE-BATCH`, `frontend useJobProgressRate.ts:46` (>10k jobs), `knowledge benchmark runner.py` (distributed lock at scale).

**Blocked on a larger refactor (do NOT pick up standalone):** `D-EXTRACTION-RAW-OUTPUT-CACHE` (ex-DEFERRED #077; persist+reuse raw extraction output) — PO-gated behind `world-core-foundation` → extraction-pipeline refactor; it is ONE slice of that refactor, not a standalone feature. Full spec/plan: `docs/specs|plans/2026-06-12-extraction-raw-output-cache.md`. *(Re-filed here at the 2026-06-16 origin/main merge — DEFERRED.md was reset to main's authoritative foundation ledger.)*

**HA / scale (document constraint, don't build now):** `D-CAMPAIGN-DRIVER-SINGLETON` (single-replica until HA claim dispatch).

**Won't-fix / superseded:** `D-RAWSEARCH-P2-COSINE-RANK` (superseded by E5B rerank), `D-JOBS-SPEND-CONSUMER-MISFIT` (money-safety micro-pattern, intentionally hand-rolled), `D-JOBS-EMIT-RECONCILE-BACKSTOP` (by-design), `D-JOBS-VIDEOGEN-OUTBOX-FLAGGATED` (flag-gated by-design).

**Low-value backfill/contract notes:** `D-TRANSL-M6B-USAGE-BACKFILL`, `D-P1-CHAPTER-RAW-AUDIT`, `D-P1-CONTRACT-DRIFT-TEST`, `D-P1-LEAF-TEXT-NESTED`, `D-DOCKER-RESTART-INVESTIGATION`, `D-WORKER-INFRA-CONFIG-TEST`, `D-CYCLE73F-LIVE-SMOKE` (when stable).

---

### Notes
- Counts are approximate; the handoff is the SSOT for exact wording — confirm each row's current state at batch start (some may have been cleared in flight).
- Live-smoke batches assume the rebuild-stale-images-first discipline (a stale image false-greens).
- When a batch completes, tick its row in the Sequencing overview + move cleared items to a "Recently cleared" note (or delete after a couple of sessions), per the project's defer-drift discipline.
