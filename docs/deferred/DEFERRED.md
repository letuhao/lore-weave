# Deferred Items

<!-- Managed by Scribe (AMAW) or main session (default mode). Do not edit manually unless cleaning up. -->
<!-- Next ID: 081 -->
<!-- merge 2026-06-11: LOOM feat/composition-service (065-072) × origin/main (065-069,071) — both allocated 065-072; LOOM open rows renumbered to 073/074; LOOM 065-070 are DONE history (detail in git + docs/03_planning/LOOM/SESSION_HANDOFF.md). -->
<!-- Note: zone-map V1.2/V2 sessions reserved IDs into 039 range; main-side GEO track allocated 013. geo-generator-amaw branch added 039 + 040 (renumbered from local 025/026 on main merge to avoid ID collision). -->


| ID | Origin | Description | Target | Severity |
|---|---|---|---|---|
| 080 | 2026-06-15 RAID-adjust /review-impl #3 | ✅ **RESOLVED 2026-06-15 (`e460a775`)** — shipped the scroll affordance: `TabScrollStrip` wraps the compose sub-tab scroller and overlays a scroll-aware left/right gradient fade (shows only on the side with more content; `Children.count`-keyed ResizeObserver after the /review-impl LOW fix in `d43b0cd0`). vitest 4 (geometry-driven fade states) + the existing CompositionPanel containment tests. The width-bounded Playwright CI guard remains optional (not added). ~~D-COMPOSE-PANEL-TABSTRIP-AFFORDANCE — compose right-panel tab strip has no scroll affordance.~~ | DONE | — |
| 079 | 2026-06-15 RAID-adjust /review-impl #2 | **D-C24-ANCHOR-ON-THE-FLY — divergence wizard blocks overriding an unanchored entity (workflow nicety).** The C24 id-space fix (`48e5e6bf`) correctly makes the wizard write the glossary anchor as `target_entity_id` and DISALLOWS overriding a `discovered`/unanchored entity (with an actionable hint), since a knowledge-node-id override silently no-ops at the packer present-lens. This is not a regression (it surfaces what was already broken), but freshly-extracted entities are often unanchored, so on a book whose glossary isn't built/promoted the wizard's top-by-mention list may show many rows that can only show the hint — forcing the user out to the Entities tab to promote first. Nicety: an "anchor & override" affordance inline in Step-3 (promote-then-override in one action). The brief scoped out new BE, so deferring is defensible. | Writer-flow polish (optional) | LOW |
| 078 | 2026-06-15 RAID-adjust /review-impl #1 | **D-WORKERAI-DECOUPLED-DEFAULT-UNBOUNDED — document the unbounded-default fan-out (ACCEPTED, doc-only).** The D-C12 fix (`ca2611fd`) wires `concurrency_level` into the decoupled trio via a per-call Semaphore; when `concurrency_level` is None (the column default = every legacy job) the fan-out changed from a SEQUENTIAL for-loop to an unbounded `asyncio.gather`. Reviewer adjudicated SAFE because `submit_job` is a fire-and-forget enqueue POST to the gateway (not a held LLM call) — so the burst is N cheap enqueues, governed downstream by the gateway/worker dispatch, not N live LLM connections. The 3-op trio matches the SDK sync path's 3-wide gather; the real (mild) exposure is multi-batch recovery/filter enqueues now firing concurrently by default. No code change owed. Lever if the gateway ever shows enqueue-side pressure: clamp `None`→a sane default cap. | Only if provider/gateway shows enqueue pressure | LOW |
| 077 | 2026-06-12 extract-raw-output-cache DESIGN | **D-EXTRACTION-RAW-OUTPUT-CACHE — persist raw extraction output + reuse cache (DESIGN done, BUILD blocked).** Glossary extraction discards the raw LLM output after POSTing to glossary → re-extracting an unchanged chapter re-spends full tokens, and there's no way to re-apply/rebuild without re-calling the LLM. Approved design: a new append-only `extraction_raw_outputs` table (raw_response + parsed_entities + content_hash + kinds_requested + model/thinking/tokens/cost) feeding (a) a **cache-gate** (skip LLM when `(content_hash, kinds)` already covered, model-independent, default-on + force override, partial-kind reuse) and (b) a **replay** path (re-apply cached results to glossary, 0 tokens) + retention delete/usage endpoints + a book-service `draft_version` field. Spec [`2026-06-12-extraction-raw-output-cache.md`](../specs/2026-06-12-extraction-raw-output-cache.md) + plan [`2026-06-12-extraction-raw-output-cache.md`](../plans/2026-06-12-extraction-raw-output-cache.md) (5-phase build order, file targets, tests). **WHY DEFERRED (PO 2026-06-12):** `knowledge-service` is now built and has become a **"god service"** — system-wide scope/logic boundaries are violated heavily and uncontrolled. A large refactor, **`world-core-foundation`, must complete FIRST**; only then does the **extraction-pipeline refactor** run, and **raw-extraction-outputs is ONE SLICE of that refactor**, not a standalone feature. Dependency chain: `world-core-foundation` → extraction-pipeline refactor → this. **Pick-up:** when the extraction-pipeline refactor begins, re-baseline the spec/plan against the new boundaries — table placement may move OUT of translation-service (supersedes the pragmatic co-locate-with-worker choice + the `D-EXTRACTION-REHOME-KNOWLEDGE` note). | extraction-pipeline refactor (after `world-core-foundation`) | MED |
| 076 | 2026-06-11 wiki mockup-vs-code audit (gap #4) | ✅ **RESOLVED 2026-06-11 (W5)** — PO greenlit; built as a **revise** model (verify is rule-based, so the 2nd model drives `revise_article`'s corrective re-gen; null ⇒ prose). `revise_model_ref`/`revise_model_source` threaded knowledge→glossary→FE; spec/plan [`2026-06-11-wiki-w5-per-step-model.md`](../plans/2026-06-11-wiki-w5-per-step-model.md); end-to-end → `D-WIKI-W5-LIVE-SMOKE`. ~~**D-WIKI-PER-STEP-MODEL — per-step prose vs verify model (decision-gated, never built).**~~ The wiki design mockup (screen ②, [`2026-06-08-wiki-llm-building-mockup.html`](../specs/2026-06-08-wiki-llm-building-mockup.html)) shows TWO model pickers — "Mô hình văn phong (prose)" + "Mô hình kiểm chứng (verify)" — but **neither FE nor BE implements it**: the whole pipeline uses one `model_ref`/`model_source` ([wiki_gen_jobs.py:43-57](../../services/knowledge-service/app/db/repositories/wiki_gen_jobs.py#L43); `writeback.py` has an unused `step_models` param always defaulted). This is a never-built design idea, NOT incomplete work (not a regression). To build (W5 in [`2026-06-11-wiki-fe-gap-closure.md`](../plans/2026-06-11-wiki-fe-gap-closure.md)): add `verify_model_ref`/`verify_model_source` to the job + thread into `verify_article`/`revise_article` (fall back to the prose model when null) + a 2nd FE picker in `GenerateWikiDialog`. **Decision-gated:** only build if the PO wants per-step model selection; otherwise drop from the design. Audit: [`docs/reports/2026-06-11-wiki-mockup-vs-code-audit.md`](../reports/2026-06-11-wiki-mockup-vs-code-audit.md) §4. | PO decision (build W5 or drop) | LOW |
| 075 | 2026-06-11 D-PLATFORM-SYSTEM-CONFIG-UI CLARIFY+PLAN | **D-PLATFORM-SYSTEM-CONFIG-UI — RBAC + runtime system-config + admin UI (BUILD deferred, conflict-blocked).** An admin-gated UI to view/edit typed platform config at runtime (no restart) — surfaces the wiki M8 flags (`wiki_fewshot_enabled`, `wiki_llm_judge_*`, `wiki_learning_enabled`) + all ops flags without an env redeploy. **CLARIFY locked (PO 2026-06-11):** full RBAC · runtime-apply via shared store + cached reads · all typed config (bool+int+float) · sliced **S0→S4** (each its own `/loom`, like campaign-service) · config store = **new `config-service`** (Go/Chi, own DB). Architecture: RBAC in auth-service (roles in JWT claim, `require_permission` middleware, env-seeded `ADMIN_EMAILS` bootstrap, gateway `/v1/admin/*` guard); `config-service` owns `system_config`+typed `config_registry`+audit; per-service `ConfigClient` (Py+Go, TTL cache, precedence **DB>env>default**). Spec [`2026-06-11-platform-system-config-ui.md`](../specs/2026-06-11-platform-system-config-ui.md) + plan [`2026-06-11-platform-rbac-system-config.md`](../plans/2026-06-11-platform-rbac-system-config.md) (per-slice file targets + acceptance + tests). **WHY DEFERRED:** another agent is concurrently making large changes to the auth/permission layer — S0 (RBAC) lands in the same auth-service files → high conflict risk. **Pick-up:** wait for that auth-permission work to merge to `main`, re-baseline S0 against the new auth shape (may already provide roles/middleware → reuse not duplicate), branch `platform/rbac-system-config` off main, `/loom` S0 from CLARIFY. **/review-impl MANDATORY on S0/S1** (auth = privilege-escalation surface). | After concurrent auth-permission work merges | MED |
| 074 | 2026-06-10 LOOM-83 (FD-25 decision) | **D-INGEST-CONSUMER-THROUGHPUT (perf).** The knowledge events consumer (`app/events/consumer.py`) processes a poll batch **sequentially** (`for msg in messages: await _handle_message`). Under a burst (e.g. bulk-publish → N `chapter.published`), ingestion serialises — mostly behind the heavy `chapter.published` inline passage embed (see `D-CM3C-PASSAGE-INLINE-BULK`). The heavy LLM R+E+F is already parallel in worker-ai (separate process), so this is an ingest-fanout gap, not an extraction-speed gap. **Fix recipe (cheap→deep):** (1) `asyncio.gather` over each XREADGROUP batch (≤`count`=10; msg_ids independent → safe; preserves the FD-18 reclaim which runs in a separate loop phase); (2) move the inline embed behind the `extraction_pending` drain (D-CM3C); (3) scale worker-ai replicas (host-based `consumer_name` → multi-instance ready). **Trigger:** publish→extract latency or Redis consumer-group lag (`XINFO GROUPS <stream>` pending) over threshold under real load. Per CLAUDE.md "fix perf when profiling shows pain" — deferred until load evidence. | before a real-load deploy | MED (perf) |
| 073 | 2026-06-10 LOOM-79 (FD-27 build) | **D-PROVIDER-REGISTRY-MIGRATE-FRESH-DB.** `migrate.Up` fails on a *clean* DB — `TestGetInternalModelInfo` (and any provider-registry integration test) errored `relation "platform_models" does not exist (SQLSTATE 42P01)` during migration setup. The live `loreweave_provider_registry` DB is fully migrated (built up incrementally), so production is unaffected and the FD-27 live-smoke passed — but a migration ALTERs/indexes `platform_models` before its `CREATE` in a single-shot fresh run. Rotted because the integration suite is skip-by-default (`TEST_PROVIDER_REGISTRY_DB_URL` unset on dev). NOT FD-27 (no migration touched). Fix: reorder the migration so `platform_models` exists before it's referenced; add a CI fresh-DB migrate smoke. | provider-registry `internal/migrate` | MED (test-rot; blocks fresh-DB integration tests) |
| 070 | 2026-06-08 LOOM cycle-14 (narrative_thread S1 foundation) | **D-NARRATIVE-THREAD-S2-S4 — the ledger producer/consumer + eval.** Cycle 14 shipped the **S1 foundation only** (PO-chosen full ledger §10.2): the `narrative_thread` table + `generation_job.state` JSONB ([composition migrate.py](../../services/composition-service/app/db/migrate.py)), the `NarrativeThread` model, the `NarrativeThreadRepo` (open_thread / update_status / list_open / list_for_project — the F2 re-injection ordering locked), 4 real-PG integration tests. ~~**The ledger is INERT**~~ — **PO reshaped FD-1 (2026-06-09): build the full feature, sliced.** ✅ **S2 (producer) DONE (LOOM-64):** `detect_and_update_threads` ([engine/narrative_thread.py](../../services/composition-service/app/engine/narrative_thread.py)) — a gated, best-effort LLM pass on the auto-generate commit opens promises/foreshadows/MICE-opens + pays resolved open threads. The ledger now has a real WRITER (no longer inert on the write side). 11 unit tests; /review-impl folded. ✅ **S3 (re-injection) CORE DONE (LOOM-65):** `gather_open_promises` lens → a protected, sanitized, capped `<open_promises>` pack block + a `build_messages` steer → **the loop is closed** (S2 writes → S3 re-injects + steers the model to honor/pay). 14 unit tests; /review-impl folded (SEC3 sanitize, steer, pack-gate). Optional follow-up: the formal `ReasoningState`/`generation_job.state` persistence (resumable). ✅ **S4a (DEBT-check BE) DONE (LOOM-66):** `GET /works/{project_id}/narrative-threads` (`status=open` = the unpaid debt, priority-ordered | `all` = the full ledger; `open_count` = a TRUE `count_open`, NOT capped by the list LIMIT) + an advisory `open_promise_count` on the chapter-generate result. Read-only; advisory per spec D4. 7 router unit tests (count≠len non-default lock + `_open_promise_count` gate); /review-impl folded (MED capped-count → `count_open`; LOW helper-test; 2 LOW accepted). 393 composition unit. ✅ **S4b (eval arm + full-loop live-smoke) DONE (LOOM-67) — 070 RESOLVED, narrative_thread FD-1/6/7/8 COMPLETE.** `reinjected_promise_count` (deterministic S3-fired signal) + a ledger-BLIND `promise_audit` judge + `/promise-audit` route + `eval_narrative_thread.py` (ON/OFF harness). **★ FULL-LOOP LIVE-SMOKE PASSED** (real stack, n=1 & n=3): S2 opens → S3 re-injects → control OFF=0 → S2 pays (5/7/7 across 3 books) — **the loop is proven end-to-end live**. **⚠️ EVAL NULL:** dropped-rate OFF 0.454 vs ON 0.429 (Δ=+0.025, within noise; ON wins 1/3; n=3 disjoint judge) — no measured lift; per §8 the gate to expand into FD-9/10/12/13 is NOT met (those stay deferred). The ledger ships **ADVISORY (D4), opt-in** — functional + live-proven, no quality-lift claim. 381 unit; /review-impl MED (rate-clamp) + 2 LOW. ~~Remaining S4b~~ DONE. | ✅ RESOLVED (built + live-proven; eval null, honestly recorded) | DONE |
| ~~065~~ | 2026-06-08 LOOM cycle-8 D-A2S1B2 live-smoke | ✅ **RESOLVED 2026-06-08 (LOOM-52, Cycle 10).** Root cause was NOT worker-ai. The M1 migration ([knowledge-service/app/db/migrate.py](../../services/knowledge-service/app/db/migrate.py)) silently renamed the `extraction_jobs_status_check` constraint `complete`→`completed` AND dropped `paused`/`cancelled` (its comment only claimed "add summarizing"), while the WHOLE codebase + FE emit `complete`/`paused`/`cancelled` (knowledge repo `update_status(...,"complete")`, `state_machine.JobStatus`, worker-ai `_complete_job`, `ExtractionJobsTab`). So every finished/paused/cancelled extraction CheckViolated → misreported `failed`. **Fix = reconcile the constraint** (inline L287 + M1 ALTER) to the full vocab `('pending','running','paused','summarizing','complete','failed','cancelled')` — a widening; **NO worker-ai/state_machine/FE change** (the roadmap's "fix worker-ai complete→completed" was the wrong diagnosis — it would diverge from the repo+FE and still leave paused/cancelled broken). Regression-lock: real-PG integration test (all 7 statuses accepted, `test_extraction_jobs_status_check_accepts_full_vocabulary`) + DDL string tests. /review-impl: 2 LOW + 1 COSMETIC folded (summarizing reserved-state doc, EXCEPTION-swallow caveat, test rename); cleared the cross-service replica concern (translation-service `extraction_jobs` is a separate DB/table with no status CHECK). Live dev `extraction_jobs` = 2 `failed` rows (the symptom), 0 `completed` → ADD will succeed on next knowledge-service restart. **Full worker-ai→knowledge extraction chain smoke → cycle 11 (066).** | DONE | — |
| 066 | 2026-06-08 LOOM cycle-8 D-A2S1B2 live-smoke | **D-A2S1B2-STATUS-EFFECTS-PRODUCER-GAP — diagnosis corrected + instrumented (LOOM-53, Cycle 11).** The cycle-8 hypothesis "`canonical_id=NULL` (anchoring) blocks the status" is **WRONG**: the Pass-2 status resolver ([pass2_writer.py](../../services/knowledge-service/app/extraction/pass2_writer.py) `_resolve_status_entity_id`) uses the chapter-local entity map keyed on `entity.id` (always set for any extracted entity — NO `≥2 links` requirement) → Tier-A.1 resolves a dead character that was extracted this chapter regardless of glossary anchoring. The whole producer path is **statically CORRECT** end-to-end: event-extraction prompt asks for `status_effects` (death→`gone`, example "Zhao"), the parser/`StatusEffect` model handles them, `event_order` is non-null in the full publish path, the resolver matches on `entity.id`. So cycle-8's "no `:EntityStatus`" = LLM non-compliance (didn't emit a well-formed status_effect) OR a path-specific event_order/fold miss — **the two silent-skip points are now a metric** (`knowledge_extraction_status_effect_total{outcome}`) so the cause is observable, not guessed. 066 is independent of 065 (status write precedes `_complete_job`). **✅ RESOLVED 2026-06-09 (LOOM-63 / FD-4) — the real bug was found + fixed.** The canonical 067 run (a part-less death-chapter book, gemma) showed entities persisted but 0 `:EntityStatus`, with knowledge logs `A2-S1b: status_effect skipped — event 'Bryn dies' has no event_order`. Root cause: `event_order` (= `chapter_base`) was derived **only** from `hierarchy_paths.chapter_index`, and the worker builds `hierarchy_paths` ONLY when `hierarchy.part is not None` → a **flat book (chapters, no parts)** got `event_order=None` → every `status_effect` skipped AND the dense timeline axis collapsed. Fix: thread the chapter's own `sort_order` as a separate `chapter_index` (pass2_writer fallback + persist-pass2 request field + worker client + runner capture-outside-part-gate). **Live-proven on the fixed/rebuilt stack:** part-less death-chapter → 2 fresh `:EntityStatus{gone}` with `from_order`=chapter_sort×stride (was 0). 5 regression tests. **Compounding cycle-8 cause also confirmed:** the original job used qwen3.6-35b (a reasoning model → emits empty/think → 0 status) + failed on the M1 constraint (065, cycle-10 fixed). | DONE | — |
| 067 | 2026-06-08 LOOM cycle-11 (066 follow-up) | **D-066-LLM-PRODUCER-DIAGNOSTIC.** Run ONE real worker-ai LLM extraction of a published death-chapter (full publish→worker-ai chain + the fresh-book extraction bootstrap — a fresh book never auto-extracts; trigger one manual extraction first) and read `knowledge_extraction_status_effect_total` on knowledge-service `/metrics`: `persisted`>0 → it works (clear 066); `skipped_no_event_order`>0 → hierarchy/event_order not threaded in that path (code fix); `skipped_unresolved`>0 → entity_ref fold-mismatch (normalize); **total stays 0 while a death was extracted → the LLM did not emit a well-formed status_effect** (model-compliance — a prompt/eval item, not a code bug; tune the prompt or accept on the local model). **CAVEAT (review-impl LOW):** a *malformed* status_effect (status∉{active,gone} / empty entity_ref) is silently filtered upstream by `_filter_status_effects` ([event.py:128](../../sdks/python/loreweave_extraction/extractors/event.py#L128)) at PARSE time with no count, so `total==0` strictly means "no **well-formed** status_effect reached the writer". Needs the full stack up + an LLM. **✅ RESOLVED 2026-06-09 (LOOM-63 / FD-4) — the diagnostic ran (full stack + LM Studio gemma-4-26b) and found the real bug → see row 066.** Outcome: `skipped_no_event_order` (the predicted "hierarchy/event_order not threaded" branch) — a part-less book never got `event_order`. Fixed + live-proven (fresh `:EntityStatus` with chapter-sort-derived `from_order`). ALSO surfaced a reusable operational caveat: a **reasoning model** (qwen3.x) emits empty extraction output (burns budget on `<think>`) → 0 events → 0 status, silently — extraction must use a non-reasoning model / `reasoning_effort=none`. (A zero-event/reasoning-model warning guard remains a worthwhile future hardening item.) | DONE | — |
| 069 | 2026-06-08 LOOM cycle-13-BUILD (was LOOM-43 LOW-1) | **D-A3-REPLACE-ORPHAN-ARC-NODES (BE).** Precisely diagnosed during the planner-FE build: `outline.commit_decomposed_tree` ([services/composition-service/app/db/repositories/outline.py](../../services/composition-service/app/db/repositories/outline.py)) ALWAYS inserts a NEW `arc` node + NEW `chapter` nodes each commit, and on `replace=true` soft-archives ONLY the target chapters' SCENES (not the prior arc/chapter nodes). So every re-plan leaves the PRIOR arc node + its chapter nodes childless (their scenes archived, a fresh arc/chapters created alongside) → orphan arc/chapter nodes accumulate in the outline. **Clean fix is BE, not FE** (the planner UI doesn't render the outline tree, so there's no FE display to filter): on `replace`, also soft-archive the prior arc + its now-childless chapter nodes for the target chapters (the recursive soft-archive idiom already exists — outline.py:5-6). Low urgency (archived scenes already hide; the orphan nodes are inert structure). **✅ RESOLVED 2026-06-10 (LOOM-78 / FD-17).** On `replace`, `commit_decomposed_tree` now archives `scene`+`chapter` nodes for all `affected` chapter_ids (also reaps pre-accumulated orphan chapter nodes) + a SCOPED childless-arc sweep over only the parent arcs of the archived chapters (`NOT EXISTS` active chapter child). **/review-impl HIGH:** the initial sweep was project-wide → would silently archive an unrelated freshly-created empty arc; re-scoped to the affected arcs (bystander regression test added). 3 real-PG tests (full-replace · partial-replace preserves out-of-target arc · bystander survives); 47 integration pass. | composition-service (outline repo) | DONE |
| ~~068~~ | 2026-06-08 LOOM cycle-12 K17 foundation | ✅ **RESOLVED 2026-06-08 (LOOM-55, cycle 12b).** Wired the producer: `POST /internal/extraction/embed-entities-backfill` ([internal_extraction.py](../../services/knowledge-service/app/routers/internal_extraction.py)) — resolves the project's model/dim, drains `embed_project_entities` (batch 200) with three terminators (`candidates<batch` drained · `embedded==0` no-progress, closes the all-skip infinite-loop · `max_iterations` backstop) + a soft `max_entities` cap; degrades clean (no model/book), 404. **6 route unit tests** (incl. the no-progress + cap-exit branches). **★ LIVE SMOKE PASSED (cross-service: knowledge ↔ provider-registry ↔ glossary ↔ Neo4j):** rebuilt knowledge-service, restored the demo project config (model `019e7f71`, dim 1024, real book `019e7850-a8d9`), POST backfill → **embedded 22/22 anchored** (drained, 1 iter) via REAL provider-registry embeddings; Neo4j `emb1024=22 has_model=22 has_version=22` (was 5 hand-stamped); `/internal/context/glossary-semantic` → **tier=semantic** ranked (元始天尊 0.825 / 闡教 / 姜子牙 / 玉虛宮) on the AUTO-embedded vecs. **Retires the DEFERRED 061 hand-stamp** — K17 now produces entity vectors automatically. /review-impl: 4 LOW (cap-exit test added; soft-cap + concurrent-double-cost + sync-drain accepted), no HIGH/MED. **Optional remaining (not blocking, route covers correctness):** an auto-incremental hook after `extraction_run_completed` + auto-enqueue a backfill from the `change_embedding_model` flow — both just call the same route/producer. | DONE | — |
<!-- ↓ main-side OPEN deferrals (origin/main). IDs 065-069/071 directly above are LOOM-branch DONE history; the same IDs below are main's OPEN items — dup is historical, reconcile at the next DEFERRED sweep. ↓ -->
| 069 | 2026-06-10 glossary-propose P2 | **D-GLOSSARY-WRITEBACK-DEDUP-RACE.** The writeback path (`createExtractedEntity`, reused by `proposeNewEntity` + `bulkExtractEntities`) does a non-atomic find-then-create with no partial-unique index on `(book_id, kind_id, cached_name) WHERE deleted_at IS NULL` → concurrent same-name creates can dup. Low practical risk (sequential chat dedups; tool-vs-pipeline reconciled by mui#1c merge). Fix = the index + `ON CONFLICT` — a **writeback-path** task (touches pipeline + merge), not glossary-assistant. | Writeback hardening / when dup drafts observed | MED |
| 068 | 2026-06-10 glossary-mcp P1 | **D-GLOSSARY-MCP-LIVE-SMOKE.** P1 glossary Go MCP server + 3 read tools + ownership guard built & unit-proven: 6 ownership tests (owned / non-owner→NotAccessible / not-found→NotAccessible / book-down→Unavailable fail-closed / cache-positive-only / no-cache-failure), 2 SO-1 token-gate tests, extraction-preserving select/kinds tests; `go build ./...` clean; ai-gateway federates glossary as provider #2 (computeCatalog 2-provider unit-covered). STILL deferred: FULL docker stack-up **chat → ai-gateway → glossary** with a real read tool (`glossary_search`/`glossary_list_kinds`) — needs postgres + book-service (ownership) + glossary + gateway + chat, not bootable in the dev session. Run at next stack-up: owner JWT → chat turn that triggers a glossary tool → confirm chat→gateway→glossary→book-service(ownership) round-trips and returns entities; verify a non-owned book_id returns the uniform not-accessible. Mechanism proven by the H3 spike (go-sdk header→ctx) + P0 gateway smoke + unit tests. | Before prod traffic / next stack-up | MED |
| 071 | 2026-06-10 architecture-review | **D-ARCH-WEAKNESS-REGISTER.** Architecture review (2026-06-10) surfaced 7 surviving weakness items, tracked in detail in [`docs/ARCHITECTURE_WEAKNESSES.md`](../ARCHITECTURE_WEAKNESSES.md) (AW-1..AW-7), with the withdrawn/recalibrated critiques recorded there so they don't get re-litigated. Planned as **post-MVP-0.1 hardening** (project is finishing for the 0.1 release). Items: **AW-1** sync cross-service call on search hot path (watch); **AW-2** audit whether Neo4j's graph half feeds retrieval vs collapsing the vector leg into pgvector (action — repo audit); **AW-3** two message systems + Postgres event_log consolidation (watch); **AW-4** BYOK credential encryption-at-rest; **AW-5** derived-store rebuild-cost economics (defer until corpus is real); **AW-6** structural-assumption risk in the LOCKED Living Worlds kernel — get a thin V1 in front of structural bets before widening design (watch); **AW-7** reframe `DATA_ARCHITECTURE.md` intro away from the "16 databases" headline (doc). **⚠️ Exception to "post-0.1": AW-4 (credential encryption) is a GATE before the first real BYOK user** — if 0.1 exposes BYOK to real users, AW-4 jumps ahead of the rest (its failure mode is irreversible). Framing: judge fit-for-target + contract correctness, NOT current load (see memory `project_living_worlds_methodology`). | Post-MVP-0.1 (AW-4 = pre-first-real-BYOK-user) | MED |
| 067 | 2026-06-10 ai-gateway P0 | **D-AIGW-P0-FULLSTACK-SMOKE.** P0 gateway proven by a REAL gateway+fake-provider integration smoke (`services/ai-gateway/test/smoke/`): federation list, per-call envelope (alice/bob, no cross-user leak), SO-1 401 on bad token, OpenAI-shaped definitions — all PASS on live node processes. STILL deferred: the FULL docker stack-up **chat → ai-gateway → knowledge** with a real `memory_*` tool (e.g. `memory_recall_entity`) — the full stack (postgres+neo4j+redis+minio+provider-registry+knowledge+chat) was not bootable in the dev session. Run at next stack-up: bring the stack up, send a chat turn that triggers a memory tool, confirm it routes chat→ai-gateway→knowledge and returns. Hard cutover means this is the prod-traffic gate. | Before prod traffic / next stack-up | MED |
| 065 | 2026-06-10 ai-arch-enforcement | **D-KNOWLEDGE-MODEL-MAPS-REGISTRY-SOURCE.** knowledge-service hardcodes model→metadata maps that duplicate provider-registry authority: (a) `app/pricing.py` model→price table (gpt-4o / claude-* / llama- … + Decimal rates) vs registry `internal/billing/DefaultPricing`; (b) `app/context/selectors/passages.py` model→embedding-dim map (`text-embedding-3-small`:1536 …) vs registry capability data. Violates "No hardcoded model names (ENFORCED)". Both are **allowlisted in `scripts/ai-provider-gate.py`** so the gate enforces NEW violations without blocking on these tracked ones — remove the allowlist entries when migrated. Migrate to source pricing/dims from provider-registry (internal lookup) with a thin local fallback. Low urgency — pricing is cost *estimates* (not user billing); dims are stable per model. | When drift bites / provider-registry exposes the lookups | LOW |
| 066 | 2026-06-10 ai-arch-enforcement | **D-AI-AGENT-MCP-MIGRATION-AUDIT.** Per the CLAUDE.md MCP-first invariant, audit AI services for agentic paths predating the rule that should become MCP tool-calls via `ai-gateway`. **Glossary slice DONE** (P0–P6 + live-smoke). Remaining: **composition-service** (raw-prompt provider calls via `clients/llm_client.py` — migrate if/where it makes tool-using decisions); confirm translation/lore-enrichment are genuinely non-agentic pipelines (exempt). Output = per-service agent-vs-pipeline classification + tickets. | When composition tool-set is scoped | MED |
| 057 | 2026-06-05 LE-PROD-2 P3 | **D-ENRICH-P2P3-EVAL-UNLOCK.** Fabrication (P2) / re-cook (P3) stay gate-LOCKED (fail-closed) until a judged eval PASSES. Unlocking is an EVAL/CONFIG task, NOT code (the run-route + judge-binding + in-container judge import all shipped in P3a/b/c): (1) register ≥2 DISTINCT judge families in provider-registry (BYOK — env currently has only ONE, qwen); (2) `POST /internal/eval/{project}/run` with those judges → if composite + Fleiss-κ + ≥2-families pass, `/gate-status` auto-unlocks P2/P3. Needs ≥1 non-rejected proposal to score; a PASS is NOT guaranteed (it is a real quality gate, not a rubber-stamp). | Pre-go-live (when P2/P3 wanted) | MED |
| 058 | 2026-06-05 LE-PROD-2 P3 | **D-ENRICH-EVAL-RUN-GUI.** The eval-run route is internal-token (server-to-server) — there is NO self-serve GUI "Run eval" button. To let an owner trigger an eval from the FE, add a gateway route + an FE control. NOT required for the unlock mechanism (the route works today; trigger via script / another service). | Track-2 / pre-go-live UX | LOW |
| 059 | 2026-06-05 LE-PROD-2 | **D-ENRICH-GOLIVE-DEPLOY.** Deploy enrichment + eval to AWS (ECS/RDS/S3/ElastiCache) + load-test + monitoring/alerting. Explicitly OUT of the in-branch hardening scope — a separate go-live task (PO-confirmed). | Go-live | MED |
| ~~061~~ | 2026-06-07 glossary-ai-pipeline-v2 mui#1+#4 | ✅ **CLEARED — D-GLOSSARY-AIPIPE-LIVE-SMOKE.** Both residuals live-proven 2026-06-07 (HEAD da82ab14, 封神 demo project 019e7850-aa1c, book 019e7850-a8d9). **mui#1 auto-trigger LIVE:** temporarily set `KNOWLEDGE_GLOSSARY_WRITEBACK_ENABLED=true` + `MIN_MENTIONS=2` (demo has 0 gaps at the locked ≥10, 17 at ≥2 — smoke tests the MECHANISM not the policy floor), POST `/internal/extraction/persist-pass2 {is_last_chapter_of_book:true}` → log `"mui#1 writeback: proposed 17 entities"` → glossary `ai-suggested` drafts 0→8 new (萬仞山/度厄真人/李靖/…, status=draft). Flag reverted + 8 smoke drafts deleted + knowledge restarted OFF after. **mui#4 vector ranking LIVE:** the entity-embedding WRITE pipeline isn't built yet (K17), so stamped 5 anchored entities with REAL provider 1024-d embeddings (provider-registry /internal/embed, model 019e7f71) as a K17 stand-in → `POST /internal/context/glossary-semantic {query:"姜子牙 元始天尊 阐教"}` → **5 ranked items, tier=semantic** (元始天尊 0.856 / 姜子牙 0.809 / 通天教主 0.697 / 玉虛宮 / 陳塘關) — semantic-first path + weighted rank confirmed. mui#1/#4 CODE was already done+pushed; this closed the data-condition sign-off. **Note:** real embedding-generation at scale still awaits the K17 pipeline (separate track, out of mui#4 scope); the 5 stamped vecs are harmless dev-KG residue. | DONE | — |
| 064 | 2026-06-07 glossary-ai-pipeline-v2 mui#3 C-adopt | **D-GROUNDING-C-ADOPT.** Composition adoption of `loreweave_grounding` DEFERRED (PO 2026-06-07) — no clean/valuable slice. composition's `app/packer/sanitize.py` `neutralize` is structurally divergent: `⟦…⟧` marker (not `[FICTIONAL] `), **angle-bracket escaping** (`<`→`＜`) to protect `<canon>`/`<guide>` assembly delimiters that the SDK's NFKC prenormalize would UNDO, and a str-only return. Forcing the shim = behavior change + risk to the delimiter defense for little gain. `GroundingCite` emission from packed lore is net-new with NO downstream consumer (composition packs a prompt, doesn't ground per-item). Revisit when a generation-side `GroundingCite` consumer exists OR composition's sanitize is rebuilt. mui#3 value is already banked (shared SDK + lore-enrichment + knowledge unified). **Re-scouted 2026-06-08 (LOOM branch-debt cycle 5): STILL no clean slice** — verified composition `neutralize` uses `⟦…⟧` + fullwidth angle-bracket escaping (`<`→`＜`) the SDK's `neutralize_proposal_text` NFKC-normalization would FOLD BACK (breaking the `<canon>`/`<guide>` delimiter defense), and grep confirms NO `GroundingCite` consumer in composition's packer/routers. **Re-CONFIRMED 2026-06-09 (LOOM-59 cycle 15) against HEAD 6492f0fa** — both blockers still hold, code-verified not assumed: (1) [sanitize.py:40](../../services/composition-service/app/packer/sanitize.py#L40) still fullwidth-escapes `<`→`＜`/`>`→`＞` + wraps directives in `⟦…⟧`, and the SDK `prenormalize` still NFKC-folds `＜`→`<` ([sdks/.../sanitize.py:110-114](../../sdks/python/loreweave_grounding/sanitize.py#L110-L114)) → adopting the shim would UNDO composition's delimiter defense; (2) grep for `GroundingCite`/`compose_cites`/`loreweave_grounding` across composition-service = ZERO matches → no per-item ground consumer, adoption is net-new with no downstream value. **PO 2026-06-09: keep deferred (re-confirmed), NOT won't-fix** — there is a genuine future trigger. **TRIGGER to revisit:** a generation-side `GroundingCite` consumer appears in composition OR composition's `sanitize.py` is rebuilt (dropping the fullwidth-escape delimiter strategy). Until then, optional + not pursued. | mui#3 follow-up — revisit on trigger | LOW |
| 063 | 2026-06-07 glossary-ai-pipeline-v2 mui#3 LE-migrate | **D-GROUNDING-COMPOSE-MIGRATE.** LE-migrate shimmed the verify trio (canon_verify/sanitize/regurgitation) over `loreweave_grounding` but DEFERRED the grounding-COMPOSE side: lore-enrichment's `app/retrieval/grounding.py` `compose_grounding` + `GroundingRef` stay as-is because `GroundingRef` is a **persisted domain type** in proposals' `source_refs_json` — converting it to the SDK's `GroundingCite` is a data-shape migration (read-compat for existing rows + the re-cook `UUID(corpus_id)` license path), not a thin shim. **Re-CONFIRMED 2026-06-09 (LOOM-61 cycle 17) against HEAD 88c79eac — the "recommended internal-only shim" prescription is NOT achievable cleanly:** the SDK's `from_grounding_ref` is **lossy on `corpus_id`** — it maps `corpus_id`→a `source_type` bucket (`glossary:canon`→`glossary_entity`) and discards the original string ([sdks/.../cites.py:156-168](../../sdks/python/loreweave_grounding/cites.py#L156)), with NO reverse adapter and no room in `GroundingCite` to carry both `corpus_id`+`chunk_id` losslessly. But `corpus_id` is the recook **license-resolution key** (`_license_lookup(ref.corpus_id)`, `admissible_ids = {lic.corpus_id…}` — [recook.py:408-520](../../services/lore-enrichment-service/app/strategies/recook.py#L408)) AND a persisted **min_length=1-validated** `SourceRef` field ([provenance.py:87](../../services/lore-enrichment-service/app/generation/provenance.py#L87)), so a `GroundingRef→Cite→GroundingRef` round-trip breaks both the license path and `source_refs_json` byte-stability. And `compose_grounding`'s dedup/sort/top-K is ALREADY byte-identical to the SDK's `merge_cites` (the SDK lifted it from there), so a shim saves ~15 lines for a lossy round-trip + risk to the gate-locked recook path = net-negative. **PO 2026-06-09: SPIN INTO ITS OWN POST-MERGE TRACK** — NOT on the composition branch (touching lore-enrichment-service here would violate LOOM track-isolation + couple PR #19 to a lore-enrichment change; the close-out gate explicitly allows 17 "done or re-deferred"). **Proper scope for that track:** a real data-shape migration — persist `GroundingCite` natively in `source_refs_json` (with read-compat for existing `{corpus_id,chunk_id,score}` rows) + carry the license key on the Cite (a `corpus_id`/`source_id` field the recook `_license_lookup` can use), informed by P2/P3 unlock context. **Trigger:** scheduled as a standalone lore-enrichment-track cycle after PR #19 merges. | **own lore-enrichment track (post-PR#19-merge)** | LOW |
| ~~062~~ | 2026-06-07 glossary-ai-pipeline-v2 mui#1c | ✅ **CLEARED — D-GLOSSARY-MERGE-LIVE-SMOKE.** Full mui#1c merge loop live-smoke PASSED 2026-06-07 on rebuilt (HEAD da82ab14) glossary+knowledge images. **(1) migrations live:** `merge_candidates`+`merge_journal` created on restart (absent before). **(2) R5 merge via gateway** (:3123, minted owner JWT, book `019e7850-a8d9…`, winner 玉虛宮 / loser 蓬萊): 200, journal `019e9ee6…`. **(3) glossary:** loser soft-deleted + `merged_into`=winner + journal(merged) + `glossary.entity_merged` outbox event (op=merged). **(4) cross-service KG (the bug surface):** worker-infra relay → `loreweave:events:glossary` → knowledge consumer → `handle_glossary_entity_merged` → KG `merge_entities` (loser node gone, **0 orphans** — MED-1 path clean; winner absorbed edges+aliases incl. 蓬萊) + `entity_alias_map` 蓬萊/蓬莱→winner **project-scoped** (MED-2 fix confirmed). **(5) un-merge:** revert 200 → loser live again + `merged_into` cleared + journal(reverted). **By-design residual:** KG `op=unmerged` is a best-effort no-op (spec §3.4 — KG derived, reconverges on re-sync), so the dev KG keeps 蓬萊 folded into 玉虛宮 until a glossary_sync re-creates it (NOT a bug). | DONE | — |
| 060 | 2026-06-05 LE-PROD-2 P1 | **D-ENRICH-CORPUS-COVERAGE.** The demo `fengshen-pd` corpus is 卷001–020 of 100 (405 chunks; covers the founding-arc entities). To widen retrieval to later chapters (聞太師/趙公明/通天/萬仙陣…), re-run `scripts/fetch_fengshen.ps1 -From 21 -To N` → `seed_pd_corpus` (clean rebuild). License-safe PD data task; mechanism done; platform does NOT web-crawl. | Optional (richer demo) | LOW |
| ~~054~~ | 2026-05-31 Phase-B C-FE | ✅ **CLEARED — D-PHASE-B-CFE-BROWSER-CLICK-SMOKE.** Full Playwright browser smoke PASSED (session 75): restarted gateway+auth, seeded an :Entity→:Entity relation + :Event for the test user, ran the Vite dev server (5174) + Playwright — logged in, opened the ZZ Smoke Hero entity detail, clicked "Correct relation", changed `ally_of`→`enemy_of`, submitted → UI refreshed to `enemy_of` AND a `predicate-fix` correction landed in `learning.corrections` (relation/predicate_fix/knowledge/user). Full chain UI→dialog→hook→api→gateway→KS→invalidate+recreate→emit→relay→learning verified end-to-end. Entity + event edit flows share the identical pattern (component-tested + BE live-smoked). Seeded data cleaned up. | DONE (session 75g) | — |
| 053 | 2026-05-31 Phase-B BUILD subB adversary F1 (WARN) | **D-PHASE-B-EMIT-PERMANENT-FAILURE-SURFACE.** `emit_correction`'s blanket `except Exception` (correct for transient PG faults — best-effort §6.6) also swallows PERMANENT failures (malformed payload, serialization edge, future schema drift) with only a `logger.warning`. The §10.1 replay backstop can only replay rows that made it INTO `outbox_events` — a row whose INSERT never succeeded has zero durability. Fix: distinguish transient (pool/conn) from permanent failures; surface non-transient via a metric / error-log-at-ERROR rather than silent drop. Low risk today (payload shape is code-controlled, json.dumps(default=str)). **Empirically confirmed (session 75 smoke):** a non-UUID-coercible `aggregate_id` (a synthetic relation id) made `emit_correction` silently drop the correction (uuid.UUID() raised → swallowed). Harmless in production (all real entity/relation/event ids are 32-hex → UUID-coercible), but it is a concrete instance of the swallow-masks-permanent-failure concern. **✅ RESOLVED 2026-06-10 (LOOM-86 / FD-19·053).** `_record_emit_failure` splits the swallow: transient (`asyncpg.PostgresError`/`ConnectionError`/`OSError`/`TimeoutError`) → warn (replay backstop applies); permanent (everything else — non-UUID aggregate_id, malformed payload, schema drift) → ERROR + `knowledge_correction_emit_failure_total{kind="permanent"}` (pre-seeded). Applied to both `emit_correction` + `emit_config_adjustment`. 2 new outbox tests (non-UUID→permanent+not-awaited; PostgresError→transient). /review-impl: pool-not-init→permanent accepted (anomaly-surface). | Phase E hardening (when corrections feed gold) | DONE |
| 052 | 2026-05-31 Phase-B BUILD subA adversary F-A2 (WARN) | **D-PHASE-B-DIFFCLASS-DESC-GRANULARITY.** `short_description` is classed as CONTENT alongside name/aliases, so a description-only edit makes the content hash differ → `diff_class="boundary"`. But `boundary` is meant for name/alias rename signal; glossary PATCH is *mostly* description edits, so `boundary` over-counts in `/corrections/stats` (feeds future eval-gold/few-shot tiers). Matches the locked design §3 (single content hash), so deferred not blocking. Fix: split a name/alias hash from the description hash (schema add) so `boundary` fires only on name/alias deltas; route description-only edits to `other`. Analytics-only, no write-path impact. **✅ RESOLVED 2026-06-10 (LOOM-87 / FD-19·052).** snapshot.py gained a `description` content-class; `split_snapshot` returns `(structural, content_hash, description_hash)` (content_hash=name/alias only); `ALTER corrections ADD before/after_description_content_hash`; `_persist_correction` persists them. `boundary` now fires only on a name/alias delta; description-only → `other`; the description change is still hashed (privacy) into `description_hash` for the future tiers (no raw text). 6 snapshot tests + handler positional-lock incl. desc-hash-persisted; full learning 141/141; real-DB migration applied. Historical rows keep their old diff_class (forward-looking; no backfill). | Phase D/E (when stats feed tiers) | DONE |
| 051 | 2026-05-31 Phase-B BUILD subA adversary F-A1 (cross-ref) | **D-PLATFORM-CONSUMER-RECLAIM-KNOWLEDGE.** The dead-retry-in-steady-state bug F-A1 (handler failure leaves a message pending in the PEL; `XREADGROUP ">"` never redelivers it; retry counter never advances until restart) was FIXED in learning-service via a periodic `XAUTOCLAIM` reclaim. **knowledge-service's `consumer.py` has the identical cloned pattern** and the same latent bug — port the reclaim there too (less critical: glossary_sync is re-convergeable, but chapter/chat extraction events can also stall). **✅ RESOLVED 2026-06-10 (LOOM-80 / FD-18).** Ported the F-A1 `XAUTOCLAIM` reclaim to knowledge `consumer.py` (`_reclaim_stale_pending` every ~60s on messages idle ≥30s, tombstone-ack + NOGROUP-tolerant). 5 new tests incl. the reclaim→fail→DLQ invariant. Accepted (documented): the `min_idle=30s` pattern can double-dispatch a >30s handler across instances — benign here (`queue_event` ON-CONFLICT-DO-UPDATE + passages delete-first are idempotent). | knowledge-service hardening | DONE |
| 050 | 2026-05-31 Phase-B design §3 (review-impl R2 / PO redact-now) | **D-PHASE-E-RAW-CONTENT-OPTIN.** Phase B redacts by default: `corrections` stores `*_structural` + `*_content_hash`, NOT raw novel text (`before_content`/`after_content` reserved, NULL). Tier-3 organic eval-gold (plan §2.2.3) needs the *raw* before/after text → a per-tenant raw-retention opt-in that populates the reserved content columns, strictly owner-isolated + retention-bounded. Until opt-in, a tenant's corrections feed Tiers 1/2 (anchor/few-shot via hash+structural) but cannot become raw gold. | Phase E (organic gold) | MED |
| 049 | 2026-05-31 Phase-B design §10.1 (review-impl MED-1 / PO build-replay-now) | **D-PHASE-B-REPLAY-TOOL.** worker-infra-hosted `ReplayCorrections` admin task: given a time window, re-XADDs correction-typed `event_log` rows (full payload retained, `UNIQUE(source_service, source_outbox_id)`) back to their streams; learning-service re-consumes idempotently (`ON CONFLICT`). Makes a learning-service-outage-induced loss recoverable. Depends on: high `streamMaxLen` for glossary/knowledge (≈200k) + bumped `event_log`/`OUTBOX_CLEANUP_RETAIN_DAYS` (≈30d) + a `learning-collector` consumer-lag alert. | Phase B BUILD sub-session A/B | MED |
| 048 | 2026-05-31 Phase-B design (plan §2.2.1 / §4) | **D-PHASE-C-TIER1-ANCHOR-CONSUME.** Tier-1 dynamic anchor update — corrections flowing into the per-project anchor index so the NEXT extraction reuses corrected canonical names/kinds/aliases. Per plan §4 this is **Phase C** (`depends_on: B`); the session-74 handoff conflated it into Phase B. Glossary-entity corrections already partly self-heal (Pass-0 rebuilds the anchor index from live `glossary list_entities` + `glossary_sync` MERGE), so the genuine gap is KS-only (non-anchored) entity corrections + relation/event corrections feeding back. Build after the Phase-B capture spine ships + correction volume accrues. | Phase C | MED |
| ~~056~~ | ARCH-1/2 C1+C2 (session 103, arch-unify-chat-rag) | **D-ARCH2-MCP-LIVE-SMOKE.** ~~Real chat-service -> knowledge-service /mcp tool call (memory_recall_entity success path) on a docker stack-up — exercises real Streamable HTTP transport + JSON-RPC init/serialization + header-only scope. Unit suites are mock/loopback only.~~ **✅ RESOLVED 2026-06-10 (LOOM-77 / FD-21).** Ran the real path from the chat-service container via `init_knowledge_client().mcp_execute_tool(tool_name="memory_recall_entity", tool_args={"entity_name":"Kael"}, user_id/project_id/session_id=…)` against the live stack → **`success=true`** AND `found=true` (recalled the `Kael` entity FD-2's chat→KG extraction wrote — confidence 1.0). Proves Streamable HTTP transport + JSON-RPC init + `call_tool` + header-only scope (X-User-Id/X-Project-Id/X-Session-Id/X-Internal-Token) end-to-end. The `use_mcp_tools=True` default ([chat config.py:40](../../services/chat-service/app/config.py)) is now live-justified; no code change needed. | DONE | — |
| ~~055~~ CLEARED | 2026-05-31 Phase-B design (plan §2.1 / §2.4) | ~~**D-PHASE-B2-CONFIG-TELEMETRY.**~~ **✅ SHIPPED session 101** (`a1adef1a`→`62584b1b`, validated `3328d7d0`): `config_registry` + `extraction_runs` + `config_adjustment_events` in learning-service; per-project `extraction_config` drives the pipeline (model/filter/recovery/autocreate/raw-prompts) via SDK `resolve_config`; `PUT /extraction-config` endpoint + FE tuning panel; writer_autocreate wired (was dormant). Proven end-to-end on a real qwen extraction. | Phase B2 | DONE |
| 046 | 2026-05-31 Phase-B design §6.6 (cross-store) | **D-PHASE-B-CORRECTION-RECONCILE.** KS graph edits are Neo4j; the correction outbox row is Postgres → no true transactional atomicity. Emit is best-effort post-Neo4j-success (try/except, WARN on failure). A dropped row under-counts the corrections log (never corrupts the graph). Hardening: a reconciliation sweep comparing Neo4j `user_edited`/`updated_at` nodes vs `corrections` rows to backfill misses. Acceptable to defer for Phase B (Tier-3 gold already needs an agreement/confidence gate). **Note (review-impl LOW-5): a dropped MIDDLE correction is a chain-BREAK, not just under-count** — correction N+1's `before` no longer matches N's `after`; any future history-reconstruction-by-chaining must tolerate gaps. | Phase E (when corrections feed eval-gold) | LOW |
| 045 | 2026-05-31 Phase-B design §11 | **D-PHASE-B-LIVE-SMOKE.** Cross-service live smoke for the capture spine (≥2 services → live-smoke token required): stack-up → user PATCH glossary entity via gateway → relay ships enriched `glossary.entity_updated` (actor=user) → learning-service `corrections` row appears with before/after + diff_class; then KS entity archive → `knowledge.entity_corrected` → corrections row. Runs at BUILD VERIFY (per sub-session). Logged now so the BUILD agent treats it as a must-do, not a happy-path skip. | Phase B BUILD VERIFY | MED |
| 047 | 2026-05-31 session 100 moisture model POST-REVIEW | **(Optional) 8-zone mapping — abundant literal Temperate plains.** After #045 (seasonality) + #046 (moisture), the green C-group mid-latitude band exists but literal `Temperate` (→`Plain` biome) is still only ~2–4 % because Köppen-correctly, wet mid-latitude *continental* interiors are **Dfa/Dfb** (warm/mild-summer humid continental, cold winters) which `classify_koppen` maps to **Boreal** (→`Forest`), and oceanic **Cfb** Temperate is intrinsically coastal. If the world wants an abundant temperate-*plains* band, remap the warm-summer D-group: in `classify_koppen`, before `t_cold < −3 → Boreal`, route `t_warm > ~16` (Dfa/Dfb, 4-season temperate) → Temperate/Subtropical, leaving only cold-summer Dfc/Dfd → Boreal. This is a **classifier/biome choice**, not a physics fix — it changes the *label* of mid-lat continental land from boreal-forest to temperate-plain. PO has NOT chosen this (flagged thrice); log as an option. NOTE: distinct from real `winter_frac` Cs/Cw subtypes (separate v2b). | Optional — if PO wants more temperate-plains | LOW |
| ~~044~~ | 2026-05-30 lore-enrichment C5 (D4-03 wiki-from-KG) | ✅ **RESOLVED 2026-06-04.** `KNOWLEDGE_SERVICE_URL: http://knowledge-service:8092` IS wired in the `glossary-service` environment block (`infra/docker-compose.yml`, with a DEFERRED-044 comment) — landed during the main-merge / a later infra edit; the row was just never marked done. `/wiki/generate` in the dev/prod stack now includes the KG relationship sections (degrades to name/kind/attributes-only only if the var is unset, which it no longer is). Verified by inspection of the compose glossary block. | DONE | RESOLVED |
| ~~042~~ | 2026-05-30 lore-enrichment C0 adversary r2 WARN#2 | ✅ **RESOLVED 2026-06-04 (already implemented in C18; row was stale).** The `/ready` readiness probe EXISTS — `app/api/observability.py` `ready()` acquires the pool and runs `SELECT 1` → 200 `{"status":"ready"}` / **503** `{"status":"not_ready"}` on pool-absent-or-failed, split from the constant-ok `/health` liveness in `main.py` (a DB blip drains traffic via `/ready` without crash-looping the pod via `/health`). Mounted via `observability.router` in `main.py`. Verified by inspection. (Landed during the C18 observability work; the deferral row was never marked done.) | DONE | RESOLVED |
| 041 | 2026-05-29 chunk-D /review-impl LOW-2 (decoration-placer arc) + 2026-05-30 chunk-C /review-impl LOW-3 (biome-theme arc) | **AC-DECO-8 + AC-BIOME-8 CI smoke gate.** Both e2e tests currently skip-tolerant on backend absence (default CI cross-browser job doesn't start tilemap-service). A wire-shape regression (template JSON field renamed, MetadataPanel filter syntax broken, primitive enum drift, biome_theme silently dropped) would not be caught by CI — only by manual smoke. Add a CI job that builds tilemap-service in release, starts it in background with LOREWEAVE_INTERNAL_TOKEN + TILEMAP_HTTP_BIND=0.0.0.0:8220, runs both `node scripts/smoke_decoration_count.js` and `node scripts/check_biome_variety.js` (per-seed sweep), expects exit 0 on each. ~20 LOC YAML in `.github/workflows/game-subtree-ci.yml`. Backend unit tests (502+) already cover algorithms; this gates the end-to-end wire stitching for BOTH the decoration density pass AND the biome-theme painter. The biome-theme stale-backend false-pass discovered during chunk-C verification (docker container 22h old silently dropped biome_theme via serde-skip-unknown) is exactly the regression class this gate would catch. | When biome-theme arc merges to main + before third quality-push theme | LOW |
| 040 | 2026-05-28 v3.5 POST-REVIEW PO directive (geo-generator-amaw, renumbered from local 026 on merge) | **Natural-fragmentation enhancements** (umbrella entry for ~10 features). Real continents are sculpted by water + wind + tectonic break-up — not just drawn algorithmically. v3.5 fractalize ships RANDOM fractal detail; future passes add PHYSICS-DRIVEN fractal detail: (a) coastal wave erosion; (b) wind/Aeolian erosion; (c) river-mouth carving/deltas; (d) tectonic rifting (active plate break-up); (e) sea-level dynamics (glacial/interglacial floods); (f) subduction-driven asymmetric coast modification; (g) hotspot volcanic island chains; (h) karst/dissolution coast detail per-biome; (i) glacial fjord carving; (j) storm-track erosion bands. Full list documented in `docs/plans/2026-05-25-phase-a-v3-roadmap.md` §13 "Natural-fragmentation enhancements". | Phase B+ (water/wind erosion) and V5 (time dynamics) | LOW |
| 039 | 2026-05-26 v3.2 spec §6 design review (geo-generator-amaw, renumbered from local 025 on merge) | `lat_banding` metric entropy rewrite for v3.2's branching shapes (Y-branch, Crab-radial legitimately span 3+ lat bands by design). Spec §6 described an area-weighted band entropy replacement; PO approved defer because v4.5 unified calibration pass will rewrite multiple metrics together. v3.2 ships with current ecotone-aware `lat_banding` and accepts whatever composite delta lands — v5.5.json regenerated at that value. If composite drops >10 PO can flag specific seeds for follow-up via /review-impl. | v4.5 calibration phase | MED |

| 037 | 2026-05-24 V1 tilemap-viewer rescope | Foundation tiles are now algorithm-generated (FFT-tileable noise + per-terrain palette + small specials per `frontend-game/scripts/gen-foundation-tiles.py`) — license-clean + seamless tileable, replaces the prior HoMM3-Flux1d placeholders. V2 still needs an AI-gen pipeline for the **prop/decoration overlay layer** (trees, structures, mushrooms placed on top of the foundation) — that layer is what the multi-book platform invests in for art polish. Front-view portrait sprites + hand-painted encounter backgrounds also belong here (separate branches). Multi-book constraint: per-book prop gen must complete in <few hours at low cost; license-clean (schnell / SDXL / CC0). | V2 asset pipeline (prop/overlay layer) | MED |
| 036 | 2026-05-24 V0 close-out | `frontend-game/src/game/main.ts` ships a `globalThis.Phaser = Phaser` shim required because Phaser 4.1.0's webpack-bundled ESM references the bare global `Phaser` identifier internally (`phaser.esm.js:88604` `new Phaser.Structs.Map()` inside SpriteGPULayer constructor; lines 33884/34948/34969 in render code). Without the shim, calling SpriteGPULayer factory throws "Phaser is not defined" at runtime. Track upstream — remove shim once Phaser ships ESM-clean patch (likely 4.x.y). Saved to memory `project_phaser4_quirks`. | When Phaser 4 ships ESM-clean patch | LOW |
| 035 | 2026-05-24 V0 close-out | game-server EchoRoom has no rate limiting — a malicious client could spam-send messages. V0 acceptable because EchoRoom is dev-only smoke; V1 ZoneRoom/CombatRoom must add per-client rate limits (Colyseus supports via `client.send` budget or middleware) before any production deploy. | V1 game-server rooms | MED |
| 034 | 2026-05-24 V0 close-out | CORS origins in tilemap-service + game-server default to `http://localhost:5174` via `LOREWEAVE_CORS_ORIGINS` env. Production deploy MUST set explicit origin(s) (not `*`); if env is unset or empty in prod, the cors layer falls back to localhost-only which would block real users. Add a startup check in V1 prod build that env is non-empty when `NODE_ENV=production`. | V1 prod deploy hardening | MED |
| 033 | 2026-05-24 V0 close-out | `frontend-game/src/config/services.ts` `SERVICES.devToken` defaults to `'dev_internal_token'` which bundles into client JS (visible in DevTools). V0 dev-only token is acceptable for echo-room handshake; V1 MUST replace with per-user JWT obtained via auth-service login flow + cookie storage (HTTP-only, SameSite=Lax). Currently EchoPanel imports + sends this token to game-server EchoRoom.onAuth. Anyone who can reach localhost:2567 can join with the leaked token. | V1 auth-service integration | MED |
| 032 | 2026-05-24 V0 close-out | EchoRoom reconnect uses Colyseus `setSeatReservationTime(30)` (30-second window). Docker container restart often takes >30s → reconnect-token reservation expires → client falls back to fresh `joinOrCreate` (correct UX per EchoPanel `onReconnect` catch handler). When V1 real ZoneRoom ships with persistent character state, reconsider whether 30s window is right or if a longer reservation (e.g. 5 min) matches user expectation. | V1 ZoneRoom design | LOW |
| 031 | 2026-05-24 V0 close-out | TilemapGPULayer documented as N/A for our iso 2:1 dimetric (spec §11.1 amended Session C Phase 1; Phaser docs `CHANGELOG-v4.0.0.md:549` confirm orthographic-only). If we ever scale Continent zone from 256² with standard `Tilemap.createLayer` and need GPU-layer perf, the only path is either (a) reproject world as orthographic (visual style change) or (b) hand-roll a custom GPU tilemap shader (V3+). Not blocking V1; current path handles V0+V1 zone sizes fine. | V3+ continent perf | LOW |
| 030 | 2026-05-24 V0 close-out | AC-FG-14 (HMR) for Phaser scene file edits relies on Vite default behavior: editing a non-React `.ts` file outside the React-fast-refresh boundary triggers a controlled full-page reload (canvas re-created cleanly, no silent freeze). Verified manually 2026-05-24. If V1 work introduces a non-React TS module that should hot-swap (e.g. a config-only file), add `import.meta.hot.invalidate` boundaries explicitly per spec §1 #3 AC-FG-14. | V1 dev experience | LOW |
| 028 | 2026-05-18 continent measurement /review-impl LOW-3 | `tilemap-service measure` always runs the offline 256² placement (~8–11 min) before the live engine→L3→L4 section. Re-running it purely to capture the live numbers (e.g. after the provider-registry model-pricing fix) re-pays that ~11-min cost for nothing. Add a `measure --live-only` flag, or serialise/cache the placed `TilemapView` so the live section can load it. | tilemap measurement ergonomics (Track 2) | LOW |
| 025 | 2026-05-18 tilemap Phase E spec §5.7 | `RoadPlacer` ships raw Dijkstra paths — no §3.4-step-3 path smoothing (3-point detour removal) and a single road kind (no `RoadKind` Highway/DirtPath). V1+30d has no town/mine edges, so every MST edge is zone-backbone — one kind suffices; smoothing is pure visual polish. Build when rendered roads look jagged or a town/mine tier needs a secondary road kind. | Visual polish (Track 2) | LOW |
| 024 | 2026-05-18 tilemap Phase D spec D-Q6 | TMP_007 §5's "curved" passage-path cost — a `search_path` cost that makes a passage corridor bow away from the zone border instead of bisecting the zone — is not built. Phase D drives `search_path` with uniform `cost = 1.0` (the shortest route); the curved cost is pure visual polish with no correctness or determinism role. Build it when rendered passage corridors look too straight / zone-bisecting. | Visual polish (Track 2) | LOW |
| 023 | 2026-05-17 tilemap Phase C human-in-loop review (Call #1) | TMP_006 §3.2 zone-config object **filtering** — `banned_object_categories`, `banned_objects`, `required_objects` — is not built in Phase C. It is a separate object-pool-filtering layer needing its own `ZoneSpec` fields (`banned_objects: BTreeSet<…>`, `required_objects: BTreeMap<…>`, …) and a filter pass over the treasure pool; no TMP_006 §8.5 resolved question makes it V1+30d-mandatory — unlike `inherit_treasure_from` (TMP-TR-Q3), which Phase C *does* build (spec D9). The A–E modificator roadmap (Phase D = ConnectionsPlacer, E = Roads/Rivers) has no slot for authoring-layer features, so this is logged with an explicit target rather than left to drift. **Also reconcile here:** Phase C spec D9 chose REPLACE over TMP-TR-Q3's UNION-with-override for the unkeyed `treasure_tiers` `Vec`; when this keyed filtering layer lands, define the per-key override semantics for `banned_objects` / `required_objects` consistently with TMP-TR-Q3. | TMP_004 authoring task (Track 2) | LOW |
| 021 | 2026-05-17 tilemap Phase A spec D12 / code-review r3 WARN-3 | `From<PlacementError> for crate::Error` bridge not built. Phase A's `place_and_connect_object` returns the module-local `PlacementError` enum (`NoSpace` / `NoSuchZone`); a placer in Phase C+ that `?`-propagates from `Modificator::process` (returns `crate::Result`) needs the conversion. Phase A has no such consumer, so the bridge is deferred to the first placer. Spec D12 records this. | Phase C (first placer) | LOW |
| 020 | 2026-05-17 tilemap Phase A spec D5 | TMP_006 §4.3 connectivity pre-filter not built — `would_seal_a_gap` runs the naive double-flood-fill on every candidate placement. TMP_006 §4.2 blesses that as "cheap enough to run on every candidate"; the §4.3 pre-filter (skip candidates adjacent to ≤1 free region) is a continent-scale perf optimisation. Output stays byte-identical. | Perf — when continent-scale generation profiles slow | LOW |
| 018 | 2026-05-16 tilemap Phase 1 human-in-loop review HIGH-1 | `penrose::assign_zone_tiles` is O(tiles × vertices) — `nearest_vertex` linearly scans the full Penrose vertex field for every grid tile. At Continent scale (256² = 65 536 tiles × ~500-1200 vertices) that is ~10⁷-10⁸ `distance_sq` calls, single-threaded, vs the TMP_002 §7 budget. Structurally the same defect as #016 (fractalize) but larger — the tile loop runs over the whole grid, not one zone — and the capped AMAW Adversary caught only #016. Invisible at the 24²-64² test fixtures. Fix: bucket vertices into a coarse uniform grid and test only neighbour buckets, or a multi-source BFS distance transform — output stays identical. **2026-05-18 continent measurement (finding O-1) confirms the pain: a 256² `place_tilemap` takes ~8–11 min release-build — recommend promoting to active work.** | Perf — **profiling now shows pain (2026-05-18)** | MED |
| 017 | 2026-05-16 tilemap Phase 1 Adversary r1 WARN-3 | `force_directed_converge` computes `ConvergenceResult.converged` + `.iteration_count`, but `place_zones` / `place_tilemap` drop them — `TilemapView` has no carrier, so a cap-hit tilemap is indistinguishable from a cleanly-converged one. PLAN chunk 0 said "add `iteration_count`/`converged` carriers as needed"; nothing in Phase 1 consumes them. Surface via a `GenerationMetadata` field on `TilemapView` when DP progress-streaming (TMP_002 §9 TMP-PLACE-Q3) lands. | Phase 2+ (progress-event streaming) | LOW |
| 016 | 2026-05-16 tilemap Phase 1 Adversary r1 WARN-2 | `fractalize::scatter_and_connect` is O(candidates²) — `nearest_cleared_dist_sq` linearly scans the growing `cleared` mask for every candidate. At Continent scale (256²) that is ~10⁸ ops per zone vs the TMP_002 §7 <500 ms budget; invisible at the 24²-64² test fixtures. Replace the linear nearest-scan with coarse spatial buckets or a multi-source BFS distance transform. **2026-05-18 continent measurement (finding O-1) confirms the pain: a 256² `place_tilemap` takes ~8–11 min release-build — recommend promoting to active work.** | Perf — **profiling now shows pain (2026-05-18)** | MED |
| 015 | 2026-05-16 tilemap Phase 1 Adversary r1 WARN-1 (severity raised by human-in-loop review) | The wall-clock-cap fallback (`force_directed` D5) is a cross-machine layout divergence: a slow machine can trip the wall-clock cap where a fast one converges, so the grid-seed fallback differs across machines for the same seed — the one path that can break the TMP-A4 axiom. Spec-D5-accepted. **A `tracing::warn!` in the `WallClockCap` arm now makes the fallback observable** (added 2026-05-16 review-fix task). The remaining deferred part is the ops-dashboard `tilemap.generation_failed` info-event (TMP-PLACE-Q2), which needs the Phase-2 progress-event channel. | Phase 2+ (progress-event streaming) | MED |
| 014 | 2026-05-16 AMAW-improve BUILD finding | `amaw-guardrail-gate.py` cheap pre-check matches a risky pattern ANYWHERE in the Bash command string — including a heredoc body / commit message. A `git commit` whose message text contains "git push" or "migrat…" false-positive-fires the gate (an `ask`, not a block — so non-fatal, the user just confirms). Refine: match the leading command token, or strip heredoc bodies before the pre-check. | AMAW hook hardening | LOW |
| 013 | 2026-05-16 AMAW-improve spec D3 | `agentic-workflow/AMAW.md` (the installable bundle source of `docs/amaw-workflow.md`) was already ~67 lines diverged from the installed copy before this task; D3 updated the operative installed docs (`amaw-workflow.md` + `amaw.md`×2) but not `AMAW.md`. Reconciling the bundle source is its own hygiene task. | agentic-workflow bundle sync | LOW |
| 010 | 2026-05-15 tilemap Phase 0b spec §2 | Anthropic **request-side** tool support — `anthropicAdapter.Stream` sends no `tools`/`tool_choice` (Anthropic-shaped tools differ from the OpenAI-shaped contract field). Phase 0b landed only the Anthropic *streamer parse-side* `tool_use`→`tool_call` mapping. Fix when an Anthropic provider needs tool-use: translate OpenAI-shaped `tools`/`tool_choice` → Anthropic `input_schema`/`tool_choice` in `anthropicAdapter.Stream`, and set `anthropicAdapter.SupportsTools()` → true. | When Anthropic tool-use needed | MED |
| 009 | 2026-05-15 tilemap Phase 0b code review (Scope Guard AC-7 PARTIAL) | D8 guard (`LLM_TOOLS_NOT_SUPPORTED_FOR_PROVIDER`) full handler-level reject path is untested — the capability component (`SupportsTools()` + `hasToolDefinitions()`) is unit-tested, but the `doLlmStream` reject path needs the live-DB + fake-adapter integration harness. Joins the pre-existing `D-PHASE5A-STREAM-INTEGRATION-TESTS` deferral. | provider-registry stream integration suite | LOW |
| ~~008~~ | 2026-05-15 amaw-precommit-no-side-effect Adversary r1 WARN-1 | ✅ **RESOLVED 2026-06-04.** `load_state()` (`scripts/workflow-gate.py`) now wraps the parse in `try/except (json.JSONDecodeError, ValueError, OSError)`: a corrupt / empty / unreadable `.workflow-state.json` prints a loud `WARN` to stderr, resets to a clean `INITIAL_STATE` (atomic save), and returns it — so no gate command (incl. the pre-commit hook) ever dies on a corrupt state file. Verified: ran `status` from a temp dir holding `{ this is not json` → warned + reset to valid initial state, no traceback. | DONE | RESOLVED |
| 007 | 2026-05-15 amaw-task-slug-validation Adversary r2 WARN-1 | `cmd_amaw_enable` normalizes the slug only on the `if args:` write path; a no-arg or already-enabled invocation can leave a raw `state["task"]` persisted in the state file. Mitigated: `cmd_complete` defensively re-normalizes so the OUTPUT tag list is always comma-safe — only the state-file display value can be stale. Fix: normalize unconditionally at the top of `cmd_amaw_enable`, or normalize on every state read. | L4 hardening | LOW |
| 001 | 2026-05-15 amaw-l3-deepen /review-impl #5 | `task_slug` not validated — comma in slug splits into multiple tags downstream (`",".join(tags)` then mcp-query.py splits back on comma). No injection risk (subprocess list form). Fix: normalize comma → dash in `cmd_amaw_enable`, or argparse-validate slug pattern `^[a-z0-9][a-z0-9-]*[a-z0-9]$`. | L4 hardening | LOW |
| 002 | 2026-05-15 amaw-l3-deepen /review-impl #6 | `"REJECTED" in evidence.upper()` substring match in `cmd_complete` bridge logic — false positives on "NOT REJECTED" or "rejected the rejected pattern". Fix: word-boundary regex `\bREJECTED\b` or check evidence prefix `"REJECTED:"` / `"status: REJECTED"`. | L4 hardening | LOW |
| 004 | 2026-05-15 amaw-l3-deepen /review-impl #8 | `cmd_pre_commit` early-exits silent when no STATE_FILE, but `&&`-chained `cmd_amaw_pre_commit` then calls `load_state()` which CREATES `.workflow-state.json` from INITIAL_STATE. Side effect: every commit by an agent NOT in any tracked task creates a stale state file, surfacing confusing `[ ]` markers on next `status`. Fix: in `cmd_amaw_pre_commit`, also early-exit if STATE_FILE absent before `load_state()`. | L4 hardening | LOW |

### Lore-enrichment workstream deferrals (`LE-` prefix; merged 2026-06-01)
> Carried under `LE-` to avoid the numeric-ID collision with main's Phase-B/geo track (both branches independently allocated 044–055). Lore-enrichment's RESOLVED items (its old 044 KNOWLEDGE_SERVICE_URL wiring, 050 injection hardening, 052-for-P1 token metering) are NOT relisted — they're in git history + `docs/03_planning/lore-enrichment/SESSION_HANDOFF.md`.

| ID | Origin | Description | Target | Severity |
|---|---|---|---|---|
| ~~LE-056~~ | 2026-05-30 lore-enrichment C15 WARN-3 | ✅ **RESOLVED 2026-06-01 (C2, post-main-merge re-decision).** Judge-diversity floor: `app/eval/judge_usefulness.py` `acceptable` now requires ≥2 judges from **≥2 DISTINCT families** (`JudgeSpec.family`, default=label, normalized strip+casefold so case/whitespace can't fake diversity) **AND** κ ≥ a configurable floor (`settings.judge_kappa_floor` / `LORE_ENRICHMENT_JUDGE_KAPPA_FLOOR`, default 0.0 = below-chance disqualifies; non-finite κ treated as uncomputable). Two qwen near-clones no longer self-certify even at κ=1.0. Wired through `runner.py` + `gate.py` message; the C15 live-smoke now tags `family`. /review-impl fixed family-normalization + NaN-κ guard. 549 pytest pass. **Residuals (accepted):** (a) κ=None (disjoint judge coverage → no common item) does NOT disqualify — gate clears on family-count alone with agreement unmeasured (deliberate PO choice; tighten later if flaky-coverage runs slip through); (b) `JudgeUsefulnessResult.reasons` is not surfaced to the scorecard/gate message (observability only). | DONE | RESOLVED |
| ~~LE-051~~ | 2026-05-30 lore-enrichment C14-FIX (WARN-1) | ✅ **RESOLVED 2026-06-04 (verified token-safe; cursor = won't-fix micro-opt).** Re-reviewed `runner.run_job`: a gap in `skip_gap_refs` is skipped at runner.py:198 **BEFORE** the cost-charge AND the LLM `run_gap` (`outcome.resumed_skipped.append(...); continue`) — so a resumed job spends **neither budget nor tokens** on done gaps (the real concern). The resume worker passes ALL already-persisted `gap_ref`s, so done gaps never reach the reload/`deduped` path. The only thing NOT done is persisting a "remainder cursor" so the runner doesn't iterate-and-skip the done prefix — but that skip is pure in-memory iteration (no I/O, no LLM), negligible even for a 1000-gap job. **Won't-fix:** the micro-opt changes no correctness or cost and adds state-tracking complexity. Token-safety + convergence are proven (test pins partial-resume). | DONE (token-safe; cursor won't-fix) | RESOLVED |
| ~~LE-058~~ | 2026-05-31 lore-enrichment RAID c17 NIT-3 | ✅ **RESOLVED 2026-06-01.** Broadened `ANACHRONISM_MARKERS` (`app/verify/canon_verify.py`) with ~23 unambiguous out-of-era tokens (modern tech 电报/电影/导弹/机器人, science 抗生素/显微镜, foreign faiths 伊斯兰/教堂/喇嘛, 股票, 隋朝). /review-impl pruned 13 collidable candidates (总统/民国/共和国/议会/宪法/警察/法院/银行/公里/千米/公斤/摄氏/元朝) that a bare substring match would FALSE-POSITIVE on plausible 封神 prose (总统六师, 安民国家, 共和 regency) → wrongful auto-reject; guard test pins they don't fire. Classical homographs 民主/选举 stay excluded. Still a finite denylist (richer era-lexicon / model-assisted check remains a future option if domains widen) but materially broader + still false-positive-safe. | DONE (denylist broadened; lexicon upgrade still optional) | RESOLVED |
| LE-059 | 2026-06-01 lore-enrichment C1 token metering | **P2/P3 post-call token reconcile + real embed usage.** ✅ **(a) DONE 2026-06-01:** `assembly.build_live_runner` now wires the `UsageMeter` for ALL techniques (was P1-only) — P2 fabrication / P3 re-cook pre-charge in tokens (3000/4500) and use the same metered seams, so a multi-pass gap's REAL token spend is captured (`UsageMeter` accumulates every call) + reconciled per gap, like P1; runner reconcile is technique-agnostic (test pins a fabrication-magnitude gap reconciling to real tokens). **(b) ✅ DONE 2026-06-04:** provider-registry `parseOpenAIEmbeddingResponse` now surfaces the upstream `usage.prompt_tokens` on `EmbedResult.PromptTokens` (json `prompt_tokens`, omitempty → wire-compatible; +2 Go tests); the `/internal/embed` handler returns `result` directly so it flows automatically. lore-enrichment `EmbedResult.prompt_tokens` + the embed seam meters on the REAL count when present, **falling back to the char-estimate only when the provider reports none** (Ollama / a too-old provider-registry) → no regression, never metered as 0 (+2 py tests: real-count vs fallback). **ADDITIVE + backward-compatible** (Go/Python both ignore unknown fields). **Op:** takes effect live only after a provider-registry rebuild (shared daemon — clobber-prone per LE-061; until then lore-enrichment gracefully estimates). **✅ LIVE-VERIFIED 2026-06-04 (D-LE059B):** rebuilt+recreated provider-registry; `/internal/embed` (test-user bge-m3) → 200, dim 1024, embeddings present; the parse reads LM Studio's `usage` block but **LM Studio reports `prompt_tokens: 0` for embeddings** (confirmed by hitting `:1234/v1/embeddings` directly) → `omitempty` drops it → lore-enrichment falls back to the char-estimate. So the full chain works as designed; the REAL-count path is exercised only by providers that report non-zero embed usage (e.g. real OpenAI) — the Go unit test pins that parse. No regression on the local stack. /review-impl added a `>0` guard so a negative count can never reduce metered spend. **(c) ✅ CLOSED 2026-06-04 (won't-fix until parallelized).** Re-reviewed `runner.run_job`: gaps are processed in a strictly SEQUENTIAL `for gap in gaps` loop, and `_reconcile_gap(before_tokens)` snapshots `meter.total_tokens` immediately after the pre-charge and diffs after the gap runs — correct by construction while runs are sequential (the docstring pins the "sequential-run invariant"). There is nothing to change today; parallelizing gaps is not planned. If/when gaps are ever parallelized, the per-gap delta must move to a per-gap meter scope — but adding that machinery now would be speculative complexity. Closed as won't-fix-until-needed. | DONE (won't-fix until gaps parallelized) | RESOLVED |
| ~~LE-060~~ | 2026-06-01 lore-enrichment C3 auto-reject (F-C12-1 + /review-impl) | ✅ **RESOLVED 2026-06-01.** `app/verify/canon_lookup.py extract_canon_terms` now segments CJK canon with **jieba POS-tagging** (`jieba>=0.42`, requirements.txt; lazily imported so its dict loads only when a canon term is actually extracted), keeping **proper-noun tags only** (ns/nr/nt/nz…) and dropping generic nouns/verbs/adjectives — consistent with the Latin Capitalized rule, so the C3 false-positive-safety holds (a benign common-word + negation can't fire a contradiction). Test pins proper nouns kept (蓬萊/昆仑/仙山) + generics dropped (地方/仙人/修道). /review-impl pinned the graceful-degrade contract (jieba absent → extract raises → verifier `verify_degraded`, no false-green). **Residuals (accepted, LOW):** (a) tag-set couples to jieba's version — a tag change drops a term → under-fires (safe), guarded by the LE-060 test; (b) Latin NER not added (Capitalized heuristic retained); (c) prod read-only-rootfs would force a jieba in-memory dict rebuild per start. ~~**⚠️ Images need rebuild to ship jieba**~~ **✅ images rebuilt + live-proven 2026-06-02** (jieba loads in-container; full e2e enrich ran through canon-verify with no `verify_degraded`). | DONE (jieba; NER + read-only-fs are LOW residuals) | RESOLVED |
| ~~LE-061~~ | 2026-06-02 live-e2e root-cause | ✅ **RESOLVED 2026-06-02** — freshness guard hardened: added a provider-registry `/internal/embed` route-presence probe (catches a fully-stale binary missing the route) AND a **UNSTAMPED** warning when a first-party image has no `git_sha` label (surfaces the degraded tier-1-only detection that let this slip); `scripts/check_stack_freshness.py` + `drift_note()` + 2 new unit tests. provider-registry itself was rebuilt + SHA-stamped. (Behavioural `/v1` staleness is now caught by tier-2 SHA-drift once stamped.) ORIGINAL: **provider-registry was a STALE, unstamped image** (`org.loreweave.git_sha` label empty → never built via `build-stack.sh`); its binary predated the embed `/v1`-normalization (`internal/provider/embed.go` `TrimSuffix(base,"/v1")`). A `user_model` whose lm_studio credential `endpoint_base_url` ends in `/v1` → the stale binary built `…/v1/v1/embeddings` → LM Studio returns **200 with `{error}` and empty `data`** → provider-registry maps it to 502 `EMBED_PROVIDER_FAILED "no embedding data in response"` — for ALL embed consumers (lore-enrichment retrieval, knowledge-service, etc.) on a `/v1`-suffixed credential. **✅ FIXED 2026-06-02 by rebuilding+recreating provider-registry** (source already strips `/v1`; test-user embed → dim=1024 after). **RESIDUAL (the real deferred work):** the freshness guard (`scripts/check_stack_freshness.py`) did NOT catch this — provider-registry is NOT in the route-probe list AND its git_sha label was empty so tier-2 SHA-drift couldn't fire either. Add a provider-registry probe (e.g. a representative `/internal/embed` reachability check) + ensure every service image is SHA-stamped at build. | DONE (guard hardened; pr rebuilt) | RESOLVED |
| ~~LE-062~~ | 2026-06-02 live-e2e ops | ✅ **RESOLVED 2026-06-02** — replaced the inherited HTTP `/health` probe (meaningless for a headless consumer) with a **background-heartbeat liveness**: `app/worker/heartbeat.py` (a bg asyncio task touches a heartbeat file every 5s — stays fresh during long generations since the event loop is alive, goes stale only on a real hang/crash) + `app/worker/healthcheck.py` (`python -m app.worker.healthcheck` exits 0/1) + compose `healthcheck` override on the worker block + 7 unit tests. Wired in `app/worker/__main__.py` (both the active and consumer-disabled branches). ORIGINAL: **`lore-enrichment-worker` healthcheck is a permanent false-negative.** The compose `lore-enrichment-worker` block inherits the service's HTTP healthcheck (`urlopen http://127.0.0.1:8093/health`) but the worker is a headless Redis-stream consumer (`python -m app.worker`) with NO HTTP server → it shows `unhealthy` forever despite functioning correctly (consumer starts + drains the resume stream — live-proven). Fix: replace with a consumer-liveness check (heartbeat key/file, or `pgrep -f app.worker`) or drop the healthcheck for the worker block. Non-blocking (does not stop consumption), purely a misleading status. | DONE (heartbeat liveness) | RESOLVED |
| ~~LE-063~~ | 2026-06-02 live-e2e (promote chain) | ✅ **RESOLVED 2026-06-02** — `ProposalsRepo.approve()` (new) walks `proposed → author_reviewing → approved` in one call (each hop DAG- + DB-trigger-legal, H0 unchanged), and the `approve` endpoint now calls it; this restores the documented "proposed/reviewing → approved" contract so a freshly-created proposal can be approved → promoted via the API alone (no DB poke). +1 unit test (`test_approve_from_proposed_walks_through_reviewing`). A distinct FE-facing "begin-review" toggle remains optional. ORIGINAL: **Proposal lifecycle API cannot promote a fresh `proposed` proposal.** The DAG (`app/services/review.py LEGAL_TRANSITIONS`) is `proposed → author_reviewing → approved → promoted`, but the API (`app/api/proposals.py`) exposes only `approve` (sets `approved` directly), `reject`, `edit` (no status change), `promote`, `retract`, `write-back` — there is **no endpoint for `proposed → author_reviewing`**. So `approve` from the initial `proposed` state 409s (`illegal transition proposed → approved`), making `promote` unreachable through the API alone. The 2026-06-02 e2e worked around it with a one-row DB `UPDATE … author_reviewing` (the H0 trigger permits it) before calling the real `approve`+`promote` API. Fix: add a `begin-review` endpoint (`proposed → author_reviewing`), OR have `approve` walk `proposed → author_reviewing → approved` in one step. The FE review flow presumably needs this too. | DONE (approve auto-walks) | RESOLVED |
| ~~LE-064~~ | 2026-06-03 enrichment closure (S1) | ✅ **RESOLVED 2026-06-03 (async).** Per-row "enrich →" in the gaps table. Rather than the synchronous `POST /jobs` (timeout risk on a 35B gen), added an optional `targets` field to the async `auto-enrich` endpoint (`app/api/gaps.py`): when set, it skips detection + top-N and enqueues exactly those gaps via the same worker path. FE: `GapsPanel` per-row Enrich button (disabled until models picked) → `useGaps.autoEnrich({…, targets:[gapToTarget(g)]})`. openapi.yaml updated. Tests: +1 BE (`test_auto_enrich_with_targets_skips_detection_and_enqueues`), +1 FE (per-row enrich). | DONE | RESOLVED |
| ~~LE-065~~ | 2026-06-03 enrichment closure (S2) | ✅ **RESOLVED 2026-06-03.** Tab count badges on `EnrichmentView` (Proposals/Sources/Jobs from the list hooks — shared react-query cache, no double-fetch; Gaps from a new `gapCount` on `EnrichmentContext` that `GapsPanel` sets on Detect, so no badge until detected). +1 FE test. | DONE | RESOLVED |
| ~~LE-066~~ | 2026-06-03 enrichment closure (S3) | ✅ **RESOLVED 2026-06-03 (expander; highlight N/A).** `DimensionList` now collapses dimensions beyond 3 behind a show-more/less toggle. The "changed-span highlight" is **not applicable to generative recook** — recook produces entirely new prose from abstracted facts, so there is no source-span diff to highlight; the transformation is instead conveyed by the new ② attribution (LE-067) + the P3 recook badge. +2 FE tests. | DONE (highlight N/A — documented) | RESOLVED |
| ~~LE-067~~ | 2026-06-03 enrichment closure (S4) | ✅ **RESOLVED 2026-06-03 (FE-only).** `model_ref` IS in `provenance_json.retrieval.model_ref` (a user_model_id) — `ProvenancePanel` now resolves it to the friendly alias via `providerApi.listUserModels` (shared 'chat' cache) and shows a Model field; plus a "② re-cooked from abstracted facts" attribution row for recook proposals. No backend change needed. +3 FE tests. | DONE | RESOLVED |
| ~~D-COMPOSE-S1-LIVE-SMOKE~~ | 2026-06-04 Compose slice 1-BE | ✅ **RESOLVED 2026-06-04.** Rebuilt this-worktree's lore-enrichment service+worker; full draft→202→worker→quarantined `compose_draft` proposal→promote-minted-anchor (facts_promoted=3, H0 retained)→retract→demo restored. **Caught a real cross-service bug** (glossary skips unknown kind_code → generic/faction promote 502; see D-GLOSSARY-KIND-ALIASES). `scripts/smoke_compose_draft_e2e.py`. | DONE | RESOLVED |
| ~~D-COMPOSE-EXISTING-PICKER~~ | 2026-06-04 Compose slice 1-FE /review-impl #1 | ✅ **RESOLVED 2026-06-04.** `ComposeTarget` existing-mode now autocompletes over the book's entities (native `<datalist>` via new `useBookEntities`→`glossaryApi.listEntityNames`; free-text fallback; conservative kind-prefill only for modeled kinds). +4 FE tests. | DONE | RESOLVED |
| ~~D-COMPOSE-S1-EMBED-REF~~ | 2026-06-04 Compose slice 1-BE /review-impl #3 | ✅ **RESOLVED 2026-06-04.** `embedding_model_ref` now optional for `draft` (mode D does no retrieval/embed; it was vestigial — `build_live_runner` never read it, the embed seam resolves from the ctx). `ComposeBody` optional; gap still 400s without it; worker/`build_live_runner` accept None. FE: embed picker no longer gates the draft Run, omitted when unset. +3 tests. | DONE | RESOLVED |
| ~~D-COMPOSE-S1-DRAFT-CAP~~ | 2026-06-04 Compose slice 1-BE /review-impl #4 | ✅ **RESOLVED 2026-06-04.** `draft_text` capped at ~50 KB → 413; openapi `maxLength: 50000`. +1 test. | DONE | RESOLVED |
| ~~D-COMPOSE-KIND-HARDCODE-INTERIM~~ | 2026-06-04 Compose live-smoke | ✅ **RESOLVED 2026-06-04 (epic E2).** Deleted `_ENRICH_TO_GLOSSARY_KIND`, `_DEFAULT_GLOSSARY_KIND`, `_glossary_kind_code`, the reactive `_write_glossary_anchor`/`UnplaceableEntityError` fallback from `app/services/writeback.py`; promote sends `proposal.entity_kind` RAW. The translation now lives in glossary as SEEDED `entity_kind_aliases` rows (faction→organization, generic→terminology). Live-proven: generic promote mints a terminology anchor via the alias (no hardcode). | DONE | RESOLVED |
| ~~D-KNOWLEDGE-ENRICHED-WRITEBACK-IDEMPOTENCY~~ | 2026-06-04 E2 live-smoke | ✅ **RESOLVED 2026-06-04.** (Investigation corrected the original wording: `internal_enrichment.py` was ALREADY a `MERGE`, never a raw CREATE.) The real defect: the anchor MERGEd on `(user_id, glossary_entity_id)` while `ON CREATE SET e.id = canon_id` (a name+kind hash) — so a NEW glossary_entity_id mapping to an existing `canon_id` (glossary entity deleted+recreated same name, or rename) MISSED the node and tripped `UNIQUE(:Entity.id)` → 500. This also mismatched the canonical glossary→KG sync, which MERGEs `(:Entity {id})`. **Fix:** the write-back now MERGEs on the canonical `id` (matching the sync), frees a stale `glossary_entity_id` from any other node first (null-before-claim, mirroring `entities.py`), and ON MATCH only coalesces the anchor (H0 canon untouched). Facts re-key to the anchor by `id`. **Live-proven** (`scripts/smoke_writeback_idempotency.py`, rebuilt knowledge-service): two write-backs with DIFFERENT glossary_entity_ids for the same canonical entity → both 200 + re-promote 200; cypher confirms a SINGLE `:Entity` node (no duplicate id); test nodes cleaned. | DONE | RESOLVED |
| ~~D-GLOSSARY-KIND-ALIASES~~ | 2026-06-04 Compose live-smoke (root cause) | ✅ **EPIC CLOSED 2026-06-04 (E1 + E2 + E3 all DONE + live-proven).** Root cause: glossary `extract-entities` silently SKIPPED an unknown `kind_code`. **E1 (glossary BE):** alias table + `unknown` kind + `source_kind_code` + alias-aware resolution-never-skip + 4 review endpoints w/ attr re-key. **E2 (lore-enrichment):** aliases seeded + hardcode deleted. **E3 (glossary FE):** unknown-kind review GUI (queue + merge-as-alias / create-kind / reassign) — `/review-impl` fixed 5 + 1 smoke-found HIGH (name-loss on reassign → `createKind` seeds a `name` attr + `rekeyEntityToKind` `name`↔`term` mapping). Live-proven via `smoke_alias_selfcode_e2e.py`. | DONE | RESOLVED |
| ~~D-GLOSSARY-UNKNOWN-BLAST-RADIUS~~ | 2026-06-04 E1 /review-impl #2/#3 | ✅ **RESOLVED 2026-06-04 (escape hatch added).** `extract-entities` now takes an optional `park_unknown_kinds` bool (`bulkUpsertRequest`): nil/true (default, backward-compatible) → an unresolvable kind PARKS under `unknown` for triage (the "never drop" design); **false → SKIP** it, so a caller that emits noisy/experimental kinds (e.g. the knowledge-service extraction pipeline) can opt out of flooding the review queue. Gated test `TestKindResolution_ParkUnknownOptOutSkips` (live-proven vs glossary_test). **Residual (accepted, LOW):** same-name + different-bogus-kind entities still collapse into one `unknown` row (findEntityByNameOrAlias keys on the single unknown kind; first source_kind_code kept) — rare; revisit only if observed. | DONE (flag added; same-name collapse accepted) | RESOLVED |
| ~~D-GLOSSARY-KIND-ALIAS-GOTEST~~ | 2026-06-04 E1 verification | ✅ **RESOLVED 2026-06-04.** Added `internal/api/kind_aliases_test.go` (gated on GLOSSARY_TEST_DB_URL): 4 tests — bogus kind → parked under `unknown` + `source_kind_code`; seeded alias `generic→terminology` resolves; merge reassigns + preserves the name across the `name`→`term` display mapping (#6); self-code merge skips the redundant alias but reassigns + dead-alias still 409 (#2-glossary); deleteKind blocks active but purges soft-deleted (#3). All PASS against the `glossary_test` DB; idempotent via a per-test fixture reset. (A `NOT EXISTS` guard on `rekeyEntityToKind` for non-triage reassign remains a tiny latent item, folded into normal review.) | DONE | RESOLVED |
| ~~D-GLOSSARY-DELETEKIND-SOFTCOUNT~~ | 2026-06-04 E3 live-smoke | ✅ **RESOLVED 2026-06-04.** Investigation found the naive "add `deleted_at IS NULL`" fix would turn the clean 409 into an FK-violation 500 (the `glossary_entities.kind_id` FK has NO `ON DELETE CASCADE`, so soft-deleted rows still physically block the kind delete). **Proper fix:** `deleteKind` now guards on ACTIVE entities only (409 if any) and, inside a tx, PURGES the kind's soft-deleted entities (their children cascade) before deleting the kind (whose attr_defs + aliases cascade) — so an effectively-empty kind is deletable. Covered by the gated Go test. | DONE | RESOLVED |
| ~~D-GLOSSARY-LISTENTITIES-TEST-STATE~~ | 2026-06-04 kind-alias gotest run | ✅ **RESOLVED 2026-06-04 (both now PASS live vs glossary_test).** Two distinct roots, both fixed: **(1) NameFilter** — the book literal `00000000-0000-0000-0001-000000c12c` had a 10-hex final group (UUIDs need 12) → `parsePathUUID` 400 "invalid book_id" before any pagination ran; replaced with a well-formed UUID. **(2) CursorWalk** "Arthur short_desc got nil" — the test seeded short_description via an EAV value under a per-test `short_description` attr_def, but on the SHARED DB multiple such attr_defs accrue for `character` so the handler's `... code='short_description' LIMIT 1` subquery resolved a DIFFERENT attr_def → nil; fixed by seeding the AUTHORED COLUMN `glossary_entities.short_description` (which the handler reads first via `COALESCE(NULLIF(e.short_description,''), short_av)` — immune to shared attr_def drift). Verified: the full `internal/api` suite passes against glossary_test (incl. both formerly-failing tests). | DONE | RESOLVED |
| ~~D-COMPOSE-S2-LIVE-SMOKE-GEN~~ | 2026-06-04 Compose slice 2 | ✅ **RESOLVED 2026-06-04 — full mode-C happy-path live-proven.** `scripts/smoke_compose_context_e2e.py` (LM-Studio bge-m3 + Qwen3-35B loaded): `copyrighted→403` ✓ · `public_domain context→202` (corpus ingested synchronously) ✓ · job ran → **completed** (worker grounded on the ingested corpus + generated) ✓ · **quarantined proposal** for the target (origin=enrichment, conf=0.3, glossary untouched — new target H0-clean) ✓; test proposal rejected for cleanup. **Root cause of the initial `EMBED_PROVIDER_FAILED`:** the running provider-registry was the STALE/clobbered image (the recurring F-LIVE issue — another branch rebuilds `infra-provider-registry-service` from a pre-embed-fix state); the source was already correct (`embed.go` strips trailing `/v1`). Fixed by `docker compose build --no-cache provider-registry-service` + `--force-recreate`. | DONE | RESOLVED |
| ~~D-COMPOSE-CONTEXT-CORPUS-SCOPE~~ | 2026-06-04 Compose slice 2 /review-impl #1 | ✅ **RESOLVED 2026-06-04 (accumulation half fixed; bleed by-design).** The "accumulate forever / no expiry" problem is solved: `_ingest_context` now tags every compose paste (mode C) + file ingest (mode F) corpus `provenance_json.compose_ephemeral=true`, and the new worker reaper (`app/worker/reaper.py` `sweep_ephemeral_corpora` → `SourceCorpusStore.reap_ephemeral_corpora`) garbage-collects them by TTL (`context_corpus_ttl_s`, default 30d). The curated `/sources` library is untagged → never reaped. Deleting a corpus is safe (chunks/grounding-refs cascade; a promoted proposal's `cultural_grounding_ref_id` is ON DELETE SET NULL + its `source_refs_json` is a value snapshot). The cross-run PROJECT-scoped *bleed* is left as-is **by design** (similarity-ranking keeps off-target context low; a re-paste re-grounds idempotently; live-proven mode C/F) — documented in `_ingest_context`. +unit tests (reap by TTL + ttl<=0 no-op). | DONE | RESOLVED |
| ~~D-COMPOSE-S4-LIVE-SMOKE~~ | 2026-06-04 Compose slice 4 | ✅ **RESOLVED 2026-06-04** (`scripts/smoke_compose_intent_e2e.py`, rebuilt lore-enrichment + LM-Studio): `resolve-intent` → the LLM proposed a NEW `character` 玄虛真人 + 4 Fengshen-grounded dimensions + a book-aware rationale (no job) → confirm → `compose` input_source=intent → job completed → quarantined fabrication proposal (conf=0.3) → cleaned up. The resolver is genuinely book-aware (read the book profile). | DONE | RESOLVED |
| ~~D-COMPOSE-S3-LIVE-SMOKE~~ | 2026-06-04 Compose slice 3 | ✅ **RESOLVED 2026-06-04** (`scripts/smoke_compose_files_e2e.py`, rebuilt lore-enrichment w/ Tesseract 5.5.0 + chi_sim/chi_tra/eng): uploaded a .txt (87 chars) + .docx (29) + an image-only "scanned" .pdf → the scan was **OCR'd live (ocr_used=True, 46 chars)** → POST /compose files → job completed → grounded quarantined proposal (conf=0.3) → cleaned up. Full upload→MinIO→extract(+OCR)→poll→files-branch→ingest→worker→proposal chain proven end-to-end. | DONE | RESOLVED |
| ~~D-COMPOSE-S3-UPLOAD-REAPER~~ | 2026-06-04 Compose slice 3 /review-impl #4 | ✅ **RESOLVED 2026-06-04.** New worker reaper (`app/worker/reaper.py`, wired into `app/worker/__main__.py` alongside the heartbeat, `reaper_enabled`/`reaper_interval_s` config) runs a periodic best-effort sweep: (1) `sweep_stale_uploads` flips `processing` rows older than `upload_stale_processing_s` (default 30m) → `failed` (the files branch stops 409-ing forever); (2) `sweep_orphan_objects` lists the MinIO bucket and deletes objects whose `enrichment_upload` row never landed, but ONLY past `upload_orphan_grace_s` (default 1h) so an in-flight upload is never raced; (3) `sweep_ephemeral_corpora` (see D-COMPOSE-CONTEXT-CORPUS-SCOPE). Each sweep is independent (`reap_once` best-effort) so one failure never aborts the others or crashes the worker. +`minio_client.list_objects` + 9 unit tests (stale/orphan-age/no-op/best-effort). | DONE | RESOLVED |
| ~~D-COMPOSE-S3-OCR-LANG~~ | 2026-06-04 Compose slice 3 /review-impl #5 | ✅ **RESOLVED 2026-06-04.** `extract.py` `tesseract_lang_for(profile.language)` maps a book's language → a Tesseract lang string, restricted to the packs SHIPPED in the image (`_INSTALLED_OCR_LANGS` = chi_sim/chi_tra/eng/jpn/vie — the platform's en/vi/ja/zh-TW locales; Dockerfile updated to install jpn+vie) so a book whose pack isn't installed falls back to the CJK+English superset rather than requesting a missing pack (which fails OCR entirely). `eng` is always folded in (embedded Latin); we do NOT add every pack (slows Tesseract). `uploads._extract_and_store` resolves the lang from the book profile for PDFs only (txt/docx/epub need none) and threads it `extract_text → _extract_pdf → _ocr_pdf_pages`. +7 unit tests (zh/zh-TW/zh-Hans/en/ja/vi/auto/unknown + threading). | DONE | RESOLVED |
| ~~LE-068~~ | 2026-06-03 enrichment closure (S7) | ✅ **RESOLVED 2026-06-04 — real browser-layer e2e of the C3 GUI run + green.** The earlier "blockers" were partly wrong: a FULL Playwright harness already exists (`frontend/tests/e2e/` — page objects, helpers, specs, `LoginPage`, `TEST_USER`), and `npx playwright test` uses node-Playwright's OWN bundled chromium (installed) — the "shared Chrome lock" was the **MCP** browser, not this. The only real gap was serving THIS worktree's FE; solved by running its **vite dev on :5273** (proxies `/v1`→the live gateway :3123). Added `pages/EnrichmentTab.ts` + `specs/enrichment-profile.spec.ts`: **2 tests PASS** (10.8s) against the live stack — (1) login→gateway→FE→Enrichment→**Profile tab loads the seeded Fengshen profile** (GET `/books/{id}/profile`, owner-checked via book-service) with suggest disabled + save visible; (2) gaps **detect shows NO "extract first" notice** on the extracted demo book (C2 signal wired correctly). Run: `cd frontend; vite dev --port 5273; PLAYWRIGHT_BASE_URL=http://localhost:5273 npx playwright test enrichment-profile`. (Promote / suggest-LLM / chapter-ground flows can be added to the same spec file later — the harness + pattern are now in place.) | DONE | RESOLVED |

> **✅ RESOLVED 2026-06-03 (was out-of-scope, fixed on request):** 9 stale failures in `knowledge/BuildGraphDialog.test.tsx`. Root cause was NOT dep-drift — commit `0bf049cd` switched the LLM `<select>` to send `user_model_id` (`value={m.user_model_id}`) but didn't update these tests, which still drove it with the old `provider_model_name` (`'gpt-5'`, not a valid option) → the select stayed empty → the estimate query never enabled → mocks never called. They went unnoticed because this worktree never ran FE tests (no `node_modules`). **Fixed (test-only):** drive the select with `'m1'` (the `user_model_id`) + update the `llm_model` assertions. Full FE suite now 100 files / 813 tests green. Dossier S5.

## Recently cleared (within 5 sessions)

| ID | Cleared | How |
|---|---|---|
| 046 | 2026-05-31 session 100 moisture-transport model | **Fixed.** Rewrote `climate.rs::moisture_field` from **averaging** upwind neighbours to **MAX best-path** downwind-directed multi-source transport (`moisture[i] = max over upwind nbrs of moisture[nb] − land_leak − OROGRAPHIC·climb`; windward-edge = off-map upwind ocean). MAX lets moisture reach inland along the wettest route from any upwind sea (multi-directional within the upwind cone) while staying wind-aware (offshore coasts dry, range rain-shadows persist). Result (seed-7 mega): interiors greener, C-group nearly doubled in the full-gradient case (equatorial spread=1: 2.2→3.9 %, `Plain` 55→98), Desert preserved 30.7 % at spread=0; bonus — maritime cooling raised Tundra/Polar (126→375 / 5.7→11 %). Tests: `moisture_takes_wettest_upwind_path` (MAX-vs-AVG discriminator), `rain_shadow_follows_the_wind` preserved. Full lib 399 green. Residual (abundant *literal* Temperate plains) split to optional **#047 (8-zone mapping)**. Commit: (this session). |
| 045 | 2026-05-31 session 100 Köppen v2 seasonality | **Fixed.** Replaced the linear insolation `lerp(28,−15,lat_dist)` with a **cosine** curve (`insolation_temp`: warms mid-lat ~6.5→15 °C at 45°) and rewrote `seasonal_amp` to be **continentality-gated** (`AMP_EQ + (AMP_MARITIME=4 + AMP_CONT_GAIN=24·cont)·lat_dist`) so maritime coasts stay low-amplitude at all latitudes. Result (seed-7 mega): Desert preserved 33.5 % at spread=0 (Köppen win intact); **Tundra opened 0→126** + Polar/Boreal gradient at spread=1; the temperate C-band is now reachable (`Plain` 0→55 with Equatorial orientation). Tests: `insolation_warms_midlatitudes`, `maritime_stays_low_amplitude`, `v2_seasonality_opens_temperate_and_polar` (drives the exact build composition). Full lib 398 green. The *residual* — Temperate stays only 1–2 % because it's a wet-mild maritime climate the single-wind moisture march under-produces — is split out as new **#046 (moisture-transport model)**, the chosen next step. Commit: (this session). |
| 038 | 2026-05-24 V1 tilemap-viewer rescope (same session it was created) | HoMM3-bundle Flux1-dev placeholders replaced with first-party algorithm-generated foundation tiles (`frontend-game/scripts/gen-foundation-tiles.py`). No third-party model output baked in; ship-anywhere license. Spec rescope: `docs/specs/2026-05-24-v1-tilemap-viewer-rescope.md`. |
| 024 | 2026-05-24 v2.1e ship (geo-generator-amaw branch) | W9 shipped as amplitude-gated RGB shading (zone relief auto-scales intensity 0→1 across Plains→Mountains; ±15% RGB modulation). Hypothesis "naive RGB regressed because eval framework had a problem" CONFIRMED: under v4.3 ecotone-aware eval, naive RGB amplitude-gated produces only −0.24 mean (neutral, 0 hard regressions) vs the v2.1b attempt's hard regression. Visual benefit currently limited (visible only on Ice zones where white amplifies modulation) but ships as foundation for future 3D map rendering. HSL approach not needed. True slope-aware hillshade (spec option B) still deferred to v3+. |
| 027 | 2026-05-22 tilemap test hardening | Added `a_bridge_mid_river_resets_the_periodic_ford_counter` (river_placer.rs): a 30×3 straight river with a road at x=6; asserts the bridge resets the shared `since_crossing` counter, so the periodic ford lands at x=18 (12 carves after the bridge) — NOT x=12 (which would mean the bridge didn't reset). Closes the interleaving coverage gap between `a_long_river_gets_periodic_fords` (pure every-Nth) and `a_river_crossing_a_road...` (pure bridge). Test-only; no output change. |
| 026 | 2026-05-22 tilemap river barrier reorder | Root cause refined: **erosion** (not fill) fragments the passable region, and the river's Mountain/Lake sources/sinks required erosion first — so the river was structurally stuck threading post-erosion narrow channels (forded most tiles). Fix: split `ObstaclePlacer` into `ObstacleSourcePlacer` (places Mountain/Lake markers on Open tiles **pre-erosion**, dual-gated per-zone + map-wide) → `RiverPlacer` (carves the wide-open zone) → `ObstacleFillPlacer` (erode + fill the rest post-river, skipping river Water; erosion also skips Water fords). Golden fixture river ford ratio: **~0.77 crossings → 0.06 fords** (AC-2 gates < 0.25). Cost tradeoff accepted: continent 6.46 s → 10.56 s (river now carves a real barrier). Strict dual `would_seal_a_gap` gate unchanged. `/review-impl` caught a HIGH (source placer needed the map-wide gate arm, not per-zone only) — fixed + regression-tested. Spec: [`docs/specs/2026-05-22-tilemap-river-barrier-reorder.md`](../specs/2026-05-22-tilemap-river-barrier-reorder.md). |
| 029 | 2026-05-21 tilemap perf fix | `place_and_connect_object` refactored to **score-first, validate-on-demand**: cheap-filter + score all candidates (no flood fill), sort by `(score desc, flat asc)`, walk in best-first order and validate (`would_seal_a_gap` + `find_access_path`) lazily. The first sorted candidate that passes IS the winner (spec §4 proof: `first ∈ V` of sorted `S` == `argmax_{v ∈ V} (score, -flat)`). Naive kept under `#[cfg(test)]` as oracle. **Measurement: `treasure_placer` 973.376 s → 2.944 s (330×); continent total 992.963 s → 21.541 s (46×).** Golden test passes byte-exact — no rebaseline. AC-1 oracle: 405 agreements across 3 fixtures × 3 templates × 3 optimisations × 3 min-distances × 5 sequential placements. Spec: [`docs/specs/2026-05-21-tilemap-place-and-connect-perf.md`](../specs/2026-05-21-tilemap-place-and-connect-perf.md). |
| 018 | 2026-05-20 tilemap perf fix | `penrose::nearest_vertex` replaced with a bucket-based spiral search (`build_vertex_buckets` + `nearest_vertex_bucketed` over the new `UniformBuckets<Vec2>` helper in `engine/placement/spatial.rs`). Bit-exact equivalent to the prior linear scan (AC-2 oracle: 800 random queries × 4 vertex-field configurations incl. 1500-vertex dense; AC-3 cross-bucket tie-break by lowest index). Per-stage continent measurement: `place_zones` (Penrose + fractalize) dropped to **0.110 s** at 256² — well under the TMP_002 §7 <500 ms budget. Spec: [`docs/specs/2026-05-20-tilemap-perf-fractalize-penrose.md`](../specs/2026-05-20-tilemap-perf-fractalize-penrose.md). |
| 016 | 2026-05-20 tilemap perf fix | `fractalize::scatter_and_connect` replaced with a bucket-based "any within radius" rewrite using the same `UniformBuckets<TileCoord>` helper — 3×3 ring neighbourhood query (sufficiency proven: `bucket_size = ceil(sqrt(coverage_sq))` ⇒ ring-2 min dist² ≥ 49 > 36 surface / 25 > 16 sea). Bit-exact equivalent to the prior `nearest_cleared_dist_sq` linear scan (AC-1 oracle: 96² zone × 5 seeds × 2 spans). The naive impl is kept under `#[cfg(test)]` as the oracle. Spec: [`docs/specs/2026-05-20-tilemap-perf-fractalize-penrose.md`](../specs/2026-05-20-tilemap-perf-fractalize-penrose.md). |
| 022 | 2026-05-18 tilemap Phase E | Resolved by the Phase-E `RiverPlacer` design (spec D-Q5/D-Q6): the `anchor` representative point **suffices** — no footprint extent needed. River sources are one lowest-flat-index `Mountain` obstacle anchor per mountain-bearing zone; sinks are the `Lake` obstacle anchors ∪ `Sea`-zone tiles. `search_path` routes to/from the anchor tile, which is added to the river search area. No additive `TilemapObjectPlacement` footprint field was required. |
| 019 | 2026-05-18 tilemap Phase D | Re-validated when ConnectionsPlacer landed. `seal_borders` (§3.1/§9) seals inter-zone borders but is not a complete wall — passage corridors stay `Walkable` and gap-critical tiles stay `Open`. The per-zone `would_seal_a_gap` is sound regardless: every tile belongs to exactly one zone, and every passability-removing mutation (placer objects, monoliths, guards, ferries, border seals) targets an `Open` tile that is zone-interior — so removing a zone-X tile can never split zone Y. Border-seal completeness is irrelevant to correctness; the original "border thickness" framing was the wrong lens. |
| 013 | 2026-05-17 GEO Phase 2 | Connectivity-aware `choose_sea_level` — binary-searches `sea_level` so the largest land component ≈ the profile target (`maxcomp(L(s))` is monotone non-increasing in `s`), plus a continental base dome for the high-land `Inland` profile (a blob-only heightmap cannot form a connected 70% landmass). Realised land fraction is now within ±0.08 of target, or a documented 8192 best-effort floor (±0.18). Verified by `land_fraction_near_target` (16-seed sweep × 4 profiles). |
| 012 | 2026-05-16 tilemap fix task | Added `TestStreamOpenAICompat_MultipleToolCalls` — two tool calls streamed with interleaved/non-monotonic `tool_calls[].index` (0,1,1,0); asserts `Index` is verbatim from the provider and per-index reassembly stays separate. |
| 011 | 2026-05-16 tilemap fix task | Dropped `nullable: true` from openapi `ChatStreamRequest.tool_choice` — the field is now `description` + `oneOf` only (idiomatic OpenAPI 3.0; still optional via not-in-`required`). No runtime change; SDKs already model it optional. |
| 005 | 2026-05-15 tilemap Phase 0b /amaw | First full-feature `/amaw` dogfood (XL, 12 phases). 6 sub-agent spawns (4 design Adversary + 1 code Adversary + 1 Scope Guard) all completed their Step-0 `python scripts/mcp-query.py` calls cleanly — no permission-prompt interruption. The allow-list is sufficient. |
| 003 | 2026-05-15 amaw-atomic-save-state | First `/amaw` dogfood. `save_state` now writes a PID-unique `.tmp` then `Path.replace()` (atomic rename); `cmd_reset` sweeps stale tmps; `finally`-unlink cleans own tmp. Adversary r1 added 3 hardening WARNs (all fixed). |
| 006 | 2026-05-15 batch pre-flight | AMAW.md Adversary + Scope Guard prompt templates now pre-instruct the Bash-heredoc fallback for writing findings/post-review docs, since the Write tool blocks sub-agents from writing report files. |
| 043 | 2026-05-30 C4 independent-adversary WARN-1 | **Manual single-entity name-fill does not propagate to Neo4j.** glossary `createEntity` emits an empty-name event (consumer skips it); the `PATCH /entities/{id}/attributes/{attr_value_id}` path (`attribute_handler.go patchAttributeValue`) that fills the name never calls `emitEntityUpdated`, so a manually-authored entity reaches Neo4j only when an unrelated `patchEntity` (status/tags/pin) fires. The enrichment DEMO path (extract-entities bulk, `extraction_handler.go`) emits the name inline and IS live-smoke-confirmed — unaffected. Fix: add `s.emitEntityUpdated(ctx, entityID, "updated")` at the end of the setClauses block in `services/glossary-service/internal/api/attribute_handler.go` (~:159), mirroring `patchEntity`. | Platform completeness — when manual single-entity authoring needs KG propagation | MED |
