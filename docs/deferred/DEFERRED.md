# Deferred Items

<!-- Managed by Scribe (AMAW) or main session (default mode). Do not edit manually unless cleaning up. -->
<!-- Next ID: 084 (cycle 38 opened 063+064; post-RAID review 2026-05-30 opened 065-077; the completeness-critic pass opened 078-080 — CI-gate coverage / world+travel domain / meta sink tables — and folded slo-budget-calculator into 069 + PRR-45 into 076; see docs/plans/2026-05-29-foundation-mega-task/POST_RAID_REVIEW_FINDINGS.md. P1 task #16 (publisher live-wiring) 2026-05-30 ADDRESSED 054 + opened 081-083 — V1 reality-set load-once / dependency-registry ClientFactory / V2 multi-replica leader election. P1 task #18 (meta-worker canon live-wiring) 2026-05-30 advanced 069 [meta-worker canon path] + opened 084-086 — consumer-group bootstrap / PEL reclaim / realityreg-shared. P1 archive-worker 2026-05-30 ADDRESSED 056+057 + opened 087 — DETACH-first read. P1 retention-worker 2026-05-30 ADDRESSED 058 + opened 088 — outbox-prune index. P1 embedding-queue 2026-05-30 (DEFERRED-059 core) live-wired sqlx writer + meta audit + tokio drain loop + axum/prometheus ops surface + embedding-worker bin (live-smoked on infra/foundation-dev; the smoke caught + fixed a 0008 NOT-NULL migration bug); 059 → PARTIAL; opened 089-091 — provider-gateway wiring / embedding audit-write durability / test-double DRY. PR-#11-CI-green pass 2026-05-30 fixed 3 pre-existing CI reds inline (gitignore MANIFEST un-tracked migration-orchestrator/pkg/manifest/*.go → Go build + db-smoke; observability validator rejected valid recording-rule `:` names; flaky blockingReadCloser select-race) + opened 092 — world-gen cross-platform golden hash (CI-ignored). P2 074/075 (admin-CLI + break-glass JWT, AMAW) 2026-05-30 ADDRESSED 074 + 075 (new `contracts/adminjwt` shared module; auth-service KMS-Sign issuance endpoints + admin_principals + admin_token_issuance_audit migrations; admin-cli RS256 verify swap) + opened 093-096 — jti denylist / real-AWS KMS live-smoke / JWKS rotation / deny-audit durability. P2 076 PII (Slice A scrubber-seam 2026-05-30; Slice B KMS-KEK adapter 2026-05-31, AMAW+/review-impl) → 076 PARTIAL (A+B done, Slice C erasure-orchestrator remains, gated on 073) + opened 097 D-PIIKMS-DESTROY-TOCTOU. 073-prerequisite (admin-cli MetaWrite adapter) 2026-05-31 — D-ADMIN-CLI-METAWRITE blocker CLEARED (new sdks/go/metapg pgx meta.DB driver + admin-cli MetaWriteSink; AMAW design×3+code×1+/review-impl); 073 command bodies remain; also closes Slice A WARN2 (admin audit now scrubbed) + opened 098-100 — in-progress-audit / error-text-retention / scrub-binary-fidelity. P2 076 Slice C (erasure orchestrator, AMAW design 2B/1W + code 1B/2W + /review-impl 3) 2026-05-31 ADDRESSED 076 (Slice C done → 076 fully ADDRESSED) + opened 101-105 — metawrite-outbox-unwired / erasure-email / erasure-cert / read-audit-enum-drift / windows-code-sign. P2 072 GDPR Art.33 breach-wiring (incident-bot, AMAW design 2B/1W + code 1B/2W + /review-impl 3) 2026-05-31 ADDRESSED 072 (live authed POST /internal/breach + breach event types + self-pruning monitor) + opened 106-110 — dpo-delivery-consumer / durable-store / broker-emitter / i18n-template / race-ci. 078-recon 2026-05-31 found the D-CI-GATE-COVERAGE claim STALE (I2 already CI-enforced; 15 validators wired) — real gap is lint-foundation RED; FIXED service-acl (canary-controller entry) + meta-write-discipline test-exemption + opened 111 D-CI-GATE-GREENING for the 4 remaining red gates. P2 101 (D-METAWRITE-OUTBOX-UNWIRED) 2026-05-31 PRODUCER SLICE landed (Option B dedicated meta-outbox relay, human-in-loop + /review-impl) — mig 030 meta_outbox + sdks/go/metaoutbox appender + allowlist xreality_topic + admin-cli cfg.Outbox wiring + probeMetaOutbox; 101 → PARTIAL (relay = slice B remains) + opened 112-114 — meta-outbox-prune / kek-destroy-via-metawrite (user.erased payload needs user_ref_id) / meta-outbox-payload-pii. P2 073 read-cmd batch (projection drift-check + archive fetch, human-in-loop + /review-impl ×2) 2026-06-01 ADDRESSED — 2 of 4 candidate tier-3 read commands shipped (drift-check fleet-wide + archive fetch w/ admin-cli's own minio wrapper + LWP1/allowlist drift tripwires); the other 2 (backup list / canon conflict-list) BLOCKED on missing persistence → opened 116-122 (backup-list-persistence / canon-conflict-persistence / drift-live-resample / archive-fetch-decode / archive-fetch-stdout-stream / admin-notwired-exit / admin-tier3-audit-pgtest). P2 breach rail (108 broker + 107 durable, human-in-loop + /review-impl) 2026-06-01 ADDRESSED 107 + 108 (incident-bot RedisEmitter → lw.incidents.breach + anchor-safe MINID trim; boot-replay rebuilds the deadline monitor, stays stateless; graceful-degrade on Redis boot failure) + RE-SCOPED 106 (delivery consumer: notification-service is the wrong home → foundation-side consumer reusing war_room Slack, its own task) + opened 123 (breach-emitter-metrics). Also fixed lint-contract.sh (Spectral ruleset discovery). P2 106 breach-notifier (new standalone delivery-consumer service, human-in-loop + /review-impl) 2026-06-01 ADDRESSED 106 — XReadGroup consumer ("0" start, XAUTOCLAIM reclaim) → pluggable Notifier (Log default + Slack scaffold) → durable breach_dpo_delivery confirmed-record; cold-start review fixed 2 HIGH (start-position miss, PEL reclaim) + MEDs; opened 124 (at-least-once) + 125 (slack-live). P2 089 embedding-provider-wiring (Rust, human-in-loop + /review-impl) 2026-06-02 ADDRESSED 089 — HttpEmbeddingProvider (reqwest → provider-registry /internal/embed, platform credential) replaces NotWiredProvider; wiremock request-shape test; cold-start review fixed request-coverage HIGH; opened 126 (per-reality attribution) + 127 (token accounting) + 128 (provider live-smoke). 059 core remains PARTIAL (provider path now done; enqueue trigger 069/079 + integrity re-enqueue still blocked). P2 064 canary-controller live-wiring (Go, human-in-loop + /review-impl) 2026-06-03 ADDRESSED 064 — 4 live adapters (pgx DeployStore via contracts/meta MetaWrite CAS on canary_stage; Prometheus SLISource; GitHub repository_dispatch Executor; PagerDuty Pager) + real controller.Tick loop + client_golang metrics, wired in main.go behind require-providers/idle; httptest contract-pinned; controller inert until deploy pipeline (063) feeds it a class=major row; opened 129 (live-smoke) + 130 (baseline-capture) + 131 (outbox-emit). P2 068 D-WS-TICKET-WIRE (Go+Rust WS wire fix, human-in-loop + /review-impl ×2) 2026-06-03 ADDRESSED 068 — Hash32 base64 (contracts/ws/ticket.go) aligning to the frozen ws/v1.yaml + Envelope seq>=1-for-data in Go envelope.go + Rust dp-kernel ws.rs (surgical 1-rule kernel touch); base64 round-trip + 32-byte-enforce + seq tests both languages; 2 cold-start reviews clean; opened 132 (cross-lang golden fixture = 077 acceptance item). P2 077 game-WS edge controls (full ticket chain, human-in-loop + /review-impl) 2026-06-03 ADDRESSED 077 — gateway RedisTicketStore (issue→shared Redis, atomic Lua, base64 per 068) + game-server onAuth ticket redeem/validate/strict-bind (§12AB.9 close codes) + per-user/per-conn rate caps (clears 035) + structured-log lifecycle audit; cross-impl golden fixture in all 3 impls (clears 132); 8 gateway jest + 29 game-server node:test; ioredis added to both (config-gated). Opened 133 (live-smoke) + 134 (audit-event-stream) + 135 (forced-disconnect) + 136 (tls-session-id) + 137 (AWS-IaC #4) + 138 (redis-HA); cold-start /review-impl found 2 HIGH (trustedProxy IP asymmetry → game-server defaults trustedProxy true to mirror the issuer; fail-open dev auth → assertWsAuthConfig boot guard) + MED-1 (cap leak → acquire moved to onJoin) all FIXED, + opened 139 (room-lifecycle test, MED-2) + 140 (redis-wire-snake-case, LOW-3); Go golden literal test added (LOW-1, closes 132 tri-impl). P2 073 destructive admin commands 2026-06-03 (XL, human-in-loop + /review-impl) ADDRESSED capacity-override (live MetaWrite→scaling_events, Tier-2) + BUILT the world-service `rebuilder` bin (FIRST live projection-apply path: sqlx event-source + generic jsonb_populate_record writer + 11 projections via crates/projections) + Go rebuild-projection/catastrophic-rebuild wiring (AttemptStateTransition freeze↔frozen/thaw + per-reality TRUNCATE + subprocess invoker + rolling orchestrator), the two destructive commands GATED behind ADMIN_CLI_ENABLE_UNPROVEN_REBUILD (fail-closed NotWired by default); deleted the dead services/admin-cli/commands pkg; opened 141-143 — unproven-gate-removal / rebuild-live-smoke / projection-upsert-shape. P2 086+121 batch 2026-06-03 (human-in-loop) ADDRESSED 121 (admin-cli `wireCommandHandler` — uniform NotWired-vs-error exit policy: a wiring-builder config error now exits 2 instead of falling through to the tier-3 NotWired exit-0; 9 wiring blocks collapsed to a map+helper) + ADDRESSED 086 (realityreg promoted to a `contracts/realityreg` peer module; publisher + meta-worker + retention-worker + archive-worker repointed, the 3 workers DROPPED their cross-service `replace publisher`); surfaced + opened 144 (D-INTEGRATION-TEST-BUILD-RED — pre-existing orphan `admin-cli/commands` import in capacity_override_test.go from 073 + missing adminjwt replace; the `-tags=integration` build is likely CI-red). P2 144 2026-06-03 ADDRESSED 144 — deleted the orphan test (coverage is a strict subset of internal/commands/capacity_override_test.go) + added the 5 missing local replaces (adminjwt/metapg/pii/piikms/metaoutbox, mirroring admin-cli's); tidy reconciled pgx→5.9.2 (correct MVS-max, the old 5.6.0 was a latent under-pin); `-mod=readonly -tags=integration` vet EXIT=0 + full test binary compiles. P2 L3.E/F slice 1 (keystone, human-in-loop) 2026-06-03 — built the `replay-aggregate` Rust bin (the integrity-checker keystone: temp-shadow of the target projection table via `CREATE TEMP TABLE … (LIKE public.… INCLUDING ALL)` + pg_temp search-path shadowing → reuses the 073 SqlxProjectionWriter UNCHANGED; bounded multi-aggregate global-order replay ≤ a boundary event; emits the row's `to_jsonb - meta_keys` payload as JSON); design doc `docs/plans/2026-06-03-l3ef-integrity-checker.md` (row-centric model, event_id→aggregate resolution, all-10 scope incl. cross-aggregate npc_session_memory); 13 unit tests; opened 145 (Go slice-2 wiring) + 146 (global-seq ordering limitation) + 147 (replay live-smoke). P2 147 D-L3EF-REPLAY-LIVE-SMOKE 2026-06-03 (human-in-loop + /review-impl) ADDRESSED 147 — round-trip live-smoke (pc clean+drift + multi-aggregate invocation) invoking the compiled bin vs in-process live row; surfaced + fixed TWO latent keystone bugs the bin had never hit live: writer Insert `SELECT *`→column-list (NOT-NULL-DEFAULT omitted cols, partially clears 143 + fixes 073 rebuilder's npc_session_memory break) + bin runtime current_thread→multi_thread (Handle::block_on IO-driver deadlock); CI db-smoke `replay_smoke` step; /review-impl no HIGH/MED (phantom-key drift risk cleared — all 11 projections insert real cols only, no nullable-with-default col); opened 148 (replay smoke verdict-coverage: found:false/skip live paths). Unblocks 141/142 keystone validation. P2 142→141 batch 2026-06-03 (human-in-loop + /review-impl) ADDRESSED 142 (rebuilder bin live-smoke `tests/rebuilder_live.rs`: pc + npc_session_memory + multi-aggregate-concurrent cases; CI `rebuild_smoke`) + ADDRESSED 141 (removed ADMIN_CLI_ENABLE_UNPROVEN_REBUILD → rebuild-projection + catastrophic-rebuild first-class on META_DATABASE_URL; 6 doc sites); /review-impl found+FIXED the MED parallel-path coverage gap inline (added multi-aggregate Case C) + opened 149 (catastrophic rolling-orchestration live-smoke, LOW). The 073/L3.E/F rebuild-gate arc is now CLOSED (projection-apply layer validated end-to-end + gate removed). Next ID: 150). -->
<!-- Note: zone-map V1.2/V2 sessions reserved IDs into 039 range; main-side GEO track allocated 013. geo-generator-amaw branch added 039 + 040 (renumbered from local 025/026 on main merge to avoid ID collision). RAID-foundation cycles 5+6 added 041-045. Cycle 7 added 046-049 (re-targeted 044+045 to later cycles). Cycle 8 added 050-052 (L2.F-I follow-ups). Cycle 10 added 053-055 (L2.C-D-L follow-ups). Cycle 11 added 056-058 (L2.J-K follow-ups) and ADDRESSED 055 (D-OUTBOX-PRUNE); re-targeted 053 + 054. Cycle 16 added 059-060 (L3.I follow-ups — embedding queue live wiring + V1+30d Redis migration per Q-L3-1). Cycle 17 added 061 (D-EVENT-STORE-LIVE-SMOKE) and RE-TARGETED 053+054+056+057+058+059 to cycle 33 (L7 ops integration) — cycle 17 was trait-shape + macros only, NOT integration wiring; the original "cycle 17 = L4.A polyglot integration" framing in cycle-10/11 comments was based on a stale L4.A definition. -->


| ID | Origin | Description | Target | Severity |
|---|---|---|---|---|
| 147 | 2026-06-03 P2 L3.E/F slice 1; **ADDRESSED** 2026-06-03 | **D-L3EF-REPLAY-LIVE-SMOKE — ADDRESSED.** `services/world-service/tests/replay_aggregate_live.rs` (PG-gated on `LOREWEAVE_TEST_PG_URL`): applies 0002+0006, seeds events, builds the live projection row in-process via the SAME `all_projections`+`SqlxProjectionWriter` the bin reuses, runs the COMPILED `replay-aggregate` bin as a subprocess (`CARGO_BIN_EXE_…`), and byte-compares `to_jsonb-meta` payloads. 3 cases: **pc clean** (==), **pc drift** (tamper→!=, replay holds correct value), **multi-aggregate invocation** (session.started+ended interleaved with npc.created from a 2nd aggregate → exercises the 2-pair IN-list events query + global ordering; clean ==). Wired into foundation db-smoke CI (`replay_smoke` DB). **Surfaced + fixed TWO latent keystone bugs:** (1) the generic writer Insert `SELECT *` NULLed omitted columns → `npc_session_memory_projection` rebuild broke on NOT NULL `summary`/`facts` → **fixed** with a column-list INSERT (omitted cols → DEFAULT; keys `ensure_ident`'d) — also fixes the 073 rebuilder's same break (partially clears 143); (2) the bin **deadlocked** (`new_current_thread` runtime + the writer's `Handle::block_on` → IO driver never ticked) → **fixed** with `new_multi_thread().worker_threads(1)` (matches the rebuilder bin). Live-smoke green on pg18 (1 passed 0.82s) + 104 lib + 7 writer-unit. **Genuine cross-aggregate WRITE convergence still deferred** (npc.said `interaction_count_increment` not a real column / npc.memory_updated TODO → generic writer has no increment concept; pairs with 146). Unblocks the 073 rebuild-gate chain (141/142). | DONE | MED |
| 148 | 2026-06-03 P2 147 (review-impl LOW) | **D-L3EF-REPLAY-SMOKE-VERDICT-COVERAGE** — the 147 live-smoke covers the `clean` / `drift` verdicts but NOT the live `found:false` (orphan-drift: replay produces no row at the PK → comparator marks DRIFT) nor `events_replayed==0` (pruned/never-existed aggregate → SKIP) paths; both are unit-tested for output shape but never run against real PG. Add a 4th case (boundary BEFORE the creating event → events>0 but row absent? or a PK the replay never writes → found:false) + a pruned/empty-aggregate case (boundary_event_id absent → events=0 → SKIP). Also: the drift assertion checks Rust-side `Value` inequality as a proxy for the Go `pkg/comparator` byte-compare (separately unit-tested) — fine, noted. Month-boundary flake (`recorded_at = date_trunc('month', now())+idx s` mismatches the partition only if the test crosses midnight on the 1st) — same implicit assumption as `embedding_live.rs`; accepted. | Next time the replay smoke is extended | LOW |
| 146 | 2026-06-03 P2 L3.E/F slice 1 | **D-L3EF-GLOBAL-SEQ** — the `events` table has NO monotonic global sequence; cross-aggregate replay orders by `(recorded_at, event_id)`, which APPROXIMATES but does not perfectly reconstruct the live consumer's apply order. So `npc_session_memory_projection` drift (the one cross-aggregate table) may include ordering artifacts (treat its drift as "investigate"). A real global sequence column (or consuming the outbox drain order) would make it exact. Revisit if live-smoke shows false-positives on npc_session_memory. | If npc_session_memory false-positives appear | LOW |
| 145 | 2026-06-03 P2 L3.E/F slice 1 (keystone); **slices 2a+2b+2c landed** 2026-06-03 | **D-L3EF-GO-WIRING (daily live path CODE-COMPLETE)** — 2a: `pkg/tablemap` + `pkg/replayloader` (subprocess → slice-1 bin). 2b: `pkg/live` row/PK-centric orchestrator (`ResolveOwning` + `Checker`, fake-tested). 2c: pgx adapters + daemon — `pkg/state_writer/pg.go` (`PgPersister` UPSERT `projection_drift_state`, NULL drift-pointers when clean), `pkg/pgsource` (`PgRowSampler` = `to_jsonb - meta` random sample + `::text` pk + `event_id`→owner lookup; pure SQL builders unit-tested), `cmd/integrity-checker/main.go` (CRON-style: one daily sweep across active realities — enumerated via meta `reality_registry` + `contracts/realityreg`, shard DSN per reality — sample→replay→compare→persist, signal-aware, exits non-zero on any reality error; no internal ticker, K8s CronJob drives cadence). Full module green (12 pkgs). **Remaining (all need a live DB / are follow-ons):** the foundation **live-smoke (147)** — the actual round-trip validation that unblocks the 073 rebuild gate (141); the L3.F **monthly/full-scan** live wiring (daily only so far); a real **degraded-mode source** (main uses a static ModeFull); **Prometheus `/metrics` emission** (the `metrics` pkg is constants only); and **removing the now-superseded skeleton path** (`daily_loop` + `sampler.RowSource`/`AggregateLoader` fetch model — `pkg/live` replaces it). Reshape the skeleton types from `(aggregate_id, version)` to ROW/PK-centric; pgx `RowSource` (`SELECT to_jsonb(t) - meta AS payload, <pk cols>, event_id FROM <table> ORDER BY random() LIMIT N` + resolve owning aggregate via `SELECT aggregate_type,aggregate_id FROM events WHERE event_id=$1`); subprocess `AggregateLoader` invoking the `replay-aggregate` bin; pgx `Persister` (UPSERT `projection_drift_state`); the per-table PK-columns + cross-aggregate-set map (8 single + embedding(npc) + npc_session_memory(session+npc)); `main` per-reality ticker + `/healthz`/`/readyz`/`/metrics` (mirror archive-worker). Then unblocks the 073 rebuild gate (141) once the foundation live-smoke (147) passes. Design: `docs/plans/2026-06-03-l3ef-integrity-checker.md`. | Next slice (after slice-1 POST-REVIEW) | MED |
| 144 | 2026-06-03 surfaced during 086 VERIFY (origin 073/074); **ADDRESSED** 2026-06-03 P2 144 | **D-INTEGRATION-TEST-BUILD-RED — ADDRESSED.** The `tests/integration` module did not build under `-tags=integration` from a clean checkout (db-smoke CI's `go test -tags=integration ./...` was red since 073). Two pre-existing causes, both fixed: **(1)** DELETED the orphan `capacity_override_test.go` — it imported `services/admin-cli/commands`, a package removed in 073 (capacity-override moved to `internal/commands`, un-importable cross-module); its 3 acceptances (24h-expiry / audit-fields / reject-over-24h) are a strict subset of `internal/commands/capacity_override_test.go` (which adds dry-run / field-validation / writer-error), so zero coverage lost. **(2)** Added the 5 missing local replaces (`contracts/adminjwt`, `sdks/go/metapg`, `contracts/pii`, `sdks/go/piikms`, `sdks/go/metaoutbox`) — `admin-cli/pkg/cliapi` pulls them transitively (auth verify + the audit_emitter chain) and Go does NOT inherit a dependency's replaces. `go mod tidy` then reconciled go.mod/go.sum: pgx 5.6.0→**5.9.2** (the correct MVS-max — piikms/metaoutbox/admin-cli/canary-controller already require 5.9.2; the old 5.6.0 pin was a latent under-pin), golang-jwt/jwt/v5 indirect added. Verified: `go vet -mod=readonly -tags=integration ./...` EXIT=0 + the full integration test binary compiles (`go test -tags=integration -run xxx` → "no tests to run"); running the live tests still needs the docker stack (deferred). | DONE | MED |
| 143 | 2026-06-03 P2 073 (rebuilder writer); **PARTIALLY ADDRESSED** 2026-06-03 by 147 | **D-REBUILD-PROJECTION-UPSERT-SHAPE** — *(147 fixed the "omits a NOT NULL column" sub-case: the writer Insert now lists the set columns so omitted NOT-NULL-DEFAULT cols take their DEFAULT instead of failing — `npc_session_memory_projection` rebuild now works. The Update-before-Insert + Tombstone + cross-aggregate-increment sub-cases below remain.)* — the generic `jsonb_populate_record` writer assumes each L3.B projection emits `Insert` on the creation event then `Update`s for mutations, replayed in version order against the exact `0006_projections` column shapes (NOT NULL/defaulted cols included in the Insert `row`). If a projection omits a NOT NULL column, emits `Update` before the creating `Insert`, or emits `Tombstone` (currently unsupported → hard error), the rebuild FAILS LOUD (correct) — but that surfaces a projection-contract gap that only the L3.E/F integrity checker (or a live-smoke) can confirm per-projection. **Two known-risky cases the gate covers:** (a) `npc_session_memory_embedding` is NOT purely event-derived — its VECTOR(1536) is produced asynchronously by the embedding-worker, so truncate+replay loses embeddings until re-enqueued (consider excluding it from the catastrophic default set, or re-triggering embedding enqueue after rebuild); (b) cross-aggregate updates (e.g. `pc.said` → `npc_session_memory_projection`) can apply `Update`-before-`Insert` under the crate's PARALLEL per-aggregate replay → silent UPDATE-of-0-rows. Triage all gaps in the first live-smoke; the projections have NEVER been exercised against real tables. | With L3.E/F validation | LOW |
| 142 | 2026-06-03 P2 073 (no dev test DB); **ADDRESSED** 2026-06-03 | **D-REBUILD-LIVE-SMOKE — ADDRESSED.** `services/world-service/tests/rebuilder_live.rs` (PG-gated, own CI DB `rebuild_smoke`): TRUNCATE→rebuild (mirrors the admin-cli freeze→TRUNCATE→exec flow), runs the COMPILED `rebuilder` bin as a subprocess, asserts `RebuildStats` + the rebuilt rows. 3 cases: **(A)** pc single-aggregate (rebuilt=1, events=2, current_region/lev correct); **(B)** npc_session_memory_projection — proves the 147 writer-Insert fix IN THE REBUILDER (session.started omits NOT NULL summary/facts → DEFAULT); **(C)** MULTI-aggregate (2 pc aggregates → rebuilt=2) exercising the `ParallelRebuilder` concurrent path (default parallel_workers=8: concurrent spawn_blocking workers + pool acquisition + same-table writers) — added during /review-impl as the MED gap. Live-smoke 1 passed 0.88s on pg18. The cross-process admin-cli→subprocess wiring + the catastrophic rolling orchestration are NOT in this smoke (orchestrator unit-tested; the bin it drives now is) → see 149. | DONE | MED |
| 141 | 2026-06-03 P2 073 (unproven gate); **ADDRESSED** 2026-06-03 | **D-REBUILD-UNPROVEN-GATE — ADDRESSED.** Removed `ADMIN_CLI_ENABLE_UNPROVEN_REBUILD`: deleted the const + both builder gate-blocks in `services/admin-cli/cmd/admin/main.go`, so `reality rebuild-projection` + `reality catastrophic-rebuild` are first-class — they wire whenever `META_DATABASE_URL` is set (and still fail-closed NotWired via the `dsn==""` path when it is not, so dev/test envs are unaffected; a partial-meta-config invocation now exits 2 fail-loud per 121 rather than NotWired). Justified by the projection-apply layer now being validated end-to-end (147 integrity-checker round-trip + 142 rebuilder live-smoke). Doc comments updated across 6 sites (main.go ×2, rebuild_projection.go, catastrophic_rebuild.go, rebuilder.rs, rebuild/mod.rs). /review-impl confirmed no deploy/CI/runbook config set the old flag (no silent breakage). | DONE | HIGH |
| 149 | 2026-06-03 P2 142 (review-impl LOW-2) | **D-REBUILD-CATASTROPHIC-ORCHESTRATION-LIVE-SMOKE** — 142 live-smokes the per-reality `rebuilder` BIN (incl. multi-aggregate concurrency). NOT yet live-smoked: the `reality catastrophic-rebuild` ROLLING orchestration end-to-end — multi-reality enumeration (`--scope all-realities`), the freeze-ONCE → per-projection TRUNCATE+rebuilder → thaw-ONCE-only-if-all-clean lifecycle, and the leave-FROZEN-on-any-failure path. The Go `rolling_rebuild` orchestrator + lifecycle gate are unit-tested; the bin they drive is now live-proven (142); the missing piece is the cross-process admin-cli→meta-DB→shard-DB→subprocess chain with a real lifecycle transition (needs meta DB + ≥1 reality_registry row + per-reality shard DSN). Fold under a foundation cross-service smoke when the meta+shard stack is bootable. | When meta + ≥1 seeded shard DB are dev/CI-runnable together | LOW |
| 140 | 2026-06-03 P2 077 (review LOW-3) | **D-WS-REDIS-WIRE-SNAKE-CASE** — the Redis ticket JSON uses camelCase field names (`ticketId`/`originHash`…, TS-native), diverging from the snake_case Go/OpenAPI HTTP wire (`ticket_id`/`origin_hash`…). Harmless today (only the gateway writer + game-server reader touch Redis + they agree), but a future Go/Rust Redis redeemer would mis-parse. Align the Redis field names to snake_case (or have the future redeemer map them). | If a Go/Rust service ever redeems from Redis | LOW |
| 139 | 2026-06-03 P2 077; **ADDRESSED** 2026-06-03 P2 139 | **D-GAME-WS-ROOM-LIFECYCLE-TEST — ADDRESSED.** Refactored EchoRoom's edge-control singletons into an injectable `EchoEdge` bundle (`__setEchoEdgeForTest` rebuilds it with a small cap / recording audit sink), so the lifecycle wiring is now integration-tested via fake Clients driven on a prototype instance (Colyseus internals stubbed). 7 new node:tests cover: onJoin acquire+welcome+audit, onLeave(consented) release+audit, **double-finalize → release-once** (review MED-2), onAuth at-cap → close 4008, onMessage rate-cap → leave 4006 + reason, reconnect-expired → finalize, reconnect-success → keep-slot. **Also FIXED** the latent double-finalize: `finalize` now guards on the per-connection limiter's presence (the "holds a slot" marker) — a second onLeave / auth-without-join cannot double-release, with no new leak-prone field. Live handshake still covered by 133. | DONE | MED |
| 138 | 2026-06-03 P2 077 (WS edge controls) | **D-WS-REDIS-HA** — the WS ticket store is a single Redis (config-gated `LW_WS_REDIS_URL`); no failover/sentinel/cluster wiring. A Redis outage drops all NEW WS handshakes (existing connections survive). Add HA + a degraded-mode policy before relying on the WS edge at scale. | When WS goes HA / at scale | LOW |
| 137 | 2026-06-03 P2 077 (WS edge controls) | **D-GAME-WS-AWS-IAC** — control #4 (AWS security groups: expose ONLY api-gateway-bff + game-server publicly; game-server ingress from the ALB SG only) is NOT in this repo (no IaC). game-server CORS is env-gated (`LOREWEAVE_CORS_ORIGINS`, 034) but there's no code-level HTTPS-only / origin-allowlist enforcement on the upgrade. Add the NLB/SG rules + (optionally) a code-level origin allowlist when the deploy IaC lands. | Before game-server prod exposure (with IaC) | MED |
| 136 | 2026-06-03 P2 077 (WS edge controls) | **D-WS-TLS-SESSION-ID** — the fingerprint = SHA-256(UA ‖ ip/24 ‖ tls-session-prefix), but tls-session-prefix is `''` on BOTH the issuer (gateway) + redeemer (game-server) until the ALB/Envoy forwards the TLS session id. Both must switch in lockstep (the fingerprints must match) when the proxy header is wired. Until then binding is UA+ip/24 only (weaker). | When ALB forwards the TLS session id | LOW |
| 135 | 2026-06-03 P2 077 (WS edge controls) | **D-WS-FORCED-DISCONNECT** — the handshake binds + authenticates, but there is no revoke-mid-session path: a logged-out / erased / kicked user's LIVE game-server connection is not force-closed (close 4002 token_revoked / 4003 user_erased / 4005 admin_kick). The gateway has a forced-disconnect Redis-pubsub consumer concept; the game-server needs the equivalent. Ticket-only handshake auth (H2) is V1; session-token + forced-disconnect is the follow-on. | When revoke-mid-session is required | MED |
| 134 | 2026-06-03 P2 077 (WS edge controls) | **D-WS-AUDIT-EVENT-STREAM** — control #3 connection-lifecycle audit is a structured-LOG sink (`LogWsAuditSink`, V1 interim), not a durable event stream. The game-server is TS (no Go MetaWrite path) + has no metrics/event surface yet. Swap the injectable `WsAuditSink` for a real emitter (publisher / meta audit consumer) when the game-server gets an observability surface. | When game-server gets an event/metrics surface | MED |
| 133 | 2026-06-03 P2 077 (WS edge controls) | **D-GAME-WS-LIVE-SMOKE** — the ticket chain is unit-tested per-impl + cross-impl golden-fixture-pinned, but the full cross-process round-trip (gateway issue → real Redis → game-server `onAuth` redeem+validate+bind over a real WS upgrade, incl. the Lua GET+DEL atomicity + a browser presenting `Sec-WebSocket-Protocol: lw.v1, ticket.<id>`) was not bootable at dev time (needs running Redis + both services + a client). Live-smoke when the stack is up. | When Redis + both services are dev-runnable | MED |
| 132 | 2026-06-03 P2 068 (review-impl, MED-1) | **D-WS-TICKET-CROSSLANG-FIXTURE** *(ADDRESSED 2026-06-03 by 077 — the shared golden fixture now exists: the SAME base64 literal `AAECAwQFBgcICQoLDA0ODxAREhMUFRYXGBkaGxwdHh8=` [bytes 0..31] + a 0xFF×32 Std-vs-URL distinguisher are asserted by the gateway redis-ticket-store.spec.ts + the game-server ticket-store.test.ts; the Go contracts/ws test pins the same StdEncoding form. Cross-impl drift now caught.)* — 068 aligned the Go ticket-hash wire form to base64 (`StdEncoding`) per the frozen `ws/v1.yaml`, but cross-language parity (Go's base64 output == what the TS gateway / a future Rust consumer decode) is asserted ONLY within Go: the Rust `Ticket` has no deserializer, and the TS gateway keeps hashes as in-memory `Buffer` (never JSON today). 077 (game-server TS ticket redemption) is the first real cross-process consumer — it MUST add a shared GOLDEN fixture (a fixed 32-byte digest → known base64 literal) asserted by BOTH the Go marshal test + the TS redeem test, so a silent `StdEncoding`-vs-`URLEncoding` drift is caught. (Node `Buffer.from(s,'base64')` is LENIENT — it decodes both variants — so it would MASK a wrong-variant producer; an explicit golden literal is the only guard.) | 077 acceptance item | MED |
| 131 | 2026-06-03 P2 064 (canary live-wiring) | **D-CANARY-OUTBOX-EMIT** — `deploy_audit` UPDATE is allowlisted to emit an event (`events_allowlist.yaml`), but `PgDeployStore` wires `MetaWrite` with **nil-Outbox** (sanctioned: the library skips emission when no appender is configured) because no consumer reads a `deploy.canary_advanced`-style event in V1. When a consumer exists (deploy dashboard fan-out, audit stream), wire `cfg.Outbox` (sdks/go/metaoutbox) + an explicit `OutboxPayload`. **GOTCHA (review #4):** the generic CDC default payload is `{after: NewValues}`, and `NewValues["canary_history"]` is a Go `[]byte` — `json.Marshal` would emit it as a **base64 string**, not the history array. So the outbox author MUST supply an `OutboxPayload` (or pass `canary_history` as `json.RawMessage`), not rely on the CDC default. (The data write itself is fine: pgx encodes `[]byte`→jsonb correctly.) Mirrors the admin-cli audit-sink nil-Outbox choice (101). | When a deploy-event consumer exists | LOW |
| 130 | 2026-06-03 P2 064 (canary live-wiring) | **D-CANARY-BASELINE-CAPTURE** — `deploy_audit` has NO `baseline_burn` column; `PgDeployStore` reads the controller's `BaselineBurn` from the first `canary_history` entry that carries a `baseline_burn` field, which **nothing writes yet** (the deploy pipeline must snapshot the pre-deploy cohort SLI at stage-0 entry). Until then `BaselineBurn=0` → the §12AH.4 `2× baseline` threshold is `0` → ANY positive cohort burn aborts at stage 1 (fail-safe, but every real canary would auto-abort). MUST clear before real rollouts: have the deploy pipeline write `canary_history[0].baseline_burn` (or add a column + migration). The controller is inert until the pipeline lands (063) so this is harmless today. | Before real canary rollouts (with 063) | MED |
| 129 | 2026-06-03 P2 064 (canary live-wiring) | **D-CANARY-LIVE-SMOKE** — the 4 live adapters' request shapes are pinned by `httptest` contract tests (DeployStore via MetaWrite CAS; Prometheus `/api/v1/query`; GitHub `repository_dispatch` POST 204; PagerDuty `/v2/enqueue` POST 202), but no real end-to-end round-trip was bootable at dev time: no real GitHub/PagerDuty/Prometheus creds, and no deploy pipeline writes `class=major` canary rows (063) or emits `lw_canary_sli_cohort`. Live-smoke the full control loop (deploy_audit row → observe → advance/abort → dispatch → page → audit) once the deploy pipeline + a Prometheus with cohort SLIs exist. **HARD prerequisite (review #6):** the `app_canary_role` GRANT UPDATE ON deploy_audit is NOT yet written (migration 023 revokes UPDATE from app_service_role and only *promises* the grant "when canary-controller ships"). Until a grant migration exists AND the controller's DB role holds it, the FIRST stage advance/rollback MetaWrite UPDATE FAILS with a permission error at runtime — not graceful inertness. Write the grant migration (test-DB only, never apply to non-test) before any live rollout. **Dashboard drift (review #3):** the deploy-progress dashboard filters canary panels by a `service` label, but the V1 controller metrics are fleet-global (unlabeled) — align the label contract (or the dashboard queries) when real metrics land; also `lw_canary_sli_baseline` (panel 2) + the services' `lw_canary_sli_cohort{stage,service}` have no emitter yet (pair with 130). Pairs with D-DEPLOY-LIVE-WIRING (063). | Post-V1 launch (when deploy pipeline first goes live) | MED |
| 128 | 2026-06-02 P2 089 (embedding provider) | **D-EMBEDDING-PROVIDER-LIVE-SMOKE** — the `HttpEmbeddingProvider` request shape is pinned by a wiremock unit test (method/path/?user_id/X-Internal-Token/body), but a real end-to-end round-trip (provider-registry up + a seeded platform `user_models` embedding row + an embeddings upstream) was not bootable at dev time. Live-smoke the full path (worker → /internal/embed → upstream → 1536-dim vector → npc_session_memory_embedding) when the stack + a platform embedding model exist. Fold under D-INCIDENT/EMBEDDING live-smoke. | When provider-registry + a platform embedding model are dev-runnable | MED |
| 127 | 2026-06-02 P2 089 (review-impl, M4) | **D-EMBEDDING-TOKEN-ACCOUNTING** — provider-registry `POST /internal/embed` returns no token usage (`EmbedResult{embeddings,dimension,model}` only), so `HttpEmbeddingProvider` records `tokens=0`. Harmless for the audit row (`service_to_service_audit` has no token column) but the `lw_embedding_provider_tokens_total` counter stays flat for real embeddings — could mislead an SRE into reading "0 tokens" as "provider idle". Surface embedding token usage from the gateway (provider.Embed → response usage) + wire it through, or annotate the dashboard. Pairs with the cost/usage sink work (080). | When embedding cost tracking matters | LOW |
| 126 | 2026-06-02 P2 089 (platform-credential decision) | **D-EMBEDDING-PER-REALITY-ATTRIBUTION** — V1 wires a single PLATFORM embedding credential (env `EMBEDDING_PLATFORM_USER_ID`/`MODEL_REF`) to pay for system NPC-memory embedding (aligns with the existing `system_only` audit; a reality is a shared world via book_reality_subscription with no single owner). If a product reason ever needs PER-reality/per-user BYOK attribution, add the reality→payer domain model (no owner column exists today) + thread `reality_id` through the `EmbeddingProvider` trait (currently `embed(&self, text)`; the platform-cred model keeps user_id/model_ref as provider config). | If per-user embedding billing is ever required | MED |
| 125 | 2026-06-01 P2 106 (breach-notifier) | **D-BREACH-SLACK-LIVE** — breach-notifier's real Slack delivery is a fail-closed SCAFFOLD (`deliver.SlackNotifier`, opt-in via `LW_BREACH_NOTIFIER=slack`): the live `chat.postMessage` round-trip is not wired (no dev creds), mirroring incident-bot's war_room slack_provider. The default `LogNotifier` IS a genuine audit-line delivery (logs incident_id+subject+body+deadline); the durable delivery-confirmed record + idempotency + consumer are all real. Wire the live Slack call (own thin client; B6 token gate already present) when a workspace + creds exist. Fold under D-INCIDENT-LIVE-SMOKE. | When a Slack/SES compliance channel + creds exist | MED |
| 124 | 2026-06-01 P2 106 (review-impl, M1) | **D-BREACH-AT-LEAST-ONCE** — breach-notifier delivery is AT-LEAST-ONCE, not exactly-once: `AlreadyDelivered`→`Deliver`→`RecordAttempt` is not atomic, so a Deliver-succeeds-then-RecordAttempt-fails window leaves no `delivered` marker → on redelivery the notice is delivered AGAIN (a duplicate DPO notice). DELIBERATE + documented at handler.go: a duplicate compliance notice is far safer than a lost legal notification (matches the meta-outbox at-least-once+idempotent-consumer posture, 115). Test-pinned (TestHandle_DeliveryFailRecordsFailedNoConfirm + the live idempotency roundtrip). A future SES/Slack wiring author must NOT assume exactly-once. | If exactly-once DPO delivery is ever required | LOW |
| 123 | 2026-06-01 P2 107/108 (breach rail) | **D-BREACH-EMITTER-METRICS** — incident-bot's breach RedisEmitter + boot-replay are log-based (slog/log); no prometheus metrics, since incident-bot's main is a V1 skeleton with no registry. Add emitted / replayed / trim counters when incident-bot gets an observability surface (fold under D-INCIDENT-LIVE-SMOKE). | When incident-bot gets a metrics surface | LOW |
| 122 | 2026-06-01 P2 073 read-cmd batch (review-impl #2, audit boundary) | **D-ADMIN-TIER3-AUDIT-PGTEST** — tier-3 reads run with an empty `reason` (the >=10-char gate is tier-1/2 only) and emit a `result_kind=success` `admin_action_audit` + same-TX `meta_write_audit` pair. Traced valid (no reason column on admin_action_audit; migration-015 CHECKs are on command_name/version/actor/result_kind/scrubber-quad, none empty here) but UNTESTED end-to-end. Add a PG-gated case to metawrite_sink_pg_test.go (or a cmd/admin live-smoke) asserting the empty-reason tier-3 pair is written + passes the migration-015 CHECKs — the admin-cli→meta cross-service shape mock-only tests can't prove. | Next audit-path coverage pass | LOW |
| 121 | 2026-06-01 P2 073 read-cmd batch (review-impl #2, framework boundary); **ADDRESSED** 2026-06-03 P2 121 | **D-ADMIN-NOTWIRED-EXIT — ADDRESSED.** Collapsed the 9 near-identical per-command wiring blocks in `cmd/admin/main.go` into a `builders` map + one testable `wireCommandHandler` helper. A wiring-builder ERROR (config present but invalid — `archive fetch` with MINIO_* unset, a per-reality read without SHARD_DB_*, allowlist load fail, etc.) is now FATAL → reports the real error + `return 2`, instead of falling through to the tier-3 NotWiredHandler's calm "recognised but not wired" exit-0. The legit `(nil handler, nil err)` no-config path (no META_DATABASE_URL, or the rebuild gate off) stays non-fatal → the dispatcher's fail-closed tier policy decides (tier-3 informs; tier-1/2 error). Mirrors `buildAuditSink`'s own config errors (already `return 2`). Applied uniformly to all 9 wired commands (the 5 reads + 4 destructive), removing the false-success bug everywhere. 3 new `wirehandler_test.go` cases (fatal-error / no-config-stays-NotWired / success-registers); admin-cli build+vet+full suite green. | DONE | LOW |
| 120 | 2026-06-01 P2 073 read-cmd batch (archive fetch, D6) | **D-ARCHIVE-FETCH-STDOUT-STREAM** — `archive fetch` writes the raw blob ONLY to `--out_path`; with no out_path it prints metadata + header-integrity (binary is never streamed to stdout, since framework output is newline/JSON-wrapped text — registry description corrected to match). If raw blob-to-stdout (piping `admin archive fetch ... > x.parquet`) is wanted, add a binary-safe stdout path that bypasses the text/JSON envelope. | If piping the blob to stdout is needed | LOW |
| 119 | 2026-06-01 P2 073 read-cmd batch (archive fetch, D5) | **D-ARCHIVE-FETCH-DECODE** — `archive fetch` validates the LWP1 envelope HEADER (magic/version/footer-row_count vs manifest) but does NOT decode the Parquet body. Full row-decode would need the canonical parquet_writer.Decoder under services/archive-worker — admin-cli deliberately avoids that cross-service module dep (D4: ships its own thin minio wrapper + a source-grep ABI tripwire for the duplicated LWP1 constants). If full decode/inspection is needed, promote the LWP1 ABI + decoder to a shared sdks/go pkg (3rd consumer) and consume from both. | When archive-blob row inspection is needed | LOW |
| 118 | 2026-06-01 P2 073 read-cmd batch (drift-check, D2) | **D-DRIFT-LIVE-RESAMPLE** — `projection drift-check --sample_size` is accepted + validated but NOT applied in v1: the command reads the maintained `projection_drift_state` summary ledger (migration 0007 forbids widening it with sample-row payloads — "drift INVESTIGATION queries issued live by SRE"). Live re-replay sampling at sample_size is the integrity-checker's job (cycle-14 L3.E/F). Wire the CLI to drive a live re-sample when that path exists; until then the output states the value is not applied. | When the integrity-checker live-resample path is CLI-drivable | LOW |
| 117 | 2026-06-01 P2 073 read-cmd batch (investigation) | **D-073-CANON-CONFLICT-PERSISTENCE** — `admin canon conflict-list` cannot be honestly built: the `l1_conflict_reports` table is NEVER created in any migration; services/meta-worker/pkg/l1_conflict_reporter ships only an `InMemoryStore` (v1 in-process) despite a comment claiming "production binds to meta-DB l1_conflict_reports". Build: an `l1_conflict_reports` (meta) migration + persist the reporter's output (meta-worker) instead of in-memory, THEN the read command (the reporter already computes conflicts — Detector + Report types + a Save interface; only persistence is missing). | When canon conflict reports are persisted | MED |
| 116 | 2026-06-01 P2 073 read-cmd batch (investigation) | **D-073-BACKUP-LIST-PERSISTENCE** — `admin backup list` cannot be honestly built: NO `backup_snapshots`/`backup_manifest`/`backup_metadata` table exists in any migration. Backup infra is policy-driven scheduling (contracts/backup/policy.yaml + backup-scheduler) + the `lw-db-backups` MinIO bucket, but no SSOT metadata table to read (filtering by reality_id/scope would need a full bucket scan). Build: a `backup_snapshots` (meta) table + wire backup-scheduler to write a row per snapshot, THEN the read command (trivial on the established perRealityPool pattern once the SSOT exists). | When backup snapshot metadata is persisted | MED |
| 115 | 2026-05-31 P2 101 (review-impl #2, relay) | **D-META-OUTBOX-XSTREAM-ATOMICITY** — the relay's xreality bridge is both-or-neither at the LOGIC level (mark published only if home + xreality XADD both succeed) but NOT atomic at the transport: a row whose home XADD succeeds then xreality XADD fails re-XADDs the home stream on retry (a duplicate; home consumers dedupe on event_id). The obvious fix (Redis MULTI/EXEC across both streams) was REJECTED: home + xreality are different keys → likely different hash slots under Redis Cluster / ElastiCache cluster-mode → CROSSSLOT error on every cross-reality emit. The publisher's fanout is deliberately non-atomic for the same reason. So the chosen design is Cluster-safe at-least-once + idempotent consumers. If exactly-once cross-stream delivery is ever required, options: a hash-tag to co-locate the streams on one slot (sacrifices spread), or a per-stream published-marker column so retries skip the already-sent stream. | If duplicate home-stream events become a consumer problem | LOW |
| 114 | 2026-05-31 P2 101 (review-impl #2, producer slice) | **D-META-OUTBOX-PAYLOAD-PII** — `meta_outbox.payload` stores the UNSCRUBBED `NewValues` (MetaWrite deliberately gives the outbox the originals — the scrubber only redacts the `meta_write_audit` COPY). Benign for the only event wired in 101 (`user.consent.revoked` → opaque IDs). But a FUTURE caller wiring `cfg.Outbox` for a PII-bearing allowlisted event (e.g. auth-service `pii_registry` INSERT → `user.created`) would land raw PII in `meta_outbox.payload`, making the table's `@pii_sensitivity: low` header inaccurate. Mitigations in place: `@retention_class: ephemeral` + the pruner (112). Before any PII-bearing event is Outbox-wired: either scrub the outbox payload (changes the consumer contract) OR store only the PK + let the consumer re-read, OR re-classify the table. | When a PII-bearing allowlisted event gets Outbox-wired | MED |
| 113 | 2026-05-31 P2 101; **ADDRESSED** 2026-05-31 P2 113 (Option 1, human-in-loop + /review-impl ×2) | **D-KEK-DESTROY-VIA-METAWRITE — ADDRESSED.** `user.erased` now emits end-to-end. The original CDC-vs-domain envelope blocker (the MetaWrite outbox payload is CDC `{pk={kek_id},after}` with no `user_ref_id`, but 071 needs top-level `user_id`) is resolved by **Option 1 — a `MetaWriteIntent.OutboxPayload` override** carrying the canonical `XRealityUserErasedV1{user_id, erased_at}`. **Slice 1 (49eb3b84):** the override (emit DOMAIN, not CDC) + meta-outbox-relay xreality domain-field promotion (top-level `user_id`/`erased_at` for 071). **Slice 2:** `piikms.DestroyKEK` shreds via `MetaWriteBatch` (atomic total shred preserved — the multi-row model mismatch handled by batching ≤1-by-unique-index intents; CAS idempotency; **fails CLOSED** — an outbox-append failure rolls the shred back so `destroyed_at` stays NULL & admin retries); KMS co-tenant suppression unchanged; admin-cli wires the Outbox cfg + a `pii_kek` startup allowlist guard. Live-PG proven (shred → real `meta_outbox user.erased` domain row → relay → `xreality.user.erased` top-level `user_id`). **Remaining (separate):** the 071 consumer SERVICE live-wiring is its own task; co-tenant TOCTOU = 097. | DONE — downstream = 071 service wiring | MED |
| 112 | 2026-05-31 P2 101; **ADDRESSED** 2026-05-31 P2 112 | **D-META-OUTBOX-PRUNE — ADDRESSED.** `services/meta-outbox-relay/pkg/prune` + a second relay goroutine periodically DELETEs `published=TRUE` rows whose `last_attempt_at` is past a grace window (default 7d = `@retention_hot`; env `PRUNE_INTERVAL`/`PRUNE_GRACE`/`PRUNE_BATCH`). Bounded by a `ctid`+LIMIT sub-select (short locks, mirrors retention-worker's events_outbox prune). NEVER touches pending rows (the drain owns them) or dead-lettered rows (kept for SRE triage) — PG-gated test proves only the published+past-grace row is deleted; pending/dead-lettered/recent-published all survive. Bounds storage + PII residency (114). Metric `lw_meta_outbox_pruned_total`. | DONE | LOW |
| 111 | 2026-05-31 078-recon; **ADDRESSED** 2026-05-31 (all 15 lint-foundation gates GREEN) | **D-CI-GATE-GREENING — ADDRESSED.** `lint-foundation` (15-lint matrix) is now fully green → a real blocking gate. **Fixes:** (a) `meta-write-discipline` — encoded a narrow sanctioned-writer allowlist (path+table) for the 3 legit writers: publisher `metahb` (heartbeat liveness), world-svc `audit_writer` + meta-worker `pgwrite` (audit-self-writes — writing an audit row IS the audit, no MetaWrite path). (b) `role-grant-validator` — switched the declared-table scan to actual `CREATE TABLE` across meta + per_reality migrations (the 4 "unknown" tables events/events_outbox/event_audit/archive_state are per-reality; also dropped false ALTER-migration names) + allowed UPDATE (not DELETE) on `deploy_audit` (a state machine, not append-only). (c) `pii-classify` — classified `026 book_reality_subscription` (no-PII opaque-FK map; mechanical). **Also caught + fixed 3 regressions from THIS session's own services** (only surfaced by running the full 15-matrix, not the per-task subset): `capacity-budget` (added `meta-outbox-relay` to budgets.yaml), `outbox-event-emit` (exempted the relay's sanctioned XADD), `meta-sensitive-read-bypass` (exempted the 071 erasure owner-scoped lookup). **Remaining (NOT gate failures, separate lower-priority):** §12X.4 PII-envelope spec gap (028's provisional `pii_envelope` class — reconcile the spec matrix); stale observability PLANNED metric names; (078 residual) no Python-service CI job + no docker-compose live-smoke job. _Original analysis:_ after the 078 fixes, `lint-foundation.yml` still had **4 RED gates**, each needing per-item judgment (real violation vs sanctioned-exempt vs stale-config): (a) `meta-write-discipline` — `crates/meta-rs` (Rust meta-lib) now exempted (the MetaWrite impl, = `/contracts/meta/`); **3 writers remain**, each a sanctioned-vs-violation judgment: **publisher `metahb.go`** (publisher_heartbeats) = high-freq liveness, likely-EXEMPT; **world-svc `audit_writer.rs`** (service_to_service_audit) = audit-table self-write, likely-EXEMPT; **meta-worker `pgwrite.go`** (meta_write_audit, canon-projection apply) = **AMBIGUOUS** — could be a real I8 gap (should route its `canon_projection` write through `meta.MetaWrite`, which audits automatically) rather than direct-writing the audit row. Resolve by encoding a documented sanctioned-writer allowlist (liveness + audit-self-write) + deciding the pgwrite case (needs the I8/S04 §12T.6 author intent). (b) `role-grant-validator` — 4: matrix references `events`/`events_outbox`/`event_audit`/`archive_state` with no migration in the scanned path → tangled with the unbuilt meta-outbox layer (**101**); fix the scan path or the matrix. (c) `pii-classify` — **my 027/028/029 FIXED 2026-05-31** (027 meta_write_audit + 029 meta_read_audit tagged spec-exactly per the §12X.4 matrix: 5y/2y hot, crypto_shred_actor, legitimate_interest; 028 pii_kek tagged sensitive/crypto_shred/contract). 20→5 missing tags. **Remaining: (i) 026** `book_reality_subscription` — a book-service CREATE; its owner must classify (not mine). **(ii) §12X.4 SPEC GAP** — the retention matrix has NO PII-envelope row for `pii_registry`/`pii_kek`; 028 uses a provisional `pii_envelope` retention_class — add the real class to §12X.4 + reconcile. (d) `observability-inventory` — **GREENED 2026-05-31** (additively registered the 25 emitted spine-worker metric names in `inventory.yaml`; gate PASS, no code/dashboard change). **Residual (non-blocking) cleanup:** the older PLANNED names (`lw_meta_worker_dispatch_total`, `lw_archive_bytes_uploaded_total`, `lw_retention_outbox_pruned_total`, …) are stale-by-refactor (the code split/renamed them) — retire or align them once the dashboard/alert blast-radius is checked (renaming emitted metrics IS load-bearing). This residual is NOT a lint failure (the lint only flags emitted-not-declared). Green each (fix or allowlist-with-rationale) so lint-foundation becomes a real blocking gate. | Dedicated CI-greening session | HIGH |
| 101 | 2026-05-31 P2 076 Slice C (review-impl #1); **ADDRESSED** 2026-05-31 P2 101 (producer + relay) | **D-METAWRITE-OUTBOX-UNWIRED — ADDRESSED (Option B, human-in-loop + /review-impl ×2).** **Producer slice (550c3d58):** `migrations/meta/030_meta_outbox` + `sdks/go/metaoutbox.Appender` (same-TX INSERT) + `meta.LoadXRealityTopics`/`EventBinding.XRealityTopic` + `cfg.Outbox` wired in admin-cli (erasure + audit Sink) + `probeMetaOutbox` startup fail-fast. **Relay slice:** new `services/meta-outbox-relay` (Go) — drains `meta_outbox` (FOR UPDATE SKIP LOCKED, reuses publisher/pkg/retry) → XADD `lw.meta.events` + an xreality.* bridge (both-or-neither for compliance events); health/metrics; service_acl + language-rule + observability rows. So `user.consent.revoked` (erasure step 7) now durably emits AND drains to Redis — proven end-to-end on real PG + Redis. **Remaining (separate concerns, NOT 101):** `user.erased` emission = **113** (DestroyKEK bypasses MetaWrite); the `lw.meta.events`/xreality consumers = **071/106/108**; the published-row pruner = **112**. Original gap below. | DONE — downstream = 071/106/108/112/113 | HIGH |
| 110 | 2026-05-31 P2 072; **ADDRESSED** 2026-06-03 P2 110 | **D-BREACH-RACE-CI — ADDRESSED.** Added a targeted `-race` step to the foundation-ci `go` job: `go -C services/incident-bot test -race -count=1 ./internal/breach` (ubuntu-latest has gcc). The breach Monitor's HTTP `Track()` vs ticker `tick()` concurrency is now race-detector-verified on every push. Bonus: `-race` DID build locally this session (the 2026-05-31 no-cgo note was stale) and the package is race-clean both locally + in CI. | DONE | LOW |
| 109 | 2026-05-31 P2 072 (Slice-C-style lean MVP) | **D-BREACH-I18N-TEMPLATE** — the DPO notice subject/body are composed inline by gdpr_breach_flow; the EN+VI `comms_template` Library exists but was not wired (kept the MVP lean). Add a `gdpr_dpo_breach_notice` template (EN+VI) + render it in the breach path for i18n notices. | When DPO i18n notices matter | LOW |
| 108 | 2026-05-31 P2 072 (design); **ADDRESSED** 2026-06-01 P2 108 | **D-BREACH-BROKER-EMITTER — ADDRESSED.** `breach.RedisEmitter` XADDs the 3 lifecycle events to the `lw.incidents.breach` Redis stream (event_type + incident_id + full-JSON payload; validate-before-emit, fail-closed). incident-bot's main selects it when REDIS_URL is set, else the 072 stdout StructuredEmitter (dev). Stream bounded by a TIME-based MINID trim (DefaultTrimHorizon 7d ≫ the 72h Art.33 deadline → anchor-safe; a count-MAXLEN was REJECTED — it could evict a still-open breach's `opened` anchor). Sanctioned in outbox-event-emit-lint (I13): these are incident-bot's own operational/compliance emissions, NOT event-store aggregates, so there is no outbox/publisher path. Proven on real Redis (emit→replay roundtrip). | DONE | MED |
| 107 | 2026-05-31 P2 072 (design); **ADDRESSED** 2026-06-01 P2 107 | **D-BREACH-DURABLE-STORE — ADDRESSED.** On boot (when the Redis broker is active) `ReplayOpenBreaches` XRANGEs `lw.incidents.breach` and `reconstructOpen` rebuilds the deadline Monitor's open-set (opened minus terminal-missed; LAST-EVENT-WINS for same-id re-declaration), so the 72h reminders survive a restart. incident-bot STAYS STATELESS — the Redis stream is the durable log, no DB (Q-L7-1). At-least-once approaching reminders on restart are accepted (a dup reminder is safe; a missed one is not) + test-pinned. A boot Redis failure DEGRADES to the in-process path (does NOT crash the multi-responsibility service). | DONE | HIGH |
| 106 | 2026-05-31 P2 072 (design); RE-SCOPED 2026-06-01; **ADDRESSED** 2026-06-01 P2 106 | **D-BREACH-DELIVERY-CONSUMER — ADDRESSED.** New standalone foundation service `services/breach-notifier`: XReadGroup consumer on `lw.incidents.breach` (group starts at "0" so a notice emitted while down is NOT missed — H1) + XAUTOCLAIM reclaim of crashed-before-ack entries (H2) → delivers the DPO notice via a pluggable `Notifier` (default `LogNotifier` = a genuine audit-line delivery incl. body; `SlackNotifier` scaffold opt-in, real call = 125) → records a durable **delivery-confirmed** row (`breach_dpo_delivery`, idempotent on delivered; delivered_at = the confirmation distinct from "emitted"). NOT routed through notification-service (wrong module/transport/semantics). Cold-start /review-impl: 2 HIGH (H1 start-pos, H2 PEL) + MEDs fixed; at-least-once semantics tracked (124), Slack-live (125). Live-proven on real Redis+PG. | DONE — downstream = 124/125 | HIGH |
| 105 | 2026-05-31 P2 076 Slice C (AV false-positive) | **D-WINDOWS-CODE-SIGN** — unsigned static Go binaries that link AES-GCM + AWS-KMS crypto-shred (the `admin` CLI) trip Bitdefender's `Gen:Variant.Tedy.*` ML heuristic as a FALSE POSITIVE (the encrypt-then-destroy-key shape resembles ransomware). Harmless for the cloud/Linux product (end users get the web app; admin-cli runs server-side on Linux), but before distributing ANY Windows binary: Authenticode code-sign with a trusted publisher cert + submit FP reports to flagging AV vendors. Evidence + remediation in `docs/SECURITY-FALSEPOSITIVE.md`. | Before any Windows binary distribution | LOW |
| 104 | 2026-05-31 P2 076 Slice C; **ADDRESSED** 2026-06-03 P2 104 | **D-READAUDIT-ENUM-DRIFT — ADDRESSED.** Reconciled the `meta_read_audit.query_type` CHECK ↔ SSOT to a 7-id set (migration **031**): added `bulk_meta_query` (contract had it, CHECK lacked it), kept `bulk_pii_read` + added it to the YAML (the contracts/pii SDK actively writes it — the YAML was the incomplete side, not the CHECK), and DROPPED `unbounded_select` + `consent_audit_export` (orphans — never written by any code, never in the contract). New CI gate `scripts/read-audit-query-type-drift-lint.sh` (in lint-foundation matrix) asserts CHECK == YAML so they can't silently drift again (negative-tested: FAILs on injected drift). Go + Rust loader tests extended to the full 7-id set. _Original:_ migration 014 CHECK and the SSOT YAML had drifted; 029 only added the 2 PII ids. | DONE | MED |
| 103 | 2026-05-31 P2 076 Slice C (step 10) | **D-ERASURE-CERT** — S08 §12X.6 step 10 (full-erasure certificate issued ~30d post-shred, after KMS key destruction + backup expiry) is not implemented — it is inherently asynchronous (a scheduled job 30d after the shred), not a synchronous admin-cli step. Build the 30d certificate issuance (scheduler + signed cert artifact to the user's last-known contact). | When the erasure-SLA / cert artifact is built | LOW |
| 102 | 2026-05-31 P2 076 Slice C (step 9) | **D-ERASURE-EMAIL** — S08 §12X.6 step 9 (confirmation email to the user's last-known address within 72h) is a logged STUB in the erasure orchestrator — admin-cli has no notification path. Wire it to the notification service (or an SMTP/SES sender) when that path exists. | When a notification path from ops tooling exists | LOW |
| 101 | 2026-05-31 P2 076 Slice C (review-impl #1) | **D-METAWRITE-OUTBOX-UNWIRED** — NO caller wires `meta.Config.Outbox` to a production `OutboxAppender` anywhere (none exists; no outbox-table migration in `migrations/meta/`), so MetaWrite runs `Outbox=nil` platform-wide and EVERY allowlisted event (`user.consent.revoked` from erasure step 7, `user.erased`=step 5, the consent grant/revoke events, …) is silently NOT emitted — the data / `revoked_at` row state is the SSOT. Build the production OutboxAppender (the publisher/054 drains the outbox→Redis) + set `cfg.Outbox` in every MetaWrite caller (incl. the admin-cli erasure handler + the audit sink). Erasure steps 4+5 (per-reality cascade) are additionally gated on **071** (the projection-side consumer). | When the MetaWrite→outbox→publisher emit layer is wired | HIGH |
| 100 | 2026-05-31 073-prereq; **ADDRESSED** 2026-06-03 P2 100 | **D-SCRUB-BINARY-FIDELITY — ADDRESSED.** `contracts/meta` `ScrubValue` is now utf8-aware: a new `scrubBytes` helper runs the 7 PII text regexes over a `[]byte` ONLY when `utf8.Valid` (text); a BINARY blob (e.g. the 32-byte `error_detail_raw_hash`) passes through VERBATIM, so a chance regex match can no longer mutate it in the `meta_write_audit` copy. Applied to BOTH `[]byte` paths (the `case []byte` fast path + the reflect Slice-of-Uint8 fallback). New `TestScrubValue_BinaryBytesPassThrough` (binary unchanged + the text/binary discriminator); the existing text-`[]byte`-still-redacted test is preserved. admin-cli MetaWriteSink forensic-note updated. | DONE | LOW |
| 099 | 2026-05-31 073-prereq; **ADDRESSED** 2026-06-03 P2 098/099 | **D-ADMINAUDIT-ERROR-TEXT — ADDRESSED.** The dispatcher now passes the RAW handler error to `emitter.Failure`; the Action carries it (`ErrorDetailRaw`, in-memory only — the stdout sink still logs only the hash) and the `MetaWriteSink` scrubs it through `cfg.Scrubber` (RegexScrubber) into the real scrubber-quad (`error_detail_scrubbed` = redacted text, `scrub_version` = regex-v1, raw_hash, scrubbed_at) — replacing the hash-only sentinel. PG-test proves a PII email is redacted out of the stored text while non-PII context is retained. | DONE | MED |
| 098 | 2026-05-31 073-prereq; **ADDRESSED** 2026-06-03 P2 098/099 | **D-ADMINAUDIT-INPROGRESS — ADDRESSED.** Migration 032 adds `'started'` to the `admin_action_audit` result_kind enum; the `MetaWriteSink` now persists the framework's Before `started` row for tier-1-destructive / tier-2-griefing commands (skipped for read/informational tiers to avoid doubling low-value audit volume). A destructive command killed between Before and the terminal hook now leaves a durable forensic `started` row; the Before write landing before the destructive op means a Before-write failure correctly aborts the command unaudited. | DONE | MED |
| 097 | 2026-05-31 P2 076 Slice B; **ADDRESSED** 2026-06-03 P2 097 | **D-PIIKMS-DESTROY-TOCTOU — ADDRESSED.** Two concurrent co-tenant erasures on a misconfigured SHARED CMK could both observe count=0 and both `ScheduleKeyDeletion`. **Fix chosen: idempotent KMS handling, NOT a serializing lock.** The prescribed "hold a lock across the UPDATE+count" was REJECTED on review — holding a pooled conn's advisory lock while blocked means N waiters pin N pool conns, risking pool-exhaustion DEADLOCK on the GDPR erasure path (a deadlocked erasure is a compliance failure, far worse than the benign, KMS-recoverable double-schedule). Instead `maybeScheduleDeletion` now treats a KMS "… is pending deletion" response as SUCCESS (`isAlreadyPendingDeletion`): the count-suppression still blocks scheduling when a LIVE co-tenant exists, and in the concurrent both-destroyed race KMS is the dedup point — net exactly-once, no locking, no deadlock. New PG-gated `TestDestroyKEK_ConcurrentCoTenants_ExactlyOneSchedule` (race-detector-clean; fake KMS models the already-pending dedup). NOTE: piikms PG tests are local/manual only (no PIIKMS_TEST_PG_URL in CI). | DONE | LOW |
| 096 | 2026-05-30 P2 074/075 (code-review WARN) | **D-ADMIN-ISSUANCE-DENY-AUDIT-DURABILITY** — admin/break-glass SUCCESS issuance audits are fatal (no token without a durable row), but DENY/ERROR issuance audits are best-effort: `auditBestEffort` logs an ERROR (+ should emit a metric) if the append-only INSERT fails, but does NOT fail the request. Under DB pressure a deny/abuse-probe row can still be dropped while the API returns a clean 4xx, softening the "audit EVERY attempt" promise for the deny class. Add retry/dead-letter (or a local spool) so deny/error rows are durable without coupling issuance availability to DB health; add the failure metric. | Pre-prod audit hardening | LOW |
| 095 | 2026-05-30 P2 074/075 (key rotation) | **D-ADMIN-JWT-JWKS-ROTATION** — V1 distributes the admin-signing PUBLIC key to admin-cli as a static `ADMIN_JWT_PUBLIC_KEY_PEM` env (kid-bound to the key fingerprint, so a stale key fails loudly). No rotation path: rotating the KMS key requires re-distributing the PEM to every admin-cli. Add a JWKS/public-key endpoint on auth-service (served from KMS `GetPublicKey`, multi-kid during overlap) + admin-cli JWKS fetch/cache so keys rotate without manual PEM redistribution. | When key rotation is operationalized | MED |
| 094 | 2026-05-30 P2 074/075 (KMS live-smoke) | **D-ADMIN-JWT-KMS-LIVE-SMOKE** — the RS256 wire path (sign→assemble→verify) is proven by unit tests via an in-process `LocalRSASigner` (real `rsa.SignPKCS1v15`, byte-identical to KMS output) + a golden/header-pinned vector, but the LIVE `kms.Sign` RPC is NOT live-smoked: LocalStack-community asymmetric Sign is historically unfaithful to real RSA (localstack#7216), so a LocalStack-issued signature may fail golang-jwt's verifier. Live-smoke against REAL AWS KMS (or a faithful KMS): create an RSA SIGN_VERIFY key, mint a token via the live endpoint, verify with admin-cli; then capture the real `GetPublicKey` SPKI-DER and commit it as a `contracts/adminjwt` testdata fixture (replacing the self-marshaled fixture). | When real AWS KMS is reachable (pre-prod) | HIGH |
| 093 | 2026-05-30 P2 074/075 (replay) | **D-ADMIN-JWT-JTI-DENYLIST** — minted admin/break-glass tokens carry a unique `jti` (uuidv7, recorded in `admin_token_issuance_audit`), but verification is stateless: a leaked bearer token replays for its full TTL (admin ≤15m; break-glass ≤24h). V1 mitigates via short TTL + recorded jti for forensics. Add a consumption-side one-time-use / jti-denylist check (e.g. admin-cli or a verify service consults a revocation/seen-jti store) for true anti-replay. | When anti-replay is required (pre-prod) | MED |
| 092 | 2026-05-30 PR-#11 CI-green pass (world-gen golden) | **D-WORLDGEN-XPLATFORM-GOLDEN** — `crates/world-gen` `flatworld_v3_0_byte_identical_seeds_1_7_13_42_100` is now `#[ignore]`d in CI: its golden hashes were captured on the geo dev's Windows/MSVC host, but the hash folds raw `f64::to_le_bytes()` of geometry coords whose low bits differ between MSVC and glibc libm (trig/sqrt rounding), so the byte-identical gate FAILS on the Linux CI runner despite logically-identical geometry. Still a valid LOCAL regression-lock (`cargo test -p world-gen -- --ignored`). To restore it as a CI gate: round coords to a fixed precision (or hash logical structure) before hashing, then rebaseline. Owned by the geo-generator track, NOT the foundation P1 spine. | When the geo track hardens determinism | LOW |
| 091 | 2026-05-30 P1 embedding-queue; **ADDRESSED** 2026-06-03 P2 091 | **D-EMBEDDING-TESTKIT — ADDRESSED.** Extracted `world_service::embedding_queue::testkit` (MemWriter [union of both copies' accessors + `failing()`], StubProvider, DeterministicEmbedder, WrongDim/ErrorProvider, NoopWriter) gated `#[cfg(any(test, feature = "testkit"))]`; the external `tests/` crate enables it via a self dev-dependency (`world-service = { path = ".", features = ["testkit"] }`). Repointed all 3 sites (embedding_queue/mod.rs unit tests, live/worker_loop.rs tests, tests/embedding_retrieval_test.rs) — removed ~224 duplicated lines. world-service build + 26 unit + integration tests + clippy + workspace build green. | DONE | LOW |
| 090 | 2026-05-30 P1 embedding-queue (059 /review-impl correctness+security) | **D-EMBEDDING-AUDIT-DURABILITY** — `MetaAuditWriter::record` logs-and-continues on a `service_to_service_audit` INSERT failure (so a meta-DB outage does NOT crash the embedding drain). Deliberate availability tradeoff but a soft Q-L1A-3 "full audit, no sampling" gap: rows are silently dropped exactly when the meta DB is unhealthy. Add retry / dead-letter (spool to a local outbox or the per-reality DB) so audit writes are durable without coupling embedding availability to meta-DB health. Also: `latency_ms` is written as 0 (the `AuditEvent` carries no timing) — thread real provider-call duration through when the provider lands (fold into 089). | Pre-prod (Q-L1A-3 hardening) | MED |
| 089 | 2026-05-30 P1 embedding-queue; **ADDRESSED** 2026-06-02 P2 089 | **D-EMBEDDING-PROVIDER-WIRING — ADDRESSED.** `HttpEmbeddingProvider` (reqwest → provider-registry `POST /internal/embed`, X-Internal-Token) replaces the fail-closed `NotWiredProvider`, using a PLATFORM embedding credential (env `EMBEDDING_PLATFORM_USER_ID`/`MODEL_REF` — system embedding is platform-paid, matching `system_only` audit; no trait change). Request shape pinned by a wiremock test; pure `parse_embed_response`; build/clippy/26-tests green; cold-start /review-impl (1 HIGH request-coverage + MEDs fixed). Deferred: per-reality attribution (126), token accounting (127), live round-trip (128). _Original:_ bind the embedding queue's `EmbeddingProvider` to the BYOK gateway. The route + s2s auth **already exist**: provider-registry-service `POST /internal/embed` behind `internalAuthMiddleware` (shared internal-secret header; `internal/api/server.go`), taking `?user_id=<uuid>` + `{texts, model_ref}` → vectors. Remaining: (1) a Rust **reqwest** `EmbeddingProvider` (first reqwest in world-service) calling that route with the internal-secret header; (2) `reality_id → owner user_id` resolution (no existing Rust accessor — the endpoint needs `user_id` to select the owner's BYOK credential); (3) embedding `model_ref` selection; (4) per-call latency capture into `AuditEvent` + non-`Ok` token accounting; (5) end-to-end live-smoke (provider-registry up + seeded BYOK credential + local embeddings upstream). Replaces the fail-closed `NotWiredProvider` the `embedding-worker` bin wires today. | When BYOK embedding goes live / pre-V1 | HIGH |
| 088 | 2026-05-30 P1 retention-worker; **ADDRESSED** 2026-06-03 P2 088 | **D-OUTBOX-PRUNE-INDEX — ADDRESSED.** Migration `per_reality/0012_events_outbox_prune_index` adds the partial index `events_outbox_prune_idx ON events_outbox (last_attempt_at) WHERE published = TRUE AND dead_lettered_at IS NULL` — exactly the retention-worker prune set, turning the inner `ctid IN (SELECT … last_attempt_at < cutoff … LIMIT N)` seq/partial scan into an index range scan at scale. Idempotent (IF NOT EXISTS / down DROP IF EXISTS), index-only, non-breaking. The retention-worker live-smoke (`tests/integration/retention_worker_live_smoke_test.go`) now applies 0012 so the prune runs against the index in CI. | DONE | LOW |
| 087 | 2026-05-30 P1 archive-worker (057 /review-impl) | **D-ARCHIVE-DETACH-FIRST** — `pgio.RowSource.LoadPartition` does a plain `SELECT` then `archive_loop` later `Drop`s the partition. `events.recorded_at` is `DEFAULT NOW()` but SETTABLE, so a backdated INSERT into an already-past-cutoff partition BETWEEN the read and the DROP would be dropped unarchived (data loss). Not exploitable today (the foundation has no backdated-write path). Fix = the deferred "ATTACH-to-staging" pattern: DETACH the partition FIRST (so no new routed inserts) → read the detached table → drop. Reorders the archive_loop orchestration / Dropper contract. | When a backdated-write path exists / hot-partition archive | MED |
| 086 | 2026-05-30 P1 task #18 (meta-worker live-wiring); **ADDRESSED** 2026-06-03 P2 086 | **D-REALITYREG-SHARED — ADDRESSED.** Promoted `realityreg` (ActiveRealities + shard-host→DSN resolver, code byte-identical) out of `services/publisher/pkg/realityreg` into a new peer module `contracts/realityreg` (own go.mod, pgx-only dep). Repointed all 4 importers — publisher, meta-worker, retention-worker, archive-worker (the cross-import was wider than the row noted: 3 workers, not just meta-worker) — plus `tests/integration/publisher_live_smoke_test.go`, to the new path. The 3 workers DROPPED their `replace services/publisher => ../publisher` + publisher require entirely (they used publisher *only* for realityreg); publisher itself now consumes the shared module; tests/integration added the realityreg replace. meta-outbox-relay keeps its publisher replace (uses pkg/retry, unrelated). All 5 modules build+test green; I3 language-rule PASS (contracts/ exempt; CI go-job auto-discovers via `find . -name go.mod`). No service depends on another service for realityreg anymore. | DONE | LOW |
| 085 | 2026-05-30 P1 task #18 (meta-worker live-wiring) | **D-METAWORKER-PEL-RECLAIM** — the meta-worker consumer reads only NEW messages (XREADGROUP `>`); a consumer that crashes between read and XACK leaves its message stuck in the group's pending-entries-list (PEL) forever (no XAUTOCLAIM/XPENDING recovery). `consumer.go` already flags this as an "operational concern". Add a periodic XAUTOCLAIM sweep (min-idle-time) so a crashed consumer's in-flight messages are reclaimed. | Before multi-replica meta-worker / prod | MED |
| 084 | 2026-05-30 P1 task #18 (meta-worker live-wiring) | **D-METAWORKER-GROUP-BOOTSTRAP** — `redisconsume.EnsureGroups` creates the consumer group at `$` (only messages from group-create time). If the publisher fans canon events BEFORE the meta-worker has ever booted (first-deploy ordering), those pre-group messages are lost. Create the consumer group as an infra/provisioning step before the publisher starts, OR seed from `0` on first boot with idempotent projection writes (UPSERT already idempotent). | First prod deploy ordering | MED |
| 083 | 2026-05-30 P1 task #16 (publisher live-wiring) | **D-PUBLISHER-V2-LEADER-ELECTION** — publisher V1 ships a no-op leader (`leader_election.NewNoOp`, Q-L2-5) and runs single-replica per shard (Q-L2D-1). The DEFERRED-054 tx refactor (Source→Batch holds the FOR UPDATE SKIP LOCKED lock across the batch) now makes the lock load-bearing, so V2 multi-replica is UNBLOCKED but not built: needs a real leader (Redis SETNX / lease) + a shard→reality assignment so N replicas partition the reality set without double-draining. Trigger: >1000 active realities (Q-L2D-1) or HA requirement. | V2 multi-replica (when reality count / HA demands) | MED |
| 082 | 2026-05-30 P1 task #16 (publisher live-wiring) | **D-PUBLISHER-DEP-REGISTRY** — `services/publisher/cmd/publisher/main.go` constructs `redis.NewClient` directly, flagged by `scripts/dependency-registry-lint.sh` (WARN-mode, exit 0 — same as the pre-existing `worker-infra` + `statistics-service` hits). Route through `contracts/dependencies.ClientFactory.For(...)` when the repo-wide ClientFactory migration flips the lint to error-mode (existing cycle-19+ debt, SR06 §12AI.2). | When dependency-registry-lint flips to error-mode | LOW |
| 081 | 2026-05-30 P1 task #16 (publisher live-wiring) | **D-PUBLISHER-REALITY-REFRESH** — the publisher loads the active reality set ONCE at startup (`realityreg.ActiveRealities` → per-reality pools), so realities created/activated after boot are NOT drained until the publisher is restarted. V1 simplification (operators restart on reality add). Future: periodic re-scan of `reality_registry` + dynamic pool add/remove (open pool on new active reality, close on archived/dropped), guarded against the open-tx mid-drain. | When dynamic reality churn matters (pre-V2 ops) | MED |
| 080 | 2026-05-30 post-RAID review (completeness-critic) | **D-META-SINK-TABLES** — meta tables 008 (session_cost_summary), 018 (user_cost_ledger), 019 (user_daily_cost), 020 (user_queue_metrics), 024/025 have ZERO INSERT writers (verified) — unpopulated sinks; only 008's worker was tracked (066). Also `infra/k8s/keda/session-cost-rollup-worker.yaml` is an ORPHANED ScaledObject targeting a nonexistent worker + a `lw_session_cost_rollup_lag_seconds` metric no code emits. Scaffold the writers OR downgrade the tables to documented stubs + remove/annotate the KEDA manifest. | L7 ops / when cost+usage tracking goes live | MED |
| 079 | 2026-05-30 post-RAID review (completeness-critic) | **D-WORLD-TRAVEL-DOMAIN** — `services/world-service` (GEO_001) + `services/travel-service` (TVL_001..005) `main.rs` are Cycle-5 scaffold binaries ("awaits DP-kernel"); the actual game-domain aggregates/projections are NOT implemented and had no deferred row. Post-foundation domain work (the foundation is the substrate), but must be tracked so it is not assumed done. | Post-foundation domain cycles | MED |
| 078 | 2026-05-30 post-RAID review (completeness-critic) | **D-CI-GATE-COVERAGE** *(CORRECTED + PARTIAL 2026-05-31 — the original claim was largely STALE)* — **I2 IS CI-enforced** (`foundation-ci.yml:70` runs `lint-no-direct-llm-imports.sh`), and `lint-foundation.yml` runs **15 invariant validators** via matrix on every PR/push. The real gap is NOT coverage but that **lint-foundation is RED** — 5 wired gates fail on the tree. **Fixed 2026-05-31:** `service-acl-matrix` (added the missing `canary-controller` ACL entry → GREEN) + `meta-write-discipline-lint` now exempts `_test.*` (test fixtures aren't production write-discipline violations; cleared the 2 hits 072's PG-test introduced, 6→4). **Remaining red gates → 111.** **ADDRESSED 2026-05-31 (via 111): all 15 lint-foundation gates now GREEN.** Residual true-coverage gaps (NOT gate failures, lower priority): no Python-service CI job; no docker-compose live-smoke job (PgEventStore PG path stays skip-green); these can be a separate CI-coverage pass. | Superseded by 111 (gates green); residual coverage = separate pass | HIGH |
| 077 | 2026-05-30 post-RAID review (PRR-20 — I1 amendment provisional) | **D-GAME-WS-EDGE-CONTROLS** *(ADDRESSED 2026-06-03 — full ticket chain: gateway issues to a shared Redis `RedisTicketStore` (config-gated `LW_WS_REDIS_URL`, atomic GET+DEL Lua, base64 wire form per 068); game-server `onAuth` redeems one-shot + validates + STRICT-binds origin+fingerprint (constant-time) with §12AB.9 close codes; #2 per-user connection cap + per-conn message-rate cap (clears 035); #3 structured-log connection-lifecycle audit. 8 gateway jest + 29 game-server node:test; cross-impl golden fixture in all 3 impls. Residual: live-smoke 133, audit-event-stream 134, forced-disconnect 135, tls-session-id 136, AWS-IaC #4 → 137, redis-HA 138.)* — I1 was amended to sanction game-server Colyseus WS as a 2nd public entry point; provisional until its edge controls are verified: (1) WS handshake validates the gateway-issued JWT/ticket (no anonymous sockets); (2) per-connection + per-user caps at the game-WS edge; (3) connection lifecycle (join/leave/kick) emitted to the audit pipeline; (4) AWS security groups expose ONLY api-gateway-bff + game-server publicly. | Before game-server prod exposure | HIGH |
| 076 | 2026-05-30 post-RAID review (PRR-01-seam/02/03/04 — PII production) | **D-PII-PRODUCTION** *(ADDRESSED 2026-05-31 — Slices A+B+C all done, AMAW design+code rounds + /review-impl; Slice C: erasure orchestrator shipped, see "Recently cleared". Residuals tracked as 101-105.)* — F2 shipped the real 7-pattern RegexScrubber + logging PII mask floor; GDPR-load-bearing remainder. **Slice A ADDRESSED:** (1) scrubber wired into the metawrite audit seam — `meta.Config.Scrubber` (optional), `ScrubValue`/`ScrubValuesMap` (immutable deep-copy, fail-closed reflect fallback covering typed containers/[]byte/named-strings/structs-via-JSON/map-keys, cycle-guarded, `ScrubText` fast-path); `writeOneInTx` scrubs the audit COPY of row_pk/before/after/reason/request_context ONLY (data write + outbox keep originals — the r1-BLOCK); migration **027** `scrub_version` col + `BuildAuditInsert` (live-verified pg16); meta-worker `pgwrite` 2nd writer enumerated (PII-safe-by-construction, accepted). (5) **PRR-45** — admin-cli `audit_emitter` now scrubs `Reason` before persist (defense-in-depth ahead of the deferred Sink adapter). **Slice B ADDRESSED (2026-05-31):** new `sdks/go/piikms` module — real AWS-KMS 3-tier envelope adapter (`AWSKMSClient` ProvisionKEK/Encrypt/Decrypt; `meta.DecryptInput` amended +`KeyMaterial`; `meta.PIIAAD` shared builder; version-bound AEAD AAD) + `PgKEKManager.DestroyKEK` (set-based total shred + per-CMK co-tenant guard + `ScheduleKeyDeletion`); migration **028** UNIQUE-active-KEK index; `pii.KEKManager.DestroyKEK`/`ErasePII` +ticket/reason. Real-KMS round-trip (LocalStack) + pg16 `DestroyKEK` live-smoked. **Slice C ADDRESSED (2026-05-31):** `admin-cli` `erasure user-erasure` wired to the full S08 §12X.6 10-step orchestrator (`internal/commands/erasure.go`) — steps 1 (legal-basis) / 3 (crypto-shred via `pii.SDK.ErasePII`→Slice B) / 7 (consent-revoke via MetaWrite) are REAL + an existence guard before the irreversible shred; steps 2/4/5/6/9/10 honest-deferred/stub (→ 071, 101-103). New production drivers `piikms.PgPIIReader` + `PgReadAuditWriter`; migration **029** (adds `pii_user_get`/`pii_user_erase` to the `meta_read_audit` query_type CHECK so ErasePII's mandatory audit row is accepted); `contracts/meta` query-builder fix (nil `ExpectedBefore` → `IS NULL` CAS, for the consent single-revoke). AMAW design (2 BLOCK/1 WARN) + code (1 BLOCK/2 WARN) + /review-impl (3, all fixed). Live-smoked on pg16 + LocalStack-KMS (ErasePII→`meta_read_audit` row lands; PgConsentRevoker IS-NULL-CAS idempotency; cost proxy). KEK rotation (`erasure kek-rotate`) still command-body-pending (073). | ADDRESSED 2026-05-31 | — |
| 075 | 2026-05-30 post-RAID review (PRR-30 break-glass JWT) | **D-BREAK-GLASS-JWT** *(ADDRESSED 2026-05-30 — see "Recently cleared")* — auth-service `POST /internal/admin/break-glass-token` mints a 24h-max `break_glass=true` RS256 JWT, dual-actor gated (both actors present their OWN normal admin token — break-glass tokens rejected as approvers; threat model = elevated-authority, PO-locked) with the shared `adminjwt.ValidateBreakGlass` policy; durable own-DB audit (reason stored as keyed HMAC + len only). | ADDRESSED 2026-05-30 | — |
| 074 | 2026-05-30 post-RAID review (PRR-29 remainder) | **D-ADMIN-CLI-JWT** *(ADDRESSED 2026-05-30 — see "Recently cleared")* — admin-cli `auth.Validate` now does real RS256 verification (shared `contracts/adminjwt`, public key from `ADMIN_JWT_PUBLIC_KEY_PEM`, kid-bound to key fingerprint, strict alg/iss/aud/exp; `dev:` shortcut still env-gated; callers unchanged). auth-service `POST /internal/admin/token` signs via AWS KMS asymmetric Sign (private key never leaves KMS). | ADDRESSED 2026-05-30 | — |
| 073 | 2026-05-30 post-RAID review (PRR-05 remainder) | **D-ADMIN-CLI-LIVE-WIRING** *(BLOCKER CLEARED 2026-05-31 — the MetaWrite adapter is BUILT)* — the D-ADMIN-CLI-METAWRITE blocker is resolved: new shared **`sdks/go/metapg`** (the pgx `meta.DB` driver — MetaWrite's first production driver) + admin-cli **`MetaWriteSink`** (Action→admin_action_audit via MetaWrite, hash-only, all 015 CHECKs, Slice-A-scrubbed) + `main` wiring behind `META_DATABASE_URL` (dev-token/unaudited guards). Live-smoked on pg16 (AMAW design×3 + code×1 + /review-impl). **COMMAND BODIES shipped 2026-05-31/06-01:** `reality stats` (reality_registry SELECT) + `migration status` (instance_schema_migrations aggregation) + **`archive list` (2026-06-01 — per-reality `archive_state` SELECT; this also BUILT the reusable per-reality pool access for admin-cli: a `perRealityPool` helper that resolves a reality's `db_host`/`db_name` from reality_registry + builds the shard DSN from `SHARD_DB_*` env [+ `SHARD_DB_HOST_OVERRIDE` for dev], unlocking the per-reality read-only command tier)** — each = reader iface + pgx impl + `build…Handler` gated registration + least-privilege ACL grant + unit + PG-gated reader tests. **REMAINING:** the other ~30 command bodies — read-only tier (backup list, projection drift-check, canon conflict-list, erasure pii-export) now mostly straightforward (meta-backed reuse reality-stats pattern; per-reality reuse the new perRealityPool helper); the destructive tier-1 ones (capacity_override, catastrophic_rebuild, rebuild_projection, …) need per-command `MetaWriter`/`FreezeOverrideWriter` + careful review. | L7 ops / incremental per command | HIGH |
| 072 | 2026-05-30 post-RAID review (PRR-31 GDPR Art.33) | **D-GDPR-BREACH-WIRING** *(ADDRESSED 2026-05-31 — AMAW design 2B/1W + code 1B/2W + /review-impl 3; see "Recently cleared")* — gdpr_breach_flow now has a LIVE path: authenticated `POST /internal/breach` (incident-bot, `internal/breach`) starts the 72h clock, emits the breach lifecycle as events (new `incidents` types GDPRBreachOpenedV1/DPONoticeRequiredV1/BreachDeadlineV1), and a race-safe self-pruning Monitor flags approaching/missed. X-Internal-Token auth (fail-closed) + detected_at anti-forgery clamp. Honoring Q-L7-1 (decide+emit), so DPO DELIVERY + durable persistence are downstream/deferred (106-110). Live-smoked (real process). | ADDRESSED 2026-05-31 | — |
| 071 | 2026-05-30 post-RAID review (PRR-09 remainder); **ADDRESSED** 2026-05-31 (Slice 1 `62a1a329` + Slice 2, human-in-loop + /review-impl) | **D-PROJECTION-ERASURE — ADDRESSED.** The GDPR erasure cascade is wired live end-to-end. **Slice 1:** `services/meta-worker/pkg/user_erased_writer/pglive` — `PgUserRealityLookup` (meta `player_character_index` DISTINCT reality_id) + `PgPerRealityScrubber` (per-reality `UPDATE pc_projection SET name='[erased]', status='deleted'`, idempotent; the ONLY per-reality projection with `user_id`). **Slice 2:** extended the Writer with an optional `MetaScrubber` → `PgMetaScrubber` scrubs the 2nd PII copy (meta `player_character_index.pc_name`) via `MetaWriteBatch` (self-audited); wired `HandleUserErased` into meta-worker `main` (registered for `xreality.user.erased`, consuming that stream; `META_ALLOWLIST_PATH`-gated meta scrub w/ graceful degrade); V1 structured-log `AuditSink`; reclassified the F4 projection-coverage allowlist entry from DEFERRED-GAP → by-design (meta-worker consumer, not a read-model projection). **/review-impl:** the relay now emits `event_type` = the xreality topic so consumers route explicitly (not stream-name fallback). **Live-PG proven:** Handle scrubs BOTH `pc_projection` + `player_character_index` + writes meta_write_audit + idempotent re-delivery. **Remaining (separate/deferred):** durable per-reality scrub audit (V1 = structured log); `pc.index.status.changed` emission on the meta scrub (meta-worker Outbox nil); audit Outcome="scrubbed" even on no-op re-run (ScrubUserRefs has no rows-affected signal). | DONE | HIGH |
| 070 | 2026-05-30 post-RAID review (PRR-28 defense-in-depth) | **D-LOGGING-PROD-TAG** — F2 added a build-tag-agnostic PII mask FLOOR (raw PII never leaks). Defense-in-depth remainder: add `-tags=prod` to every deployable Go Dockerfile + CI release path (+ a CI assertion that release artifacts are prod-tagged) so the compile-time DEBUG-drop + Sensitive-drop + NoopRedactor-rejection also engage. | When deployable Dockerfiles/CI release path built | MED |
| 069 | 2026-05-30 post-RAID review (PRR-33 / PRR-37 worker fleet) | **D-WORKER-FLEET-LIVE-WIRING** *(PARTIAL 2026-05-30 — P1 task #18: meta-worker CANON path live-wired + end-to-end live-smoked: publisher → xreality.book.canon.updated → meta-worker XREADGROUP → canon_writer → canon_projection + meta_write_audit, on `infra/foundation-dev`; see P1_069_metaworker_consumer_live_wiring.md)* — REMAINING skeletons: meta-worker's OTHER writers (user_erased_writer, force_propagate, canon_history_writer, l1_conflict_detector/reporter), integrity-checker (blocked on AggregateLoader = Rust `dp_kernel::load_aggregate`), alert-recorder, slo-budget-calculator, incident-bot, postmortem-bot, statuspage-updater — all still cmd/main.go exit-0 skeletons. Live-wire each + extend the cross-service smoke. (publisher/archive/retention/embedding/canary already tracked: 054[ADDRESSED]/057/058/059/064; PgEventStore live-PG: 061[ADDRESSED].) | Post-V1 / L7 ops integration | HIGH |
| 068 | 2026-05-30 post-RAID review (PRR-24 / PRR-25 WS contract) | **D-WS-TICKET-WIRE** *(ADDRESSED 2026-06-03 — code aligned to the frozen base64 contract: `Hash32` base64 Marshal/Unmarshal [32-byte-enforcing] in Go ticket.go; `Envelope` seq≥1-for-data in BOTH Go envelope.go + Rust dp-kernel ws.rs [surgical 1-rule kernel touch]; base64-round-trip + 32-byte-enforce + seq tests; the Rust `Ticket` has no serde derive so the int-array never manifested there. 2 cold-start reviews clean; cross-language golden fixture → 077 acceptance item 132.)* — ws/v1.yaml says the ticket hash is base64 (format: byte) but Go ticket.go + Rust dp-kernel ws.rs marshal a JSON 32-int array → client/server cannot interop per spec. Fix: base64 both sides (Go marshaler, Rust serde_bytes) OR change spec to int-array; also enforce Envelope.seq>=1 for data frames in both Validate; + round-trip test. | ADDRESSED 2026-06-03 (unblocks 077 ticket validation) | MED |
| 067 | 2026-05-30 post-RAID review (PRR-06/07/08 — L4.B macros) | **D-DERIVE-PROJECTION-MACRO** — #[derive(Projection)] proc-macro + event_annotations.rs (@event/@version/@upcast parser) + derive_projection_test.rs were deferred from cycle 17 to "cycle 21" but cycle 21 shipped prompt/ws skeletons instead — never built, though the plan listed them as real. crates/dp-kernel-macros has only derive_aggregate. | When projection ergonomics needed | MED |
| 066 | 2026-05-30 post-RAID review (PRR-17 cost rollup) | **D-SESSION-COST-WORKER** — migrations/meta/008_session_cost_summary is an empty sink: the session-cost-rollup-worker (Q-L1A-1 60s rollup) was never scaffolded. Scaffold the Go worker OR downgrade 008 to a documented stub so reviewers don't assume cost-rollup works. | L7 ops / when cost-tracking goes live | MED |
| 065 | 2026-05-30 post-RAID review (PRR-18/41/42/38/39/14/15 grouped) | **D-POST-RAID-MISC** — small cleanups: (18) add audit-DB-cluster capacity row to contracts/capacity/budgets.yaml for the ~10TB/5y s2s-audit; (41) knowledge-service config.py neo4j_password literal default → fail-closed for prod; (42) broaden scripts/lint-no-direct-llm-imports.sh beyond litellm/openai/anthropic (+ note it can't scan Go/Rust); (38) projection verify scripts grep test-fn NAMES not execution; (39) projection unit tests assert count+table only, not field values; (14) CYCLE_LOG lacks "## Cycle N" headers for 8-16; (15) accept/doc the raid-23/raid-24 commit-naming variant; (26) service_acl/matrix.yaml lists phantom callers (roleplay-service/knowledge-service) absent from services/ — add a soft caller-resolution lint; (27) canon_history.yaml omits the svidBearer security block + 401 its sibling canon contracts have. | Opportunistic | LOW |
| 064 | 2026-05-30 RAID cycle 38 (L7.K deploy pipeline — canary-controller live wiring) | **D-CANARY-LIVE-WIRING** *(ADDRESSED 2026-06-03 — all 4 live adapters built + wired in main.go; httptest-pinned; residual: live e2e → 129, baseline capture → 130, outbox emit → 131)* — Cycle 38 ships `services/canary-controller/` as a PURE state machine (`internal/canary` Decide() + `internal/cohort_router` + `internal/controller`) with ALL external deps behind interfaces (DeployStore, SLISource, RolloutExecutor, Pager). `cmd/canary-controller/main.go` is a SKELETON (validates env creds, exposes /healthz+/readyz+/metrics, no-op tick loop). Real wiring needs: (1) pgx DeployStore binding `ActiveCanary`/`AdvanceStage`/`MarkRolledBack`/`MarkComplete` to `deploy_audit` via contracts/meta MetaWrite() (migration 023 already grants UPDATE to app_canary_role for canary_history + completed_at); (2) Prometheus SLISource querying `lw_canary_sli_cohort{stage,service}` cohort-scoped burn + baseline; (3) RolloutExecutor triggering canary.yml via GitHub Actions repository_dispatch (Q-L7K-1 GitHub Actions V1) for Promote/Rollback; (4) PagerDuty Pager binding PageSRE to the cycle-35 PD escalation; (5) tokio/goroutine tick loop calling controller.Tick on the 30s interval; (6) emit `lw_canary_stage` + `lw_canary_sli_cohort` + `lw_canary_abort_total` + `lw_deploy_freeze_active` Prom metrics the deploy-progress dashboard reads. The PURE state machine (advance/abort/2×-baseline) is fully unit + integration tested without these deps. **064 ADDRESSED 2026-06-03**: the live adapters (pgx DeployStore via MetaWrite CAS, Prometheus SLISource, GitHub repository_dispatch Executor, PagerDuty Pager) + the real `controller.Tick` loop + Prom metrics (`lw_canary_stage`/`_sli_cohort`/`_abort_total`/`lw_deploy_freeze_active`) are built, wired in `cmd/canary-controller/main.go`, and contract-tested; the loop runs but is inert until the deploy pipeline (063) feeds it a `class=major` row. | ADDRESSED 2026-06-03 (live e2e → 129) | HIGH |
| 063 | 2026-05-30 RAID cycle 38 (L7.K deploy pipeline — CI deploy/canary live steps) | **D-DEPLOY-LIVE-WIRING** — `.github/workflows/deploy.yml` deploy job + `canary.yml` signal step are placeholders (build/test/classify/freeze gates are REAL + functional; the image push + traffic shift + live controller POST are stubs). Real wiring needs the deploy tooling (container build + registry push + ECS/k8s traffic shift) + `secrets.CANARY_CONTROLLER_URL` POST. Depends on D-CANARY-LIVE-WIRING (064). Q-L7K-1: GitHub Actions V1 (ArgoCD V2+ if multi-cluster). | Post-V1 launch (with D-CANARY-LIVE-WIRING) | MED |
| 062 | 2026-05-29 RAID cycle 33 (L7.H STANDARDS.md introduction) | **D-DASHBOARD-STANDARDS-BACKFILL** *(ADDRESSED in cycle 34 — see "Recently cleared" below)* — Cycle 34 backfilled all 6 pre-STANDARDS.md dashboards to STANDARDS.md conformance (cycle-<N> + cycle-34-backfill tags added, timezone utc, refresh present, datasource UID rewritten from `Prometheus`/`prom` string → `{"type":"prometheus","uid":"prom-primary"}` LOCKED dict form). Grandfather lists in `scripts/dashboard-validator.sh` + `tests/integration/dashboard_render_test.go` cleared (TEMPLATE.json remains exempted due to intentional `_template` uid). Files updated: `dashboards/backup-verification.json`, `dashboards/capacity-planner.json`, `dashboards/per-reality-health.json`, `dashboards/projection-health.json`, `dashboards/shard-health.json`, `dashboards/ws-health.json`. | ADDRESSED cycle 34 | — |
| 061 | 2026-05-29 RAID cycle 17 (L4.A follow-up — PgEventStore live smoke) | **D-EVENT-STORE-LIVE-SMOKE** *(ADDRESSED 2026-05-30 — post-RAID review F7: live-smoked on `infra/foundation-dev` Postgres :55432 with per-reality migrations 0002+0004; surfaced+fixed PRR-46 (timestamp `+00`→`Z` round-trip) + PRR-47 (suite reality_b re-run-safety); full `dp-kernel` suite green incl. PG integration)* — Cycle 17 ships `crates/dp-kernel/src/event_store_pg.rs::PgEventStore` (Q-L4A-1 WRAPPED PgPool) + `tests/integration_event_store.rs` gated by `LOREWEAVE_TEST_PG_URL`. The full `shared_test_suite::run_event_store_tests` runs against `InMemoryEventStore` in cycle-17 unit tests (proves contract soundness) but the Postgres variant only runs when the env var is set. Live smoke needs: (1) CI workflow appends `LOREWEAVE_TEST_PG_URL=postgres://lw:lw@localhost:55432/dp_kernel_test` env to cargo invocation; (2) docker-compose service in `infra/foundation-dev/docker-compose.yml` named `dp-kernel-test-pg` (separate DB from foundation pg so per-reality migration 0002+0004 can be applied without polluting dev data); (3) `scripts/raid/test-infra-up-dps.sh 17 1` extension that brings up the test DB + runs `migrate up` on it; (4) gate `cargo test -p dp-kernel --test integration_event_store` in CI's verify-cycle-17 step. Same shape as cycle-16 D-EMBEDDING-QUEUE-LIVE-WIRING. | RAID cycle 33 (L7 ops integration) or earlier opportunistic CI hardening | MED |
| 060 | 2026-05-29 RAID cycle 16 (L3.I follow-up — V1+30d Redis-backed queue per Q-L3-1) | **D-EMBEDDING-QUEUE-REDIS-MIGRATION** — Cycle 16 ships the embedding queue as an in-process `VecDeque<MemoryRef>` inside `world-service`. Q-L3-1 LOCKED says "V1 in world-service async queue; V1+30d extract if embedding volume justifies". The V1+30d trigger is volume > N realities × M memories/min sustained (precise threshold TBD by L7 ops obs). Migration shape: keep `EmbeddingProvider`+`EmbeddingWriter`+`AuditWriter` traits intact; swap `Queue` for a Redis-backed FIFO (XADD/XREADGROUP on `lw-embedding-jobs` stream) so multi-replica world-service deployments don't double-bill the provider. New `embedding-worker` service shape mirrors archive-worker/retention-worker pattern (cycles 11). | RAID cycle 33 (L7 ops integration) or 1-month post-V1 launch | MED |
| 059 | 2026-05-29 RAID cycle 16 (L3.I follow-up — embedding queue live wiring) | **D-EMBEDDING-QUEUE-LIVE-WIRING** *(PARTIAL 2026-05-30 — P1 embedding-queue: the DB-real core is live-wired + live-smoked on `infra/foundation-dev`. Shipped: async-converted IO traits + `Worker`; `SqlxEmbeddingWriter` (real `UPDATE npc_session_memory_embedding SET embedding=$1 WHERE … AND embedding IS NULL`, pgvector bind + dim guard); `MetaAuditWriter` (real `service_to_service_audit` INSERT, Q-L1A-3 per-call); fail-closed `NotWiredProvider`; tokio drain loop + graceful shutdown; axum `/healthz`+`/readyz`+`/metrics` (`lw_embedding_queue_depth`/`_failures_total`/`_provider_tokens_total`/`_audit_outcome_total`); `embedding-worker` bin; gated `tests/embedding_live.rs` + CI db-smoke step (worldservice_smoke/_meta). The live-smoke CAUGHT + FIXED a real bug: `0008_pgvector_setup` path A (fresh pgvector DB) never `DROP NOT NULL` on the embedding column, which would have made the NULL-sentinel projection-insert + backfill dead. REMAINING split out: provider gateway → **089**; audit durability/latency → **090**; enqueue trigger (no `ProjectionRunner` exists — domain work, 069/079); integrity re-enqueue (blocked on `dp_kernel::load_aggregate`). See docs/plans/2026-05-30-embedding-queue-live-wiring-059.md)* (RE-TARGETED by cycle 17) — Cycle 16 ships `services/world-service/src/embedding_queue/` as pure library code with abstract IO (`EmbeddingProvider`, `EmbeddingWriter`, `AuditWriter` traits). Real wiring needs: (1) `provider-registry-service` HTTP client adapter binding `EmbeddingProvider` to BYOK provider gateway (CLAUDE.md provider gateway invariant — NO direct vendor SDK); (2) sqlx PgPool `EmbeddingWriter` running `UPDATE npc_session_memory_embedding SET embedding=$1::vector WHERE npc_id=$2 AND session_id=$3 AND embedding IS NULL` against the per-reality DB (DSN looked up via cycle-7 reality_registry); (3) `meta-rs::MetaWrite` adapter binding `AuditWriter` to `service_to_service_audit` table per Q-L1A-3; (4) tokio task spawning + graceful shutdown; (5) `/healthz` + `lw_embedding_queue_depth` + `lw_embedding_queue_failures_total` + `lw_embedding_provider_tokens_total` Prom metrics; (6) projection-runner wire-up: cycle-12 dp-kernel `ProjectionRunner` calls `embedding_queue.enqueue(MemoryRef)` whenever `npc.memory_embedded` event lands and the row was INSERTed with `embedding=NULL`; (7) daily integrity-checker (cycle 15) extension that scans for stale-NULL embeddings (older than 1h) and re-enqueues. Cycle 17 confirmed L4.A is trait-shape + macros only (NOT integration); re-target to cycle 33 (L7 ops integration) consolidated with rows 054/057/058. | RAID cycle 33 (L7 ops integration) | HIGH |
| 058 | 2026-05-29 RAID cycle 11 (L2.K follow-up — retention-worker live wiring) | **D-RETENTION-WORKER-LIVE-WIRING** *(ADDRESSED 2026-05-30 — P1 retention-worker live-wiring; see "Recently cleared")* — live-wired: pgx `pkg/pgio` Deleter (bounded `events_outbox` DELETE via `ctid IN (SELECT … LIMIT N)`, only published+un-dead-lettered+past-grace), os/exec `pkg/scriptrun` ScriptRunner (wraps `event-audit-retention-cron.sh`, DSN via PGURI env not argv, parses dropped/non_flagged/flagged), reality_registry DSN map, ticker + graceful shutdown, /healthz+/readyz+/metrics (`lw_retention_*`). End-to-end live-smoked on `infra/foundation-dev` (outbox prune + audit row-DELETE) + wired into CI. Follow-up 088 (prune index). | ADDRESSED 2026-05-30 | — |
| 057 | 2026-05-29 RAID cycle 11 (L2.J follow-up — archive-worker live wiring + production Parquet) | **D-ARCHIVE-WORKER-LIVE-WIRING** *(ADDRESSED 2026-05-30 — P1 archive-worker live-wiring; see "Recently cleared")* — live-wired: pgx Catalog (pg_inherits) + RowSource + PartitionDropper (DETACH+DROP 1 tx) + state.Postgres; minio-go ObjectStore on `lw-event-archive`; **real parquet-go + ZSTD** encoder (replaced the JSONL stub, kept the LWP1 header/footer ABI, SchemaVersion→2); `cmd/archive-worker/main.go` (per-reality Loop + ticker + graceful shutdown + /healthz+/readyz+/metrics); `cmd/archive-restore` + `pkg/restore` (List → MinIO Get → decode → re-INSERT `events_restore_<m>`). End-to-end live-smoked on `infra/foundation-dev` + wired into CI. Remaining: DETACH-first read (row 087); per-reality cron scheduler from `archive-worker-cron.yaml` (ticker used instead). | ADDRESSED 2026-05-30 | — |
| 056 | 2026-05-29 RAID cycle 11 (L2.K follow-up — archive_state migration) | **D-ARCHIVE-STATE-MIGRATION** *(ADDRESSED 2026-05-30 — P1 archive-worker; shipped `contracts/migrations/per_reality/0011_archive_state.up.sql` (PK reality_id+partition_name; object_key, byte_size, row_count, format_header, archived_at; nonneg + 4-byte-header CHECKs). Pre-condition for 057, now both closed.)* | ADDRESSED 2026-05-30 | — |
| 055 | 2026-05-29 RAID cycle 10 (L2.C follow-up — outbox prune) | **D-OUTBOX-PRUNE** *(ADDRESSED in cycle 11 — see services/retention-worker/pkg/outbox_pruner)* — outbox_pruner package implements the bounded-batch DELETE WHERE published=TRUE AND dead_lettered_at IS NULL AND last_attempt_at < NOW()-24h. Test suite enforces the 3 safety invariants (pending preserved, dead-letter preserved, recent preserved). Production wire-in tracked under D-RETENTION-WORKER-LIVE-WIRING row 058. | ADDRESSED cycle 11 | — |
| 054 | 2026-05-29 RAID cycle 10 (L2.D follow-up — production wiring) | **D-PUBLISHER-LIVE-WIRING** *(ADDRESSED 2026-05-30 — P1 task #16; see "Recently cleared")* — publisher live-wired: `poll_loop` refactored to a transactional `Source`→`Batch` API (SELECT FOR UPDATE SKIP LOCKED + row UPDATEs share one tx per reality); new pgx `pgsource`, go-redis `redisemit` (Emitter `lw.events.<reality>` + StreamEmitter `xreality.<type>`), pgx `metahb` heartbeat, full `realityreg` meta-DB client + shard-host→DSN resolver; `cmd/publisher/main.go` real wiring (multi-pool + redis + single-goroutine poll+heartbeat tickers + graceful shutdown + /healthz+/readyz+/metrics). Live-smoked end-to-end on `infra/foundation-dev` (drain→XADD→published + xreality fanout + retry/backoff-exclusion + bootstrap discovery), wired into `foundation-ci.yml` db-smoke. | ADDRESSED 2026-05-30 (P1 #16) | — |
| 053 | 2026-05-29 RAID cycle 10 (L2.L follow-up — validator enforcement) | **D-XREALITY-METADATA-VALIDATE** *(RE-TARGETED by cycle 17)* — Cycle 17 (L4.A) is dp-kernel Event/EventStore/Snapshot trait shape + macros; it does NOT touch `contracts/events/validators_go/` (Go-side L2.L). Validator hardening remains a separate concern. Cycle 17's new Rust-side L2.I `event_validator.rs` (cycle 8) is unchanged. Re-target to cycle 33 (L7 ops integration where Go validator surface is also revisited) OR a dedicated validator-hardening micro-cycle. Mitigation today: publisher's `row.CrossReality()` is still the guard, and the integration test enforces the round-trip. | RAID cycle 33 (L7 ops) or dedicated micro-cycle | MED |
| 052 | 2026-05-29 RAID cycle 8 (L2.F-I follow-up — schema-as-code path protection) | **D-EVENTS-CODEOWNERS** — Schema files in `contracts/events/` + `_registry.yaml` are highly sensitive (a stealth field add or version bump can poison the event stream). Solo-dev hobby project has no CODEOWNERS file (PRE_FLIGHT signed off with CI disabled on foundation branch); when team grows or branch protection re-enables, add `CODEOWNERS` mapping `contracts/events/*` to a security/data-platform reviewer group. | When CI re-enabled / second contributor onboards | LOW |
| 051 | 2026-05-29 RAID cycle 8 (L2.G follow-up — Q-L4-3 contractgen unification) | **D-CONTRACTGEN-V2-UNIFY** *(RE-TARGETED by cycle 17)* — Cycle 8 ships `eventgen --scope events` (the default; `--scope contracts` errors out cleanly as forward-compatible reservation). Q-L4-3 LOCKED says the polyglot codegen surface unifies into `contractgen` (events + API DTOs + capacity budgets + service ACLs) in V2. Implementation: extend `tools/eventgen/runner.go` to dispatch on `--scope`, add per-scope readers (`contracts/api/*.yaml`, `contracts/capacity/*.yaml`, …) + emitters. Likely rename binary to `contractgen` at that point. Cycle 17 confirmed L4.A is dp-kernel trait shape only; the polyglot/codegen work is L4.C-L4.Q scope (cycles 18-22). | RAID cycle 18-22 (L4.C-L4.Q polyglot contracts) | MED |
| 050 | 2026-05-29 RAID cycle 8 (L2.G follow-up — AST-driven field map) | **D-EVENTGEN-AST-PARSE** — Cycle 8 ships `tools/eventgen/field_map.go` as a HAND-MAINTAINED map of event fields used by Rust/TS/Python emitters. Adding a new event = struct + registry entry + field_map entry (3 places). The principled fix is to parse the Go AST in `contracts/events/*.go`, extract the struct fields + their `json` tags + Go types, and infer the polyglot types automatically (1 source of truth). Track 4 cycle 12+ will implement; until then the field_map drift is caught by `scripts/eventgen-validate.sh` indirectly (a field-map miss → generated file missing field → if any consumer uses it → its test fails). | RAID cycle 12+ (Track 4 polyglot port harden) | MED |
| 049 | 2026-05-29 RAID cycle 7 (L1.K negative-test corpus) | **D-LINT-FIXTURE-CORPUS** — Each of the 15 L1.K lints needs a deliberate-violation fixture under `tests/lint-fixtures/<lint>/bad.<ext>` + `good.<ext>` so the lint's exit-1 path has a real test. Cycle 7's lints all FIRED on real codebase patterns during iterative regex hardening (effectively a "live negative test"), but a deterministic fixture corpus is the proper future-proof gate. | RAID cycle 32+ (L7 ops hardening) | LOW |
| 048 | 2026-05-29 RAID cycle 7 (L1.H live runner) | **D-BACKUP-LIVE-RESTORE-RUNNER** — `scripts/restore-drill.sh` ships the contract + audit-write semantics + alert-on-failure path; the actual `pg_basebackup → restore-DB` live runner (libpq + pg_restore exec inside backup-scheduler service) is deferred. Until then, the script writes status='inconclusive' on non-dry-run and exits 0 (preflight succeeded). Same safety-first pattern as cycle 5 orphan_scanner. | RAID cycle 33 (L7 ops backup integration) | MED |
| 047 | 2026-05-29 RAID cycle 7 (L1.J live harness) | **D-DEGRADED-LIVE-SMOKE** *(ADDRESSED in cycle 33 — see "Recently cleared" below)* — wired via `scripts/raid/degraded-live-smoke.sh` orchestrator + `tests/integration/degraded_mode_test.go` now reads `LW_DEGRADED_LIVE_HARNESS_RESULT` env. Live path asserts both metric (`lw_service_mode{mode="limited"}` after kill) AND log (Loki query `{service="world-service"} \|= "mode_shift"`) trails of the L1.J Full→Limited→Full transition. | ADDRESSED cycle 33 | — |
| 046 | 2026-05-29 RAID cycle 7 RE-EVAL of cycles 5+6 deferrals | **D-MIGRATE-CLI-LIVE-WIRING (RE-TARGETED)** — cycle 6 row 044 originally targeted cycle 7. Cycle 7 re-evaluated: the live MetaWriter RPC binding requires the meta-worker service + RPC contract, neither of which exists yet (meta-worker ships cycle 24 L5). Re-targeting to cycle 24+ where the RPC infra lands. Similarly **D-PROVISIONER-PROM-SCRAPE-WIRING (RE-TARGETED)** — cycle 6 row 045 partially implemented via cycle 7's observability-inventory-lint enforcement; the file-write Effect impl re-targets to cycle 17 (L4.A DP-kernel) where world-service gets its full Effects impl. Both row 044 + 045 marked RE-TARGETED below — text rows kept for trace, new disposition is here. | See per-row notes | MED |
| 045 | 2026-05-29 RAID cycle 6 (L1.I.1 integration hook) | **D-PROVISIONER-PROM-SCRAPE-WIRING** *(RE-TARGETED by cycle 7 — see row 046)* — file-write Effect impl now targets cycle 17 (L4.A DP-kernel). The observability-inventory-lint preconditions are met. | RAID cycle 17 (L4.A) | MED |
| 044 | 2026-05-29 RAID cycle 6 (L1.D.4 CLI) | **D-MIGRATE-CLI-LIVE-WIRING** *(RE-TARGETED by cycle 7 — see row 046)* — live MetaWriter RPC binding now targets cycle 24+ when meta-worker service + RPC infra land (L5). Until then, non-dry-run exits cleanly with deferred message. | RAID cycle 24+ (L5 meta-worker) | MED |
| 043 | 2026-05-29 RAID cycle 5 carry-forward (called out in cycle 6) | Production Redis adapter (cycle 5 shipped Cache interface + InMemoryCache test double; real adapter for connecting to the `lw-meta-cache` Redis Sentinel cluster is L7 ops scope). | RAID cycle 33 (L7 Redis ops) | LOW |
| 042 | 2026-05-29 RAID cycle 5 carry-forward (called out in cycle 6) | Real provisioner Effects RPC wiring (cycle 5 shipped the Effects trait pattern; live RPC binding from Rust world-service to Go contracts/meta MetaWriter is L1.C ↔ L1.D integration scope). | RAID cycle 7 (L1.C live wiring) | MED |
| 041 | 2026-05-29 RAID cycle 5 carry-forward (called out in cycle 6) | Live docker stack smoke test for cross-service integration paths. Cycle 5's verify documented "live infra unavailable" because the cycle runner could not bring up docker-compose. Will exercise on the next cycle that mandates a stack boot (cycle 7 L1.D live wiring is the natural trigger). | RAID cycle 7 | LOW |

| 040 | 2026-05-28 v3.5 POST-REVIEW PO directive (geo-generator-amaw, renumbered from local 026 on merge) | **Natural-fragmentation enhancements** (umbrella entry for ~10 features). Real continents are sculpted by water + wind + tectonic break-up — not just drawn algorithmically. v3.5 fractalize ships RANDOM fractal detail; future passes add PHYSICS-DRIVEN fractal detail: (a) coastal wave erosion; (b) wind/Aeolian erosion; (c) river-mouth carving/deltas; (d) tectonic rifting (active plate break-up); (e) sea-level dynamics (glacial/interglacial floods); (f) subduction-driven asymmetric coast modification; (g) hotspot volcanic island chains; (h) karst/dissolution coast detail per-biome; (i) glacial fjord carving; (j) storm-track erosion bands. Full list documented in `docs/plans/2026-05-25-phase-a-v3-roadmap.md` §13 "Natural-fragmentation enhancements". | Phase B+ (water/wind erosion) and V5 (time dynamics) | LOW |
| 039 | 2026-05-26 v3.2 spec §6 design review (geo-generator-amaw, renumbered from local 025 on merge) | `lat_banding` metric entropy rewrite for v3.2's branching shapes (Y-branch, Crab-radial legitimately span 3+ lat bands by design). Spec §6 described an area-weighted band entropy replacement; PO approved defer because v4.5 unified calibration pass will rewrite multiple metrics together. v3.2 ships with current ecotone-aware `lat_banding` and accepts whatever composite delta lands — v5.5.json regenerated at that value. If composite drops >10 PO can flag specific seeds for follow-up via /review-impl. | v4.5 calibration phase | MED |

| 037 | 2026-05-24 V1 tilemap-viewer rescope | Foundation tiles are now algorithm-generated (FFT-tileable noise + per-terrain palette + small specials per `frontend-game/scripts/gen-foundation-tiles.py`) — license-clean + seamless tileable, replaces the prior HoMM3-Flux1d placeholders. V2 still needs an AI-gen pipeline for the **prop/decoration overlay layer** (trees, structures, mushrooms placed on top of the foundation) — that layer is what the multi-book platform invests in for art polish. Front-view portrait sprites + hand-painted encounter backgrounds also belong here (separate branches). Multi-book constraint: per-book prop gen must complete in <few hours at low cost; license-clean (schnell / SDXL / CC0). | V2 asset pipeline (prop/overlay layer) | MED |
| 036 | 2026-05-24 V0 close-out | `frontend-game/src/game/main.ts` ships a `globalThis.Phaser = Phaser` shim required because Phaser 4.1.0's webpack-bundled ESM references the bare global `Phaser` identifier internally (`phaser.esm.js:88604` `new Phaser.Structs.Map()` inside SpriteGPULayer constructor; lines 33884/34948/34969 in render code). Without the shim, calling SpriteGPULayer factory throws "Phaser is not defined" at runtime. Track upstream — remove shim once Phaser ships ESM-clean patch (likely 4.x.y). Saved to memory `project_phaser4_quirks`. | When Phaser 4 ships ESM-clean patch | LOW |
| 035 | 2026-05-24 V0 close-out | *(ADDRESSED 2026-06-03 by 077 — per-user `ConnectionCap` + per-connection `MessageRateLimiter` at the game-server WS edge [close 4008 / 4006], `rateLimitsFromEnv` + unit tests)* game-server EchoRoom has no rate limiting — a malicious client could spam-send messages. V0 acceptable because EchoRoom is dev-only smoke; V1 ZoneRoom/CombatRoom must add per-client rate limits (Colyseus supports via `client.send` budget or middleware) before any production deploy. | V1 game-server rooms | MED |
| 034 | 2026-05-24 V0 close-out | CORS origins in tilemap-service + game-server default to `http://localhost:5174` via `LOREWEAVE_CORS_ORIGINS` env. Production deploy MUST set explicit origin(s) (not `*`); if env is unset or empty in prod, the cors layer falls back to localhost-only which would block real users. Add a startup check in V1 prod build that env is non-empty when `NODE_ENV=production`. | V1 prod deploy hardening | MED |
| 033 | 2026-05-24 V0 close-out | `frontend-game/src/config/services.ts` `SERVICES.devToken` defaults to `'dev_internal_token'` which bundles into client JS (visible in DevTools). V0 dev-only token is acceptable for echo-room handshake; V1 MUST replace with per-user JWT obtained via auth-service login flow + cookie storage (HTTP-only, SameSite=Lax). Currently EchoPanel imports + sends this token to game-server EchoRoom.onAuth. Anyone who can reach localhost:2567 can join with the leaked token. | V1 auth-service integration | MED |
| 032 | 2026-05-24 V0 close-out | EchoRoom reconnect uses Colyseus `setSeatReservationTime(30)` (30-second window). Docker container restart often takes >30s → reconnect-token reservation expires → client falls back to fresh `joinOrCreate` (correct UX per EchoPanel `onReconnect` catch handler). When V1 real ZoneRoom ships with persistent character state, reconsider whether 30s window is right or if a longer reservation (e.g. 5 min) matches user expectation. | V1 ZoneRoom design | LOW |
| 031 | 2026-05-24 V0 close-out | TilemapGPULayer documented as N/A for our iso 2:1 dimetric (spec §11.1 amended Session C Phase 1; Phaser docs `CHANGELOG-v4.0.0.md:549` confirm orthographic-only). If we ever scale Continent zone from 256² with standard `Tilemap.createLayer` and need GPU-layer perf, the only path is either (a) reproject world as orthographic (visual style change) or (b) hand-roll a custom GPU tilemap shader (V3+). Not blocking V1; current path handles V0+V1 zone sizes fine. | V3+ continent perf | LOW |
| 030 | 2026-05-24 V0 close-out | AC-FG-14 (HMR) for Phaser scene file edits relies on Vite default behavior: editing a non-React `.ts` file outside the React-fast-refresh boundary triggers a controlled full-page reload (canvas re-created cleanly, no silent freeze). Verified manually 2026-05-24. If V1 work introduces a non-React TS module that should hot-swap (e.g. a config-only file), add `import.meta.hot.invalidate` boundaries explicitly per spec §1 #3 AC-FG-14. | V1 dev experience | LOW |
| 028 | 2026-05-18 continent measurement /review-impl LOW-3 | `tilemap-service measure` always runs the offline 256² placement (~8–11 min) before the live engine→L3→L4 section. Re-running it purely to capture the live numbers (e.g. after the provider-registry model-pricing fix) re-pays that ~11-min cost for nothing. Add a `measure --live-only` flag, or serialise/cache the placed `TilemapView` so the live section can load it. | tilemap measurement ergonomics (Track 2) | LOW |
| 025 | 2026-05-18 tilemap Phase E spec §5.7 | `RoadPlacer` ships raw Dijkstra paths — no §3.4-step-3 path smoothing (3-point detour removal) and a single road kind (no `RoadKind` Highway/DirtPath). V1+30d has no town/mine edges, so every MST edge is zone-backbone — one kind suffices; smoothing is pure visual polish. Build when rendered roads look jagged or a town/mine tier needs a secondary road kind. | Visual polish (Track 2) | LOW |
| 024 | 2026-05-18 tilemap Phase D spec D-Q6 | TMP_007 §5's "curved" passage-path cost — a `search_path` cost that makes a passage corridor bow away from the zone border instead of bisecting the zone — is not built. Phase D drives `search_path` with uniform `cost = 1.0` (the shortest route); the curved cost is pure visual polish with no correctness or determinism role. Build it when rendered passage corridors look too straight / zone-bisecting. | Visual polish (Track 2) | LOW |
| 023 | 2026-05-17 tilemap Phase C human-in-loop review (Call #1) | TMP_006 §3.2 zone-config object **filtering** — `banned_object_categories`, `banned_objects`, `required_objects` — is not built in Phase C. It is a separate object-pool-filtering layer needing its own `ZoneSpec` fields (`banned_objects: BTreeSet<…>`, `required_objects: BTreeMap<…>`, …) and a filter pass over the treasure pool; no TMP_006 §8.5 resolved question makes it V1+30d-mandatory — unlike `inherit_treasure_from` (TMP-TR-Q3), which Phase C *does* build (spec D9). The A–E modificator roadmap (Phase D = ConnectionsPlacer, E = Roads/Rivers) has no slot for authoring-layer features, so this is logged with an explicit target rather than left to drift. **Also reconcile here:** Phase C spec D9 chose REPLACE over TMP-TR-Q3's UNION-with-override for the unkeyed `treasure_tiers` `Vec`; when this keyed filtering layer lands, define the per-key override semantics for `banned_objects` / `required_objects` consistently with TMP-TR-Q3. | TMP_004 authoring task (Track 2) | LOW |
| 021 | 2026-05-17 tilemap Phase A spec D12 / code-review r3 WARN-3 | `From<PlacementError> for crate::Error` bridge not built. Phase A's `place_and_connect_object` returns the module-local `PlacementError` enum (`NoSpace` / `NoSuchZone`); a placer in Phase C+ that `?`-propagates from `Modificator::process` (returns `crate::Result`) needs the conversion. Phase A has no such consumer, so the bridge is deferred to the first placer. Spec D12 records this. | Phase C (first placer) | LOW |
| 020 | 2026-05-17 tilemap Phase A spec D5 | TMP_006 §4.3 connectivity pre-filter not built — `would_seal_a_gap` runs the naive double-flood-fill on every candidate placement. TMP_006 §4.2 blesses that as "cheap enough to run on every candidate"; the §4.3 pre-filter (skip candidates adjacent to ≤1 free region) is a continent-scale perf optimisation. Output stays byte-identical. | Perf — when continent-scale generation profiles slow | LOW |
| 018 | 2026-05-16 tilemap Phase 1 human-in-loop review HIGH-1 | `penrose::assign_zone_tiles` is O(tiles × vertices) — `nearest_vertex` linearly scans the full Penrose vertex field for every grid tile. At Continent scale (256² = 65 536 tiles × ~500-1200 vertices) that is ~10⁷-10⁸ `distance_sq` calls, single-threaded, vs the TMP_002 §7 budget. Structurally the same defect as #016 (fractalize) but larger — the tile loop runs over the whole grid, not one zone — and the capped AMAW Adversary caught only #016. Invisible at the 24²-64² test fixtures. Fix: bucket vertices into a coarse uniform grid and test only neighbour buckets, or a multi-source BFS distance transform — output stays identical. **2026-05-18 continent measurement (finding O-1) confirms the pain: a 256² `place_tilemap` takes ~8–11 min release-build — recommend promoting to active work.** | Perf — **profiling now shows pain (2026-05-18)** | MED |
| 017 | 2026-05-16 tilemap Phase 1 Adversary r1 WARN-3 | `force_directed_converge` computes `ConvergenceResult.converged` + `.iteration_count`, but `place_zones` / `place_tilemap` drop them — `TilemapView` has no carrier, so a cap-hit tilemap is indistinguishable from a cleanly-converged one. PLAN chunk 0 said "add `iteration_count`/`converged` carriers as needed"; nothing in Phase 1 consumes them. Surface via a `GenerationMetadata` field on `TilemapView` when DP progress-streaming (TMP_002 §9 TMP-PLACE-Q3) lands. | Phase 2+ (progress-event streaming) | LOW |
| 016 | 2026-05-16 tilemap Phase 1 Adversary r1 WARN-2 | `fractalize::scatter_and_connect` is O(candidates²) — `nearest_cleared_dist_sq` linearly scans the growing `cleared` mask for every candidate. At Continent scale (256²) that is ~10⁸ ops per zone vs the TMP_002 §7 <500 ms budget; invisible at the 24²-64² test fixtures. Replace the linear nearest-scan with coarse spatial buckets or a multi-source BFS distance transform. **2026-05-18 continent measurement (finding O-1) confirms the pain: a 256² `place_tilemap` takes ~8–11 min release-build — recommend promoting to active work.** | Perf — **profiling now shows pain (2026-05-18)** | MED |
| 015 | 2026-05-16 tilemap Phase 1 Adversary r1 WARN-1 (severity raised by human-in-loop review) | The wall-clock-cap fallback (`force_directed` D5) is a cross-machine layout divergence: a slow machine can trip the wall-clock cap where a fast one converges, so the grid-seed fallback differs across machines for the same seed — the one path that can break the TMP-A4 axiom. Spec-D5-accepted. **A `tracing::warn!` in the `WallClockCap` arm now makes the fallback observable** (added 2026-05-16 review-fix task). The remaining deferred part is the ops-dashboard `tilemap.generation_failed` info-event (TMP-PLACE-Q2), which needs the Phase-2 progress-event channel. | Phase 2+ (progress-event streaming) | MED |
| 014 | 2026-05-16 AMAW-improve BUILD finding | `amaw-guardrail-gate.py` cheap pre-check matches a risky pattern ANYWHERE in the Bash command string — including a heredoc body / commit message. A `git commit` whose message text contains "git push" or "migrat…" false-positive-fires the gate (an `ask`, not a block — so non-fatal, the user just confirms). Refine: match the leading command token, or strip heredoc bodies before the pre-check. | AMAW hook hardening | LOW |
| 013 | 2026-05-16 AMAW-improve spec D3 | `agentic-workflow/AMAW.md` (the installable bundle source of `docs/amaw-workflow.md`) was already ~67 lines diverged from the installed copy before this task; D3 updated the operative installed docs (`amaw-workflow.md` + `amaw.md`×2) but not `AMAW.md`. Reconciling the bundle source is its own hygiene task. | agentic-workflow bundle sync | LOW |
| 010 | 2026-05-15 tilemap Phase 0b spec §2 | Anthropic **request-side** tool support — `anthropicAdapter.Stream` sends no `tools`/`tool_choice` (Anthropic-shaped tools differ from the OpenAI-shaped contract field). Phase 0b landed only the Anthropic *streamer parse-side* `tool_use`→`tool_call` mapping. Fix when an Anthropic provider needs tool-use: translate OpenAI-shaped `tools`/`tool_choice` → Anthropic `input_schema`/`tool_choice` in `anthropicAdapter.Stream`, and set `anthropicAdapter.SupportsTools()` → true. | When Anthropic tool-use needed | MED |
| 009 | 2026-05-15 tilemap Phase 0b code review (Scope Guard AC-7 PARTIAL) | D8 guard (`LLM_TOOLS_NOT_SUPPORTED_FOR_PROVIDER`) full handler-level reject path is untested — the capability component (`SupportsTools()` + `hasToolDefinitions()`) is unit-tested, but the `doLlmStream` reject path needs the live-DB + fake-adapter integration harness. Joins the pre-existing `D-PHASE5A-STREAM-INTEGRATION-TESTS` deferral. | provider-registry stream integration suite | LOW |
| 008 | 2026-05-15 amaw-precommit-no-side-effect Adversary r1 WARN-1 | `load_state()` calls `json.loads()` with no try/except — a corrupt or empty `.workflow-state.json` (manual edit, disk corruption) makes every gate command, incl. `cmd_amaw_pre_commit`, die with a traceback that blocks the commit. (Risk much reduced by DEFERRED #003's atomic save_state — crashes can no longer half-write the file — but manual-edit / disk-corruption paths remain.) Fix: wrap `load_state`'s parse in try/except; on corruption, print a clear error and either reset or refuse cleanly. Affects all `load_state` callers, so it is its own task — not in #004's scope. | L4 hardening | LOW |
| 007 | 2026-05-15 amaw-task-slug-validation Adversary r2 WARN-1 | `cmd_amaw_enable` normalizes the slug only on the `if args:` write path; a no-arg or already-enabled invocation can leave a raw `state["task"]` persisted in the state file. Mitigated: `cmd_complete` defensively re-normalizes so the OUTPUT tag list is always comma-safe — only the state-file display value can be stale. Fix: normalize unconditionally at the top of `cmd_amaw_enable`, or normalize on every state read. | L4 hardening | LOW |
| 001 | 2026-05-15 amaw-l3-deepen /review-impl #5 | `task_slug` not validated — comma in slug splits into multiple tags downstream (`",".join(tags)` then mcp-query.py splits back on comma). No injection risk (subprocess list form). Fix: normalize comma → dash in `cmd_amaw_enable`, or argparse-validate slug pattern `^[a-z0-9][a-z0-9-]*[a-z0-9]$`. | L4 hardening | LOW |
| 002 | 2026-05-15 amaw-l3-deepen /review-impl #6 | `"REJECTED" in evidence.upper()` substring match in `cmd_complete` bridge logic — false positives on "NOT REJECTED" or "rejected the rejected pattern". Fix: word-boundary regex `\bREJECTED\b` or check evidence prefix `"REJECTED:"` / `"status: REJECTED"`. | L4 hardening | LOW |
| 004 | 2026-05-15 amaw-l3-deepen /review-impl #8 | `cmd_pre_commit` early-exits silent when no STATE_FILE, but `&&`-chained `cmd_amaw_pre_commit` then calls `load_state()` which CREATES `.workflow-state.json` from INITIAL_STATE. Side effect: every commit by an agent NOT in any tracked task creates a stale state file, surfacing confusing `[ ]` markers on next `status`. Fix: in `cmd_amaw_pre_commit`, also early-exit if STATE_FILE absent before `load_state()`. | L4 hardening | LOW |

## Recently cleared (within 5 sessions)

| ID | Cleared | How |
|---|---|---|
| 074+075 | 2026-05-30 P2 admin-CLI + break-glass JWT (AMAW: 3 design-review rounds + 2 code-review rounds) | **D-ADMIN-CLI-JWT + D-BREAK-GLASS-JWT** closed end-to-end. New standalone `contracts/adminjwt` module (shared by both modules): `AdminClaims` + strict `Verify` (RS256-only, alg:none/HS-downgrade rejected, exp required, iss/aud pinned, kid bound to `KeyFingerprint`=SHA-256(PKIX-DER)) + `ParseRSAPublicKeyPEM` (SPKI only, PKCS#1 rejected) + `ValidateBreakGlass` (single pinned dual-actor/≥100-reason/ticket/≤24h policy). auth-service: AWS KMS asymmetric **Sign** signer (RSA private key never leaves KMS; in-process `LocalRSASigner` for faithful unit wire-tests; shared `assembleRS256` so unit bytes == KMS bytes; golden/header-pinned vector); two internal endpoints `POST /internal/admin/token` + `/break-glass-token` behind a NEW `requireAdminIssuerToken` (dedicated `ADMIN_TOKEN_ISSUER_SECRET`, ≥32, distinct from `INTERNAL_SERVICE_TOKEN`, constant-time) + rate-limit; break-glass requires BOTH actors' own normal admin tokens (issuer-side dual-actor, break-glass tokens rejected as approvers); `admin_principals` (FK `ON DELETE RESTRICT`; lookup excludes soft-deleted users) + `admin_token_issuance_audit` (append-only, every attempt via `outcome`, keyed-HMAC reason + len, unique jti) migrations. admin-cli: `auth.Validate` swapped to RS256 verify (env PEM, kid-bound, fail-closed), `break_glass.Validate` delegates to `adminjwt`. All 3 modules build/vet/test green; gofmt + language-rule-lint clean. AMAW design review caught the KMS-wire-unpinned + shared-issuer-secret + break-glass-bootstrap issues; code review caught the success-path-audit-loss BLOCK. Opened 093-096. Plan: `.../shimmying-splashing-micali.md`. LIVE KMS Sign smoke deferred (094, localstack#7216). |
| 058 | 2026-05-30 P1 retention-worker live-wiring (full human-in-loop) | **D-RETENTION-WORKER-LIVE-WIRING** closed. pgx `pkg/pgio` Deleter — bounded `events_outbox` DELETE (`WITH d AS (DELETE … WHERE published=TRUE AND dead_lettered_at IS NULL AND last_attempt_at < $cutoff AND ctid IN (SELECT ctid … LIMIT $batch) RETURNING 1) SELECT count(*)`); NEVER touches pending / dead-lettered / `events`. os/exec `pkg/scriptrun` ScriptRunner — wraps the pre-existing `scripts/event-audit-retention-cron.sh`, hands the DSN via **PGURI env (not `--db` argv — credential hygiene, caught in the /review-impl self-review)**, parses `dropped`/`non_flagged`/`\bflagged` (the `\b` avoids capturing `non_flagged` as `flagged`). `cmd/retention-worker/main.go` — meta pool → active realities → per-reality pools + DSN map → one retention_loop.Loop → ticker + graceful shutdown + /healthz+/readyz+/metrics (5 `lw_retention_*` counters). End-to-end live-smoke on foundation-dev (PG): outbox prune (eligible deleted, pending+dead-lettered+recent kept) + audit row-DELETE (45d non-flagged deleted, 45d flagged kept) via real psql; re-run-safe. CI db-smoke +retention smoke. `/review-impl`: 0 HIGH/MED; 1 LOW tracked (088 prune index). Reuses publisher `realityreg`. |
| 056+057 | 2026-05-30 P1 archive-worker live-wiring (full human-in-loop) | **D-ARCHIVE-STATE-MIGRATION + D-ARCHIVE-WORKER-LIVE-WIRING** closed together. Migration `0011_archive_state` (per_reality). pgx `pkg/pgio` (Catalog via pg_inherits + `events_p_YYYY_MM` bound parse / RowSource SELECT / PartitionDropper DETACH+DROP one tx, name regex-guarded) + `state.Postgres` (archive_state AlreadyArchived/RecordArchived ON CONFLICT/List) + `pkg/miniostore` (minio-go Put/Get/Exists/EnsureBucket on `lw-event-archive`). **Real Parquet+ZSTD**: `parquet_writer` swapped from the JSONL stub to parquet-go + ZSTD column compression, keeping the LWP1 header/footer ABI + VerifyHeader (SchemaVersion→2; empty partition → 20-byte envelope). `cmd/archive-worker/main.go` (per-reality Loop + ticker + graceful shutdown + 4 Prom counters), `cmd/archive-restore` + `pkg/restore` (List/RestoreMonth → `events_restore_<YYYYMM>`, ON CONFLICT idempotent). End-to-end live-smoke on foundation-dev (PG+MinIO): old partition → Parquet+ZSTD → MinIO → verify → archive_state → DETACH+DROP → restore round-trip; re-run-safe. CI db-smoke +MinIO (docker-run) +archive smoke; go-version 1.24→1.25 (parquet-go). `/review-impl`: 1 MED tracked (087 DETACH-first read-then-drop window — not exploitable in V1). Plan: `P1_056_057_archive_worker_live_wiring.md`. |
| 054 | 2026-05-30 P1 task #16 (publisher live-wiring, full human-in-loop) | **D-PUBLISHER-LIVE-WIRING** closed. `poll_loop` refactored from `Fetcher`+`StateWriter` to a transactional `Source`→`Batch` pair (SELECT FOR UPDATE SKIP LOCKED + row UPDATEs share ONE tx per reality → SKIP-LOCKED lock now load-bearing for V2). New adapters: pgx `pgsource` (backoff-aware pending scan, no schema change — exponential deadline derived inline from `attempts`+`last_attempt_at`), go-redis `redisemit` (Emitter `lw.events.<reality>` + StreamEmitter for `xreality.<type>`), pgx `metahb` (publisher_heartbeats upsert), full `realityreg` meta-DB client + shard-host→DSN resolver (dev `*` override). `cmd/publisher/main.go`: fail-closed config, multi-reality pools, redis, SINGLE-goroutine poll+heartbeat tickers (fixes a heartbeat.Loop data race found in `/review-impl`), per-reality startup-pool-open isolation, graceful shutdown, `/healthz`+`/readyz`+`/metrics` (7 Prom counters). Live-smoked on `infra/foundation-dev` (3 integration tests: drain→XADD→published+fanout, bootstrap `ActiveRealities`, backoff-exclusion) + wired into `foundation-ci.yml` db-smoke (PG+Redis services). Plan: `docs/plans/2026-05-29-foundation-mega-task/P1_054_publisher_live_wiring.md`. Opened follow-ups 081-083. |
| 062 | 2026-05-30 RAID cycle 34 (L7.I + L7.J SLO + Alertmanager) | All 6 pre-STANDARDS.md dashboards backfilled to STANDARDS.md conformance: `cycle-<N>` + `cycle-34-backfill` tags added (cycle-6 origin for backup-verification/capacity-planner/per-reality-health/shard-health/ws-health; cycle-15 for projection-health; cycle-28 for ws-health), `timezone: "utc"`, `refresh: "30s"`/"1m" present, all panel + templating datasources rewritten from `"Prometheus"` / `{uid:"prom"}` to `{type:"prometheus",uid:"prom-primary"}` (LOCKED set). Grandfather lists in `scripts/dashboard-validator.sh` + `tests/integration/dashboard_render_test.go::grandfathered` collapsed to `TEMPLATE.json` only. Files: `dashboards/backup-verification.json`, `dashboards/capacity-planner.json`, `dashboards/per-reality-health.json`, `dashboards/projection-health.json`, `dashboards/shard-health.json`, `dashboards/ws-health.json`. |
| 047 | 2026-05-29 RAID cycle 33 (L7.F + L7.H observability infra) | Live degraded-mode smoke orchestrator `scripts/raid/degraded-live-smoke.sh` ships, bringing up meta-HA + observability stacks together. The Go test `tests/integration/degraded_mode_test.go::TestDegradedMode_KillMetaPrimary_BufferFills_FlushesOnRecovery` now reads `LW_DEGRADED_LIVE_HARNESS_RESULT` env from the shell harness (PASS/FAIL). Smoke asserts (a) Prom HA pair captures the `lw_service_mode{mode="limited"}` flip, (b) Loki captures the `mode_shift` log trail (cycle-32 logger emit + cycle-33 Vector ingest), (c) flush counter ticks after recovery. Q-L1I-1 (Prom HA) + Q-L7F-1 (Loki self-hosted) + Q-L7-3 (no service mesh) all honored. |
| 038 | 2026-05-24 V1 tilemap-viewer rescope (same session it was created) | HoMM3-bundle Flux1-dev placeholders replaced with first-party algorithm-generated foundation tiles (`frontend-game/scripts/gen-foundation-tiles.py`). No third-party model output baked in; ship-anywhere license. Spec rescope: `docs/specs/2026-05-24-v1-tilemap-viewer-rescope.md`. |
| 024 | 2026-05-24 v2.1e ship (geo-generator-amaw branch) | W9 shipped as amplitude-gated RGB shading (zone relief auto-scales intensity 0→1 across Plains→Mountains; ±15% RGB modulation). Hypothesis "naive RGB regressed because eval framework had a problem" CONFIRMED: under v4.3 ecotone-aware eval, naive RGB amplitude-gated produces only −0.24 mean (neutral, 0 hard regressions) vs the v2.1b attempt's hard regression. Visual benefit currently limited (visible only on Ice zones where white amplifies modulation) but ships as foundation for future 3D map rendering. HSL approach not needed. True slope-aware hillshade (spec option B) still deferred to v3+. |
| 027 | 2026-05-22 tilemap test hardening | Added `a_bridge_mid_river_resets_the_periodic_ford_counter` (river_placer.rs): a 30×3 straight river with a road at x=6; asserts the bridge resets the shared `since_crossing` counter, so the periodic ford lands at x=18 (12 carves after the bridge) — NOT x=12 (which would mean the bridge didn't reset). Closes the interleaving coverage gap between `a_long_river_gets_periodic_fords` (pure every-Nth) and `a_river_crossing_a_road...` (pure bridge). Test-only; no output change. |
| 026 | 2026-05-22 tilemap river barrier reorder | Root cause refined: **erosion** (not fill) fragments the passable region, and the river's Mountain/Lake sources/sinks required erosion first — so the river was structurally stuck threading post-erosion narrow channels (forded most tiles). Fix: split `ObstaclePlacer` into `ObstacleSourcePlacer` (places Mountain/Lake markers on Open tiles **pre-erosion**, dual-gated per-zone + map-wide) → `RiverPlacer` (carves the wide-open zone) → `ObstacleFillPlacer` (erode + fill the rest post-river, skipping river Water; erosion also skips Water fords). Golden fixture river ford ratio: **~0.77 crossings → 0.06 fords** (AC-2 gates < 0.25). Cost tradeoff accepted: continent 6.46 s → 10.56 s (river now carves a real barrier). Strict dual `would_seal_a_gap` gate unchanged. `/review-impl` caught a HIGH (source placer needed the map-wide gate arm, not per-zone only) — fixed + regression-tested. Spec: [`docs/specs/2026-05-22-tilemap-river-barrier-reorder.md`](../specs/2026-05-22-tilemap-river-barrier-reorder.md). |
| 029 | 2026-05-21 tilemap perf fix | `place_and_connect_object` refactored to **score-first, validate-on-demand**: cheap-filter + score all candidates (no flood fill), sort by `(score desc, flat asc)`, walk in best-first order and validate (`would_seal_a_gap` + `find_access_path`) lazily. The first sorted candidate that passes IS the winner (spec §4 proof: `first ∈ V` of sorted `S` == `argmax_{v ∈ V} (score, -flat)`). Naive kept under `#[cfg(test)]` as oracle. **Measurement: `treasure_placer` 973.376 s → 2.944 s (330×); continent total 992.963 s → 21.541 s (46×).** Golden test passes byte-exact — no rebaseline. AC-1 oracle: 405 agreements across 3 fixtures × 3 templates × 3 optimisations × 3 min-distances × 5 sequential placements. Spec: [`docs/specs/2026-05-21-tilemap-place-and-connect-perf.md`](../specs/2026-05-21-tilemap-place-and-connect-perf.md). |
| 018 | 2026-05-20 tilemap perf fix | `penrose::nearest_vertex` replaced with a bucket-based spiral search (`build_vertex_buckets` + `nearest_vertex_bucketed` over the new `UniformBuckets<Vec2>` helper in `engine/placement/spatial.rs`). Bit-exact equivalent to the prior linear scan (AC-2 oracle: 800 random queries × 4 vertex-field configurations incl. 1500-vertex dense; AC-3 cross-bucket tie-break by lowest index). Per-stage continent measurement: `place_zones` (Penrose + fractalize) dropped to **0.110 s** at 256² — well under the TMP_002 §7 <500 ms budget. Spec: [`docs/specs/2026-05-20-tilemap-perf-fractalize-penrose.md`](../specs/2026-05-20-tilemap-perf-fractalize-penrose.md). |
| 016 | 2026-05-20 tilemap perf fix | `fractalize::scatter_and_connect` replaced with a bucket-based "any within radius" rewrite using the same `UniformBuckets<TileCoord>` helper — 3×3 ring neighbourhood query (sufficiency proven: `bucket_size = ceil(sqrt(coverage_sq))` ⇒ ring-2 min dist² ≥ 49 > 36 surface / 25 > 16 sea). Bit-exact equivalent to the prior `nearest_cleared_dist_sq` linear scan (AC-1 oracle: 96² zone × 5 seeds × 2 spans). The naive impl is kept under `#[cfg(test)]` as the oracle. Spec: [`docs/specs/2026-05-20-tilemap-perf-fractalize-penrose.md`](../specs/2026-05-20-tilemap-perf-fractalize-penrose.md). |
| 022 | 2026-05-18 tilemap Phase E | Resolved by the Phase-E `RiverPlacer` design (spec D-Q5/D-Q6): the `anchor` representative point **suffices** — no footprint extent needed. River sources are one lowest-flat-index `Mountain` obstacle anchor per mountain-bearing zone; sinks are the `Lake` obstacle anchors ∪ `Sea`-zone tiles. `search_path` routes to/from the anchor tile, which is added to the river search area. No additive `TilemapObjectPlacement` footprint field was required. |
| 019 | 2026-05-18 tilemap Phase D | Re-validated when ConnectionsPlacer landed. `seal_borders` (§3.1/§9) seals inter-zone borders but is not a complete wall — passage corridors stay `Walkable` and gap-critical tiles stay `Open`. The per-zone `would_seal_a_gap` is sound regardless: every tile belongs to exactly one zone, and every passability-removing mutation (placer objects, monoliths, guards, ferries, border seals) targets an `Open` tile that is zone-interior — so removing a zone-X tile can never split zone Y. Border-seal completeness is irrelevant to correctness; the original "border thickness" framing was the wrong lens. |
| 013 | 2026-05-17 GEO Phase 2 | Connectivity-aware `choose_sea_level` — binary-searches `sea_level` so the largest land component ≈ the profile target (`maxcomp(L(s))` is monotone non-increasing in `s`), plus a continental base dome for the high-land `Inland` profile (a blob-only heightmap cannot form a connected 70% landmass). Realised land fraction is now within ±0.08 of target, or a documented 8192 best-effort floor (±0.18). Verified by `land_fraction_near_target` (16-seed sweep × 4 profiles). |
| 012 | 2026-05-16 tilemap fix task | Added `TestStreamOpenAICompat_MultipleToolCalls` — two tool calls streamed with interleaved/non-monotonic `tool_calls[].index` (0,1,1,0); asserts `Index` is verbatim from the provider and per-index reassembly stays separate. |
| 011 | 2026-05-16 tilemap fix task | Dropped `nullable: true` from openapi `ChatStreamRequest.tool_choice` — the field is now `description` + `oneOf` only (idiomatic OpenAPI 3.0; still optional via not-in-`required`). No runtime change; SDKs already model it optional. |
| 005 | 2026-05-15 tilemap Phase 0b /amaw | First full-feature `/amaw` dogfood (XL, 12 phases). 6 sub-agent spawns (4 design Adversary + 1 code Adversary + 1 Scope Guard) all completed their Step-0 `python scripts/mcp-query.py` calls cleanly — no permission-prompt interruption. The allow-list is sufficient. |
| 003 | 2026-05-15 amaw-atomic-save-state | First `/amaw` dogfood. `save_state` now writes a PID-unique `.tmp` then `Path.replace()` (atomic rename); `cmd_reset` sweeps stale tmps; `finally`-unlink cleans own tmp. Adversary r1 added 3 hardening WARNs (all fixed). |
| 006 | 2026-05-15 batch pre-flight | AMAW.md Adversary + Scope Guard prompt templates now pre-instruct the Bash-heredoc fallback for writing findings/post-review docs, since the Write tool blocks sub-agents from writing report files. |
