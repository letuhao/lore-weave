# Open decisions — standing questions, none blocking

*Generated 2026-09-02 by `scripts/toolloop/dq_digest.py`. Derived from the ledger at emit time — do not hand-edit; re-run it.*

**No defect is open.** The 1 question(s) below hold no work: each is a standing question kept as a record, and answering one releases nothing that is currently blocked. They are still worth ruling on when convenient — a question left open is analysis nobody has decided to act on, not analysis that was wrong.

A ruling goes on the question's row as an `answer_<date>` field. The loop reads it there and builds it **as worded** — if it cannot be built, the question comes back with the measurement showing why, rather than a substituted mechanism.

## All open questions, by how many defects they release

### DQ-T38 — releases 0 defects

**Asked:** Why does the public catalogue list nothing while 44 books are marked public?

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better. 🔴 AND THIS ONE IS NOT YET A DECISION AT ALL. Its own recommendation says so: 'NO RECOMMENDATION YET, AND THAT IS THE ANSWER … no cause has been established, so every option would be a guess dressed as a fix. What is owed first is one measurement.' So this is WORK before it is a question, and it should not sit in a decision pack until that measurement has been run.

**⚠ Correction on record (parked_2026_08_31):** OWNER 2026-08-31: `park=yes`. PARKED, NOT ANSWERED AND NOT WITHDRAWN. No open defect names this question in `blocked_by_dq`, so it gates nothing; it is a standing question kept as a record rather than a queue item. The state stays `open` on purpose -- marking it `answered` would claim a ruling nobody made, and `withdrawn` would claim the question was wrong. What changes is that the digest lists it separately from the questions that hold work.

**⚠ Correction on record (owner_direction_2026_09_02):** OWNER DIRECTED: run the measurement. This question's own recommendation says 'NO RECOMMENDATION YET, AND THAT IS THE ANSWER ... no cause has been established, so every option would be a guess dressed as a fix. What is owed first is one measurement.' Taking that instruction: establish WHY the public catalogue lists nothing while 44 books are marked public -- the query, the visibility flag, or the publish path -- and return a question that can actually be ruled on.

**⚠ Correction on record (THE_DIRECTED_MEASUREMENT_2026_09_02__THE_CATALOGUE_IS_CORRECT):** MEASURED, AND IT DISSOLVES THE QUESTION RATHER THAN ANSWERING IT. The catalogue lists nothing because THERE IS NOTHING TO LIST. Not one of the 44 books marked public still exists. HOW THE LISTING WORKS, which is what made the measurement decidable: `queryPublicBooks` is TWO stages -- `fetchPublicIDs` asks sharing-service for public book ids, then `fetchProjection(id)` asks book-service for each, skipping any that does not resolve or is not `lifecycle_state='active'`. So an empty result means stage 1 returned nothing, or stage 2 dropped everything. It is stage 2, completely: sharing_policies 58 rows (44 public, 13 private, 1 unlisted) ...pointing at a book that EXISTS 1 row ...of the 44 PUBLIC ones 0 rows books in loreweave_book 867 🔴 THE INSTRUMENT WAS CONTROLLED BEFORE THE NUMBER WAS BELIEVED, because '0 of 44' is exactly the shape of a query that is measuring itself. The same `id::text IN (...)` predicate against three ids taken FROM the books table returns 3 of 3. The id list is well-formed (1,716 chars, 44 quoted UUIDs). And both stores were checked, not one: book-service and sharing-service both point at `postgres:5432` (infra-postgres-1), and the second postgres on this host (lw-iso-postgres-1, 481 books) does not hold them either. WHY THEY ARE GONE -- AND IT IS ENVIRONMENT DRIFT, NOT A CODE DEFECT: the 44 policies were last written 2026-04-04 (22), 04-05 (16), 04-10, and 4 in June the OLDEST book that still exists 2026-07-20 The book store was reset after those policies were written; the sharing store was not. That is the same shape as the host-env drift this loop h…

**⚠ Correction on record (my_recommendation_2026_09_02):** CLOSE THIS QUESTION -- its premise is refuted and the tool it doubted is correct. Then decide the residue separately, because it is a different question with a much smaller stake: (a) EMIT A BOOK-DELETED EVENT and have sharing-service drop the policy. Correct and consistent with how this platform already propagates cross-service facts (the outbox relay auto-routes any aggregate_type), but it is new wiring on a destructive path. (b) A PERIODIC SWEEP that deletes policies whose book no longer resolves. Cheapest, self-healing, and tolerant of the drift that caused this -- but it is a reconciler, and a reconciler hides the missing event rather than adding it. (c) LEAVE IT. Nothing user-visible is wrong; the cost is that an internal count misleads a reader, which is exactly what it did here for six weeks. I LEAN (a), and would not do it without also clearing the 57 existing orphans -- a fix that prevents new orphans while leaving the old ones would leave this question's own evidence standing and the next reader would re-open it.

**My recommendation:** NO RECOMMENDATION YET, AND THAT IS THE ANSWER. The question asks WHY the public catalogue lists nothing while 44 books are marked public, and its evidence field is EMPTY - no cause has been established, so every option would be a guess dressed as a fix. What is owed first is one measurement: does the catalogue query return rows for those 44 books, and if not, which predicate excludes them. Recorded as owed rather than answered.

