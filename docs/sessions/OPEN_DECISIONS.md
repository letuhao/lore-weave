# Open decisions — the tool-loop is waiting on these

*Generated 2026-08-31 by `scripts/toolloop/dq_digest.py`. Derived from the ledger at emit time — do not hand-edit; re-run it.*

**25 defects are open and 20 of these questions are what hold them.** The loop's own check reports every open defect as decision-blocked, so nothing else moves until some of these are ruled on.

A ruling goes on the question's row as an `answer_<date>` field. The loop reads it there and builds it **as worded** — if it cannot be built, the question comes back with the measurement showing why, rather than a substituted mechanism.

## ⚠ Read these first — their premise was refuted

Each of these carries a ruling or a premise that this loop measured and found wrong. Ruling on the question as originally worded would spend a decision on something that is not true.

- **DQ-T44** — Five *_restore tools have NO way to discover what they can restore, and unlike the canon-rule family none of them has an implemented listing to expos…
  - *what changed:* 2026-08-30 — scope corrected: three of the five named families are not live surfaces (nine of the ten *_restore tools are visibility: legacy), and the two that remain have NO list tool for the ruling's filter to land on. They need a new op=list on the unified…
- **DQ-T58** — An author asks to attach a motif to an ARC, and the platform has no tool that does that — `composition_motif_bind_edit` binds to a CHAPTER node. Shou…
  - *what changed:* 2026-08-30 — premise refuted: the tool was ALREADY advertised 5 of 5 on the arc prompt BEFORE the synonyms, so 'declare the phrasing so the turn reaches the tool' addressed a problem that did not exist. The refusal half is built and cannot fire while composit…
- **DQ-T64** — composition_build_cast_and_graph does in one call what vision-to-book and populate-from-notes do in two steps each. It is named by no workflow; its t…
  - *what changed:* 2026-08-30 — the ruling's own equivalence condition FAILED: the 'one call' tool is a six-op pipeline whose op=start returns a worklist and stops, and in 0 of 70 recorded sessions did anything call op=approve_plan. Re-pointing would swap a path that lands draf…
- **DQ-T66** — composition's confirm route names a BookClientError (502) upstream failure at _execute_authoring_run_gate -- 'could not read the book's chapters', di…
  - *what changed:* 🔴 THE QUESTION'S PREMISE IS WRONG, AND I WROTE IT. 2026-08-30. It asserts: 'there is no per-request fault-injection path (BookClient's base_url/timeout come from global settings shared by every concurrent request; overriding them to force a failure would affe…

## All open questions, by how many defects they release

### DQ-T71 — releases 3 defects

**Asked:** When a turn's REPLY contradicts its own tool results — in EITHER direction — what should the platform do: (a) append a deterministic server line stating what actually happened, the remedy DQ-T54 chose for the carded case; (b) steer the MODEL on the Tier-A approval path the way `_CONFIRM_CARD_STOP_NOTE` already steers the Tier-W/S path, so it never makes the premature claim it later over-corrects; or (c) neither, and accept that the author is occasionally told the opposite of what happened? BOTH DIRECTIONS ARE THE SAME DECISION and are asked together deliberately: the call SUCCEEDED and the reply says it failed — 28 of 1,786 runs (1.57%) the call REFUSED and the reply says it succeeded — 22…

**⚠ Correction on record (the_premise_verified_2026_08_30):** 28 of 1,786 recorded runs (1.57%) close by denying a write their own tool result confirms — across override, trunc, booksync and canonrestore batches. Re-measured on the current build, the founding scenario is 2 of 5 (it was 3 of 5 when filed). The mechanism is a premature past-tense claim on the card-minting pass, over-corrected after approval. The Tier-A approval path receives no equivalent of the Tier-W/S `_CONFIRM_CARD_STOP_NOTE`.

**⚠ Correction on record (the_second_direction_added_2026_08_30):** D-A-REFUSAL-BECOMES-A-CLAIM-THAT-THE-WORK-WAS-ALREADY-DONE was folded in here rather than raised as its own question. Its measured instance: `plan_validate` returned {ok: false, 'not found or not accessible'} and the reply said 'the S1-S8 validation have already been applied'. Nothing told it that; the one tool that could have refused, in the same turn, in its own context. Its control is worth your eye when you decide: the SAME refusal on another run of the same batch produced an honest report — 'I can't run a golden-rules validation … because the plan hasn't been formally validated through the system yet'. The model is not uniformly at fault on this shape, which is why (b) — steering — is plausible rather than hopeless.

**⚠ Correction on record (the_premise_number_is_DILUTED_corrected_2026_08_30):** 🔴 CORRECTING MY OWN QUESTION BEFORE YOU RULE ON IT — and the correction does not merely resize the problem, it REFUTES the symmetry the question is built on. The question states the two directions as near-equal, 1.57% and 1.23%, both over 1,786 runs. But most runs are in NEITHER trigger population: a run where no write landed cannot deny one, and a run where nothing was refused cannot claim past a refusal. Each direction has its own denominator and I used the same padded one for both. Re-derived with the shipped predicates over all 227 raw batch files, 2,112 runs: hits ELIGIBLE rate (pooled, as the question quotes it) landed -> denied 28 1,621 1.73% 1.33% refused -> claimed 35 810 4.32% 1.66% 🔴 THE SECOND DIRECTION IS 2.5x THE FIRST, not slightly rarer. Only 810 runs ever had a call refused, so a defect that lives only in those runs was being averaged across 1,300 runs where it could not occur. The numerator moved 22 -> 35 with corpus growth; the RATE change is dominated by finally using the right denominator. WHY THIS MATTERS TO THE RULING, since it is the only reason to correct a question rather than just note it. The question asks both directions together because they looked like one symmetric problem. They are not the same size: telling an author that work SUCCEEDED when their own call was REFUSED happens roughly 1 turn in 23 among turns that hit a refusal, against 1 in 58 for the inverse. If the two halves are ever traded off — and (b) steers only the Tier-A approval path, which is the FIRST direction's shape — the bigger half is the one the steer does not reach. In l…

**⚠ Correction on record (today_s_stream_fix_neither_helped_nor_hurt_this_2026_08_30):** A REGRESSION CHECK I OWED, run because today's fix creates the exact conditions this defect lives in. D-RAIL-PINNED-TURN-NEVER-COMPLETES now aborts a looping pass and STEERS the model to act — and a steered model resuming mid-turn is a plausible new source of premature and over-corrected claims. post-fix 40 eligible runs 0 denials 🔴 AND THAT IS NOT EVIDENCE OF ANYTHING, which is the point of writing it down rather than quoting it. At the measured 1.77% base rate, P(0 in 40) is about 0.49 — a coin flip. It is not an improvement and it is not a clean bill; it is too few runs to say either, and a later reader must not mine '0 of 40' as a result. THE INSTRUMENT WAS CHECKED TOO, and this one IS conclusive. `denied_a_write_that_landed` excludes the platform's own honest sentences by phrase, and its docstring records that an earlier version counted five of them as defects. Today's fix adds a NEW server-authored line an author can see — 'I got stuck deciding how to do that and stopped rather than loop.' — so it was tested directly against the detector: it does NOT match `_DENIED_THE_WRITE`, so the fix cannot inflate this measurement through its own give-up message.

**⚠ Correction on record (MY_OWN_JUSTIFICATION_FOR_b_IS_UNSUPPORTED__2026_08_30):** 🔴 I RAN THE CONTROL THAT COULD REFUTE MY OWN RECOMMENDATION, AND IT DID. My recommendation reads '(b) FIRST … the Tier-W/S path already PROVES the steer works on the identical shape'. That claim has never been measured. It has now. THE PLATFORM ALREADY RUNS THE A/B, which is why it looked like proof. A Tier-W/S tool returns a confirm card as its RESULT and the turn continues, so `_CONFIRM_CARD_STOP_NOTE` is appended and the model is steered. A Tier-A tool SUSPENDS — the model has already written its reply and there is no further pass — so no steer is possible. Two populations, one steered, one not, same defect. Tier-A suspend, NO steer possible 628 runs 6 false 'done' 0.96% confirm-token result, STEERED 69 runs 0 false 'done' 0.00% 🔴 AND ZERO OF 69 PROVES NOTHING. At the measured 0.96% base rate, the chance of seeing zero in 69 runs even if the steer does absolutely nothing is 0.52 — a coin flip. The steered population has never been large enough to demonstrate anything, so 'the Tier-W/S path already proves the steer works' is not a finding; it is an absence of counter-evidence read as support. WHAT THIS DOES TO THE RECOMMENDATION. (b) is still what I would try FIRST, but for a weaker and more honest reason: it is cheap, it rides an existing tool result, and it removes the premature claim rather than papering over it. The strong reason I gave — that an equivalent steer is already demonstrated to work here — is withdrawn. If the owner was weighing (b) against (a) partly on 'it is proven next door', that weight should come off. AND IT SHARPENS THE TEST I ALREADY ASKED FOR. M…

**⚠ Correction on record (option_a_s_MECHANISM_is_already_deployed_next_door_and_TRENDING__2026_08_30):** 🔴 WITHDRAWN WITHIN THE HOUR BY MY OWN CONTROL. Recorded in full rather than deleted, because what misled me is the thing worth keeping. I measured the shipped Tier-A suspend note before and after it landed and reported it as trending: BEFORE the note 520 runs 6 false 'done' 1.15% ON/AFTER 108 runs 0 false 'done' 0.00% THEN I ASKED WHICH SCENARIOS THE SIX CAME FROM, AND WHETHER THOSE SCENARIOS STILL RUN: 4x composition-entity-override-edit-work-scoped before 4 runs AFTER 0 runs 1x glossary-create-evidence-attrvalue before 26 runs AFTER 0 runs 1x kg-propose-edge-approve before 11 runs AFTER 0 runs instances whose scenario is exercised after the cut: ZERO of six. So the post-note population contains no run of any scenario that ever produced this defect. The comparison measures WHICH SCENARIOS WERE RUN, not whether the note works. 0 of 108 is not evidence of anything, and the asymmetry I drew from it — '(a) trending, (b) not' — is withdrawn entirely. Both options remain undemonstrated. AND THE 1.15% BASELINE WAS ITSELF A POOLED RATE OVER A MIXED POPULATION. One scenario produced 4 of the 6 in FOUR runs — a 100% rate — while everything else sat near zero. Quoting 1.15% as 'the rate' averages a scenario that always fails with hundreds that never do, which also makes the '259 runs to significance' figure I derived from it meaningless. WHAT WOULD ACTUALLY TEST THE NOTE: re-run composition-entity-override-edit-work-scoped, which failed 4 of 4 before the note shipped. Four runs at a 100% prior is a real experiment; 108 runs of scenarios that never exhibited the defect is not. That i…

**⚠ Correction on record (the_test_I_named_has_NO_scenario_definition__2026_08_30):** 🔴 THE EXPERIMENT I NAMED CANNOT BE RUN, AND FINDING OUT WHY EXPOSED A SMALL CLASS. Having withdrawn the '(a) is trending' finding, I said the honest test was to re-run `composition-entity-override-edit-work-scoped`, which failed 4 of 4 before the suspend note shipped. That scenario HAS NO DEFINITION IN THE REPO. It exists only as recorded results in docs/eval/toolloop/2026-08-14/c-override3 and c-override4. CENSUSED, because one missing scenario is an annoyance and a pattern is a problem: scenario ids DEFINED in scripts/toolloop 307 scenario ids with RECORDED runs 148 recorded but NO LONGER DEFINED 5 (117 runs) 85 runs composition-glossary-build-with-an-ontology 12 runs composition-arc-apply-named 10 runs composition-entity-override-edit-work-scoped 5 runs glossary-build-is-it-in-the-catalogue 5 runs plan-bootstrap-apply-compiled TWO OPEN ROWS REST ON THEM. `composition-entity-override-edit-work-scoped` produced 4 of the 6 instances behind DQ-T71's Tier-A figures, and `composition-glossary-build-with-an-ontology` carries one of D-A-TURN-PROMISES-WORK-THAT-CONTINUES-AFTER-IT-ENDS's seven. Their evidence stands — the runs happened and are on disk — but neither can be RE-RUN to test a fix, which is precisely what a defect row's best test case is for. WHAT IS RECOVERABLE, and it is most of it: the raw batch records the prompt verbatim — 'Override entity field — in this WORK only, Aldric Vane should be a cartographer, not a soldier. Leave the book's glossary entry as it is.' — plus the tool under test (composition_entity_override_edit) and the ids the run used. What is NOT reco…

**⚠ Correction on record (THE_HONEST_EXPERIMENT_WAS_RESTORED_AND_RUN__2026_08_30):** 🔴 THE TEST I NAMED AFTER WITHDRAWING THE POOLED ONE HAS NOW BEEN RUN, and it is decisive where the 108-run comparison was not. THE SCENARIO HAD TO BE REBUILT FIRST. `composition-entity-override-edit-work-scoped` produced 4 of the 6 recorded Tier-A false-'done' instances and had NO definition in the repo — one of five orphans. It was reconstructed from the recording rather than invented: the prompt verbatim from the raw batch, the tier and approval shape from the recorded `pending_approval`, the op and field from the recorded call args, and the SEED copied from scenarios-c-evidence2.json, which already seeds the same entity with an `occupation` attribute. One deliberate difference: the value is seeded as `soldier`, so the prompt's change to cartographer is a real change and not a no-op. It lives at scripts/toolloop/scenarios-c-override-restored.json. BEFORE the suspend note (c-override3/4, 2026-08-14) 4 of 4 claimed DONE falsely AFTER (c-override-restored, K=5, today) 0 of 5 Fisher exact, two-tailed p = 0.0079 AND THE MECHANISM IS VISIBLE IN THE OUTPUT. All five replies end with the server-appended line itself — 'Nothing has been saved yet; confirm the card above to apply it.' The turn shape is unchanged from the original: 5 of 5 called the tool, 5 of 5 suspended on a Tier-A card, 0 errored. WHY THIS COUNTS WHERE THE EARLIER ONE DID NOT: it is the SAME scenario, at a 100% prior. The withdrawn comparison put 0 of 108 runs against a 1.15% pooled rate over scenarios that had never once exhibited the defect. Four runs at 100% is an experiment; a hundred runs of a population tha…

**⚠ Correction on record (a_FRESH_instance_population_from_the_restored_arc_arm__2026_08_30):** The restored confirmation-turn scenario (c-arcapply-confirm, K=5) produced THREE fresh instances of this question's SECOND direction — the call refused and the reply claims success — in a single K=5 batch: claimed a write its own call refused 3 of 5 e.g. "I've applied 'The Vagepepo Ascent' to your book. This template sets up a three-chapter arc…" 🔴 RE-DERIVED 2026-08-30 AGAINST THE FIXED DETECTOR, AND IT STANDS AT 3 OF 5. When the server-sentence defect below was found, every figure from this detector became suspect, including this one — these runs are dated the day the suspend line shipped. Re-running the corrected detector over the same batch returns the SAME three, because none of the three carries a server line at all: they are the model's own sentences, on a turn where its tool never reached the wire. The withdrawal below removed 13 artefacts and no real instance. AND THE SHAPE IS THE ONE THE CORRECTED FIGURES POINT AT. The refused→claimed direction is 1.9x the inverse (3.14% vs 1.66% on their own trigger populations — the corrected numbers, see below); here it fires at 60% on a turn where the tool was not even on the wire. A turn that cannot act is the condition under which the model asserts it did — which is also what D-A-TURN-PROMISES-WORK-THAT-CONTINUES-AFTER-IT-ENDS observes from the future tense. This is a live, reproducible population for the direction option (b) does NOT reach: (b) steers the Tier-A approval path, and no card is involved here at all.

**⚠ Correction on record (the_orphan_class_RESOLVED__3_restored_2_were_renamed__2026_08_30):** Closing the orphan thread honestly, and correcting my own framing of it. I recorded '5 scenarios have results but no definition' as if all five needed rebuilding. Checked each against the LIVE scenario set by prompt, and only three did. RESTORED — no live scenario shares the prompt, so the evidence was unrunnable: composition-entity-override-edit-work-scoped -> scenarios-c-override-restored.json composition-glossary-build-with-an-ontology -> scenarios-c-gbuild-restored.json composition-arc-apply-named -> scenarios-c-arcapply-confirm.json RENAMED, NOT LOST — a live scenario already runs the same prompt, so rebuilding them would have created a DUPLICATE rather than restored anything: plan-bootstrap-apply-compiled lives on as plan-bootstrap-apply-compiled- answered (c-planapply2) and two siblings glossary-build-is-it-in-the-catalogue lives on as glossary-build-past-the-card, in the SAME batch file its runs came from 🔴 THE CHECK MATTERED. 'It has results and no definition' looks like a missing test case and is sometimes just an id that moved. Rebuilding those two would have added scenarios that already exist under better names — inflating the suite while claiming to repair it, which is the same shape as splitting a defect to inflate a count. WHAT THE THREE RESTORATIONS BOUGHT, since the point was never tidiness: the override arm gave a p=0.0079 before/after on the suspend note; the gbuild arm exposed the pipeline tool taking a turn from its parts and led to 0-of-80; the arc arm settled a re-attribution D-R1-IS-KEYED-ON-THIS-TURN had explicitly declined to claim, and produced t…

**⚠ Correction on record (a_REGRESSION_on_two_derivative_scenarios__1_of_60_to_8_of_8__2026_08_30):** 🔴 THE REGRESSION IS WITHDRAWN — IT WAS THE PLATFORM'S OWN SENTENCE BEING COUNTED AS THE MODEL'S CLAIM. I recorded a decisive within-scenario regression (1 of 60 -> 8 of 8, p = 0.0000) and then read the replies. They were HONEST: 'Nothing has been saved yet; confirm the card above to apply it.I've attempted to apply the override for Aldric Vane, but I cannot find an existing "what-if" derivative … Would you like me to create one?' `_CLAIMED_DONE` matches `has been saved`, which is a SUBSTRING of the suspend line the server appends. The negation in 'NOTHING has been saved yet' is invisible to the pattern. And that line shipped 2026-08-28 — the exact date of my cut — so every carded turn after it looked like a new completion claim. The 'regression' was the fix for a NEIGHBOURING row arriving. corpus-wide hits from the shipped detector 39 survive once the server line is removed 26 were the server line ALONE 13 <- all dated on/after 2026-08-28, and all 13 are the 8+5 'regression' THE DETECTOR IS FIXED, not worked around. `_SERVER_APPENDED_LINES` already existed and was already applied by the sibling detector `denied_a_write_that_landed`, whose own docstring records it counting five server sentences as defects. The mechanism was there; it simply was not applied on this side. One line, at the one place that destroys the distinction. AND THE FIGURES I GAVE YOU FOR THIS QUESTION MOVE AGAIN: as first stated after dilution fix after THIS fix landed -> denied 1.57% 1.73% 1.66% refused -> claimed 1.23% 4.32% 3.14% ratio ~equal 2.5x 1.9x The asymmetry survives — refused-then-claimed is…

**⚠ Correction on record (the_artefact_is_CONTAINED_to_two_batches_2026_08_30):** THE ARTEFACT IS CONTAINED, AND THE SWEPT FIGURE ABOVE IS UNHARMED. When the server-sentence defect was found, every number this detector ever produced became suspect. Rather than reason about which, the whole corpus was swept BOTH WAYS through the SHIPPED predicate — the fix neutralised at runtime by emptying `_SERVER_APPENDED_LINES`, not by a second implementation of the rule: runs scanned 2,197 (242 batches) flagged BEFORE the fix 39 flagged AFTER the fix 26 All 13 losses fall in TWO batches, and both go to zero: 2026-08-28/c-fixturename1 5 -> 0 2026-08-28/c-fixturename2 8 -> 0 Every other batch is unchanged, to the run. So the live-scoped sweep recorded above — 22 across c-memory, c-cancel1, c-motiflink8/9, c-perop1, c1-approve, c-hollowdoc2, c-kgedge1, c-override8 and c-hyphen1 — needs no correction: not one of its instances was a server sentence. The class stands; only the 'regression' was ever an artefact, and it was exactly these two batches. 🔴 AND THE INSTRUMENT CHECK THAT MADE THIS READABLE. The first version of this sweep re-implemented the predicate inline to get the 'before' number, and returned deltas that were NEGATIVE — it counted fewer than the shipped detector, because `store_diff` is filtered against TURN_BOOKKEEPING_TABLES rather than tested for truthiness. A second implementation of a rule is not a measurement of that rule. Driving the shipped code with its constant emptied is.

**My recommendation:** (b) FIRST, because it removes the cause rather than papering the symptom: the premature claim is what the model is later over-correcting, and the Tier-W/S path already proves the steer works on the identical shape. It costs nothing extra — the note rides an existing tool result — and if it works the denial has nothing to correct. 🔴 HELD WEAKLY, AND HERE IS WHY. This loop has measured and refuted FIVE prose interventions, and (b) is a sixth. The honest test is cheap and I would run it before spending anything on (a): ship the steer, re-run the founding scenario, and see whether the premature claim disappears. If it does not, (a) is the fallback — and (a) is the remedy you already chose for t…

**Why it is yours, not mine:** (a) and (c) change what an author reads, and this loop's standing rule is that it does not invent user-facing prose. (b) changes how the model is steered on the most common write path in the product, which is a behaviour change with its own cost. All three are judgements about the product, not correctness.

**Blocks:** `D-A-REFUSAL-BECOMES-A-CLAIM-THAT-THE-WORK-WAS-ALREADY-DONE`, `D-A-TURN-DENIES-THE-WRITE-ITS-OWN-TOOL-RESULT-CONFIRMS`, `D-THE-MODEL-CLAIMED-A-CANCEL-THAT-THE-TOOL-REFUSED`

### DQ-T65 — releases 2 defects

**Asked:** jobs-service's ReconcileSweeper only ever calls `GET /internal/{svc}/jobs?since=` -- a DELTA fetch. Checked: composition-service registers exactly one internal jobs endpoint (`reconcile_jobs`, the same ?since= one) and no full-enumeration mode exists anywhere in the five registered sources (composition, knowledge, video_gen, lore_enrichment, translation, book). So the backstop can apply a row it is TOLD changed and can never notice a row that stopped being told about -- which is why 22 controllable jobs across three services have sat at running/pending for 18-47 days with a single write and then silence, and jobs_list keeps advertising cancel on them. Building detection needs each owning se…

**⚠ Correction on record (the_retention_number_is_RE_DERIVED_and_it_is_STATIC_2026_08_30):** This question blocks on a fact it could not settle: 'are the 414 COMPLETED composition jobs the projection carries with no owning row left intentional retention, or the same leak in a terminal state?' The count itself is now re-derived against both stores, two days on. composition rows in job_projection 910 owning rows in generation_job 464 projection rows with NO owning row 446 by status: 414 completed ages 8-82 days 12 running ages 8-51 10 failed ages 8-82 10 pending ages 8-10 created in the last 2 days: 0 🔴 THE COMPLETED FIGURE IS EXACTLY 414, UNCHANGED SINCE 2026-08-28, and I nearly reported the opposite. Seeing 446 against the question's 414 I first read it as '+32 in two days, so it is a live leak, and the urgency is higher than the question assumes'. The status split refutes that: the extra 32 are not new and not completed — they are orphans in OTHER states that the question's number never covered, and nothing at all has been orphaned in the last 8 days. On the evidence available the terminal-state population is STATIC, which is what the intentional-retention reading predicts and the live-leak reading does not. 🔴 BUT THE QUESTION'S OWN 'WHERE IT NO LONGER MATTERS' DOES NOT COVER ALL OF THEM. Twenty-two of the orphans are in CONTROLLABLE states — 12 running and 10 pending — and for those the projection is advertising a job an author can cancel while the owning row is gone. That is the case where it does matter, and it is 22 rows rather than the zero the completed-only framing implies. NOT CLAIMED: that these 22 are the same 22 the question describes as 'controllable…

**My recommendation:** Do NOT build a five-service enumeration pass on this evidence alone -- the measured population is small (22 rows, ~2.4% of composition's traffic in the period) and this loop has already found three separate five-service redesigns are not the right first move today (T63's marker list, T44's per-family listing). The cheaper, already-decided fix is DQ-T46: jobs-service verifies control_caps against the owning service AT LIST TIME rather than trusting the projection. That closes the symptom (a bad cap offered to an author) without touching the reconcile sweep at all, and it is a single chokepoint. This row (the backstop's structural blindness) is real and should stay open, but is a SEPARATE, la…

**Blocks:** `D-A-SINCE-WATERMARK-BACKSTOP-IS-BLIND-TO-A-JOB-THAT-VANISHED`, `D-JOBS-LIST-ADVERTISES-CANCEL-ON-JOBS-THAT-CANNOT-BE-CANCELLED`

### DQ-T80 — releases 2 defects

**Asked:** This row is named for a TOOL — composition_motif_search — and three re-derivations have now shown the tool is not what discriminates. Should it be re-characterised, and if so along which axis: (a) SPLIT BY ERROR KIND, moving the `upstream sent "error" with no error message` runs to D-UPSTREAM-ERROR-WITH-NO-MESSAGE, which has already read that failure's real body off the wire, and keeping only the ReadTimeout runs here; (b) re-characterise by SCENARIO, as this row's own earlier recommendation proposed; (c) close it as SUPERSEDED, on the grounds that both halves now belong to rows that name their mechanism; (d) leave it as is.

**My recommendation:** RECOMMEND (a), SPLIT BY ERROR KIND — and it explicitly REPLACES this row's earlier recommendation of the scenario axis, which today's measurement refutes. Re-derived over all 227 raw batch files, by recorded error message: composition-motif-link-edit 19 ReadTimeout + 29 upstream-no-message composition-motif-adopt-named 0 ReadTimeout + 10 upstream-no-message translation-update-settings 15 ReadTimeout + 0 upstream-no-message The scenario axis groups link-edit and adopt-named together because both call the search. By error kind they are not one thing: adopt-named is PURE upstream-silent and shares nothing with the ReadTimeout half. Grouping them would put a fixed mechanism and an unfixed one i…

**Why it is yours, not mine:** It changes what a defect row MEANS and moves measured evidence between rows, which the standing rule reserves to the owner ('never split a defect to inflate the count' cuts both ways — merging and re-attributing are the same act in reverse). It also retires my OWN earlier recommendation on this row, and I should not overwrite a recommendation the owner has not yet seen.

**Blocks:** `D-THE-STALL-CONCENTRATES-ON-COMPOSITION-MOTIF-SEARCH`, `D-UPSTREAM-ERROR-WITH-NO-MESSAGE`

### DQ-T44 — releases 1 defect

**Asked:** Five *_restore tools have NO way to discover what they can restore, and unlike the canon-rule family none of them has an implemented listing to expose: composition_entity_override_restore, composition_scene_link_restore, composition_structure_template_restore, glossary_entity_restore, glossary_user_restore. Each takes an id, and the only way to hold that id is to have written it down BEFORE deleting. Should each family get a listing surface, and as WHAT - a flag on a tool that does not yet exist, or a per-domain recycle-bin read?

**⚠ Correction on record (answer_2026_08_28):** OWNER: a `deleted=true` (soft-deleted) FILTER on each family's EXISTING list/search tool. No new tools. The five *_restore tools keep their id argument; what changes is that something can now produce that id. SCOPE, so the work is not mistaken for one change: five families across two services — composition entity_override / scene_link / structure_template, glossary entity / user. Each needs its list tool to accept the filter AND its query to stop excluding soft-deleted rows, which is the half that is easy to miss. The owner explicitly declined a per-domain recycle-bin tool (new surface in a catalogue this loop is shrinking) and declined dropping the restore tools.

**⚠ Correction on record (reopened_2026_08_28):** The owner's answer -- 'a deleted=true filter on each family's EXISTING list/search tool' -- is buildable for only 2 of the 5 named families, checked against the LIVE tool surface (deprecated tools excluded per DQ-T36): structure_template HAS a live list op (composition_structure_template_edit op=list); FIXED 2026-08-28, proven live canon rule already fixed on this row before today entity_override NO list/search MCP tool exists at all. list_overrides_for_work is repo/router-only (packer + FE REST), never exposed via MCP. scene_link NO list/search MCP tool of its own EITHER, but composition_list_outline already returns scene_links AND already has an include_archived argument -- it just isn't passed to the scene_links.list_by_project call underneath glossary_user visibility=legacy -- OUT OF SCOPE per DQ-T36 For entity_override and scene_link, 'a filter on an existing list tool' has no tool to land on for entity_override, and a filter that already exists elsewhere but isn't wired through for scene_link -- two different remedies, neither of which is 'add a filter argument' as literally stated.

**⚠ Correction on record (the_corrected_question):** Two sub-decisions, since the two remaining live families need different things: (a) entity_override has no list capability anywhere on the wire. Should composition_entity_override_edit gain a new op=list (same shape as composition_structure_template_edit already has for create/update/clone/archive/restore/list) -- a new OPERATION on a tool that already ships, not a new catalogue tool -- or should this family be left undiscoverable? (b) scene_link's discovery ALREADY EXISTS on composition_list_outline, which already takes include_archived for outline nodes; the fix is wiring that same flag through to the scene_links.list_by_project call it already makes. This is the cheapest of everything in this row -- no new argument, no new op, one existing flag reaching one more query. My RECOMMENDATION is to do this one without waiting for (a), since it is architecturally identical to the structure-template fix just shipped and carries the same low risk. Flagging rather than doing it unilaterally because the standing rule is that DQs are decided by the owner, and this DQ is still open on (a).

**⚠ Correction on record (the_scope_is_TWO_families_not_five_2026_08_30):** The ruling names five families. Re-derived over the LIVE catalogue, three of them are not live surfaces at all — nine of the ten `*_restore` tools carry `visibility: legacy`, including `glossary_user_restore`, whose own description says user-tier standards 'are managed by YOU in the GUI — kept for existing callers only'. OF THE FIVE NAMED: glossary entity BUILT AND PROVEN LIVE 2026-08-30 (restore 0-of-5 -> 5-of-5 called) structure template already fixed 2026-08-28 on its unified op=list glossary user LEGACY — not a gap; out of scope by the deprecated-tools rule entity override NO list tool exists; the ruling has nothing to filter scene link NO list tool exists; the ruling has nothing to filter Confirmed from the deployed schemas: composition_entity_override_edit's op enum is ['add','update','delete','restore'] and composition_scene_link_edit's is ['create','delete','restore'] — each restores, neither lists. MY RECOMMENDATION, unchanged in substance and now precisely scoped: the last two need a new `op=list` on the unified `*_edit` tool they already ship. That is an OPERATION on an existing tool, not a new tool, so it does not add a catalogue entry — which is what the ruling declined. NOT BUILT: adding an op changes a tool's contract and the ruling as worded does not authorise it.

**⚠ Correction on record (returned_corrected):** 2026-08-30 — scope corrected: three of the five named families are not live surfaces (nine of the ten *_restore tools are visibility: legacy), and the two that remain have NO list tool for the ruling's filter to land on. They need a new op=list on the unified *_edit tool they already ship, which the ruling as worded does not authorise.

**⚠ Correction on record (the_DEMAND_measurement_CANNOT_be_taken_from_this_store__2026_08_30):** My recommendation says 'I have not measured how often an author actually needs any of these five undos, and without that the ranking above is a judgement about evidence, not about value.' I tried. THE STORE CANNOT ANSWER IT, and that is the finding rather than a failure to look. every *_restore / *_undo call ever recorded: glossary_entity_restore 32 calls composition_entity_override_restore 0 composition_scene_link_restore 0 composition_structure_template_restore 0 glossary_user_restore 0 successful *_delete calls, whole corpus: 17 across 16 sessions 🔴 ALL 32 ARE HARNESS TRAFFIC, NOT AUTHORS. They fall on exactly two days — 27 on 2026-08-22 and 5 on 2026-08-30, the latter being this loop's own batch after the recycle-bin filter shipped — and there are 32 DISTINCT SESSIONS for 32 calls, which is the one-throwaway-fixture-per-run signature. This is a test corpus; it records what the loop asked for, not what an author reached for. So 'how often does an author need this undo' is not answerable here at all, and no number from this store should be offered as if it were. WHAT IT CAN HONESTLY SAY, and it still bears on the ruling: - FOUR OF THE FIVE FAMILIES HAVE NEVER BEEN CALLED ONCE, by anyone, including the loop. A listing surface for a tool nothing has ever invoked is capability built on no demand signal whatsoever — which is the same direction the returned correction already pointed (three of the five are not live surfaces). - THE UPSTREAM EVENT IS RARE TOO: 17 successful deletes in the entire corpus. Even a perfect undo has had 17 opportunities to matter here. 🔴 AND ONE NUM…

**My recommendation:** Do NOT add five tools. The measured shape says the cheap half is already done: where a repository supports listing archived rows, exposing it costs one optional argument and no catalogue slot - that is what was done for canon rules today. For these five the repositories have no such support, so any fix is new capability in the service FIRST and a surface decision second. My recommendation is to treat them by BLAST RADIUS rather than uniformly: glossary_entity_restore has a MEASURED live failure (batch 25, 5 of 5 runs, the model correctly answered 'I can't see her in the trash without an ID'), so it is the one with evidence behind it. The other four have none yet.

**Blocks:** `D-RESTORE-WITH-NO-WAY-TO-SEE-WHAT-IS-RESTORABLE`

### DQ-T58 — releases 1 defect

**Asked:** An author asks to attach a motif to an ARC, and the platform has no tool that does that — `composition_motif_bind_edit` binds to a CHAPTER node. Should (a) the tool accept an arc and bind the motif to the chapters beneath it, (b) the arc phrasing be declared on the tool so the turn reaches it and the author gets an explicit refusal naming chapters, or (c) neither, and the request stay unreachable?

**⚠ Correction on record (the_premise_verified_2026_08_27):** c-silentprobe1 (the row's own): the natural phrasing reaches the tool on 0 of 27 passes, the model calls it 0 of 5, and reports the binding done on 5 of 5 with the store unchanged. c-bindwire1, same fixture, wording changed to the tool's declared synonym: on the wire 5 of 5, called 5 of 5, claims the binding done on 0 of 5, and the tool answers `node_id must be a UUID — received 'arc_1'` on 3 of 5. The store is unchanged in BOTH arms.

**⚠ Correction on record (answer_2026_08_28):** OWNER: DECLARE THE ARC PHRASING on composition_motif_bind_edit so the turn reaches the tool and the author gets an EXPLICIT refusal naming chapters. The tool is not taught to accept an arc. The owner declined inventing the semantic (is a motif on an arc a property of the arc, or a shortcut for its chapters? nothing in the model defines it) and declined leaving the request unreachable. So this is a declaration + refusal-wording change, and the bar is that the refusal must say what the tool DOES bind to — a refusal that does not name chapters leaves the author exactly as stuck.

**⚠ Correction on record (reopened_2026_08_28):** 🔴 THE ANSWER CANNOT BE BUILT AS WORDED, and the reason is mechanical rather than a judgement I am entitled to make — so it goes back rather than being quietly substituted. The ruling was: DECLARE THE ARC PHRASING on composition_motif_bind_edit so the turn reaches the tool and the author gets an explicit refusal naming chapters. `answerable_tools` matches a synonym as a CONTIGUOUS phrase. The measured prompt is 'Attach Emberfall Seam to the opening arc as this book's motif.' — the motif's NAME sits between the verb and the target, so no arc-shaped phrase spans it. Tested against the live matcher in the deployed container: 'attach motif to arc' -> no match 'bind motif to arc' -> no match 'set arc motif' -> no match 'motif for the arc' -> no match 'attach to the arc' -> no match 'arc motif' -> no match The only strings that DO match are ones that should not be declared: 'opening arc' / 'to the opening arc' — the FIXTURE's arc title. Overfitting the test. 'attach' — matched 2 of 4 unrelated control prompts. 'motif' — matched 4 of 4, i.e. it would force this Tier-A WRITE onto every turn that says the word, including pure reads. So (b) as worded buys either a fixture-shaped declaration or an over-broad one that puts a destructive write on the wire for questions. AND THE PREMISE MOVED UNDERNEATH THE QUESTION. Re-measured 2026-08-28 (c-bindarc1): the tool is now on the wire 31 of 31 passes (was 0 of 27) and the model claims the binding on 0 of 5 (was 5 of 5), with two runs naming 'chapter or scene' unprompted. The unreachability the question was asked about is largely gone, though…

**⚠ Correction on record (built_and_MEASURED_2026_08_30):** BOTH HALVES WERE BUILT. One is proven correct and unproven live; the other is measurably INERT, and the measurement says the ruling's premise does not hold. 🔴 HALF ONE — 'DECLARE THE ARC PHRASING ... so the turn reaches the tool' — WAS SOLVING A PROBLEM THAT DOES NOT EXIST. The tool was ALREADY reaching the turn. Measured against the server's own advertised_tools, on the batch that was run BEFORE this change (c-bindarc1, the arc prompt 'Attach Emberfall Seam to the opening arc as this book's motif'): composition_motif_bind_edit advertised 5 of 5 BEFORE the synonyms composition_motif_bind_edit advertised 5 of 5 AFTER (c-bindarc2) It arrives through the DOMAIN HOT SET, not through R1, so declaring synonyms changes nothing here. And the synonyms do not even match: R1 matches a CONTIGUOUS phrase, and the author's sentence puts the motif's NAME between the verb and the target — 'Attach **Emberfall Seam** to the opening arc' — so no arc-shaped phrase spans it without becoming generic enough to fire on unrelated turns. Verified: with the five new synonyms declared, `answerable_tools` on that exact prompt still returns the EMPTY SET. 🔴 HALF TWO — the refusal naming chapters — IS BUILT AND CANNOT FIRE YET. It is correct and guarded (6 tests, 2 proven red), and it never ran in 5 live runs because the model cannot FORM the call that would be refused: to be told 'that is an arc, this tool binds chapters' it must first pass an arc id, and `composition_arc_list` was advertised 0 of 5 in BOTH batches. The model called `composition_list_outline` instead (advertised 5 of 5, called 5 of 5),…

**⚠ Correction on record (returned_corrected):** 2026-08-30 — premise refuted: the tool was ALREADY advertised 5 of 5 on the arc prompt BEFORE the synonyms, so 'declare the phrasing so the turn reaches the tool' addressed a problem that did not exist. The refusal half is built and cannot fire while composition_arc_list is off the wire (0 of 5), so the ruling's stated outcome is unreachable without a surfacing change.

**My recommendation:** (b), and only because it is honest: an author who asks for something the platform cannot do should be told so by the tool that owns the concept, not left with a confident sentence describing a change that did not happen. It is the cheapest of the three and it converts the worst outcome — a false report — into a plain one. (a) is the right answer if binding a motif across an arc is a capability the product wants, which is not a question this loop can settle.

**Why it is yours, not mine:** (a) is a capability change to a Tier-A write. (b) deliberately routes a request to a tool that cannot satisfy it, trading a confabulation for a guaranteed error — better, but a product judgement about what an author should be told. (c) accepts that the measured outcome of the natural phrasing is an invented success.

**Blocks:** `D-THE-MODEL-CLAIMS-A-BINDING-IT-NEVER-MADE`

### DQ-T64 — releases 1 defect

**Asked:** composition_build_cast_and_graph does in one call what vision-to-book and populate-from-notes do in two steps each. It is named by no workflow; its two parts are named by two. Should it get a workflow of its own, should the existing recipes be re-pointed at it, or should the tool be withdrawn as a duplicate of a path the platform already teaches?

**⚠ Correction on record (the_premise_verified_2026_08_28):** All 12 published system workflows in loreweave_agent_registry: the tool appears in 0, its two rivals in 2 each (vision-to-book steps 5-6, populate-from-notes). kg-build assumes the cast already exists; glossary-bootstrap is ontology setup. R1 forces exactly this tool and no rival for its own prompt. The tool WORKS when driven directly over MCP (established on this row) and has 0 organic calls since 2026-07-27 against 2,788 across 62 other composition tools.

**⚠ Correction on record (answer_2026_08_28):** OWNER: RE-POINT THE EXISTING RECIPES at composition_build_cast_and_graph. One call replaces the two-step pair in vision-to-book and populate-from-notes; no third path is added and the tool stops competing with its own parts. 🔴 CONDITIONAL ON AN EQUIVALENCE CHECK, which was part of the recommendation: the one-call tool must be shown on a throwaway book to produce what the two-step recipes produce. Re-pointing a taught path onto a worse one would be a bigger defect than the duplication. If it is not equivalent, that is a different cause and comes back rather than shipping.

**⚠ Correction on record (reopened_2026_08_28):** The owner's answer -- re-point the existing recipes -- was given with a condition I attached: verify equivalence first. Checked, and 're-point' is not the 1-for-1 step swap it reads as. composition_build_cast_and_graph is a FIVE-CALL state machine, not a single tool: op=start (mints a run, plans a worklist) -> poll op=status -> op=approve_plan (a HUMAN GATE -- the worklist is reviewed) -> poll op=status again -> op=project_kg -> op=approve_edges (a SECOND human gate -- the proposed relationships are reviewed). It is also async_job=True and paid=True, same as the two steps it would replace (glossary_extract_entities_from_doc is also paid, but SYNCHRONOUS -- one call, no polling, no gate). vision-to-book's and populate-from-notes's steps 5-6 (capture-cast, save-cast) are TWO plain `gate: none` steps, one tool call each. Re-pointing them at build_cast_and_graph as literally 'the same two slots, new tool' does not fit -- the tool needs five calls and two confirm gates, not two calls and zero gates. It IS representable in the rail, though -- checked against precedent rather than assumed impossible. vision-to-book's own step 3 already chains a gate:confirm step to an earlier step's output via inputs_map (apply-categories consumes adopt-categories.confirm_token), and kg-build already threads repeat/done_when polling for kg_add_nodes. The PIECES exist; assembling them into build_cast_and_graph's five ops has not been done or measured.

**⚠ Correction on record (the_corrected_question):** Two shapes, both buildable, genuinely different in size and product surface: (a) EXPAND the two existing recipes. Replace capture-cast/save-cast in BOTH vision-to-book and populate-from-notes with the full five-step sequence (start / approve_plan-gated / poll / project_kg / approve_edges-gated). Keeps the two published workflows as the only entry points; grows each from ~7-9 steps to ~10-12 and adds a SECOND confirm gate neither currently has mid-recipe. (b) A SMALL NEW WORKFLOW whose five steps ARE build_cast_and_graph's own op sequence -- the row's ORIGINAL recommendation, before I offered 're-point' as an alternative. Simpler per-workflow, but adds a THIRD entry point next to the two it would partially overlap with, and vision-to-book/populate-from-notes would still inline the old two-step sequence unless also edited to point at it -- and workflows have no step type that CALLS another workflow, so 'point at it' means duplicating the reference by hand, not composing. MY RECOMMENDATION, corrected from the unconditional 're-point' I gave before: (b) is safer and smaller to ship first -- it does not touch two already-published recipes users may depend on today -- and (a) can follow once (b) is measured working end to end. I am not choosing between them; both are real product-surface decisions.

**⚠ Correction on record (the_equivalence_check_FAILED_2026_08_30):** 🔴 THE RULING'S OWN CONDITION IS NOT MET, SO NOTHING WAS RE-POINTED. The answer says: 'the one-call tool must be shown on a throwaway book to produce what the two-step recipes produce. Re-pointing a taught path onto a worse one would be a bigger defect than the duplication. If it is not equivalent, that is a different cause and comes back rather than shipping.' It comes back. IT IS NOT ONE CALL. `composition_build_cast_and_graph` is a SIX-OP pipeline by its own declaration — start · approve_plan · status · project_kg · approve_edges · cancel — and `op=start` returns `status: "plan_ready"` with `next: "Show the user this worklist; call op=approve_plan when they agree."` Reaching saved entities takes start THEN approve_plan, with a human approval between them; reaching the graph takes five. 🔴 AND IT HAS NEVER ONCE GOT PAST THE FIRST OP. Every recorded call, across the whole store: op=start 73 calls 55 ok op absent 13 calls 0 ok op=approve_plan 0 calls — in 0 of 70 sessions op=project_kg 0 calls — in 0 of 70 sessions So the tool has produced a worklist 55 times and a saved entity ZERO times through the chat path. WHAT IT WOULD REPLACE, by contrast, saves and needs no confirm step — `populate-from-notes`'s own notes say the drafts are 'saved as review drafts, not canon — there is no separate confirm step': glossary_book_ontology_read 612 calls 536 ok 376 sessions glossary_extract_entities_from_doc 122 calls 93 ok 120 sessions glossary_propose_entities 416 calls 223 ok 269 sessions Re-pointing would replace a path that lands drafts with one that has never finished.

**⚠ Correction on record (where_the_premise_came_from):** Worth naming, because the ruling was answered in good faith on it: the phrase 'in ONE call' is the TOOL'S OWN DESCRIPTION. It opens 'Build the KNOWLEDGE GRAPH and the CAST for a book from a story you have been given — in ONE call' and then, in the same paragraph, lists six ops and instructs 'Always show the user the worklist before approving it'. The description contradicts itself, and the question inherited the first half. That is a defect in the DESCRIPTION independent of this decision, and it is measurable: 0 of 70 sessions ever called the second op, which is exactly what a reader of the first sentence would not expect.

**⚠ Correction on record (returned_corrected):** 2026-08-30 — the ruling's own equivalence condition FAILED: the 'one call' tool is a six-op pipeline whose op=start returns a worklist and stops, and in 0 of 70 recorded sessions did anything call op=approve_plan. Re-pointing would swap a path that lands drafts for one that has never finished.

**⚠ Correction on record (the_pipeline_tool_TOOK_a_turn_from_its_parts__measured_2026_08_30):** 🔴 A LIVE BEFORE/AFTER ON AN IDENTICAL PROMPT, produced by restoring an orphaned scenario (scripts/toolloop/scenarios-c-gbuild-restored.json — the prompt is verbatim from the 2026-08 recording, story text included). ORIGINAL, 2026-08-23 tools: glossary_propose_entities (+ ontology reads) carded 5/5 glossary_entities null -> 2 rows, every run RESTORED, 2026-08-30 tools: composition_build_cast_and_graph 5/5 carded 5/5 glossary_entities NO CHANGE, every run THE PIPELINE TOOL HAS TAKEN THE TURN FROM ITS PARTS, and the dates line up exactly: composition_build_cast_and_graph's FIRST recorded call anywhere is 2026-08-24 (91 calls since, still active today); glossary_propose_entities ran 416 times from 2026-07-09 and its LAST call was 2026-08-26. The original runs predate the pipeline tool's existence. So this is not a stochastic re-roll — it is a before/after across the tool's introduction, on the same words. 🔴 WHAT IT MEANS IS GENUINELY AMBIGUOUS AND I AM NOT RESOLVING IT. Two readings fit: A REGRESSION — the author asked for their cast to be extracted, used to get two entities, and now gets none. Combined with this question's own correction (op=approve_plan was called in 0 of 70 recorded sessions), the pipeline tool wins the turn and then never completes, which is the worst of both. A DELIBERATE TIGHTENING — the old behaviour CREATED ROWS BEFORE THE CARD WAS APPROVED, which is the exact shape DQ-T73 is about ('a turn writes something, then suspends asking approval'). Writing nothing until approval is arguably the correct order, and the old 2-row diff was the defect. WHICH ONE IT…

**⚠ Correction on record (the_APPROVED_arm_settles_it__2026_08_30):** 🔴 THE APPROVED ARM ANSWERS IT, AND IT IS THE REGRESSION READING. Batch c-gbuild-approve, K=5, `--approvals allow-under-test` (throwaway fixtures, the 2026-08-22 owner decision), same prompt as the restored arm word for word — so the ONLY difference is that the write was permitted: composition_build_cast_and_graph called 24 times across 5 runs loreweave_composition.glossary_build_runs 1 row created, every run loreweave_glossary.glossary_entities NULL — every run pending card at the end NONE — it was not blocked So with the write ALLOWED and no card in the way, the pipeline tool records a build RUN and produces NO CAST. The turn then ends by asking the author a question: '…proceed with building these into your world and mapping how they connect? I have not re-read the book's current state on this turn, so my understanding of its contents may be stale. Would you like me to query the connection map…?' THE THREE-WAY COMPARISON ON ONE PROMPT: 2026-08-23 glossary_propose_entities carded 2 entities created 2026-08-30 pipeline, not approved carded nothing 2026-08-30 pipeline, APPROVED no card nothing — 1 build_run row, then a question This rules out the tightening reading I was careful to leave open. 'Nothing is written until approval' would predict the cast appearing once approval is granted. It does not appear. The author asked for their cast to be extracted, used to get it, and now gets a build-run record and a question — which is what D-A-PIPELINE-TOOL-COMPETES-WITH-ITS-OWN-PARTS says happens when the one-call tool takes a turn from the parts that completed it. IT ALSO CORROBOR…

**⚠ Correction on record (ZERO_of_80_turns_ever_produced_a_cast__2026_08_30):** 🔴 IT GENERALISES, AND IT IS NOT CLOSE. Today's 5-run result held across the WHOLE recorded corpus. Every batch run that called each tool, with the store diff the harness already captures: turns calling composition_build_cast_and_graph 80 created a cast 0 0.0% turns calling glossary_propose_entities 131 created a cast 85 64.9% Fisher exact, two-tailed p = 8.8e-13 AND IT IS NOT ONE SCENARIO. The 80 pipeline turns span five: composition-glossary-build-with-an-ontology (30), -NAMED-arm-tests-the-falsifier (20), -SELECTION-arm (20), gbuild-control-named-outright (5), and today's approved arm (5). 🔴 THE CONTROL THAT MATTERED MOST: DOES IT WRITE THE CAST SOMEWHERE ELSE? No. Across all 80 turns exactly ONE table ever moves — 73 of 80 loreweave_composition.glossary_build_runs 0 of 80 anything else, including glossary_entities So the tool records a build-run RECEIPT that work was requested, and never produces the thing requested. 'The cast is created elsewhere' was the obvious innocent explanation and it is refuted: the only write is the receipt. WHAT THIS SETTLES FOR THE QUESTION. It asks whether a one-call tool that does what two two-step paths do should keep competing with its parts. On the record it does not do what they do — the parts deliver a cast in 65% of their turns and the pipeline in 0% of 80, including when the write was explicitly permitted. The competition is real (an identical prompt routed to the parts before 2026-08-24 and to the pipeline after) and the outcome of winning it is that the author gets nothing. SCOPE, because a zero deserves its limits stated: this rea…

**⚠ Correction on record (the_tool_ASKS_for_a_second_round_trip_that_never_comes__2026_08_30):** 🔴 THE MECHANISM, IN THE TOOL'S OWN WORDS. The 0-of-80 above is an OUTCOME; this is why it happens, read from a recorded result payload rather than inferred: {"run_id": "01a0356f-…", "status": "plan_ready", "worklist": [{"why": "The protagonist climbing the black stair.", "kind": …}], "next": "Show the user this worklist; call op=approve_plan when they …"} THE TOOL INSTRUCTS A SECOND HUMAN ROUND-TRIP. `op=start` returns ok:true with a plan and a `next` field telling the model to show the worklist and wait. So the model ending its turn with 'Would you like me to proceed with building these into your world?' is NOT a model failure — it is the model doing exactly what the tool told it to do. AND THE SECOND ROUND-TRIP HAS NEVER HAPPENED. This question's own returned correction found op=approve_plan called in 0 of 70 recorded sessions. Put together with the outcome measurement, the picture is complete and consistent: the tool wins the turn from its parts an identical prompt routed to the parts before 2026-08-24 and to it after it answers ok and asks for approval `next: show the user … call op=approve_plan` the approval step is never taken 0 of 70 sessions so the cast is never created 0 of 80 turns, p = 8.8e-13 vs the parts WHY THAT MATTERS TO THE RULING more than the raw zero did: the one-call tool does not merely underperform its parts, it REPLACES a one-turn job with a two-turn protocol, and the second turn has no observed instance anywhere in the corpus. Whether that protocol is wrong, or right-but-unreachable, is the decision — but 'it does the same thing in one call' is not…

**⚠ Correction on record (approve_plan_WORKS__the_protocol_is_never_traversed__2026_08_30):** 🔴 IT IS RIGHT-BUT-UNREACHABLE, NOT BROKEN — and that is the answer to the question this row leaves open. The protocol was driven DIRECTLY through the harness's own authenticated calls, with no model in the loop, so nothing here can be blamed on selection. Throwaway fixture, torn down. op=start -> status 'plan_ready', worklist, next: 'Show the user this worklist; call op=approve_plan when they agree.' op=approve_plan -> status 'building', next: 'Building has started — poll op=status.' op=status x3 -> 'building' … then 'proposed', items include 'Aldric Vane' glossary_entities on the fixture: 2 TWO ENTITIES — the same count glossary_propose_entities produced from the same story on 2026-08-23. So the pipeline tool CAN do what its parts do. It simply never gets the chance: 0 of 80 recorded turns reach a cast, and op=approve_plan appears in 0 of 70 sessions. WHAT THAT MEANS FOR THE RULING, and it changes the shape of the question. The tool is not defective — it is a THREE-ROUND-TRIP protocol wearing a one-call description. Its own declaration says 'Build the KNOWLEDGE GRAPH and the CAST for a book … in ONE call', while its own schema declares six ops (start | approve_plan | status | project_kg | approve_edges | cancel) and requires run_id on every one except start. The model reads the description, calls it once, is told to show a worklist and wait — and the turn ends there, correctly, forever. 🔴 MY FIRST PROBE WOULD HAVE REPORTED THE OPPOSITE. It read glossary_entities immediately after approve_plan and saw ZERO — because the build is ASYNC and had just started. 'approve_plan do…

**⚠ Correction on record (the_FULL_protocol_is_five_calls_and_two_gates__2026_08_30):** 🔴 THE WHOLE PROTOCOL RUNS, AND IT IS FIVE CALLS WITH TWO HUMAN GATES. Driven directly, no model in the loop, throwaway fixture, torn down: op=start -> 'plan_ready' next: 'Show the user this worklist; call op=approve_plan' op=approve_plan -> 'building' next: 'Building has started — poll op=status.' op=status x7 -> 'proposed' glossary_entities: 2 op=project_kg -> 'edges_ready' next: 'Show the user these relationships; call op=approve_edges to write them.' op=approve_edges -> 'done' edges_applied 2, edges_failed 0 final status -> done SO THE TOOL DELIVERS EXACTLY WHAT IT PROMISES — the cast AND the graph, 2 entities and 2 edges — and it takes FIVE CALLS AND TWO SEPARATE HUMAN APPROVALS to get there. ITS DESCRIPTION SAYS 'in ONE call'. Verbatim from the live catalogue: 'Build the KNOWLEDGE GRAPH and the CAST for a book from a story you have been given — in ONE call.' 🔴 THAT GAP IS THE DEFECT, AND IT EXPLAINS EVERY NUMBER ON THIS ROW. The model reads a one-call promise, makes one call, and is told to show a worklist and wait. It does exactly that — correctly — and the turn ends. Nothing in the corpus ever takes step two, so: 0 of 70 sessions call op=approve_plan 0 of 80 turns produce a cast an identical prompt that reached the parts before 2026-08-24 now reaches this instead WHAT THIS DOES TO THE QUESTION AS WORDED. It asks whether a tool that 'does in one call what vision-to-book and populate-from-notes do in two steps each' should compete with its parts. It does not do it in one call — it does it in five, with two gates, which is MORE round-trips than the two-step paths it di…

**⚠ Correction on record (the_part_is_NOT_ON_THE_WIRE__measured_2026_08_30):** 🔴 WITHDRAWN THE SAME DAY BY MY OWN CONTROL. THE PART WAS ON THE WIRE. I recorded that the parts do not lose a contest because they are not in one: `answerable_tools` does not return glossary_propose_entities for the recorded prompt, only composition_build_cast_and_graph, kg_add_nodes and kg_graph_query. That measurement is correct and the conclusion drawn from it was not. THE CONTROL WAS THE HISTORY THE SAME PROMPT ALREADY HAS. If the words cannot reach the tool, how was it called 60 times across the 90 recorded runs of this scenario? Reading what the runs record as ADVERTISED — the union of the `advertised` lists across every surface snapshot, not the answerability check: batch runs propose_entities ADVERTISED / CALLED pipeline ADVERTISED 2026-08-30 restored 5 5 / 0 5 2026-08-30 approve 5 5 / 0 5 2026-08-14 gbuild8 15 15 / 15 15 2026-08-14 gbuild13 15 15 / 0 15 The tool was advertised on EVERY run of both 2026-08-30 batches, beside the pipeline tool, and was not called. There was a choice and the model made it. The reachability gate's own caveat says exactly this — answerability is the DOMINANT reach path, not the only one — and I quoted that caveat while writing a conclusion that ignored it. SO THIS ARM IS THE SIBLING-PICK SHAPE AFTER ALL, which is the opposite of what I claimed when I said it did not generalise to that row. Both tools in front of the model, the pipeline tool chosen, and it produces no cast. That STRENGTHENS D-SURFACING-IS-NECESSARY-BUT-NOT-SUFFICIENT-THE-MODEL-PICKS-THE-SIBLING rather than standing apart from it. WHAT SURVIVES, AND IT IS THE PART THAT M…

**⚠ Correction on record (the_head_to_head_where_BOTH_are_advertised__2026_08_30):** THE MEASUREMENT THAT REPLACES THE WITHDRAWN ONE. Having established that both tools are advertised together, the question stops being reach and becomes a straight head-to-head: on the turns where the model saw BOTH, which did it call? Read from each run's own surface snapshots and wire records, over every gbuild batch on disk — 135 runs where both were advertised: batch both advertised parts pipeline 2026-08-14 c-gbuild7 5 5 0 2026-08-14 c-gbuild8 15 15 0 2026-08-14 c-gbuild9 10 10 0 2026-08-14 c-gbuild10 5 4 1 2026-08-14 c-gbuild11 15 12 3 2026-08-14 c-gbuild12 15 9 6 2026-08-14 c-gbuild13 15 0 15 2026-08-14 c-gbuild-ctl2 5 0 5 2026-08-28 c-gbuildAB 10 0 10 2026-08-28 c-gbuildAB2 10 0 10 2026-08-28 c-gbuildsplit1 10 0 10 2026-08-28 c-gbuildstable1 10 0 10 2026-08-30 c-gbuild-approve 5 0 5 2026-08-30 c-gbuild-restored 5 0 5 TOTAL 135 55 80 before 2026-08-28 85 runs parts 55 (65%) pipeline 30 from 2026-08-28 50 runs parts 0 ( 0%) pipeline 50 🔴 THE 65% THIS QUESTION ALREADY QUOTES FOR THE PARTS IS EXACTLY THIS NUMBER, and it is now 0 of 50. The parts have not become unreachable — they are advertised on every one of those 50 runs — they have stopped being CHOSEN. Whatever changed, changed the preference, and the tool that wins produces no cast in 0 of 80 turns. THE DATE IS NOT THE WHOLE STORY, stated because the table invites a tidier reading than it supports: c-gbuild13 and c-gbuild-ctl2 went 0/15 and 0/5 to the pipeline tool on 2026-08-14, so the pipeline tool could already win in the early era. What the split shows is that the outcome used to VARY by arm and no longer vari…

**My recommendation:** (a), weakly, and NOT before someone decides whether the pipeline's extras are wanted. The row is precise about what the author loses — not the cast, which they get, but the worklist, the deep profiles, the review drafts and the proposed relationships. If those are valued, (a) is the cheapest way to make the platform teach the tool it already ships. If they are not, (c) is more honest than leaving a tool with 0 organic calls in the catalogue competing with a path the platform actively teaches. I hold this weakly because (b) is arguably the correct fix and I cannot judge whether vision-to-book's per-entity granularity is deliberate. That is a question for whoever authored the 9 steps.

**Why it is yours, not mine:** A workflow is an AUTHORED recipe — product content, not code — and re-pointing vision-to-book changes what every write turn on that rail does. Either direction reshapes a journey the platform ships, and the third option deletes a working tool.

**Blocks:** `D-A-PIPELINE-TOOL-COMPETES-WITH-ITS-OWN-PARTS`

### DQ-T66 — releases 1 defect

**Asked:** composition's confirm route names a BookClientError (502) upstream failure at _execute_authoring_run_gate -- 'could not read the book's chapters', distinguishing a retryable failure from a rejected request. This branch is REAL: book-service genuinely can fail in production (timeout, restart, network blip), so the product DOES reach it. What cannot happen is THIS LOOP safely rehearsing it -- there is no per-request fault-injection path (BookClient's base_url/timeout come from global settings shared by every concurrent request; overriding them to force a failure would affect other requests in flight, the same SAFETY violation as taking book-service down outright, just smaller). DQ-T48 answere…

**⚠ Correction on record (returned_corrected):** 🔴 THE QUESTION'S PREMISE IS WRONG, AND I WROTE IT. 2026-08-30. It asserts: 'there is no per-request fault-injection path (BookClient's base_url/timeout come from global settings shared by every concurrent request; overriding them to force a failure would affect other requests in flight, the same SAFETY violation as taking book-service down outright, just smaller)'. That enumerates two options — per-request override, or take down the shared service — and both are indeed unavailable. It MISSES A THIRD, which is neither: an ISOLATED INSTANCE. BUILT AND RUN TODAY, and it breaks no rule. A throwaway composition-service container on the same docker network, from the same image, with the same settings except one env var: docker run -d --name comp-fault --network infra_default \n --env-file <the real container's env> -e BOOK_INTERNAL_URL=http://127.0.0.1:9 \n infra-composition-service It reached 'Application startup complete'. Nothing shared was touched: no request in flight was affected, book-service stayed up, the real composition-service's BOOK_INTERNAL_URL is still http://book-service:8082 (re-read after teardown), and the container was removed. AND THE BRANCH'S PRECONDITION REPRODUCES INSIDE IT: BookClient(settings.book_internal_url, ...).list_chapters(<throwaway book>, ...) -> BookClientError: book-service 502 BOOK_SERVICE_UNAVAILABLE which is exactly the exception `_execute_authoring_run_gate` catches, and `str(exc)` is exactly the string the fix puts in `detail`. So 'this loop cannot safely rehearse it' is false: the failure mode is reproducible, safely, in about a minute.…

**⚠ Correction on record (are_upstream_branches_COMMON_across_services__MEASURED_2026_08_30):** My recommendation says option (ii) 'is not obviously wrong and might be worth it if upstream-failure branches turn out to be common across services (I have not measured that)'. Measured, and the answer depends entirely on the definition — which is itself the finding. NARROW — handlers over a NAMED client-error class (BookClientError, GlossaryClientError, InternalClientError), non-test: composition-service 42 campaign-service 1 TOTAL 43 across TWO services; 18 name the reason, 25 are bare Read alone this says the branches are a composition-service peculiarity and (ii) is not justified. THAT READING IS WRONG. WIDE — any handler over an upstream failure, including the httpx exceptions most services catch DIRECTLY rather than through a named class: composition-service 87 campaign-service 15 lore-enrichment-service 45 translation-service 7 knowledge-service 42 chat-service 4 worker-ai 19 learning + jobs 2 TOTAL 221 across NINE services 🔴 SO THE ANSWER IS YES, AND MY FIRST CENSUS UNDER-COUNTED BY FIVE TIMES because it keyed on a class-NAMING convention that only composition-service follows. Every other service reaches for httpx's own exception types, so a census over `*ClientError` sees none of them and reports a concentration that does not exist. This is the same shape as the tool-name census corrected on DQ-T70 today: a convention mistaken for the population. WHAT IT MEANS FOR THE RULING: the condition I attached to (ii) — 'if upstream-failure branches turn out to be common across services' — IS MET. That does not make (ii) right; my recommendation also flags that a fault-inje…

**My recommendation:** (i). The branch is already correctly named, the class principle (name what a caller can act on) is satisfied, and this loop has already closed other rows on code+guard evidence when a live run is unreachable by construction -- SAFETY should win the conflict, not get engineered around. (ii) is not obviously wrong and might be worth it if upstream-failure branches turn out to be common across services (I have not measured that), but it is new capability with its own risk (a fault-injection header is an attack surface if it is ever reachable outside a throwaway fixture) and should be sized before building, not assumed cheap. I am not deciding it here.

**Blocks:** `D-CONFIRM-REFUSAL-IS-AN-OPAQUE-ACTION-ERROR`

### DQ-T67 — releases 1 defect

**Asked:** Now that the intent-skill router genuinely runs (fixed the same day, D-A-TOOL-REACHES-THE-WIRE-WITHOUT-ITS-DOMAINS-GUIDANCE), its top-2 cap and 0.35 floor are visibly too loose to discriminate for at least one real prompt -- 12 of 12 skills clear the floor, and the two that win the cap are not the one the prompt needed. Three independent levers, any of which could move the outcome, none of which is obviously right without measuring the others held constant: (a) RAISE ROUTER_MAX_ADDITIONS (2 -> 3 or more) -- lets more of what clears the floor through, at the cost of injecting MORE skills per turn (context cost, and the risk the comment already named: 'the flood when the whole set clears the…

**⚠ Correction on record (the_frequency_instrument_BUILT_and_RUN__2026_08_30):** My recommendation was 'Measure before moving any lever… I am not choosing a lever without it', and specified the measurement: over a corpus of real prompts, compute the router's own score distribution and report how often the CORRECT domain clears the floor but misses the cap, and how often an UNRELATED skill makes the cap. Built and run. METHOD. 300 real user turns sampled from the live store, restricted to turns whose reply actually called tools so there is a ground-truth signal. Each embedded with the DEPLOYED embedding model inside infra-chat-service-1 and cosine-ranked against the same 12 skill vectors the router uses — `_get_skill_vectors`, not a reimplementation. Ground truth is the tool PREFIX of what the turn called, mapped only where the mapping is unambiguous (book_/jobs_/glossary_/composition_/plan_/kg_/translation_/settings_); 55 turns called only unmappable tools and are excluded rather than guessed at. evaluated 245 correct domain MADE the cap 134 54.7% cleared the floor but MISSED the cap 89 36.3% did not even clear the floor 22 9.0% an UNRELATED skill took a cap slot 111 45.3% 🔴 THE FLOOR DISCRIMINATES ALMOST NOTHING, and the row's single-prompt observation is the MAJORITY case rather than an outlier. Skills clearing 0.35, per prompt: 12 of 12 skills clear 146 prompts 60% 10 or more clear 184 prompts 75% 3 or fewer clear 21 prompts 9% SO THE CAP IS THE BINDING CONSTRAINT, NOT THE FLOOR — which is the question I said I could not choose a lever without. Only 9% of prompts fail at the floor, while 36% have the right domain sitting above the floor and losing t…

**My recommendation:** Measure before moving any lever. Build the frequency instrument the parent row's own 'what is NOT known' already called for -- for a representative corpus of real prompts (this loop has used a 564- and a 2,135-prompt live corpus elsewhere), compute the router's OWN score distribution per prompt (cheap: no LLM call needed, this is pure embedding + cosine, exactly how I ran the single-prompt probe above) and report: how often does the CORRECT domain (by some independent signal, e.g. which tools actually got called) clear the floor but miss the cap; how often does an UNRELATED skill make the cap. That single measurement would tell (a) whether raising the cap helps without flooding, and (b) whe…

**Blocks:** `D-THE-INTENT-ROUTERS-TOP-K-CAP-CROWDS-OUT-THE-RIGHT-DOMAIN`

### DQ-T68 — releases 1 defect

**Asked:** R1 answerability keys on ONLY the current turn's user text (`user_message_content = _stripped_msg`, verified against the deployed code -- no lookback exists). A two-turn journey -- instruct, then confirm -- loses every tool on the confirmation turn, because 'Yes, please create them.' matches no tool's declared vocabulary. Measured live (batch c-planapply3, K=5): plan_bootstrap_apply and its transitively-forced supplier are both on the wire on the instructing turn and both ABSENT on the confirming turn, surfaced 0/5, called 0/5. The shape of a fix is clear -- when the current turn's text yields no answerable tools AND the previous assistant turn ended in a question, the request under discuss…

**⚠ Correction on record (the_precision_recall_I_said_was_unmeasured__2026_08_30):** My recommendation states: 'I have not measured this narrower version's precision/recall and would do so before shipping it.' The RECALL side is now measured against the live store; the full figures are on the defect row. turns opening with a bare affirmative 779 of 9,622 user turns ... after an assistant turn that called tools 246 <- the narrow trigger ... of those, the reply called NOTHING 59 24.0% ... of those, the reply DID reach a tool 169 68.7% WHAT IT MEANS FOR THE RULING. The narrow fix I recommended fires only when the current turn's answerable set is empty AND there was a concrete pending action. Its reach is therefore AT MOST the 59, not the 246 and certainly not every two-turn journey — because nearly seven in ten confirmation turns already reach a tool by some other path (hot-set, domain selection). That is a much smaller prize than the row's framing implies, against a change to the surface every turn passes through. IT ALSO SHARPENS THE PRECISION RISK RATHER THAN SETTLING IT. The 169 turns that already work are the ones a lookback could DAMAGE: re-running R1 against the prior user message on a turn that is already reaching tools could add a competitor to a surface that was fine. My condition (b) — only when the answerable set is EMPTY — is what keeps those 169 untouched, so it is load-bearing rather than conservative decoration, and I would not drop it. STILL NOT MEASURED: whether the 169 reached the RIGHT tool. 'Called something' is not 'confirmed the pending action', so 59 is a floor on the defect and 246 a ceiling. Closing that gap needs a per-turn read of…

**⚠ Correction on record (the_recall_range_is_59_to_81_not_59__2026_08_30):** Refining the figure I recorded earlier today, which understated the reach of a fix by counting 'called a tool' as 'worked'. Over the 228 trigger turns with a reply: called NOTHING 59 unambiguous defect called a pending tool 54 confirmed different tool, SAME domain 93 most likely a correct next step different tool, UNRELATED domain 22 most likely the defect THE FIX'S REACH IS 59 TO 81 TURNS, not 59 and not 246. IT DOES NOT CHANGE MY RECOMMENDATION and it does not soften the precision risk. My condition (b) fires only when the answerable set is EMPTY, which by construction is the 59 — the 22 unrelated-domain turns DID reach tools, so an empty-set trigger never sees them. If the owner wants those 22 as well, that is a WIDER trigger than the one I recommended and it would put the lookback in front of 147 turns that are currently working. I would not do that without measuring the damage first, and this measurement does not measure it. The domain prefix is a proxy — it separates plan_ from glossary_ and nothing finer — which is why this is a range rather than a number.

**⚠ Correction on record (the_arc_apply_instance_is_no_longer_retroactive__2026_08_30):** 🔴 THE RE-ATTRIBUTION THIS ROW DECLINED TO CLAIM IS NOW MEASURED. The row says of composition_arc_apply: 'NOT PROVEN for arc_apply — that run predates the R1 instrumentation, so its wire log does not exist and I will not claim it retroactively.' The scenario was ORPHANED (12 recorded runs, no definition in the repo), so it could not simply be re-run. It was restored from the recording — both turns verbatim, seed copied unchanged from the live single-turn arm — as scripts/toolloop/scenarios-c-arcapply-confirm.json, and run today with the instrumentation that did not exist in August. batch c-arcapply-confirm, K=5, real provider, throwaway fixture per run turn 1 'Apply arc template — use my library arc template …' composition_arc_apply ON THE WIRE and CALLED — 5 of 5 turn 2 'Yes, go ahead and do it.' (the MEASURED turn) composition_arc_apply ABSENT FROM THE SURFACE — 5 of 5 the model calls composition_arc_template_get / _list instead SO THE 0/5 ON A BARE AFFIRMATIVE IS A SURFACE PROPERTY, NOT A MODEL ONE. The pre-registered conclusion the row quotes — 'a bare affirmative cannot answer a which-one question', a statement about the model's reasoning — cannot be what happened here: the model was never offered the tool. This is the same shape P1-SURFACE already found once, and it is now established for arc_apply rather than inferred. WHAT IS STILL NOT CLAIMED: that R1's turn-keying is the specific mechanism. The row establishes separately that R1 keys on the current turn's text and that no lookback exists; this supplies the missing INSTANCE — a tool present on turn 1 and gone on tu…

**My recommendation:** A NARROW version, scoped tightly enough to avoid guessing at the general policy: when (a) the current turn's answerable set is empty, AND (b) the immediately PRIOR assistant turn's tool_calls history shows a tool was surfaced-but-not-called or a card was minted (i.e. there was a real pending action, not just prose), re-run R1 against the prior USER turn's text too, additively. This is deliberately narrower than 'look back until the topic changes' -- it only fires when there is a CONCRETE pending action to confirm, which is a structural signal (a card, a forced-supplier chain) rather than a guess about conversational topic drift. I have not measured this narrower version's precision/recall a…

**Blocks:** `D-R1-IS-KEYED-ON-THIS-TURN-SO-A-CONFIRMATION-TURN-LOSES-THE-TOOL`

### DQ-T69 — releases 1 defect

**Asked:** A turn ends promising a LATER message or asking the author to WAIT -- a synchronous turn cannot keep either promise; no further message will arrive. Measured over 158 batches / 1,551 runs: 5 clean instances (store unmoved, nothing queued, nothing pending) -- about 0.3%. The signal is precisely characterised (a promise of a later message / a request to wait, discriminated by determiner, NOT future tense generally -- 'I'll cancel them now' is fine) and TWO naive detection clauses were measured and rejected before this count shipped ('in a moment' fired on the assistant asking the USER to retry, the opposite of a promise; a sentence-boundary anchor on 'one moment' cost a true positive). The ro…

**⚠ Correction on record (the_population_re_derived__2026_08_30):** The question quotes '5 clean instances -- about 0.3%'. Re-derived with the shipped detector over 2,112 runs (the count was over 1,551): SEVEN clean instances, 0.33%. The rate is unchanged and the defect is STILL REPRODUCING — two new instances since the count, spread across five distinct scenarios rather than concentrated in one. This does not change my recommendation of (b) and slightly strengthens its reasoning: the population is small enough that I would not build a reactive matcher for it (the row's own measurement already rejected two clauses), AND it is not decaying on its own, so 'wait and see' is not a free option either. An upstream instruction of one or two sentences remains the cheaper half of that trade.

**My recommendation:** (b), scoped narrowly in wording (one or two sentences, matching the row's own precise characterisation of the signal so it does not also suppress a legitimate in-turn intention like 'I'll cancel them now'), and A/B'd the same way today's other prompt fixes were -- same prompt, control vs treatment, before shipping broadly. The population is small enough that I would NOT build a reactive guard on this evidence alone; a matcher this loop's own measurement already showed misfiring twice (rejecting two clauses before landing on a working count) is real ongoing maintenance for a 0.3% population, where an upstream instruction is a few sentences with no matcher to maintain. I am not shipping (b) w…

**Blocks:** `D-A-TURN-PROMISES-WORK-THAT-CONTINUES-AFTER-IT-ENDS`

### DQ-T70 — releases 1 defect

**Asked:** 25 live tool pairs share a normalised synonym after `_answer_norm` strips articles -- 'write chapter'/'write a chapter'/'write the chapter' all collapse to one key, so book_chapter_create, book_chapter_save_draft and composition_generate all arrive together on a request that (per the authors' own distinct phrasing) meant only one of them. The only proposed remedy (re-rank the tie-break by specificity) was measured and is INERT -- truncation never fires for any of the 25 (max set size 3, cap is 8), so sort order changes nothing. The only remedy that would actually separate the three is loosening `_answer_norm`'s article-stripping, which touches matching for all 1,472 live synonym declaration…

**⚠ Correction on record (the_cost_side_is_HALF_what_the_question_says__2026_08_30):** This question asks whether a normalisation-wide fix is worth it for 25 tie instances. Measured today with the full legacy set (the row's own count excluded only PYTHON legacy tools and said so): the live tie population is 13, not 25. raw ties 97 · dissolved by legacy 84 · LIVE 13 The raw figure (97) and the distinct-synonym count both reproduce the row's numbers, so this is the same measurement with the gap the row flagged closed — not a competing one. The full list of 13 is on the defect row. IT SHOULD MAKE (b) HARDER TO JUSTIFY, not easier: a platform-wide change to `_answer_norm` touches 1,479 declarations to resolve thirteen collisions, several of which ('stop translation' → jobs_cancel vs translation_job_control) are arguably genuine ambiguity that no normalisation rule should be expected to settle. My recommendation is unchanged in direction and now held more firmly against the wide fix.

**My recommendation:** No, not as its own project on this evidence. The population is small, each instance still resolves (a tie the model breaks, not a wrong answer demonstrated), and the blast radius (1,472 declarations) is disproportionate to a benefit that has not been shown to exist -- nobody has measured that the MODEL chooses badly among a tied set, only that the tie exists. If a future batch DOES show a tied set producing a wrong choice, that measurement would change this answer; today there is none. I am not closing the row on this recommendation myself -- leaving it open and blocked, since a future finding could change the answer and the row's own history (two 'obvious' fixes measured worse this same we…

**Blocks:** `D-THE-ANSWERABILITY-TIE-IS-MANUFACTURED-NOT-GENUINE`

### DQ-T72 — releases 1 defect

**Asked:** When R1 answerability returns EXACTLY ONE tool for a request and that tool is on the wire, and the model uses a different one anyway, should the platform (a) force the choice — set `tool_choice` to that tool for the pass; (b) force it ONLY when the single answer is a Tier-R READ, leaving writes to the model; (c) de-advertise the competing sibling when a more specific tool matches; or (d) neither, and treat selection as the model's to get right?

**⚠ Correction on record (the_premise_verified_2026_08_30):** The decisive case reproduces TODAY (batch c-siblingpick1, K=5): glossary_propose_batch called 0 of 5, glossary_adopt_standards used 4 of 5, while glossary_propose_batch was advertised on 36 of 36 passes and was the ONLY tool answerable_tools returned for the prompt. The surface was correct and the choice was not.

**⚠ Correction on record (option_c_s_PRECISION_MEASUREMENT__and_it_REFUTES_c__2026_08_30):** My recommendation said of option (c): 'it needs its own precision measurement — which sibling, on what evidence — and I have not done it, so I am not recommending it yet.' Done now, and it argues AGAINST (c) rather than for it. Every user turn in the live store re-run through the DEPLOYED `answerable_tools` against the live catalogue, then compared with what the turn actually called: turns scanned 8,650 R1 returned EXACTLY ONE tool 2,996 34.6% the turn called it 1,732 57.8% the turn called something ELSE 893 29.8% the turn called nothing 371 12.4% WHAT IT CALLED INSTEAD, live tools only (a call to a since-removed tool is stale corpus, not a rival — see the instrument note below): 34 jobs_cancel (A) -> jobs_list 31 glossary_book_sync_apply (W) -> glossary_book_sync_available 30 kg_propose_edge (A) -> kg_add_nodes 27 book_steering_list (R) -> kg_project_create 26 glossary_create_evidence (A) -> book_list 26 glossary_create_evidence (A) -> book_read 24 propose_edit (R) -> glossary_search 22 book_steering_list (R) -> glossary_book_ontology_read 22 propose_edit (R) -> book_read 22 jobs_pause (A) -> jobs_list 21 glossary_curation_list (R) -> glossary_search 20 book_steering_list (R) -> glossary_list_system_standards 🔴 MOST OF THE 'COMPETING SIBLINGS' ARE SUPPLIERS, NOT RIVALS. jobs_cancel -> jobs_list and jobs_pause -> jobs_list are the model fetching the id it needs before acting. glossary_book_sync_apply -> glossary_book_sync_available is check-then-apply. glossary_create_evidence -> book_list/book_read and propose_edit -> glossary_search/book_read are the same shape. De-adver…

**⚠ Correction on record (a_10_of_10_instance_fell_out_of_a_MIS_AIMED_batch__2026_08_30):** A batch I launched for a different question — and which was uninformative for that one — produced the strongest instance this question has. Batch c-kgedge-pending, K=10, scenario kg-propose-edge, prompt 'Record that Aldric Vane and Mira Solene know each other.', both entities seeded and asserted present: kg_propose_edge ON THE WIRE 10 of 10 kg_propose_edge CALLED 0 of 10 kg_add_nodes called instead 10 of 10 tables written NONE — only entity_access_log, a read log all 10 carded; reply ends 'Nothing has been saved yet; confirm the card above…' THE SURFACE WAS CORRECT EVERY TIME AND THE CHOICE WAS WRONG EVERY TIME, which is this question's exact subject. It is a much cleaner instance than the founding case (glossary_propose_batch, 0 of 5): here the intended tool is on the wire in ALL ten, the substitute is called in ALL ten, and the author's request — a RELATIONSHIP — produced a pending card for adding NODES. 🔴 AND IT MAY STILL BE THE SUPPLIER SHAPE, WHICH IS WHY IT IS NOT YET DECISIVE. kg_add_nodes could be a legitimate precursor: the glossary entities are seeded but their KG nodes may not exist, so 'add the nodes, then the edge' is a defensible order. The turn stopped at the card, so nothing shows whether the model would have gone on to propose the edge once the nodes landed. That is exactly the ambiguity my option-(c) measurement already flagged — jobs_cancel -> jobs_list and glossary_book_sync_apply -> _available have the same shape — and it is the reason I recommended against de-advertising the 'sibling'. AN APPROVED ARM IS RUNNING to settle it: --approvals allow-under-t…

**⚠ Correction on record (TWO_failed_arms_before_the_question_could_be_asked__2026_08_30):** 🔴 TWO ATTEMPTS FAILED BEFORE THIS QUESTION COULD EVEN BE PUT TO A RUN. Both are recorded rather than retried quietly, because each failed for a reason that will catch the next person the same way. c-kgedge-pending (K=10, --approvals none) Produced the 10-of-10 instance above, but every run stopped at a card, so it cannot say whether the model would have proposed the edge once the nodes landed. Not a failure of the batch — a limit of it, and the limit is the whole question. c-kgedge-approved (K=5, --approvals allow-under-test) ← FAILED Meant to let the write land. It did not: 5 of 5 still carded, nothing written but the read log. `allow-under-test` grants `allow` to exactly the batch's TOOLS_UNDER_TEST, and the scenario declares kg_propose_edge — the tool the model never calls. The approval landed on a tool that was never invoked while the one that was invoked stayed gated. THE LESSON IS GENERAL AND CHEAP TO MISS: `allow-under-test` is keyed on the DECLARED tool, so on any scenario where the model substitutes, the flag silently does nothing. Every substitution scenario — which is to say every scenario this question is about — has this trap built into it. A third arm (scripts/toolloop/scenarios-c-kgedge-nodes.json) declares kg_add_nodes as the tool under test so ITS write can land, which is the precondition for observing whether the edge follows. Running now.

**⚠ Correction on record (THE_10_of_10_INSTANCE_IS_WITHDRAWN__it_was_a_CARD_ending_the_turn__2026_08_30):** 🔴 WITHDRAWING THE INSTANCE I CALLED 'THE STRONGEST THIS QUESTION HAS' TWO HOURS AGO. The arm built to refute it did. c-kgedge-nodes, K=5, --approvals allow-under-test on kg_add_nodes (the tool the model actually chooses), same prompt, same seed: kg_add_nodes called 5 of 5 kg_propose_edge CALLED 5 of 5 <- the tool I said was 'never chosen' glossary_search 3 of 5 SO IT IS SUPPLIER-THEN-ACT, NOT A SUBSTITUTION. In c-kgedge-pending the model called kg_add_nodes, that call CARDED, and the turn ended there — so kg_propose_edge was never reached. I read '0 of 10 called, on the wire 10 of 10' as the model declining a tool it could see. It was not declining anything; it never got a further pass. Remove the card from the precursor and the edge follows every single time. WHAT MISLED ME: 'on the wire and not called' looks like a choice, and here it was a TERMINATION. A Tier-A card ends the turn, so any tool the model would have reached AFTER the carded one is indistinguishable — from the outside — from a tool it rejected. That is a measurement trap in every scenario where the first call cards, and this question is about exactly those scenarios. 🔴 IT STRENGTHENS MY RECOMMENDATION AGAINST OPTION (c), materially. De-advertising the 'competing sibling' would, on this turn shape, have removed kg_add_nodes — the tool the model needs FIRST — and the edge would then have had nothing to attach to. The option-(c) measurement already found most 'competitors' are suppliers; this is the same finding proven by construction rather than inferred from a pair list. A LEAD, NOT A CLAIM, AND IT REACHES T…

**⚠ Correction on record (a_THIRD_of_carded_turns_cannot_tell_rejected_from_unreached__2026_08_30):** 🔴 A MEASUREMENT-VALIDITY CAVEAT THAT REACHES EVERY SELECTION CLAIM DRAWN FROM A CARDED TURN, sized after the kg-propose-edge instance was withdrawn today. A Tier-A card ENDS the turn. So on a carded turn that made only one call, a tool that is 'advertised and not called' may have been rejected by the model — or may simply never have been reached, because the turn stopped at the card before a second pass. From the outside those are identical. carded turns in the whole corpus 667 ... that made ONE tool call or none 221 33% ONE IN THREE CARDED TURNS IS IN THE AMBIGUOUS SHAPE. That is not a claim that a third of selection findings are wrong — many carded turns make several calls, and many selection findings do not come from carded turns at all. It is a bound on how much of the evidence CANNOT distinguish the two readings without an extra arm. THE TEST THAT SEPARATES THEM is cheap and now demonstrated twice: declare the tool the model ACTUALLY calls as `tool_under_test`, run with --approvals allow-under-test so its write lands and the turn continues, and see whether the intended tool follows. On kg-propose-edge it followed 5 of 5, converting 'never chosen' into 'never reached'. 🔴 AND `allow-under-test` HAS ITS OWN TRAP, which cost an arm today: it grants approval to the DECLARED tool_under_test. On a substitution scenario the declared tool is by definition the one the model does NOT call, so the flag silently grants nothing and the card still stands. The arm must name the chosen tool, not the intended one.

**⚠ Correction on record (the_FOUNDING_instance_SURVIVES_the_card_test__2026_08_30):** 🔴 THE FOUNDING INSTANCE SURVIVES THE TEST THAT DISSOLVED THE OTHER ONE. Having withdrawn the kg-propose-edge instance as a card ending the turn, I aimed the identical construction at this question's own founding case — the strongest available attempt to refute it. c-siblingpick-adopt, K=5, --approvals allow-under-test on glossary_adopt_standards (the tool the model actually chooses), prompt and seed unchanged: glossary_adopt_standards called 3 of 5 ... and it WROTE 3 of 5 (book_kinds, book_genres, book_attributes, book_kind_genres, book_active_genres all moved) carded 0 of 5 — nothing was gated glossary_propose_batch called 0 of 5 SO THE CARD EXPLANATION DOES NOT APPLY HERE. The precursor ran, its write landed, no card stood in the way, the turn continued — and the tool the prompt asks for was still never called. On kg-propose-edge the same construction produced 5 of 5; here it produces 0 of 5. The two instances have DIFFERENT causes, and only one of them was an artefact. THE CLEANEST SUB-POPULATION IS 0 OF 3: the three runs where adopt_standards actually ran and wrote. The other two called neither tool (glossary_plan, glossary_task_provide_input, glossary_list_system_standards) and say nothing either way. WHAT THIS DOES FOR THE QUESTION: its central instance is now tested against the strongest alternative explanation this loop has found — and holds. 'Surfacing is necessary but not sufficient' is supported here by a turn that had the tool on the wire, had no card in its way, completed its precursor, and chose otherwise. NOT CLAIMED: that every recorded substitution is real…

**My recommendation:** (b), and I am recommending it KNOWING IT DOES NOT FIX THIS ROW'S OWN CASE — which is the thing to weigh. `glossary_propose_batch` is Tier W, so a read-only forced choice never reaches the decisive instance. (b) buys safety and a real 12.7% of turns where the platform stops second-guessing an unambiguous read; it does not buy this row. (a) would fix the row and forces a delete on 41 measured messages. I do not recommend trading a selection defect for that. (c) is the one I would look at NEXT if (b) proves out: de-advertising the sibling is narrower than forcing, and this loop already ships a de-advertise for legacy tools. But it needs its own precision measurement — which sibling, on what ev…

**Why it is yours, not mine:** (a) removes the model's choice on nearly a quarter of turns, including destructive ones. (c) takes a capability off the wire on a guess about which tool the author meant. Both change what the product does with an ambiguous request, and (d) accepts a measured, reproducing failure. None is a correctness call.

**Blocks:** `D-SURFACING-IS-NECESSARY-BUT-NOT-SUFFICIENT-THE-MODEL-PICKS-THE-SIBLING`

### DQ-T73 — releases 1 defect

**Asked:** A turn writes something, then SUSPENDS asking approval for a LATER write, and the card says nothing about the write that already landed. Should the platform (a) name the completed writes ON the card — 'created 2 entities and 2 graph nodes; this will build the graph'; (b) append the same account to the turn's TEXT, beside the 'Nothing has been saved yet' line DQ-T54 already puts there; or (c) neither, and let the card describe only the action it is asking about?

**⚠ Correction on record (the_premise_verified):** 150 of 1,370 clean runs: empty-or-not reply, card pending, and the store already moved. The founding instance: glossary_entities gained 2 rows and kg_add_nodes reported nodes_created 2 BEFORE the card was raised, and the author saw a question about step two with no account of step one.

**⚠ Correction on record (the_premise_denominator_is_CORRECTED_2026_08_30):** 🔴 RE-DERIVED, AND THE POPULATION IS THREE TIMES DENSER THAN THE QUESTION SAYS. The question quotes '150 of 1,370 clean runs' — 10.9%. But the thing being decided is what a CARD says, and a card only appears on a carded turn. Runs that never raised one cannot exhibit this and are denominator padding. Re-derived over all 227 raw batch files: clean runs 1,954 ended with a card PENDING 628 ... and the store had already MOVED 228 = 36.3% OF CARDED TURNS On the question's own all-runs basis the figure is stable — 228 of 1,954 is 11.7% against the filed 10.9%, the numerator having grown with the corpus. The change is entirely the denominator. SO: MORE THAN ONE CARDED TURN IN THREE shows the author a question about the next write while silently holding a write that already landed. Option (a) — naming the completed writes on the card — would therefore change roughly a third of every card this platform raises, not one run in nine. That is a materially larger change than the question implies, in both value and cost, and it should be weighed as such. INSTRUMENT CHECKED, because a figure that jumps from 10.9% to 36.3% is usually the measurement measuring itself. `store_diff` is a per-table {before, after} map, so 'the dict is non-empty' would count a RECORDED diff rather than a CHANGE. Both predicates were run — non-empty, and before != after — and both return 228, because the runner only records tables that moved. The first predicate was right by luck; it is reported here as verified rather than as assumed.

**⚠ Correction on record (the_FE_caveat_is_RESOLVED_and_it_moves_the_recommendation__2026_08_30):** My recommendation carried this caveat: '(a) needs an FE change to render it, and this loop has a standing rule that an affordance needs a UI path to satisfy it. I have NOT verified that the card component can show a "what already happened" section, so (a) may be more expensive than it looks.' Verified now, in the frontend source. THE UI PATH ALREADY EXISTS AND IS ALREADY LIVE. frontend/src/features/chat/components/ConfirmCard.tsx renders a generic list of server-supplied rows: const rows = preview?.preview_rows ?? []; {rows.map((r, i) => (<li …><span>{r.label}</span><span>{r.value}{r.note ? ` — ${r.note}` : ''}</span></li>))} and `ActionPreviewRow` is `{label, value, note?}` — free-form. So 'created 2 entities and 2 graph nodes' is expressible TODAY as one more row. NO NEW COMPONENT IS NEEDED, and the standing affordance rule is satisfied rather than assumed. 🔴 BUT IT IS NOT FREE, AND THE COST MOVED RATHER THAN VANISHED. The rows come from a TOKEN-SCOPED endpoint — GET /v1/composition/actions/preview?token= — which decodes the confirm token and describes THAT action. It is handed no session and no turn, so it cannot know what already landed earlier in the same turn. Two ways to close that, both server-side or nearly so: - give the preview route the turn's context, so it can add the row itself; or - put the account in the MINT args (the server knows what has landed at mint time) and have the card read rows from `record.args` as well as from the preview. That is a SMALL FE change — one more source for an existing renderer — not a new component. WHAT THIS DOES TO THE RECOMMEN…

**⚠ Correction on record (a_CURRENCY_caveat_on_the_36_3_percent__2026_08_30):** The 36.3%-of-carded-turns figure I recorded earlier today is corpus-wide, and the corpus spans a period in which this behaviour's population changed. Tracked by day (sessions dated from chat_sessions.created_at, not from batch folder names): 2026-08-22 carded 279 wrote first 60 21.5% 2026-08-23 carded 113 wrote first 74 65.5% 2026-08-24 carded 62 wrote first 54 87.1% 2026-08-25 carded 23 wrote first 11 47.8% 2026-08-26 carded 20 wrote first 17 85.0% 2026-08-27 carded 23 wrote first 11 47.8% 2026-08-28 carded 7 wrote first 0 0.0% 2026-08-29 carded 45 wrote first 0 0.0% 2026-08-30 carded 66 wrote first 6 9.1% 🔴 AND THE DROP IS MOSTLY THE POPULATION MOVING, NOT THE BEHAVIOUR STOPPING — checked BEFORE quoting it, because the identical shape fooled me once already today. Of the 227 write-before-card instances before 2026-08-28, only 48 belong to a scenario that has run since. The other 179 come from scenarios that simply stopped being exercised: glossary-create-evidence-attrvalue (26), tool-load (19), kg-propose-edge (15), composition-entity-override-edit-namefix (15) and others, all with ZERO post-cut runs. THE ONE SCENARIO THAT SPANS THE CUT CHANGED FOR A KNOWN REASON. composition-glossary-build-with-an-ontology wrote before its card 48 times before, and 0 of 5 after — but it now routes to composition_build_cast_and_graph instead of glossary_propose_entities, and that tool writes nothing before its card by design. So even the within-scenario change is a tool substitution, not evidence that carding order was fixed. SO WHAT THE OWNER SHOULD TAKE FROM IT: 36.3% is the rate over…

**⚠ Correction on record (MY_36_PERCENT_COUNTED_A_READ_LOG_AS_A_WRITE__CORRECTED_2026_08_30):** 🔴 CORRECTING MY OWN HEADLINE FIGURE, AND IT IS WRONG BY MORE THAN HALF. I reported that 36.3% of carded turns had already written before the card. That predicate was 'any table in store_diff changed', and the most frequent table it counted is a READ LOG. carded turns 640 ANY table moved (what I reported) 235 36.7% an AUTHOR-VISIBLE table moved 95 14.8% moved ONLY a read/queue log 140 tables it was counting: 147 loreweave_knowledge.entity_access_log <- a READ log 63 loreweave_glossary.glossary_entities 46 loreweave_knowledge.knowledge_projects 25 loreweave_knowledge.extraction_pending <- a QUEUE 13 loreweave_composition.glossary_build_runs 8 loreweave_glossary.entity_revisions A turn that merely READS an entity writes an access-log row. Counting that as 'the store already moved' turns every carded turn that looked something up into an instance of this defect. THE REAL FIGURE IS 14.8% OF CARDED TURNS — 95, not 235. IT DOES NOT MAKE THE QUESTION GO AWAY. 95 carded turns did write something an author would recognise — glossary entities, knowledge projects, entity revisions, evidences — before being asked to approve a LATER write. That is roughly one carded turn in seven, and it is the population option (a) would name on the card. But 'more than one in three' was my number and it was wrong, and a ruling weighed against it would have been weighed against a read log. HOW IT WAS CAUGHT: by running the LEFT-PENDING arm (below) and noticing that its carded runs showed `entity_access_log` moving and nothing else. The corpus-wide predicate had no such sanity check against it — it was…

**⚠ Correction on record (the_LEFT_PENDING_arm_and_the_FAILED_attempt_before_it__2026_08_30):** 🔴 THE FIRST ATTEMPT FAILED AND IS RECORDED RATHER THAN RETRIED QUIETLY. To ask whether write-before-card is still current, I re-ran glossary-create-evidence-attrvalue — the scenario that carried 26 historical instances. It produced 0 CARDED RUNS OF 5, because it declares `approve: approved_once` and answers its own card. An auto-approving arm cannot answer a question about pending cards; that is the standing rule 'a scenario that auto-approves never suspends', met in practice rather than in theory. A LEFT-PENDING arm was then built — copied verbatim except for `approve` — and run with --approvals none (scripts/toolloop/scenarios-c-evidence-pending.json, batch c-evidence-pending): carded runs 2 of 5 of those, an author-visible write had already landed: 0 TWO CARDED RUNS IS NOT AN ANSWER and is not offered as one. What it establishes is that the arm WORKS as an instrument where its sibling could not, and that the first two observations do not show the behaviour. The tool was called in only 2 of 5 runs, so a bigger K buys carded runs slowly — roughly two per five — and settling 'still current or not' needs a batch sized for that, not another K=5.

**⚠ Correction on record (the_scenario_carried_8_not_26__and_the_PRIOR_is_28_percent__2026_08_30):** One more figure of mine that the read-log correction takes down with it. I described glossary-create-evidence-attrvalue as 'the scenario that carried 26 historical instances'. That 26 was the any-table count. Re-derived with the author-visible predicate: runs recorded for this scenario 40 carded 28 carded AND an author-visible write first 8 28.6% what the any-table predicate would say 28 100% 🔴 THE ANY-TABLE PREDICATE CALLS EVERY CARDED RUN AN INSTANCE — 28 of 28 — because every one of them touched entity_access_log. That is the clearest possible demonstration that the predicate was measuring 'did this turn read anything', and it is why the corpus figure fell from 36.7% to 14.8% when the read log was excluded. IT ALSO GIVES THE PRIOR THE RE-RUN NEEDS. At 28.6% among carded runs, seeing ZERO in the two carded runs of the first LEFT-PENDING arm has probability 0.71 — which is why that arm was explicitly not offered as an answer. Ten carded runs would put a continued zero at 0.036, so a K=20 arm (this scenario cards on roughly 40-70% of runs) is the right size. That batch is running.

**⚠ Correction on record (the_K20_arm_ran__ONE_carded_run_short_of_decisive__2026_08_30):** The batch sized for this ran. Both LEFT-PENDING arms combined (c-evidence-pending K=5 and c-evidence-pending20 K=20, --approvals none, throwaway fixture per run): runs 25 called glossary_create_evidence 8 32% carded 8 32% (historically 28 of 40 = 70%) carded WITH an author-visible write first 0 P(0 in 8 | the historical 28.6% among carded) = 0.068 carded runs needed for a zero to reach p < 0.05: 9 🔴 ONE CARDED RUN SHORT, AND I AM NOT ROUNDING IT DOWN. 0.068 is suggestive and it is not 0.05. The honest statement is that eight carded runs produced no write-before-card where history predicts between two and three, and that is worth knowing without being worth calling settled. A ninth carded run would decide it; at today's 32% card rate that is another K=5 or so. 🔴 AND THE CARD RATE ITSELF MOVED, which is a second finding and a caution about the first. This scenario carded on 70% of its historical runs and 32% of today's — the tool is simply being called less often (8 of 25). So part of 'no write before the card' is 'fewer turns reach the card at all', which is the same population-shift confound that has bitten this corpus repeatedly. The two cannot be separated by running more of the same scenario. WHAT IT MEANS FOR THE RULING AS IT STANDS: the corrected corpus figure is 14.8% of carded turns (95 of 640), the write-before-card behaviour has not been observed today in 8 carded runs of the scenario that historically carried it most, and no fix was ever aimed at it. 'Not reproducing lately, cause unknown, population also shrinking' is the state — not 'fixed', and not 'still runn…

**⚠ Correction on record (the_ninth_carded_run_LANDED__p_0_048__2026_08_30):** The deciding arm ran. Three LEFT-PENDING batches combined (K=5 + K=20 + K=10, --approvals none, throwaway fixture per run): runs 35 carded 9 26% (historically 28 of 40 = 70%) carded WITH an author-visible write first 0 P(0 in 9 | this scenario's own historical 28.6% among carded) = 0.048 SO IT IS BELOW 0.05, AND ONLY JUST. Nine carded runs produced no write-before-card where this scenario's history predicts between two and three. That is a real result and it is one carded run from not being one — a single counter-example would put it back above the line, so it should be read as 'not reproducing' rather than 'cannot happen'. 🔴 AND IT IS NOT A FIX, WHICH IS THE PART THAT DECIDES WHAT IT IS WORTH. Nothing was ever built for this behaviour. The standing rule is explicit — a row that stopped reproducing is not fixed, no fix, no credit — and it applies here in full. What has been established is that the behaviour is absent from current runs of the scenario that carried it most, not that anything prevents it. 🔴 THE CARD RATE CONFOUND STANDS TOO. This scenario carded on 70% of its historical runs and 26% of these 35. Fewer turns reach the card at all, so some of the zero is 'the trigger fires less', not 'the defect stopped'. Running more of the same scenario cannot separate them, and I have not separated them. THE STATE FOR THE RULING, all three numbers together: corpus, corrected for the read log 95 of 640 carded turns 14.8% this scenario, historically 8 of 28 carded runs 28.6% this scenario, today 0 of 9 carded runs p = 0.048 That is a real population in the record, no current i…

**⚠ Correction on record (the_scenario_breakdown_RE_DERIVED__half_is_ONE_scenario__2026_08_30):** The read-log correction cascades further than the headline: the SCENARIO breakdown I gave earlier was also built on the any-table predicate, and it named the wrong scenarios. Re-derived with the author-visible predicate over 647 carded turns (95 real instances, against 242 by any-table): real (any-table said) scenario 48 48 composition-glossary-build-with-an-ontology 9 11 kg-propose-edge-approve 8 26 glossary-create-evidence-attrvalue 7 7 gbuild-control-named-outright 6 6 translation-job-control-refederated 5 15 composition-entity-override-edit-namefix 4 6 an-override-refusal-must-arm-the-supplier-it-names 🔴 FORTY-EIGHT OF THE NINETY-FIVE — MORE THAN HALF — ARE ONE SCENARIO, composition-glossary-build-with-an-ontology. And that scenario's behaviour is already explained elsewhere on this question: it was orphaned, restored today, and now routes to composition_build_cast_and_graph, which writes nothing before its card by design. So the single largest contributor to this population changed for a reason that has nothing to do with carding order. 🔴 AND kg-propose-edge, WHICH I HAD LISTED AT 15, HAS ZERO. Its 15 were entirely read-log rows: 21 runs, 15 carded, 0 with an author-visible write. I launched a K=10 batch of it as a generalisation test BEFORE measuring that prior — a batch against a 0% baseline cannot show a decrease, so it is uninformative by construction and is recorded as a mis-aimed experiment rather than quietly dropped. Measuring the prior first would have cost one query. WHAT THIS LEAVES. The 14.8% corpus figure stands, but it is NOT spread across the platform:…

**My recommendation:** (a), the card. It is the surface the author is actually looking at when they decide, and the decision they are making — approve or not — is better informed by knowing what already happened. (b) puts the account in prose the model may contradict in the same message, which is the failure mode DQ-T71 is about. 🔴 HELD WEAKLY AND HERE IS THE HONEST CAVEAT: (a) needs an FE change to render it, and this loop has a standing rule that an affordance needs a UI path to satisfy it. I have NOT verified that the card component can show a 'what already happened' section, so (a) may be more expensive than it looks. If it is, (b) is the cheap version and it is server-side only — the line already exists, it…

**Why it is yours, not mine:** All three change what an author reads on the most common write path in the product. (a) and (b) also put the platform's words next to the model's, which this loop does not do without a ruling.

**Blocks:** `D-A-TURN-THAT-EXHAUSTS-ITS-PASSES-WRITES-AND-SAYS-NOTHING`

### DQ-T74 — releases 1 defect

**Asked:** 14 tools are `proven` and have never once returned ok:true and never reached a confirm card, measured over the whole chat store (204 of 204 proven tools covered). The LIVE bar asks `called >= 1`, so proven means REACHABLE, not FUNCTIONAL. Should the verdict state the call outcome, or should these rows be demoted?

**⚠ Correction on record (re_derived_2026_08_30_and_MY_OWN_REASONING_IS_REFUTED):** RE-DERIVED against the live chat store after today's ~60 additional runs, on the question's own criterion (never returned ok:true AND never reached a confirm card): proven tools 204 ever returned ok:true 136 never ok, but DID reach a confirm card 57 NEITHER ok NOR a card 11 (the question says 14) 🔴 INSTRUMENT CHECKED FIRST, AND IT CAUGHT A 6x OVERSTATEMENT. The first pass tested only 'never returned ok' and produced 68 — because the question's criterion has TWO halves, and 57 of those tools are Tier-A writes that legitimately stop at a card and never execute. 68 would have been a shocking number reported by a measurement measuring itself. The card witness was checked too: `chat_suspended_runs` retains history (1,441 rows from 2026-07-19 to today, 1,386 already expired and still present), so it is a valid record of cards ever raised rather than only those pending now. 🔴 AND MY RECOMMENDATION'S REASONING IS WRONG. It says 'most of these tools are probably fine: they fail on a missing required id, which is a SURFACING defect with its own rows.' Read the errors these 11 actually returned and only FOUR do: MISSING A REQUIRED ARGUMENT — the assumed case, 4 of 11 composition_conformance_run(args) · lore_enrichment_auto_enrich(args) plan_keep_material(run_id) · kg_entity_edge_timeline(entity_id) AN INVENTED OR MIS-SHAPED ID — 2 composition_get_generation_job: job_id must be a UUID, received '0-27f0eb…' composition_arc_import_analyze: import_source_id must be a UUID TWO DIFFERENT REQUIRED IDS SET TO THE SAME VALUE — 2, and this one is its own shape plan_pass_status and composition_…

**My recommendation:** Not my call. STATE IT, do not demote. Adding 'proven (reachable; 0 of N calls returned ok)' to the verdict line costs nothing, changes no state, and makes the silent half audible — the same shape as the selection-rate annotation accepted under DQ-T51. Demoting 14 rows re-opens part of the catalogue by side effect, and most of these tools are probably fine: they fail on a missing required id, which is a SURFACING defect with its own rows. The figure is now over COMPLETE coverage (the earlier 17 was over a fifth of the catalogue and named nine tools wrongly), so it is no longer a floor — but it counts only tools the model has actually called through the chat path.

**Blocks:** `D-SEVENTEEN-PROVEN-TOOLS-HAVE-NEVER-ONCE-RETURNED-OK`

### DQ-T75 — releases 1 defect

**Asked:** DQ-T33 IS ANSWERED AND BUILT AND IT REACHES 74 OF 94 TURNS. What should a turn say when it produces no user-visible text and NOTHING FAILED — no error to surface, no card, the tools returned ok?

**⚠ Correction on record (WHICH_READING_WAS_WRONG_is_now_ESTABLISHED_2026_08_30):** This question recorded an open uncertainty — 'either the 08-28 count was mistaken or the column was later rewritten. I could not tell.' It is now settled, against the live store. THE 08-28 CONTROL WAS WRONG WHEN IT WAS WRITTEN. DQ-T33 shipped on the claim that 'of the 94 target turns, ALL 94 carry a tool error'. Three checks, each of which could have explained the gap innocently, and none does: 1. NOT A PREDICATE DIFFERENCE. Three readings of 'carries a tool error' were run over the 94 rows and all three return exactly 74: ok = false AND a non-empty error string 74 (what _last_tool_error_for_author uses) ok = false, whatever the error text 74 an error key present and non-null 74 A looser predicate is the obvious way to reach 94 and no looser predicate does. 2. NOT AN EMPTY-CALLS ARTEFACT. Zero of the 94 have no tool calls at all, so none is 'blank because nothing ran'. 3. NOT A DIFFERENT POPULATION. Nearby definitions were checked for one where the claim would be TRUE at about this size, and there is none: stop + empty content 94 turns 74 with an error <- the target set stop + empty + has calls 94 74 outcome=failed + empty 276 111 any finish_reason + empty 1,555 1,278 WHAT REMAINS POSSIBLE, stated rather than buried: a rewrite of `tool_calls` on exactly those 20 rows cannot be excluded from outside. It is a much weaker reading — it would require 20 rows to lose their errors while the outcome breakdown (67/21/6) stayed identical — but a column I cannot version is a column I cannot fully clear. WHY IT MATTERS TO YOUR RULING: the shipped remedy was justified by a control sayi…

**⚠ Correction on record (the_population_has_not_grown_and_the_window_is_narrow__2026_08_30):** CURRENCY CHECK, 2026-08-30. The population is unchanged at 94 and the newest member is dated 2026-08-25. Daily, from the store (assistant turns with finish_reason=stop and empty content): 2026-08-22 32 silent / 1,170 turns 2.7% 2026-08-23 16 / 512 3.1% 2026-08-24 0 / 294 2026-08-25 1 / 170 2026-08-26 0 / 311 2026-08-27 0 / 205 2026-08-28 0 / 60 2026-08-29 0 / 263 2026-08-30 0 / 342 ZERO IN 1,181 ASSISTANT TURNS SINCE 2026-08-26. At the historical 1.044% (94 of 9,006) that predicts about twelve; P(0) is effectively 0. 🔴 AND IT IS STILL NOT FIXED. Nothing was ever built for this row — the row says so itself — so this is the 'stopped reproducing' state the standing rule refuses to credit, not a repair. What it adds is a much firmer date: the behaviour ends between 2026-08-23 and 2026-08-26, which is a narrow window for whoever eventually looks for the cause. THE POPULATION-SHIFT CONFOUND IS WEAKER HERE THAN ELSEWHERE, and it is worth saying why rather than waving it away. The other currency checks today rested on ONE scenario, where a zero can simply mean the trigger stopped being run. This denominator is 1,181 assistant turns across every batch since — many scenarios, several services, both approved and unapproved arms. It is not immune (today's batches were chosen by me and skew toward the rows I was working), but it is not one turn shape either. WHAT WOULD SETTLE THE CAUSE: the row's own 'what is NOT known' still stands. This says WHEN it stopped, not why, and a date range is not a mechanism.

**⚠ Correction on record (THE_FIX_EXISTS_and_shipped_2026_08_24__found_2026_08_30):** 🔴 THIS ROW HAS A FIX, AND ITS OWN TEXT SAYS IT HAS NONE. Found by asking what shipped in the window where the population stopped growing — 16 silent turns on 2026-08-23, zero on 2026-08-24. 8bca0df55 2026-08-24 11:22 fix(chat): retry the degenerate pass — the platform had already decided this, for the same trigger shape The commit describes THIS population in its own words: 'one run in five ended SILENT. RUN_FINISHED, reasoning events present, zero TEXT_MESSAGE_* events, no card, no error, textlen 0.' It was shipped to unblock settings_provider_inventory, not for this row — which is exactly why this row never recorded it. THE DEPLOYED CODE NAMES THIS ROW, at stream_service.py:648 inside infra-chat-service-1: DEGENERATE_PASS_INTERVENTION_CAP = 1 # D-SILENT-TURN-NO-CARD-NO-PROSE — the SAME shape as the loop above, degenerate in ONE # pass … the model emits reasoning-channel content and no call and no prose. Measured, # that content is exactly 12 characters, the literal `<tool_call|>` … every turn # producing PROSE has reasoning_length 0; every silent finish_reason='stop' turn has # reasoning_length 12. So the remedy already chosen above (abort, steer, reasoning OFF # for the retry, capped) targets exactly this turn's correlate. AND IT IS STILL FIRING: the retry logged twice in this session's own batches (docker logs infra-chat-service-1, last 12h). SO 'NO FIX AND NO CAUSE. Nothing was changed for this row' IS FALSE, and has been since 2026-08-24. There is a named mechanism (a degenerate pass is aborted and retried with reasoning off, capped at one), a measured correlate (rea…

**My recommendation:** NOT MY CALL — the owner already declined a generic failure line for this question, so inventing one now would be substituting a mechanism quietly, which is the thing I must not do. WHAT I WOULD RECOMMEND, and it is the same principle the owner accepted last time — REPORT, DO NOT INVENT: these turns are not empty-handed. They hold a SUCCESSFUL tool result. A turn that read the package tree and said nothing could say what it read, from the result already in the record, the way the error case speaks the error already in the record. That keeps the rule that made DQ-T33 answerable (the assistant says only what the turn actually produced) and it needs no new prose. 🔴 AND IT IS A BIGGER SURFACE DE…

**Blocks:** `D-SILENT-TURN-NO-CARD-NO-PROSE`

### DQ-T76 — releases 1 defect

**Asked:** The fabricated-id guard tests SYNTAX, so a well-formed invented UUID reaches the tool. FIVE mechanisms have now been measured and every one fails on PRECISION. Is this blindness accepted, or is the platform willing to pay one of the measured costs to close it?

**⚠ Correction on record (option_c_s_PREMISE_partially_checked__2026_08_30):** Option (c) — the one I said I would take — rests on a specific claim: the version rule 'had perfect precision over 12,276 real arguments and was defeated by ONE platform-issued value: the RESOLVING sentinel is v0. If that sentinel were minted as a v7 (or excluded by identity rather than by version)…'. Whether (c) is nearly free turns on that sentinel being a PLATFORM-ISSUED id. Checked, and the answer so far points the other way. the literal 00000000-0000-0000-0000-00000000000a appears in exactly TWO files, and BOTH ARE TESTS: services/chat-service/tests/test_a_placeholder_uuid_is_not_an_identifier.py:64 services/glossary-service/internal/api/export_handler_test.go:830 No production code mints it. sentinel-SHAPED ids (00000000-0000-0000-0000-*) in the live stores: books.id 0 chapters.id 0 chapter_entity_links.entity_id 0 of 8,936 glossary_entities.entity_id 0 of 7,575 🔴 THIS IS PARTIAL AND MUST NOT BE READ AS A REFUTATION. The claim it bears on came from a scan of 38,314 real ids; I have checked roughly 16,500 across four columns in two databases. Zero here does not mean zero everywhere, and 'absence in the tables I looked at' is exactly the shape of an absence argument this loop has been wrong about before. WHY IT STILL MATTERS TO THE RULING: if the blocking value is a TEST fixture rather than a platform-issued id, (c) is not merely cheap — it may be unnecessary, because the version rule's one measured false positive would never occur in production at all. That would move (c) from 'the option I would take' to 'the option that may already be true', and it would change what…

**⚠ Correction on record (option_c_s_PREMISE_HOLDS__and_it_CORRECTS_my_note_from_an_hour_ago__2026_08_30):** 🔴 WITHDRAWING MY OWN FINDING FROM EARLIER TODAY. I checked four id columns, found zero sentinel-shaped values, and wrote that option (c)'s premise 'points the other way' — that the RESOLVING sentinel might be a TEST fixture. I flagged in that same note that 'absence in the tables I looked at' is exactly the shape of absence argument this loop has been wrong about before, and then I was wrong about it in that shape. THE FULL SCAN, over EVERY uuid id-column in five databases (497 columns): database distinct ids with <= 2 distinct hex digits glossary 1,210,381 18 chat 41,695 3 That is 21, against the shipped docstring's 'its 20' — the original figure is CONFIRMED, not refuted. My first pass over occurrences rather than distinct values said 4,130, which is the same handful of ids counted once per foreign key. AND THE SENTINEL IS THERE, AT VERSION 0: 00000000-0000-0000-0000-000000000001 … 00000000-0000-0000-0000-00000000000d (13, v0) 00000000-0000-0000-0000-00000000bbbb · …000000033003 (v0) 00000000-0000-0000-0001-000000000001 · …0002-…0002 · …0003-…0003 (v0) ---- and in chat, the fabrications the guard DOES catch ---- 11111111-… · 22222222-… · 33333333-… (v1, v2, v3) SO OPTION (c)'s PREMISE HOLDS: `00000000-0000-0000-0000-00000000000a` is a real, resolving id in the live glossary store, and it is version 0. 🔴 BUT IT IS A FAMILY OF EIGHTEEN, NOT ONE VALUE, and that changes what (c) costs. The question says 'if that sentinel were minted as a v7 (or excluded by identity rather than by version)'. Re-minting means re-issuing 18 ids and every foreign key that points at them — far mo…

**My recommendation:** NOT MY CALL. Three options, with what each costs, and I would take the third. (a) ACCEPT THE BLINDNESS. The tool's own 'not found or not accessible' is already the backstop, and it is honest. The cost is that the author sees a tool failure rather than a refusal that says 'this id was invented' — and the model, told only that a row is missing, invents a better-formatted one. That is the measured behaviour this row was filed on. (b) PAY FOR THE EXISTENCE CHECK. It is the only mechanism that catches a well-formed fabrication in general, and it costs ~1 refusal in 4 on non-context ids. Too expensive on today's numbers, and the row already says so. (c) 🔴 MAKE THE VERSION RULE TRUE BY CHANGING TH…

**Blocks:** `D-FABRICATION-GUARD-IS-BLIND-TO-A-VALID-LOOKING-UUID`

### DQ-T77 — releases 1 defect

**Asked:** A turn that could not reach the tool the author asked for answered from a DIFFERENT tool and attributed the verdict to the one it had not run — in the author's own words, not the tool's identifier. Two detectors have now been built and measured and both fail. Is this accepted, or should the platform constrain how a reply may attribute a verdict?

**My recommendation:** NOT MY CALL. I would ACCEPT THE BEHAVIOUR AND FIX THE SURFACE, on this row's own evidence — and I record that this is the opposite of what the row argues, because the row is explicit that 'a surface fix would stop this instance without touching the behaviour'. The reason to disagree with it is the natural experiment the row itself produced: the model distinguishes the two tools perfectly when it holds both. The misattribution is not a habit it has; it is what it does when the capability the author named is absent. Two mechanisms aimed at the reply have now failed on opposite ends of the same problem — identifiers never fire, product words fire on everything — and a third attempt would need…

**Blocks:** `D-THE-PROBLEMS-PANEL-IS-REPORTED-AS-THE-GOLDEN-LINTER`

### DQ-T78 — releases 1 defect

**Asked:** The DECLARED-UUID arm of the fabricated-id guard exempts the three runtime CONTEXT ids (book_id / chapter_id / project_id). That exemption is the reason this row's own instance — a fixture NAME passed as `project_id` — reaches the tool. Removing it catches 165 more model-supplied values AND is the exact change that once broke the dispatch. Is the platform willing to make that trade?

**⚠ Correction on record (the_precision_question_ANSWERED_from_the_code_path__2026_08_30):** My recommendation names the gap: 'The precision question is whether any legitimate runtime-injected context id is long, and I have not measured it — the 320 are all model-supplied, so they cannot answer it.' The store cannot answer it, but the CODE PATH can, and it gives a sharper answer than a sample would. WHERE A RUNTIME-INJECTED CONTEXT ID ACTUALLY COMES FROM (stream_service.py ~9471): _ctx_book_id = (editor_context or {}).get('book_id') or (book_context or {}).get('book_id') or (studio_context or {}).get('book_id') or str(session_row['book_id']) <- chat_sessions.book_id, uuid column 🔴 ONLY THE LAST OF THE FOUR IS TYPE-GUARANTEED. `chat_sessions.book_id` and every equivalent column (chat_suspended_runs.book_id / chapter_id / project_id) are `uuid` in the schema, so a value from that fallback CANNOT be long-and-hyphenated, a book title, or a bare English word. The other three come from the request body's context objects — the code's own comment calls `_ctx_book_id` 'client-supplied' and deliberately contrasts it with the server-resolved id used for canon capture — and nothing parses them as UUIDs at this point. AND THE FRONTEND DOES NOT CONSTRAIN THEM EITHER: `editorContext?: { book_id: string; chapter_id: string }` is a plain string, not a branded id, and the FE's own test asserts `editor_context` equals `{ book_id: 'b1', chapter_id: 'ch1' }`. A short bare non-UUID flows through this exact shape in at least one code path today. 🔴 WORTH YOUR EYE WHEN YOU RULE: 'b1' is also, by this question's own account, 'the single value the five red tests actually bought'. I am NOT c…

**My recommendation:** NOT MY CALL — the row says so itself: 'the trade is real and it is not mine to make on 320 samples from one deployment.' I agree with that and am not overriding it. WHAT I WOULD RECOMMEND is the narrower version the measurement supports: keep the exemption for a value the runtime could plausibly have injected, and drop it for one it could not. Every one of the 165 survivors is either long-and-hyphenated (a fixture name), a book TITLE, or a bare English word ('all', 'none'). The single value the five red tests actually bought is `b1` — a short bare non-UUID with no placeholder token. 🔴 AND I WOULD MEASURE THAT BEFORE SHIPPING IT, because a length-or-shape rule is exactly the kind of blacklis…

**Blocks:** `D-FIXTURE-NAME-IS-THE-MOST-PLAUSIBLE-LOOKING-ID`

### DQ-T79 — releases 1 defect

**Asked:** May an INSTRUMENT row be closed on the instrument WORKING, when neither a reproduction nor a cause can be bought? The row names this option itself and says it 'is a judgement about what `fixed` means for a scenario rather than a measurement, and is not mine'.

**⚠ Correction on record (the_numbers_MOVED_the_same_day_2026_08_30):** TWENTY MORE RUNS OF THE SAME SCENARIO, taken after this question was filed — batch c-motiflink-postfix, composition-motif-link-edit, K=20, real provider, throwaway fixture per run: 0 errored, the tool called 20/20. post-fix total was 2 of 35 (5.7%) post-fix total now 2 of 55 (3.6%) Fisher exact vs the founding 6 of 10: p = 0.00060 -> p = 0.000063 🔴 AND THEY ARE NOT A CLEAN REPLICATION, which matters more than the improvement and is why this is recorded rather than just appended. Those 20 runs were taken under a fix that did not exist when the earlier 35 were: D-RAIL-PINNED-TURN-NEVER-COMPLETES shipped today, and the cut FIRED on this very scenario (tool_call_repeat:composition_motif_link_editx9). So the platform under the last 20 runs is not the platform under the first 35, and pooling them into one 'post-fix total' quietly averages two different systems — the same mistake this loop has corrected four times in other rows. READ EACH WAY, both are stated because they answer different things: as ONE population 2 of 55, 3.6% — the scenario's fitness TODAY, which is what the question's 'may it close on the instrument working' turns on STRATIFIED 2 of 35 before today's fix, 0 of 20 after. P(0 in 20) at the old 5.7% is 0.31, so the 20 do NOT establish a further improvement — they are consistent with the rate simply being unchanged MY RECOMMENDATION IS UNCHANGED and the reasoning is untouched: yes for an INSTRUMENT row specifically, because the claim is about the scenario's fitness and fitness is measurable. What is different is that the residual whose cause is unknown — the 2 — i…

**My recommendation:** NOT MY CALL, and I would say YES for an INSTRUMENT row specifically — not as a general relaxation of 'a row that stopped reproducing is not fixed'. The reason is what an instrument row CLAIMS. This one's own `why_it_matters` is 'a scenario that loses 60% of its runs cannot measure anything at K=5'. That claim is about the SCENARIO'S FITNESS, and fitness is directly measurable: 35 post-fix runs at 5.7%, 60 clean runs across three configurations today. The scenario measures again. For a PLATFORM row the rule must stay as it is, because there the claim is about behaviour reaching an author and a quiet spell proves nothing. 🔴 WHAT SAYING YES COSTS: the residual 2 of 35 is real and its cause is…

**Blocks:** `D-THE-MOTIF-LINK-SCENARIO-TIMES-OUT-6-OF-10`

### DQ-T81 — releases 1 defect

**Asked:** May a defect row be closed as EXPLAINED-AND-REPAIRED when (i) a mechanism has been fixed and proven live elsewhere, (ii) that mechanism accounts for every one of the row's symptoms including the four it independently refuted, and (iii) the mechanism is proven present and firing in the row's own scenario — but no stalling instance of THIS row was ever captured and shown to be that mechanism, and the fix has made capturing one impossible? Or must such a row stay open permanently? This governs D-TURN-STALLS-AFTER-THE-SURFACE-IS-BUILT and, if answered generally, the ReadTimeout half of D-UPSTREAM-ERROR-WITH-NO-MESSAGE.

**My recommendation:** RECOMMEND: closeable, but ONLY under a named state that is not `fixed`, and only with the un-demonstrated half written on the row. THE TRAP IN 'WAIT FOR A CAPTURED STALL' is that it is unsatisfiable BY CONSTRUCTION. If the fix works, the stall cannot recur; if it recurs, the fix is wrong and the row was never closeable. So the rule as literally worded converts a successful repair into a permanently open row, which makes the ledger's open count stop meaning 'work remaining'. That is the same failure mode as a queue reporting NO WORK while work exists, in the other direction. BUT `fixed` OVERSTATES IT, and this loop has been burned by exactly that word before — a verdict word promising more t…

**Why it is yours, not mine:** It is a rule about what EVIDENCE closes a row, not a fact about this system, and the standing bar cuts both ways here. 'A row that STOPPED REPRODUCING is not fixed: no fix, no credit' exists to stop exactly this kind of reasoning — but its premise is 'no fix', and here a fix shipped, was proven red-able, and is proven firing in the row's own scenario. Deciding that a shipped fix plus full symptom…

**Blocks:** `D-TURN-STALLS-AFTER-THE-SURFACE-IS-BUILT`

### DQ-T1 — releases 0 defects

**Asked:** What is `entity_lifecycle_ledger` for, and what should write it? Options seen, none chosen: (a) every status transition (approve/retire/reject via glossary_propose_curation) appends a row, making the ledger the audit trail its columns describe; (b) it is delete/restore-only by design and the column set is aspirational; (c) it is superseded by entity_revisions and should be dropped.

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**My recommendation:** RECOMMEND (a) - every status transition appends. Not because the ledger is used, but because somebody paid for an append-only TRIGGER on it: that is a deliberate integrity guarantee, and a guarded table holding three rows from a single day is the shape that later gets read as an audit trail it never was. (c) drop-it is the honest alternative and is cheaper; what makes it the wrong DEFAULT is that the columns (op, prior_status, new_status, actor_type, actor_id, reason) already describe the curation transition exactly, so the intent is not in doubt. Decide (c) only after checking for readers - if anything reads it today, (c) is a silent capability loss.

### DQ-T2 — releases 0 defects

**Asked:** Should a deprecated tool be required to name a replacement, and what replaces the ones that have none? Options seen, none chosen: (a) superseded_by becomes mandatory for visibility=legacy and the 55 gaps are filled; (b) a retired-with-no-successor tool is legitimate and tool_list's description should stop promising a label; (c) those 55 are not actually deprecated and the visibility tag is wrong.

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**⚠ Correction on record (newly_relevant_2026_08_30):** NOT STALE — this question got SHARPER today. It asks whether a deprecated tool should be required to name a replacement, and what replaces the ones that have none. D-THE-MODEL-ASKS-INSTEAD-OF-RAISING-THE-CARD-IT-HAS shipped a fix that transfers a dropped legacy tool's declared phrasing to its live successor — and measured, against the live 316-tool catalogue, that 31 legacy tools name NO resolvable successor (book_create, book_chapter_publish, and composition_publish, whose chain ends at a legacy tool with no `superseded_by`). For those 31 the vocabulary is simply lost when the tool is dropped, and nothing invents a destination for it. So option (a) — make `superseded_by` mandatory for `visibility=legacy` — now has a measured cost of NOT doing it, which it did not have when the question was raised.

**⚠ Correction on record (the_census_is_RE_DERIVED_and_the_headline_is_STALE_2026_08_30):** The question asks whether 'the 55 gaps are filled'. Re-derived over the live 316-tool catalogue cache — seventeen days after the 2026-08-13 census the question quotes: visibility=legacy 117 unchanged ... with NO superseded_by 31 was 55 TWENTY-FOUR GAPS HAVE BEEN FILLED, AND ALL OF THEM WERE GLOSSARY'S. Compared per service against the question's own breakdown: glossary 28 -> 4 24 filled book 11 -> 11 unchanged kg 11 -> 11 unchanged lore 4 -> 4 unchanged composition 1 -> 1 unchanged So the decision is a 31-tool one, not a 55-tool one, and four of the five services are exactly where they were — the scope shrank in one place and moved nowhere else. 🔴 AND THE TWO NUMBERS IN THIS SAME QUESTION ARE NOT THE SAME SET. `newly_relevant_2026_08_30` says '31 legacy tools name NO resolvable successor' and cites composition_publish as an example of a chain ending at a legacy tool. Those are two different criteria and they do not give the same answer: no superseded_by at all 31 no RESOLVABLE successor (chain) 32 The difference is exactly composition_publish, which DOES name a successor — book_chapter_publish — that is itself legacy with no successor of its own. So it is absent from the first set and present in the second, and the note's figure of 31 is the wrong one for the criterion it describes. Both numbers being 31 in the same document is the sort of coincidence a later reader welds together; they are separated here on purpose. My recommendation — (a), make superseded_by mandatory for visibility=legacy and enforce it with the existing lint — is unchanged, and cheaper than the questio…

**My recommendation:** RECOMMEND (a) - superseded_by mandatory for visibility=legacy, the 55 gaps filled, enforced by a lint. tool_list's own description PROMISES the label, and 55 of 117 legacy tools do not carry it, so the promise is false for nearly half of them. (b) 'stop promising it' is internally consistent but trades a fixable data gap for a permanent capability loss - the successor is exactly what a model needs when it lands on a legacy tool. The enforcement shape already exists: scripts/lint_superseded_synonyms.py fails the pair at build.

### DQ-T34 — releases 0 defects

**Asked:** Should a *_edit tool that requires an opaque id ALSO accept the human-readable key its own sibling lists (here `code`, e.g. 'throwaway-loop-skeleton-b15')?

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**⚠ Correction on record (the_premise_could_NOT_be_REPRODUCED_2026_08_30):** 🔴 THE HEADLINE PAIR IS NOT RECONSTRUCTIBLE FROM THE ARTEFACTS, and that is the finding. The recommendation leans on '45 of 86 id-requiring tools name no supplier tool at all' (measured 2026-08-23, world_* excluded). Re-derived today over the live catalogue cache and `argument_emitters`, across every reading of 'id-requiring' I could construct: id-requiring no supplier all tools, ambient ids counted 178 106 live only (legacy dropped) 98 50 ambient book_id/project_id excluded 134 65 live only AND ambient excluded 65 20 the question says 86 45 NONE of them lands on 86/45. The closest is live-only at 98/50. Part of the gap is real drift — seven days and a catalogue that has moved — but the criterion itself was never written down, so which tools counted as 'id-requiring' cannot be recovered, and a number nobody can rebuild is a lead rather than evidence. WHAT SURVIVES ANYWAY, and it is what the recommendation actually needs: the SHARE is stable across readings — 60%, 51%, 49% — against the question's 52%. Only the strictest reading (live tools with ambient ids excluded) drops to 31%. So 'roughly half of the tools requiring an opaque id name no supplier for it' holds under every reading but one, and the argument for accepting a human-readable key does not depend on the exact pair. MY RECOMMENDATION IS UNCHANGED. What should change is the record: whoever rules on this should have the criterion stated, because the next person to re-derive it will land somewhere between 20 and 106 depending on choices nobody wrote down.

**My recommendation:** RECOMMEND yes - an *_edit tool requiring an opaque id should also accept the human-readable key its own sibling lists. This DQ has far more support now than when it was filed: measured 2026-08-23, with world_* excluded entirely, 48% of every tool-call failure in this loop is a required argument the model could not supply, and 45 of 86 id-requiring tools name no supplier tool at all. The code key is already unique per owner and is already what op=create accepts, so taking it here removes the resolution step for the commonest case without inventing a new identifier.

### DQ-T35 — releases 0 defects

**Asked:** Should composition_generate's `model_ref` become OPTIONAL with a server-resolved default, the way 16 of its 19 siblings already declare?

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**⚠ Correction on record (the_census_is_VERIFIED_UNCHANGED_2026_08_30):** Re-derived over the live 316-tool catalogue cache, sixteen days after the census this question quotes, and it is EXACT: tools declaring model_ref 19 (question: 19) REQUIRED 3 (question: 3) composition_generate · composition_library_translate · kg_build_wiki optional 16 (question: 16) The same three tools, unchanged. A sixteen-day-old number that has been re-checked is worth more than one that has not, and this one needs no correction: the outlier argument the recommendation rests on — a model that learned from sixteen tools that omitting model_ref is correct will omit it on the seventeenth — is exactly as true today as when it was written.

**My recommendation:** RECOMMEND yes - make composition_generate's model_ref OPTIONAL with a server-resolved default. 16 of 19 model_ref properties in the catalogue are already optional and say so in their own descriptions; three are required, and composition_generate is the outlier on the tool where guessing wrong costs the most. Consistency is not tidiness here: a model that has learned from sixteen tools that omitting it is correct will omit it on the seventeenth.

### DQ-T37 — releases 0 defects

**Asked:** Should registry_propose_workflow validate that a step's `tool` names a tool that exists?

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**My recommendation:** RECOMMEND yes - validate that a proposed workflow step's tool name refers to a tool that exists. It is a cheap check at the one place the name enters the system, and the alternative is a workflow that fails at run time on a step nobody can fix without reading the registry. 🔴 THE evidence FIELD OF THIS DQ IS EMPTY: no measurement of an invalid step reaching the store is on file, so this is a design recommendation and not an evidenced one.

### DQ-T38 — releases 0 defects

**Asked:** Why does the public catalogue list nothing while 44 books are marked public?

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better. 🔴 AND THIS ONE IS NOT YET A DECISION AT ALL. Its own recommendation says so: 'NO RECOMMENDATION YET, AND THAT IS THE ANSWER … no cause has been established, so every option would be a guess dressed as a fix. What is owed first is one measurement.' So this is WORK before it is a question, and it should not sit in a decision pack until that measurement has been run.

**My recommendation:** NO RECOMMENDATION YET, AND THAT IS THE ANSWER. The question asks WHY the public catalogue lists nothing while 44 books are marked public, and its evidence field is EMPTY - no cause has been established, so every option would be a guess dressed as a fix. What is owed first is one measurement: does the catalogue query return rows for those 44 books, and if not, which predicate excludes them. Recorded as owed rather than answered.

### DQ-T4 — releases 0 defects

**Asked:** Should glossary-service's non-ambient tools DECLARE book_id required, or should they BECOME ambient? Options seen, none chosen: (a) declare required, as T10-D1 did for the three legacy entity-detail reads - honest, zero behaviour change; (b) tag them WithAmbientBook and switch to bookToolAuthAmbient, so a model inside a book studio may omit book_id exactly as it can for glossary_get_entity and glossary_search; (c) per-tool, on whether the tool is reachable from a studio surface at all.

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**My recommendation:** RECOMMEND (a) - declare book_id REQUIRED on the 37 remaining. Zero behaviour change, it makes the wire honest, and it is the same fix T10-D1 already applied to three of them, so both the precedent and the shape are settled. (b) ambient-ify is the better END state for tools genuinely reachable from a studio surface, but it is a per-tool behaviour change across 37 tools and must not ride along on a declaration cleanup. Do (a) now, then (c) deliberately and per-tool.

### DQ-T40 — releases 0 defects

**Asked:** Should gate.py excuse the LIVE clean bar when every errored run is a NAMED transport defect and the tool was demonstrably exercised on the clean runs?

**⚠ Correction on record (superseded_2026_08_23):** 🔴 WITHDRAW THIS QUESTION. Its premise was 'thirteen hypotheses refuted, the cause is not findable, and at ~49% a transport-clean K=5 has ~3% probability, so re-running is not a path'. Every clause of that was wrong, and wrong in a way worth recording. TWO NAMED CAUSES, both found by looking at what the turn DID rather than at how often it failed: 1. THE FIXTURE HAD BEEN DEAD FOR TWO DAYS. Both motifs were archived by a sibling scenario and never restored — unrecreatable (the unique index has no status predicate), unresolvable (get_by_codes is active-only), and asserting green (the seed_assert had no status filter). 2. THE REFUSAL DELETED THE ANSWER. With the fixture repaired, all five runs read link_edit -> motif_search -> motif_search -> dead. The model resolved both endpoints to NAMES, the whitespace arm dropped them correctly, and the refusal reported them MISSING — so the follow-up search had nothing to search for and went out blank, twice. AND THE INSTRUMENT THAT 'REFUTED' CONTENTION COULD NOT SEE CONTENTION. `ps aux` under Git Bash does not enumerate native Windows processes; two runners were live at one point while it reported none. That hypothesis was never actually tested, and it was recorded as refuted. THE GENERAL LESSON, which outlives this tool: a failure RATE is not a diagnosis, and a long list of refuted hypotheses is not evidence that the cause is unfindable — it is evidence about where I had been looking. The rate said 'flaky platform'; the call SEQUENCE said 'unwinnable turn', and it was in every run the whole time. Do NOT add an `unmeasurable` terminal s…

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better. 🔴 THIS ONE ALREADY RECOMMENDS ITS OWN WITHDRAWAL. Its `superseded_2026_08_23` field opens 'WITHDRAW THIS QUESTION' and shows every clause of its premise was wrong; `state_note` reads 'recommend withdrawn — pending owner'. It needs one word from the owner, not analysis. It is NOT withdrawn here: a DQ is the owner's to close.

**My recommendation:** Do NOT widen the excused set generally. If anything, add a THIRD terminal state — something like `unmeasurable`, requiring a registered defect id, the arm count, and the clean-run evidence — so a tool blocked by a platform failure leaves the queue with its situation described rather than being silently reclassified as `blocked`, which would read as a property of the tool. Until then P3 stays at 6/7 and the loop moves on to the next problem.

### DQ-T43 — releases 0 defects

**Asked:** Should the runner's 'READ-INTENT TURN WROTE TO THE STORE' flag fire on the platform's own per-turn lifecycle event?

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**My recommendation:** Either exclude platform lifecycle events from the write flag, or have the flag name the event type so a reader can tell a tool's write from the turn's own bookkeeping. As it stands the flag says 'a defect whatever it said' about something the tool did not do.

### DQ-T5 — releases 0 defects

**Asked:** How does a NEW book get its first glossary entity? Options seen, none chosen: (a) let a refusal UNLOCK the tool it names for the rest of the turn, the way a pinned rail's step tools are already exempted from the intent gate; (b) make the router inject glossary_shaping on prose like 'set up my book, adopt the standard lore categories', which it does not today; (c) let glossary_ontology_upsert create a kind that is actually usable (with a display attribute), so the non-gated path works end to end; (d) bootstrap a default ontology with the book, the way a knowledge project is already bootstrapped 197ms after creation (OBS-T17).

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**My recommendation:** RECOMMEND (a) - a refusal UNLOCKS the tool it names, for the rest of that turn. The platform already does exactly this for a pinned rail's step tools, so the mechanism exists and only its trigger is new; and a refusal naming its own remedy is the strongest evidence of intent available - the system itself just said which tool is needed. It is reactive rather than pre-emptive, which is the pattern that has held up elsewhere here. (b) widens a phrase list and leaves the class; (c) and (d) fix the bootstrap for one path and leave the gate wrong.

### DQ-T6 — releases 0 defects

**Asked:** Should the hot-seed budget be allowed to advertise a domain's WRITE tools while dropping its primary READ tool? NOTE: this is about what a plain request LANDS ON, not about reachability — a budget-dropped tool is still reachable via tool_list -> tool_load, proven 2026-08-13. Options seen, none chosen: (a) reserve a slot for each domain's read tools before any write is seeded; (b) spend the budget by RELEVANCE to the turn's request rather than by domain order; (c) raise or scale the 2000-token hot-seed budget; (d) treat a read/write imbalance in a seeded domain as a loud failure rather than silently seeding whatever fits.

**⚠ Correction on record (evidence_2026_08_23):** FOUR NEW INSTANCES, AND THEY DO NOT SUPPORT THIS DQ'S FRAMING - recorded because a DQ strengthened with the wrong evidence is worse than one left alone. This DQ asks whether the hot-seed budget should advertise a domain's WRITE tools while dropping its primary READ. Four tools blocked on 2026-08-23 all failed because a required id's supplier was on no pass of the wire, which looks like the same thing. It is not. Two of the four pairs are Tier A consumer <- Tier R supplier and fit the framing; the other two are Tier A <- Tier A (plan_bootstrap_apply <- plan_bootstrap_propose) and Tier R <- Tier R (composition_arc_template_get <- composition_arc_template_list), and neither is a read losing to a write. What the four share is DEPTH, not tier: the request's words select one tool and nothing carries its prerequisites. See D-THE-LOOPS-DOMINANT-FAILURE-IS-AN-ID-THE-MODEL-CANNOT-GET. The recommendation on this DQ - spend the budget by relevance rather than domain order - is unchanged and still stands on its own 2026-08-13 measurement (book_read withheld for budget while two book WRITES were advertised).

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**My recommendation:** RECOMMEND (b) - spend the hot-seed budget by RELEVANCE to the request, not by domain order. The platform has already committed to this principle one stage later: R1 answerability exists to force a tool whose declared vocabulary answers the request onto the wire 'whatever the budget, the domain selection or the rail decided'. (b) applies that same rule at seeding time, so it removes the need for the rescue instead of adding a second mechanism beside it. (a) fixes the measured case and not the class; (c) raising the budget buys time and changes nothing structural.

### DQ-T82 — releases 0 defects

**Asked:** `loreweave_grants._fetch` returns GrantLevel.NONE both when a user genuinely has no grant and when the grant authority cannot be reached — it logs 'grant authority unavailable (fail-closed deny)' and then collapses the two. Every consumer therefore answers a permission denial (403, bare) to an OUTAGE. Should the SDK distinguish them: (a) add an explicit unavailable signal consumers may surface as a retryable failure, keeping the DENY behaviour identical; (b) leave it collapsed and accept that an outage is indistinguishable from a denial; (c) distinguish it only in the one consumer this was measured in, without touching the shared SDK?

**⚠ Correction on record (the_blast_radius_MEASURED_and_it_changes_the_SHAPE_of_the_fix):** My recommendation said 'I have NOT measured how many consumers read this return, and that count should be part of costing (a)'. Measured now, and it does more than cost the option — it rules out the obvious way to build it. services referencing GrantLevel (non-test) 6 campaign · chat · composition · knowledge · lore-enrichment · translation non-test files referencing it 62 non-test call sites of resolve_access/_owner 15 comparisons against GrantLevel.NONE 24 == GrantLevel.NONE 20 is GrantLevel.NONE 4 by ORDERING 0 🔴 EVERY ONE OF THOSE 24 IS AN EQUALITY TEST, AND THAT MAKES AN ENUM SENTINEL DANGEROUS. The shape at each site is: if lvl == GrantLevel.NONE: raise <deny> if not lvl.at_least(need): raise <deny> Add `GrantLevel.UNAVAILABLE` and the FIRST deny is skipped at all 24 — the value is no longer equal to NONE. Whether the request is then denied at all depends entirely on the integer chosen: below NONE it still fails closed via `at_least` (but reports the wrong reason — 'insufficient level' rather than 'no grant'), and ABOVE OWNER it fails OPEN. A security posture that hinges on an enum member's integer is one nobody would think to review in a diff, and 62 files would carry it. SO (a) IS STILL MY RECOMMENDATION, BUT ITS SHAPE IS NOW CONSTRAINED, and the constraint is the useful part of this measurement: the unavailable signal must ride OUT OF BAND — a separate method, or a field the caller must ask for — and MUST NOT be a new GrantLevel member. That keeps all 24 equality checks byte-identical in behaviour, leaves 62 files untouched, and lets consumers opt in one at a time…

**My recommendation:** RECOMMEND (a), and note it is the SAME principle this platform already accepted one layer up: composition's confirm route names its BookClientError 502 because 'an UPSTREAM failure is not a fact about the caller's data'. A grant authority being down is exactly that — it discloses nothing about whether anyone holds a grant, so surfacing it is not an oracle and H13 is not engaged. The DENY must not soften: fail-closed on an unreachable authority is correct and should stay byte-identical. What changes is only that the caller can tell 'you may not' from 'I could not find out', which is the difference between an author giving up and an author retrying. MEASURED, not argued: a real gate confirm t…

**Why it is yours, not mine:** It changes a SHARED SDK's contract, read by every service that gates on a grant, so it is a cross-cutting scope and cost decision rather than a correctness one. It also sits next to a security invariant — fail-CLOSED must not change, and H13 requires not-found and not-permitted to stay uniform — so any change here should be authorised rather than inferred.

### DQ-T83 — releases 0 defects

**Asked:** 22 defect rows sit at `state: proven` — not fixed, not withdrawn, and by this goal's bar not finished. Their evidence is real but lives in prose. They are being verified and closed one per cycle without a ruling; what is left for you is whether `proven` should remain a legal DEFECT state once they are gone, given the TOOL vocabulary already uses that word for 'the tool works'.

**⚠ Correction on record (the_population_is_ALREADY_GUARDED__measured_2026_08_30):** MEASURED AFTER FILING, AND IT MAKES (b) MUCH CHEAPER THAN I ESTIMATED. Every one of the 23 rows' `lives_in` files still resolves, 17 name a symbol that is still present, and scanning 1,776 test files across every service: ALL 17 of those symbols are named by at least one test. AND THE SYMBOL NAMES ARE THE REPAIRS, not the wounds: T2-D3 number sent where the schema says string _stringify_int_args_declared_string T3-D1 entity_ids arrived as objects _unwrap_object_items_for_string_array T6-D1 an id carrying its own field name _strip_field_name_prefix_from_ids T16-D1 right id under the wrong key _repair_aliased_required_id T7-D1 116 deprecated tools in the listing _tool_list_include_deprecated Four of their guards are named after the defect itself: test_int_arg_where_schema_says_string, test_object_items_for_string_array, test_field_name_prefixed_id, test_aliased_required_id_is_repaired. Those functions exist BECAUSE the defect was fixed, and the row was updated to point at the fix while its `state` was left behind. THE THREE I READ IN FULL ARE FIXED AND GREEN. T7-D1, T7-D2 and T7-D3 in tool_discovery.py, with test_tool_list_default_hides_deprecated, test_tool_list_never_reports_a_held_tool_as_absent and test_tool_list_names_a_bad_category_guess — 56 tests, all passing. 🔴 WHAT THIS DOES NOT ESTABLISH, and the distinction is the whole reason this is still a question. A guard existing proves a guard exists; it does not prove it covers THIS row's defect. That is the same over-reading this loop has already recorded — a verdict word promising more than its bar checks — so I am not…

**⚠ Correction on record (one_of_the_two_is_now_DONE__2026_08_31):** D-RAIL-DONE-SUPPRESSES-THE-REREAD has been verified and moved to `fixed`, so this question now covers 24 rows, not 25: the 23 bare ones plus DQ-T30-ANSWER-FROM-THIS-TURN. IT IS ALSO A WORKED EXAMPLE OF OPTION (b), and it cost one cycle: re-check the commit, run the guard (5 passed), read the chokepoint against what the row claims, and confirm the DEPLOYED image carries it — the container's stream_service.py is byte-identical to the host file by sha256. No fresh live run; the row's live evidence is the original one from 2026-08-14, and the note on the row says so rather than letting `fixed` imply otherwise. That is the shape I am recommending for the rest. It does not presume your ruling for the 23 bare rows, which carry no commit, no test and no evidence field and therefore cannot be verified this way — for those the guard has to be matched to the defect by reading both, which is the part that needs your call on whether it is worth 23 cycles.

**⚠ Correction on record (BOTH_evidence_carrying_rows_are_now_DONE__2026_08_31):** The second of the two, DQ-T30-ANSWER-FROM-THIS-TURN, is now `fixed` as well, so this question covers 23 rows — all of them the bare kind, carrying only found_by / lives_in / title / note. THE TWO WERE NOT THE SAME JOB, and that is worth knowing before ruling on the 23. D-RAIL-DONE already had recorded live evidence, so verifying it was one cycle of re-checks. DQ-T30-ANSWER had only a 0-of-3 measurement OF ITS OWN BROKEN FIRST DRAFT, so it needed a real K=5 live run (5/5 called on the measured re-ask, its void-control passed, both rule texts quoted). A row that LOOKS evidenced can still be unproven, and the difference was only visible by reading which version the number described. That is the risk in the 23: none of them carries any evidence field at all, so none can be assumed to be the cheap case.

**⚠ Correction on record (THE_BARE_FRAMING_IS_WITHDRAWN__my_scan_read_FIELD_NAMES__2026_08_31):** 🔴 THE PREMISE OF THIS QUESTION WAS WRONG, AND IT IS THE FAILURE THIS LOOP WARNS ABOUT BY NAME: 'a census returning a shocking number is usually measuring itself — a key-name scan called 82 rows unproven where 7 was true.' I split the 25 rows by whether they carried a `commit`, `test`, `evidence`, `fix`, `chokepoint`, `falsifier` or `what_shipped` FIELD, called the other 23 'bare', and told you they 'carry no commit, no test and no evidence field, so there is nothing on them to re-verify'. THE OLD ROW FORMAT PUTS ALL OF IT IN `note`. Re-measured by CONTENT across the same 23 rows: carry a fix marker in their text 21 of 23 mention guards / CODE 19 mention a LIVE A/B or live measurement 12 say a falsifier was proven RED 2 quote a commit sha 2 T1-D1 — the row I opened as a 'worked example of the expensive case' — turned out to carry a COMPLETE fix record in its note: the chokepoint (`_missing_run_message`), 8 guards, falsifiers proven RED by injecting each original, a control of its own that went red and was rewritten to the invariant rather than to its wording, and a live A/B on the deployed image with the before/after strings and what the model did next. It verified in one cycle and is now `fixed`. AND THE TWO ROWS WITH NO MARKER ARE NOT COUNTEREXAMPLES: T7-D3 I had already read in code — tool_discovery.py:1467 returns the named 'unknown category' refusal and test_tool_list_names_a_bad_category_guess guards it. WHAT THIS DOES TO THE QUESTION. The disposition is no longer a judgement call about unverifiable rows; it is ordinary verification work of the kind this loop does eve…

**My recommendation:** (b) VERIFY-AND-MARK, one row per cycle, no bulk edit. Each row gets read against today's code; if the defect is gone AND a guard exists it becomes `fixed` citing both; if it is gone with no guard, that is a missing-falsifier finding and the guard is written; if it still reproduces it becomes `open` with a class. Slow and it is the only option that cannot manufacture credit. WHAT I REJECT AND WHY. (a) bulk-mark all 23 `fixed` — forbidden outright: 'never write fixed when the live run exercised only part of the fix', and here there would be no run at all. (c) bulk-`withdrawn` — worse, it says they were never defects when three demonstrably were. (d) declare `proven` terminal for defects — the…

### DQ-T84 — releases 0 defects

**Asked:** How is a defect closed when its live trigger is a MODEL ERROR that cannot be commanded? T1-D2 fires only when the model emits a book_id differing from the studio's book. Everything around it is proven; the trigger is not reproducible on demand.

**My recommendation:** (a), scoped narrowly: acceptable ONLY when the guard asserts the CALL SITE (not just the helper), the falsifier is proven red by injection, the deployed image is verified by content, and the surrounding path has a live run — all four are true here. I would NOT take (b): the affordance would be used far beyond this row, and its failure mode is a scenario that proves whatever it was built to prove. If (a) is accepted, T1-D2 closes on the evidence above and the rule applies to any later row whose trigger is a model error.

