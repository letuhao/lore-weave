# Open decisions — the tool-loop is waiting on these

*Generated 2026-09-01 by `scripts/toolloop/dq_digest.py`. Derived from the ledger at emit time — do not hand-edit; re-run it.*

**6 defects are open and 5 of these questions are what hold them.** The loop's own check reports every open defect as decision-blocked, so nothing else moves until some of these are ruled on.

A ruling goes on the question's row as an `answer_<date>` field. The loop reads it there and builds it **as worded** — if it cannot be built, the question comes back with the measurement showing why, rather than a substituted mechanism.

## ⚠ Read these first — their premise was refuted

Each of these carries a ruling or a premise that this loop measured and found wrong. Ruling on the question as originally worded would spend a decision on something that is not true.

- **DQ-T72** — When R1 answerability returns EXACTLY ONE tool for a request and that tool is on the wire, and the model uses a different one anyway, should the plat…
  - *what changed:* 🔴 (b) CANNOT BE BUILT AS WORDED ON THIS PLATFORM'S TARGET PROVIDER. "Set tool_choice to it" is not expressible: LM Studio's /v1/responses accepts ONLY the three string forms. PROBED DIRECTLY, one request per shape, with a control that must pass first: omitted…
- **DQ-T76** — The fabricated-id guard tests SYNTAX, so a well-formed invented UUID reaches the tool. FIVE mechanisms have now been measured and every one fails on…
  - *what changed:* 🔴 (c) CANNOT BE BUILT AS WORDED, AND THE MEASUREMENT SAYS WHY. The ruling rests on one premise: "'exclude by identity' is a bounded list and not an open-ended one", supported by "TWENTY-ONE sentinel-shaped ids". Re-derived, that figure counts the wrong popula…

## All open questions, by how many defects they release

### DQ-T91 — releases 2 defects

**Asked:** A chat turn dies because the provider drops the model between two passes of the SAME turn. The request is provably fine. What should the platform do about a provider error that produced NOTHING — retry it, prevent the eviction, or keep telling the author the turn failed?

**⚠ Correction on record (option_a_is_SIZED__2026_09_01):** OPTION (a) IS SIZED, using the terminal line shipped earlier today — which is the first instrument on this platform that can express the bar at all, because `status` plus `usage` is exactly "a provider error that produced NEITHER an output delta NOR a usage chunk". Over 2,088 terminal lines from the running provider-registry: 1,268 success usage=true no output (a tool-call turn: the model emitted calls, not prose) 749 success usage=true output 50 success usage=false no output 21 provider_error usage=false no output <- THE PROPOSED RETRY BAR SO (a) WOULD FIRE ON 21 OF 2,088 STREAMS — 1.0%. Each is provably a stream that generated nothing and billed nothing, so a retry cannot duplicate work, double-spend, or replay half a reply to the author. That is the property the narrow bar was chosen for, and it is now a measurement rather than an argument. 🔴 AND THE 50 ROWS ABOVE IT ARE A DIFFERENT POPULATION THE BAR CORRECTLY MISSES. `success / usage=false / no output` is the shape recorded today as a limitation of the terminal line: a stream chat-service abandoned (the identical-repeat cut) still reports success, because breaking out of the stream does not reach provider-registry as an error. The retry bar keys on `provider_error`, so it does not touch them — which is right, since retrying a stream the platform itself cut would restart the loop the cut exists to stop. Stated because 50 is more than twice 21, and anyone reading only the small number would have the ratio backwards. WHAT IS STILL NOT MEASURED: whether a retry would SUCCEED. That needs the retry built and the reproducer…

**My recommendation:** (a) AND (b), and they are not alternatives: (b) makes it stop happening on this machine, (a) makes it survivable anywhere. (a) is the one I would build, and its bar is narrow on purpose: retry ONLY when the stream produced no output delta and no usage chunk. That is provably not a partial answer, so a retry cannot double-write, double-bill, or replay half a reply to the author. Anything broader — retrying a stream that emitted tokens — I would not build without a separate ruling. (c) is the cleanest engineering answer and the most disruptive; it is worth having as a position on whether one provider should serve both roles, but it is not this row's to settle. (d) I would argue against. The a…

**Why it is yours, not mine:** Every option changes what happens on EVERY provider error, or changes the machine's configuration. A retry is a behaviour change on a path that currently fails closed; pinning or pre-loading models is host configuration on the owner's machine; and doing nothing is a decision too, since the measured cost is a dead turn for the author. None of these is a bug fix I can take on my own authority.

**Blocks:** `D-THE-STALL-CONCENTRATES-ON-COMPOSITION-MOTIF-SEARCH`, `D-UPSTREAM-ERROR-WITH-NO-MESSAGE`

### DQ-T72 — releases 1 defect

**Asked:** When R1 answerability returns EXACTLY ONE tool for a request and that tool is on the wire, and the model uses a different one anyway, should the platform (a) force the choice — set `tool_choice` to that tool for the pass; (b) force it ONLY when the single answer is a Tier-R READ, leaving writes to the model; (c) de-advertise the competing sibling when a more specific tool matches; or (d) neither, and treat selection as the model's to get right?

**⚠ Correction on record (the_premise_verified_2026_08_30):** The decisive case reproduces TODAY (batch c-siblingpick1, K=5): glossary_propose_batch called 0 of 5, glossary_adopt_standards used 4 of 5, while glossary_propose_batch was advertised on 36 of 36 passes and was the ONLY tool answerable_tools returned for the prompt. The surface was correct and the choice was not.

**⚠ Correction on record (option_c_s_PRECISION_MEASUREMENT__and_it_REFUTES_c__2026_08_30):** My recommendation said of option (c): 'it needs its own precision measurement — which sibling, on what evidence — and I have not done it, so I am not recommending it yet.' Done now, and it argues AGAINST (c) rather than for it. Every user turn in the live store re-run through the DEPLOYED `answerable_tools` against the live catalogue, then compared with what the turn actually called: turns scanned 8,650 R1 returned EXACTLY ONE tool 2,996 34.6% the turn called it 1,732 57.8% the turn called something ELSE 893 29.8% the turn called nothing 371 12.4% WHAT IT CALLED INSTEAD, live tools only (a call to a since-removed tool is stale corpus, not a rival — see the instrument note below): 34 jobs_cancel (A) -> jobs_list 31 glossary_book_sync_apply (W) -> glossary_book_sync_available 30 kg_propose_edge (A) -> kg_add_nodes 27 book_steering_list (R) -> kg_project_create 26 glossary_create_evidence (A) -> book_list 26 glossary_create_evidence (A) -> book_read 24 propose_edit (R) -> glossary_search 22 book_steering_list (R) -> glossary_book_ontology_read 22 propose_edit (R) -> book_read 22 jobs_pause (A) -> jobs_list 21 glossary_curation_list (R) -> glossary_search 20 book_steering_list (R) -> glossary_list_system_standards 🔴 MOST OF THE 'COMPETING SIBLINGS' ARE SUPPLIERS, NOT RIVALS. jobs_cancel -> jobs_list and jobs_pause -> jobs_list are the model fetching the id it needs before acting. glossary_book_sync_apply -> glossary_book_sync_available is check-then-apply. glossary_create_evidence -> book_list/book_read and propose_edit -> glossary_search/book_read are the same shape. De-adver…

**⚠ Correction on record (a_10_of_10_instance_fell_out_of_a_MIS_AIMED_batch__2026_08_30):** A batch I launched for a different question — and which was uninformative for that one — produced the strongest instance this question has. Batch c-kgedge-pending, K=10, scenario kg-propose-edge, prompt 'Record that Aldric Vane and Mira Solene know each other.', both entities seeded and asserted present: kg_propose_edge ON THE WIRE 10 of 10 kg_propose_edge CALLED 0 of 10 kg_add_nodes called instead 10 of 10 tables written NONE — only entity_access_log, a read log all 10 carded; reply ends 'Nothing has been saved yet; confirm the card above…' THE SURFACE WAS CORRECT EVERY TIME AND THE CHOICE WAS WRONG EVERY TIME, which is this question's exact subject. It is a much cleaner instance than the founding case (glossary_propose_batch, 0 of 5): here the intended tool is on the wire in ALL ten, the substitute is called in ALL ten, and the author's request — a RELATIONSHIP — produced a pending card for adding NODES. 🔴 AND IT MAY STILL BE THE SUPPLIER SHAPE, WHICH IS WHY IT IS NOT YET DECISIVE. kg_add_nodes could be a legitimate precursor: the glossary entities are seeded but their KG nodes may not exist, so 'add the nodes, then the edge' is a defensible order. The turn stopped at the card, so nothing shows whether the model would have gone on to propose the edge once the nodes landed. That is exactly the ambiguity my option-(c) measurement already flagged — jobs_cancel -> jobs_list and glossary_book_sync_apply -> _available have the same shape — and it is the reason I recommended against de-advertising the 'sibling'. AN APPROVED ARM IS RUNNING to settle it: --approvals allow-under-t…

**⚠ Correction on record (TWO_failed_arms_before_the_question_could_be_asked__2026_08_30):** 🔴 TWO ATTEMPTS FAILED BEFORE THIS QUESTION COULD EVEN BE PUT TO A RUN. Both are recorded rather than retried quietly, because each failed for a reason that will catch the next person the same way. c-kgedge-pending (K=10, --approvals none) Produced the 10-of-10 instance above, but every run stopped at a card, so it cannot say whether the model would have proposed the edge once the nodes landed. Not a failure of the batch — a limit of it, and the limit is the whole question. c-kgedge-approved (K=5, --approvals allow-under-test) ← FAILED Meant to let the write land. It did not: 5 of 5 still carded, nothing written but the read log. `allow-under-test` grants `allow` to exactly the batch's TOOLS_UNDER_TEST, and the scenario declares kg_propose_edge — the tool the model never calls. The approval landed on a tool that was never invoked while the one that was invoked stayed gated. THE LESSON IS GENERAL AND CHEAP TO MISS: `allow-under-test` is keyed on the DECLARED tool, so on any scenario where the model substitutes, the flag silently does nothing. Every substitution scenario — which is to say every scenario this question is about — has this trap built into it. A third arm (scripts/toolloop/scenarios-c-kgedge-nodes.json) declares kg_add_nodes as the tool under test so ITS write can land, which is the precondition for observing whether the edge follows. Running now.

**⚠ Correction on record (THE_10_of_10_INSTANCE_IS_WITHDRAWN__it_was_a_CARD_ending_the_turn__2026_08_30):** 🔴 WITHDRAWING THE INSTANCE I CALLED 'THE STRONGEST THIS QUESTION HAS' TWO HOURS AGO. The arm built to refute it did. c-kgedge-nodes, K=5, --approvals allow-under-test on kg_add_nodes (the tool the model actually chooses), same prompt, same seed: kg_add_nodes called 5 of 5 kg_propose_edge CALLED 5 of 5 <- the tool I said was 'never chosen' glossary_search 3 of 5 SO IT IS SUPPLIER-THEN-ACT, NOT A SUBSTITUTION. In c-kgedge-pending the model called kg_add_nodes, that call CARDED, and the turn ended there — so kg_propose_edge was never reached. I read '0 of 10 called, on the wire 10 of 10' as the model declining a tool it could see. It was not declining anything; it never got a further pass. Remove the card from the precursor and the edge follows every single time. WHAT MISLED ME: 'on the wire and not called' looks like a choice, and here it was a TERMINATION. A Tier-A card ends the turn, so any tool the model would have reached AFTER the carded one is indistinguishable — from the outside — from a tool it rejected. That is a measurement trap in every scenario where the first call cards, and this question is about exactly those scenarios. 🔴 IT STRENGTHENS MY RECOMMENDATION AGAINST OPTION (c), materially. De-advertising the 'competing sibling' would, on this turn shape, have removed kg_add_nodes — the tool the model needs FIRST — and the edge would then have had nothing to attach to. The option-(c) measurement already found most 'competitors' are suppliers; this is the same finding proven by construction rather than inferred from a pair list. A LEAD, NOT A CLAIM, AND IT REACHES T…

**⚠ Correction on record (a_THIRD_of_carded_turns_cannot_tell_rejected_from_unreached__2026_08_30):** 🔴 A MEASUREMENT-VALIDITY CAVEAT THAT REACHES EVERY SELECTION CLAIM DRAWN FROM A CARDED TURN, sized after the kg-propose-edge instance was withdrawn today. A Tier-A card ENDS the turn. So on a carded turn that made only one call, a tool that is 'advertised and not called' may have been rejected by the model — or may simply never have been reached, because the turn stopped at the card before a second pass. From the outside those are identical. carded turns in the whole corpus 667 ... that made ONE tool call or none 221 33% ONE IN THREE CARDED TURNS IS IN THE AMBIGUOUS SHAPE. That is not a claim that a third of selection findings are wrong — many carded turns make several calls, and many selection findings do not come from carded turns at all. It is a bound on how much of the evidence CANNOT distinguish the two readings without an extra arm. THE TEST THAT SEPARATES THEM is cheap and now demonstrated twice: declare the tool the model ACTUALLY calls as `tool_under_test`, run with --approvals allow-under-test so its write lands and the turn continues, and see whether the intended tool follows. On kg-propose-edge it followed 5 of 5, converting 'never chosen' into 'never reached'. 🔴 AND `allow-under-test` HAS ITS OWN TRAP, which cost an arm today: it grants approval to the DECLARED tool_under_test. On a substitution scenario the declared tool is by definition the one the model does NOT call, so the flag silently grants nothing and the card still stands. The arm must name the chosen tool, not the intended one.

**⚠ Correction on record (the_FOUNDING_instance_SURVIVES_the_card_test__2026_08_30):** 🔴 THE FOUNDING INSTANCE SURVIVES THE TEST THAT DISSOLVED THE OTHER ONE. Having withdrawn the kg-propose-edge instance as a card ending the turn, I aimed the identical construction at this question's own founding case — the strongest available attempt to refute it. c-siblingpick-adopt, K=5, --approvals allow-under-test on glossary_adopt_standards (the tool the model actually chooses), prompt and seed unchanged: glossary_adopt_standards called 3 of 5 ... and it WROTE 3 of 5 (book_kinds, book_genres, book_attributes, book_kind_genres, book_active_genres all moved) carded 0 of 5 — nothing was gated glossary_propose_batch called 0 of 5 SO THE CARD EXPLANATION DOES NOT APPLY HERE. The precursor ran, its write landed, no card stood in the way, the turn continued — and the tool the prompt asks for was still never called. On kg-propose-edge the same construction produced 5 of 5; here it produces 0 of 5. The two instances have DIFFERENT causes, and only one of them was an artefact. THE CLEANEST SUB-POPULATION IS 0 OF 3: the three runs where adopt_standards actually ran and wrote. The other two called neither tool (glossary_plan, glossary_task_provide_input, glossary_list_system_standards) and say nothing either way. WHAT THIS DOES FOR THE QUESTION: its central instance is now tested against the strongest alternative explanation this loop has found — and holds. 'Surfacing is necessary but not sufficient' is supported here by a turn that had the tool on the wire, had no card in its way, completed its precursor, and chose otherwise. NOT CLAIMED: that every recorded substitution is real…

**⚠ Correction on record (option_b_is_SIZED_against_option_a__measured_2026_08_31):** OPTION (b) IS SIZED AGAINST OPTION (a) — the trade the question turns on, measured rather than argued. Option (c) is already refuted by its own precision measurement, so the live choice is 'force the single answer always' against 'force it only when that answer is a Tier-R READ'. POPULATION, stated because it is NOT the 2,996 this question quotes: the 2,197 batch runs on disk, of which 826 have a singleton answerable set computed with the SHIPPED matcher against the live catalogue. answerable returned EXACTLY ONE tool 826 runs the turn CALLED it 502 61% called something ELSE 291 35% called nothing 33 4% (The 61% agrees with this question's own 57.8% on the larger corpus, which is the check that the two populations behave alike even though they are not the same set.) THE TIER OF THE SINGLE ANSWER, on the 291 turns that went elsewhere: Tier A 152 52% Tier W 74 25% Tier R 65 22% SO THE TRADE IS: (b) reaches ONE IN FIVE of the cases — 65 of 291 — and every one it touches is a read, so the worst outcome of forcing is a redundant lookup. (a) reaches all 291, and 78% of its firings would compel a WRITE the model had declined to make: 152 Tier-A calls that raise a confirm card, plus 74 Tier-W. WHAT THIS DOES NOT DECIDE: whether the 226 write cases are the model being WRONG. A turn that declines the single matching write may be right to — the author's phrasing matched a tool, which is not the same as the author asking for it to fire. That is precisely why the question separates (a) from (b), and the split is now 22/78 rather than an intuition. MY RECOMMENDATION, unchanged in direct…

**⚠ Correction on record (owner_direction_2026_08_31):** OWNER DIRECTION, 2026-08-31, verbatim: "c-> need investigate more about workflow, description and user and agent communicate". RULES OUT, for now: picking any of the override levers - forcing tool_choice, de-advertising a sibling, or loosening _answer_norm. None is to be chosen on today's evidence. ASKS FOR: an investigation of the three things that decide whether the right tool is reachable at all - the WORKFLOWS the platform teaches, the DESCRIPTIONS tools advertise themselves with, and the USER-AGENT exchange (what the author said, what the turn could see of it). The row stays open and blocking; the investigation is the next unit of work, not a substitute for the decision.

**⚠ Correction on record (answer_2026_08_31):** OWNER 2026-08-31: (b) FORCE ONLY A TIER-R READ. When R1 returns exactly one tool, that tool is on the wire, and it is a READ, set tool_choice to it; leave writes to the model. Taken KNOWING IT DOES NOT FIX THIS ROW'S OWN CASE -- `glossary_propose_batch` is Tier W, so a read-only forced choice never reaches the decisive instance. (a) would fix the row and forces a delete on 41 measured messages, which is not a trade worth making.

**⚠ Correction on record (returned_corrected_2026_09_01):** 🔴 (b) CANNOT BE BUILT AS WORDED ON THIS PLATFORM'S TARGET PROVIDER. "Set tool_choice to it" is not expressible: LM Studio's /v1/responses accepts ONLY the three string forms. PROBED DIRECTLY, one request per shape, with a control that must pass first: omitted (control) ACCEPTED, called memory_timeline 'auto' ACCEPTED 'required' ACCEPTED {"type":"function","name":"..."} HTTP 400 {"type":"function","function":{"name":...}} HTTP 400 error: "Expected 'none' | 'auto' | 'required', received object", param tool_choice AND IT WAS BUILT AND CAUGHT LIVE, WHICH IS THE ONLY REASON I KNOW. The rule was implemented at the request site, 7 guards green, whole chat-service suite green at 3,844, deployed and verified by content. The LIVE arm then failed 5 of 5 with that 400 — every singleton-Tier-R turn on this provider would have died. A source-level guard cannot see a provider's accepted vocabulary, and no test in the suite could have. REVERTED AND CONFIRMED: the forcing is gone from the running build (verified by content), and the same scenario is 3 of 3 clean with 0 errors against 5 of 5 broken. NOT SUBSTITUTED. `'required'` IS accepted and would force SOME tool; forcing THAT tool with it needs the advertised set reduced to one, which is option (c) — a mechanism you did not choose, so it is offered below rather than shipped.

**⚠ Correction on record (option_c_MEASURED__and_it_has_no_clean_target__2026_09_01):** THE MEASUREMENT I SAID I HAD NOT TAKEN. My note on (c) read: "it needs its own precision measurement — which sibling, on what evidence — and I have not done it, so I am not recommending it yet." Taken now, since (c) became the nearest expressible option once the provider refused (b). THE POPULATION: 380 recorded runs where R1's answer was a SINGLETON and the turn called something else FIRST — i.e. every run (c) would act on. distinct tools that 'won' 26 distinct (single answer, winner) PAIRS 38 top 3 winners cover 138 of 380 (36%) So there is no small nameable set. A rule would carry 38 entries and still leave two thirds of the population to a tail. 🔴 AND ITS HIGHEST-VOLUME ENTRIES WOULD DE-ADVERTISE THE SUPPLIER. Checked against the contract registry's own `argument_emitters`, 4 of the top 10 pairs name a tool that EMITS the single answer's required id: jobs_list emits jobs_cancel.job_id 35 runs glossary_search emits glossary_create_evidence.entity_id 32 jobs_list emits jobs_pause.job_id 32 kg_add_nodes emits kg_propose_edge.source_entity_id 29 R1 FORCE-ADVERTISES THOSE SUPPLIERS ON PURPOSE — 'a tool whose required id has a declared emitter is unusable without it'. So (c) as stated would remove the tool the platform deliberately put on the wire to make the single answer callable, and the model calling it FIRST is the chain working rather than a mis-selection. THE OTHER SIX ARE NOT DECLARED SUPPLIERS BUT READ THE SAME WAY: book_read before propose_edit, glossary_book_sync_available before _apply, kg_triage_list before kg_triage_schema_write, workflow_list before registry_l…

**My recommendation:** (b), and I am recommending it KNOWING IT DOES NOT FIX THIS ROW'S OWN CASE — which is the thing to weigh. `glossary_propose_batch` is Tier W, so a read-only forced choice never reaches the decisive instance. (b) buys safety and a real 12.7% of turns where the platform stops second-guessing an unambiguous read; it does not buy this row. (a) would fix the row and forces a delete on 41 measured messages. I do not recommend trading a selection defect for that. (c) is the one I would look at NEXT if (b) proves out: de-advertising the sibling is narrower than forcing, and this loop already ships a de-advertise for legacy tools. But it needs its own precision measurement — which sibling, on what ev…

**Why it is yours, not mine:** (a) removes the model's choice on nearly a quarter of turns, including destructive ones. (c) takes a capability off the wire on a guess about which tool the author meant. Both change what the product does with an ambiguous request, and (d) accepts a measured, reproducing failure. None is a correctness call.

**Blocks:** `D-SURFACING-IS-NECESSARY-BUT-NOT-SUFFICIENT-THE-MODEL-PICKS-THE-SIBLING`

### DQ-T76 — releases 1 defect

**Asked:** The fabricated-id guard tests SYNTAX, so a well-formed invented UUID reaches the tool. FIVE mechanisms have now been measured and every one fails on PRECISION. Is this blindness accepted, or is the platform willing to pay one of the measured costs to close it?

**⚠ Correction on record (option_c_s_PREMISE_partially_checked__2026_08_30):** Option (c) — the one I said I would take — rests on a specific claim: the version rule 'had perfect precision over 12,276 real arguments and was defeated by ONE platform-issued value: the RESOLVING sentinel is v0. If that sentinel were minted as a v7 (or excluded by identity rather than by version)…'. Whether (c) is nearly free turns on that sentinel being a PLATFORM-ISSUED id. Checked, and the answer so far points the other way. the literal 00000000-0000-0000-0000-00000000000a appears in exactly TWO files, and BOTH ARE TESTS: services/chat-service/tests/test_a_placeholder_uuid_is_not_an_identifier.py:64 services/glossary-service/internal/api/export_handler_test.go:830 No production code mints it. sentinel-SHAPED ids (00000000-0000-0000-0000-*) in the live stores: books.id 0 chapters.id 0 chapter_entity_links.entity_id 0 of 8,936 glossary_entities.entity_id 0 of 7,575 🔴 THIS IS PARTIAL AND MUST NOT BE READ AS A REFUTATION. The claim it bears on came from a scan of 38,314 real ids; I have checked roughly 16,500 across four columns in two databases. Zero here does not mean zero everywhere, and 'absence in the tables I looked at' is exactly the shape of an absence argument this loop has been wrong about before. WHY IT STILL MATTERS TO THE RULING: if the blocking value is a TEST fixture rather than a platform-issued id, (c) is not merely cheap — it may be unnecessary, because the version rule's one measured false positive would never occur in production at all. That would move (c) from 'the option I would take' to 'the option that may already be true', and it would change what…

**⚠ Correction on record (option_c_s_PREMISE_HOLDS__and_it_CORRECTS_my_note_from_an_hour_ago__2026_08_30):** 🔴 WITHDRAWING MY OWN FINDING FROM EARLIER TODAY. I checked four id columns, found zero sentinel-shaped values, and wrote that option (c)'s premise 'points the other way' — that the RESOLVING sentinel might be a TEST fixture. I flagged in that same note that 'absence in the tables I looked at' is exactly the shape of absence argument this loop has been wrong about before, and then I was wrong about it in that shape. THE FULL SCAN, over EVERY uuid id-column in five databases (497 columns): database distinct ids with <= 2 distinct hex digits glossary 1,210,381 18 chat 41,695 3 That is 21, against the shipped docstring's 'its 20' — the original figure is CONFIRMED, not refuted. My first pass over occurrences rather than distinct values said 4,130, which is the same handful of ids counted once per foreign key. AND THE SENTINEL IS THERE, AT VERSION 0: 00000000-0000-0000-0000-000000000001 … 00000000-0000-0000-0000-00000000000d (13, v0) 00000000-0000-0000-0000-00000000bbbb · …000000033003 (v0) 00000000-0000-0000-0001-000000000001 · …0002-…0002 · …0003-…0003 (v0) ---- and in chat, the fabrications the guard DOES catch ---- 11111111-… · 22222222-… · 33333333-… (v1, v2, v3) SO OPTION (c)'s PREMISE HOLDS: `00000000-0000-0000-0000-00000000000a` is a real, resolving id in the live glossary store, and it is version 0. 🔴 BUT IT IS A FAMILY OF EIGHTEEN, NOT ONE VALUE, and that changes what (c) costs. The question says 'if that sentinel were minted as a v7 (or excluded by identity rather than by version)'. Re-minting means re-issuing 18 ids and every foreign key that points at them — far mo…

**⚠ Correction on record (per_argument_scoping_MEASURED_and_REFUTED__2026_08_31):** A SIXTH CANDIDATE MEASURED AND REFUTED — PER-ARGUMENT SCOPING, the one this row left as its own open item ('PER-ARGUMENT SCOPING IS UNTESTED. The spread suggests a narrow rule could work; no such rule was built or measured'). MEASURED over every recorded tool call in chat_messages — 6,729 assistant rows across 4,953 sessions — asking, per argument name, how often the UUID sent was PUBLISHED to the model earlier in the same session (sent | published): world_id 161 | 161 100% book_id 7,634 | 6,004 79% winner_id 31 | 31 100% chapter_id 1,448 | 1,278 88% user_model_id 22 | 22 100% entity_id 434 | 345 80% base_version_id 20 | 20 100% job_id 311 | 253 81% id 19 | 19 100% project_id 1,921 | 1,037 54% run_id 185 | 75 41% target_entity_id 88 | 10 11% source_entity_id 19 | 1 5% THE SPREAD IS REAL — 5% to 100% — so the row's intuition was right that it discriminates. A rule refusing an unpublished value ONLY on the five 100% arguments would have 253 of 253 clean: PRECISION 100%, zero false positives on the whole corpus. 🔴 AND ITS RECALL IS ZERO. Every fabricated-looking id ever sent — the whole population, taken as DISTINCT values with at most three distinct hex digits: 00000000-0000-0000-0000-000000000000 n=36 base_version, book_id, chapter_id, entity_id 76666666-7666-7666-7666-766666666666 n=22 book_id, project_id, run_id 77777777-7777-7777-7777-777777777777 n= 2 proposal_id 66666666-6666-6666-6666-666666666666 n= 1 book_id NOT ONE of those 61 values arrived on a 100%-published argument. They land on exactly the high-volume arguments the rule cannot cover — book_id, chapter_id, pro…

**⚠ Correction on record (owner_direction_2026_08_31):** OWNER DIRECTION, 2026-08-31, verbatim: "d->check the tool itself, it it follow standard? guard cannot work because lack of parameters?" RULES OUT, for now: building a seventh guard mechanism. RE-AIMS THE ROW at a hypothesis it had not stated - that a fabricated id is a SUPPLY failure rather than a detection failure, and no guard can fix a value the platform never gave the model. The measurement that tests it already exists in part: per-argument publication rates run from 100% (world_id, winner_id, user_model_id, base_version_id, id) down to 5% (source_entity_id), 11% (target_entity_id), 41% (run_id), 54% (project_id). The direction asks the next question - for each low-publication argument, does a LIVE tool exist that returns that id, and was it on the wire? - plus whether these tools conform to the platform's own tool standard. Row stays open.

**⚠ Correction on record (answer_2026_08_31):** OWNER 2026-08-31: (c) CHANGE THE RESOLVING SENTINEL so the version rule becomes true. Its premise now holds: the full scan of 497 uuid id-columns across five databases found TWENTY-ONE sentinel-shaped ids, confirming the original figure rather than refuting it, so 'exclude by identity' is a bounded list and not an open-ended one. NOT (b): an existence check costs about one refusal in four on non-context ids, which is too much on today's numbers and the row already says so.

**⚠ Correction on record (returned_corrected_2026_09_01):** 🔴 (c) CANNOT BE BUILT AS WORDED, AND THE MEASUREMENT SAYS WHY. The ruling rests on one premise: "'exclude by identity' is a bounded list and not an open-ended one", supported by "TWENTY-ONE sentinel-shaped ids". Re-derived, that figure counts the wrong population. TWENTY-ONE IS THE COUNT OF SENTINEL-SHAPED IDS. THE VERSION RULE REFUSES EVERY NON-v7/v5 UUID, which is a different and vastly larger set. Scanned across all five main stores, every uuid column named `id` or `*_id`: DISTINCT non-v7/v5 ids STORED 1,286,858 An exemption list by identity would need to hold those, not 21. THE RIGHT DENOMINATOR IS ARGUMENT-REACHABLE IDS, AND THIS ROW HOLDS TWO NOTES THAT DISAGREE ABOUT IT. One says the store count is 'the wrong denominator, since what the guard inspects is a tool ARGUMENT'; the other uses the store count to call the refutation 'massively strengthened'. Both cannot be right, so the measurement neither had was made: for every `*_id`/`*_ref` argument name any LIVE tool declares, what version are the ids in the columns that back it? 62,434 v4 + 907 v0 glossary.book_genres.book_id 1,552 v4 chat.chat_suspended_runs.run_id 251 v4 glossary.glossary_entities.book_id 14 v4 glossary.entity_lifecycle_ledger.entity_id 13 v4 composition.entity_override.target_entity_id 5 v4 chat.chat_sessions/chat_messages.model_ref 4 v4 book.world_maps.world_id 3 v3 chat.chat_sessions/chat_messages.session_id 2 v4 book.chapters.structure_node_id EVERY ONE OF THOSE ARGUMENT NAMES IS DECLARED BY LIVE TOOLS — world_id by 8, model_ref by 19, target_entity_id by 3, run_id by 26. So the rule would refus…

**My recommendation:** NOT MY CALL. Three options, with what each costs, and I would take the third. (a) ACCEPT THE BLINDNESS. The tool's own 'not found or not accessible' is already the backstop, and it is honest. The cost is that the author sees a tool failure rather than a refusal that says 'this id was invented' — and the model, told only that a row is missing, invents a better-formatted one. That is the measured behaviour this row was filed on. (b) PAY FOR THE EXISTENCE CHECK. It is the only mechanism that catches a well-formed fabrication in general, and it costs ~1 refusal in 4 on non-context ids. Too expensive on today's numbers, and the row already says so. (c) 🔴 MAKE THE VERSION RULE TRUE BY CHANGING TH…

**Blocks:** `D-FABRICATION-GUARD-IS-BLIND-TO-A-VALID-LOOKING-UUID`

### DQ-T88 — releases 1 defect

**Asked:** A multi-call protocol's instruction to make the NEXT call lives in a tool RESULT, and a tool result does not survive to the turn where that call is due. Should the platform carry it across the turn boundary, and how?

**⚠ Correction on record (the_cost_option_a_said_MUST_be_measured__2026_09_01):** Option (a) says replaying tool results has "a real context cost that must be measured, not assumed". Measured, so the choice is not made on an adjective. WHAT (a) WOULD ADD, per session, over 5,182 sessions that made a tool call: average tool_calls bytes 10,346 median 3,270 p95 38,369 MAX 890,805 (average PROSE content per session, for scale: 1,759) So the replay is roughly SIX TIMES the size of everything the author and assistant actually said, and the tail is nearly a megabyte in one session. A trimmed projection is not a refinement of (a), it is the only affordable form of it. WHAT (b) WOULD ADD, over 12,310 successful calls: calls carrying a `next` field 80 (0.65%) tools that emit one 1 composition_build_cast_and_graph average size of the `next` string 66 bytes average size of a whole call 3,884 bytes 🔴 SO (b) IS ~150x CHEAPER PER CALL AND TOUCHES 0.65% OF THEM — and the note I wrote when I recommended it ("it makes one tool's invention into a mechanism") is literally true: `next` is emitted by EXACTLY ONE tool. That cuts both ways and both belong in front of you. It is the cheapest possible change; it is also a platform contract built for a single caller, and nothing here says a second tool would adopt it. AND THE DEFECT (a) WOULD FIX IS MUCH LARGER THAN THE PROTOCOL THAT RAISED IT: sessions 5,655 multi-turn (more than one user turn) 1,702 multi-turn AND made a tool call 1,503 (27% of all sessions) Every one of those 1,503 loses its earlier tool results at the next turn, whether or not any tool declared `next`. NOTHING IS DECIDED HERE and no option is withdrawn. This…

**⚠ Correction on record (the_turn_boundary_may_not_be_the_whole_cause__measured_2026_09_01):** 🔴 A CORRECTION TO THIS QUESTION'S OWN PREMISE, measured rather than argued. This question rests on: the instruction and the run_id live in a tool result, and the result does not survive to the turn where the next call is due. Both halves were made available IN-TURN without any platform change — `op=status` now returns the worklist with its `next` sentence, and the ACTIVE_RUN refusal names the run_id and points at `status`. MEASURED (t64-protocol5, K=5, chat-service restarted so the arm is valid): runs reaching op=status 3 of 5 (was 1 of 5) status calls 9 (was 3) each received the worklist, the run_id and 'call op=approve_plan with this run_id' runs that then called approve_plan 0 casts created 0 So a model holding the worklist, the id and the instruction in one turn still does not take the next step. Options (a) REPLAY RESULTS and (b) PROMOTE `next` both aim at getting that information across the boundary; this suggests the information reaching the model is not the binding constraint, or not the only one. IT DOES NOT REFUTE THE QUESTION. Three of five runs still never reach status at all, and for them the boundary loss is exactly the problem. What it says is that fixing the boundary would move 3 of 5 to where the other 3 already are, and those 3 did not finish — so the expected gain from (a) or (b) is smaller than it looks, and (c) THE RAIL STEP-RUNNER, which drives the next step rather than merely informing it, is worth more weight than I gave it.

**⚠ Correction on record (the_in_turn_evidence_is_WEAKER_than_I_said__2026_09_01):** 🔴 CORRECTING MY OWN NOTE ON THIS QUESTION, added earlier today. It reported 'runs reaching op=status 3 of 5 (was 1 of 5)' and concluded that a model holding the worklist, the id and the instruction in one turn still does not act — which weakened options (a) and (b). A THIRD ARM CAME BACK AT 1 OF 5, and the three arms (1, 3, 1) are not distinguishable: Fisher two-tailed p = 0.52 for the best pair. So the number of runs that reach the recovered state is not established, and the inference built on it is weaker than I presented. WHAT STILL STANDS, because it does not depend on the rate: on every run that DID reach op=status — four across the three arms — the model held the worklist, the run_id and an explicit instruction naming the next op, and none called approve_plan. That is a small n and it is the only direct evidence there is about what a model does with the recovered state. SO THE WEIGHT I SHIFTED TOWARD (c) THE RAIL STEP-RUNNER IS STILL ARGUABLE, but it rests on 4 observations rather than on a measured rate difference, and I am marking it as such rather than leaving the stronger claim standing.

**My recommendation:** (b), then measure before considering (a). It is the smallest change that puts the instruction where the model can act on it, it reuses a pattern the platform already ships and trusts, and unlike (a) it does not grow every turn's context for a case that affects a handful of tools. (c) is more principled and much larger — it turns a tool's protocol into a registry artefact — and nothing measured says the rail driver would fare better with an op the model has never once called. I am NOT taking it, because it changes what the runtime sends the model on every turn where a tool declares `next`, which is a platform contract rather than a bug fix. And (d) is a real option I do not recommend but wil…

**Why it is yours, not mine:** Every option changes a cross-cutting runtime behaviour: what history the model receives, or what the runtime does with a result field. The ruling I was given (fix the description) was built exactly as worded and measured twice; this question is the correction going back, with the evidence that forced it.

**Blocks:** `D-A-PIPELINE-TOOL-COMPETES-WITH-ITS-OWN-PARTS`

### DQ-T90 — releases 1 defect

**Asked:** The score-distribution instrument DQ-T67 required now exists and has run. Which lever moves, given the cap owns 83% of the failures but the correct domain misses by a median 0.0266 in a field only 0.1537 wide?

**My recommendation:** (b), and (c) alongside it as bookkeeping. The cap owns the failures but only because the ranking hands it an unrankable list: a median margin of 0.0266 means the cap boundary is falling inside embedding noise, so ANY cap number is arbitrary. Raising the cap treats the symptom and the K-sweep already priced it — a smooth curve with no knee is what buying coverage by brute force looks like. (a) is defensible as a stopgap IF the owner wants the 29.2% reduced today and accepts the context cost; it is cheap and reversible. I would not ship it as the answer. (d) is the most interesting and the least measured: nothing here says whether surfaced tools rank the skills better than the prompt does. It…

**Why it is yours, not mine:** Every option changes what every turn receives — more injected context, different skill texts, or a different ranking signal. DQ-T67 explicitly deferred the lever choice until the instrument existed; it exists now, so this is that choice, with the numbers it was waiting for.

**Blocks:** `D-THE-INTENT-ROUTERS-TOP-K-CAP-CROWDS-OUT-THE-RIGHT-DOMAIN`

### DQ-T2 — releases 0 defects

**Asked:** Should a deprecated tool be required to name a replacement, and what replaces the ones that have none? Options seen, none chosen: (a) superseded_by becomes mandatory for visibility=legacy and the 55 gaps are filled; (b) a retired-with-no-successor tool is legitimate and tool_list's description should stop promising a label; (c) those 55 are not actually deprecated and the visibility tag is wrong.

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better.

**⚠ Correction on record (newly_relevant_2026_08_30):** NOT STALE — this question got SHARPER today. It asks whether a deprecated tool should be required to name a replacement, and what replaces the ones that have none. D-THE-MODEL-ASKS-INSTEAD-OF-RAISING-THE-CARD-IT-HAS shipped a fix that transfers a dropped legacy tool's declared phrasing to its live successor — and measured, against the live 316-tool catalogue, that 31 legacy tools name NO resolvable successor (book_create, book_chapter_publish, and composition_publish, whose chain ends at a legacy tool with no `superseded_by`). For those 31 the vocabulary is simply lost when the tool is dropped, and nothing invents a destination for it. So option (a) — make `superseded_by` mandatory for `visibility=legacy` — now has a measured cost of NOT doing it, which it did not have when the question was raised.

**⚠ Correction on record (the_census_is_RE_DERIVED_and_the_headline_is_STALE_2026_08_30):** The question asks whether 'the 55 gaps are filled'. Re-derived over the live 316-tool catalogue cache — seventeen days after the 2026-08-13 census the question quotes: visibility=legacy 117 unchanged ... with NO superseded_by 31 was 55 TWENTY-FOUR GAPS HAVE BEEN FILLED, AND ALL OF THEM WERE GLOSSARY'S. Compared per service against the question's own breakdown: glossary 28 -> 4 24 filled book 11 -> 11 unchanged kg 11 -> 11 unchanged lore 4 -> 4 unchanged composition 1 -> 1 unchanged So the decision is a 31-tool one, not a 55-tool one, and four of the five services are exactly where they were — the scope shrank in one place and moved nowhere else. 🔴 AND THE TWO NUMBERS IN THIS SAME QUESTION ARE NOT THE SAME SET. `newly_relevant_2026_08_30` says '31 legacy tools name NO resolvable successor' and cites composition_publish as an example of a chain ending at a legacy tool. Those are two different criteria and they do not give the same answer: no superseded_by at all 31 no RESOLVABLE successor (chain) 32 The difference is exactly composition_publish, which DOES name a successor — book_chapter_publish — that is itself legacy with no successor of its own. So it is absent from the first set and present in the second, and the note's figure of 31 is the wrong one for the criterion it describes. Both numbers being 31 in the same document is the sort of coincidence a later reader welds together; they are separated here on purpose. My recommendation — (a), make superseded_by mandatory for visibility=legacy and enforce it with the existing lint — is unchanged, and cheaper than the questio…

**⚠ Correction on record (parked_2026_08_31):** OWNER 2026-08-31: `park=yes`. PARKED, NOT ANSWERED AND NOT WITHDRAWN. No open defect names this question in `blocked_by_dq`, so it gates nothing; it is a standing question kept as a record rather than a queue item. The state stays `open` on purpose -- marking it `answered` would claim a ruling nobody made, and `withdrawn` would claim the question was wrong. What changes is that the digest lists it separately from the questions that hold work.

**⚠ Correction on record (my_recommendation_is_WITHDRAWN_2026_08_31):** 🔴 I RECOMMENDED (a) — make `superseded_by` mandatory for legacy tools and fill the gaps — AND THAT IS EFFORT ON DEAD TOOLS. The owner asked the right question: 'legacy tools are dead, why are we still spending effort on them?' THE CODE SETTLES IT. `stream_service` documents the widening in as many words: CORRECTED 2026-08-25/26: this used to read 'The rule is narrower than "drop every legacy tool": only when the named replacement is present'. The rule was WIDENED on 2026-08-25 — EVERY legacy tool is dropped, WITH OR WITHOUT a replacement. So `superseded_by` does not gate anything: no legacy tool reaches a model whether it carries the label or not. Measured against the live 316-tool catalogue: 117 legacy tools, 31 without a successor (the row's '55' is stale), and the gate withholds 116 of them on every turn regardless — its own log lists `book_create` and `book_audio_generate` among the withheld, both of which carry NO superseded_by. MY CORRECTED RECOMMENDATION: WITHDRAW DQ-T2. Filling the label changes nothing any model ever sees. It stays OPEN because closing a DQ is the owner's call, never mine. ONE REAL RESIDUE, cheap and unrelated to the label: the gate's own INFO line still says 'legacy tool(s) whose replacement is on the same wire', which was true before the 2026-08-25 widening and is not now. A log sentence that describes a narrower rule than the one running is how the next reader inherits the wrong model.

**My recommendation:** RECOMMEND (a) - superseded_by mandatory for visibility=legacy, the 55 gaps filled, enforced by a lint. tool_list's own description PROMISES the label, and 55 of 117 legacy tools do not carry it, so the promise is false for nearly half of them. (b) 'stop promising it' is internally consistent but trades a fixable data gap for a permanent capability loss - the successor is exactly what a model needs when it lands on a legacy tool. The enforcement shape already exists: scripts/lint_superseded_synonyms.py fails the pair at build.

### DQ-T38 — releases 0 defects

**Asked:** Why does the public catalogue list nothing while 44 books are marked public?

**⚠ Correction on record (not_blocking_anything_2026_08_30):** TRIAGED 2026-08-30: NOTHING IS CURRENTLY BLOCKED ON THIS. No OPEN defect row names it in `blocked_by_dq`. It is a standing question, not a gate on today's queue — answer it when convenient, and no defect waits on the answer. 🔴 THAT IS NOT THE SAME AS STALE, and the distinction is why these were not swept. Of the 11 questions in this position, ZERO are moot: each carries evidence and, in most cases, a written recommendation. Four have citing rows that were since fixed, proven or withdrawn — the work moved past the question without anyone closing it — and six were never attached to a defect row at all. Withdrawing them in bulk would have destroyed real analysis to make a counter look better. 🔴 AND THIS ONE IS NOT YET A DECISION AT ALL. Its own recommendation says so: 'NO RECOMMENDATION YET, AND THAT IS THE ANSWER … no cause has been established, so every option would be a guess dressed as a fix. What is owed first is one measurement.' So this is WORK before it is a question, and it should not sit in a decision pack until that measurement has been run.

**⚠ Correction on record (parked_2026_08_31):** OWNER 2026-08-31: `park=yes`. PARKED, NOT ANSWERED AND NOT WITHDRAWN. No open defect names this question in `blocked_by_dq`, so it gates nothing; it is a standing question kept as a record rather than a queue item. The state stays `open` on purpose -- marking it `answered` would claim a ruling nobody made, and `withdrawn` would claim the question was wrong. What changes is that the digest lists it separately from the questions that hold work.

**My recommendation:** NO RECOMMENDATION YET, AND THAT IS THE ANSWER. The question asks WHY the public catalogue lists nothing while 44 books are marked public, and its evidence field is EMPTY - no cause has been established, so every option would be a guess dressed as a fix. What is owed first is one measurement: does the catalogue query return rows for those 44 books, and if not, which predicate excludes them. Recorded as owed rather than answered.

### DQ-T89 — releases 0 defects

**Asked:** composition_generate's model_ref is optional now and resolves from the WORK. On the ~98% of books with no Work-level model, should it fall back to the ACCOUNT default — and through WHICH capability, given there is no composer/prose one — or keep refusing?

**My recommendation:** (c), then (b) if it proves too slow. The author is the only party who can price a prose call, the Work tier is where the answer belongs (it is per-book, which is how authors think about voice), and the mechanism to store it already exists and is merely empty. (a) is the tempting one and I recommend against it: 'the model they picked for chat' is not consent to spend it on prose, and nothing in the store suggests authors intended that mapping. NOT TAKEN: every option spends the author's money differently, which is the definition of a product call rather than a defect.

**Why it is yours, not mine:** DQ-T35 already ruled that this specific sub-decision is the owner's — 'which capability a spending tool falls back to is a product call' — and its ruling answered the optionality question without naming the tier. This is that residue, with the live measurement that made it load-bearing rather than theoretical.

