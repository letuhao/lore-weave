# 04_play_loop — Index

> **Category:** PL — Play Loop (core runtime)
> **Catalog reference:** [`catalog/cat_04_PL_play_loop.md`](../../catalog/cat_04_PL_play_loop.md) (owns `PL-*` stable-ID namespace)
> **Purpose:** The moment-to-moment core gameplay — turn submission, response, session-tick, time-advancement, scene transitions. High-touch with hot-path SDK (being designed by another agent).

**Active:** **PL_007 + PL_007b Item Foundation DRAFT 2026-07-26** — closes audit finding [AUD-F5](../../12_module_coverage_audit.md) (*Item / equipment module absent — STRUCTURAL, V1-blocking*) and the *Inventory 🟡 partial* row. This is the file [EF_001 §Defers-to](../00_entity/EF_001_entity_foundation.md) named (`features/04_play_loop/PL_007_item.md`) and EF_001 §1 opened by flagging as "Gap 1 — PL_005 nợ Item"; it went unwritten for three months while PL_005 shipped four of five InteractionKinds with `// V1 placeholder; V1+ enforces via Item aggregate` in their validation rules. Those placeholders now resolve. New: `ITM-*` namespace · `item_instance` + `actor_equipment` aggregates · `item.*` namespace · Stage 3.5.e validator sub-stage · 4 `Item:*` EVT-T1 sub-types. **DRAFT ready for `/review-impl`.**

_Previously:_ **PL folder closure COMPLETE 2026-04-26** — PL_005 + PL_005b + PL_005c + PL_006 all CANDIDATE-LOCK 2026-04-26 (Phase 3 + closure pass). Folder ready for V1 vertical-slice integration tests. PF-Q4 + MAP-Q3 watchpoints RESOLVED via PL_005 ExamineTarget extension. Stage 3.5 group fully integrated across all 4 files. interaction.* (5 V1) + status.* (3 V1) namespaces enumerated.

---

## Feature list

| ID | Conversational name | Title | Status | File | Commit |
|---|---|---|---|---|---|
| PL_001 | **Continuum** (CON) | Contract layer §1-§10: aggregates + tier table + DP primitives + capabilities + subscribe + patterns + failure UX + cross-service handoff. Boundary-review tightened 2026-04-25 (TurnEvent envelope rule + thin RejectReason + actor-removal hook). | CANDIDATE-LOCK 2026-04-25 (boundary-tightened) | [`PL_001_continuum.md`](PL_001_continuum.md) | b4ea611 → 1364487 → a4f2d26 → tightening pending |
| PL_001b | Continuum lifecycle (CON-L) | Lifecycle layer §11-§20: 5 sequences (normal/sleep/travel/reconnect/rejection) + bootstrap + 16 acceptance criteria. §18 deferrals updated 2026-04-25 with G1/G3/G4/G5 + B2/B3 boundary-review items. | CANDIDATE-LOCK 2026-04-25 (boundary-tightened) | [`PL_001b_continuum_lifecycle.md`](PL_001b_continuum_lifecycle.md) | a4f2d26 → tightening pending |
| PL_002 | **Grammar** (GR) | Command grammar (5 V1 commands: /verbatim /prose /sleep /travel /help) + intent classifier dispatch + tool-call allowlist + per-rule_id Vietnamese reject copy. Resolves MV12-D9. Option C event-model terminology applied (§2.5 by event-model agent). §13 acceptance criteria added 2026-04-25 (10 scenarios). | **CANDIDATE-LOCK** 2026-04-25 | [`PL_002_command_grammar.md`](PL_002_command_grammar.md) | f89aa48 → closure pending |
| ~~PL_003~~ → **NPC_002** | (relocated) | Multi-NPC turn ordering — was originally drafted here as PL_003 Chorus, then relocated to `05_npc_systems/` because catalog NPC-7 ("Multi-NPC conversation turn arbitration") is the correct domain. See [`../05_npc_systems/NPC_002_chorus.md`](../05_npc_systems/NPC_002_chorus.md). | RELOCATED 2026-04-25 | (moved) | d11dea0 (original) → relocate pending |
| PL_005 | **Interaction** (INT) | Core gameplay primitive — 4-role pattern (agent / tool / direct_targets / indirect_targets) + 5 V1 InteractionKinds (Speak / Strike / Give / Examine / Use) + **ExamineTarget enum (Phase 3 — V1 Place via PlaceId; V1+ MapNode at non-cell tier; resolves PF-Q4 + MAP-Q3 watchpoints)** + ProposedOutputs/ActualOutputs split (world-rule WA_001 Lex derives ActualOutputs at validator stage) + Stage 3.5 group integration (entity_affordance/place_structural/map_layout/cell_scene fail-fast before Lex) + downstream cascade hooks via EVT-G Generator framework. Zero new aggregates V1 (deliberate scope discipline; references existing aggregates from PL_001/NPC_001/PCS_001/WA_001/WA_006/PL_002 + Stage 3.5 foundation tier EF/PF/MAP/CSC). 6 acceptance scenarios + 11 deferrals (INT-D1..D11). `interaction.*` V1 namespace = 5 rules (target_unreachable/tool_unavailable/tool_invalid/target_invalid/intent_unsupported). Builds on Continuum + Grammar + Cast + Chorus + Lex + Mortality + foundation tier. | **CANDIDATE-LOCK** 2026-04-26 (Phase 3 + closure pass) | [`PL_005_interaction.md`](PL_005_interaction.md) | e31c9ea → 5a8af78 → closure pass commit |
| PL_005b | Interaction contracts (INT-C) | Contract layer §1-§12: common payload base + per-kind payload schemas (Speak / Strike / Give / Examine / Use with full Rust struct definitions) + OutputDecl aggregate_type taxonomy + **per-kind Stage 0-9 pipeline (§8)** + **§8.1 Stage 3.5 sub-stage applicability matrix** + **§8.2 Stage 4 lex severity** + **§8.3 Stage 7 world-rule actions** + **§9.0 sub-namespace canonical allocation** + per-kind reject rule_ids with Vietnamese copy + 16 expanded acceptance scenarios + 10 deferrals (INT-CON-D1..D10; D9+D10 added Phase 3). ExamineTarget extension via §5.3 (PL_005 §2). | **CANDIDATE-LOCK** 2026-04-26 (Phase 3 + closure pass) | [`PL_005b_interaction_contracts.md`](PL_005b_interaction_contracts.md) | 16fc969 → 26dc37b → closure pass commit |
| PL_005c | Interaction integration (INT-I) | Cross-feature integration layer §1-§11: **§1.1 Stage 3.5 group inserted (4 sub-stages between Stage 3 A6 sanitize and Stage 4 lex_check)** + **§1.2 timing summary updated (target_dead → Stage 3.5.a)** + NPC_002 Chorus consumption flow + PCS_001 mortality side-effect flow with **§3.1 Strike pre-condition note (Stage 3.5.a guards Stage 7)** + NPC_001 opinion drift flow + V1+ Generator triggers (4 butterfly Generators: PoliceCallout / GriefDrift / RumorSeed / WitnessReport) + failure compensation **(§6.1 per-stage namespace allocation)** + replay determinism + V1 minimum implementation scope + 8 Phase 3 deferrals (INT-INT-D1..D8). actor_status (PL_006) post-commit derivation added to §1.1. 27 total deferrals across PL_005/b/c (PL_005 added INT-D10/D11; PL_005b added INT-CON-D9/D10). | **CANDIDATE-LOCK** 2026-04-26 (Phase 3 + closure pass) | [`PL_005c_interaction_integration.md`](PL_005c_interaction_integration.md) | 61d911c → d435fef → closure pass commit |
| PL_007 | **Item Foundation** (ITM) | The **body** behind `EntityId::Item` — the contract EF_001 declared and no feature owned. 9 axioms: **ITM-A1** def/instance split on the System tenancy tier (`Forge:EditItemDef` is the only def-write path) · **ITM-A2 the representation rule** (instanced ⇒ EF_001 entity; fungible ⇒ RES_001 balance; exactly one per thing, **enforced** at bootstrap by ITM-V10 — resolves the two-half-schemas standoff that left the audit's Inventory row partial) · **ITM-A3** items carry `StatModifier`s against DF07's closed `StatSlot` enum, never stat values (PL_007 ships the `EquipmentStats` impl DF7-D1 deferred to it, zero DF07 change) · **ITM-A4** LLM-zero-item-math · **ITM-A5** equipment is a slot assignment, not a location (keeps `LocationKind` closed — no 5th variant) · **ITM-A6** V1 flat inventory (**resolves EF-D3**) · **ITM-A7** item-management as 4 `Item:*` EVT-T1 sub-types, not new InteractionKinds (PL_005's closed-set proof intact; TG-A3 precedent). 2 aggregates (`item_instance` + sparse `actor_equipment`); `ItemClass` 8 V1; `UseEffectDecl` 7 V1 variants all dispatching to aggregates other features already own; `CombatItemProfile { reach, two_handed, strike_kinds }` as the COMB seam; `InstrumentTag` closing a three-way latent break across PROG_001/DF07 (**resolves PROG-D15**). 21 V1 `item.*` rejects + 2 warnings, with **5 checks explicitly delegated** to EF/PF/CSC rather than duplicated. Supplies `lex_tags` — the Stage-4 operand PL_005b §8.2 called CRITICAL and never had. Resolves EF-D3 (decision), **RES-D1 (withdrawal)**, RES-D2/D4 (schema homes), the `ItemId`/`ItemInstanceId` drift (ITM-C1). 13 AC (AC-ITM-1..13) · 13 deferrals · 5 open questions · 7 consistency rules. | **DRAFT** 2026-07-26 | [`PL_007_item.md`](PL_007_item.md) | (this cycle) |
| PL_007c | Item contracts & integration (ITM-C) | **§9–§19, continuing PL_007's numbering** (not restarting at §1 — peer docs already cite `PL_007 §9.1` / `§11` / `§12.8`, and renumbering would silently break them). Holds: §9 validators ITM-V1..V13 + the `item.*` rule_id ledger (**counted per source** — the first draft claimed 13 and actually declared 21 rejects + 2 warnings) + consistency rules ITM-C1..C11 · §10 event-model mapping · §11 manifest extensions · §12 cross-feature integration incl. **§12.13 answering the two questions ABL_001 routed to the PL_007 owner** (ABL-Q9 `UseEffectDecl` ⊂ `EffectOp` merge — *agreed in principle, deferred in execution* to the first CANDIDATE-LOCK closure, with the change-coupling reason stated; and `EquipDecl.grants_ability` — *agreed, V1+*) · §13 sequences · §14 acceptance criteria **AC-ITM-1..23** · §15 deferrals **ITM-D1..D23** · §16 open questions **all resolved** · §19 **review-pass findings ledger: 11 defects found and fixed**, 4 of them HIGH, every one sitting at a seam with another feature. Split out of PL_007 at the review pass when the combined doc hit 1150 lines against the 800-line hard cap. | **DRAFT** 2026-07-26 | [`PL_007c_integration.md`](PL_007c_integration.md) | (this cycle) |
| PL_007b | Inventory & Capacity (INV) | The inventory half. **ITM-A8** one inventory, two stores, no third — `actor_inventory_view` is a **derived union**, never an aggregate (the read-view EF_001 AC-EF-10 asserted but never specified), with `lifecycle_state = Existing` as a *required* filter because EF_001 §3.1 freezes destroyed entities' locations for audit. **ITM-A9** inventory context is **fixed-size** (≤29 lines for any inventory of any size) — discharges EF_001 §3.1's one-sentence AssemblePrompt-summarization promise and applies the fix CSC_001 measured at 31K→2.5K tokens; elision is **truthful** so the narrator can't assert "he had nothing else". Defines what a capacity slot counts (activating EF_001 Q6b + RES-D2), keeps V1's uncapped posture while naming the new risk, and **decides the boundary behaviour** rather than leaving it to implementation: single transfers hard-reject, bulk drains partial-fill with the remainder left at source. Ground items + a **soft** cell cap whose asymmetry is the point (proposals reject, cascades only warn — a cascade is a consequence and must never fail). 11 transfer flows mapped to store + gate + capacity path; names the RES_001 instanced-trade gap (ITM-D13) and the V2 weight prerequisite (ITM-D14). 10 AC (AC-INV-1..10) · 6 deferrals · 3 open questions. | **DRAFT** 2026-07-26 | [`PL_007b_inventory.md`](PL_007b_inventory.md) | (this cycle) |
| PL_006 | **Status Effects** (STA) | Status foundation — owns `StatusFlag` closed-set enum (V1: Drunk / Exhausted / Wounded / Frightened; V1+ reserved Stunned/Bleeding/Poisoned/Charmed/Encumbered/Buffed/Tired/Hungry/Restrained) + `actor_status` aggregate (T2/Reality, cross-actor PC+NPC; ActorId from EF_001 §5.1) + apply/dispel lifecycle (V1; V1+30d adds auto-expire via Scheduled:StatusExpire Generator). Status applies via PL_005 Interaction OutputDecl (`aggregate_type=actor_status`). Stack policies per flag (Drunk=Sum / Exhausted=ReplaceIfHigher / Wounded=Sum / Frightened=ReplaceIfHigher). **Status apply on dead target → Stage 3.5.a entity.lifecycle_dead** (canonical reject path, NOT status.target_dead — namespace allocated to EF_001). **`status.*` V1 = 3 rules** (unknown_flag/dispel_not_present/invalid_magnitude). 7 V1-testable acceptance scenarios + 8 deferrals (STA-D1..D8). PCS_001 + future NPC_003 reference this enum (no drift). PCS_001 brief §S5 pc_stats_v1_stub.status_flags resolves via actor_status query (read-side projection); writes target actor_status directly. | **CANDIDATE-LOCK** 2026-04-26 (Phase 3 + closure pass) | [`PL_006_status_effects.md`](PL_006_status_effects.md) | 1aca0c1 → closure pass commit |

---

## Kernel touchpoints (shared across PL features)

- `02_storage/SR11_turn_ux_reliability.md` §12AN — TurnState 8-state machine + `turn_outcomes` audit
- `02_storage/S09_prompt_assembly.md` — AssemblePrompt() for every turn; intent classification at turn-input
- `02_storage/R07_concurrency_cross_session.md` — session-as-concurrency-boundary; one command processor per session
- `03_multiverse/` (MV12) — fiction-time model; every turn has fiction_duration; page-turn time advancement
- `05_llm_safety/` — A3 World Oracle for determinism · A5 intent classifier · A6 injection defense
- **Hot-path SDK** (being designed externally) — PL features MUST go through SDK, no direct kernel calls

---

## Naming convention

`PL_<NNN>_<short_name>.md`. Sequence per-category.

## How to add a feature

See root [`../_index.md`](../_index.md) § "How to add a new feature".

---

## Coordination note

~~Play-loop features have highest coupling with hot-path SDK. When SDK design is still in flux...~~ **2026-04-25 update:** the hot-path SDK design landed as the LOCKED DP contract in [`../../06_data_plane/`](../../06_data_plane/) (Phase 1-4 complete: DP-A1..A19 + DP-T0..T3 + DP-R1..R8 + DP-K1..K12 + DP-Ch1..Ch53). PL features now reference DP primitives by name and use [`22_feature_design_quickstart.md`](../../06_data_plane/22_feature_design_quickstart.md) as the authoring template. PL_001 is the first feature to do this and serves as the example for subsequent PL_NNN docs.
