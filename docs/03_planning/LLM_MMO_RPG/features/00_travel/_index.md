# 00_travel — Index

> **Category:** TVL — Travel Mechanics (V1+ foundation-adjacent consumer feature; first feature consuming V1+30d activation triangle POL + SET + ROUTE)
> **Catalog reference:** [`catalog/cat_00_TVL_travel_foundation.md`](../../catalog/cat_00_TVL_travel_foundation.md) (owns `TVL-*` stable-ID namespace)
> **Purpose:** Implements inter-settlement travel via atomic single-segment Route traversal — actor at from_cell issues `Travel:Initiate { route_id }`; system advances TDIL clocks (actor + body + realm; soul preserved unless BodyOrSoul::Soul form) over `route.default_fiction_duration` × per-turn scheduler ticks; actor arrives at to_cell when journey reaches 100%. Composes with SET_001 Settlement.role for hospitality availability at arrival (Hamlet → outdoor camp narration; status effect application via PL_006 Exhausted on overnight no-inn travel); RES_001 food/water consumption per league; AIT_001 Tracked tier discipline (Untracked NPCs excluded). Narrative-only V1+30d (LLM generates journey description via S9 prompt-assembly); mechanical encounter generation deferred V1+30d+. PC + Tracked NPC parity V1+30d.

**Active:** none. _Last released 2026-05-16_ by main session (TVL_005 V1+30d+ Group/Party Travel DRAFT — Phase 0 TVP-D1..D7 LOCKED via user `approve all` directive; lock released; ready for /review-impl adversarial pass).

---

## Feature list

| ID | Conversational name | Title | Status | File | Commit |
|---|---|---|---|---|---|
| TVL_001 | **Travel** (TVL) | V1+ primary consumer of GEO_004 ROUTE_001 V1+30d Route graph. Atomic single-segment travel V1+30d; pure mechanics (encounter generation deferred V1+30d+); on-demand Dijkstra over Route graph at request time; selective TDIL clock advancement (actor + body + realm; soul preserved); narrative-only journey description via S9 prompt-assembly with [TRAVEL_CONTEXT] sub-section; PC + Tracked NPC parity (Untracked excluded per AIT_001 quantum-observation discipline); OnFoot mode V1+30d only (ByBoat V1+30d+ schema-reserved). **NEW aggregate**: `actor_travel_state` (T2/Reality, sparse per-(actor, journey)). **NEW namespace**: `travel.*` (15 V1+30d rule_ids + 2 V2+ reservations). **NEW EVT-T sub-types**: EVT-T1 `Travel:Initiate` (player intent) + EVT-T5 `Scheduled:TravelTick` (per-turn progress) + EVT-T6 `Travel:JourneyNarration` (LLM journey description). **NEW V1+30d service**: `travel-service` (owns actor_travel_state aggregate + apply_delta + per-turn tick generator). **Cross-feature schema dependency**: EF_001 closure pass adds `entity.travel_journey_id: Option<JourneyId>` field (EF_001 schema_version 1 → 2 V1+30d bump per I14). **ROUTE_001 cross-feature gate**: TVL-V14 `route_in_use_by_journey` validator added to ROUTE_001 RemoveRoute pipeline. **Hospitality at arrival**: Settlement.role determines inn-available vs outdoor-camp; if outdoor-camp AND wakeful_duration > 16h → PL_006 StatusFlag::Exhausted applied. **Provisions cost**: food_per_league + water_per_league deducted from RES_001 vital_pool at Travel:Initiate (defaults 1.0/2.0 units/league OnFoot V1+30d). **15 V1+30d-testable acceptance scenarios** AC-TVL-1..15; 12 deferrals TVL-D1..D12 + 7 open questions TVL-Q1..Q7; 15 TVL-V* validators. **Phase 0 TVL-D1..D7 LOCKED via single deep-dive 2026-05-14** with user `continue` directive interpreted as approve all defaults: D1 own `00_travel/` folder / D2 pure mechanics V1+30d / D3 on-demand Dijkstra / D4 selective TDIL clocks / D5 narrative-only / D6 atomic single-segment / D7 PC + Tracked NPC parity. **First feature consuming V1+30d activation triangle** (POL + SET + ROUTE all locked baseline; TVL_001 leverages full geographic substrate). Owns TVL-* sub-prefix in NEW catalog `cat_00_TVL_travel_foundation.md`. | **DRAFT 2026-05-14** | [`TVL_001_travel.md`](TVL_001_travel.md) | (this commit) |
| TVL_002 | **Composite Travel** (CTV) | V1+30d+ convenience layer over TVL_001 atomic travel. Player declares a destination cell; system runs Dijkstra over the ROUTE_001 Route graph, freezes the ordered segment list, and auto-traverses N segments — each segment a plain TVL_001 atomic journey underneath. **NEW aggregate**: `composite_journey` (T2/Reality, sparse per-(actor, composite_journey)). **NEW EVT-T sub-types**: EVT-T1 `CompositeTravel:Initiate` + `CompositeTravel:Cancel` (player intent) + EVT-T8 `Forge:CancelCompositeJourney` (admin). **Cross-feature schema dependency**: `actor_travel_state` gains additive `composite_journey_id: Option<CompositeJourneyId>` field (schema_version bump per I14; TVL_001 closure pass). **Cross-feature gate**: CTV-V15 `actor_in_composite_journey` validator added to TVL_001 `Travel:Initiate` pipeline (manual atomic travel blocked while a composite is Active). **Plan freeze + re-plan fallback**: Dijkstra freezes at initiate; if a future planned segment's route is removed mid-journey, one re-plan from the current cell; re-plan failure / cap-hit / under-provisioned → `Stranded` terminal status. **Smart overnight stops**: at inn-bearing intermediate settlements (Village/Town/City/Capital) auto-rest resets wakeful_duration; no-inn stops apply the TVL_001 Exhausted check. **Whole-journey provisions pre-pay** at `CompositeTravel:Initiate`; proportional refund on cancel/strand. **Read-only `composite_travel_plan` preview query** (path + distance + ETA + provisions + intermediate settlements; mutates nothing). **Self-cancel** takes effect at the next segment boundary; **admin** `Forge:CancelCompositeJourney` is immediate. **15 V1+30d+-testable acceptance scenarios** AC-TVL-16..30; 9 deferrals CTV-D1..D9 + 7 open questions CTV-Q1..Q7; 17 CTV-V* validators; 12 `travel.*` rule_ids (10 player-meaningful + 2 defensive). **Phase 0 CTV-D1..D7 LOCKED via single deep-dive 2026-05-16** with user `approve all` directive: D1 new `composite_journey` aggregate / D2 freeze-at-initiate + re-plan fallback / D3 smart overnight stops / D4 whole-journey provisions pre-pay / D5 destination-cell + reachability check / D6 self-cancel at next segment boundary / D7 read-only preview before commit. **Second TVL feature; first convenience layer** (a feature consuming a feature). No new service — lives in `travel-service`. | **DRAFT 2026-05-16** | [`TVL_002_composite_travel.md`](TVL_002_composite_travel.md) | `0bcc7d7a` |
| TVL_003 | **Mount/Vehicle Travel** (TVM) | V1+30d+ feature letting a journey use a mount/vehicle instead of OnFoot — a horse on the Road, a river boat on a RiverNavigation route, a ship across a SeaLane, a carriage on a Road. Activates the `ByBoat` `TravelMode` variant TVL_001 schema-reserved + adds `OnHorseback` / `ByShip` / `ByCarriage` — a closed-enum additive bump (R3) of TVL_001's `TravelMode` 2 → 5. **NEW aggregate**: `mount` (T2/Reality, sparse per-mount; instanced — owned, located, kinded). **NEW EVT-T sub-type**: EVT-T8 `Forge:GrantMount` (admin grant); mount selection rides TVL_001's `Travel:Initiate` (payload gains `mount_id`). **Cross-feature schema dependency**: TVL_001 `actor_travel_state` gains `mount_id: Option<MountId>` additive field + the `TravelMode` enum bump + the `Travel:Initiate` payload field (schema_version bump per I14; TVL_001 closure pass — now serving THREE consumer features: TVL_002 `composite_journey_id` + TVL_004 `encounter_schedule` + TVL_003 `mount_id`/`TravelMode`). **MountKind 4-variant** (Horse / RiverBoat / Ship / Carriage — each maps 1:1 to a non-OnFoot mode); **MountLocation 2-variant** (AtCell / InTransit); **MountAcquisition 2-variant** (CanonicalSeed / ForgeGrant). **Per-mode speed modifier** on `route.default_fiction_duration` (OnFoot 1.0 / OnHorseback 0.4 / ByCarriage 0.6 / ByBoat 0.5 / ByShip 0.3 — hardcoded V1+30d+). **Mode↔route compatibility matrix** (Horse→Road/Trail · Carriage→Road · Boat→RiverNavigation · Ship→SeaLane — hardcoded). **Acquisition**: canonical declaration + Forge grant V1+30d+ (market purchase deferred TVM-D1). The mount must be at the actor's origin cell; it travels `InTransit` with the actor and is `AtCell(destination)` on arrival. Provisions cost unchanged (distance-based — a mount is faster, not shorter). **RealityManifest extension**: `canonical_mounts: Option<Vec<MountDecl>>`. **Composite-journey travel with a mount is deferred V1+30d+** (TVM-D5) — TVL_002 as shipped rejects every non-OnFoot composite (CTV-V9); it needs a TVL_002 closure pass. V1+30d+ TVL_003 ships mounts for atomic TVL_001 journeys only. **15 V1+30d+-testable acceptance scenarios** AC-TVL-46..60; 9 deferrals TVM-D1..D9 + 7 open questions TVM-Q1..Q7; 10 TVM-V* validators; 10 `travel.*` rule_ids (8 new + 2 reused — supersedes TVL_001's `travel.mount_unavailable` V2+ reservation). **Phase 0 TVM-D1..D7 LOCKED via single deep-dive 2026-05-16** with user `approve all` directive: D1 NEW `mount` aggregate / D2 TravelMode 2→5 additive bump / D3 canonical + Forge acquisition / D4 hardcoded mode↔route matrix / D5 per-mode speed modifier / D6 minimal mount state (location only) / D7 ≤1 mount per journey + PC/Tracked parity. **Third TVL feature by number** (designed after TVL_004) — the conveyance layer; unblocks ROUTE_001 water routes + TVL_002 CTV-D5. No new service — lives in `travel-service`. | **DRAFT 2026-05-16** | [`TVL_003_mount_vehicle_travel.md`](TVL_003_mount_vehicle_travel.md) | (this commit) |
| TVL_004 | **Travel Encounters** (CTE) | V1+30d+ feature generating encounters *during* a journey — bandit ambush on a remote Trail, merchant caravan on a Road, storm on a MountainPass, herb cache by a River. Reads the V1+30d substrate (GEO_001 biome, ROUTE_001 `route.kind`, POL_001 province) and attaches mechanical encounter events to TVL_001's `actor_travel_state`. **NEW aggregate**: `travel_encounter` (T2/Reality, sparse per-(journey, encounter)). **NEW EVT-T sub-types**: EVT-T1 `Encounter:Resolve` (player intent) + EVT-T6 `Encounter:SceneNarration` (LLM scene + outcome proposal) + EVT-T8 `Forge:ResolveEncounter` (admin force-resolve/skip). **Deterministic pre-roll**: the encounter schedule is pre-rolled (Poisson) at `Travel:Initiate` and **pinned** onto a NEW additive `actor_travel_state.encounter_schedule` field (schema_version bump per I14 — HIGH-2 /review-impl fix: the schedule depends on mutable `world_geometry` biome + `route.kind`, so it must be stored, not re-derived). The TVL_001 closure pass for TVL_004 is **schema + behavioral** — schema = the additive field; behavioral = `Scheduled:TravelTick` gains clamp-to-crossed-point encounter detection + a pause that skips progress AND clock advancement. **Journey pause**: an unresolved `travel_encounter` row freezes `progress_fraction` advancement (no `TravelStatus` enum change). **EncounterKind 4-variant** (Combat / Social / Hazard / Discovery); **EncounterApproach 4-variant** (Confront / Avoid / Parley / Investigate — fixed subset per kind); **EncounterStatus 4-variant** (Pending / Resolving / Resolved / Skipped); **ProvinceDangerBand 3-variant** (Safe / Contested / Lawless). **Resolution**: encounter pauses the journey → actor picks a per-kind approach → chat-service LLM narrates the scene + proposes an `EncounterOutcome` → engine clamps to author-declared `OutcomeBounds` (PROG_001 Q7 precedent) → journey resumes; `Combat` encounters resolve via a one-step **combat abstraction** (no turn-by-turn loop — deferred to a future combat feature CTE-D1). **Abstract participants**: the bandit/merchant are described, NOT instantiated as persistent EF_001/AIT entities (V2+ promotion CTE-D3). **RealityManifest extension**: `encounter_tables: Option<Vec<EncounterTableDecl>>` (author-declared per `BiomeKind × RouteKind`; engine default fallback; immutable post-bootstrap). **TVL_002 composite journeys are encounter-agnostic** (CTV-D1 confirmed — a paused segment pauses the composite, no composite-side change). **15 V1+30d+-testable acceptance scenarios** AC-TVL-31..45; 9 deferrals CTE-D1..D9 + 7 open questions CTE-Q1..Q7; 12 CTE-V* validators; 11 `travel.*` rule_ids (10 new + 1 reused). **Phase 0 CTE-D1..D7 LOCKED via single deep-dive 2026-05-16** with user `approve all` directive: D1 NEW `travel_encounter` aggregate + pause-via-unresolved-row / D2 deterministic pre-rolled schedule at Travel:Initiate / D3 EncounterKind 4-variant / D4 author-declared encounter tables + engine default / D5 pause + choice-based LLM-narrated + engine-validated resolution (combat abstracted) / D6 abstract ephemeral participants / D7 PC + Tracked NPC parity. **Fourth TVL feature** — the encounter layer TVL_001 §1 Gap 3 was built toward and TVL_002 CTV-D1 kept composite-agnostic for. No new service — lives in `travel-service`. | **DRAFT 2026-05-16** | [`TVL_004_travel_encounters.md`](TVL_004_travel_encounters.md) | `dda45ef8` |
| TVL_005 | **Group/Party Travel** (TVP) | V1+30d+ feature letting several actors travel a journey together as one unit — a PC + Tracked-NPC companions crossing the realm in a single party. **NEW aggregate**: `travel_party` (T2/Reality, sparse per-party) — owns the member set, the leader, the lifecycle. A party travels on the **leader's** TVL_001 `actor_travel_state` journey: `Party:Travel` creates the leader's journey + binds every member; per-tick progress + `Travel:Arrive` cascade every member's `entity.current_cell_id` (lockstep arrival). Members get **no** individual journey rows. **NEW EVT-T sub-types**: EVT-T1 `Party:Form` / `Party:Join` / `Party:Leave` / `Party:Travel` (player intent) + EVT-T8 `Forge:DisbandParty` (admin). **No cross-feature schema field** — a member's "party-bound" state is derived by querying `travel_party`; the TVL_001 closure pass for TVL_005 is **behavioral only** (the TVP-V11 cross-feature validator blocking a member's solo `Travel:Initiate` + the `Travel:Arrive` member cascade). **PartyStatus 3-variant** (Forming / Traveling / Disbanded); **PartyRole 2-variant** (Leader / Member). **≤6 members** V1+30d+. **OnFoot-only** V1+30d+ — each member pays their **own** TVL_001 distance-based provisions (any member short → the whole `Party:Travel` rejects). **Encounters**: a TVL_004 encounter on the bound journey pauses the **whole party**; the leader resolves it; V1+30d+ the outcome applies to the leader only (party-wide distribution deferred — TVL_004 CTE-D7). **Leadership transfer** on a leader leaving a Forming party (to the earliest-joined member); membership frozen while Traveling. **RealityManifest extension**: `canonical_parties: Option<Vec<PartyDecl>>`. **15 V1+30d+-testable acceptance scenarios** AC-TVL-61..75; 9 deferrals TVP-D1..D9 + 7 open questions TVP-Q1..Q7; 13 TVP-V* validators; 13 `travel.*` rule_ids (12 new + 1 reused). **Phase 0 TVP-D1..D7 LOCKED via single deep-dive 2026-05-16** with user `approve all` directive: D1 NEW `travel_party` aggregate / D2 party travels on the leader's TVL_001 journey, members bound / D3 Party:Form/Join/Leave formation, co-located to join / D4 ≤6 size cap / D5 OnFoot-only V1+30d+ / D6 encounter pauses the party, leader resolves, outcome to leader only / D7 PC + Tracked NPC members, ≤1 party per actor, leader committed mid-journey. **Fifth TVL feature** — the group layer TVL_001 TVL-D5 deferred and TVL_002 CTV-D8 / TVL_003 TVM-D3 / TVL_004 CTE-D7 each declared a dependency on. Composite party travel + mounted party travel stay deferred (TVP-D4/D5). No new service — lives in `travel-service`. | **DRAFT 2026-05-16** | [`TVL_005_group_party_travel.md`](TVL_005_group_party_travel.md) | (this commit) |

---

## Kernel touchpoints

- `06_data_plane/02_invariants.md` — DP-A14 scope/tier annotations (T2/Reality aggregate); DP-Ch reality-channel binding for actor_travel_state
- `06_data_plane/12_channel_primitives.md` — Reality-scoped aggregate scope marker
- `07_event_model/03_event_taxonomy.md` — EVT-T1 Submitted sub-type `Travel:Initiate` + EVT-T5 Scheduled sub-type `Scheduled:TravelTick` + EVT-T6 Proposal sub-type `Travel:JourneyNarration`
- `_boundaries/01_feature_ownership_matrix.md` — `actor_travel_state` aggregate owned by TVL_001 (added 2026-05-14)
- `_boundaries/02_extension_contracts.md` §1 — TurnEvent EVT-T1 sub-types row gains `Travel:Initiate`; EVT-T5 sub-types row gains `Scheduled:TravelTick`; EVT-T6 sub-types row gains `Travel:JourneyNarration`
- `_boundaries/02_extension_contracts.md` §1.4 — NEW `travel.*` RejectReason namespace (15 V1+30d rule_ids + 2 V2+ reservations)

---

## Naming convention

`TVL_<NNN>_<short_name>.md`. Sequence per-category. TVL_001 is the foundation; future TVL_NNN candidates:

- `TVL_002` Composite Multi-Segment Travel (V1+30d+ — Dijkstra over Route graph + auto-traverse N segments)
- `TVL_003` Mount/Vehicle Travel (V1+30d+ — OnHorseback / ByBoat / ByShip / ByCarriage; activates ByBoat mode)
- `TVL_004` Travel Encounters (V1+30d+ — random events / weather / combat during journey; attaches to actor_travel_state)
- `TVL_005` Group/Party Travel (V1+30d+ — multiple actors traversing same Route together; party-formation aggregate)
- `TVL_006` Soul-Projection Travel (V2+ — BodyOrSoul::Soul form astral travel per wuxia/cultivation archetype canon)
- `TVL_007` Off-Camera Background NPC Migration (V2+ — Untracked NPC ambient migration; quantum-observation pattern)

Whether these become separate TVL_NNN files or sub-sections of TVL_001 depends on size at design time.

## How to add a feature

See root [`../_index.md`](../_index.md) § "How to add a new feature".

---

## Coordination notes

TVL_001 V1+30d is the FIRST feature consuming the V1+30d activation triangle (POL + SET + ROUTE). It demonstrates the consumer-feature pattern:
- Geographic substrate features (GEO_001..GEO_004) populate world_geometry layers
- TVL_001 V1+30d consumes those layers (reads routes + settlements + provinces + biome/climate context) and produces per-actor runtime state (actor_travel_state aggregate)
- LLM prompt-assembly extends with `[TRAVEL_CONTEXT]` sub-section grounding journey narration

Future consumer features (encounter generators, strategy gameplay, faction politics) layer atop the V1+30d activation triangle following the same pattern: read locked substrate, produce per-actor or per-faction runtime state, surface via S9 LLM-context grounding.

Cross-feature schema dependency: EF_001 entity_binding gains `travel_journey_id: Option<JourneyId>` field (additive per I14; EF_001 schema_version 1 → 2 bump at TVL_001 V1+30d ship). EF_001 closure pass coordinates the addition; world-service writes the field; travel-service reads it (cross-service-write boundary per DP-A11 single-writer discipline).

NEW V1+30d service: `travel-service`. Owns actor_travel_state aggregate. Cross-service handoff with world-service (reads world_geometry) + chat-service (LLM journey narration) + auth-service (no new capability; standard PC/NPC action authorization).
