# 01 — Feature Ownership Matrix

> **Status:** seed 2026-04-25 — reflects the 11 features designed before this folder existed.
>
> **Lock-gated:** edit only with the `_LOCK.md` claim active.

---

## How to read

For every aggregate, schema, namespace, or validator slot in the LoreWeave LLM-MMO-RPG design, exactly ONE feature owns it. Other features REFERENCE without redefining.

Before designing a new feature: search this matrix for the aggregates / concepts you intend to own. If already owned by another feature, EITHER reference the existing owner OR escalate via boundary-review (claim lock + propose split / transfer).

---

## Aggregate ownership

| Aggregate | Tier × Scope | Owner feature | Notes |
|---|---|---|---|
| `fiction_clock` | T2 / Reality singleton | **PL_001 Continuum** | Per-reality fiction time. |
| `scene_state` | T2 / Channel (cell) | **PL_001 Continuum** | Per-cell ambient state. |
| `participant_presence` | T1 / Channel (cell) | **PL_001 Continuum** | Live "who's here" view; re-derived from DP-emitted MemberJoined/Left. |
| ~~`actor_binding`~~ → `entity_binding` | T2 / Reality | **EF_001 Entity Foundation** (CANDIDATE-LOCK 2026-04-26; transferred from PL_001 2026-04-26) | TRANSFERRED 2026-04-26: was "Where is X covers PCs + NPCs uniformly" under PL_001 §3.6. EF_001 extends scope to all 4 EntityType variants (Pc/Npc/Item/EnvObject) via 4-state `LocationKind` discriminator (InCell / HeldBy / InContainer / Embedded) + 4-state `LifecycleState` (Existing / Suspended / Destroyed / Removed) + per-instance `affordance_overrides`. PL_001 reopen: §3.6 now references EF_001 as owner. §14 acceptance: 10 scenarios AC-EF-1..10 (Phase 3 cleanup + closure-pass precision-tightening). See [EF_001 §3.1](../features/00_entity/EF_001_entity_foundation.md#31-entity_binding-t2--reality--primary). |
| `entity_lifecycle_log` | T2 / Reality (append-only) | **EF_001 Entity Foundation** (CANDIDATE-LOCK 2026-04-26) | Per-entity append-only audit trail of LifecycleState transitions. Causal-ref to triggering EVT-T1 Submitted. Reason kinds: CanonicalSeed / RuntimeSpawn / PcMortalityKill / NpcCold / AdminDecanonize / AutoRestoreOnCellLoad / AdminRestoreFromRemoved / InteractionDestructive / HolderCascade / Unknown (split AdminRestore in Phase 3 cleanup; HolderCascade added with §6.1 cascade rules). V1+ archiving deferred (EF-D10). See [EF_001 §3.2](../features/00_entity/EF_001_entity_foundation.md#32-entity_lifecycle_log-t2--reality-append-only). |
| `place` | T2 / Channel-cell | **PF_001 Place Foundation** (CANDIDATE-LOCK 2026-04-26) | Semantic place identity per cell-tier channel; 1:1 strict V1 (cell-only). Owns 10-variant PlaceType closed enum + 5-variant ConnectionKind (bidirectional hint-only V1) + 4-state StructuralState machine with cascade-only-on-Destroyed rule + 4-step cascade ordering (place delta → PlaceDestroyed signal → consumer cascades → cell-resident cascade) + 11-variant EnvObjectKind + fixture-seed split (author-declared vs materialized; world-service computes seed_uid). RealityManifest extension `places: Vec<PlaceDecl>` REQUIRED V1. Cascades into EF_001 §6.1 on `Pristine/Damaged/Restored → Destroyed` (HolderCascade for embedded EnvObjects + at-cell Items). §15 acceptance: 10 V1 scenarios AC-PF-1..10 (Phase 3 cleanup + closure-pass precision-tightening on AC-PF-7/8/9/10). See [PF_001 §3.1](../features/00_place/PF_001_place_foundation.md#31-place-t2--channel-cell-scope--primary). |
| `map_layout` | T2 / Channel (all tiers) | **MAP_001 Map Foundation** (CANDIDATE-LOCK 2026-04-26) | Visual graph layer per channel; covers all tiers (continent through cell). Owns 5-variant ChannelTier closed enum + author-positioned absolute u32 (0..=1000) per-tier viewport + Option<TierMetadata> conditional (Some non-cell, None cell — PF_001 supplies display_name/canon_ref) + 5-variant MapConnectionKind matching PF_001 + distance_units (canonical abstract leagues; invariant) + default_fiction_duration (OnFoot baseline V1; V1+ TVL_001 method matrix) + 3 image asset slots (icon/background/inline_artwork; V1 schema-only with V1+ MAP_002 phased pipeline) + 4-variant AssetSource + 3-variant AssetReviewState. Composes with PF_001 at cell tier (PF_001 owns place semantic + cell ConnectionDecl; MAP_001 owns cell visual layer + non-cell connections). RealityManifest extension `map_layout: Vec<MapLayoutDecl>` + `travel_defaults: TravelDefaults` REQUIRED V1. §15 acceptance: 11 V1 scenarios AC-MAP-1..11 (Phase 3 cleanup + closure-pass expanded AC-MAP-7 + AC-MAP-9 + new AC-MAP-11). See [MAP_001 §3.1](../features/00_map/MAP_001_map_foundation.md#31-map_layout-t2--channel-scope--primary). |
| `cell_scene_layout` | T2 / Channel-cell | **CSC_001 Cell Scene Composition** (CANDIDATE-LOCK 2026-04-26) | 4-layer composition pipeline turning cell-tier place into renderable in-scene UI. Owns: Layer 1 hand-authored skeleton template registry (3 Tavern + 1 default_generic_room V1) + Layer 2 seeded procedural fixture placer (5 FixtureKinds: Counter/Table/Chair/Fireplace/Window; deterministic per blake3 seed + ChaCha8Rng) + Layer 3 LLM categorical zone-assignment (JSON contract + 4 validators + 3-retry feedback loop + per-entity fallback chain + PC race policy) + Layer 3 canonical default fallback (engine algorithm; always succeeds; no LLM) + Layer 4 LLM creative narration (Vietnamese xianxia free-form; in-memory LRU cache; best-effort replay V1). Each layer's failure mode bounded; cell scene always renders. Architectural axiom CSC-A1: LLM tasks confined to categorical (L3) + creative (L4); spatial coord manipulation handled by deterministic engine (L1+L2). Demo evidence at `_ui_drafts/CELL_SCENE_v1..v4`: v3 LLM-as-grid 31K tokens (failed); v4 LLM-as-zone 2.5K tokens (succeeded) — 12.7× cost reduction. RealityManifest extension `scene_skeleton_overrides: HashMap<ChannelId, SkeletonId>` (optional V1 per-cell override). §16 acceptance: 11 V1 scenarios AC-CSC-1..11 (Phase 3 cleanup expanded + new AC-CSC-11). See [CSC_001 §3.1](../features/00_cell_scene/CSC_001_cell_scene_composition.md#31-cell_scene_layout-t2--channel-cell-scope--primary). |
| `turn_idempotency_log` | T2 / Reality | **PL_001 Continuum** | Reconnect/idempotency safety. *Borderline; may relocate to PL_004 session lifecycle when designed.* |
| `tool_call_allowlist` | T2 / Reality | **PL_002 Grammar** (CANDIDATE-LOCK 2026-04-25) | Per-actor-type LLM tool-call allowlist. §13 acceptance: 10 scenarios. |
| `npc_reaction_priority` | T2 / Channel (cell) | **NPC_002 Chorus** (CANDIDATE-LOCK 2026-04-26) | Per-(cell, NPC) priority hint for multi-NPC reactions. §14 acceptance: 10 scenarios (SPIKE_01 turn 5 reproducibility verified). |
| `chorus_batch_state` | T1 / Channel (cell) | **NPC_002 Chorus** (CANDIDATE-LOCK 2026-04-26) | Transient orchestrator coordination. |
| `actor_core` | T2 / Reality | **ACT_001 Actor Foundation** (CANDIDATE-LOCK 2026-04-27 closure pass 5/5; was DRAFT 2026-04-27 — Tier 5 Actor Substrate; unification refactor; replaces `npc` R8 anomaly) | UNIFIED per-actor identity aggregate (PC + NPC; Synthetic excluded V1 per ACT-A7). Always present post-creation per ACT-A3. Fields: actor_id (EF_001 §5.1) + glossary_entity_id + current_region_id + current_session_id (Option) + mood (ActorMood — renamed from NpcMood; kind-agnostic) + core_beliefs (CanonRef) + flexible_state (FlexibleState). **Mutable** via canonical seed (ActorBorn) + Forge admin (Forge:EditActorCore) + runtime mood/flexible_state drift (preserved from NPC_001 §13 session-end derivation). Synthetic actors forbidden V1 (`actor.synthetic_actor_forbidden`). Cross-reality strict V1 (`actor.cross_reality_mismatch` V2+ Heresy reservation). **L1 Identity layer** per ACT-A2 3-layer model. **REPLACES** R8-locked `npc` aggregate (NPC_001 §3.1) — NPC_001 closure-pass-extension transfers ownership to ACT_001 in commit 3/5. RealityManifest extension `canonical_actors: Vec<CanonicalActorDecl>` ownership transfers from PL_001+NPC_001 → ACT_001 (extension contract §2 unified). 6 V1 reject rule_ids in `actor.*` namespace + 3 V1+ reservations. §15 acceptance: 10 V1-testable AC-ACT-1..10 + 4 V1+ deferred. 10 deferrals (ACT-D1..ACT-D10). See [ACT_001 §3.1](../features/00_actor/ACT_001_actor_foundation.md#31-actor_core-t2--reality-scope--primary-always-present). |
| `actor_chorus_metadata` | T2 / Reality | **ACT_001 Actor Foundation** (CANDIDATE-LOCK 2026-04-27 closure pass 5/5; was DRAFT 2026-04-27) | UNIFIED AI-drive metadata aggregate; sparse storage per ACT-A4 (populated when actor's control source = AI). V1: NPCs always populated; PCs NEVER populated (always User-controlled V1). V1+ AI-controls-PC-offline activation: PCs populate when offline; row removed/inactive when online. Fields: actor_id + greeting_obligation + priority_tier_hint + desires (Vec<DesireDecl> — renamed from NpcDesireDecl; NPC_003 ownership transfers to ACT_001 in commit 3/5). **Mutable** via canonical seed (ActorChorusMetadataBorn for NPCs only) + Forge admin (Forge:EditChorusMetadata). **L3 Control state layer** per ACT-A2. **REPLACES** scattered NPC-specific declarative fields (greeting_obligation + priority_tier_hint were on CanonicalActorDecl; desires was npc aggregate field added 2026-04-26 NPC_003) — unified into single sparse aggregate. Future-proofs AI-controls-PC-offline V1+ feature without aggregate refactor (additive activation). See [ACT_001 §3.2](../features/00_actor/ACT_001_actor_foundation.md#32-actor_chorus_metadata-t2--reality-scope--sparse-ai-drive-metadata). |
| `actor_actor_opinion` | T2 / Reality | **ACT_001 Actor Foundation** (CANDIDATE-LOCK 2026-04-27 closure pass 5/5; was DRAFT 2026-04-27 — replaces `npc_pc_relationship_projection`) | UNIFIED bilateral opinion aggregate; sparse per-(observer_actor_id, target_actor_id). V1 active patterns: NPC→PC (preserved from npc_pc_relationship_projection session-end derivation per NPC_001 §13). V1+ patterns: PC→NPC (ACT-D2) + NPC→NPC sect rivalry drama (ACT-D3) + PC→PC multi-PC realities (ACT-D4). Fields: id (uuidv5(observer, target)) + observer_actor_id (indexed) + target_actor_id (indexed) + trust (i16 -100..+100) + familiarity (u16) + stance_tags (Vec<StanceTag>) + last_updated_turn. Observer ≠ target enforced (`actor.opinion_self_target_forbidden` Stage 0 schema). Synthetic forbidden as observer/target. Symmetry NOT enforced (du_si→ly_minh and ly_minh→du_si stored separately; values may differ). **Mutable** via session-end derivation (preserved from NPC_001 §13) + Forge admin (Forge:EditActorOpinion) + V1+ runtime events for V1+ patterns. **L3 Relationship layer** per ACT-A2. **REPLACES** R8-locked `npc_pc_relationship_projection` (NPC_001 §3.3) — NPC_001 closure-pass-extension transfers ownership in commit 3/5; bilateral key composite enables symmetric pair patterns. See [ACT_001 §3.3](../features/00_actor/ACT_001_actor_foundation.md#33-actor_actor_opinion-t2--reality-scope--sparse-bilateral). |
| `actor_session_memory` | T2 / Reality | **ACT_001 Actor Foundation** (CANDIDATE-LOCK 2026-04-27 closure pass 5/5; was DRAFT 2026-04-27 — replaces `npc_session_memory`) | UNIFIED per-(actor, session) session memory aggregate. V1: NPCs populated (preserved from npc_session_memory; bounded R8-L2: ≤100 facts + ≤2000 char summary; LRU eviction; rolling LLM summary every 50 events). V1+ AI-controls-PC-offline activates PC population (ACT-D1; world-service handles offline PC LLM context via this aggregate). Fields: id (uuidv5(actor_id, session_id) per R8-L2) + actor_id (indexed) + session_id + summary (String ≤2000 chars) + facts (Vec<MemoryFact> ≤100 LRU) + embeddings_ref (Option; pgvector dedicated table per R8-L6). **Mutable** via session-end derivation (preserved from NPC_001) + Forge admin (Forge:EditActorSessionMemory). **L3 LLM context layer** per ACT-A2. **REPLACES** R8-locked `npc_session_memory` (NPC_001 §3.2) — NPC_001 closure-pass-extension transfers ownership in commit 3/5. See [ACT_001 §3.4](../features/00_actor/ACT_001_actor_foundation.md#34-actor_session_memory-t2--reality-scope--per-session). |
| `npc_node_binding` | T2 (T3 on handoff) / Reality | **NPC_001 Cast** (CANDIDATE-LOCK 2026-04-26; closure-pass-extension 2026-04-27) | NPC owner-node mapping with epoch fence — KEPT under NPC_001 ownership post-ACT_001 unification (NPC-specific writer-node binding mechanism; PC uses entity_binding from PL_001/EF_001 — different mechanism). ACT_001 unification touches NPC_001 §3 (3 aggregates moved to ACT_001) but `npc_node_binding` stays NPC-owned. V1+ may consolidate to unified actor_node_binding when AI-controls-PC-offline ships (ACT-D10). |
| `lex_config` | T2 / Reality singleton | **WA_001 Lex** (CANDIDATE-LOCK 2026-04-25) | Per-reality physics/ability/energy axioms. §14 acceptance: 10 scenarios. |
| `actor_contamination_decl` | T2 / Reality | **WA_002 Heresy** (CANDIDATE-LOCK 2026-04-25) | Per-(actor, kind) contamination exception. |
| `actor_contamination_state` | T2 / Reality | **WA_002 Heresy** (CANDIDATE-LOCK 2026-04-25) | Per-(actor, kind) runtime budget tracking. |
| `world_stability` | T3 / Reality singleton | **WA_002 Heresy** (CANDIDATE-LOCK 2026-04-25) | 5-stage state machine. |
| `forge_audit_log` | T2 / Reality | **WA_003 Forge** (CANDIDATE-LOCK 2026-04-25) | Append-only audit; ALSO USED by PLT_001 + PLT_002 + WA_006 with their own AuditAction sub-shapes. WA_003 closure-reframed 2026-04-25 — patterns extractable to future CC_NNN are V1-essential here, V2+ optimization not boundary fix. §14 acceptance: 10 scenarios. |
| `coauthor_grant` | T2 / Reality | **PLT_001 Charter** (CANDIDATE-LOCK 2026-04-25; formerly WA_004; relocated 2026-04-25) | Active Co-Author grants. §14 acceptance: 10 scenarios AC-CHR-1..10. |
| `coauthor_invitation` | T2 / Reality | **PLT_001 Charter** (CANDIDATE-LOCK 2026-04-25) | Pending invitations with TTL. |
| `ownership_transfer` | T2 (T3 at Finalize) / Reality | **PLT_002 Succession** (CANDIDATE-LOCK 2026-04-25; formerly WA_005; relocated 2026-04-25) | Multi-stage state machine. PLT_002b lifecycle split 2026-04-25 — §14 acceptance: 10 scenarios AC-SUC-1..10. |
| `mortality_config` | T2 / Reality singleton | **WA_006 Mortality** (CANDIDATE-LOCK 2026-04-25) | Per-reality death-mode config. WA_006 thin-rewritten 2026-04-25 closure pass (730 → 403 lines); only legitimate WA scope kept. §12 acceptance: 6 scenarios. |
| `pc_user_binding` | T2 / Reality | **PCS_001 PC Substrate** (CANDIDATE-LOCK 2026-04-27 closure pass 4/4; was DRAFT 2026-04-27 — Tier 5 Actor Substrate post-ACT_001 priority per Q2 LOCKED) | Per-PC user-binding aggregate (sparse PC-only; V1 cap=1 per reality per Q9 LOCKED row count Stage 0 schema validator). Single aggregate Q2 LOCKED bundling: pc_id + user_id (Option<UserId> auth-service ref; None canonical seed → bound via Forge admin V1) + current_session (Option<SessionId>) + body_memory (PcBodyMemory: SoulLayer + BodyLayer + LeakagePolicy 4-variant per Q5+Q6 LOCKED) + last_login_at_turn + last_xuyenkhong_at_turn. PcId Uuid newtype + DP-A12 module-private constructor per Q1 LOCKED. PcBodyMemory schema: SoulLayer (origin_world_ref Option<GlossaryEntityId> per Q8 LOCKED + knowledge_tags + native_skills V1 empty Vec reserved per Q5 REFINEMENT + native_language) + BodyLayer + LeakagePolicy 4-variant (NoLeakage / SoulPrimary { body_blurts_threshold } / BodyPrimary { soul_slips_threshold } / Balanced). **Mutable** via canonical seed (PcRegistered EVT-T4) + Forge admin (5 sub-shapes per Q3 LOCKED) + V1+ runtime PcTransmigrationCompleted (renamed from PcXuyenKhongCompleted per user direction; English type name). **3-layer architectural model post-ACT_001** (PCS-A1): L1 identity (ACT_001 actor_core) + L2 kind (ActorId::Pc) + L3 PC-specific (PCS_001 owns). Synthetic forbidden V1 (PCS-A7). Cross-reality strict V1 (PCS-A8 V2+ Heresy). RealityManifest CanonicalActorDecl additive fields V1 (REQUIRED for kind=Pc): body_memory_init + user_id_init. 7 V1 reject rules `pc.*` namespace + 3 V1+ reservations. §16 acceptance: 10 V1 AC + 4 V1+ deferred. 10 deferrals (PCS-D1..PCS-D10). See [PCS_001 §3.1](../features/06_pc_systems/PCS_001_pc_substrate.md#31-pc_user_binding-t2--reality-scope--sparse-pc-only). |
| `pc_mortality_state` | T2 / Reality | **PCS_001 PC Substrate** (CANDIDATE-LOCK 2026-04-27 closure pass 4/4; was DRAFT 2026-04-27 — Tier 5 Actor Substrate post-ACT_001; handoff from WA_006 §6 closure pass commit f436e60 RESOLVED) | Per-PC mortality state machine (sparse PC-only; V1 cap=1 per reality matching pc_user_binding). 4-state schema per Q7 LOCKED: Alive / Dying { died_at_turn, died_at_cell, will_respawn_at_fiction_time, spawn_cell } / Dead { died_at_turn, died_at_cell } / Ghost { died_at_turn, died_at_cell }. V1 active death transitions Alive→{Dead, Dying, Ghost} per `mortality_config.mode` (Permadeath / RespawnAtLocation / Ghost). V1+ deferred PCS-D2: Respawn flow (Dying → Alive) + Resurrection (Ghost → Alive) + GhostDispersed (Ghost → Dead). 6-variant TransitionTrigger (DamageOverflow / PoisonStarvation / ForgeAdmin / RespawnComplete V1+ / Resurrection V1+ / GhostDispersed V1+). MortalityTransition history Vec ≤20 V1. **Mutable** via mortality EVT-T3 (DyingTransition + DeathTransition + GhostTransition V1) + Forge admin (Forge:EditPcMortalityState). Reject `pc.respawn_unsupported_v1` Stage 0 schema canonical seed if mortality_config.mode = RespawnAtLocation declared without V1+ Respawn flow active (V1 reality must use Permadeath or Ghost). See [PCS_001 §3.2](../features/06_pc_systems/PCS_001_pc_substrate.md#32-pc_mortality_state-t2--reality-scope--sparse-pc-only-handoff-from-wa_006). |
| `meta_user_pending_invitations` | global meta-DB table | **PLT_001 Charter** (CANDIDATE-LOCK 2026-04-25; formerly WA_004) | Cross-reality denormalized; CHR-D9 watchpoint. |
| `actor_status` | T2 / Reality | **PL_006 Status Effects** (CANDIDATE-LOCK 2026-04-26 PL folder closure; was DRAFT 2026-04-26) | Cross-actor (PC + NPC) status foundation. Per-(reality, actor_id) row holds `Vec<StatusInstance>`. ActorId source-of-truth: EF_001 §5.1. Owns **StatusFlag closed-set enum** (V1: Drunk / Exhausted / Wounded / Frightened); V1+ kinds reserved (Stunned/Bleeding/Poisoned/Charmed/Encumbered/Buffed/Tired/Hungry/Restrained). RES_001 DRAFT 2026-04-26 promotes Hungry from V1+reserved → V1 active (downstream — PL_006 closure pass folds in magnitude semantics: 1-3=mild / 4-6=severe / 7+=critical→MortalityTransitionTrigger Starvation). Apply/Dispel via PL_005 Interaction OutputDecl with `aggregate_type=actor_status`. V1+30d auto-expire via Scheduled:StatusExpire Generator (EVT-T5). PCS_001 + future NPC_003 reference this enum (no drift). PCS_001 brief §S5 `pc_stats_v1_stub.status_flags` resolves via actor_status query (read-side projection); writes target actor_status directly per §11 migration note. Phase 3 cleanup: `status.target_dead` re-allocated to canonical `entity.lifecycle_dead` (Stage 3.5.a EF_001 owner) avoiding duplicate rule. §15 acceptance: 7 V1-testable scenarios. |
| `vital_pool` | T2 / Reality | **RES_001 Resource Foundation** (DRAFT 2026-04-26) | Body-bound vital pool for actors (PC + NPC). Per-(reality, actor_ref) row holds `Vec<VitalInstance>`. **NON-TRANSFERABLE** (type-system enforced — no transfer event accepts vital_pool source/target; body-bound semantics per Q3). Owns **VitalKind closed-set enum** (V1: Hp / Stamina; V1+ Mana reserved). VitalProfile shape RES_001-owned: `{ kind, max_value, regen_rule, depletion_rule, on_zero_effect }`. PCS_001 + NPC_001 declare per-actor-class max_value via RealityManifest `vital_profiles` extension. Hp=0 + OnZeroEffect=EmitMortalityTrigger → emit MortalityTransitionTrigger consumed by WA_006. Stamina=0 + OnZeroEffect=ApplyStatus(Exhausted) → PL_006 actor_status. Read-side projection `actor_vital_summary` provides hot-path lookup for AssemblePrompt + combat tick. §14 acceptance: AC-RES-7 (Starvation mortality threshold) + V2 / V5 / RES-V1+V2 validators. See [RES_001 §4.1](../features/00_resource/RES_001_resource_foundation.md#41-vital_pool-aggregate). |
| `resource_inventory` | T2 / Reality | **RES_001 Resource Foundation** (DRAFT 2026-04-26) | Portable resource ownership. Per-(reality, owner: EntityRef) row holds `Vec<ResourceBalance>`. Owner = EntityRef any (PC / NPC / Cell V1; Item V1+30d; Faction V3). Holds 4 V1 ResourceKind variants in inventory: `Consumable(ConsumableKindId)` / `Currency(CurrencyKindId)` / `Material(MaterialKindId)` / `SocialCurrency(SocialKind)`. ResourceBalance shape: `{ kind, amount: u64, instance_id: Option<ItemInstanceId> }` — `instance_id=None` V1 (locked schema for V1+30d Item kind via Q1a additive). `inventory_cap: Option<CapacityProfile>` field reserved on entity_binding (EF_001) — None V1, Some V1+30d slot/weight cap (Q6b). Transfer via PL_005 Interaction OutputDecl with `aggregate_type=resource_inventory`. Cell-as-owner = inventory tracks cell stockpile (production accumulates here per Q4); NPC owner auto-collects daily (Q4f Generator). PC owner manual harvests (Q4g — PL_005 Use kind / harvest sub-intent). NPC finite liquidity validator (Q12c) — RES-V3 enforces. SocialCurrency Reputation V1 — wuxia/xianxia danh tiếng SSOT. §14 acceptance: AC-RES-1..10. See [RES_001 §4.2](../features/00_resource/RES_001_resource_foundation.md#42-resource_inventory-aggregate). |
| `race_assignment` | T2 / Reality | **IDF_001 Race Foundation** (CANDIDATE-LOCK 2026-04-26 IDF folder closure 3/15; was DRAFT 2026-04-26 — Tier 5 Actor Substrate) | Per-actor race assignment (PC + NPC; ActorId source EF_001 §5.1). Birth-fixed V1 (immutable race_id; V1+ Reincarnation/Transformation lifts via additive AssignmentReason variants). Owns **RaceId opaque string per-reality** (closed-set declared in RealityManifest.races; cross-reality collision allowed). Owns **SizeCategory closed-set enum 6-variant** (Tiny/Small/Medium/Large/Huge/Gargantuan; Pathfinder 2e full coverage per POST-SURVEY-Q2 LOCKED 2026-04-26). RaceDecl declarative metadata: display_name (I18nBundle) + default_lifespan_years (u16) + size_category + default_mortality_kind_override (Option<MortalityKind> per WA_006) + allowed_lex_axiom_tags (Vec<String> for V1+ Lex gate). AssignmentReason 4-variant V1: CanonicalSeed / PcCreation / NpcSpawn / AdminOverride{reason}. WA_006 mortality_config consumer reads race-default override at runtime (Permadeath / RespawnAtLocation / Ghost / AlreadyDead). V1+ AxiomDecl.requires_race hook reserved for Lex axiom gate at Stage 4 lex_check (V1 always None; first axiom V1+ at WA_001 closure). RAC-D11 deferral: Cultivation realm = SEPARATE V1+ feature CULT_001 (NOT IDF_001 expansion; mutable rank-tier within Cultivator race). RealityManifest extension `races: Vec<RaceDecl>` REQUIRED V1. §15 acceptance: 10 V1-testable scenarios AC-RAC-1..10 + 3 V1+ deferred (AC-RAC-V1+1..3). See [IDF_001 §3.1](../features/00_identity/IDF_001_race.md#31-race_assignment-t2--reality-scope--primary). |
| `actor_language_proficiency` | T2 / Reality | **IDF_002 Language Foundation** (CANDIDATE-LOCK 2026-04-26 IDF folder closure 6/15; was DRAFT 2026-04-26 — Tier 5 Actor Substrate) | Per-actor language proficiency (PC + NPC; ActorId source EF_001 §5.1). HashMap<LanguageId, ProficiencyMatrix> 4-axis × 5-level (Read/Write/Speak/Listen × None/Basic/Conversational/Fluent/Native). LanguageId opaque per-reality (distinct from RES_001 LangCode — runtime newtype assert V1; LNG-Q11 LOCKED; LNG-D8 compile-time V1+). LanguageDecl declarative metadata: display_name (I18nBundle) + writing_system (5-variant: None/Logographic/Alphabetic/Syllabary/Custom) + default_in_origin_packs (V1+ IDF_004 ref) + canon_ref. SPIKE_01 turn 5 literacy slip canonical reproducibility gate (LM01 Quan thoại Native + Cổ ngữ Read=None → A6 detector V1+ flags BodyKnowledgeMismatch). PL_005b §2.7 Speak validator consumes (Stage 7). RealityManifest extension `languages: Vec<LanguageDecl>` REQUIRED V1. §15 acceptance: 10 V1-testable (AC-LNG-1..10) + 2 V1+ deferred. See [IDF_002 §3.1](../features/00_identity/IDF_002_language.md#31-actor_language_proficiency-t2--reality-scope--primary). |
| `actor_personality` | T2 / Reality | **IDF_003 Personality Foundation** (CANDIDATE-LOCK 2026-04-26 IDF folder closure 9/15; was DRAFT 2026-04-26 — Tier 5 Actor Substrate) | Per-actor personality archetype (PC + NPC; ActorId source EF_001 §5.1). Single archetype field V1 (V1+ overlay PRS-D3). 12 V1 archetypes per POST-SURVEY-Q1 LOCKED: Stoic/Hothead/Cunning/Innocent/Pious/Cynic/Worldly/Idealist + Loyal/Aloof/Ambitious/Compassionate. PersonalityArchetypeDecl: display_name (I18nBundle) + voice_register (5-variant Formal/Neutral/Casual/Crude/Archaic) + opinion_modifier_table (HashMap<PersonalityArchetypeId, i8> -10..=+10; 12×12 = 144 entries required) + speech_pattern_hints (Vec<String> for V1+ NPC_002 LLM prompt) + canon_ref. **Resolves PL_005b §2.1 speaker_voice orphan ref** (was "PCS_001 PcVoiceRegister"; now IDF_003 actor_personality.archetype.voice_register). **Resolves PL_005c INT-INT-D5** (per-personality opinion modifier deferral) via opinion_modifier_table. NPC_002 §6 priority Tier 2-3 consumes archetype for filtering. RealityManifest extension `personality_archetypes: Vec<PersonalityArchetypeDecl>` REQUIRED V1. Synthetic actors forbidden V1 (PRS-Q11 LOCKED). §15 acceptance: 10 V1-testable AC-PRS-1..10 + 2 V1+ deferred. See [IDF_003 §3.1](../features/00_identity/IDF_003_personality.md#31-actor_personality-t2--reality-scope--primary). |
| `actor_origin` | T2 / Reality | **IDF_004 Origin Foundation** (CANDIDATE-LOCK 2026-04-26 IDF folder closure 12/15; was DRAFT 2026-04-26 — Tier 5 Actor Substrate) | Per-actor immutable birth-context (PC + NPC; ActorId source EF_001 §5.1). V1 minimal stub 4 fields: birthplace_channel: Option<ChannelId> (PF_001 cell-tier; ORG-Q3 LOCKED) + lineage_id: Option<LineageId> (opaque tag V1 per ORG-Q4 LOCKED — NO parent/sibling refs in V1; FF_001 V1+ resolves) + native_language: LanguageId (IDF_002 ref REQUIRED V1) + default_ideology_refs: Vec<IdeologyId> (IDF_005 ref; suggestions). origin_pack_id: Option<OriginPackId> reserved V1+ schema slot. OriginPackDecl V1+ enrichment: display_name (I18nBundle) + default_birthplace + default_native_language + default_ideology_refs + naming_convention V1+ + values_list V1+. **V1+ FF_001 Family Foundation = HIGH priority post-IDF closure** (BEFORE PCS_001) per POST-SURVEY-Q4 + ORG-D12. IDF_002 default proficiency seed reads native_language; IDF_005 default ideology seed reads default_ideology_refs Light fervor. Synthetic actors forbidden V1 (ORG-Q7 LOCKED). RealityManifest extension `origin_packs: Vec<OriginPackDecl>` OPTIONAL V1 (empty registry V1; populated V1+). §15 acceptance: 10 V1-testable AC-ORG-1..10 + 3 V1+ deferred. See [IDF_004 §3.1](../features/00_identity/IDF_004_origin.md#31-actor_origin-t2--reality-scope--primary). |
| `actor_ideology_stance` | T2 / Reality | **IDF_005 Ideology Foundation** (CANDIDATE-LOCK 2026-04-26 IDF folder closure 15/15 FINAL; was DRAFT 2026-04-26 — Tier 5 Actor Substrate) | (see §3.1 IDF_005 — multi-stance per actor; ONLY mutable IDF aggregate V1; Apply/Drop/AdjustFervor events) |
| `family_node` | T2 / Reality | **FF_001 Family Foundation** (CANDIDATE-LOCK 2026-04-26 closure pass 4/4; was DRAFT 2026-04-26 — Tier 5 Actor Substrate post-IDF priority per IDF_004 ORG-D12) | Per-actor family graph node (PC + NPC; ActorId source EF_001 §5.1). **Mutable V1** via Apply events (Marriage / MarkDeceased / V1+ AddChild / RemoveSpouse / AddAdoptedParent). Direct relations (per Q2 LOCKED): parent_actor_ids + sibling_actor_ids + spouse_actor_ids + children_actor_ids — `Vec<(ActorId, RelationKind)>` for parent/child sides; `Vec<ActorId>` for sibling/spouse sides. 6-variant RelationKind closed enum (per Q6 LOCKED): BiologicalParent / AdoptedParent / Spouse / BiologicalChild / AdoptedChild / Sibling. dynasty_id: Option<DynastyId> (membership). is_deceased + deceased_at_turn (V1 mark deceased; refs preserved). Bidirectional sync validated at canonical seed + Forge admin events. **Q5 LOCKED: NO separate family_event_log aggregate** — events in channel stream per EVT-A10. Synthetic actors forbidden V1 (matches IDF discipline). RealityManifest extension `canonical_family_relations: Vec<FamilyRelationDecl>` REQUIRED V1 (sparse storage allowed; empty Vec valid for orphan-PC reality). §15 acceptance: 10 V1-testable AC-FF-1..10 + 4 V1+ deferred (AC-FF-V1+1..4). 12 deferrals (FF-D1..D12). Boundary discipline: FF_001 = biological + adoption only; V1+ FAC_001 owns sect/master-disciple/sworn (per Q4 LOCKED). See [FF_001 §3.1](../features/00_family/FF_001_family_foundation.md#31-family_node-t2--reality-scope--primary). |
| `dynasty` | T2 / Reality | **FF_001 Family Foundation** (CANDIDATE-LOCK 2026-04-26 closure pass 4/4; was DRAFT 2026-04-26 — Tier 5 Actor Substrate; sparse storage) | Per-dynasty metadata (multi-generational house). Sparse storage — only declared dynasties get rows (typically 0-3 per reality V1). DynastyDecl declarative metadata: dynasty_id + display_name (I18nBundle) + founder_actor_id (Option) + current_head_actor_id (Option) + member_count (sparse query helper) + canon_ref. **Mutable V1+** via succession events (current_head_actor_id updates on heir transition). V1 minimal schema; V1+ additive enrichment (parent_dynasty_id for cadet branches; traditions; perks per CK3 pattern). RealityManifest extension `canonical_dynasties: Vec<DynastyDecl>` REQUIRED V1 (sparse; empty Vec valid for sandbox). V1+ TIT_001 Title Foundation reads dynasty.current_head_actor_id for heir selection. See [FF_001 §3.2](../features/00_family/FF_001_family_foundation.md#32-dynasty-t2--reality-scope--sparse). |
| `actor_progression` | T2 / Reality | **PROG_001 Progression Foundation** (DRAFT 2026-04-26 — 6th and final V1 foundation feature; closes foundation tier; Tier 5 Actor Substrate concurrent with IDF/FF) | Multi-genre dynamic progression substrate per actor (PC + NPC V1 active; Item V1+30d reserved). Owns ProgressionType closed enum (3 V1: Attribute / Skill / Stage; ResourceBound V1+30d) + BodyOrSoul discriminator (Body/Soul/Both for xuyên không cross-reality stat translation; default Body) + ProgressionInstance with raw_value + current_tier + last_trained_at + **last_observed_at** (quantum-observation Q4 REVISED) + training_log_window. Owns `tracking_tier: Option<NpcTrackingTier>` field reserved (None V1; future AI Tier feature populates). Per-reality author-declared schema via RealityManifest `progression_kinds: Vec<ProgressionKindDecl>` (empty default = sandbox/freeplay valid V1; no universal default since modern social ≠ tu tiên ≠ D&D). 3 curve types V1 (Linear/Log/Stage flat tier list with breakthrough); 4 CapRule types (SoftCap/HardCap/TierBased/Unbounded); validity matrix Q2j enforced. 2 training trigger sources V1 (Action via PL_005 cascade hot-path indexed by InteractionKind + Time via day-boundary Generator); 3 V1 TrainingConditions (LocationMatch + StatusRequired + StatusForbidden). **Hybrid observation-driven NPC model (Q4 REVISED)**: PCs eager Generator iteration; Tracked NPCs lazy materialization on observation; Untracked NPCs = no aggregate (future AI Tier feature owns; PROG_001 silently skips). Materialization computation: per-day replay with conservative single-state assumption (V1+30d intermediate-state interpolation PROG-D20). Hybrid combat damage Q7: LLM proposes damage_amount in PL_005 Strike payload; engine validator computes bounds [min, max] from offense/defense stat sums; clamps silently (no reject; preserves narrative flow); RealityManifest extension `strike_formula: Option<StrikeFormulaDecl>` with offense/defense terms + factors + post_damage_hooks for status application. NO atrophy V1 (V1+ lazy at materialization PROG-D5). NO level / no chiến lực (PROG-A2). DF7 PC Stats placeholder SUPERSEDED at PROG_001 DRAFT (DF7-V1+ becomes "Combat Damage Formulas Full" sub-feature reading PROG_001 ProgressionInstance). 12 V1-testable acceptance scenarios AC-PROG-1..12 + 30+ deferrals (PROG-D1..D32) + 6 open questions (PROG-Q1..Q6). chaos-backend reference: actor-core Subsystem→Contribution pattern V1+30d lift (PROG-D6); damage law chain V1+ DF7-equivalent (PROG-D24). See [PROG_001 §3.1](../features/00_progression/PROG_001_progression_foundation.md#31-actor_progression-t2--reality--primary). | Per-actor mutable belief stance (PC + NPC; ActorId source EF_001 §5.1). **ONLY mutable IDF aggregate V1** per IDL-Q11 LOCKED. Multi-stance V1 per IDL-Q2 LOCKED — `Vec<IdeologyStanceEntry>` (Wuxia syncretism: Lý Minh holds Đạo + Phật + Nho with varying fervor; verified by classic wuxia novels). FervorLevel 5-level enum per IDL-Q1 LOCKED: None/Light/Moderate/Devout/Zealous. IdeologyDecl: display_name (I18nBundle) + parent_ideology_id (V1+ hierarchy schema slot per IDL-Q4) + lex_axiom_tags (Vec<String> for V1+ Lex gate) + canon_ref. Apply/Drop/AdjustFervor events per IDL-Q3 LOCKED with audit (IdeologyChangeReason 6-variant per IDL-Q10). Atheist = empty Vec per IDL-Q8 LOCKED. **V1 free conversion per IDL-Q13 LOCKED (POST-SURVEY-Q3)** — NO cost / NO opinion penalty / NO time-period; IDL-D11 V1+ cost mechanic (when scheduler V1+30d ships OR IDL-D3 ideology-conflict modifier ships). V1+ AxiomDecl.requires_ideology hook reserved for Lex gate at Stage 4. IDF_004 default_ideology_refs auto-seed Light fervor at canonical seed per IDL-Q9 LOCKED. Synthetic actors forbidden V1 (IDL-Q12 LOCKED). RealityManifest extension `ideologies: Vec<IdeologyDecl>` REQUIRED V1. §15 acceptance: 10 V1-testable AC-IDL-1..10 + 2 V1+ deferred. See [IDF_005 §3.1](../features/00_identity/IDF_005_ideology.md#31-actor_ideology_stance-t2--reality-scope--primary). |
| `faction` | T2 / Reality | **FAC_001 Faction Foundation** (CANDIDATE-LOCK 2026-04-26 closure pass 4/4; was DRAFT 2026-04-26 — Tier 5 Actor Substrate post-IDF + post-FF_001 priority) | Per-faction declarative entity (sect/order/clan-retinue/guild/coalition). Sparse storage (only declared factions get rows; typically 5 sects per Wuxia preset). 6-variant FactionKind closed enum (Sect / Order / Clan / Guild / Coalition / Other). FactionDecl: faction_id + display_name (I18nBundle) + faction_kind + roles (Vec<RoleDecl> author-declared per Q3 LOCKED with role_id + display_name + authority_level u8 0-100) + requires_ideology (Option<Vec<(IdeologyId, FervorLevel)>> RESOLVES IDF_005 IDL-D2) + default_relations (HashMap<FactionId, RelationStance> 3-variant Hostile/Neutral/Allied static V1 per Q5 LOCKED) + founder_actor_id (Option) + current_head_actor_id (Option) + member_count (sparse query helper). **Mutable V1+** via SetCurrentHead (V1+ TIT_001 succession). Boundary discipline: FAC_001 = sect/order/clan-retinue/guild + master-disciple + V1+ sworn brotherhood (FAC-D10 deferred per Q7 REVISION); FF_001 = biological/adoption only (separated per FF_001 Q4 LOCKED); V1+ TIT_001 = title rank (separated); V1+ REP_001 = reputation projection (separated); V1+ CULT_001 = cultivation method binding via faction_id; V1+ DIPL_001 = dynamic inter-faction relations layered on FAC_001 static default_relations. RealityManifest extension `canonical_factions: Vec<FactionDecl>` REQUIRED V1 (sparse; empty Vec valid for sandbox). §15 acceptance: 10 V1-testable AC-FAC-1..10 + 4 V1+ deferred (AC-FAC-V1+1..4). 17 deferrals (FAC-D1..D17). See [FAC_001 §3.1](../features/00_faction/FAC_001_faction_foundation.md#31-faction-t2--reality-scope--sparse). |
| `actor_faction_membership` | T2 / Reality | **FAC_001 Faction Foundation** (CANDIDATE-LOCK 2026-04-26 closure pass 4/4; was DRAFT 2026-04-26 — Tier 5 Actor Substrate) | Per-actor faction memberships (PC + NPC; ActorId source EF_001 §5.1). `Vec<FactionMembershipEntry>` with **V1 validator cap=1 per Q2 REVISION** (V1+ relax cap = NO schema migration via single-line validator change). FactionMembershipEntry: faction_id + role_id + rank_within_role (u16 numeric V1 per Q4 REVISION; named computed display) + master_actor_id (Option<ActorId> RESOLVES FF-D7 master-disciple) + joined_at_turn + joined_at_fiction_ts + joined_reason (4-variant V1: CanonicalSeed/PcCreation/NpcSpawn/AdminOverride). V1+ additive: sworn_bond_id (FAC-D10 per Q7 REVISION; V1 NOT shipped) + explicit_rank_name (FAC-D17 / Q4 REVISION enrichment). **Mutable V1+** via JoinFaction/LeaveFaction/ChangeRole/ChangeRank/SetMaster/RemoveMaster. Synthetic actors forbidden V1 per Q10 LOCKED. Bidirectional sync at canonical seed (faction.current_head_actor_id ↔ membership with role authority_level=100). Master-disciple chain validated (same faction + authority higher + no cycle) — RESOLVES FF-D7. Ideology binding validated at canonical seed (FactionDecl.requires_ideology vs actor_ideology_stance) — RESOLVES IDL-D2. RealityManifest extension `canonical_faction_memberships: Vec<FactionMembershipDecl>` REQUIRED V1 (sparse; empty Vec valid for unaffiliated actors). 8 V1 reject rule_ids in faction.* namespace. See [FAC_001 §3.2](../features/00_faction/FAC_001_faction_foundation.md#32-actor_faction_membership-t2--reality-scope--primary). |
| `actor_clocks` | T2 / Reality | **TDIL_001 Time Dilation Foundation** (DRAFT 2026-04-27 — architecture-scale; NOT foundation tier; Einstein-relativity origin) | 4-clock relativity model per actor (PC + NPC; Untracked = ephemeral in session.untracked_cache per AIT_001 promotion crystallizes). Always-present V1 (mirror ACT_001 actor_core L1 pattern; 5th actor-related aggregate alongside actor_core + actor_chorus_metadata + actor_actor_opinion + actor_session_memory). 3 actor-side clocks: **actor_clock** (proper time τ integrated; canonical worldline; monotonic V1) + **soul_clock** (BodyOrSoul::Soul progressions read this; xuyên không source_a brings) + **body_clock** (BodyOrSoul::Body progressions + future aging V2+; xuyên không source_b keeps). Realm clock (per-channel; existing PL_001 fiction_clock) NOT in this aggregate. **Per-turn lockstep V1** (all 3 actor-side advance proper_advance same per turn); divergence cases V1+30d (TDIL-D3/D4/D5 — Option B subjective rate / chamber DilationTarget BodyOnly|SoulOnly|AllClocks / soul wandering). **Initial value divergence V1 OK** at canonical seed events: author InitialClocksDecl on CanonicalActorDecl OR PCS_001 §S8 xuyên không clock-split (soul→soul, body→body, actor=0; twin paradox preserved). **Generator clock-source matrix LOCKED** (Q6): channel-bound generators (CellProduction/NPCAutoCollect/CellMaintenance) read wall_advance; actor-bound generators (HungerTick/CultivationTick/Aging) read appropriate proper-time clock per BodyOrSoul. **Convention B `time_flow_rate` semantic** (proper time per wall time): >1 fast (Dragon Ball chamber); <1 slow (Tây Du Ký heaven); range V1 [0.001, 1000.0]. Channel-level `time_flow_rate` on MAP_001 MapLayoutDecl + cell-level `time_flow_rate_override: Option<f32>` on PF_001 PlaceDecl (REPLACE semantic, NOT multiply). **Atomic-per-turn travel** (TDIL-V1) — actor in EXACTLY ONE channel per turn; no mid-turn cross. **Per-realm turn streams** — heaven_clock advances only on heaven activity; idle = frozen (extends AIT_001 quantum-observation). **Cross-realm observation O(1) materialization** — 1 calculation regardless of magnitude. **Replay determinism FREE V1** (static rates + per-channel turn streams + atomic travel + monotonic clocks); Forge past-clock edits FORBIDDEN PERMANENTLY V1+. **Closure-pass revisions in same commit (mechanical):** PROG_001 Q3f day-boundary → turn-boundary; RES_001 Q4 day-boundary → turn-boundary; AIT_001 §7.5 materialization O(1). 4 V1 rule_ids in time_dilation.* namespace + 6 V1+30d reservations. 10 V1-testable AC-TDIL-1..10. 16 deferrals (TDIL-D1..D16) + 5 open questions (TDIL-Q1..Q5). See [TDIL_001 §4.1](../features/17_time_dilation/TDIL_001_time_dilation_foundation.md#41-aggregate-definition). |
| `actor_title_holdings` | T2 / Reality | **TIT_001 Title Foundation** (DRAFT 2026-04-27 — Tier 5 Actor Substrate post-FF_001 + FAC_001 + REP_001 priority; closes the political-rank triangle) | Per-(actor, title_id) sparse edge aggregate for political/social rank holdings (PC + NPC; ActorId source EF_001 §5.1; TitleId author-declared per reality). **Sparse storage V1** per Q1 A LOCKED — only granted holdings get rows; wuxia preset typical ~5-15 rows V1. **Distinct from FAC_001 actor_faction_membership** (per-(actor, faction) operational role) **and from REP_001 actor_faction_reputation** (per-(actor, faction) bounded standing) per TIT-A4 3-layer separation discipline. ActorTitleHolding shape: actor_ref + title_id + granted_at_fiction_ts + granted_via (CanonicalSeed/ForgeAdmin/SuccessionCascade V1; V1+ QuestReward + FactionElectVote) + designated_heir Option<ActorRef> sparse + schema_version. **Mutable** via 3 V1 events: TitleGranted EVT-T4 (canonical seed + Forge admin + cascade V1 active per Q6 C LOCKED) + Forge:GrantTitle/RevokeTitle/DesignateHeir EVT-T8 + TitleSuccessionTriggered EVT-T3 (sparse on cascade) + TitleSuccessionCompleted EVT-T1 narrative (LLM milestone). **Cross-aggregate validator TIT-C1** per Q7 A LOCKED: title-holder death (WA_006 mortality EVT-T3 actor_dies) triggers synchronous succession cascade same turn (joins existing C1-C17 cross-aggregate consistency rules from P4 commit). **Author-declared per-reality discipline** per TIT-A1 (mirrors PROG-A1 + REP_001 + FAC_001) — engine schema generic; reality declares own title list (Wuxia 掌门/族长/皇帝 / Modern President/CEO / D&D King/Knight). **TitleBinding** discriminated 3-variant enum per Q2 B LOCKED (Faction/Dynasty/Standalone). **SuccessionRule** 3 V1 + 1 V1+ enum per Q3 A LOCKED (Eldest FF_001 dynasty traversal / Designated canonical+Forge / Vacate; FactionElect V1+ DIPL_001 V2+ dependency). **MultiHoldPolicy** per-title author-declared per Q5 C LOCKED (Exclusive/StackableUnlimited default/StackableMax(N)). **VacancySemantic** per-title author-declared per Q9 D LOCKED (PersistsNone default/Disabled/Destroyed). **TitleAuthorityDecl** per Q8 A + narrative_hint LOCKED: faction_role_grant Option<FactionRoleGrant> V1 active (atomic FAC_001 role grant on title-grant via 3-write atomic pattern) + narrative_hint I18nBundle V1 active (LLM persona briefing) + lex_axiom_unlock_refs Vec<AxiomDeclRef> V1 schema-reserved per Q10 B LOCKED (validator V1+ via WA_001 closure pass adding 5-companion-fields uniformly: race + ideology + faction + reputation + title). **Schema-stable / activation-deferred V1+ discipline (TIT-A8)**: TitleDecl.min_reputation_required Option<MinRepGate> field active V1 per Q4 C LOCKED (declarations stored; runtime validator V1+ alongside REP-D1 runtime delta milestone). Synthetic actors forbidden V1 per TIT-A6. Cross-reality strict V1 per TIT-A7 (V2+ Heresy via TIT-D9). **RESOLVES**: FF-D8 (full V1) + FAC-D6 (full V1) + REP-D9 (V1 partial; runtime gating V1+) + WA_006 sect-leader-death cascade gap (full V1). RealityManifest extensions `canonical_titles: Vec<TitleDecl>` + `canonical_title_holdings: Vec<TitleHoldingDecl>` BOTH OPTIONAL V1 (sparse opt-in; empty Vec valid for sandbox). 9 V1 reject rule_ids in `title.*` namespace (Phase 3 cleanup 2026-04-27 added binding.faction_membership_required + binding.dynasty_membership_required for canonical seed + Forge:GrantTitle binding-actor membership validation) + 5 V1+ reservations. §10 acceptance: 10 V1-testable AC-TIT-1..10 + 4 V1+ deferred. 12 deferrals (TIT-D1..TIT-D12). See [TIT_001 §3.1](../features/00_titles/TIT_001_title_foundation.md#31-actor_title_holdings-t2--reality-scope--primary). |
| `session` | T2 / Reality | **DF05_001 Session/Group Chat Foundation** (DRAFT 2026-04-27 — V1-blocking biggest unknown RESOLVED; multi-session-per-cell sparse architecture per user direction billion-NPC AIT scaling concern + real-life conversation parallel) | Per-session aggregate for explicit social acts at cells (PC creates with `/chat @actor` OR canonical seed; ChannelId from PF_001 cell-tier; AnchorPcId from PCS_001 PC kind via ACT_001 actor_core kind). **Sparse storage V1** per DF5-A8 — only Active sessions hot; Closed sessions cold archival. **2-state lifecycle V1** per DF5-A4: Active (turn-take loop) / Closed (terminal immutable per DF5-A7); Idle V1+30d (DF5-D4); Frozen V2+ (DF5-D8). **PC anchor invariant DF5-A4** — Active session MUST have ≥1 PC; last PC leaves → auto-close cascade. **Same-channel constraint DF5-A1** per TDIL-A5 atomic-per-turn travel — all participants exactly same channel_id. **One Active per actor DF5-A5** — any actor (PC or NPC) ≤1 Active session at a time (reject `session.actor_busy_in_other_session`). **Tier eligibility DF5-A6** — Untracked NPC NOT eligible per AIT-A8 (reject `session.actor_not_eligible_untracked`). **Per-cell soft cap DF5-A8** — ≤50 Active sessions per cell V1 (reject `session.cell_session_overload`). **Per-session participant cap** — ≤8 participants V1 inclusive of PC anchor (reject `session.participant_cap_exceeded`); V2+ assembly = separate feature for 20+ groups. **Closed = immutable DF5-A7** — no participant writes after Closed transition; aggregate frozen except archival queries. **Time-flow inheritance DF5-A3** — session inherits channel time_flow_rate per TDIL-A6; cannot override. **Mutable** via 2 V1 events: SessionBorn EVT-T4 (canonical seed) OR aggregate_type=session Born EVT-T3 Derived (runtime PC-trigger) + 9 V1 Forge AdminAction sub-shapes (Forge:CreateSession + CloseSession + KickFromSession + EditActorSessionMemory + RegenSessionDistill + PurgeActorSessionMemory + AnonymizePcInSessions + BulkRegenSessionDistill + BulkPurgeStaleSessions). **Per-actor POV memory distill on close DF5-A9** — LLM × N participants on Closed transition; cached in EVT-T6 Proposal + EVT-T3 Derived payload for replay-determinism per Q12 LOCKED; skip distill if turn_count<3 per Q2-D2. **No cross-session leak DF5-A10** — Session A's content NOT readable by Session B participants (privacy + isolation). **Replay deterministic DF5-A11** — session state derivable from EVT-T1 + EVT-T3 stream filtered by session_id; POV-distill cached per Q12-D3. **SDK Architecture LOCKED** §16 — `contracts/api/session/v1/` versioned contract (SessionService trait 7 lifecycle ops + MemoryProvider trait 4 read ops + capability probe + versioned DTOs + MemoryQuery DSL + ContractTestSuite ~30 scenarios) + `services/session-service/` swappable backends (LruDistillProvider V1 / SalienceTranscriptProvider V1+30d / KnowledgeServiceBridge V2+); CI lint enforces consumer-trait-only imports. RealityManifest extension `canonical_sessions: Vec<CanonicalSessionDecl>` OPTIONAL V1 (sparse opt-in for V1+ author-scripted set-piece sessions). 13 V1 reject rule_ids in `session.*` namespace + 5 V1+ reservations. §19 acceptance: 25 V1-testable AC-DF5-1..25. 17 deferrals (DF5-D1..DF5-D17). 4 validator slots DF5-V1..V4 + 4 cross-aggregate consistency rules DF5-C1..C4 (mapped to global C26-C29 in 03_validator_pipeline_slots.md). **RESOLVES**: PC-D1 (multi-PC parties redirected V2 multi-PC join via DF5-D1) + PC-D2 (PvP V2 deferred via DF5-D3) + PC-D3 (no global chat per multi-session sparse model) + B4 PARTIAL (multi-NPC turn arbitration via NPC_002 Chorus). See [DF05_001 §3.1](../features/DF/DF05_session_group_chat/DF05_001_session_foundation.md#31-session-t2--reality-sparse--active-hot-closed-archival--primary). |
| `actor_user_session` | T2 / Reality | **PO_001 Player Onboarding** (DRAFT 2026-04-27 — first user-visible feature post-foundation closure; FE-first design via wireframes Phase 0 commits 19855a5b + 4c4fd6d7) | Per-(actor, session) sparse aggregate for user session lifecycle tracking (PC-only V1 per Q5 LOCKED + PCS-A9 cap=1). ActorUserSession shape: actor_ref + session_id (DF5 SessionId) + user_id (auth-service ref) + onboarding_completed_at_turn (Some on completion; None during onboarding) + onboarding_mode (Canonical/Custom/XuyenKhong per Q1 A LOCKED) + onboarding_draft (V1 schema-reserved per PO-D3; V1+30d active for auto-save). **Distinct from PCS_001 pc_user_binding** (PCS owns stable PC-user mapping V1; PO_001 owns ephemeral session lifecycle). **3-mode onboarding architecture** per PO-A2 LOCKED: Mode A Canonical (BG3 Origin Character pattern; pick from canonical_pcs ref list) + Mode B Custom (8-step Basic Wizard + Advanced Settings ~46 V1 fields + AI Character Assistant 3-level UX progression per PO-A3 LOCKED) + Mode C XuyenKhong (Disco Elysium amnesia + wuxia transmigration; uses PCS_001 PcBodyMemory SoulLayer + BodyLayer + LeakagePolicy 4-variant). **AI Character Assistant V1 active** per PO-A4 LOCKED: chat-service + LiteLLM + knowledge-service constraint awareness; reality-aware (skips V1+ deferred fields); 6 quick actions (Surprise me / Make tragic / Make cunning / Generate backstory / Validate consistency / Open full screen). **PC creation cascade orchestration** per PO-A5 + PO-C1: Forge:CompleteOnboarding triggers synchronous 14-feature cascade same turn (PCS_001 + ACT_001 + EF_001 + IDF_001..005 + FF_001 + FAC_001 + REP_001 + TIT_001 + PROG_001 + RES_001 + TDIL_001 + SR11). **Per-reality author-declared** per PO-A6 — RealityManifest.onboarding_config OPTIONAL V1 (modes_enabled subset + canonical_pcs ref list + ai_assistant_enabled bool + default_spawn_cell). **Schema-stable / activation-deferred V1+ discipline (PO-A8)**: actor_user_session.onboarding_draft + 2 EVT-T8 sub-shapes (Forge:CreateOnboardingDraft + Forge:UpdateOnboardingDraft) schema-reserved V1; activation V1+30d per PO-D3 (auto-save). **RESOLVES**: PCS-D1 (V1+ runtime login flow PC creation; full V1) + PCS-D10 (V1+ PO_001 Player Onboarding integration; full V1). RealityManifest extensions `onboarding_config: Option<OnboardingConfigDecl>` + `canonical_pcs: Vec<ActorRef>` BOTH OPTIONAL V1. EVT-T8 Forge:CompleteOnboarding V1 active (Forge:CreateOnboardingDraft + Forge:UpdateOnboardingDraft V1+30d active per PO-D3). EVT-T1 OnboardingCompleted narrative milestone (LLM scene narration context). 7 V1 reject rule_ids in `onboarding.*` namespace + 5 V1+ reservations. §10 acceptance: 12 V1-testable AC-PO-1..12 + 4 V1+ deferred. 12 deferrals (PO-D1..PO-D12). See [PO_001 §3.1](../features/03_player_onboarding/PO_001_player_onboarding.md#31-actor_user_session-t2--reality-scope-sparse--primary). |
| `session_participation` | T2 / Reality | **DF05_001 Session/Group Chat Foundation** (DRAFT 2026-04-27) | Per-(session, actor) sparse edge aggregate tracking participation lifecycle. **Composite key** `(session_id, actor_id)` — duplicate write rejects `session.participant_already_joined`. **2-variant ParticipantRole** V1: Anchor (PC creator; exactly 1 per session enforced via DF5-C1 cross-validator) / Joined (NPC accepted invite OR V2+ PC2 joins existing). **3-variant PresenceState** V1 per Q10 LOCKED: Connected (active) / Disconnected (WebSocket lost; in 30s wall-clock grace window per Q10-D1) / Left (formally departed; left_fiction_time populated). **6-variant LeftReason** V1: Explicit (/leave or close-chat UI) / MovedCell (PL_001 §13 travel cascade) / Inactive (V1+30d auto-detect) / SessionClosed (cascade) / AnchorPcLeft (forced exit when last PC leaves) / Kicked (V1 Forge OR V1+ DF4 rule). **Disconnect grace 30 wall-seconds** per Q10-D5 (NOT fiction-time; disconnect = real-world infra event); reconnect within window restores presence=Connected; timeout fires grace_timeout_fired marking Left with LeftReason=Inactive + check_anchor_invariant. **Forge override** `Forge:KickFromSession { actor_id, force=true }` skips 30s timer per Q10-D4 (abuse mitigation). **Closed = immutable** — after parent session.state=Closed, no new participation writes; existing rows immutable except left_reason finalization. See [DF05_001 §3.2](../features/DF/DF05_session_group_chat/DF05_001_session_foundation.md#32-session_participation-t2--reality-sparse-per-session-actor--per-participant). |
| `actor_faction_reputation` | T2 / Reality | **REP_001 Reputation Foundation** (CANDIDATE-LOCK 2026-04-27 closure pass 4/4; was DRAFT 2026-04-27 — Tier 5 Actor Substrate post-FAC_001 priority; resolves FAC-D7) | Per-(actor, faction) reputation aggregate (PC + NPC; ActorId source EF_001 §5.1; FactionId from FAC_001). **Sparse storage V1** per Q2 LOCKED — only declared canonical seed rows + V1+ lazy-create on first runtime delta touch. Bounded `score: i16` in [-1000, +1000] per Q3 LOCKED. **8-tier engine-fixed ReputationTier** (display layer; not stored): Hated/Hostile/Unfriendly/Neutral/Friendly/Honored/Revered/Exalted; asymmetric thresholds (-1000..-501 / -500..-251 / -250..-101 / -100..+100 / +101..+250 / +251..+500 / +501..+900 / +901..+1000); Wuxia I18n display labels (Đại nghịch / Nghịch tặc / Kẻ thù / Người lạ / Đệ tử / Trưởng lão / Tôn sư / Đại Thánh nhân). Default Neutral (0) for missing rows V1 per Q4 REVISION; V1+ hybrid (membership-derived) via REP-D16 alongside Q6 cascade. **Mutable** via Apply events (canonical seed + Forge V1 active per Q5 LOCKED; runtime gameplay delta V1+). Synthetic actors forbidden V1 per Q9 LOCKED. Cross-reality forbidden V1 per Q8 LOCKED (V2+ Heresy). NO cascade V1 per Q6 LOCKED (V1+ via REP-D2 FactionDecl.rep_cascade_config). NO decay V1 per Q7 LOCKED (V1+ via REP-D3 FactionDecl.rep_decay_per_week). **3-layer separation discipline per Q10 LOCKED**: REP_001 actor_faction_reputation (per-(actor, faction) bounded standing) ≠ RES_001 SocialCurrency::Reputation (per-actor unbounded global "danh tiếng" sum) ≠ NPC_001 npc_pc_relationship_projection (per-(NPC, PC) personal opinion). **RESOLVES FAC-D7**. RealityManifest extension `canonical_actor_faction_reputations: Vec<ActorFactionReputationDecl>` OPTIONAL V1 (sparse opt-in; empty Vec valid for sandbox). 6 V1 reject rule_ids in `reputation.*` namespace + 4 V1+ reservations. §15 acceptance: 8 V1-testable AC-REP-1..8 + 4 V1+ deferred. 17 deferrals (REP-D1..REP-D17). See [REP_001 §3.1](../features/00_reputation/REP_001_reputation_foundation.md#31-actor_faction_reputation-t2--reality-scope--primary). |

---

## Schema / envelope ownership

These are SHARED across multiple features; each has a designated ENVELOPE owner who governs additive evolution.

| Schema | Envelope owner | Extending features | Extension rule |
|---|---|---|---|
| `TurnEvent` payload (EVT-T1 PlayerTurn / EVT-T2 NPCTurn) | **PL_001 Continuum** §3.5 | PL_002 (`command_kind`), NPC_002 (`reaction_intent`, `aside_target`, `action_kind`), WA_006 (provisional `outcome`) | See [`02_extension_contracts.md` §1](02_extension_contracts.md#1-turnevent-envelope) — additive only per foundation I14; envelope versioned `TurnEventSchema = N`. |
| `RealityManifest` | **(unowned — needs split)** ⚠ | PL_001 Continuum (starting_fiction_time + root_channel_tree), **ACT_001 Actor Foundation (canonical_actors: Vec<CanonicalActorDecl> with chorus_metadata extension fields, REQUIRED V1, OWNERSHIP TRANSFERRED FROM PL_001+NPC_001 → ACT_001 2026-04-27 unification)**, WA_001 (LexConfig), WA_002 (contamination_allowances), WA_006 (mortality_config provisional), **PF_001 Place Foundation (places: Vec<PlaceDecl>, REQUIRED V1, added 2026-04-26)**, **MAP_001 Map Foundation (map_layout: Vec<MapLayoutDecl> + travel_defaults: TravelDefaults, REQUIRED V1, added 2026-04-26)**, **CSC_001 Cell Scene Composition (scene_skeleton_overrides: HashMap<ChannelId, SkeletonId>, OPTIONAL V1, added 2026-04-26)**, **RES_001 Resource Foundation (resource_kinds: Vec<ResourceKindDecl> + currencies: Vec<CurrencyDecl> + vital_profiles: Vec<VitalProfileDecl> + producers: Vec<ProducerProfile> + prices: Vec<PriceDecl> + cell_storage_caps: HashMap<PlaceTypeRef, u64> + cell_maintenance_profiles: HashMap<PlaceTypeRef, MaintenanceCost> + initial_resource_distribution: Vec<InitialDistributionDecl> + social_initial_distribution: HashMap<ActorRef, i64>, ALL OPTIONAL V1 with engine defaults, added 2026-04-26)**, **NPC_003 NPC Desires (npc_desires field migrated 2026-04-27 ACT_001 unification → desires field on actor_chorus_metadata; CanonicalActorDecl.chorus_metadata.desires)**, **REP_001 Reputation Foundation (canonical_actor_faction_reputations: Vec<ActorFactionReputationDecl>, OPTIONAL V1, added 2026-04-27)**, **PCS_001 PC Substrate (CanonicalActorDecl additive fields body_memory_init: Option<PcBodyMemory> + user_id_init: Option<UserId>; both REQUIRED for kind=Pc V1; sparse PC-only; added 2026-04-27 per Q3+Q5+Q6 LOCKED)** | See [`02_extension_contracts.md` §2](02_extension_contracts.md#2-realitymanifest) — proposal: extract to a new `IF_001_reality_manifest.md` infrastructure feature. |
| `ForgeEditAction` enum | **WA_003 Forge** §7 | PLT_001 Charter (CharterInvite/Accept/...), PLT_002 Succession (Succession*), WA_006 Mortality (MortalityConfig edits — provisional) | Closed enum extended via additive variants per I14. |
| Capability JWT (`forge.roles` + `forge.roles_version`) | **PLT_001 Charter** §6.3 | extended by PLT_002 (RealityOwner role), WA_006 (provisional MortalityAdmin) | Borderline with auth-service — flag for review when auth-service contributes. |
| `EVT-T8 Administrative` sub-shapes (was AdminAction; renamed in Option C redesign 2026-04-25) | event-model agent (Phase 2) | WA_003 (`ForgeEdit`), PLT_001 (`Charter*`), PLT_002 (`Succession*`), WA_006 (`MortalityAdminKill` provisional), core-admin (`Pause`/`Resume`/`ForceEndScene`/`WorldRuleOverride`) | Each feature DECLARES its sub-shapes; event-model `_index.md` lists current union. EVT-A11 mandates exactly-one ownership. |
| **EVT-T1 Submitted sub-types** (Option C redesign 2026-04-25 + PL_005 Interaction added 2026-04-26 DRAFT; **PL_005 CANDIDATE-LOCK 2026-04-26 PL folder closure**) | 07_event_model envelope; sub-types feature-owned | PL_001 + PL_002 own `PCTurn` (Speak/Action/MetaCommand/FastForward); NPC_001 + NPC_002 own `NPCTurn`; **PL_005 Interaction** owns `Interaction:Speak` / `Interaction:Strike` / `Interaction:Give` / `Interaction:Examine` / `Interaction:Use` (5 V1 kinds; V1+ Collide/Shoot/Cast/Embrace/Threaten reserved). PL_005 §16 acceptance: 6 V1 scenarios AC-INT-1..6. Phase 3 cleanup added ExamineTarget enum (V1: Place via PlaceId; V1+: MapNode at non-cell tier) — resolves PF-Q4 + MAP-Q3 watchpoints. Stage 3.5 group integration in §10 sequence + §9 reject path namespace split. 11 deferrals INT-D1..D11. Future quest-engine owns `QuestOutcome`. | Per EVT-A11 sub-type ownership discipline. Replaces former T2 NPCTurn category. |
| **EVT-T3 Derived sub-types** (Option C redesign 2026-04-25; EF_001 transfer 2026-04-26; PF_001 + PlaceDestroyed cascade-trigger added 2026-04-26 Phase 3) | 07_event_model envelope; sub-discriminator = `aggregate_type` (most cases) + dedicated cascade-trigger sub-shapes (cross-feature signals) | Each aggregate-owner feature owns its delta-kinds (PL_001 owns fiction_clock/scene_state/participant_presence — `actor_binding` transferred to **EF_001 as `entity_binding`** 2026-04-26 + new `entity_lifecycle_log`; NPC_001 owns npc_pc_relationship_projection/npc_node_binding; PL_006 owns actor_status; **PF_001 owns `aggregate_type=place` + dedicated `PlaceDestroyed` cascade-trigger sub-shape with occupants list — see PF_001 §7+§9 for cascade ordering contract**; etc.). Calibration sub-shapes (DayPasses/MonthPasses/YearPasses) owned by PL_001 (derived from FictionClock advance). | Replaces former T7 CalibrationEvent category — calibration is mechanically Derived. Cross-feature cascade-trigger sub-shapes (e.g., PlaceDestroyed) are EVT-T3 sub-shapes that consumer features subscribe to for explicit cascade contracts; alternative would be generic delta-filtering subscribe pattern, but explicit signals reduce implicit coupling. |
| **EVT-T4 System sub-type `EntityBorn`** (EF_001 CANDIDATE-LOCK 2026-04-26) | 07_event_model envelope; sub-type feature-owned | **EF_001 Entity Foundation** owns `EntityBorn { entity_id, entity_type, cell_id }` emitted at canonical seed (RealityBootstrapper) and runtime spawn (world-service). Cell membership emitted alongside via DP-A18 MemberJoined. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T4 System sub-type `PlaceBorn`** (PF_001 CANDIDATE-LOCK 2026-04-26) | 07_event_model envelope; sub-type feature-owned | **PF_001 Place Foundation** owns `PlaceBorn { place_id, place_type, parent_channel }` emitted at RealityManifest bootstrap (one per cell-tier channel) and V1+ runtime spawn via Forge. Cascades cell-channel `MemberJoined` for canonical actors who start at this cell. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shape `Forge:EditPlace`** (PF_001 CANDIDATE-LOCK 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **PF_001 Place Foundation** owns `Forge:EditPlace { place_id, edit_kind, before, after }`. Uses `forge_audit_log` (WA_003-owned) for audit trail. Edit kinds V1: UpdateNarrativeDrift / UpdateStructuralState / UpdateConnections / UpdateFixtureSeed / Rename / ChangePlaceType. AC-PF-8 covers 3-write transaction atomicity (place row + EVT-T8 emit + forge_audit_log entry). | Joins existing EVT-T8 sub-shape registry alongside Charter*/Succession*/MortalityAdminKill. |
| **EVT-T4 System sub-type `LayoutBorn`** (MAP_001 CANDIDATE-LOCK 2026-04-26) | 07_event_model envelope; sub-type feature-owned | **MAP_001 Map Foundation** owns `LayoutBorn { channel_id, tier, has_tier_metadata }` emitted at RealityManifest bootstrap (one per channel; runs after PF_001 PlaceBorn at cell tier per PL_001 §16.2 step ordering) and V1+ runtime spawn via Forge. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shape `Forge:EditMapLayout`** (MAP_001 CANDIDATE-LOCK 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **MAP_001 Map Foundation** owns `Forge:EditMapLayout { channel_id, edit_kind, before, after }`. Uses `forge_audit_log` (WA_003-owned) for audit trail. Edit kinds V1: UpdatePosition / UpdateTierMetadata / UpdateConnections / UpdateImageAsset / Rename. AC-MAP-10 covers 3-write transaction atomicity. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T4 System sub-type `SceneLayoutBorn`** (CSC_001 CANDIDATE-LOCK 2026-04-26) | 07_event_model envelope; sub-type feature-owned | **CSC_001 Cell Scene Composition** owns `SceneLayoutBorn { channel_id, skeleton_id, procedural_seed }` emitted at first cell entry (lazy compute) or RealityManifest bootstrap (eager option). One per cell-tier channel. Layer 1+2 deterministic; cached forever until structural change. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shape `Forge:EditCellScene`** (CSC_001 CANDIDATE-LOCK 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **CSC_001 Cell Scene Composition** owns `Forge:EditCellScene { channel_id, edit_kind, before, after }`. Uses `forge_audit_log` (WA_003-owned). Edit kinds V1: ChangeSkeleton / RerollSeed / ForceLayer3Refresh / ForceLayer4Refresh / ResetToCanonicalDefaults. AC-CSC-9 covers 3-write transaction atomicity. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T4 System sub-type `RaceBorn`** (IDF_001 DRAFT 2026-04-26) | 07_event_model envelope; sub-type feature-owned | **IDF_001 Race Foundation** owns `RaceBorn { reality_id, actor_id, race_id }` emitted at canonical seed (RealityBootstrapper) per actor; emitted ALONGSIDE EF_001 EntityBorn (RaceBorn carries race; EntityBorn carries entity_type + cell_id). | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shape `Forge:EditRaceAssignment`** (IDF_001 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **IDF_001 Race Foundation** owns `Forge:EditRaceAssignment { actor_id, before_race_id, after_race_id, reason }`. Uses `forge_audit_log` (WA_003-owned). V1 single edit kind: race override. AC-RAC-9 covers 3-write transaction atomicity (race_assignment row + EVT-T8 emit + forge_audit_log entry). | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T8 Administrative sub-shape `Forge:EditLanguageProficiency`** (IDF_002 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **IDF_002 Language Foundation** owns `Forge:EditLanguageProficiency { actor_id, language_id, axis: ProficiencyAxis, before_level, after_level }`. Uses `forge_audit_log` (WA_003-owned). V1 axes: Read/Write/Speak/Listen. AC-LNG-9 covers 3-write transaction atomicity. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T8 Administrative sub-shape `Forge:EditPersonality`** (IDF_003 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **IDF_003 Personality Foundation** owns `Forge:EditPersonality { actor_id, before_archetype, after_archetype, reason }`. Uses `forge_audit_log` (WA_003-owned). V1 single edit kind: archetype override. AC-PRS-9 covers atomicity. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T8 Administrative sub-shape `Forge:EditOrigin`** (IDF_004 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **IDF_004 Origin Foundation** owns `Forge:EditOrigin { actor_id, edit_kind, before, after, reason }`. Uses `forge_audit_log` (WA_003-owned). V1 edit kinds: UpdateLineage / UpdateBirthplace / UpdateNativeLanguage / UpdateDefaultIdeologyRefs. AC-ORG-9 covers atomicity. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T8 Administrative sub-shape `Forge:EditIdeologyStance`** (IDF_005 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **IDF_005 Ideology Foundation** owns `Forge:EditIdeologyStance { actor_id, edit_kind, before, after, reason }`. Uses `forge_audit_log` (WA_003-owned). V1 edit kinds: ApplyIdeology / DropIdeology / AdjustFervor / ReplaceStance. AC-IDL-10 covers atomicity. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T4 System sub-type `FamilyBorn`** (FF_001 DRAFT 2026-04-26) | 07_event_model envelope; sub-type feature-owned | **FF_001 Family Foundation** owns `FamilyBorn { actor_id, parent_refs, dynasty_id }` emitted at canonical seed (RealityBootstrapper) per actor; emitted alongside EF_001 EntityBorn + IDF_001 RaceBorn. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shape `Forge:EditFamily`** (FF_001 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **FF_001 Family Foundation** owns `Forge:EditFamily { actor_id, edit_kind, before, after, reason }`. Uses `forge_audit_log` (WA_003-owned). V1 edit kinds: AddParent / RemoveParent / AddSpouse / RemoveSpouse / AddChild / RemoveChild / MarkDeceased / SetDynasty. Bidirectional sync validated. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T8 Administrative sub-shape `Forge:RegisterDynasty`** (FF_001 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shape | **FF_001 Family Foundation** owns `Forge:RegisterDynasty { dynasty_id, display_name, founder, reason }`. Uses `forge_audit_log`. V1 single edit kind: register dynasty (V1+ traditions/perks updates). | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T4 System sub-types `FactionBorn` + `FactionMembershipBorn`** (FAC_001 DRAFT 2026-04-26) | 07_event_model envelope; sub-types feature-owned | **FAC_001 Faction Foundation** owns `FactionBorn { faction_id, faction_kind, roles_count }` (per-faction at canonical seed) + `FactionMembershipBorn { actor_id, faction_id, role_id, rank }` (per-actor at canonical seed). Both emitted by Bootstrap role at canonical seed; FactionMembershipBorn for actors with non-empty memberships only. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shapes `Forge:RegisterFaction` + `Forge:EditFaction` + `Forge:EditFactionMembership`** (FAC_001 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shapes | **FAC_001 Faction Foundation** owns 3 AdminAction sub-shapes: `Forge:RegisterFaction { faction_id, faction_decl, reason }` (V1 register new faction); `Forge:EditFaction { faction_id, edit_kind, before, after, reason }` (V1 edit kinds: SetCurrentHead / EditDefaultRelations / EditRoles); `Forge:EditFactionMembership { actor_id, edit_kind, before, after, reason }` (V1 edit kinds: JoinFaction / LeaveFaction / ChangeRole / ChangeRank / SetMaster / RemoveMaster). Uses `forge_audit_log` (WA_003-owned). 3-write atomic pattern. WA_003 closure folds into ForgeEditAction enum. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T4 System sub-type `ReputationBorn`** (REP_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-type feature-owned | **REP_001 Reputation Foundation** owns `ReputationBorn { actor_id, faction_id, initial_score }` emitted at canonical seed (RealityBootstrapper) per declared row in `canonical_actor_faction_reputations`. Sparse storage discipline — only declared rows produce events; missing pairs implicit Neutral default per Q4 LOCKED. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shapes `Forge:SetReputation` + `Forge:ResetReputation`** (REP_001 DRAFT 2026-04-27) | event-model agent (Phase 2) — feature-defined sub-shapes | **REP_001 Reputation Foundation** owns 2 AdminAction sub-shapes per Q5 LOCKED: `Forge:SetReputation { actor_id, faction_id, before_score, after_score, reason }` (V1 set absolute score; clamps to [-1000, +1000]); `Forge:ResetReputation { actor_id, faction_id, before_score, reason }` (V1 reset to Neutral 0; primary use case = Skyrim-style bounty forgiveness). Uses `forge_audit_log` (WA_003-owned). 3-write atomic pattern. WA_003 closure folds into ForgeEditAction enum. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T3 Derived sub-type — REP_001 V1+ delta_kinds reserved** (REP_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-discriminator = `aggregate_type=actor_faction_reputation` | **REP_001 Reputation Foundation** reserves 3 V1+ delta_kinds (NOT V1 active per Q5 LOCKED): `Delta { score_change, source }` (V1+ runtime gameplay delta — PL_005 Strike on faction member, etc.); `CascadeDelta { score_change, source_event, source_faction }` (V1+ enrichment per Q6 LOCKED via REP-D2 FactionDecl.rep_cascade_config); `DecayTick { score_change }` (V1+ enrichment per Q7 LOCKED via REP-D3 FactionDecl.rep_decay_per_week). All 3 V1+ delta_kinds ship together as "V1+ runtime reputation milestone" coupled per Q5+Q6+Q7 V1+ enrichment cohesion. | Per EVT-A11 sub-type ownership discipline. V1 ships canonical seed + Forge admin only; runtime gameplay V1+. |
| **EVT-T4 System sub-types `ActorBorn` + `ActorChorusMetadataBorn`** (ACT_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-types feature-owned | **ACT_001 Actor Foundation** owns `ActorBorn { actor_id, kind: ActorKind, traits_summary }` (per actor at canonical seed; PC + NPC; Synthetic excluded V1) + `ActorChorusMetadataBorn { actor_id }` (per NPC at canonical seed; sparse — PCs not populated V1; populated V1+ when AI-controls-PC-offline activates). Both emitted by Bootstrap role (RealityBootstrapper). | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shapes `Forge:EditActorCore` + `Forge:EditChorusMetadata` + `Forge:EditActorOpinion` + `Forge:EditActorSessionMemory`** (ACT_001 DRAFT 2026-04-27) | event-model agent (Phase 2) — feature-defined sub-shapes | **ACT_001 Actor Foundation** owns 4 AdminAction sub-shapes per Q5 LOCKED: `Forge:EditActorCore { actor_id, edit_kind, before, after, reason }` (V1 edit kinds: UpdateMood / UpdateFlexibleState / UpdateCoreBeliefs); `Forge:EditChorusMetadata { actor_id, edit_kind, before, after, reason }` (V1 edit kinds: UpdateGreetingObligation / UpdatePriorityTierHint / AddDesire / RemoveDesire); `Forge:EditActorOpinion { observer_actor_id, target_actor_id, edit_kind, before, after, reason }` (V1 edit kinds: UpdateTrust / UpdateFamiliarity / SetStanceTags); `Forge:EditActorSessionMemory { actor_id, session_id, edit_kind, before, after, reason }` (V1 edit kinds: AppendFact / RewriteSummary / EvictFact). Uses `forge_audit_log` (WA_003-owned). 3-write atomic pattern. WA_003 closure folds into ForgeEditAction enum. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T3 Derived sub-types — ACT_001 session-end derivation** (ACT_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-discriminator = `aggregate_type=actor_actor_opinion` + `aggregate_type=actor_session_memory` | **ACT_001 Actor Foundation** owns 2 V1 active delta_kinds (preserved from NPC_001 §13 session-end derivation pattern; aggregate_type renamed): `Update` for `actor_actor_opinion` (per (observer, target) trust+familiarity+stance_tags update); `Update` for `actor_session_memory` (per (actor, session) fact append + summary rewrite). V1+ delta_kinds reserved: `ActorControlSourceChange { actor_id, before, after }` (V1+ AI-controls-PC-offline transition per ACT-D1) + bilateral V1+ patterns (PC→NPC + NPC→NPC + PC→PC per ACT-D2..D4). REPLACES NPC_001 §13 EVT-T3 entries (npc_pc_relationship_projection + npc_session_memory) — bilateral key composite enables V1+ pattern expansion. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T4 System sub-type `PcRegistered`** (PCS_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-type feature-owned | **PCS_001 PC Substrate** owns `PcRegistered { pc_id, body_memory, user_id }` (per PC at canonical seed; sparse PC-only; emitted by Bootstrap role AFTER ACT_001 ActorBorn). Carries PcBodyMemory canonical state + user_id (Option; None at canonical seed typically). | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shapes `Forge:RegisterPc` + `Forge:BindPcUser` + `Forge:EditPcUserBinding` + `Forge:EditBodyMemory` + `Forge:EditPcMortalityState`** (PCS_001 DRAFT 2026-04-27) | event-model agent (Phase 2) — feature-defined sub-shapes | **PCS_001 PC Substrate** owns 5 V1 AdminAction sub-shapes per Q3+Q5+Q7 LOCKED: `Forge:RegisterPc { pc_id, body_memory_init, user_id_init?, spawn_cell }` (V1 register new PC at runtime); `Forge:BindPcUser { pc_id, user_id, before, after, reason }` (V1 bind user to existing PC); `Forge:EditPcUserBinding { pc_id, edit_kind, before, after, reason }` (V1 edit kinds: UpdateUserId / UpdateCurrentSession / ClearSession); `Forge:EditBodyMemory { pc_id, edit_kind, before, after, reason }` (V1 edit kinds: UpdateLeakagePolicy / UpdateSoulKnowledgeTags / UpdateBodyKnowledgeTags / UpdateOriginWorldRef / UpdateHostBodyRef); `Forge:EditPcMortalityState { pc_id, edit_kind, before, after, reason }` (V1 edit kinds: SetState / AppendHistory). Uses `forge_audit_log` (WA_003-owned). 3-write atomic pattern. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T3 Derived sub-types — PCS_001 mortality transitions** (PCS_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-discriminator = `aggregate_type=pc_mortality_state` | **PCS_001 PC Substrate** owns 3 V1 active delta_kinds per Q7 LOCKED: `DyingTransition { pc_id, will_respawn_at_fiction_time, spawn_cell, trigger }` (Alive → Dying when mortality_config.mode = RespawnAtLocation; V1 schema entry; flow exit V1+ via PCS-D2); `DeathTransition { pc_id, died_at_turn, died_at_cell, trigger }` (Alive → Dead when mortality_config.mode = Permadeath); `GhostTransition { pc_id, died_at_turn, died_at_cell, trigger }` (Alive → Ghost when mortality_config.mode = Ghost). 3 V1+ delta_kinds reserved: `RespawnTransition { pc_id, respawn_cell }` (PCS-D2; Dying → Alive); `ResurrectionTransition { pc_id, ... }` (PCS-D-N; Ghost → Alive rare wuxia); `GhostDispersedTransition { pc_id }` (PCS-D-N; Ghost → Dead). Triggered by PROG_001 strike (HP=0 DamageOverflow) + RES_001 vital_pool (PoisonStarvation) + Forge admin (ForgeAdmin). | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T4 System sub-type `TitleGranted`** (TIT_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-type feature-owned | **TIT_001 Title Foundation** owns `TitleGranted { actor_id, title_id, granted_at_fiction_ts, granted_via }` (per declared TitleHoldingDecl row at canonical seed; runtime via Forge:GrantTitle admin; runtime via SuccessionCascade cross-aggregate validator). granted_via 3-variant V1 enum: CanonicalSeed / ForgeAdmin / SuccessionCascade (V1+ QuestReward + FactionElectVote reserved). Carries TitleId + ActorId + fiction_ts. Emitted alongside atomic FAC_001 actor_faction_membership.role_id update when TitleAuthorityDecl.faction_role_grant is Some (3-write atomic per WA_003 Forge pattern). | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shapes `Forge:GrantTitle` + `Forge:RevokeTitle` + `Forge:DesignateHeir`** (TIT_001 DRAFT 2026-04-27) | event-model agent (Phase 2) — feature-defined sub-shapes | **TIT_001 Title Foundation** owns 3 V1 AdminAction sub-shapes per Q6 C + Q8 A LOCKED: `Forge:GrantTitle { actor_id, title_id, designated_heir, reason }` (V1 admin runtime grant; validates binding constraints + multi_hold + exclusive); `Forge:RevokeTitle { actor_id, title_id, reason, trigger_succession }` (V1 admin removal; trigger_succession=true cascades; false applies VacancySemantic); `Forge:DesignateHeir { actor_id, title_id, new_heir, reason }` (V1 runtime heir change for Designated SuccessionRule). Uses `forge_audit_log` (WA_003-owned). 3-write atomic pattern. WA_003 closure folds into ForgeEditAction enum. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T3 Derived sub-type `TitleSuccessionTriggered`** (TIT_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-discriminator = `aggregate_type=actor_title_holdings` | **TIT_001 Title Foundation** owns `TitleSuccessionTriggered { from_actor_id, to_actor_id: Option<ActorId>, title_id, trigger_reason, fiction_ts, source_event_id }` per Q7 A LOCKED (immediate cascade on WA_006 mortality EVT-T3). SuccessionTriggerReason 4-variant V1 enum: HolderDeceased (primary trigger via WA_006 cascade) / HolderRevoked (Forge:RevokeTitle admin) / HeirIneligible (designated heir died or doesn't meet criteria) / NoEligibleHeir (Vacate semantic or empty dynasty); V1+ FactionVoteCompleted reserved (TIT-D1). Sparse — only fires on cascade events; carries causal-ref to source WA_006 mortality EVT-T3 per EVT-A6. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T4 System sub-type `SessionBorn`** (DF05_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-types feature-owned | **DF05_001 Session/Group Chat Foundation** owns `SessionBorn { session_id: SessionId, channel_id: ChannelId, anchor_pc_id: ActorId }` (per session at canonical seed V1+ author-scripted; runtime PC-trigger uses EVT-T3 Derived `aggregate_type=session` Born variant instead). Emitted by RealityBootstrapper for canonical sessions declared in `RealityManifest.canonical_sessions` (OPTIONAL V1; sparse opt-in). | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T8 Administrative sub-shapes — DF05_001 9 V1 Forge AdminActions** (DF05_001 DRAFT 2026-04-27) | event-model agent (Phase 2) — feature-defined sub-shapes | **DF05_001 Session/Group Chat Foundation** owns 9 V1 AdminAction sub-shapes per Q11 LOCKED: `Forge:CreateSession { channel_id, anchor_pc_id, initial_participants, reason }` (V1 author canonical seed at bootstrap OR runtime); `Forge:CloseSession { session_id, reason }` (V1 force-close active session); `Forge:KickFromSession { session_id, actor_id, force, reason }` (V1 eject participant; force=true skips 30s grace per Q10-D4); `Forge:EditActorSessionMemory { actor_id, session_id, edit_kind, before, after, reason }` (pre-close edits OK per Q11-D1; V1 edit kinds: Add/Remove/ModifyFact/AdjustSalience/EditI18nString); `Forge:RegenSessionDistill { session_id, reason }` (post-close regen per Q11-D2; re-runs LLM POV-distill for all participants; bumps prompt_template_version); `Forge:PurgeActorSessionMemory { actor_id, session_id, reason }` (post-close GDPR per Q11-D3; deletes all distilled facts for one actor); `Forge:AnonymizePcInSessions { actor_id, reason }` (post-close GDPR cascade per Q11-D3; replaces PC name in OTHER actors' memories with "kẻ lạ"); `Forge:BulkRegenSessionDistill { reality_id, session_filter, prompt_template_version, reason }` (bulk reality-scoped per Q11-D4); `Forge:BulkPurgeStaleSessions { reality_id, before_fiction_time, reason }` (admin cleanup reality-scoped per Q11-D4). All 9 use `forge_audit_log` (WA_003-owned) — audit-grade retention (cannot delete audit layer). 3-write atomic pattern (aggregate write + EVT-T8 emit + forge_audit_log entry). WA_003 closure folds into ForgeEditAction enum. | Joins existing EVT-T8 sub-shape registry. |
| **EVT-T3 Derived sub-types — DF05_001 session lifecycle** (DF05_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-discriminator = `aggregate_type=session` + `aggregate_type=session_participation` | **DF05_001 Session/Group Chat Foundation** owns 4 V1 active delta_kinds: `Born` for `aggregate_type=session` (runtime PC-trigger creation per `/chat` command); `ClosingTransition` for `aggregate_type=session` (anchor leave detected per DF5-A4; emits PRE-distill); `ClosedTransition` for `aggregate_type=session` (POST-distill terminal marker; aggregate state set to Closed per DF5-A7); `Born` for `aggregate_type=session_participation` (actor joins session); `Update` for `aggregate_type=session_participation` (LeftTransition with left_fiction_time + left_reason populated; presence transitions Connected→Disconnected→Left). PLUS POV-distill cache: `Update` for `aggregate_type=actor_session_memory` with `update_kind=SessionPovDistill` payload `{ actor_id, session_id, facts: Vec<MemoryFact>, llm_model_id, prompt_template_version, provider_id, generated_at_fiction_time, generated_at_wall_time, input_token_count, output_token_count, attempt_count }` per Q12-D1 LOCKED full JSON cache (no compression V1) for replay-determinism per Q12-D3 LOCKED (replay reads cache; never re-LLM-calls). Cache invalidation triggers per Q12-D2: prompt_template_version mismatch + llm_model_id deprecated + Forge:RegenSessionDistill admin. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T1 Submitted sub-type `TitleSuccessionCompleted`** (TIT_001 DRAFT 2026-04-27) | 07_event_model envelope; sub-type feature-owned | **TIT_001 Title Foundation** owns `TitleSuccessionCompleted { actor_id, title_id, fiction_ts }` narrative milestone event per Q8 A + narrative_hint LOCKED (mirrors PcTransmigrationCompleted pattern). V1 ACTIVE — emitted on every successful succession (after TitleSuccessionTriggered + role grant + state update commit). LLM uses for dramatic narration ("Lý Tử kế nhiệm Đông Hải Đạo Cốc Chưởng Môn"). Subscribers: NPC_001 persona AssemblePrompt re-renders title in briefing + LLM narration via TitleAuthorityDecl.narrative_hint I18nBundle. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T1 Submitted sub-type `PcTransmigrationCompleted`** (PCS_001 DRAFT 2026-04-27 — renamed from PcXuyenKhongCompleted per user direction 2026-04-27; English type name; Vietnamese term "xuyên không" preserved as parenthetical narrative annotation) | 07_event_model envelope; sub-type feature-owned | **PCS_001 PC Substrate** owns `PcTransmigrationCompleted { old_actor_id, new_pc_id, body_actor_id, soul_origin_ref: Option<GlossaryEntityId>, body_memory_init: PcBodyMemory, soul_clock_at_split: i64, body_clock_at_split: i64, split_at_turn: u64, split_reason: TransmigrationReason }` per Q10 LOCKED. TransmigrationReason 4-variant V1: SuddenDeath / Lightning / Sleep / AuthorDeclared { reason: I18nBundle }; V1+ Reincarnation (PCS-D8) + Possession (PCS-D9). **SCHEMA ACTIVE V1; runtime emission V1+ deferred** (PCS-D-N). Canonical seed declares pre-split state V1 (no transition event needed). Subscribers: TDIL_001 actor_clocks (clock-split per TDIL §10) + RES_001 vital_pool/resource_inventory/entity_binding cell_owner (inheritance per RES §5.3) + WA_006 mortality_config (V1+ origin actor death cross-reality coordination). | Per EVT-A11 sub-type ownership discipline. Aggregate-Owner discipline preserved (TDIL_001 owns actor_clocks; PCS_001 emits domain event). |
| **EntityKind trait** + **EntityBindingExt** (EF_001 CANDIDATE-LOCK 2026-04-26) | **EF_001 Entity Foundation** §4 | PCS_001 implements `EntityKind for Pc` · NPC_001 implements `EntityKind for Npc` · future Item feature implements `EntityKind for Item` · future EnvObject feature implements `EntityKind for EnvObject`. Each consumer MUST implement `type_default_affordances(&self)` (no default impl; `&self` for dyn-dispatch) — forces explicit affordance declaration. | EntityKind: 4 body-only methods (`entity_id` · `entity_type` · `type_default_affordances` · `display_name`). EntityBindingExt: 2 binding-side methods (`lifecycle_state` · `effective_affordances`). Phase 3 cleanup split body-vs-binding properties for cleaner dyn dispatch. See [EF_001 §4](../features/00_entity/EF_001_entity_foundation.md#4-entitykind-trait-specification). |
| **EVT-T5 Generated sub-types** (Option C redesign 2026-04-25) | 07_event_model envelope; sub-types feature-owned | future gossip aggregator owns `BubbleUp:RumorBubble`; future world-rule-scheduler owns `Scheduled:NPCRoutine` (V1+30d) + `Scheduled:WorldTick` (V1+30d); future quest-engine owns `Scheduled:QuestTrigger`; future combat owns RNG-based generators | Replaces former T9 QuestBeat:Trigger / T10 NPCRoutine / T11 WorldTick categories — all three were Generated mechanism. EVT-A9 RNG determinism applies. |
| **Generator Registry** (Phase 6 EVT-G1, 2026-04-25 late evening) | 07_event_model envelope; specific generators feature-owned | Each registered generator declared with `logical_id`=`(feature_owner:sub_type)`, `registry_uuid`=blake3(logical_id), `trigger_sources`, `output_category`, `rng_seed_strategy`, `capacity_ceiling`, `owner_service`. V1 generators (when implemented): future gossip aggregator (`gossip:RumorBubble`); future scheduler (`world-rule-scheduler:NPCRoutine`/`WorldTick`); future combat (`combat:DamageRoll`); future quest-engine (`quest:Trigger`). Coordinator role in-process per channel-writer (no new service V1) — see [EVT-G5](../07_event_model/12_generation_framework.md#evt-g5--coordinator-service-responsibilities--deployment). | Closes original-goal #4 ("generate event theo điều kiện + xác suất") at systematic level. EVT-A9 + EVT-A12 (f) operationalized. |
| `RejectReason` namespace prefixes | **PL_001 Continuum** §3.5 (envelope) | `lex.*` → WA_001, `heresy.*` → WA_002, `mortality.*` → WA_006 (provisional), `world_rule.*` → cross-cutting, `oracle.*` → 05_llm_safety A3, `canon_drift.*` → 05_llm_safety A6, `capability.*` → DP-K9, `parse.*` → PL_002, `chorus.*` → NPC_002, `forge.*` → WA_003, `charter.*` → PLT_001, `succession.*` → PLT_002, `interaction.*` → PL_005, `status.*` → PL_006, `entity.*` → EF_001, `place.*` → PF_001, `map.*` → MAP_001, `csc.*` → CSC_001, `resource.*` → RES_001, `race.*` → IDF_001, `language.*` → IDF_002, `personality.*` → IDF_003, `origin.*` → IDF_004, `ideology.*` → IDF_005, `family.*` → FF_001, `faction.*` → FAC_001, `progression.*` → PROG_001, `reputation.*` → REP_001, `actor.*` → ACT_001, `pc.*` → PCS_001, `title.*` → TIT_001, `session.*` → DF05_001, `onboarding.*` → PO_001 | Each feature owns its prefix; Continuum doesn't enumerate. **Path A tightening applied 2026-04-25 (commit f7c0a54).** RES_001 DRAFT 2026-04-26 also introduces `RejectReason.user_message: I18nBundle` envelope-level field per i18n contract — PL_001 closure pass folds in (additive per I14). |
| **i18n `I18nBundle` cross-cutting type** | **RES_001 Resource Foundation** §2 (introducer; engine standard going forward) | Used by RES_001 for: `CurrencyDecl.display_name` / `ResourceKindDecl.display_name` / `RejectReason.user_message` (envelope extension). Future features SHOULD use this pattern: English `snake_case` for stable IDs, `I18nBundle` for user-facing strings. Existing features (PL_006 / NPC_001 / NPC_002 / PL_002 / WA_*) currently use Vietnamese hardcoded reject copy — i18n cross-cutting audit deferred (low priority — cosmetic, doesn't block V1 functionality). | RES_001 is FIRST adopter; pattern propagates as future features ship. |
| **EVT-T3 Derived sub-types — RES_001 aggregates** (DRAFT 2026-04-26) | 07_event_model envelope; sub-discriminator = `aggregate_type` | **RES_001 Resource Foundation** owns `aggregate_type=vital_pool` (Vital delta — Hp/Stamina mutations from combat / status / regen) + `aggregate_type=resource_inventory` (balance delta from production / consumption / transfer). Replaces ad-hoc reference; explicit registration required for event-model agent's Phase 2 union. | Per EVT-A11 sub-type ownership discipline. |
| **EVT-T5 Generated sub-types — RES_001 4 V1 generators** (DRAFT 2026-04-26) | 07_event_model envelope; sub-types feature-owned | **RES_001 Resource Foundation** owns 4 V1 day-boundary Generators (all trigger via EVT-G2 `FictionTimeMarker`): `Scheduled:CellProduction` / `Scheduled:NPCAutoCollect` / `Scheduled:CellMaintenance` / `Scheduled:HungerTick`. Coordinator sequencing per EVT-G6: Production → AutoCollect → Maintenance → HungerTick. Determinism per EVT-A9 (RNG seeds blake3-derived from reality_id || day_marker || generator_kind). Cycle-free V1 (no inter-generator triggers within same day). | Joins existing EVT-T5 sub-type registry alongside future gossip/scheduler/quest/combat generators. |
| **EVT-T8 Administrative sub-shapes `Forge:EditCellProducerProfile`/`Forge:EditPriceDecl`/`Forge:EditCellMaintenanceCost`/`Forge:GrantInitialResources`** (RES_001 DRAFT 2026-04-26) | event-model agent (Phase 2) — feature-defined sub-shapes | **RES_001 Resource Foundation** owns 4 AdminAction sub-shapes for runtime resource configuration via WA_003 Forge. Use `forge_audit_log` (WA_003-owned). WA_003 closure folds into ForgeEditAction enum. | Joins existing EVT-T8 sub-shape registry. |

---

## Stable-ID prefix ownership

Per [`../00_foundation/06_id_catalog.md`](../00_foundation/06_id_catalog.md). Reproduced here for boundary-review convenience.

| Prefix | Owner | Status |
|---|---|---|
| `DP-A*` / `DP-T*` / `DP-R*` / `DP-S*` / `DP-K*` / `DP-C*` / `DP-X*` / `DP-F*` / `DP-Ch*` | 06_data_plane (LOCKED) | locked |
| `EVT-A1..A12` / `EVT-T1`/`T3`/`T4`/`T5`/`T6`/`T8` (active) + `EVT-T2`/`T7`/`T9`/`T10`/`T11` (`_withdrawn` 2026-04-25 Option C redesign per I15) / `EVT-P1`/`P3`/`P4`/`P5`/`P6`/`P8` (active) + `EVT-P2`/`P7`/`P9`/`P10`/`P11` (`_withdrawn`) / `EVT-V1..V7` / `EVT-L1..L19` / `EVT-S1..S6` / **`EVT-G1..G6`** (Phase 6 Generation Framework, locked 2026-04-25 late evening) / `EVT-Q*` | 07_event_model | Phase 1-6 LOCKED + Option C redesign 2026-04-25 |
| `MV*` / `MV12-D*` | 03_multiverse | locked |
| `R*` / `S*` / `C*` / `SR*` | 02_storage | locked |
| `I*` (foundation invariants) | 00_foundation | locked |
| `EF-*` (foundation tier; `EF-A*` axioms · `EF-D*` deferrals · `EF-Q*` open questions) | catalog/cat_00_EF_entity_foundation.md (added 2026-04-26) | EF_001 Entity Foundation |
| `PF-*` (foundation tier; `PF-A*` axioms · `PF-D*` deferrals · `PF-Q*` open questions) | catalog/cat_00_PF_place_foundation.md (added 2026-04-26) | PF_001 Place Foundation |
| `MAP-*` (foundation tier; `MAP-A*` axioms · `MAP-D*` deferrals · `MAP-Q*` open questions) | catalog/cat_00_MAP_map_foundation.md (added 2026-04-26) | MAP_001 Map Foundation |
| `CSC-*` (foundation tier; `CSC-A*` axioms · `CSC-D*` deferrals · `CSC-Q*` open questions) | catalog/cat_00_CSC_cell_scene_composition.md (added 2026-04-26) | CSC_001 Cell Scene Composition |
| `RES-*` (foundation tier; `RES-A*` axioms · `RES-D*` deferrals · `RES-Q*` open questions) | catalog/cat_00_RES_resource.md (added 2026-04-26) | RES_001 Resource Foundation |
| `RAC-*` (foundation tier — Tier 5 Actor Substrate; `RAC-A*` axioms · `RAC-D*` deferrals · `RAC-Q*` decisions) | catalog/cat_00_IDF_identity_foundation.md (added 2026-04-26 with IDF folder DRAFT) | IDF_001 Race Foundation |
| `LNG-*` (foundation tier — Tier 5 Actor Substrate; `LNG-A*` axioms · `LNG-D*` deferrals · `LNG-Q*` decisions) | catalog/cat_00_IDF_identity_foundation.md (shared with IDF_001) | IDF_002 Language Foundation |
| `PRS-*` (foundation tier — Tier 5 Actor Substrate; `PRS-A*` axioms · `PRS-D*` deferrals · `PRS-Q*` decisions) | catalog/cat_00_IDF_identity_foundation.md (shared with IDF_001/002) | IDF_003 Personality Foundation |
| `ORG-*` (foundation tier — Tier 5 Actor Substrate; `ORG-A*` axioms · `ORG-D*` deferrals · `ORG-Q*` decisions) | catalog/cat_00_IDF_identity_foundation.md (shared with IDF_001/002/003) | IDF_004 Origin Foundation |
| `IDL-*` (foundation tier — Tier 5 Actor Substrate; `IDL-A*` axioms · `IDL-D*` deferrals · `IDL-Q*` decisions) | catalog/cat_00_IDF_identity_foundation.md (shared with IDF_001/002/003/004) | IDF_005 Ideology Foundation |
| `FF-*` (foundation tier — Tier 5 Actor Substrate post-IDF; `FF-A*` axioms · `FF-D*` deferrals · `FF-Q*` decisions) | catalog/cat_00_FF_family_foundation.md (NOT YET CREATED — defer to first DRAFT-stable cycle) | FF_001 Family Foundation |
| `FAC-*` (foundation tier — Tier 5 Actor Substrate post-IDF + post-FF_001; `FAC-A*` axioms · `FAC-D*` deferrals · `FAC-Q*` decisions) | catalog/cat_00_FAC_faction_foundation.md (NOT YET CREATED — defer to first DRAFT-stable cycle) | FAC_001 Faction Foundation |
| `REP-*` (foundation tier — Tier 5 Actor Substrate post-FAC_001; `REP-A*` axioms · `REP-D*` deferrals · `REP-Q*` decisions) | catalog/cat_00_REP_reputation_foundation.md (added 2026-04-27 with REP_001 DRAFT) | REP_001 Reputation Foundation |
| `TIT-*` (foundation tier — Tier 5 Actor Substrate post-FF_001 + FAC_001 + REP_001; closes the political-rank triangle; `TIT-A*` axioms · `TIT-D*` deferrals · `TIT-Q*` decisions · `TIT-C*` cross-aggregate consistency rules) | catalog/cat_00_TIT_title_foundation.md (added 2026-04-27 with TIT_001 DRAFT) | TIT_001 Title Foundation |
| `PO-*` (first user-visible feature post-foundation closure; 3 V1 onboarding modes + AI-driven character creation; `PO-A*` axioms · `PO-D*` deferrals · `PO-Q*` decisions · `PO-C*` cross-aggregate consistency rules) | catalog/cat_03_PO_player_onboarding.md (replaces archived multiverse content 2026-04-27 with PO_001 DRAFT) | PO_001 Player Onboarding |
| `DF5-*` (V1-blocking biggest unknown RESOLVED 2026-04-27; multi-session-per-cell sparse architecture; `DF5-A*` axioms · `DF5-D*` deferrals · `DF5-Q*` open questions · `DF5-V*` validator slots · `DF5-C*` cross-aggregate consistency rules) | catalog/cat_18_DF5_session_group_chat.md (added 2026-04-27 with DF05_001 DRAFT) | DF05_001 Session/Group Chat Foundation |
| `ACT-*` (foundation tier — Tier 5 Actor Substrate; unification refactor 2026-04-27 replaces NPC_001 R8 anomaly; `ACT-A*` axioms · `ACT-D*` deferrals · `ACT-Q*` decisions) | catalog/cat_00_ACT_actor_foundation.md (added 2026-04-27 with ACT_001 DRAFT) | ACT_001 Actor Foundation |
| `PROG-*` (foundation tier — 6th V1 foundation; multi-genre dynamic progression substrate; `PROG-A*` axioms · `PROG-D*` deferrals · `PROG-Q*` decisions) | catalog/cat_00_PROG_progression.md (added 2026-04-26 with PROG_001 DRAFT) | PROG_001 Progression Foundation |
| `AIT-*` (architecture-scale; NOT foundation tier — 3-tier NPC architecture for billion-NPC scaling; `AIT-A*` axioms · `AIT-D*` deferrals · `AIT-Q*` decisions) | catalog/cat_16_AIT_ai_tier.md (added 2026-04-27 with AIT_001 DRAFT) | AIT_001 AI Tier Foundation |
| `TDIL-*` (architecture-scale; NOT foundation tier — 4-clock relativity model for cross-realm time + cultivation pace + xuyên không clock-split; `TDIL-A*` axioms · `TDIL-D*` deferrals · `TDIL-Q*` decisions) | catalog/cat_17_TDIL_time_dilation.md (added 2026-04-27 with TDIL_001 DRAFT) | TDIL_001 Time Dilation Foundation |
| `PL-*` / `WA-*` / `PO-*` / `NPC-*` / `PCS-*` / `SOC-*` / `NAR-*` / `EM-*` / `PLT-*` / `CC-*` / `DL-*` | catalog/cat_NN_*.md per category | each catalog file owns its prefix |
| `LX-D*` / `LX-Q*` | WA_001 | per-feature deferral IDs |
| `HER-D*` / `HER-Q*` | WA_002 | per-feature deferral IDs |
| `FRG-D*` / `FRG-Q*` | WA_003 | per-feature deferral IDs |
| `CHR-D*` / `CHR-Q*` | PLT_001 | per-feature deferral IDs |
| `SUC-D*` / `SUC-Q*` | PLT_002 | per-feature deferral IDs |
| `MOR-D*` / `MOR-Q*` | WA_006 | per-feature deferral IDs |
| `GR-D*` / `GR-Q*` | PL_002 | per-feature deferral IDs |
| `CST-D*` / `CST-Q*` | NPC_001 | per-feature deferral IDs |
| `CHO-D*` / `CHO-Q*` | NPC_002 | per-feature deferral IDs |
| `DSR-D*` / `DSR-Q*` | NPC_003 | per-feature deferral IDs (added 2026-04-26 — NPC Desires LIGHT) |

Per-feature `*-D*` / `*-Q*` IDs are deferral IDs scoped to the feature's design doc. They never collide because the prefix is feature-specific. Foundation I15 stable-ID-renaming rule applies.

---

## Drift watchpoints (cross-cutting)

These are documented mismatches that require cross-feature coordination to resolve. Each is owned by the feature that flagged it, but resolution may require boundary-folder edits.

| ID | Owner-flagger | Drift | Resolution path |
|---|---|---|---|
| **GR-D8** | PL_002 | Rejected-turn commit primitive (`t2_write` per PL_001 §15 vs `advance_turn` per EVT-T1 spec) | event-model agent Phase 2 to absorb per-outcome sub-spec |
| **CST-D1** | NPC_001 | `npc.current_session_id` semantic (R8 wording vs OOS-1 in DP); also see EF-Q2 — npc.current_region_id may migrate to entity_binding post-EF_001 | reconcile with 02_storage agent + EF_001 review |
| **LX-D5** | WA_001 | Lex slot ordering in EVT-V* | event-model agent Phase 3 |
| ~~**EF-Q3**~~ | EF_001 | ~~Validator slot ordering: EVT-V_entity_affordance vs EVT-V_lex~~ | **RESOLVED 2026-04-26 alignment review** → Stage 3.5.a per `_boundaries/03_validator_pipeline_slots.md` |
| ~~**PF-Q1**~~ | PF_001 | ~~Validator slot ordering: EVT-V_place_structural relative to EVT-V_entity_affordance + EVT-V_lex~~ | **RESOLVED 2026-04-26 alignment review** → Stage 3.5.b per `_boundaries/03_validator_pipeline_slots.md` |
| ~~**PF-Q4**~~ | PF_001 | ~~Place addressability: separate ExamineTarget discriminator (V1) vs 5th EntityId variant (V1+) — requires PL_005 closure-pass extension to add ExamineTarget enum~~ | **RESOLVED 2026-04-26 PL folder closure** → PL_005 §2 ExamineTarget enum (V1: Place via PlaceId) + §14.1 Examine Place sequence; V1+ collapse to `EntityId::Place` deferred per INT-D10 |
| ~~**MAP-Q1**~~ | MAP_001 | ~~Validator slot ordering: EVT-V_map_layout relative to EVT-V_place_structural + EVT-V_entity_affordance + EVT-V_lex~~ | **RESOLVED 2026-04-26 alignment review** → Stage 3.5.c per `_boundaries/03_validator_pipeline_slots.md` |
| ~~**MAP-Q3**~~ | MAP_001 | ~~PL_005 Examine of non-cell-tier map node ("examine the country") — requires PL_005 ExamineTarget extension to accept ChannelId at non-cell tier (extends PF-Q4)~~ | **RESOLVED 2026-04-26 PL folder closure** → PL_005 §2 ExamineTarget::MapNode(ChannelId, ChannelTier) variant + §14.2 Examine MapNode sequence; V1+ author-content-gated runtime activation per INT-D11 |
| ~~**CSC-Q2**~~ | CSC_001 | ~~Validator slot ordering: EVT-V_cell_scene relative to EVT-V_map_layout / EVT-V_place_structural / EVT-V_entity_affordance / EVT-V_lex~~ | **RESOLVED 2026-04-26 alignment review** → Stage 3.5.d per `_boundaries/03_validator_pipeline_slots.md` |
| **HER-D8** | WA_002 | EVT-T11 WorldTick V1+30d activation | event-model agent Phase 4 |
| **HER-D9** | WA_002 | LexSchema v1→v2 migration sequencing | implementation phase ops |
| **CHR-D9** | PLT_001 | Cross-reality `meta_user_pending_invitations` table | platform infrastructure |
| **WA_006 over-extension** | boundary review 2026-04-25 | 5 sections of WA_006 belong to PCS_001 / 05_llm_safety / PL_001 | rewrite WA_006 when feature owners take over |
| **B2 RealityManifest envelope** | boundary review 2026-04-25 | No single owner of the manifest schema | propose IF_001_reality_manifest.md (deferred) |

---

## Adding a new entry

When a new feature is designed:
1. Lock-claim `_boundaries/_LOCK.md`
2. Add the feature's owned aggregates to "Aggregate ownership"
3. If the feature extends a shared schema: add to "Schema / envelope ownership" + update `02_extension_contracts.md`
4. If the feature adds a validator slot: update `03_validator_pipeline_slots.md`
5. If the feature uses a new stable-ID prefix: add to "Stable-ID prefix ownership"
6. Append a row to `99_changelog.md`
7. Lock-release

---

## When ownership changes

Aggregate transfers are RARE (most aggregates stay with their original owner). When they happen (e.g., WA_006's `pc_mortality_state` will move to PCS_001):
1. Lock-claim
2. Update the matrix entry: change Owner, mark date of transfer, add reason
3. Update both feature design docs (giving + receiving)
4. Append a row to `99_changelog.md` with full transfer details
5. Lock-release
