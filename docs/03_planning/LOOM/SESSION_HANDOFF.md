# LOOM — Track Charter & Session Handoff

> **Track:** **LOOM** — the lore-grounded **co-writer** + its **Canon Model** foundation.
> **Last updated:** 2026-06-04 · **Branch:** `feat/composition-service` · **HEAD:** Canon Model Cycle 0 complete + Composition M0–M4+M6 (M5 folded) (see latest commit) · **Session:** LOOM-08 (Composition M6 — co-write engine + critic)
> **Workflow:** 12-phase **v2.2 human-in-loop** (PO checkpoint at CLARIFY-end + POST-REVIEW). `/amaw` **opt-in** for: CM1 (schema/migration), CM3 (cross-service contract cutover), composition M1 (schema), M5 (authz/isolation).
> **Isolated from** the `lore-enrichment/*` track — LOOM **never touches `services/lore-enrichment-service/`** (siblings, not deps).

## What LOOM is
The name = LoreWeave's **loom**. The **Canon Model** is the **warp** (the fixed structural threads: *published* canon, in-world/reading order, provenance); the AI **co-writer** weaves the **weft** (new prose) through it. Spoiler-safety = you can only weave with threads already laid down (no future-canon leak). LOOM turns a book into living canon, then co-writes grounded in it.

Two parts, built in order:
1. **Canon Model (Cycle 0, prerequisite)** — fixes pre-existing platform bugs B1–B7 that composition surfaced (NOT composition scope). 4 primitives: editorial lifecycle · canon=published · dual-order populated · provenance (aligned with enrichment H0, no enrichment edits).
2. **Composition (V0)** — lore-grounded co-writer + visual planning, built on the Canon Model.

## Design SSOT (locked 2026-06-03, commit `bad7acda`, after 4 adversarial review rounds)
- Canon Model: [spec](../../specs/2026-06-03-canon-model.md) · [Cycle-0 plan](../../plans/2026-06-03-canon-model-cycle0.md) (§8 = corrected/authoritative)
- Composition: [design](../../specs/2026-06-02-composition-design.md) (§12 contract-review, §13 ops-benchmark) · [V0 plan](../../plans/2026-06-02-composition-service-v0.md)

## Locked decisions
- Full **event-driven** extraction rework (coalescing per-project drainer over `extraction_pending`, respects the one-active-job/project index; retract-before-reextract; pinned-revision).
- **canon = published** (extract on `chapter.published` at the pinned revision; `chapter.saved` no longer canonizes).
- **CM-FE (publish UI) in Cycle 0** (else KG/passages stale platform-wide).
- **chapter-gate publish** — composition publishes a chapter only when ALL its scenes are `status='done'`.
- **dual-order:** reading-order (`event_order` from `sort_order`) is the dense V0 spoiler axis; `chronological_order` (from `event_date_iso`) is best-effort/sparse for CJK corpus.
- **enriched-lens deferred** to composition's final phase (glossary has no structured enrichment-content read yet).
- Boundary: touches book/worker-infra/knowledge/worker-ai/SDK + FE; **lore-enrichment never**.

## Build order
**Cycle 0 (Canon Model):** CM1 (book lifecycle + `/publish` + migration) → CM3a (revision_id event + internal revision-text endpoint) → CM2 (relay confirm — no-op) → CM3b (knowledge queue + worker-ai coalescing drainer + pinned-revision + retract-before-reextract + B7 fix) → CM3c (passage-ingest + manual-rebuild gating) → CM4 (dual-order + backfills) → CM-FE (publish affordance) → CM5 (provenance).
**Then Composition V0:** M0 skeleton → M1 schema → M2 repos → M3 clients/prose-source → M4 packer → M5 isolation → M6 engine+critic → M7 contract+gateway → M8 FE tab → M9 OI-1 publish wiring.

## ✅ CM1 DONE (LOOM-01, 2026-06-04) — book-service editorial lifecycle
Shipped (book-service only, additive): `chapters.editorial_status` (draft|published) + `published_revision_id` (FK ON DELETE SET NULL) + `canon_model_migration` marker table + marker-gated one-time `backfillSQL`; `publishChapter`/`unpublishChapter` handlers + routes; `chapter.published{book_id,chapter_id,revision_id}` / `chapter.unpublished` events; import → published + pinned revision; editorial fields on `getChapterByID` + `getInternalBookChapter`. 12-phase v2.2 + `/amaw`: design adversary R1 BLOCK→R2 APPROVED_WITH_WARNINGS, code adversary R1 APPROVED_WITH_WARNINGS — all folded (down-migration gate, publish RETURNING+FOR UPDATE, import NULL-pointer + error-check, marker-gated backfill, stable tiebreak). `go build` + `go test` (migrate/api/config) green. CLARIFY PO: import=published · /unpublish ships · edit-after-publish stays published · always-snapshot · publish-empty→404 · no in_review.

## ✅ CM3a DONE (LOOM-02, 2026-06-04) — internal revision-text endpoint
Shipped (book-service only): `GET /internal/books/{book_id}/chapters/{chapter_id}/revisions/{revision_id}/text` under `requireInternalToken`; IDOR-guarded (`rv.id+rv.chapter_id+c.book_id+lifecycle=active` → cross 404); plain-text projection (`->>'_text'`, ordered, null-safe). 12-phase v2.2 (S, self-review) + **/review-impl** (0 HIGH; folded MED-2 caller-gate contract comment, LOW-2 trim `body` from response, MED-1 tightened the live-smoke to require the IDOR-negative case; accepted LOW-1 body_format='json' / LOW-3 projection-swallow). `go build`+`go vet` green. **Contract:** serves ANY revision by id — canon=published depends on CM3b passing `chapters.published_revision_id`.

## ✅ CM2 DONE (LOOM-02, 2026-06-04) — relay confirm, ZERO code change
Verified in-code (`worker-infra/internal/tasks/outbox_relay.go:149-205`): `processSource` SELECTs all unpublished outbox rows (no event_type filter); `streamKey = "loreweave:events:" + aggregate_type` (`:179`); event_type only carried in the values map (`:184`), never routes/filters. `insertOutboxEvent` hardcodes `aggregate_type='chapter'` and book-service is already a relay source (`chapter.saved` flows today) → `chapter.published` + `chapter.unpublished` auto-flow to `loreweave:events:chapter`. No allowlist; statistics-service (separate consumer group) ignores unknown types; knowledge consumes in CM3b. No code, no commit needed beyond this note.

## ✅ CM3b DONE (LOOM-03, 2026-06-04) — graph-extraction cutover (default v2.2 + /review-impl)
knowledge: `extraction_pending +revision_id +aggregate_type filter +upsert_chapter_pending` (keep-LATEST/re-arm); `handle_chapter_published` (queue at pinned rev) / `handle_chapter_unpublished` (`remove_evidence_for_source` retract — **closes D-CM1-UNPUBLISH-RETRACT**) / `chapter_saved` drops graph-queue; main register; persist-pass2 **retract-then-write** (B6, all paths); `JobScope +chapters_pending`. worker-ai: `_enumerate_pending_chapters` + `process_job` chapters_pending branch (shared loop, revision-text, **mark-by-revision**) + `_ensure_chapters_pending_jobs` poll (reuse last-job models+cap, swallow-409, fail-stop guard) + `clients.get_chapter_revision_text` (CM3a) + **B7** chat-drain filter. **/review-impl: 0 HIGH, fixed MED-1 (re-publish-during-drain race → mark-by-revision) + MED-2 (stale-model recreate-fail loop → 1h fail-stop).** Verify: knowledge 2008 pass/1 pre-existing (lifecycle/budget MagicMock, not CM3b); worker-ai 101 pass; py_compile all. **Closes B6, B7, D-CM1-UNPUBLISH-RETRACT.**

## ✅ CM3c DONE (LOOM-04, 2026-06-04) — passage-ingest cutover + manual-rebuild draft-gating (AMAW)
Completes canon=published for the two CM3b-uncovered paths, across **3 services** (the §8.3 "(knowledge)" label under-counted — worker-ai + book-service list-endpoint gap, per §8.2). **book-service:** `getInternalBookChapters` optional `?editorial_status=published` filter (items **+** COUNT, `+published_revision_id IS NOT NULL`) + exposes `editorial_status`/`published_revision_id` on list items + COUNT error→500. **worker-ai:** `ChapterInfo.editorial_status`; `list_chapters(editorial_status=)` maps `published_revision_id`→`revision_id`; `_enumerate_chapters` published-gate (server-side) + null-revision **WARN**-skip; `p3_is_last` scope-guard (the coalescing `chapters_pending` drain never asserts whole-book is_last — closes a latent CM3b spurious-roll-up). **knowledge:** `BookClient.get_chapter_revision_text` (CM3a); `ingest_chapter_passages(+revision_id, +delete_stale_on_missing)`; `handle_chapter_published` now ALSO ingests L3 passages at the pinned revision (`_ingest_published_passages`, graph-queue first); `handle_chapter_unpublished` retracts passages (independent best-effort try); **`handle_chapter_saved` DELETED** + unregistered (main.py); estimate gates to published.
**AMAW 12-phase:** design adversary R1 BLOCK→R2 BLOCK→R3 APPROVED_WITH_WARNINGS (4 BLOCK + 3 WARN folded: server-side filter, null-ptr WARN, is_last scope-guard, positional-arg wiring, delete_stale_on_missing, independent unpublish-retract, parity lock); code adversary R1 APPROVED_WITH_WARNINGS (Go placeholder-arithmetic + dual-retract verified correct; folded published-filter NOT-NULL + COUNT-err→500); Scope Guard **CLEAR**. **Verify:** book go build/vet/test green; worker-ai 104 pass (+3 CM3c); knowledge 1956 unit pass. **LIVE-SMOKE deferred → D-CANON-CYCLE0-LIVE-SMOKE** (running stack is pre-CM1: live `loreweave_book` has no `editorial_status` col → CM3c can't smoke in isolation; whole CM1→CM3c chain tested together at a future stack-up). **Closes LOW-3** (manual rebuild no longer extracts drafts). PO: drop chapter.saved · passages publish-only.

## ✅ CM4 DONE (LOOM-04, 2026-06-04) — dual-order population + backfills (v2.2 + /review-impl)
Populates the reading + chronology axes the timeline already filtered on but B1 left perpetually NULL. **Knowledge-internal.** **write-path** (`pass2_writer`): `event_order = HierarchyPaths.chapter_index × EVENT_ORDER_CHAPTER_STRIDE + within-chapter idx` (chapter ordinal already threaded via P3; legacy/chat → None → timeline null-sinks); `merge_event` ON MATCH coalesce→**MIN-keep** (monotone-earliest; **⚠️ event_id is chapter-keyed so this is re-extraction stability, NOT cross-chapter — the PO premise was off**); `rerank_chronological_order` (sequential date-rank, undated→NULL, stable id tiebreak) called **debounced** (only when ≥1 dated event written) in the same Tx. **passage** `chapter_index` ← `get_chapter_sort_orders` (the CM3c-kept client). **backfill** `backfill_orders.py` + internal `POST /internal/projects/{id}/backfill-orders` (one-time legacy migration, mirrors C18). **PO:** event_order min · backfill = internal endpoint.
**/review-impl (3 MED folded):** INT32→**INT64 null-sentinel** (`9223372036854775807`) in the 3 timeline ORDER BY — books >2147 chapters breached INT32_MAX once event_order was populated; backfill gap-fill **collision** (re-run-after-growth dup order) → reverted to unconditional one-time SET; **shared `EVENT_ORDER_CHAPTER_STRIDE`** (was duplicated pass2_writer+backfill → drift). LOW: stride = composition cutoff contract (`before_order = N × stride`). **Verify:** knowledge 1969 unit pass (+CM4); single-service. **Neo4j Cypher ordering correctness deferred to integration/D-CANON-CYCLE0-LIVE-SMOKE** (units cover orchestration via fakes only).

## ✅ CM-FE DONE (LOOM-04, 2026-06-04) — Publish affordance (v2.2 + /review-impl)
FE-only (gateway `pathFilter '/v1/books'` proxies CM1's publish/unpublish transparently; `getChapter` exposes `editorial_status` per CM1). **`books/api.ts`**: Chapter editorial fields + `publishChapter`(sends `expected_draft_version`)/`unpublishChapter`. **`usePublishChapter`** hook (publish/unpublish, 409 `CHAPTER_DRAFT_CONFLICT` toast, confirm state). **`PublishControl`** component: status badge + Publish/Re-publish + Unpublish behind a **destructive confirm** (explains KG+passage retract). **`ChapterEditorPage`**: `editorialStatus` state + `refreshEditorialStatus` (meta-only refetch, no editor clobber) + control by Save; **publish disabled while dirty** (publish snapshots the SERVER draft → save first; `save()→load()→setVersion` gives a fresh draft_version). i18n ×4. **PO:** badge+Re-publish (no divergence detect) · confirm on unpublish. **/review-impl (3 LOW folded):** render-null when `editorial_status` undefined (pre-load flicker + pre-CM1-BE graceful degrade); bare-`status:409` test; i18n key regression-lock. **Verify:** tsc clean; **9** PublishControl tests pass. Browser smoke → D-CANON-CYCLE0-LIVE-SMOKE (stack pre-CM1).

## ✅ CM5 DONE (LOOM-04, 2026-06-04) — provenance (minimal) (v2.2 + /review-impl)
Extraction stamps an authorship-origin tag on every node it writes. **Knowledge-internal · NO scoring change · lore-enrichment untouched.** `PersistPass2Request.provenance` = closed `Literal{human_authored, ai_assisted, enrichment}` (default `human_authored`) → threaded `persist-pass2 → write_pass2_extraction → resolve_or_merge_entity/merge_entity/merge_entity_at_id/merge_event/merge_fact`. **PO: ACCUMULATE** — node property `provenances` is a deduped **array** via the existing `source_types` CASE pattern (ON CREATE `[hint]` / ON MATCH `coalesce(x.provenances,[]) + hint`; coalesce guards pre-CM5 nodes). Relations excluded (V0=nodes). Worker + 3 orchestrator call-sites inherit the `human_authored` default; composition sends `ai_assisted` later. **/review-impl (0 HIGH/MED):** verified models are plain BaseModel (`extra='ignore'`) so `provenances` is dropped on read (no 500/etag drift) + added converter-tolerance regression-locks. **Verify:** knowledge 1974+6 unit pass; boundary clean. `provenances` array-accumulate Cypher → D-CANON-CYCLE0-LIVE-SMOKE.

## 🎉 CANON MODEL CYCLE 0 BACKEND COMPLETE — CM1·CM2·CM3a·CM3b·CM3c·CM4·CM-FE·CM5 all DONE.

## ✅ D-CANON-CYCLE0-LIVE-SMOKE — LARGELY PROVEN (2026-06-04, stack rebuilt to HEAD 59626bdd)
Rebuilt book+knowledge+worker-ai via `build-stack.sh` (all 3 now git-SHA-stamped at HEAD) + `compose up -d` (migrations applied). LM Studio up (73 models), provider-registry healthy. Smoke on the 封神演义 demo book (owned by test acct `019d5e3c`) + a throwaway book (trashed after):
- **Migrations ✅** — CM1 `chapters.editorial_status`+`published_revision_id`; CM3b `extraction_pending.revision_id`. CM1 backfill: demo's 5 chapters all `published` + pinned pointer.
- **CM1 publish/unpublish/re-publish ✅** (gateway, JWT) — new chapter starts `draft`; publish→`published`+pointer; `chapter.published` payload `revision_id == pinned pointer`; unpublish→`draft`+NULL+`chapter.unpublished`; re-publish→pointer ADVANCES + new event. Sequence created→published→unpublished→published. (The canon spine that had NO DB-backed Go test.)
- **CM3a ✅** — internal revision-text returns pinned text; cross-chapter revision→**404** (IDOR).
- **CM3c ✅** — book list `?editorial_status=published`→5 items w/ fields; `=draft`→0; invalid→**400**.
- **CM3b END-TO-END ✅** (the load-bearing cutover) — re-publish → `extraction_pending` armed with the **pinned** `revision_id` → worker poll → "created chapters_pending drain job" → fetched **PINNED revision text** via CM3a (`GET …/revisions/{pinned}/text`→200, NOT live draft) → LLM extract → `persist-pass2`→200 (entities=4) → pending row **drained** (mark-by-revision).
- **CM4 ✅ (endpoint)** — backfill endpoint→200, correctly skips NULL-`chapter_id` legacy events (events_ordered=0 is right). [event_order at WRITE not live-checked: demo chapters are legacy/no-P3-hierarchy → event_order=None by design; unit-tested.]
- **CM5 ✅** — the 4 freshly-extracted entities carry `provenances=["human_authored"]`; 30 old entities keep NULL provenances and coexist (no error) → proves the `coalesce` write-guard + `extra='ignore'` read-tolerance LIVE.
- **Gateway ✅** — transparently proxies publish/unpublish (CM-FE FE-only assumption confirmed).
**Residual (low risk):** CM-FE browser click-through (API path proven via the publish/unpublish flow + 9 unit tests; only the React render is unsmoked); CM4 write-time `event_order` on a P3-hierarchical chapter; unpublish graph+passage retract on a project-backed chapter (unit-tested; unpublish event proven). **Pre-existing (NOT a CM bug):** "pass2 filter batch failed cat=relation: model not found" → relations=0 on this stack (relation-filter model unconfigured — eval-config gap).

## ✅ Composition V0 — M0 DONE (skeleton, 2026-06-04, v2.2)
New `services/composition-service/` mirroring knowledge-service house style (single-DB `loreweave_composition`). main.py lifespan (create_pool→run_migrations no-op→routers) + config/pool/migrate(no-op, M1 fills §1.2 DDL)/logging/metrics + 3 middleware copies + health/ping/metrics routers + 3-stage Dockerfile + requirements + test_skeleton/conftest. Infra: db-init `loreweave_composition` line + compose block **host 8217→container 8093** (python-urllib healthcheck). **Verify gate MET LIVE:** built + `compose up` → Up(healthy) → `GET /health` 200 `{status:ok,db:ok}` + `/v1/composition/ping` `{pong:true}` + `/metrics`; host pytest 3 pass. Boundary clean (additive infra only; no lore-enrichment/other-service edits). DB created manually on running stack (db-init only auto-runs on fresh volume).

## ✅ Composition V0 — M1 DONE (schema + migrate, 2026-06-04, v2.2)
§1.2 DDL in `migrate.py` `_SCHEMA_SQL` — **7 tables** (`composition_work`, `structure_template`, `outline_node`, `scene_link`, `canon_rule`, `generation_job` incl. **`base_revision_id`** OI-2 guard, `outbox_events`) + indexes + CHECK constraints (all `IF NOT EXISTS`). **6 built-in `structure_template`** seeded (owner NULL, full canonical beats — save_the_cat 15 / hero_journey 12 / story_circle 8 / kishotenketsu 4 / web_novel 6 / generic 3) via deterministic UUIDs + `ON CONFLICT DO NOTHING` (idempotent). `app/db/models.py` = 7 Pydantic row models + StringConstraints caps. **Verify (gate MET + LIVE):** integration `test_migrate` 3 pass on real PG (migrate ×2 idempotent + 6 seeded once; insert all 7 tables; scene-no-chapter → CheckViolation); host unit 3 pass; composition-service rebuilt+restarted → Up(healthy), dev `loreweave_composition` has 7 tables + 6 templates with correct beat counts. Cross-DB ids = no-FK (§1.4, app-validated); in-DB FKs OK. Test DB throwaway dropped.

## ✅ Composition V0 — M2 DONE (repos + Work resolution, 2026-06-04, v2.2 + /review-impl)
6 repos in `app/db/repositories/`: **works** (CRUD + `resolve_by_book` prefers-marked §6.2 + If-Match), **outline** (tree CRUD, fractional `rank` via new `rank.py`, If-Match, **recursive soft-archive** PO-decision), **scene_links** (CRUD, hard-delete edges), **canon_rules** (CRUD + `list_active` + soft-archive), **generation_jobs** (lifecycle + idempotency replay), **outbox** (txn-local `emit(conn,…)`). `__init__` exports `VersionMismatchError`+`rows_changed`. **PO pulled the knowledge client forward into M2** → `clients/knowledge_client.py` (**JWT-forward** — contract-verified `GET /v1/knowledge/projects?book_id` is JWT-only, no internal variant) + `work_resolution.py` (full §6.2: found/candidates/unmarked_single/unmarked_candidates/none/unavailable; takes `bearer` param, router-free). `deps.py` factories; `main.py` closes the client. **M5 isolation folded in:** every method user_id-first, every query (incl recursive CTE) user_id-filtered.
**/review-impl (1 HIGH + 2 MED FIXED, 3 regression-locks):** HIGH — resolve filtered `project_type=='book'` but the platform binds a book's grounding project by **book_id ONLY** (editor panel `listProjects({book_id,limit:1})`, FE create defaults 'general') → would miss the real project → POST /work makes a **duplicate empty project** → empty-graph grounding; fixed to book_id-only match. MED — `rank` ordering assumed byte-order but the DB is **en_US.utf8** (confirmed via pg_database; PG18 dropped `SHOW lc_collate`) → added `COLLATE "C"` to list_tree ORDER BY + `_next_rank` max(). MED — `archive_node` used `UNION ALL` → a parent cycle (reachable via reparent, no guard) recurses forever/hangs → switched to `UNION`.
**Verify:** **44 pass** (22 unit: rank algorithm, §6.2 6-branch resolution, JWT-forward via respx; 16 integration on **REAL infra-postgres :5555**: If-Match 412-vs-404, recursive archive, **cycle-terminates**, **collation-order**, idempotency replay, **txn-local outbox commit+rollback atomic**; 6 M0/M1 carryover). Integration FK caught a test bug (random outline_node_id→fixed). py_compile + import smoke clean; throwaway DB dropped. **NO rebuild needed** (M2 is library code; no new endpoints/schema — M1 schema unchanged, running container behavior identical until M3 routers wire it).

## ✅ M2-closure batch DONE (2026-06-04, v2.2) — all 3 M2 defers solved
**D-COMP-M0-LOG-SERVICE-LABEL** → `logging_config` labels logs `composition-service` (unit-locked). **D-COMP-M2-XREF-OWNERSHIP** → closed at the **repo layer** (defense-in-depth, no router needed): new `ReferenceViolationError`; `outline.create_node`/`update_node` reject cross-user/cross-project parents + reparent self/descendant **cycles** (`_descendant_ids` CTE) — malformed cycle now blocked at source, `archive_node` UNION stays backstop; `scene_link.create` requires both endpoints owned-in-project; `generation_job.create` requires its node owned-in-project. **D-COMP-M2-RESOLVE-SMOKE** → **live** cross-service smoke PASSED (minted test JWT → live knowledge-service `localhost:8216` JWT-forward returned demo book's project; `resolve_work`→`unmarked_single` w/ correct `book_project_id` vs live `loreweave_composition`; cross-user JWT→`[]` = ownership server-side; also live-confirms the HIGH book_id-only fix). **Verify: 49 pass** (44 +5 guard/label locks; forge-cycle test rewired to raw-SQL bypass so UNION backstop still proven). Accepted nuance: reparent validate-then-write not in one txn (TOCTOU; V0 single-user + UNION backstop). No schema/API/migration change.

## ✅ Composition V0 — M3 DONE (clients + prose-source, 2026-06-04, XL, v2.2 + /review-impl)
**PO: FULL client scope + works router in M3.** **5 clients** (all contract-verified vs real book/glossary/knowledge code): `book_client`(JWT-forward prose — `owns_book` SEC2 primitive, `get_draft`, `patch_draft` mandatory-version, `list_revisions`; typed `BookClientError` for proxy status-map); `glossary_client`(X-Internal `select_for_context`+`list_entities`, graceful-degrade); `knowledge_client` EXTENDED(`build_context` internal; `timeline`/`get_entity`/`search_drawers` JWT — `project_id` always sent per A1, drawers `project_id` REQUIRED, `raw_score`/`chapter_index` int|null); `llm_client`(loreweave_llm SDK wrapper + retry loop + `.sdk` stream escape-hatch); `eval_client`(reuses llm — JudgeLLMClient Protocol structural — + `messages[0].content` extractor). **Routers:** `works.py`(GET /books/{id}/work → `resolve_work` wired + GET/PATCH /works If-Match 412) + `prose.py`(GET/PUT proxy, **mandatory `expected_draft_version`** body field→422-if-absent, draft-only, 409 CHAPTER_DRAFT_CONFLICT map). `get_bearer_token` dep; deps factories; main register+client-close; metrics counters.
**Contract facts locked (verify-vs-real-code):** prose draft GET/PATCH are book-service PUBLIC JWT-only → composition FORWARDS JWT (book enforces ownership 404); `expected_draft_version` optional server-side→composition makes MANDATORY (client-supplied from GET, presence-enforced, NOT re-read — corrects §11 wording); `base_revision_id` from `GET …/revisions` items[0]; SEC2 composition-side chokepoint (`owns_book`) applies to INTERNAL glossary/knowledge reads (M4), NOT the JWT-forwarded prose path.
**/review-impl (1 MED fixed + 2 LOW test-adds):** MED — `ProsePutBody.body` `Any`→`dict[str,Any]` (explicit `null` body would forward + risk clobbering the draft to null; TipTap root is always an object). LOW — added router tests for `candidates` serialization + `body_format`/`commit_message` forwarding. Accepted LOW: prose GET `(draft_version, base_revision_id)` from 2 non-atomic book calls (version guard still protects writes; note for M4).
**Verify: 79 pass** (58 unit: respx contract-shapes incl JWT-vs-internal-token assertions, llm retry-loop via fake SDK, eval gotcha, 12 router tests; 16 integ M2; 7 M0/M1) + **LIVE SMOKE** on rebuilt+restarted composition image (:8217): resolve→found; prose GET proxies REAL book draft+base_revision_id (JWT-forward); PUT no-version→422; **PUT stale-v999→409 CHAPTER_DRAFT_CONFLICT** (OI-2 no-blind-clobber LIVE through composition→book); cross-user→404. Throwaway DB+work-row+smoke script cleaned. **Composition image rebuilt** (git-SHA stamped d7c25dc9; contains uncommitted M3 tree at smoke time → re-stamp after this commit if re-deploying).

## ✅ Composition V0 — M4 DONE (RAG packer, 2026-06-04, XL, v2.2 + /review-impl)
**6 packer modules + sanitize** (`app/packer/`): `profile`(BookProfile NEUTRAL default from `composition_work.settings`; `resolve_source_language` auto→fallback); `lenses`(`_safe_*` gatherers — L0 canon[story_order-windowed] / L1a present=glossary bios+knowledge relations / L1b timeline / L2+L2′ structural / L3 recent-prose[chapter-tail approximation, M8→SceneAnchor] / L4 lore); `spoiler`(**two-axis**: L1b in-world `<story_order` + L4 reading-order `<scene sort_order`, **conservative-drop + LOG `l4_dropped_no_position`**, both FAIL-CLOSED on unknown scene position); `budget`(priority ladder, protected tiers never-drop, `over_budget` flag, tiktoken); `sanitize`(§13 SEC3 tag-not-delete + escapes `<block>` delimiters, bounds guide); `assemble`(**A1 `assert_project_scoped`** + §2.4 blocks + sanitize); `pack`(orchestrator: A1→**SEC2 `owns_book` chokepoint**→parallel gather→spoiler→budget→assemble; **C3a `grounding_available`** signal, **DI3** soft-absent skip caching glossary `entity_id`). **+ `routers/grounding.py`** GET preview, **`book_client.get_chapter_sort_orders`**(internal-token, batch≤200, gated by SEC2), main wiring.
**Contract-verified (real code):** `POST /internal/chapters/sort-orders`(X-Internal-Token) for chapter→sort_order; `hit.chapter_index` populated post-CM4 (best-effort, nullable → resolver fallback). sort_order = dense 1-based INT.
**/review-impl (1 MED fixed + 1 LOW test + earlier 502 fix):** MED — `_applies_at` was fail-OPEN on `story_order=None` → a `reveal_gate`'s spoiler text leaked into `<canon>` for unplaced scenes; fixed to FAIL-CLOSED (ungated world rules only, consistent with L1b). LOW — added L4 fallback-resolver test (`chapter_index=None`→`source_id`→`sort_order`→filtered). review-code earlier fixed book-service-down→grounding 502 (was generic 500).
**Verify: 110 pass** (89 unit incl 31 packer: profile NEUTRAL, sanitize tag-not-delete+escape, budget trim-lowest/protected/over_budget, spoiler L1b-future+None-fail-closed / L4-at-after+conservative-no-position+scene-None-fail-closed, pack A1-ValueError/SEC2-OwnershipError/C3a/L4-spoiler+resolver-fallback/DI3-soft-absent/canon-window+None-fail-closed, book_client sort-orders, grounding 404s+happy+ownership-404+book-down-502; 16 integ M2; M0/M1) + **LIVE SMOKE** on rebuilt composition image: GET grounding on a demo Dracula scene 200 — full packer ran (SEC2 owns_book + A1 + all lenses + budget + assemble), blocks=present(glossary)/beat(COMP)/recent(book L3 tail), token_count=1721, **C3a grounding_available=false+warning** (honest no-graph signal), profile NEUTRAL, cross-user→404. Demo rows+smoke+test-DB cleaned.
**M5 isolation: FOLDED** (per plan — every lens project_id+user_id scoped; A1 chokepoint asserts non-null project_id; SEC2 owns_book before internal reads; cross-user→404 throughout). No separate M5 cycle needed.

## ✅ Composition V0 — M6 DONE (co-write engine + critic, 2026-06-04, XL, v2.2 + /review-impl)
`engine/cowrite.py`: `build_messages`(de-bias — language/voice threaded, NO English-only phrases) + `stream_draft`(via `llm.sdk.stream`, relays TokenEvent/ReasoningEvent, **real UsageEvent metering** — absent/zero-OUTPUT frame→char-estimate fallback clamp≥0 never-0, mid-stream output cap→partial). `engine/critic.py` `judge_prose`: 4 dims + per-violation verdicts; **tolerant parse** (`parse_critique_json` fence-strip+balanced; `normalize_critique` score-coerce + filter malformed violations); **CC4** LLMError/non-completed→degrade never-raise; de-bias judge-in-source_language. `routers/engine.py`: `POST /generate`(SSE: pack→budget pre-check[413]→**S2 create-then-cancel-others-if-created**+idempotency→stream→meter→job-update) · `suggest-cast`(glossary select-for-context) · `GET /jobs/{id}` · `/jobs/{id}/critique`(**CC2 fresh list_active** + **critic≠drafter enforced**, skip+warn if equal/unset) · `/jobs/{id}/dismiss-violation`. **PO model-selection:** drafter (model_source,model_ref) in /generate request; critic in `composition_work.settings` (critic_model_source/_ref).
**Contract-verified (real code):** `sdk.stream(StreamRequest, user_id=)`→TokenEvent.delta/UsageEvent.input+output_tokens/DoneEvent; chat `_run_composer` reference; judge mirrors `judge_precision` tolerant parse; provider-registry:8085 container.
**/review-impl (3 MED/LOW fixed + 3 regression-locks):** MED#1 idempotent retry was cancelling the original in-flight stream → create-first, cancel-others-only-if-created. MED#2 `model_source`/`mode` plain str → invalid value caused mid-stream StreamRequest ValidationError (orphan running job) / DB CheckViolation 500 → `Literal` types→422-pre-stream. LOW/MED#3 zero-OUTPUT usage frame (input>0,output=0) metered output=0 despite prose → gate on `out_tok>0`→char-fallback.
**Verify: 141 pass** (118 unit incl 29 M6: critic parse-tolerance/4-dims/CC4-degrade/malformed-filter, cowrite real+empty+zero-frame+input-only+mid-cap+LLMError metering, engine generate-SSE/S2-cancel/idempotency-no-cancel/invalid-enum-422/critique-CC2/critic≠drafter-skip/dismiss/get-job; 16 integ M2; M0/M1) + **LIVE STREAM SMOKE** real LM Studio via gateway (coder drafter, non-thinking): /generate streamed **136 prose tokens** (text_len=623), job completed, output_tokens=137 **measured=True** (real frame); GET job→completed; POST critique→**DISTINCT critic** (qwen3.5-9b≠drafter) judge_prose coherence=5 canon_consistency=5. Demo rows+smoke cleaned.

## ▶ NEXT SESSION — Composition V0 — M7 (contract + gateway)
**M7** (`/loom M7`, **default v2.2**). **Additive infra only** (per boundary). `contracts/api/composition/v1/openapi.yaml` — contract-first, additive; cover the shipped surface: works(resolve/CRUD) · prose(GET/PUT) · outline/scene-links (NOTE: outline router not built yet — §5 lists GET/POST/PATCH/DELETE outline+scene-links; M2 repos ready, NO router → either build the outline router in M7 or defer to M8) · canon-rules+templates (NOTE: also no router yet) · grounding · engine(generate SSE/suggest-cast/jobs/critique/dismiss). `services/api-gateway-bff/src/gateway-setup.ts`: add `compositionProxy` (pathFilter `/v1/composition`, **`selfHandleResponse:false` for SSE passthrough** — the /generate stream), 503-on-down handler like knowledgeProxy, + `instance.use` branch + `COMPOSITION_URL` env. `infra/docker-compose.yml`: add `COMPOSITION_URL=http://composition-service:8093` to api-gateway-bff env + `depends_on`. Read design §5 (full API) + plan §4 (gateway/compose wiring). **Verify:** gateway routes `/v1/composition/*`; **SSE passes through un-buffered** (live-smoke: /generate stream THROUGH the gateway). ⚠️ **CAUTION:** M7 touches api-gateway-bff + compose — verify the existing gateway proxies (knowledge/chat) are the pattern; do NOT edit lore-enrichment blocks. Then M8 FE tab → M9 OI-1.
**⚠️ Router gap to resolve at M7/M8 CLARIFY:** the §5 contract lists **outline** (GET tree, POST/PATCH/DELETE nodes) + **scene-links** + **canon-rules** + **GET /templates** endpoints. M2 built all the repos but M3–M6 only built works/prose/grounding/engine routers. So outline/scene-links/canon-rules/templates routers are NOT yet built. Decide at M7 (contract) or M8 (FE needs them): build these CRUD routers (repos ready, ~mechanical) before/with the FE. They're required for the M8 Outline/Canon panels.
**Ops note for M7:** composition image serves M0–M6 routers (rebuilt this session at HEAD-of-M6-tree). Gateway is api-gateway-bff (container :3000, host :3123). Composition in-cluster = `composition-service:8093`, host :8217. For the SSE-through-gateway smoke: a configured non-thinking drafter (test acct user_model `019e8897` qwen3-coder-30b works; the thinking qwen3.6-35b burns budget on reasoning — see D-COMP-M6-STREAM-THINKING-DISABLE). Run pytest from service dir WITHOUT shell-exporting INTERNAL_SERVICE_TOKEN/JWT_SECRET.
**Deferred carry:** D-COMP-M6-STREAM-SPEND-GUARDRAIL (NEW — streaming bypasses the gateway reserve/reconcile; only local pre-check + mid-stream output cap; matters for platform_model spend caps, low-risk BYOK/local) · D-COMP-M6-CRITIQUE-PRESENT-FACTS (NEW low — critique checks active canon rules only, not present-state contradictions; would need to reload the node's cast) · D-COMP-M6-STREAM-THINKING-DISABLE (NEW — StreamRequest lacks chat_template_kwargs so a thinking-by-default drafter emits only reasoning; use a non-thinking drafter or an SDK/gateway enhancement) · D-COMP-OUTLINE-CANON-ROUTERS (NEW — §5 outline/scene-links/canon-rules/templates routers unbuilt; repos ready) · D-COMP-M3-PROSE-GET-NONATOMIC · D-COMP-M4-L4-SAME-CHAPTER · D-CM4-RERANK-PERF · D-CM4-NEO4J-ORDERING-SMOKE · D-CM3B-* accepted races · D-CM3C-PASSAGE-INLINE-BULK · D-EXTRACTION-JOBS-TEST-ROT · relation-filter-model config (this stack).
**Recently cleared:** M6 engine+critic shipped (real stream metering + distinct-critic judge_prose live-proven) · M4 packer (spoiler two-axis + A1/SEC2) · M5 isolation FOLDED · M3 prose-source.

## Deferred / watch
- ~~D-CM1-UNPUBLISH-RETRACT~~ ✅ **CLOSED by CM3b** (unpublish retracts evidence). CM3c adds the **passage** half (unpublish also `delete_passages_for_source`).
- ~~LOW-3 (manual rebuild extracts drafts)~~ ✅ **CLOSED by CM3c** (`_enumerate_chapters` gates `editorial_status='published'` server-side).
- **D-CM3C-PASSAGE-INLINE-BULK (NEW, knowledge owner):** `handle_chapter_published` runs L3 passage embed/upsert INLINE in the event consumer (CM3c dropped the old C12a save-time skip-gate → every publish embeds). A composition **bulk-publish** (all `done` scenes → N sequential `chapter.published`) serializes the consumer behind N embed round-trips, back-pressuring chat/glossary events. Canon never lost (graph-queue written FIRST; passage half best-effort/non-fatal). **Fix at CM-FE:** move passage-ingest behind the `extraction_pending` drain, OR fire-and-forget the best-effort passage half (mind consumer task-lifecycle). Cycle-0 single-publish cadence makes inline acceptable now.
- ~~D-CM3C-BOOK-LIST-GO-TEST~~ ✅ **mostly CLOSED by /review-impl** — extracted `appendEditorialStatusFilter` pure helper + `TestAppendEditorialStatusFilter` locks the placeholder arithmetic (R2-BLOCK#2), the published `IS NOT NULL` gate, and draft/empty variants. Residual (DB-only, → D-CANON-CYCLE0-LIVE-SMOKE): the live count+items parity, invalid→400, COUNT-err→500 paths.
- **D-CM4-RERANK-PERF (NEW, knowledge owner):** `rerank_chronological_order` is O(project events) and fires per dated-chapter persist → O(chapters × events) on a fully-dated whole-book build. Book-scale OK; watch very large/dated projects. Future option: rerank once at end-of-job (worker drain completion) instead of per-chapter — but per-chapter is needed for the single-publish incremental case. Debounce already skips undated chapters/chat.
- **D-CM4-NEO4J-ORDERING-SMOKE (NEW, folded into D-CANON-CYCLE0-LIVE-SMOKE):** CM4's Cypher (min-keep ON MATCH, sequential date-rank ORDER, INT64 null-sentinel ordering past chapter 2147, backfill stamping + idempotence) is unit-tested only via fake-session orchestration. Real ranking/ordering needs a Neo4j integration run: assert `timeline?before_chronological=` + reading-order return correct NON-EMPTY ordered sets; a >2147-chapter event sorts BEFORE null-order events; backfill re-run is deterministic.
- **D-CM3C-DEPLOY-ORDER (NEW, /review-impl #3, ops note):** worker-ai/knowledge CM3c must deploy **AFTER** book-service CM3c. Against a pre-CM3c book-service (which ignores the unknown `?editorial_status=` param and omits the new fields), worker `list_chapters` maps every `revision_id`→None → `_enumerate_chapters` skips ALL chapters (manual rebuild extracts nothing); the estimate over-counts (drafts included). The direction is SAFE (never extracts drafts) but SILENT. **Deploy book-service first.** This is the current running-stack reality (pre-CM1).
- **/review-impl accepted (LOW, documented):** (#4) `ChapterInfo.editorial_status` is populated but unread by the worker (gate keys on `revision_id`) — kept as wire-contract documentation/observability. (#6) cursor-resume restarts the whole rebuild from scratch if the cursor chapter is unpublished/revision-purged mid-run (now filtered out → "not found" → restart); idempotent (retract-then-write) but wasteful — pre-existing trigger widened by the published filter.
- **D-CANON-CYCLE0-LIVE-SMOKE** (tightened; **Postgres/repo half PROVEN 2026-06-04**): ran the knowledge Postgres integration suite against a throwaway `loreweave_knowledge_test` DB on infra-postgres (`:5555`, creds loreweave/loreweave_dev; set `TEST_KNOWLEDGE_DB_URL`, leave `TEST_NEO4J_URI` unset). **CM-relevant tests PASS (42: extraction_pending_repo + migrations) → CM1 schema + CM3b revision_id/aggregate_type/upsert_chapter_pending validated at a real DB.** ⚠️ **Running infra stack is PRE-CM1** (live `loreweave_book` has no `editorial_status` column) → the CM1→CM3c chain is NOT deployed; a real cross-service smoke needs rebuilding book+knowledge+worker-ai images (`scripts/build-stack.sh`) so CM1's migration applies. Still needed: **Neo4j half** (entities/relations/provenance — needs a *dedicated* test Neo4j; community single-DB can't isolate from the demo graph) + **cross-service flow** (publish→queue→drain→extract at pinned rev; re-publish re-arm [MED-1]; unpublish retract; unpublish-during-drain [MED-3]; stale-model fail-stop [MED-2]; **CM3c: manual rebuild skips drafts + passages from pinned revision; unpublish drops passages**) on a stack-up. ⚠️ The Postgres fixture TRUNCATEs `knowledge_projects CASCADE` — NEVER point `TEST_KNOWLEDGE_DB_URL` at live `loreweave_knowledge`.
- **D-EXTRACTION-JOBS-TEST-ROT (NEW, pre-existing, knowledge owner):** `tests/integration/db/test_extraction_jobs_repo.py` uses stale status strings `'complete'/'cancelled'/'paused'` (lines 154/156/181/195…) but the schema enum is `pending/running/summarizing/completed/failed` → 16 CheckViolation failures when run against a real DB. Rotted because it skips by default (no dev-host run). NOT CM3b. Fix: update the test to the current status vocabulary (and add a regression-lock matching the migrate CHECK). Surfaces the broader risk: the heavy integration-skip suite needs a periodic real-DB run (CI Gate-2) to stop rot.
- **CM3b accepted races/edges (review-impl):** MED-3 unpublish-during-drain re-canonize (narrow; re-unpublish fixes) · LOW-1 paused-drain-on-cap blocks until resumed (visible) · LOW-2 per-persist retract assumes per-chapter persist (commented; future per-chunk would break).
- L5 long-term-summary lens (no HTTP read endpoint) — future knowledge surface.
- `chronological_order` quality for non-ISO in-world dates (CM4) — reading-order fallback.

## Deferred / watch
- **D-CM1-UNPUBLISH-RETRACT** — `/unpublish` flips status but does NOT retract already-extracted KG facts; **CM3b** wires `remove_evidence_for_source` on `chapter.unpublished`. Until then: temporary canon drift on unpublish.
- **D-CANON-CYCLE0-LIVE-SMOKE** — book-service has no DB-backed Go test harness; CM1 publish/unpublish tx + backfill one-time property + NULL-pointer scan + **CM3a revision-text endpoint** are string/build-tested only → need a live-smoke at stack-up, OR keep deferred. **Required assertions:** publish→revision pinned→event; re-publish; backfill double-run; unpublish; **CM3a: revision-text returns the published revision's text AND a cross-book/cross-chapter revision_id → 404 (the IDOR-negative, review-impl MED-1 — not just happy path).** (Also CM3b proves publish→extraction end-to-end.)
- L5 long-term-summary lens (no HTTP read endpoint) — future knowledge surface.
- `chronological_order` quality for non-ISO in-world dates — extraction-quality follow-up; reading-order is the fallback.
- Composition V1/V2 (branches/takes, autonomous loop, consistency sweep, 同人) — post-V0.
