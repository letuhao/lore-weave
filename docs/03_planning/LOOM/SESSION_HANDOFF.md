# LOOM — Track Charter & Session Handoff

> **Track:** **LOOM** — the lore-grounded **co-writer** + its **Canon Model** foundation.
> **Last updated:** 2026-06-25 · **Branch:** `feat/composition-service` · **HEAD:** T5.4 M1 BUILT (XL in milestones; BUILD: ...T3.6 + T5.4-M1 in); **Track 2 COMPLETE; Track 3 in progress**; PR #29 open; local chain LOOM-68->104; **BRANCH NOT CLOSEABLE (PO 2026-06-10): the v3 studio drafts are the PRODUCT SPEC - complete ALL drafted V1 features to a USABLE state before close.** **NEXT = T5.4 M2 Dock rail (DockRail dnd-kit reorder/show-hide + ComponentPicker; WorkspaceShell renders docked panels from layout when flag ON). T5.4 is built in milestones M1 hoist DONE -> [M2 dock] -> M3 float -> M4 OS pop-out (BroadcastChannel + SharedWorker). Spec: docs/specs/2026-06-25-loom-t5.4-dock-float-windowing.md; plan: docs/plans/2026-06-25-loom-t5.4-dock-float-windowing.md. FINAL V1 feature; branch closeable when M4 done.** T5.4 M1 DONE (live-state hoist: LiveStateContext owns the co-writer stream above the panel layer so a docked/floated/popped Compose survives a move; WorkspaceLayoutContext flag default-OFF + layout + validated localStorage + reducer; WorkspaceShell flag-gate renders CompositionPanel unchanged; ComposeView consumes hoisted stream; wired in ChapterEditorPage keyed by bookId for the book-change reset (review-impl MED fix). 408 composition tests green, flag-OFF byte-identical fallback). T3.6 References DONE (L, FS — composition-OWNED reference_source table + real[] vector + brute-force cosine; embed via provider-registry /internal/embed (NOT knowledge — explicit boundary: provider-registry owns the embed capability, knowledge owns the graph, composition owns its authoring data); embed model in work.settings write-through AFTER a successful embed (review-impl MED fix: pre-embed write-through bricked the work); per-Work scope; auto+manual query; pins via item_type=reference reusing T3.4; packer <references> block (sanitized, SEC3); FE ReferencesPanel subtab + useReferences + i18n x4). BUILD ORDER (scout-locked, see docs/03_planning/LOOM/REMAINING-BUILD-PLAN.md): T5.1 DONE -> T4.2 DONE -> T3.5 DONE -> T5.2 DONE -> T5.3 DONE -> T4.1 DONE -> T5.5 DONE -> T3.6 DONE -> [T5.4]. T4.1 Flywheel Panel DONE (M, FS — knowledge-service BE + FE). PIVOT from plan: instead of an extraction_jobs migration + threading deltas through the separate worker-ai (L, 3 services), stamp created_job_id ON CREATE (Entity/Event/Relation primary MERGE paths) + compute the delta ON-READ via Cypher -> no migration, no worker change. Writer: job_id threaded through merge_entity/merge_event/create_relation + resolve_or_merge_entity (anchor-hit/redirect paths skip — they do not create) + the 4 pass2_writer call sites; created_job_id set ONLY ON CREATE so re-runs/re-mentions never double-count (additive, dormant=NULL for old nodes). Read: neo4j_repos/flywheel.py (count exact + [0..N]-capped named sample WHERE created_job_id=$job_id, $user_id-scoped) + GET /v1/knowledge/projects/{id}/flywheel (resolve latest COMPLETED job -> FlywheelDelta{has_delta,job_id,completed_at,entities/relations/events_added,new_items[{kind,id,name}]}; 404 no-leak; has_delta=false neutral empty state). FE: useFlywheel + FlywheelPanel (in CompositionPanel `flywheel` subtab; +N stat buttons deep-link entity->Cast/event->Timeline/relation->Relations, named chips deep-link the specific item — entity chip focuses Cast by name via setCastSearch) + knowledgeApi.getFlywheel + FlywheelDeltaWire + i18n x4 (tab label `flywheel` string vs `flywheelPanel` object — separate keys avoid the t() object/string clash). CLARIFY (all locked): net-new created only; entities+events+relations all chipped; latest completed job, chip->item/counts->tab. /review-impl: 2 MED fixed+locked (collect materialized/shipped the entire created set -> [0..N] Cypher slice bounds payload, count stays exact; writer-stamp->count chain only in a throwaway smoke -> committed tests/integration/db/test_flywheel_repo.py exercises the REAL merge_*->stamp->get_flywheel_delta chain, asserting net-new + unstamped-sibling exclusion, 2 passed live) + 2 LOWs deferred. Verify: BE 591 extraction/writer + 6 flywheel unit + 2 flywheel integration (live Neo4j); FE tsc 0, FlywheelPanel 6, useFlywheel 3, CompositionPanel no-unmount 5 (18 subtabs), composition+editor 394. live smoke: flywheel Cypher net-new counting verified on real Neo4j (stamped counted, unstamped excluded, relation rendered). T5.3 AI Provenance Highlight DONE (M, FE-only): a stored Tiptap `provenance` mark (ProvenanceMark.ts — inclusive:false, attrs source/status/model/ts round-trip by key like CitationMark; click-to-review PM plugin flips an unreviewed span to reviewed keeping the mark; helpers applyProvenanceOver/reviewProvenanceAt/markAllProvenanceReviewed/countUnreviewedProvenance/setProvenanceVisible, all guarded inert when the mark is absent from the editor schema) riding in chapter body_json. Open Q2 RESOLVED in code: book-service patchDraft stores body as json.RawMessage VERBATIM (json.Valid only, no allowlist/pandoc/strip; server.go:1620-1673) so the mark round-trips save->reload losslessly; KG extraction reads plain text from chapter_blocks so the mark never interferes -> pure FE, no BE change. TiptapEditor: registers the mark + optional provenance arg on insertAtCursor/replaceSelection (marks [from, post-insert selection]) + handle methods markAllProvenanceReviewed/setProvenanceVisible/getUnreviewedProvenanceCount. useProvenance (controller: visibility localStorage per-device default-on, live unreviewed count derived from doc, mark-all). ProvenanceToolbar (eye toggle + count + mark-all, self-hides when nothing to review) + ProvenanceTag (hover tag reads data-* : AI/model/status, GlossaryTooltip pattern). CLARIFY (all locked): mark ALL 4 AI seams (compose accept + co-writer + chapter-assemble via CompositionPanel acceptProse which threads the model name; chat ProposeEditCard replace+insert); review keeps mark + flips status=reviewed (fades); faint violet underlay always-on + toolbar toggle. index.css underlay styles + i18n x4. /review-impl: 2 MED fixed+locked (helpers crash if mark absent from target editor schema -> if(!type) guard + inert test; core insert->mark-range untested -> headless handle-path test proving exactly the inserted text is marked) + 1 LOW fixed (ts attr was always null -> stamped at both accept sites), 2 LOWs deferred. Verify: FE tsc 0, ProvenanceMark 9, ProvenanceToolbar 5, useProvenance 5, editorBridge 5 (stub extended), CompositionPanel no-unmount 5; composition+editor+chat 514 green. FE-only -> no cross-service live-smoke needed. T5.2 Mention Heatmap DONE (S, FE-only): useMentionHeatmap (react-query: knowledge listEntities sort_by=mention_count, top-15, 0..4 density bands by ratio-to-max, exposes aliases for tinting) + HeatmapExtension (PM decoration plugin: tints entity-name + alias occurrences with heat-band-N class on word boundaries, longest-first, inert-until-enabled, never serialized into doc JSON) + TiptapEditor handle (setHeatmapTerms/setHeatmapEnabled) + ChapterEditorPage wiring (tints name+aliases, in-prose toggle) + GroundingPanel heatmap section (bars + in-prose toggle) + index.css band tints (blue->red) + i18n x4. Bundled T3.5 bug-fix: StyleVoicePanel cast picker read e.entity_id (undefined) -> e.id (the real knowledge entity-list key; the T3.5 test passed only because it mocked the wrong shape). NOTE (D-T5.2-WINDOWED-MENTIONS): mention_count is WHOLE-BOOK aggregate; per-chapter spoiler-windowing blocked on D-P2-PER-SCENE-FANOUT (no per-chapter mention edges exist). /review-impl: 1 MED fixed+locked (in-prose tint matched only canonical_name, missing aliases -> critical for CJK/xianxia content where chars are referenced by many titles/aliases; fix: HeatEntity.aliases + tint [name,...aliases]; locked by an alias test), 3 LOWs deferred. Verify: FE tsc 0, useMentionHeatmap 5, HeatmapPlugin 5, GroundingPanel 8, StyleVoicePanel 7 (id-fix), 25 total green. FE-only -> no cross-service live-smoke needed. T3.5 Style & Voice DONE (L, FS): style_profile (per-scope work|chapter|scene density/pace 0-100) + voice_profile (per-character entity_id+name+tags[]) tables; StyleProfileRepo(resolve scene>chapter>work) + VoiceProfileRepo(list_for_entities present-only); style_voice router GET/PUT/DELETE x2 (per-user works.get gate); BookProfile +density_level/pace_level/character_voices + centralized style_directive() (only outer slider bands steer; mid=balanced/silent); pack() resolves+threads both (optional/dormant kwargs like grounding_pins); cowrite/select(via build_messages)/stitch render style; grounding-preview profile surfaces it. FE useStyleVoice + StyleVoicePanel (scope toggle + density/pace sliders + per-character voice chips + cast-search picker) + style subtab + i18n x4. /review-impl: 1 MED fixed+locked (selection-edit used stale from_settings profile not pc.profile -> eff_profile=pc.profile; locked by test_selection_edit_threads_styled_profile + test_pack glue tests), 3 LOWs deferred. Verify: BE 182 + cowrite/stitch/select 41 + pack/engine 75 (fix locks); FE tsc 0, useStyleVoice 5, StyleVoicePanel 7, CompositionPanel 5 (style folded into no-unmount invariant, now 17 panels). T4.2 Progress Stats DONE (L, FS): composition_daily_progress (per-chapter word snapshot) + composition_progress_baseline (pre-existing count, insert-once on chapter open) tables; DailyProgressRepo (report upsert + ensure_baseline + read_aggregate: day-words = COALESCE(LAG,baseline) diff, book_total = latest-snapshot + unwritten-baselines); GET .../progress?today= (streak walk-back + dense 30-day sparkline) + POST .../progress/report + POST .../progress/baseline (per-user works.get gate); FE useProgress/useReportProgress/useEnsureBaseline/useSetDailyGoal, ProgressPanel (recharts sparkline 7/30 + goal bar + editable daily_goal via work.settings merge-PATCH), progress subtab, ChapterEditorPage reports on save + seeds baseline on load; i18n x4. PO decisions: snapshot/server-differenced words, streak=wrote-anything(>0), daily_goal unset default, SEED-baseline+count-fully (review-impl HIGH fix avoids first-day-zero AND enablement spike). Verify: BE progress unit + repo integration 90 passed (4 baseline integration + 4 router); FE tsc 0, useProgress 9, ProgressPanel 5, CompositionPanel 5 (progress folded into no-unmount invariant), ChapterEditorPage 3. /review-impl: 1 HIGH fixed+locked, 1 LOW fixed (words le=5M), 4 LOWs deferred. T5.1 Focus/Typewriter DONE: useFocusMode (localStorage toggle) + FocusLineExtension (PM node Decoration marks caret block .focusline - NOT classList; PM strips foreign classes on re-render) + TiptapEditor focusMode prop (typewriter rAF scroll-to-center) + ChapterEditorPage focus toggle/panel-hide/continuity-pill + index.css dim 0.28 + editor.json x4. Verify: tsc 0, useFocusMode 4, FocusLineExtension 4, ChapterEditorPage 3, comp FE suite 324, live Playwright smoke (ON: panel-hide+dim+pill+caret-follow / OFF: restored). /review-impl: 1 MED fixed+locked (FocusLineExtension.test 4 cases incl non-serialize invariant), 3 LOWs deferred -> see Deferred. (was: NEXT = T3.5 Style & Voice (continue BUILD P0->P1->P2).) T1.1 Outline tree feature COMPLETE (a-d, LOOM-97-100). T1.2 Beat Sheet BUILT (LOOM-101): beats SubTab in CompositionPanel; pick a structure template -> beats as cards (buildBeatSheet pure join beat.key<->node.beat_role) w/ mapped node(s) + fill-state (done->drafted/drafting->writing/empty/none->unplaced); unplaced beats + unmapped nodes listed; assign via dnd (drag node->beat card / ->unmap zone) + <select> a11y fallback; click->jump. PO chose scene+CHAPTER mapping -> MIGRATION relaxed outline_beatrole_scene->outline_beatrole_kind CHECK(beat_role IS NULL OR kind IN(scene,chapter)) (idempotent DO-block + inline; live-proven real-PG: chapter ok, arc rejects). /review-impl: MED clear-via-HTTP(exclude_unset+explicit null) now router-tested. Verify: FE vitest 1315/1315 (BeatSheetView 7) - tsc 0 - BE outline unit 21/21 - live smoke BE migration 10/10 real-PG. T1.3 Scene Graph BUILT (LOOM-102, XL FE-only, BE delta none): graph SubTab in CompositionPanel; scenes as a hand-rolled SVG graph (autoLayout pure: columns by story_order) with typed edges (setup_payoff solid arrow / custom dashed via SceneNode/SceneEdge); pick-two-nodes + "+ link" create (click OR keyboard-select role=button/Enter, 409 SCENE_LINK_EXISTS -> toast no-dup), edge-click -> delete, open -> jump to scene; drag-to-reposition persisted to work.settings.scene_graph.positions (useSetWorkSettings merge, device-shared, last-write-wins); useSceneLinks shares the outline GET via same query key (select scene_links). Decision: scene-specific now, extract generic <GraphCanvas> when T2.2/T2.5 consume it. /review-impl: MED shared-key contract proven (real-QueryClient test, getOutline deduped to 1 call), COSMETIC drag-assert strengthened, 2 LOW documented. Verify: FE vitest composition 161/161 (SceneGraphCanvas 10 + autoLayout + useOutline 2) - tsc 0 - i18n x4 parity. T2.1 Cast & Codex BUILT (LOOM-103, XL FS): cast sub-tab in CompositionPanel; book cast (knowledge entities, grouped by kind, searchable) with spoiler-safe story-state (active|gone from :EntityStatus); expand -> aliases/1-hop relations/recent events/known facts. PO chose BE-resolved spoiler window + per-entity facts "properly": knowledge-service now (:Fact)-[:ABOUT]->(:Entity) edge (pass2_writer stops dropping the resolved subject + from_order stamp), list_facts_for_entity, statuses_detail_at_order(+from_order), merge_entities ABOUT-transfer, fail-closed resolve_before_order (chapter_id->before_order via book_client+STRIDE; inverts book-client fail-open), 3 routes (entities/statuses batch + entities/{id}/facts + timeline before_chapter_id). FE reuses features/knowledge api (getEntityStatuses/getEntityFacts), useCast + CastCodexPanel + CastEntityRow, i18n x4 (cast tab + codex.*). /review-impl HIGH: fact subject linking used plain id-match (no name-repair) -> would under-link in prod; FIXED to resolve by subject NAME via _resolve_status_entity_id (Tier-A repair) + regression test that catches it; 4 LOW documented. Verify: BE 27 (13 unit routes/resolver + 3 pass2-fact + 5 pass2-status + 6 LIVE-Neo4j vs infra-neo4j-1) - FE 655/655 + i18n x4 - tsc/py_compile clean. Caveats: existing facts (NULL from_order, no ABOUT) wont show until re-extraction; relation count on expand not collapsed. T2.2 Relationship Map + generic GraphCanvas BUILT (LOOM-104, XL FE-only, BE delta none): EXTRACTED generic <GraphCanvas> (controlled SVG: drag-gesture/5px-threshold/extent/bg-clear/defs; render+persist+click delegated via props) and REFACTORED SceneGraphCanvas (T1.3) onto it - SceneNode/SceneEdge UNCHANGED, T1.3 10 tests stayed green (refactor net held). RelationshipMap = ego-network (relmap sub-tab): focus entity -> 1-hop relations (GET /entities/{id}); click neighbour -> refocus; expand(B) -> accrete 1-hop via useQueries (node cap 60 + overflow hint); pending_validation edges dashed + predicate on hover; radial layout; drag positions per-device in localStorage. useRelationshipMap (buildGraph dedup + radialLayout pure), GraphEntityNode + RelationEdge. i18n x4 (relmap tab + relations.*). /review-impl MED: controlled <select value=focusId> desynced when focus not in the capped entity list -> FIXED (always surface focus as an option) + regression test; MED accretion-glue + 2 LOW documented. Verify: tsc clean - vitest composition 179/179 (RelationshipMap 11 incl pure buildGraph/radialLayout; SceneGraphCanvas 10 still green; CompositionPanel 11-panel) - i18n x4. **T2.3 Timeline UI COMPLETE (LOOM-105, L FE-only):** spoiler-safe event_order axis (hand-rolled SVG, PO call), decoupled cutoff via before_chapter_id+windowed total (NO FE STRIDE constant -> eliminates spec MED-3 coupling; pagination-safe ascending-prefix), entity/date filters + reversed-range 422 friendly error, click->open-chapter, 12th sub-tab. Self-review caught+fixed multi-page spurious-marker bug (gate marker to the page straddling the cutoff) + regression test. Files: useTimeline (2-query + pure visibleOnPage/axisX) + TimelineView/TimelineEventPoint/SpoilerCutMarker, i18n x4 (timeline tab + chrono.*). Verify: tsc clean - vitest composition 190/190 (TimelineView 11 incl pure helpers + multi-page lock; CompositionPanel 12-panel) - i18n parity 22ns x3. **T2.4 Character Arc COMPLETE (LOOM-106, L FE-only):** per-character arc = entity-scoped Timeline projection. Reuses T2.3 TimelineEventPoint/SpoilerCutMarker/axisX/visibleOnPage (same decoupled cutoff, no FE stride) for the event spine; shaded active/gone state band split at spoiler-windowed from_order (pure arcBandSplit); direction-aware relations strip; character picker (T2.2 desync-guard + focusName for cast-launch-beyond-cap). Launched from Cast codex via lifted arcEntityId + per-row 📈 button (CastEntityRow header restructured to sibling buttons - no button-in-button). Files: useCharacterArc + CharacterArcView/ArcRelationsStrip, CompositionPanel (arc subtab = 13 panels), CastCodexPanel/CastEntityRow onViewArc, i18n x4 (arc tab + chararc.* + codex.viewArc). Verify: tsc clean - vitest composition 201/201 (CharacterArcView 11 incl pure arcBandSplit; CompositionPanel 13-panel; T2.1 cast green post-restructure) - i18n parity 22ns x3. PO decisions (CLARIFY): wire Cast launch now + shaded band. Deferred: D-T2.4-ARC-ENTITY-BOOK-RESET (stale arcEntityId across in-editor book switch, fail-safe empty); D-T2.4-ARC-PAGING (single page @ 100 events). **T2.5 World Map COMPLETE (LOOM-107, XL MULTI-SERVICE):** spatial places graph (3rd GraphCanvas consumer) + author-arranged SERVER-persisted positions (work.settings.world_map) + backdrop image (reuses book-service media upload, stores URL) + add-place/link-places authoring + click->Cast-codex (prefill search). **CLARIFY caught a blocker:** PO chose include-authoring but verified NO create-entity/create-relation endpoint existed (graph was extraction-only) -> PO chose BUILD THE BE. **BE (knowledge-service):** POST /entities (merge_entity, kind-gated {character,location,faction,concept}, manual/conf1, idempotent dedup) + POST /relations (recreate_relation, 409 missing-endpoint, 422 self-loop), both user_id-scoped (multi-tenant), NO new Cypher, gateway catch-all auto-proxies. **FE:** useWorldMap (+pure buildPlaceGraph/gridLayout) + WorldMap + PlaceNode, reused GraphCanvas (+backward-compat background slot) + RelationEdge; cast-search lifted to CompositionPanel (worldmap subtab=14 panels). **review-code MED fixed:** two-writer world_map clobber (positions vs backdrop both rewrote one blob from stale prop) -> merged wmRef. **/review-impl (ran): no HIGH;** locked the clobber fix (useWorldMap.test 2). **Verify:** tsc clean - FE composition 212/212 (WorldMap 9 + useWorldMap 2 + 14-panel) - BE 6 new + 54 regression unit - i18n parity 22ns x3 - **LIVE SMOKE (rebuilt knowledge-service): create-place(location/manual/conf1)->list?kind=location(2)->link(borders,pending=false)->detail(edge)->missing 409->bad-kind 422->cleanup ALL PASS via gateway+real Neo4j** (rebuild was needed - running image stale). Deferred: D-T2.5-MANUAL-GLOSSARY-SYNC, D-T2.5-AUTHORING-EVENTS (manual creates no flywheel events), D-T2.5-PLACE-DETAIL-FANOUT, D-T2.5-BACKDROP-INSTANT, D-T2.5-LINK-DIRECTION. **T3.1 AI Co-writer Chat COMPLETE (LOOM-108, L FE-only):** book-memory-grounded brainstorm chat as the 15th composition sub-tab, REUSING features/chat <Chat> + useEmbeddedChatBinding (streaming + book-memory binding + degrade warning - no reimplementation). Added Insert-as-draft (-> onAccept) + Use-as-guide (-> pre-fills the lifted compose guide) on the LATEST AI reply + starter chips in an empty active thread. Embedded with bookId+composeMode (RAG, prose-only) NO editorContext (manual Insert). Touch to shared chat = ONE additive prop: Chat actionBar -> ChatView footerSlot (between messages+input, inside providers; backward-compatible, chat suite green). ComposeView guide lifted to a controlled prop (Dispatch setter keeps the canon-revise functional append). Files: CoWriterChat + CoWriterActions (reads useChatStream/useChatSession), Chat.tsx + ChatView.tsx (footerSlot), ComposeView.tsx (guide prop), CompositionPanel (cowriter subtab + lifted composeGuide), i18n x4 (cowriter tab + cw.*). **review-code verified safe:** the always-mounted chat NewChatDialog cannot pop over the compose tab (renders inline not a portal -> display:none ancestor suppresses it). **Verify:** tsc clean - vitest composition+chat 291/291 (CoWriterActions 6; ComposeView 17 guide-as-prop + stateful-revise; CompositionPanel 15-panel; chat suite green post-footerSlot) - i18n parity 22ns x3. BE delta none (chat-service unchanged). Deferred: D-T3.1-SCENE-HINT (per-scene grounding), D-T3.1-GUIDE-APPEND (use-as-guide replaces vs append). **T3.2 Selection Tools COMPLETE (LOOM-109, XL MULTI-SERVICE):** inline AI rewrite/expand/describe over a prose selection. **BE (composition-service):** new POST /works/{pid}/selection-edit (SSE) decoupled from outline_node_id; build_selection_messages with EXPLICIT per-op dispatch that RAISES on unknown (no draft_scene fallback - LOOM-39) + Literal field (422 at boundary); reuses pack(scene_context grounding, degrades voice-only)+stream_draft. **FE:** useCompositionStream extended (selection branch); SelectionToolbar (Tiptap BubbleMenu @tiptap/react/menus + model picker + 3 ops + saved-range ghost Accept/Discard, bounds-check on stale range); additive TiptapEditor selectionMenu slot; ChapterEditorPage resolves work->projectId + lifts activeSceneId; CompositionPanel sceneId controlled-or-internal. **PO: model picker in toolbar + scene_context=compose panel active scene.** **/review-impl found+fixed HIGH:** selection-edit job tagged outline_node_id=scene_context masqueraded as the scene's latest draft in chapter_scene_drafts(stitch)/prior_scene_drafts(S1 reinjection)/publish-gate canon count -> FIX outline_node_id=None (scene id -> input.scene_context); +MED correction_stats skew (mode=cowrite selection edits inflated denominator) -> exclude via input.selection_edit filter; both regression-locked (endpoint test + real-PG integration). **Verify:** tsc clean - composition unit 402 (engine-router 2 new locks; cowrite 3 dispatch locks) + correction_stats 4 real-PG integration (1 new exclusion) - FE composition 223 + ChapterEditorPage 2 - i18n parity 22ns x3 - **LIVE SMOKE (rebuilt composition-service): selection-edit no-token 401, bad-op(summarize) 422, missing-work 404, empty-sel 422 ALL PASS** (route registered + Literal-422 = no silent fallback; streaming reuses live-proven stream_draft/pack). Deferred: D-T3.2-SELECTION-INLINE-GHOST (popover preview vs inline overlay), D-T3.2-SELECTION-RANGE-MAP (PM position-mapping vs bounds-check-abort on concurrent edit). **T3.3 Classic/AI Inline Mode COMPLETE (LOOM-110, L FE-only):** moves the cowrite ghost from the side panel INTO the manuscript at the caret. Classic<->AI mode toggle (per-device localStorage); AI mode ✦ Continue streams /generate{operation:continue, outline_node_id=active scene} as a position-fixed overlay at the cursor + accept-bar (Accept commits +critique / Edit inserts no-critique / Regenerate / Esc-Discard). Reuses useCompositionStream + useCritique + T3.2's lifted activeSceneId; work default_model_ref. **PO: positioned overlay at cursor coords (not a PM decoration) + disable-Continue-when-no-scene** (chapter-scoped fallback deferred - non-streaming UX mismatch). KEY: the ghost overlay is ANCHOR-based not mode-gated, so an in-flight stream survives a Classic toggle (AC: never lose an in-flight stream). review-code fixed: disable Continue while a ghost is pending (no orphaned ghost). Files: useInlineGhost + InlineGhost overlay + InlineAiLayer, TiptapEditor aiLayer slot (additive, the recurring extend-reused-component-via-slot pattern), ChapterEditorPage wire, i18n x4 (inline.*). **Verify:** tsc clean - composition 230/230 (InlineAiLayer 8: mode-toggle-no-remount, Continue-streams, accept-bar, anchor-survives-toggle, disabled-states, mode-persist) - i18n parity 22ns x3. BE delta none. Deferred: D-T3.3-SLASH-CONTINUE (slash /continue; ✦ button ships), D-T3.3-CHAPTER-CONTINUE (undecomposed-chapter streaming continue), D-T3.3-GHOST-POS-MAP (PM position-mapping vs bounds-check-abort on concurrent edit). NEXT = T3.4 Grounding Pin/Exclude (P3, [spec](../../specs/composition-v1-design/T3.4-grounding-pin-exclude.md)) ... (~9 features to T5.5). Deferred: open-in-cast nicety; D-COMP-RELMAP-GRAPH-ENDPOINT (server N-hop) until perf bites. DESIGN + reviews done (LOOM-92->95): 24 specs + /review-impl + architecture review ([report](../../reports/2026-06-11-composition-architecture-review.md)). Program [index](../../plans/2026-06-10-composition-design-program.md).** (6 tracks T0–T5 in [`docs/specs/composition-v1-design/`](../../specs/composition-v1-design/); program [index](../../plans/2026-06-10-composition-design-program.md) = cross-cutting decisions + new-BE list + per-feature UX/wireframe/contract/acceptance/tests). Open questions cleared per-track (Lead + PO forks). PO build-direction: build the real product (facts route, world-map persist+backdrop, full windowing incl OS pop-out — no stubs). Source = the coverage [review](../../reports/2026-06-10-composition-fe-be-coverage-review.md) + build [plan](../../plans/2026-06-11-composition-gap-closure-and-e2e.md). The next session does CLARIFY+DESIGN for the backlog: for each feature write the UX (against the v3 draft HTML), the FE component/hook shape, the BE delta (new endpoints/fields), the data contract, and acceptance + e2e outline — land it as spec file(s) in `docs/specs/` (a design-checkpoint commit, NO code yet), run `/review-impl` on the design, get PO sign-off, THEN build per-feature. Backlog to design: **Phase 0** (threads-debt panel, suggest-cast — BE done, FE-unwired) · **Phase 1** PARTIAL→usable (outline browser · grounding pin/exclude · cast-state codex · co-writer chat · classic/AI inline ghost+accept · selection rewrite/expand/describe) · **Phase 2** MISSING (beat-sheet · timeline UI · style&voice · flywheel panel · scene-graph canvas + story-map power-view · relationship map · focus mode · mention heatmap · provenance). Each then = its own `/loom` cycle (FE±BE + vitest + Playwright + 4-locale i18n). Excluded only: V2 drafts (doujin, what-if) + catalog-LATER (corkboard/arc/world-map/references/progress) — re-confirm with PO. Then the Part-B QC/e2e gate. **PR #19 stays OPEN until the V1 product is complete + usable.** · ~~feature-debt Phase 1 + Phase 3 COMPLETE — every actionable FD resolved (built or consciously deferred-with-decision). Phase 2 (FD-6→14) stays eval-gated/deferred per the NULL lift gate. PR #19 ready to merge.** · **LOOM-88 (FD-26, S, decisions-only):** (1) mining node→run provenance = keep-deferred (time-window attribution PO-locked; trigger=if-too-coarse, documented inline); (2) truncation char-estimate richer signal = DROP (the authoritative finish_reason `truncated` flag is shipped cy16, covers winner+repair; a char-estimate would be redundant/worse). No code. · **PRIOR LOOM-87 FD-19·052** (local chain LOOM-68→87 ahead of pushed `0afc2c57` (LOOM-67 S4b); **Phase 3: FD-17/27/18/24/20/25/16/15 + FD-19(053,052) done; FD-19 remainder (048/049/050/046) phase-deferred; FD-26 last.** · **LOOM-87 (FD-19·052, M, BE migration):** glossary description-only edits were mis-classed `diff_class=boundary` (rename signal). Fixed: snapshot `description` class + `split_snapshot` 3-tuple (content_hash=name/alias, description_hash=short_description, privacy-hashed) + `ALTER corrections` +before/after_description_content_hash + `_persist_correction` INSERT. boundary now rename-only; description-only→other; the change is still recorded in description_hash for Phase-D/E tiers. learning 141/141 + real-DB migration applied; /review-impl locked desc-hash-persisted (1 LOW-MED gap folded). ⚠ docker-cp-nests-tests trap re-confirmed (rm -rf /app/tests before cp). · **PRIOR LOOM-86 FD-19·053** (chain LOOM-68→86 ahead of pushed `0afc2c57` (LOOM-67 S4b); **Phase 3: FD-17/27/18/24/20/25/16/15 done + FD-19 in progress (053 done; 052 next; 048/049/050/046 phase-deferred) + FD-26 pending.** · **LOOM-86 (FD-19·053, S, BE):** triaged FD-19's 6 items (all gated on future learning Phases C/D/E). Built 053: `emit_correction`/`emit_config_adjustment` split swallowed failures transient(warn)/permanent(ERROR + `knowledge_correction_emit_failure_total{kind}`, pre-seeded) — a non-UUID aggregate_id / malformed payload no longer vanishes silently. 6 outbox tests; /review-impl no HIGH/MED (1 cosmetic folded). **▶ NEXT: 052** (separate name/alias vs description hash — schema migration + split_snapshot arity); 048/049/050/046 deferred to their phases (046 is cross-service: new KS user_edited endpoint, learning has no Neo4j). · **PRIOR Phase 3: FD-17/27/18/24/20/25/16/15 done; remaining FD-19, FD-26.** · **LOOM-85 (FD-15, L, FE):** planner affordances — beat_role editable (PlannerTree→editChapter), cast add/remove + resolve-unresolved (PlannerSceneRow, reuses useGlossaryRoster→editScene), planner-local model picker (PlannerView overrides panel model). Pure-FE. /review-impl verified commit body accepts beat_role+present_entity_ids (CommitChapter/CommitScene). 8 vitest + 96 composition + tsc + i18n parity (plan=31×4). 3 accepted LOWs (stale-local-model / resolve-on-display_name / roster-outage — all graceful). · **PRIOR LOOM-84 FD-16 canon-rule FE** (chain LOOM-68→84 ahead of pushed `0afc2c57` (LOOM-67 S4b); **PR #19 open** → `main` · **Phase 3: FD-17/27/18/24/20/25/16 done; remaining FD-15 (reuses useGlossaryRoster), FD-19, FD-26.** · **LOOM-84 (FD-16, M, FE):** canon create only sent text+scope + `patch` hook was unused. Built `useGlossaryRoster` (shared, reuses glossary listEntities) + `CanonRuleForm` (scope-conditional entity picker / reveal window / active) + `CanonRulesPanel` rewrite (full-payload create + edit-in-place). /review-impl verified PATCH null-clearing (nullable-update cols → scope→world clears bounds) + added edit-clears-bounds lock. 7 panel vitest + 90 composition + tsc + i18n parity 121×4. · **🏁 feature-debt Phase 1 COMPLETE; Phase 3 in progress (FD-17/27/18/24/20/25 done; remaining: FD-15/16, FD-19, FD-26).** · **LOOM-83 (FD-25, docs-only DECISION):** events-consumer throughput. PO decision: **keep deferred (resolve when the platform scales up)** — recorded as DEFERRED 072 (D-INGEST-CONSUMER-THROUGHPUT, perf) with fix recipe (batch-`gather` / D-CM3C embed-move / worker-ai replicas) + trigger (publish→extract latency / `XINFO GROUPS` lag). Rationale: heavy LLM R+E+F already parallel in worker-ai; consumer handlers are mostly ms-upserts; the one heavy per-msg cost (inline embed) is already tracked as D-CM3C; multi-instance already supported (host-based consumer_name). No code change. · **🏁 feature-debt Phase 1 COMPLETE; Phase 3 in progress (FD-17, FD-27, FD-18, FD-24, FD-20 done; remaining: FD-25, FD-15/16, FD-19, FD-26).** · **LOOM-82 (FD-20, S):** triaged 4 "dead-code" items — only `composition/api.ts getJob` was genuinely dead (no callers/tests) → deleted (tsc clean). The other 3 are documented deferrals, NOT dead code (pass2 `scene_id=chapter_id`=D-P2-PER-SCENE-FANOUT; events.py axes=D-K19e-α-01/02/03; packer L5 lens docstring). Roadmap paths were stale (corrected). · **🏁 feature-debt Phase 1 COMPLETE; Phase 3 in progress (FD-17, FD-27, FD-18, FD-24 done).** · **LOOM-81 (FD-24, S, docs-only DECISION):** the "M7d-3 dormant worker-feed" is actually a COMPLETE, dual-gated online translation fidelity judge (translation `n_feed_enabled` ships truncated src+tgt → learning `online_translation_judge_enabled` runs LLM judge → `n_fidelity` 0–1 metric). Off-by-default is correct (per-translation LLM cost) + no `n_fidelity` consumer yet → **PO decision: keep as documented opt-in** (recorded, not a silent gap). Enable recipe + revisit-trigger in the roadmap. No code change. · **🏁 feature-debt Phase 1 COMPLETE; Phase 3 in progress (FD-17, FD-27, FD-18 done).** · **LOOM-80 (FD-18/051, S, BE):** knowledge `consumer.py` was a clone of learning's but lacked the F-A1 `XAUTOCLAIM` reclaim → a failed chapter/chat/glossary event stalled in the PEL until restart (retry counter never advanced → never DLQ'd). Ported the proven reclaim (consts + `loop_count` trigger ~every 60s + `_reclaim_stale_pending`: tombstone-ack, NOGROUP-tolerant, pagination terminator). 13 tests (5 new incl. the reclaim→fail→DLQ **invariant**, not just the mechanism — /review-impl LOW). **Idempotency assumption (accept):** `min_idle=30s` reclaim can double-dispatch a >30s handler across instances; benign — `queue_event` is ON-CONFLICT-DO-UPDATE + passages delete-first (idempotent). Single-service; unit-proven. · **🏁 feature-debt Phase 1 COMPLETE; Phase 3 in progress (FD-17, FD-27 done).** · **LOOM-79 (FD-27, L, cross-svc):** silent zero-output extraction was unobservable (a reasoning model burns budget on `<think>`→empty JSON→0 entities/events, no signal). Built BOTH (PO choice): (1) **symptom guard** — worker-ai post-`extract_pass2`, substantive input + `candidates.is_empty()` → `EXTRACTION_ZERO_OUTPUT` warn + `worker_ai_extraction_zero_output_total{source_type}` (cause-agnostic); (2) **capability pre-check** — new pr `GET /internal/models/{src}/{ref}/info` (name+kind, no secrets) → worker-ai `ProviderRegistryClient` → `_is_likely_reasoning_model` name-heuristic → once-per-job `EXTRACTION_REASONING_MODEL` warn + counter (gated to extracting scopes; best-effort, lookup-fail→off). **/review-impl MED folded:** advisory fired on `glossary_sync` (no LLM) → scope-gated. worker-ai 143 pass (24 new), 1 env-only fail; pr go build+vet+units pass. **★ cross-svc live-smoke:** real refs — gemma→advisory False, qwen3.6→True, gpt-4o-mini→False (`o`-trap), missing→None. **Caveat:** heuristic is name-based best-effort (false-negatives on novel models); fires once/job for routine qwen3 extraction (intended — prompt-suppression is ~95%, not hard). · **🏁 feature-debt Phase 1 COMPLETE; now in Phase 3 (polish/UX/hardening, post-merge LOW-MED).** · **LOOM-78 (FD-17/069, S, BE):** `outline.commit_decomposed_tree(replace=True)` archived only `kind='scene'` → prior `chapter`+`arc` nodes orphaned + accumulated each re-plan. Fixed: on replace, archive `scene`+`chapter` for all `affected` chapter_ids (reaps pre-accumulated orphan chapter nodes too) + a SCOPED childless-arc sweep (only the parent arcs of the archived chapters, `NOT EXISTS` active chapter child). **/review-impl HIGH folded:** the first sweep was project-wide → would silently archive an unrelated freshly-created empty arc; re-scoped to the affected arcs only. 3 real-PG regression tests (full-replace=no orphans · partial-replace=out-of-target arc preserved · bystander empty arc survives); 47 integration pass. DEFERRED 069 cleared. · **LOOM-77 FD-21 MCP live-smoke gate** (PR #19 pre-merge debt clear; ready to merge on your go.** · **LOOM-77 (FD-21, M, cross-svc, docs-only):** proved the chat-service→knowledge-service `/mcp` tool path LIVE — `init_knowledge_client().mcp_execute_tool("memory_recall_entity", {"entity_name":"Kael"})` from the chat container → `success=True, found=True` (recalled the `Kael` entity LOOM-76 wrote; the two cycles chain). Negative bad-token → `success=False "invalid internal token"` (wire auth enforced). Real Streamable HTTP + JSON-RPC init + header scope all proven; `use_mcp_tools=True` default now live-justified (no code change). DEFERRED 056 cleared. **Repro:** `docker compose exec chat-service python -c '<mcp_execute_tool smoke>'` (recorded in DEFERRED 056). · **close-out roadmap COMPLETE (cycles 10–17); feature-debt roadmap [Phase 1 DONE](../../plans/2026-06-09-feature-debt-roadmap.md).** · **LOOM-76 (FD-2, L, FS, 3 svcs):** chat→KG was dead at 2 points — knowledge enqueued `aggregate_type='chat_session'` (worker drains `'chat'`) + worker passed `text=""`. Fixed: type-align (handlers.py) + new chat-service internal `GET /internal/chat/turns/{id}/text` (joins user Q + assistant A; role-guarded) + worker-ai `ChatClient` threaded through the turn loop. **/review-impl folded 2:** the live-smoke was contract-only → ran the **real full-chain** (auto job → endpoint 200 → extract → persist, $0.004); **chat had no auto-drain → built `_ensure_chat_pending_jobs`** (mirror of the chapter drainer: active-job guard + model/cap reuse + 1h fail-backoff) so chat→KG is now near-real-time per turn, **proven live** (scope='chat' job auto-created + drained). knowledge 4/4 chat-handler · chat 5/5 internal · worker-ai 3/3 new + 120/121 (1 env-only). **Known residue:** seeded chat session `3333…`/msgs `1111…`/`2222…` + this smoke's pending/job remain in the test-account project (harmless). · **Session:** LOOM-61 — cycle-17 063 docs-only re-defer; cycles 14/15/16 (narrative_thread S1, 064 re-confirm, cosmetic LOW batch — see chain below). Prior LOOM-51 — **🏁 BRANCH-DEBT CLEANUP CAMPAIGN COMPLETE (cycles 5–9). Cycle 9 (M, FE): CompositionPanel 5 sub-tabs → always-mounted CSS-hidden (in-progress state survives tab switch; CLAUDE.md rule). /review-impl no HIGH/MED (eager-mount + layout checked safe). 69 composition vitest + tsc clean. Branch-debt cleanup campaign COMPLETE. **▶ PO DIRECTIVE (2026-06-08): do NOT merge PR #19 yet — clear ALL remaining debt + missing features FIRST, then close the branch.** New plan: [pre-merge close-out roadmap](../../plans/2026-06-08-pre-merge-closeout-roadmap.md) — cycle 10 worker-ai status-enum (DEFERRED 065 HIGH) → 11 A2-S1b status_effects (066) → 12 K17 entity-embedding → 13 D-A3-PLANNER-FE (+orphan-arc-nodes) → 14 narrative_thread ledger → 15/16 optional+cosmetic → THEN merge. ✅ **cycle 10 DONE (LOOM-52):** DEFERRED 065 was MISDIAGNOSED — root cause was the M1 migration constraint (`complete`→`completed` + dropped `paused`/`cancelled`), NOT worker-ai; fixed by reconciling `extraction_jobs_status_check` (inline + M1 ALTER) to the full code vocab `('pending','running','paused','summarizing','complete','failed','cancelled')` (widening, NO worker-ai/state_machine/FE change); real-PG integration regression-lock; /review-impl 2 LOW + 1 COSMETIC folded. Also merged main twice into the branch (raw-search PR#23 + translation-v3 PR#24): resolved learning-service event-handler conflicts (kept BOTH composition + translation handlers) + fixed a pre-existing knowledge test (FakeProjectsRepo.create_or_get). ✅ **cycle 11 DONE (LOOM-53):** 066 diagnosis CORRECTED — the cycle-8 "canonical_id=NULL anchoring blocks the status" hypothesis was WRONG (the Pass-2 resolver uses the chapter map keyed on `entity.id`, always set, no `≥2 links` need); the whole producer path is statically CORRECT (prompt emits status_effects, parser handles, event_order threaded, resolver matches on entity.id). PO chose instrument-first → added `knowledge_extraction_status_effect_total{outcome=persisted|skipped_no_event_order|skipped_unresolved}` so the silent-skip (066's core concern) is now a metric. knowledge unit 2102 + 4 delta-asserts; live: rebuilt knowledge-service, **cycle-10 constraint swap CONFIRMED live** (pg_get_constraintdef = full 7-value set), metric exposed on /metrics. /review-impl: 1 LOW (parse-layer `_filter_status_effects` drops malformed status_effects upstream with no count → `total==0` means "no well-formed status_effect reached the writer"; documented in row 067). 066 independent of 065. **Remaining = the real-LLM death-chapter diagnostic → DEFERRED 067** (run one real extraction, read the counter — now cheap to interpret). 🟡 **cycle 12 FOUNDATION DONE (LOOM-54):** K17 entity-embedding write pipeline. **Major finding:** raw-search (just merged from main) shipped a complete embedding-write pipeline (`passage_ingester` + `upsert_passage`) → K17 REUSES the mechanics, not design-from-scratch; trigger = batch-backfill + incremental (B, PO-locked) because an entity is cross-chapter (unlike chapter-scoped passages). **Foundation (S1+S2):** `set_entity_embedding` + `find_entities_needing_embedding` (entities.py) + `embed_project_entities` (entity_embedder.py — degrade-safe, glossary-fallback, dim-validated, drain-cap); 7 unit tests + full knowledge unit 2109. /review-impl MED fixed (embedding is a derived write → does NOT bump the shared `updated_at`, which feeds recency-ordered listings). PO re-scoped: cycle 12 = foundation, **S3 wiring (internal backfill route + incremental hook) + S4 cross-service live-smoke → cycle 12b (DEFERRED 068)**. ✅ **cycle 12b DONE (LOOM-55) — K17 COMPLETE:** wired the producer via `POST /internal/extraction/embed-entities-backfill` (drain loop, 3 terminators incl. the `embedded==0` all-skip guard + soft `max_entities` cap; degrade clean; 404). 6 route unit tests + full knowledge unit. **★ LIVE SMOKE PASSED (cross-service knowledge↔provider-registry↔glossary↔Neo4j):** rebuilt knowledge-service, restored the demo project config (model 019e7f71, real book 019e7850-a8d9), backfill → **embedded 22/22 anchored** via REAL provider-registry; Neo4j emb1024=22/has_model=22/has_version=22 (was 5 hand-stamped); `/glossary-semantic` → **tier=semantic** ranked (元始天尊 0.825 / 闡教 / 姜子牙 / 玉虛宮) on the AUTO-embedded vecs — **retires the DEFERRED 061 hand-stamp**. /review-impl 4 LOW (cap-exit test added; rest accepted), no HIGH/MED. **Optional follow-up (not blocking):** auto-incremental hook after `extraction_run_completed` + auto-backfill from `change_embedding_model` (both call the same route). 🟡 **cycle 13 DESIGN-CHECKPOINT DONE (LOOM-56):** D-A3-PLANNER-FE design+plan locked in [docs/plans/2026-06-08-a3-planner-fe.md](../../plans/2026-06-08-a3-planner-fe.md) (no code, PO-chosen XL discipline — large [FE] feature, long session). Planner sub-tab in CompositionPanel (CSS-hidden), flow template+premise+model → preview tree → inline edit → commit (409→replace-confirm); MVC usePlanner+PlannerView+PlannerTree+PlannerSceneRow + locked api/types; 4 slices (S1 api+types · S2 preview+hook+tab · S3 edit+commit/replace · S4 orphan-handling+i18n+a11y). /review-impl on design folded (preview-step 400 surfacing; unmount + 409-replace mitigations). Clears D-A3-PLANNER-FE + D-A3-REPLACE-ORPHAN-ARC-NODES when built. ✅ **cycle 13-BUILD DONE (LOOM-57) — D-A3-PLANNER-FE CLEARED:** built the Planner sub-tab from the locked design — api+types + `usePlanner` + `PlannerView`/`PlannerTree`/`PlannerSceneRow`, always-mounted CSS-hidden in CompositionPanel; flow template+premise+model → preview arc→chapter→scene tree → inline edit (title/synopsis/tension + chapter intent + add/remove scene) → commit (409→named-chapters replace-confirm). tsc clean + composition vitest 76 (incl. a NO-UNMOUNT round-trip lock for the planner tab + an edit→commit-payload non-default regression). i18n×4. /review-impl: 1 fix (the no-unmount test), 3 LOW accepted (beat_role editable, model hint, cast-add-from-roster — all follow-ups). **D-A3-REPLACE-ORPHAN-ARC-NODES → DEFERRED 069** (precisely diagnosed: a BE fix in `commit_decomposed_tree` — archive the prior arc/chapter on replace — NOT FE, since the planner doesn't render the outline tree). 🟡 **cycle 14 S1 FOUNDATION DONE (LOOM-58) — narrative_thread ledger (L, BE Phase-B feature):** PO chose the FULL ledger §10.2 (over a lighter intent-reinjection lever). Built the schema+repo foundation: `narrative_thread` table (kind promise/foreshadow/question/mice_thread · status open→progressing→paid|dropped · `opened_at_node`/`payoff_node` **FK → outline_node ON DELETE SET NULL** · priority SMALLINT · the `payoff-only-when-paid` CHECK · 2 indexes incl the open-set partial) + `generation_job.state` JSONB ([migrate.py](../../../services/composition-service/app/db/migrate.py)); `NarrativeThread` Pydantic model ([models.py](../../../services/composition-service/app/db/models.py)); `NarrativeThreadRepo` ([narrative_thread.py](../../../services/composition-service/app/db/repositories/narrative_thread.py) — open_thread / update_status (payoff CASE `$5::uuid` only-when-paid) / **list_open** = the F2 re-injectable open-promise set, ordered priority DESC + created_at ASC / list_for_project). **4 real-PG integration tests** (lifecycle open→pay · payoff-only-when-paid · list_open ordering · node-delete→SET-NULL). /review-impl: MED (missing FK on opened_at_node/payoff_node → orphan refs) FIXED + an ordering test + a node-delete SET-NULL test added; 2 LOW accepted (repo takes plain-str kind/status guarded by CHECK; paid-with-null-payoff is legal). **The ledger is INERT** — no producer/consumer/eval yet → **S2 OPEN-detection (LLM/rule pass opens+pays threads) + S3 re-injection (list_open→pack F2) + compress + S4 DEBT-check + eval → DEFERRED 070** (build when long-form promise-drift is the measured pain; ADVISORY per spec D4 — never a hard gate). Foundation unblocks the merge gate (schema+repo locked; the producer is purely additive, no future migration). ✅ **cycle 15 DONE (LOOM-59) — 064 D-GROUNDING-C-ADOPT re-confirmed (XS, docs-only):** re-derived BOTH blockers against HEAD 6492f0fa (not trusting cy5) — composition's [sanitize.py:40](../../../services/composition-service/app/packer/sanitize.py#L40) still fullwidth-escapes `<`→`＜` + `⟦…⟧` directives, which the SDK's NFKC `prenormalize` ([sdks/.../sanitize.py:110-114](../../../sdks/python/loreweave_grounding/sanitize.py#L110-L114)) would FOLD BACK (breaking the `<canon>`/`<guide>` delimiter defense), and grep = ZERO `GroundingCite`/`compose_cites` consumers in composition. PO chose **keep deferred (re-confirmed), NOT won't-fix** — genuine future trigger (a cite consumer OR a sanitize rebuild); DEFERRED 064 stamped with the re-confirmation + trigger. ✅ **cycle 16 DONE (LOOM-60) — cosmetic LOW batch (M, all 3 per PO):** (a) [works.py](../../../services/composition-service/app/routers/works.py) stale comment → states D-COMP-POST-WORK-RACE resolved cy6 knowledge-side (`create_or_get`+advisory lock); (b) **revise-path truncation now surfaced** — new `ReflectResult.revise_finish_reason` ([canon_check.py](../../../services/composition-service/app/engine/canon_check.py)), `run_canon_reflect` captures the last text-producing revise pass's stop reason ([canon_reflect.py](../../../services/composition-service/app/engine/canon_reflect.py)), all 3 engine sites (auto/scene/chapter/stitch) OR `=="length"` into the job `truncated` flag (a cut-off canon-repair is no longer a silent green — closes the cy7 winner-scoped LOW); (c) [engine.py:759](../../../services/composition-service/app/routers/engine.py#L759) stitch-revise `packed_prompt="" → chapter_intent`. Carried the signal on `ReflectResult` (field, default None) not a 4th return → zero churn to 5 test sites + 3 callers. **361 composition unit** incl a new truncating-revise test + a non-default contrast (clean revise asserts `revise_finish_reason is None`). Single-service. ✅ **cycle 17 DONE (LOOM-61) — DEFERRED 063 SPUN INTO ITS OWN POST-MERGE TRACK (XS, docs-only):** re-derived 063 against HEAD 88c79eac — the year-old "internal-only shim" is NOT achievable cleanly: the SDK's `from_grounding_ref` is **lossy on `corpus_id`** (→`source_type` bucket, discards the original; no reverse adapter — [cites.py:156-168](../../../sdks/python/loreweave_grounding/cites.py#L156)), but `corpus_id` is the recook **license key** ([recook.py:408](../../../services/lore-enrichment-service/app/strategies/recook.py#L408)) + a persisted min_length=1 `SourceRef` field ([provenance.py:87](../../../services/lore-enrichment-service/app/generation/provenance.py#L87)) → a Ref→Cite→Ref round-trip breaks the license path + `source_refs_json` byte-stability; and `compose_grounding` is already byte-identical to the SDK's `merge_cites` (lifted from it) → a shim is net-negative. PO chose **spin into its own post-merge lore-enrichment track** (NOT on this branch — touching lore-enrichment-service here violates LOOM isolation + couples PR #19; the close-out gate allows 17 re-deferred). 063 re-targeted to a standalone data-shape migration (persist `GroundingCite` natively + read-compat + carry the license key on the Cite) post-merge. **🏁 PRE-MERGE CLOSE-OUT ROADMAP COMPLETE (cycles 10–17).** ▶ **NEXT: MERGE PR #19** → `main` (composition V1 + Canon arc — feature-complete; needs explicit push/merge approval). Post-merge tracks: 063 grounding-compose migration (own track); narrative_thread S2–S4 (DEFERRED 070); 067 LLM status_effect diagnostic; 069 orphan-arc-nodes BE fix. ⭐ **NEW (LOOM-62 audit):** a full [feature-debt roadmap](../../plans/2026-06-09-feature-debt-roadmap.md) (FD-1…FD-26) maps EVERY in-branch feature gap — 3-agent grounded audit found the branch has **half-built "feature-lies"** (Phase 1: narrative_thread INERT/zero-callers, chat→KG extraction is a no-op `text=""`, scene-leaf summary uses placeholder text, status_effect prompt DORMANT, full-mode `NotImplementedError`), the **Phase-B reasoning engine** designed-but-unbuilt (ReasoningState/F2, search/backtrack, autonomous arc-loop, craft_recipe+checks — eval-gated, validate-first), and **polish/hardening** (planner FE affordances, canon-rule edit, 051/048/050/.., throughput parallelism). **PO production-ready principle: no silent seams** — every FD item is BUILT or a recorded prove/build/remove decision. ✅ **Phase 1 cheap-verifies DONE (LOOM-63):** **FD-5** = stale docstring only (full-mode built+reached, no NotImplementedError — fixed docstring); **FD-22** = PO chose to BUILD the Redis push-notify (worker was already polling): knowledge XADDs a best-effort `extraction.wake` on job-start, worker-ai blocks on it (`XREAD BLOCK`) over a blind sleep → ~immediate pickup. **Wake-over-poll** (poll stays source-of-truth → no double-process); degrades to polling on any Redis fault; both sides kill-switched. /review-impl: 2 MED fixed (block=0 hang floor; cross-service stream-name pin test), 2 LOW documented. 2122 knowledge + 115 worker unit; **live-smoke PASS (real Redis :6399)**. plan [2026-06-09-fd22-extraction-wake.md](../../plans/2026-06-09-fd22-extraction-wake.md). ✅ **FD-4 DONE (LOOM-63) — found + fixed a REAL HIGH bug (066/067 RESOLVED).** Premise was wrong (the status_effect prompt was already active since fe0dae9e). The PO-insisted **canonical 067 run** (full worker chain, gemma, on a part-less death-chapter) revealed: `event_order` was derived ONLY from the part-gated `hierarchy_paths.chapter_index`, so a **flat book (chapters, no parts) silently dropped EVERY status_effect** (`skipped_no_event_order`) AND collapsed the dense timeline axis — a broad production data-loss bug (many books are flat). **Fix:** thread the chapter's own `sort_order` as a separate `chapter_index` (pass2_writer fallback + persist-pass2 request field + worker client + runner capture-outside-part-gate). **Live-proven** on the rebuilt stack: part-less death-chapter → 2 fresh `:EntityStatus{gone}` with `from_order`=chapter_sort×stride (was 0). Also fixed the stale event.py dormancy comments; confirmed 065 (job→complete on the cycle-10 constraint). **/review-impl ×3** (predicted the stitch failure → found the bug → hardened: ge=0, runner+model regression tests). knowledge 2127 + worker 116 unit (5 new). **Filed FD-27** (zero-event/reasoning-model extraction guard — reasoning models silently emit empty → 0 status). Dev residue: gemma model `51ea9fd7` + 2 `:EntityStatus(bryn)` in dev KG (harmless). ✅ **FD-1 reshaped + S2 DONE (LOOM-64).** PO chose to build the FULL narrative_thread feature, sliced (not a "minimal" sliver). **S2 (producer) built:** `detect_and_update_threads` ([engine/narrative_thread.py](../../../services/composition-service/app/engine/narrative_thread.py)) — a per-Work-gated (`narrative_thread_enabled`, default off), best-effort LLM pass on the auto-generate commit (scene+chapter) that OPENS promises/foreshadows/MICE-opens + PAYS resolved open threads. The ledger now has a real WRITER (no longer inert). Never fails generate (F1); dedup-by-fold + bounded (max_open + top-50 context cap); reasoning-disabled (FD-4 lesson); regen-idempotent; project-scoped pays. **11 unit tests** (detector logic + the extracted `_maybe_detect_narrative_threads` wiring). /review-impl: MED (wiring untested → helper + 3 tests) + LOW (context cap) fixed; 1 LOW (concurrent-dup TOCTOU) recorded as accepted. 372 composition unit; single-service. plan [2026-06-09-fd1-narrative-thread-s2-producer.md](../../plans/2026-06-09-fd1-narrative-thread-s2-producer.md). ✅ **S3 (re-injection = FD-7) CORE DONE (LOOM-65) — the loop is CLOSED.** `gather_open_promises` lens → `LensBundle.open_promises` → a protected, **sanitized** (SEC3), capped (`pack_open_promises_cap=8`, priority-ordered) `<open_promises>` pack block ([assemble.py](../../../services/composition-service/app/packer/assemble.py)/[lenses.py](../../../services/composition-service/app/packer/lenses.py)/[pack.py](../../../services/composition-service/app/packer/pack.py)) + a `build_messages` **steer** (honor/advance/pay open promises; gated by block presence so the default prompt is unchanged). Gated identical to S2 (`narrative_thread_enabled`), degrade-safe, both pack sites wired. **14 unit tests** (assemble block protected/ordered/blank-skip/sanitized; lens map/cap/degrade; build_messages steer-present/absent; pack-gate enabled/flag-off/no-repo). /review-impl: MED (unsanitized re-injection → `sanitize_lore`) + LOW (no steer → added) + LOW (pack-gate untested → 3 tests) fixed; LOW (position-filter forward-leak edge) recorded → S4 spoiler-axis. 386 composition unit; single-service. plan [fd1-narrative-thread-s2-producer.md](../../plans/2026-06-09-fd1-narrative-thread-s2-producer.md) (S3 in the same arc). ✅ **S4a (DEBT-check BE = FD-8 part 1) DONE (LOOM-66):** the §7 unpaid-promise debt surface — `GET /works/{project_id}/narrative-threads` (`status=open` = the unpaid debt, priority-ordered | `all` = the full ledger; `open_count` = a TRUE `count_open`, NOT capped by the list LIMIT — review-impl MED#1) + an advisory `open_promise_count` on the chapter-generate result ([narrative_threads.py](../../../services/composition-service/app/routers/narrative_threads.py) · repo `count_open` · engine `_open_promise_count` helper). Read-only, advisory per spec D4 (never a hard gate), gated identical to S2/S3. **7 router unit tests** (a count≠len non-default lock + the helper gate); /review-impl: MED#1 (open_count was `len(list_open)` → capped → silent under-report → fixed with `count_open` true COUNT) + LOW#3 (helper gate test) fixed, 2 LOW accepted (count+list = two queries; chapter-only flag). **393 composition unit**; single-service. ✅ **S4b (eval arm + full-loop live-smoke = FD-8 part 2 → FD-8 COMPLETE) DONE (LOOM-67):** the §8 validate-first eval + the live proof. **Code:** `PackedContext.reinjected_promise_count` (a DETERMINISTIC per-generation S3-fired signal, distinct from S4a's arc-end debt) echoed on the scene-auto + chapter responses; [engine/promise_audit.py](../../../services/composition-service/app/engine/promise_audit.py) — a **ledger-BLIND** dropped-promise judge (re-detects promises from PROSE, never from narrative_thread, so it scores both arms apples-to-apples — anti-self-reinforcement) + `POST /internal/composition/eval/promise-audit`; [scripts/eval_narrative_thread.py](../../../services/composition-service/scripts/eval_narrative_thread.py) — the n-book ON/OFF harness + inline full-loop live-smoke. **★ FULL-LOOP LIVE-SMOKE PASSED** (rebuilt composition + booted gateway/auth; real stack composition↔provider-registry↔glossary↔book↔auth↔gateway, LM Studio drafter + disjoint gemma judge): all 4 asserts green on premise #1 across n=1 AND n=3 — **S2 opens** (scene 1) → **S3 re-injects** (`reinjected_promise_count` grows as the ledger accumulates) → **control** OFF=0 rows → **S2 pays** (5/7/7 paid across 3 books). **The loop is PROVEN end-to-end live** (narrative_thread is functional, NOT a feature-lie). **⚠️ EVAL = NULL (no measured lift):** dropped-promise-rate OFF mean=0.454 vs ON mean=0.429 (Δ=+0.025, within noise; ON wins 1/3 books; high variance 0.29–0.57; `introduced` counts swing 7–12, confirming the re-injection introduced-inflation confound). Per validate-first §8 the gate to expand into the SPECULATIVE Phase-B levers (FD-9 search/backtrack, FD-10 autonomous loop, FD-12/13 recipes) is **NOT met** → they stay deferred/speculative. The ledger SHIPS as **ADVISORY (D4), opt-in (default off)** — functional + live-proven, giving authors the §7 debt view (S4a) + re-injection steering, WITHOUT claiming a quality lift the metric can't show. **381 composition unit** (13 new incl. a dropped_rate-clamp lock). /review-impl: MED#1 (dropped_rate could exceed 1.0 when the LLM lists `dropped ⊄ introduced` → clamped [0,1] + regression lock) fixed; 2 LOW documented (separate-plan-per-arm structural confound; introduced-inflation), 2 LOW accepted. plan [fd8-narrative-thread-s4b-eval-livesmoke.md](../../plans/2026-06-09-fd8-narrative-thread-s4b-eval-livesmoke.md). **🏁 narrative_thread (FD-1/6/7/8) COMPLETE — built, live-proven, measured (null lift, honestly recorded; no silent seam).** ▶ **NEXT: Phase-1 platform gaps — FD-3** (scene-leaf prose placeholder) / **FD-2** (chat→KG no-op extraction `text=""`) / **FD-21** (MCP live-smoke gate); then **merge PR #19** (the composition V1 + Canon + narrative_thread arc is feature-complete — PO's call: merge-now-with-fast-follow vs after FD-2/3). Optional narrative_thread follow-up: `ReasoningState`/`generation_job.state` persistence (resumable). ✅ **EVAL v2 + GENERATION-QUALITY DIAGNOSTIC DONE (LOOM-68):** PO asked to re-examine the eval before trusting the FD-8 null. **(1) Strong-judge (Claude) hand-read of the v1 dumps** found v1's null was confounded (unstable `introduced` denominator + truncated-arc cutoff mislabeling sustained tension as dropped + coarse local judge). **(2) Built eval v2** — a FIXED, SPEC-derived (premise+plan, ledger-blind) tracked-promise set scored identically on both arms as paid/progressing/abandoned/absent ([promise_audit.py](../../../services/composition-service/app/engine/promise_audit.py) `extract_tracked_promises`/`score_promise_coverage` + `/promise-extract` + `/promise-coverage`), plus harness **chapter-mode** generation (`--mode=chapter`, default) so arcs REACH the payoff (per_scene+short-tok truncates before it → pay-rate stuck at 0). **(3) Conclusive run:** v2 + chapter mode **now DISCRIMINATES** (pay-rate spans 0–1, was stuck at 0). n=1 hinted a big lift (ON pay 1.00 vs 0.33) but **the powered n=3 did NOT replicate** — pay OFF=0.500 vs **ON=0.292** (ON WORSE; paid/tracked OFF 0.477 vs ON 0.292), abandon ON=0.000 vs OFF=0.056, sustained ON marginally higher. **The LOOM-67 null HOLDS under the better metric** (the n=1 was variance — LOOM-32 pattern). Only consistent signal: the ledger makes the model **abandon less / sustain more but PAY less** (biases toward keeping threads open over resolving them). **(4) GENERATION-QUALITY architecture probe:** ran chapter-mode on the same premise → **kiến trúc is sound, NOT broken** — chapter single-pass produces a coherent, progressing arc (re-establishment GONE; gemma 1 defect vs V0 8); the per_scene weakness was mode-choice. **Sentence-craft ceiling = the MODEL** (qwen3.6-35b-local): stylistic tics ("the wind did not X; it Y"), recurring "bruised sky" imagery across all books, and a CJK code-switch (`紧绷` leaked into English prose) persist even in chapter mode → only a stronger drafter (BYOK frontier) or an anti-repetition lever helps. **392 composition unit** (11 new v2 tests); /review-impl MED (pay_rate denominator → added paid/TRACKED cross-check, conclusion robust) + 1 LOW. plan [2026-06-10-fd8-eval-v2-fixed-promise-coverage.md](../../plans/2026-06-10-fd8-eval-v2-fixed-promise-coverage.md). **Implication:** the eval is now methodologically sound and STILL shows no ledger lift → FD-9/10/12/13 stay deferred with stronger evidence; the ledger ships advisory/opt-in as-is. ▶ **NEXT (PO multi-cycle plan, in order):** **(a)** [optional, key-blocked] frontier-drafter ceiling probe — needs a cloud BYOK key; **(c)** FE settings control — expose `narrative_thread_enabled` (currently UNexposed) + consolidate assembly_mode/guide/drafter-model per work (merge-patch pattern from `useChapterAssembly`); **(d)** anti-repetition + cross-chapter coherence levers (the highest-value quality lever now, per the diagnostic — helps BOTH modes, partly model-independent). Then FD-3/FD-2/FD-21 + merge PR #19. ✅ **(c) DONE (LOOM-69) — composition settings FE (quality-lever, author control):** a new **"Settings" sub-tab** in CompositionPanel ([CompositionSettingsView.tsx](../../../frontend/src/features/composition/components/CompositionSettingsView.tsx)) exposing the per-Work settings authors should control: the **narrative_thread ledger toggle** (was UNexposed — only settable by an API call), the default **assembly_mode** (per_scene/chapter), and a **persisted default drafter model** (the #1 prose-quality lever per the diagnostic). Generic `useSetWorkSettings` merge-patch hook (server replaces the whole settings blob → MUST merge, like `useSetAssemblyMode`); the panel's model selector now derives `effectiveModelRef = session-pick \|\| persisted-default` (no useEffect, mirrors `effectiveScene`) with a **stale-default guard** (a deactivated default_model_ref falls back to '' — initialvalues-respect-dynamic-gates lesson). CSS-hidden sub-tab (state survives tab switch). BE already consumes all three (verified: engine `narrative_thread_enabled` gate, `resolve_assembly_mode`, per-request `model_ref`) → pure FE. i18n ×4 (en/vi/ja/zh-TW). **84 composition vitest** (8 new: initial-values, merge-preserve, toggle-off-sends-false, model/mode patch, pending-disable, error) + tsc clean. /review-impl: LOW (stale default model) fixed. ✅ **(d) DONE (LOOM-70) — anti-repetition craft lever + repetition metric:** added an **imagery/opening-variation clause** to the drafter system prompt ([cowrite.py](../../../services/composition-service/app/engine/cowrite.py) `build_messages` — "Vary your prose: do NOT reuse a distinctive image, metaphor, or sentence-opening you've already used…") targeting the diagnostic's stylistic-tic gap (the anti-RE-ESTABLISHMENT clause from LOOM-36 was already present; this adds anti-REPETITION). Always-on, model-agnostic, unit-locked. Added a **deterministic repetition metric** (`repetition_score` = opening-repeat + trigram-repeat, NO LLM) + a tic-counter to the eval harness (permanent measurement assets). 393 composition unit (1 new clause-lock). **Measured (rebuilt composition, live n=1 chapter):** the deterministic metric is **noise-level** (post OFF trigram 0.046 vs pre 0.053 — repetition was already numerically tiny ~5%, so the metric under-captures the *perceptual* tic); the **tic-counter** is more telling — post-clause OFF arm = **0** "wind did not X" / 0 "bruised" / 0 CJK-leak (pre-clause book-1 OFF had the tic 4+×), but the ON arm still ticced 3× → **DIRECTIONAL improvement, NOT conclusive** (n=1, variance; consistent with the diagnostic that a prompt clause is a *mild, model-bound* lever — the real ceiling is the local 35B). Clause ships as a no-downside craft improvement; metric+counter enable future powered validation. /review-impl: 2 LOW (clause is aspirational — model only sees the context window, not the whole work; metric under-captures perceptual repetition) accepted+documented. **🏁 PO multi-cycle plan (b→c→d) COMPLETE.** ▶ **NEXT:** **FD-3** (scene-leaf prose placeholder) / **FD-2** (chat→KG no-op extraction) / **FD-21** (MCP live-smoke), then **merge PR #19**. Optional: **(a)** frontier-drafter ceiling probe (needs a cloud BYOK key — confirmatory only). **Pending push:** LOOM-68/69/70 are local (ahead of pushed `0afc2c57`). ✅ **(a) DONE + a REAL BYOK BUG FIXED (LOOM-71):** PO supplied an OpenAI key; registered it (provider-registry `POST /v1/model-registry/providers` + `/user-models`, gpt-4o, default pricing → no 402). The probe surfaced a genuine cross-cutting bug: the gateway forwards `reasoning_effort` to ALL openai-compatible adapters, but real OpenAI gpt-* chat models **400 on it** (`Unrecognized request argument supplied: reasoning_effort` — it's o-series-only); composition's `reasoning_effort="none"` (local thinking-suppression) thus **broke EVERY OpenAI non-o-series BYOK chat model**. **Fix:** `stripDefaultOpenAIUnsupportedFields` in [adapters.go](../../../services/provider-registry-service/internal/provider/adapters.go) — on the DEFAULT OpenAI endpoint, strip `chat_template_kwargs` (already, TR-4) + `reasoning_effort` UNLESS o-series (`openaiIsReasoningModel`: o1/o3/o4/o5); custom base_url (local OpenAI-compat) keeps both. 3 new Go unit tests; **live cross-service smoke: gpt-4o now generates** via composition→gateway→OpenAI (was 400). Memory [[feedback_openai_reasoning_effort_o_series_only]] (inverse of gateway-passthrough-forward-all). Probe-tooling: harness `_req` now surfaces upstream error bodies + a `--drafter=<id>` override. **★ CEILING READ (the (a) finding):** gpt-4o on the SAME pipeline = **clean, tic-free prose** (trigram-repeat 0.002 vs qwen's ~0.05, no "wind did not X" tic, no CJK leak), honors grounding (cast + sealed-letter + traitor threads) + continues mid-scene (no re-establishment) → **CONFIRMS the architecture is sound; the prose ceiling was the local 35B model** (qwen has higher vivid-highs but tic/leak lows; gpt-4o is more controlled/consistent). The quality lever is author-selectable stronger drafter (now exposed via the LOOM-69 settings default-model) + chapter mode, NOT more ledger. Cost: ~$0.15 of the $2. **Note:** the chapter-mode decompose with gpt-4o returned NO_CHAPTER_PLAN — **diagnosed (LOOM-73): it was the SAME reasoning_effort bug** (the decompose LLM call 400'd → degraded silently to empty scenes → NO_CHAPTER_PLAN at generate), **RESOLVED by LOOM-71** (verified: gpt-4o now decomposes 3/5/4 scenes). Robustness hardening (LOOM-73): `EMPTY_DECOMPOSE_PLAN` 400 guard at commit (after IDOR) so an all-empty plan fails fast with an actionable code instead of silently committing nothing → mysterious NO_CHAPTER_PLAN later. +1 unit test (394 total). **PO: rotate the OpenAI key (shared in plaintext).** ✅ **MEASUREMENTS RECORDED + 2 BUGS FIXED (LOOM-72/73/74):** **LOOM-72** — durable [composition-quality-measurements.md](composition-quality-measurements.md) (qwen vs gpt-4o n=3 chapter pay/abandon/sustained/repetition + conclusions + cost) so we never re-spend to re-measure; + harness `--drafter`/`--decompose` overrides. **★ powered gpt-4o n=3 ceiling eval:** gpt-4o beats qwen on every metric (pay 0.61–0.63 vs 0.29–0.50; trigram-repeat 0.035 vs ~0.05) → ceiling=local model confirmed; **the ledger's value is MODEL-DEPENDENT** — neutral/negative on qwen (ON pay 0.500→0.292), neutral-positive on gpt-4o (pay flat, abandon 0.074→0.033, sustained 0.926→0.967). Cost $0.34 console. **LOOM-73** — cloud-decompose robustness (above). **LOOM-74 — billing-accuracy fix:** root-caused the internal-vs-console ~2× gap to `usage-billing server.go:258` billing a **flat $2/1M total tokens** ignoring per-model `Pricing` → cloud UNDER-billed (gpt-4o out=4×in) + local WRONGLY charged. Fix: provider-registry computes the authoritative cost from `Pricing` (the same `actual` it reconciles with) → sends `total_cost_usd` in `/record` (worker + stream settle); usage-billing `recordCostUSD` uses it, flat fallback only when absent. **★ live-verified:** gpt-4o now bills per-dimension (ratio **1.000**, was 0.335 flat), qwen-local bills **$0.000000**; reconcile + audit now agree. 3 new Go tests both services. Memory [[feedback_actual_cost_must_use_per_model_pricing]]. ✅ **FD-3 DONE (LOOM-75) — scene-leaf summary real prose (knowledge+book):** `summary_processor._load_scene_leaf_texts` was a stub returning Neo4j `s.path` strings → chapter/part/book summaries were silently built from path noise. Rewired to fetch REAL scene `leaf_text` from book-service (`get_book_client().list_scenes_by_chapter`, ordered) mirroring the production-proven `pass2_orchestrator._fetch_chapter_leaf_text` D8 contract; `None`(transport)→`_DefensiveCheckFailed` (transient re-enqueue) vs `[]`(legacy)→`get_chapter_draft_text` fallback (PO chose: legacy chapters get real draft summaries, not abandoned). **5 unit tests** (real-ordered-prose, legacy→draft, scenes-no-text→draft, transport→transient, truly-empty→[]); existing tests insulated (patch the fn). **2132 knowledge unit. ★ live-smoke knowledge↔book: book-service /internal/.../scenes returns real `leaf_text`** ("The story opens with a quiet morning…") — the exact contract the fix reads. The summary md5 naturally re-keys (paths→prose) → chapters re-summarize correctly (no migration; old path-summaries were garbage). ▶ **NEXT: FD-2** (chat→KG no-op extraction `text=""`) / **FD-21** (MCP live-smoke gate), then merge PR #19. **Pending push:** LOOM-68→75 local.** · LOOM-50 — **BRANCH-DEBT cleanup cycle 8 (L, verification, no prod code): live-stack verify + plan quality. (A) plan-ledger-drift → re-scoped Phase-B feature (narrative_thread unimplemented; P1 drift was a resolved grounding artifact). (B) D-A2S1B2 → DEFINITIVELY DIAGNOSED via the faithful full publish→worker-ai chain (benchmark PASS recall@3=1.0 → real LLM extraction): producer does NOT emit :EntityStatus from a real death-chapter. Filed 2 producer bugs (out of branch): DEFERRED 065 worker-ai status 'complete'≠'completed' (HIGH, breaks extraction completion platform-wide), 066 status_effects/anchoring gap (MED). ▶ NEXT cycle 9: FE Assemble sub-tab CSS-hidden refactor (last campaign cycle).** · LOOM-49 — **BRANCH-DEBT cleanup cycle 7 (M): truncation surfacing. Composition-only (gateway already provides + normalizes finish_reason; roadmap's cross-service assumption was wrong). DraftMetering+finish_reason captured in stream_draft/_one_draft/stitch_chapter → truncated=='length' on all 4 engine paths. /review-impl: MED auto-path test + 2 LOW documented. 336 unit incl real-capture tests. ▶ NEXT cycle 8: D-A2S1B2 publish→worker-ai live-smoke + decompose-plan-ledger-drift.** · LOOM-48 — **BRANCH-DEBT cleanup cycle 6 (L, cross-service): job & work lifecycle. (A) D-COMP-POST-WORK-RACE → knowledge `create_or_get` (book-typed, advisory lock 0x4B50, 200-on-existing); composition needs no race change. (B) D-COMP-CHAPTER-INFLIGHT-REAPER → reap_stale_jobs + opportunistic-in-guard + lifespan sweep (job_reaper_sweep_secs=600). /review-impl: MED committed reaper tests + 2 LOW (OpenAPI 200 doc, partial index) fixed. 326 unit + 40 comp-integration + 22 knowledge-integration + live smoke 8/8. ▶ NEXT cycle 7: truncation surfacing (finish_reason, cross-service).** · LOOM-47 — **BRANCH-DEBT cleanup cycle 5 (M): ledger close-out + contract locks, NO behavior change. D-COMP-M2-XREF-OWNERSHIP CLEARED (reparent-cycle prevention was already wired+tested; stale comment fixed). D-COMP-TIPTAP-SHAPE-DRIFT LOCKED (new source-canary test reads book tiptap.go + asserts 7 shape tokens). 064 re-scouted → still no slice. STITCH-PERSCENE-CEILING + b-đúng prose-carry formally CLOSED won't-fix. 326 unit. Roadmap committed → [branch-debt cleanup](../../plans/2026-06-08-branch-debt-cleanup-roadmap.md). ▶ NEXT cycle 6: job & work lifecycle (inflight-reaper + post-work-race).** · LOOM-46 — **PROD-debt cycle 2: chapter/stitch job in-flight guard (M). NEW `create_chapter_job_guarded` — per-(project,chapter) advisory lock (`0x10B0`) → replay-under-lock → in-flight guard (any recent node-less chapter job, cross-operation) → 409 `CHAPTER_JOB_IN_FLIGHT`. PO-locked 409-reject. /review-impl: MED#1 staleness window (`chapter_inflight_stale_secs=1800`, crash-orphan lockout fix) + LOW#2 `_persist_chapter_draft` except Exception + LOW#4 partial index, all fixed; #3 documented. 324 unit + LIVE-SMOKE 6/6 vs real PG (incl asyncio.gather race 1-win-1-block + stale-no-block). ▶ CAMPAIGN COMPLETE (cycles 1-4 all done) → MERGE PR #19.** · LOOM-45 — **PROD-debt cycle 4: verification + drift locks (M). chapter_sort double-fetch FIXED (`PackRequest.chapter_sort_hint` → the chapter endpoint passes the sort it already fetched so `pack()` skips the redundant `book.get_chapter_sort_orders`; unit-locked both that pack USES the hint and that the endpoint PASSES it). tiptap-drift ACCEPTED (composition-side-locked: prose_doc golden + B4 persist smoke; no clean Python↔Go automation). **D-A2S1B2 live-smoke ATTEMPTED + INCONCLUSIVE** — bounded `extract-item` ran the real pass-2 extractor on a death-chapter (3 entities/events) but wrote NO :EntityStatus (standalone path → `event_order=None` → A2-S1b-1 skips status_effects); a faithful test needs the full publish→worker-ai chain (chapter_index→event_order via P3) + fresh-book bootstrap → **D-A2S1B2 stays DEFERRED** (refined: bounded path insufficient). /review-impl: 1 LOW fixed (wiring lock). 322 unit + py_compile. Single-service. **→ Prod-debt campaign substantially DONE (cycles 1,3,4 + cycle-2 mitigated); ready to MERGE PR #19.**** · LOOM-44 — **PROD-debt cycle 3: long-form token hardening (M). Deterministic anti-truncation: chapter/stitch `max_out = min(scene_count × chapter_gen_per_scene_tokens(700), ceiling(8192))` (was flat 4096) so long chapters get proportional room (language-agnostic); compress input cap (`cap_recent_prose` — keep most-recent N chars, D-COMP-COMPRESS-INPUT-CAP). /review-impl: MED-1 — the planned estimate truncation flag was DROPPED (char_estimate /3 ≈ 1.33× real EN tokens → `>=max_out` false-flags normal EN chapters at ~75% budget AND never fires for CJK; a misleading signal is worse than none). Accurate truncation surfacing deferred → D-COMP-TRUNCATION-SURFACING (gateway finish_reason). 321 unit + py_compile. Single-service deterministic (no live rebuild). → cycle 4 next (verification + drift locks).** · LOOM-43 — **PROD-debt cycle 1: decompose-commit safety (M). D-A3-COMMIT-IDEMPOTENCY + D-A3-COMMIT-TRUE-REPLACE cleared. NEW `decompose_commit` ledger table (exactly-once, unique (user,project,key)) + `outline.commit_decomposed_tree` (per-project advisory xact lock → replay → in-Tx guard → replace-archive-scenes → insert → ledger; UniqueViolation backstop). plan.py `CommitRequest +replace/idempotency_key` (`force`=deprecated alias), `AlreadyPlannedError`→409, returns `replay`. /review-impl: 2 MED fixed (MED-1 advisory lock closes no-key concurrent TOCTOU the plain SELECT left open; MED-2 idempotency scope (user,key)→(user,PROJECT,key) — endpoint is per-project, caught by the smoke replaying a deleted book's arc); LOW-1 D-A3-REPLACE-ORPHAN-ARC-NODES + LOW-2 ledger-no-GC documented. 319 unit + LIVE-SMOKE PASS 5/5 (keyed replay no-dup · 409 re-plan · replace→2-not-4 · concurrent-no-key→[201,409] 2-not-4). → next debt cycles: 2 chapter/stitch job idempotency · 3 long-form token hardening · 4 verification+drift locks.** · LOOM-42 — **BUILD FE assembly-mode (L, [FE]) — closes the human-feedback gap for chapter/stitch. NEW "Assemble" sub-tab in `CompositionPanel` (`ChapterAssembleView` + `useChapterAssembly`): assembly_mode toggle (per_scene|chapter, merge-patches `work.settings`), Generate-chapter (B2) + Stitch-chapter (B3, gated all-scenes-done) buttons, editable preview = inline edit-capture, Accept/Regenerate/Reject → `submitCorrection(jobId)` → `composition.generation_corrected` → learning-service. persist=false (no draft clobber; Accept inserts via onAccept). api.ts +generateChapter/stitchChapter/patchWork; types +ChapterGeneration/AssemblyMode; i18n×4. /review-impl: MED-1 fixed (settings-merge was hook-internal + component test mocked it → added useChapterAssembly hook test proving the merged patch); LOW-1/2 documented (FE-trim vs BE-block edit gate; api body unverified by a live FE call). 67 composition vitest (9 files, +12 new) + tsc clean. FE-only (BE live-smoked B4). → human-quality data now accrues for chapter/stitch once authors use the tab.** · LOOM-41 — **BUILD B4 (eval `--assembly` 3-arm + LIVE-SMOKE, M) — chapter-assembly-modes ARC COMPLETE. `scripts/eval_a_grounded.py +--assembly={per_scene|stitch|chapter|all}` builds the 3 arms on ONE shared book+plan, judges each vs V0; `--persist-smoke` = the cross-service persist round-trip. ★ LIVE-SMOKE PASSED (rebuilt composition→HEAD 55d65642, ran across composition+book+glossary+knowledge+provider-registry): PERSIST ROUND-TRIP PASS (assembled chapter → book draft → round-trips as valid Tiptap, NO shape drift); n=1 directional — per_scene 0-1 V0 (10 vs 0 defects, granularity loss), stitch 1-0 V0 (1 vs 6), chapter 1-0 V0 (0 vs 6). GRANULARITY THESIS CONFIRMED apples-to-apples: stitch+chapter beat V0, per_scene loses on granularity not state — matches eval_a_fair 3-0. Cleared D-COMP-ASSEMBLY-LIVE-SMOKE + D-COMP-TIPTAP-SHAPE-DRIFT. 317 unit unaffected. No /review-impl (dev-only harness; B2/B3 prod code already reviewed).** · LOOM-40 — **BUILD B3 (per_scene+stitch path + MED-2 persistence, XL): NEW `POST /works/{p}/chapters/{chapter_id}/stitch` — gated on all-scenes-done (409 SCENES_NOT_DONE) → gather the chapter's scene winners (`jobs.chapter_scene_drafts`; 0→400 NO_SCENE_DRAFTS) → `engine/stitch.py` `stitch_chapter` ONE LLM pass (dedup intros/echoes, smooth transitions, change-no-facts; char-cap head+tail `cap_scene_drafts` MED-3; degrade→raw concat) → post-stitch `run_canon_reflect` → best-effort persist to book draft. **MED-2 DONE (both modes):** shared `_persist_chapter_draft` writes the assembled chapter to the book-service DRAFT via `patch_draft` (read-version→write, 409-safe, NEVER discards text — cross-store best-effort); book stores body verbatim (no server text→tiptap) so NEW `engine/prose_doc.py` `text_to_tiptap_doc` replicates book's `tiptap.go` shape (top-level `_text`). B2 chapter endpoint also persists now (+`persist` flag on both). 317 unit (296→+21). /review-impl: no HIGH/MED; LOW-1 fixed (persist-after-canon-revise regression-lock), LOW-2 documented (D-COMP-TIPTAP-SHAPE-DRIFT). Single-service code; cross-service persist live-smoke deferred to B4.** · LOOM-39 — **BUILD B2 (chapter single-pass path, L): NEW `POST /works/{p}/chapters/{chapter_id}/generate` (PO: separate endpoint) — loads the A3 decompose plan (`outline.scenes_for_chapter`; 0 scenes→400 NO_CHAPTER_PLAN) → builds a synthetic in-memory pack node (NEVER persisted; `chapter_id`, `story_order=sort*1000`, union cast, combined synopsis via new `engine/chapter_gen.py`) → packs at the chapter position → single `diverge(k=1)` draft (`chapter_gen_max_tokens`) → chapter-level `run_canon_reflect` over the union cast → JSON. B1's `/generate` 501 guard → 409 `USE_CHAPTER_ENDPOINT` (explicit override only; a work whose DEFAULT is chapter still allows per-scene co-write). `_STRIDE=1000` deduped → shared `STORY_ORDER_CHAPTER_STRIDE`. 296 unit (283→+13). /review-impl: 1 MED fixed (`draft_chapter` op wasn't in `_OPERATION_INSTRUCTIONS` → whole-chapter pass got the per-scene instruction; registered + regression-locked), LOWs tracked; no HIGH. Single-service; 3-mode live-smoke deferred to B4.** · LOOM-38 — **BUILD B1 (chapter-assembly-modes plumbing, M): work `settings.assembly_mode` {per_scene|chapter} validated enum (default `per_scene`), config default + declare-ahead `chapter_gen_max_tokens`/`stitch_max_tokens` knobs, pure `resolve_assembly_mode` (override→setting→default, bad-value fallback), `GenerateBody.assembly_mode` override (422 on bad), 501 `ASSEMBLY_MODE_NOT_IMPLEMENTED` guard for `chapter` (before any job — B2 replaces it), `WorkPatch` settings enum validator, mode echoed on all 3 response paths. 283 unit (269→+14). /review-impl: 4 LOW/COSMETIC folded (auto-replay echo, drift-lock test, auto-echo test, 501-before-create assertion); no HIGH/MED. Single-service; live-smoke of 3 modes deferred to B4.** · LOOM-37 — **DESIGN-checkpoint: chapter-assembly-modes ([plan](../../plans/2026-06-07-comp-chapter-assembly-modes.md)) — work `assembly_mode` {per_scene+stitch | chapter}; /review-impl folded (MED: chapter-scoped entry, book-service draft storage, stitch input-cap; canon-check VERIFIED equivalent-granularity, NOT a regression). BUILD (B1-B4) deferred to a fresh session per XL discipline.** · LOOM-36 — **anti-re-establishment prompt instruction SHIPPED (`cowrite.build_messages`, shared by auto[select.py:98]+cowrite): gross cross-chapter re-narration GONE — the P0 A3 draft is now ONE continuous arc (was re-narrated 3×); defects 20→17; 269 unit. A3 still loses the concat pairwise but now on GRANULARITY (9 stitched scenes vs 3 single-pass), NOT state — the C eval already validated per-scene at equal granularity (3-0). PO pivot → chapter-stitch/granularity.** · LOOM-35 — **⚠️ the LOOM-33/34 "b-đúng SUPERSEDED" claim was WRONG.** Seeding the KG timeline per chapter (`eval_a_grounded --seed-kg`, 10/9 events) + the fixed lens did **NOT** fix re-establishment: A3 0-2, defects A3:20 vs V0:1 (worse than bios-only A3:11); the P0 arrival (treeline→Bryn-gate→Mira-hall) is **re-narrated 3× across the 3 chapters** despite the `memory` block carrying prior events. **KG event-carry is necessary-but-INSUFFICIENT** — re-establishment is a PROSE-continuity problem (S1 prose-reinjection is intra-chapter only). The **b-đúng cross-chapter PROSE carry is NOT superseded**; the timeline-axis fix stands (real bug, facts needed for non-contradiction). · LOOM-33 — **Round 2 RESOLVED: the chapter-boundary re-establishment residual was the KG timeline lens querying the SPARSE `chronological_order` axis (dateless events — the majority, esp. CJK — silently dropped) so prior-chapter plot never carried. FIXED: `gather_timeline`/`filter_inworld_events` → DENSE `event_order` axis (`before_order=scene_at_order(sort)`) + a RECENT-WINDOW (`after_order`, `pack_timeline_recent_chapters=5`; endpoint orders event_order ASC+LIMIT). 268 unit; /review-impl MED#1(window)+LOW#2 folded. The b-đúng prose-carry mechanism is SUPERSEDED — the KG carries cross-chapter plot once the lens axis is right.** · LOOM-34 — **D-COMP-TIMELINE-AXIS-LIVE-SMOKE PASSED** (brought the stack back up: rabbitmq/neo4j + the service chain; rebuilt composition to dcd6ca7d): `diag_kg_timeline_carry` shows the ch2 scene's pack now carries BOTH ch1 events incl. the **dateless** "Kael is cast into exile" (pre-fix dropped); ch1 scene memory empty (correct — own/future excluded). The fix is live-proven. · LOOM-32 — **⚠️ CORRECTION: the LOOM-31 "A3 BEATS V0 1-0-1 / first genuine win" did NOT replicate.** An n=3 re-run FAILED the gate (V0 2-1, defects A3:16 vs V0:9); the n=2 win was LLM variance + a confound (knowledge-svc rebuilt between runs). A diagnostic + a grounding-first eval (`scripts/diag_s1_regurgitation.py`, `scripts/eval_a_grounded.py`) isolated the real causes: (1) the Bryn-style **gender flip = eval UNDER-GROUNDING** (bare glossary stubs, no bio) → FIXED by seeding bios (defects 16→11, W/L 1-2→1-1); (2) chapter-boundary **RE-ESTABLISHMENT = a real residual** needing cross-chapter PLOT-state carry. S1+S2 still fire (compress engaged 5×), they just don't win the concat metric alone. Prior LOOM-30 S1 (spurious, now corrected); LOOM-29 PLAN (S4a: canon panel + Revise, /review-impl 3 fixes; S4b: publish-gate canon blocked/unchecked chip, /review-impl 4 fixes; 68 vitest pass, tsc clean; see ✅ below). · Prior **LOOM-20 — the full A2 entity-status canon arc (9 commits):** `db68ab25` A2-S1a (:EntityStatus primitive) · `829d01e7` **CM3b-RETRACT-FIX** (a pre-existing HIGH canon-drift bug A2-S1b-1 VERIFY surfaced — retract was a silent no-op) · `e868254a` A2-S1b-1 (status persist plumbing) · `fe0dae9e` A2-S1b-2 (prompt activation + backfill, relative-A/B eval-gate) · `d80b8ba6` A2-S2 (fact-for-check read) · `7c3a835e` A2-S3a (SCORE symbolic guard) · `95e9f3f1` A2-S3b (LLM-judge + reflect check→revise in the auto path) · `8560e98d` D-A2S3B-PUBLISH-GATE · `77165280` canon-status (dirty-data: explicit skip, live-proven). **A character's death now flows persist → KG status → fact-for-check → SCORE guard → LLM-judge → reflect/repair → publish-gate, each link tested.** See the per-milestone ✅ sections below.
> **▶ NEXT — chapter-assembly-modes ARC COMPLETE + n=3 VALIDATED (B1→B4, [plan](../../plans/2026-06-07-comp-chapter-assembly-modes.md)). Pick the next track:** (1) **n=3 powered eval DONE (2026-06-08):** per_scene **0-3** V0 (defects 36 vs 2 — control, granularity loss as predicted) · stitch **2-1** V0 (9 vs 13) · chapter **2-1** V0 (7 vs 15). **Lift confirmed** — both assembly arms beat V0 2-of-3 and ~HALVE defects; lead with the defect-halving (discriminating signal), not the 2-1 W/L. **P1 (bios-only) loss DIAGNOSED + RESOLVED 2026-06-08:** the chapter arm re-narrated the ledger theft across the chapter boundary = cross-chapter RE-ESTABLISHMENT, an eval-grounding artifact (bios-only run had NO cross-chapter state: `prior_scene_drafts`/`gather_recent` are intra-chapter ONLY; cross-chapter carry comes solely from the KG timeline/canon lenses, empty unless seeded/published — same LOOM-32 under-grounding). **`--seed-kg` re-measure (n=3, KG seeded 8-10 events/book) CONFIRMS it:** per_scene **1-2** V0 (def 14 vs 8) · stitch **2-1** V0 (def 8 vs 15) · **chapter 3-0 V0 (def 3 vs 19) — CLEAN SWEEP, ~⅙ the defects, matches eval_a_fair's 3-0; P1 FLIPPED loss→win** (chapter P1: 0 def vs V0 6). **chapter single-pass is the winning arm + the right default for autonomous long-form.** **stitch residual (D-COMP-STITCH-PERSCENE-CEILING, LOW):** stitch still loses P1 even with KG carry (7 vs 1 def) — it merges per-scene drafts that already baked in re-establishment; one merge pass can't fully remove it, so stitch's ceiling is bounded by its per-scene inputs (chapter mode, one pass + carry, is cleaner). stitch = the human-in-loop assembly path; chapter = autonomous. n=3 directional not powered, but the signal is clean + consistent. **→ Validation done.** **FE assembly-mode DONE (LOOM-42).** **PO PIVOT (2026-06-08): make the branch PRODUCTION-READY before merge → debt-payoff campaign (don't let debt drift).** Cycles: **1 ✅ (LOOM-43)** decompose-commit safety · **2 ✅ (LOOM-46)** chapter/stitch job in-flight guard · **3 ✅ (LOOM-44)** long-form token hardening · **4 ✅ (LOOM-45)** verification+drift locks (chapter_sort fixed; tiptap-drift accepted; D-A2S1B2 inconclusive→deferred). **▶ CAMPAIGN COMPLETE (all 4 cycles done) → MERGE PR #19 next** (all real correctness debts + the perf fix paid; BE in-flight guard now hardened, not just FE-mitigated). **Residual deferrals (none blocking, Track-2 / fix-when-touched):** D-A2S1B2-LIVE-SMOKE (needs full publish→worker-ai chain), D-COMP-TRUNCATION-SURFACING (gateway finish_reason), D-A3-REPLACE-ORPHAN-ARC-NODES (planner-FE), D-COMP-STITCH-PERSCENE-CEILING (accepted), D-COMP-TIPTAP-SHAPE-DRIFT (accepted, persist-smoke catches), D-COMP-CHAPTER-INFLIGHT-REAPER (LOW — a proper stale-job reaper that marks orphaned `running`→failed; the cycle-2 staleness window already prevents permanent lockout), D-A3-PLANNER-FE (feature). **After merge:** K17 entity-embedding write pipeline (next net-new track). **Accept-and-document (not fixing):** D-COMP-STITCH-PERSCENE-CEILING, b-đúng prose-carry. **Separate feature:** D-A3-PLANNER-FE (planner tree-UI), FE sub-tab CSS-hidden refactor. **Merge PR #19 after the campaign.** ▶ **Campaign DONE — next action: MERGE PR #19.** (2) **Merge PR #19** → `main` (the composition V1 arc — A1/A2/A3 + state-reinjection + chapter-assembly — is feature-complete; needs explicit push approval, then merge). (3) **Debt-payoff roadmap** ([docs/plans/2026-06-07-debt-payoff-roadmap.md](../../plans/2026-06-07-debt-payoff-roadmap.md)): 064 re-scout (XS) → 063 grounding-compose migrate (M) → **K17 entity-embedding write pipeline (L/XL, highest net-new value)**. **Assembly-mode wiring (shipped, for FE/eval):** work `settings.assembly_mode {per_scene|chapter}` (validated); per-scene `POST …/generate` (explicit `assembly_mode=chapter`→409 redirect); chapter `POST …/chapters/{id}/generate`; stitch `POST …/chapters/{id}/stitch` (gated all-scenes-done); all persist to the book draft via `_persist_chapter_draft` (best-effort, `persist` flag). **Deferrals (LOW, none blocking):** chapter_sort double-fetch (perf), no auto-idempotency for chapter/stitch jobs, long-chapter revise output ceiling, stitch-revise `packed_prompt=""`, D-COMP-TIPTAP-SHAPE-DRIFT (live-smoke-validated 2026-06-08; no automated cross-lang lock — re-run the persist smoke if book `tiptap.go` changes). **Open deferrals:** D-COMP-COMPRESS-INPUT-CAP, D-A3-COMMIT-TRUE-REPLACE, D-A3-COMMIT-IDEMPOTENCY, D-A3-PLANNER-FE, D-A2S1B2-LIVE-SMOKE, D-COMP-DECOMPOSE-PLAN-LEDGER-DRIFT. **b-đúng cross-chapter prose-carry = deprioritized** (helps finer continuity, not the granularity-bound pairwise). **Recently cleared:** D-COMP-TIMELINE-AXIS-LIVE-SMOKE (LOOM-34). **Tools:** `eval_a_grounded.py` (bios + `--seed-kg`), `eval_a_fair.py` (equal-granularity plan-value), `diag_kg_timeline_carry.py`. **Lessons:** data-present≠defect-fixed (validate by reading drafts); the autonomous-concat pairwise is granularity-confounded — use `eval_a_fair` for plan-value. Compress threshold = compose env `PACK_COMPRESS_RECENT_THRESHOLD_CHARS` (default 6000).

## ✅ T3.4 Grounding Pin/Exclude DONE (LOOM-111, 2026-06-24, XL [FS], v2.2 + /review-impl) — per-scene grounding steering

Author can **pin** (force-keep through the budget) or **exclude** (drop) addressable grounding items (present-entity / canon-rule / lore-source) per scene; honored by BOTH the grounding preview AND engine generation (same `pack()` chokepoint → preview == what the model sees).
- **BE:** `scene_grounding_pins` table + `GroundingPinsRepo` (upsert-flip / clear / list); `pack()` reads the set AFTER the spoiler filters → drops excluded, force-protects pinned lore; `PackedContext.grounding_items[]` exposes addressable items + state; `PUT /v1/composition/works/{pid}/scenes/{nid}/grounding-pins` (`item_id` capped 200) + `grounding_items` on the GET. Wired into ALL 4 pack call sites (1 grounding + 3 engine), each regression-locked.
- **FE:** `GroundingPanel` per-line rows with 📌/🚫 toggles + `useGroundingPins` (optimistic + invalidate); `api.ts` `setGroundingPin`; types; i18n ×4 (`groundingPins.*`).
- **Invariants:** stable canonical-id keys (`entity_id` glossary anchor / `CanonRule.id` / lore `source_id`) survive language/derivative changes; `gather → spoiler → pin/exclude → budget` so a pin can NEVER resurrect a spoiler-dropped item; pinned lore still flows through `sanitize_lore`.
- **Reviews:** Phase-7 — 1 MED (`item_id` unbounded → 422 cap, prevents RETURNING-row 500). /review-impl — 1 MED (3 engine sites un-locked → fake_pack captures + per-site assert) + 3 LOWs deferred.
- **VERIFY:** composition `test_pack` 33 (7 new), `test_grounding_router` 13 (7 new), `test_engine_router` 39, `test_assembly_mode`+narrative 40, integration `grounding_pins` 2 (real PG); FE composition 320 (+6); `tsc` clean; **live smoke** (gateway→real composition DB): PUT exclude→row persisted in `scene_grounding_pins`, GET returns `grounding_items`, PUT none→cleared; migration applied on container boot.

### T3.4 Deferred (tracked)
- `D-T3.4-CHAPTER-MODE-PINS` (Naturally-next-phase) — chapter-mode generation (`build_chapter_pack_node` id=None) does NOT honor per-scene pins (different granularity; safe no-op). Add chapter-level steering if asked.
- `D-T3.4-EXTRA-CANON` (Track 2) — a derivative's override-added canon (`extra_canon`, plain text, no id) is not pin/exclude-addressable.
- `D-T3.4-LORE-CHUNK-ID` (Perf/feature) — lore steering is at `source_id` (chapter) granularity; per-chunk needs a knowledge-side hit id.
- `D-T5.1-EXT-ALWAYS-ON` (Cleanup/by-design) — FocusLineExtension mounts on every TiptapEditor (one inert decoration when not in focus mode). Harmless; scope to focus-only mount if other editors ever pay a cost.
- `D-T5.1-NO-CROSS-TAB-SYNC` (Feature) — useFocusMode is per-instance localStorage; toggling in one tab does not live-sync siblings. Add a `storage` event listener if multi-tab focus-mode parity is wanted.
- `D-T5.1-LIST-BLOCK-WHOLE` (By-design) — caret inside a list/blockquote lights the whole top-level block, not the sub-item. Matches `.lw-focus > .focusline` CSS contract; revisit only if per-item dim is requested.
- `D-T4.2-AGG-FULL-SCAN` (Perf) — read_aggregate scans all daily rows <= today (the streak walk needs full history). Fine V1 (indexed, per-user); add a date floor + a separate long-streak counter if a heavy multi-year writer feels it.
- `D-T4.2-REPORT-BEFORE-WORK-RESOLVE` (Feature) — the first save(s) after a fresh load may not accrue until useWorkResolution resolves (composeProjectId null). Self-heals next save (snapshot is absolute); seed/report effect re-fires when the Work resolves.
- `D-T4.2-MIDNIGHT-KEY-STALE` (Feature) — writing across local midnight without a remount reports the new date while useProgress is still keyed to the old today; invalidation refetches with the stale anchor until the next render. Resolves on re-render.
- `D-T4.2-PANEL-LINES` (Cosmetic) — ProgressPanel ~155 lines > ~100 guideline; extract the sparkline sub-component if it grows.
- `D-T3.5-STITCH-UNSTYLED` (Cleanup/by-design) — the chapter-stitch merge + canon-reflect passes use the from_settings profile (no chapter-level density/pace); the per-scene drafts they merge are already styled, so prose carries the style. Resolve chapter/work style in the stitch fn if a styled merge is wanted.
- `D-T3.5-SCOPEID-UNVALIDATED` (Robustness) — the style PUT does not check scope_id matches scope_type (e.g. work-row with scope_id != project_id). FE always sends the right id; a mismatched row is inert (resolve never matches). Add a server check if a non-FE client appears.
- `D-T3.5-PACK-EXTRA-READS` (Perf) — pack() does 2 extra indexed reads (style resolve + voice list) per generation/preview. Fold into the asyncio.gather if profiling shows pain.
- `D-T5.2-WINDOWED-MENTIONS` (Feature/blocked, gate 4) — the heatmap uses WHOLE-BOOK mention_count; spoiler-windowing to the scene chapter cutoff needs per-chapter mention edges, blocked on `D-P2-PER-SCENE-FANOUT` (pass2_writer.py:388, no per-chapter fanout). Revisit when per-chapter mention counts exist.
- `D-T5.2-EDITOR-WIRING-COVERAGE` (Coverage, gate 1) — ChapterEditorPage editor-handle glue (setHeatmapTerms/setHeatmapEnabled wiring + the name+alias term flatMap) is untested because the page test stubs TiptapEditor as a handle-less div; the decoration + band math + hook are unit-locked. Add a handle-mock page test when the page test harness grows a real editor handle. (Also accepted+documented: in-prose toggle requires a scene selected; heatmap+glossary decorations stack — opt-in cosmetic.)
- `D-T5.3-SPLIT-ON-EDIT` (Feature, gate 2) — typing INSIDE an AI provenance span keeps the mark (no per-edit split); a true split needs more PM plumbing (mark the edited slice human). inclusive:false already stops boundary-bleed. Build when authors ask to distinguish in-span human edits.
- `D-T5.3-COUNT-SEMANTICS` (Cosmetic) — countUnreviewedProvenance counts text NODES, so one AI insertion split across nodes (bold inside / multi-block prose) over-counts the toolbar badge vs "number of insertions". Harmless; markAll still flips all. Coalesce adjacent runs if it ever matters.
- `D-T5.3-VISIBILITY-DOM-CLASS` (Robustness) — setProvenanceVisible toggles `provenance-underlay-off` imperatively on view.dom; ProseMirror may reconcile editorProps.attributes on update and drop it. Self-heals via the useProvenance doc-change effect re-asserting visibility; a one-frame flash is possible. Move to a stable React-owned wrapper class if a flash is observed.
- `D-T4.1-LIVE-SMOKE-FULL-CHAIN` (Verification/blocked, gate 4) — the full publish->extract->/flywheel chain was NOT live-smoked end-to-end because the loreweave stack is down (knowledge_db Postgres crash-loops knowledge-service; infra Neo4j not up). The two residual risks ARE covered: the writer-stamp->count chain by tests/integration/db/test_flywheel_repo.py (ran live vs the ContextHub Neo4j), the endpoint glue by unit tests. Re-run the full chain when the loreweave stack boots.
- `D-T4.1-PATTERN-WRITER-UNSTAMPED` (Coverage, gate 5) — pattern_writer (Pass-1) + the glossary-anchor entity upsert do NOT stamp created_job_id, so entities minted only via those paths are under-counted in the flywheel. Deliberate: glossary anchors are pins (not chapter-extracted), and Pass-1 patterns are not the publish->extraction flywheel path. Thread job_id there if the under-count is ever noticed.
- `D-T4.1-REEXTRACT-EMPTY` (UX/by-design, gate 5) — a no-op re-extraction (0 net-new) makes the panel show the empty state even though canon grew on an earlier job, because the panel shows the LATEST completed job (CLARIFY-3). Switch to since-last-viewed cumulative if users find the reset confusing.
- `D-T3.6-MODEL-CHANGE-ORPHANS` (Drift, gate 4) — references all share ONE embedding model (work.settings.reference_embed_model_ref, set write-through on the first successful add). If that setting is later changed manually, older references (a different vector dimension) become invisible to search — the cosine length-mismatch guard returns 0 (fail-safe, no crash), but they silently never match. No UI changes it today. Add a re-embed-all migration if model-switching is ever exposed.
- `D-T3.6-AUTOQUERY-CAP` (Robustness, gate 5) — the per-scene auto-query (q omitted) concatenates goal/synopsis/beat/title uncapped (synopsis can be 20000 chars) while the manual `q` is capped at 2000. The embed provider truncates, so it is harmless today; cap the auto-query if a provider errors on long input.
- `D-T3.6-LIVE-SMOKE` (Verification/blocked, gate 4) — the full add->embed(provider-registry)->cosine->pack live chain was NOT smoked end-to-end (the loreweave stack + provider-registry + lm_studio embed backend were not bootable at dev time). Residual risk covered by unit tests (embed mocked) + the provider-registry embedding_client mirrors knowledge-service's proven client. Re-run when the stack + an embedding model are up.
- `D-T5.4-THREADS-GATE` (Coverage, gate 3/M2) — defaultLayout() includes the `threads` panel unconditionally, but threads is conditionally rendered (threadsEnabled). The M2 dock render MUST gate the threads panel on threadsEnabled (don't render it from layout when the Work hasn't opted into narrative_thread). Fix in M2 when the dock renders from layout.
- `D-T5.4-SERVER-SYNC` (Feature, gate 1) — the workspace layout + flag are per-device localStorage only; multi-device same arrangement needs a server-prefs sync. Per-device is the V1 contract.
- `D-T5.4-EDITOR-MULTIWINDOW` (Feature, gate 2) — V1 shares ONE Tiptap editor instance; a fully independent second editor view (multi-window collaborative editing) is out of scope.
- `D-T5.4-MOBILE` (Feature, gate 1) — windowing is desktop-only; mobile keeps the fixed strip.
- `D-T5.5-ESC-PROPAGATION` (Robustness, gate 5) — PowerViewOverlay's window-level Escape handler does not stopPropagation, so a background Esc-discard listener (if any sibling adds one) could also fire on the same keypress. None exists today; add stopPropagation if a conflicting global Esc consumer appears.
- `D-T5.5-FOCUS-TRAP` (A11y, gate 2) — the overlay is role=dialog aria-modal but does NOT trap focus or restore it to the trigger on close; Tab can escape to the (visually hidden) editor behind it. Add a focus-trap (focus the switcher on open, restore on close) when a11y hardening pass runs.
- `D-T5.5-MOBILE-SWITCHER` (UX, gate 1) — the 5-tab switcher row is fixed horizontal; on a narrow viewport the tabs + Back button can crowd. Wrap/scroll the switcher when mobile power-view UX is addressed.

<details><summary>(superseded PO decision-point — Phase A complete, options A-D)</summary>

> **Composition V1 Phase A (A1+A2+A3) is COMPLETE + live-proven.** The validate-first §8.2 Phase-A gate ("does A beat V0 on coherence?") is **INCONCLUSIVE: coherence-median SATURATES at 5/5 on short scenes** (the A1 finding, now confirmed at A3 too). The mechanisms work + ship (no regression, real adaptive-K efficiency), but the DOC coherence-**lift** thesis is unproven on this metric. **Candidate NEXT (PO picks):** **(a) eval-metric redesign** — longer multi-scene generation + a completeness/variance rubric (the A1 carry-over; the only way to actually validate the Phase-A thesis before Phase B); **(b) Phase B** — recipe library + `narrative_thread` ledger (speculative surface; the validate-first gate says prove-first, so (a) should precede (b)); **(c) FE for the A3 planner** (preview/edit/commit UI — BE+eval shipped, FE deferred like A2→A2-S4); **(d) merge PR #19** + clear the deferral backlog. **A3 deferrals:** D-A3-COMMIT-TRUE-REPLACE (force adds, doesn't replace — own design), D-A3-COMMIT-IDEMPOTENCY (no key → double-submit dupes), D-A3-PLANNER-FE. **Remaining A2 deferral: D-A2S1B2-LIVE-SMOKE** (worker LLM *producing* status_effects from a published death-chapter PROMPT). **D-EVAL-METRIC-SATURATION (cross-A1/A3): coherence-median can't discriminate on short single scenes — redesign before trusting any A-slice quality gate.**

</details>

> **Workflow:** 12-phase **v2.2 human-in-loop** (PO checkpoint at CLARIFY-end + POST-REVIEW). `/amaw` **opt-in** for: CM1 (schema/migration), CM3 (cross-service contract cutover), composition M1 (schema), M5 (authz/isolation).
> **Isolated from** the `lore-enrichment/*` track — LOOM **never touches `services/lore-enrichment-service/`** (siblings, not deps).

## 🏁 BRANCH-DEBT CLEANUP CAMPAIGN COMPLETE (cycles 5–9, LOOM-47→51) — composition/LOOM branch debt cleared
All 5 cycles done. The composition/LOOM branch's debt ledger is now EMPTY except the deliberately-excluded tracks (K17 entity-embedding write pipeline; D-A3-PLANNER-FE + its coupled D-A3-REPLACE-ORPHAN-ARC-NODES) and 2 NEWLY-FILED producer bugs that live OUTSIDE the branch (DEFERRED 065 worker-ai status-enum HIGH, 066 A2-S1b status_effects gap MED). **Cycles:** 5 ✅ ledger close-out + contract locks · 6 ✅ job & work lifecycle (cross-service) · 7 ✅ truncation surfacing · 8 ✅ live-stack verification (D-A2S1B2 diagnosed) · 9 ✅ FE sub-tab CSS-hidden. **Notable:** 4 deferrals turned out NOT to be shortcut-debt once checked against code (reparent-guard already done, finish_reason already on the wire, plan-ledger-drift = Phase-B feature, 064 re-scout no-slice) — checking-before-scoping repeatedly saved mis-scoped work; and the cycle-8 faithful live-smoke surfaced 2 real cross-service producer bugs mocks never would. **The composition/LOOM branch's OWN ledger is clean — but per the 2026-06-08 PO directive the branch will NOT merge yet: clear ALL remaining debt + missing features first (the previously-excluded tracks + the producer bugs) → [pre-merge close-out roadmap](../../plans/2026-06-08-pre-merge-closeout-roadmap.md) (cycles 10–16), THEN merge PR #19.**

## ✅ BRANCH-DEBT cleanup cycle 9 DONE (LOOM-51, 2026-06-08, M, [FE], v2.2 + /review-impl) — Assemble sub-tab CSS-hidden refactor
Final campaign slice. Closes the FE state-loss flagged at LOOM-42: [CompositionPanel.tsx](../../../frontend/src/features/composition/components/CompositionPanel.tsx) rendered its 5 sub-tabs (compose/assemble/grounding/canon/quality) with `{tab===x && …}` → switching tabs UNMOUNTED the inactive panel, destroying in-progress hook state (a co-write draft, a chapter/stitch preview). **Fix (PO: holistic):** all 5 panels now always-mounted, toggled with CSS `hidden` — CLAUDE.md "never conditionally unmount stateful components". **/review-impl: no HIGH/MED** — verified the eager-mount is safe (GroundingPanel self-guards on empty sceneId via `enabled=!!sceneId`; ChapterAssembleView is mutations-only; QualityPanel/CanonRulesPanel fire bounded react-query-cached eager fetches — the accepted holistic trade-off, now code-noted) and the wrapper div is layout-neutral (all panel roots are content-sized `flex flex-col`, no `h-full`/`flex-1`). **Verify:** 69 composition vitest (67 + 2 new [CompositionPanel.test.tsx](../../../frontend/src/features/composition/components/__tests__/CompositionPanel.test.tsx) — proves NO remount on a tab round-trip via a mount-counter, the real invariant, not just the visibility class) + `tsc --noEmit` clean. FE-only, no API change.

## ✅ BRANCH-DEBT cleanup cycle 8 DONE (LOOM-50, 2026-06-08, L, v2.2) — live-stack verification & plan quality
Fourth slice of the [branch-debt cleanup roadmap](../../plans/2026-06-08-branch-debt-cleanup-roadmap.md). A VERIFICATION cycle — no production code. **(A) `D-COMP-DECOMPOSE-PLAN-LEDGER-DRIFT` → RE-SCOPED as a Phase-B feature (not campaign-debt).** `narrative_thread` (the "ledger") is unimplemented in composition — a Phase-B feature per the reasoning-engine spec §4/§5 — and the P1 "drift" the eval observed was already diagnosed as a cross-chapter grounding artifact (resolved by `--seed-kg`, LOOM-32). So the proper fix is net-new feature work (same category as the excluded planner-FE), not shortcut-debt → out of the campaign. **(4th deferral this campaign that wasn't really campaign-debt once checked** — reparent-guard, finish_reason, this, + 064 re-scout). **(B) `D-A2S1B2` → DEFINITIVELY DIAGNOSED via the full publish→worker-ai chain** (PO chose the faithful chain; pursued through the benchmark gate). NEW dev smoke `_smoke_a2s1b2_fullchain.py` (gitignored) drove: login→resolve chat+embed models→book+3 chapters→knowledge project→PATCH embed-model (If-Match)→**K17.9 benchmark PASS** (recall@3=1.0, runs=3; the runs=1 fail was `min_runs=3`)→publish death-chapter→`extraction/start`→real worker-ai LLM extraction. **Result: the producer chain does NOT emit `:EntityStatus` from a real death-chapter** — 3 entities (Bryn/Kael/Mira) extracted but NO `:EntityStatus{gone}` despite an unambiguous death. **Surfaced 2 real producer bugs (both OUT of the composition branch → filed, not fixed):** **DEFERRED 065 (HIGH)** worker-ai `_mark_complete` writes `status='complete'` but the constraint wants `'completed'` → EVERY finished extraction CheckViolates→marked `failed` (entities ARE written; completion signal broken platform-wide); **DEFERRED 066 (MED)** A2-S1b status_effects→`:EntityStatus` producer gap (entities had `canonical_id=NULL` — the anchor-link gap → status unresolved silently). This is a precise diagnosis, a big step past cycle-4's "inconclusive". **Verify:** live-smoke (full chain drove book→benchmark→publish→real extraction; 3 entities, 0 :EntityStatus). No production code. Dev residue (1 book/project/3 entities) left in the dev KG (harmless). → NEXT cycle 9: FE Assemble sub-tab CSS-hidden refactor.

## ✅ BRANCH-DEBT cleanup cycle 7 DONE (LOOM-49, 2026-06-08, M, v2.2 + /review-impl) — truncation surfacing
Third slice of the [branch-debt cleanup roadmap](../../plans/2026-06-08-branch-debt-cleanup-roadmap.md). Clears `D-COMP-TRUNCATION-SURFACING` (the cycle-3 char-estimate heuristic was DROPPED because a reliable signal needed the gateway `finish_reason`). **The roadmap's "cross-service /amaw" assumption was WRONG** — provider-registry's aggregator ALREADY stamps `finish_reason` on both boundaries ([aggregator.go:279/545](../../../services/provider-registry-service/internal/jobs/aggregator.go#L279) → `result["finish_reason"]`; streamers → `DoneEvent.finish_reason`) and **normalizes Anthropic `max_tokens`→`length`** ([anthropic_streamer.go:231](../../../services/provider-registry-service/internal/provider/anthropic_streamer.go#L231) `mapAnthropicStopReason`), so `finish_reason=="length"` is correct cross-provider. Composition just **discarded** it everywhere. **Now captured + surfaced as an authoritative `truncated` flag (composition-only, NO gateway change):** `DraftMetering +finish_reason`; captured in [`stream_draft`](../../../services/composition-service/app/engine/cowrite.py) (was `elif DoneEvent: pass`), [`_one_draft`](../../../services/composition-service/app/engine/select.py) (`job.result["finish_reason"]`), [`stitch_chapter`](../../../services/composition-service/app/engine/stitch.py) (now returns `(text, finish_reason)`); all 4 [engine.py](../../../services/composition-service/app/routers/engine.py) paths (cowrite SSE, auto, chapter, stitch) emit `truncated` (`=="length"`) + raw `finish_reason`. Distinct from cycle-1 `capped` (composition's own hard-cap abort — both surfaced on the cowrite path; a consumer treats `capped OR truncated` as "incomplete"). **/review-impl (1 MED + 2 LOW; #1 fixed, #2/#3 documented):** MED#1 the auto select_draft path's truncated was the one untested derivation → added a test + non-default contrast; LOW#2 truncated is winner-scoped not final_text-scoped (a canon-revise rewrite isn't re-checked — documented; winner is the cap-prone gen, revise is bounded+rare); LOW#3 capped-vs-truncated relationship documented. **Verify:** 336 composition unit (335 + auto-path test) incl REAL-capture tests ([test_truncation_capture.py](../../../services/composition-service/tests/unit/test_truncation_capture.py) — stream_draft DoneEvent + _one_draft job.result, not stub-only) + non-default truncated locks on all 4 paths + test_stitch tuple-return. Single-service; gateway provenance code-verified (live max_tokens=1 probe optional follow-up).

## ✅ BRANCH-DEBT cleanup cycle 6 DONE (LOOM-48, 2026-06-08, L, cross-service, v2.2 + /review-impl) — job & work lifecycle robustness
Second slice of the [branch-debt cleanup roadmap](../../plans/2026-06-08-cycle6-job-work-lifecycle.md). Clears `D-COMP-POST-WORK-RACE` + `D-COMP-CHAPTER-INFLIGHT-REAPER`. Cross-service (composition + knowledge). **(A) D-COMP-POST-WORK-RACE → knowledge-side get-or-create.** Two concurrent first-POSTs for the same book each created a duplicate empty book project ([works.py:128](../../../services/composition-service/app/routers/works.py#L128) `resolve_work=none` → both `create_project`). NEW [`ProjectsRepo.create_or_get`](../../../services/knowledge-service/app/db/repositories/projects.py) (returns `(project, created)`): for `project_type='book'` + `book_id`, per-(user,book) `pg_advisory_xact_lock(0x4B50)` → return existing non-archived book project else insert; gated so general-project create UX is untouched; no UNIQUE constraint (legacy multi-project-safe); `create`→`_insert` refactor keeps the old signature for all callers. Router returns **200** on existing / 201 on new (OpenAPI `responses` documents it). **Composition needs NO race change** — both POSTs get the SAME project_id → the existing work unique-violation catch dedupes the work; verified composition's `knowledge_client` accepts 200/201 + FE `apiJson` uses `res.ok` (any 2xx). **(B) D-COMP-CHAPTER-INFLIGHT-REAPER → both.** The cycle-2 window prevents lockout but left orphaned `running` jobs lingering (no scheduler existed). NEW [`GenerationJobsRepo.reap_stale_jobs(cutoff)`](../../../services/composition-service/app/db/repositories/generation_jobs.py) (global backstop) + **opportunistic** reap of this chapter's stale node-less jobs inside `create_chapter_job_guarded` (under the lock) + a **lifespan periodic sweep** ([main.py](../../../services/composition-service/app/main.py) `_reap_stale_jobs_loop`, `job_reaper_sweep_secs=600`, window reuses `chapter_inflight_stale_secs=1800`, cancelled cleanly on shutdown, multi-replica idempotent, 0 disables). **/review-impl (1 MED + 3 LOW; #1/#2/#3 fixed, #4 accepted):** MED#1 the reaper had only the GITIGNORED smoke → added 2 COMMITTED integration tests (reap_stale + opportunistic) [also caught+fixed an orphaned-line slip from the insert]; LOW#2 OpenAPI 200 doc; LOW#3 partial index `idx_generation_job_active` (speeds sweep + cycle-2 guard); LOW#4 reaper-window-vs-worst-case-gen accepted (self-recovers via update_status; same trade-off as the cycle-2 window). **Verify:** 326 composition unit + 40 composition integration (38 + 2 new reaper, throwaway DB) + 22/22 knowledge projects-repo integration (18 + 4 new create_or_get incl concurrent-gather→1-insert, real DB) + composition live smoke 8/8 (A-F + G sweep-reap + H opportunistic-reap) vs real PG. New deferral **D-COMP-CHAPTER-INFLIGHT-REAPER fully cleared** (replaced by the implemented reaper).

## ✅ BRANCH-DEBT cleanup cycle 5 DONE (LOOM-47, 2026-06-08, M, v2.2) — ledger close-out + contract locks
First slice of the [branch-debt cleanup roadmap](../../plans/2026-06-08-branch-debt-cleanup-roadmap.md) (clear ALL branch-born debt except K17 + planner-FE, batched over 5 cycles). The cheap ledger-clearing cycle — **no production behavior change**. **(1) `D-COMP-M2-XREF-OWNERSHIP` CLEARED** — investigation found reparent-cycle PREVENTION already fully wired (`update_node`→`_validate_reparent`→`_descendant_ids`, `update_node_commit_aware` inherits it) AND tested (`test_outline_reparent_guards` + `test_outline_reparent_cycle_blocked` integration + `test_patch_node_ifmatch_412_and_cycle_400` unit); the [outline.py:597](../../../services/composition-service/app/db/repositories/outline.py#L597) comment claiming "prevention is a router-layer validation, tracked as D-COMP-M2-XREF-OWNERSHIP" was STALE → fixed (the UNION is a raw-SQL backstop; prevention is upstream). **(2) `D-COMP-TIPTAP-SHAPE-DRIFT` LOCKED** — verified `prose_doc.text_to_tiptap_doc` is byte-exact vs book's `plainTextToTiptapJSON` (CRLF→LF, `\n\n` split, `TrimRight`, `_text` snapshot, empty-para branch, `{doc,content}` wrapper all match); NEW [test_tiptap_drift_canary.py](../../../services/composition-service/tests/unit/test_tiptap_drift_canary.py) reads book-service's `tiptap.go` from the monorepo + asserts all 7 shape tokens + the empty-para branch persist → a book-side serializer drift fails LOUDLY (the cross-lang lock without a Go build; `test_prose_doc` already pins the Python side). **(3) `D-GROUNDING-C-ADOPT` (064) re-scouted → STILL no clean slice** (composition `neutralize` `⟦…⟧`+`<`→`＜` delimiter defense would be NFKC-folded by the SDK's `neutralize_proposal_text`; no `GroundingCite` consumer in composition) → DEFERRED rationale updated, stays optional. **(4+5) `D-COMP-STITCH-PERSCENE-CEILING` + b-đúng prose-carry → formally CLOSED (won't-fix, see below).** **Verify:** 326 composition unit (324 + 2 canary) + py_compile. Single-service, no behavior change → no live-smoke. No /review-impl (no load-bearing/concurrency/auth code; the one prior concern, reparent cycles, was verified already-guarded). **Campaign roadmap committed.**

### Cycle-5 formal won't-fix close-outs (debt PAID by documented decision)
- **`D-COMP-STITCH-PERSCENE-CEILING` — WON'T-FIX.** Stitch merges per-scene drafts that already baked in cross-chapter re-establishment; ONE merge pass structurally cannot fully remove it, so stitch's quality ceiling is bounded by its per-scene inputs (measured: stitch P1 7 vs chapter 1 defects with KG carry). Chapter single-pass (one pass + carry) is the clean autonomous path; stitch is the human-in-loop assembly path where the bound is acceptable. Not a bug — an inherent property of the merge approach. Removed from the active ledger.
- **b-đúng cross-chapter prose-carry — WON'T-FIX (deprioritized).** Cross-chapter plot state is carried by the KG timeline/canon lenses (LOOM-33/34 axis fix, live-proven); raw prior-chapter PROSE carry would help only fine-grained continuity, not the granularity-bound pairwise metric that drove the validation. The mechanism that mattered (KG event-carry) ships; raw prose-carry is not worth the spoiler-safety + budget cost. Removed from the active ledger.

## ✅ PROD-DEBT cycle 2 DONE (LOOM-46, 2026-06-08, M, v2.2 + /review-impl) — chapter/stitch job in-flight guard
Closes the last campaign cycle: the two **chapter-level** endpoints (`…/chapters/{id}/generate`, `…/stitch`) had NO server-side concurrency guard (the per-scene `/generate` cancels-in-flight, but chapter jobs are node-less so that didn't apply) — `idempotency_key` only covered *replay*, not two concurrent distinct submits, which would double-spend the LLM + race the best-effort book-draft persist. **NEW `GenerationJobsRepo.create_chapter_job_guarded`** ([generation_jobs.py](../../../services/composition-service/app/db/repositories/generation_jobs.py)): in ONE Tx — **per-(project,chapter) `pg_advisory_xact_lock`** (NS `0x10B0`, distinct from outline's `0x10AF`) → **replay-under-lock** (same key → existing job, bypasses the guard — AC#2) → **in-flight guard** (any RECENT active node-less job for this chapter, ANY operation → generate blocks stitch & vice-versa) → reuse `create(conn=c)`. Lock releases at commit, BEFORE the minutes-long generation. **NEW `ChapterJobInFlightError(active_job_id)`** → router **409 `CHAPTER_JOB_IN_FLIGHT`**. PO-locked **409-reject** (not cancel-restart — protects the expensive in-flight job). **/review-impl (1 MED + 3 LOW; #1/#2/#4 fixed, #3 documented):** **MED #1** — a `running` job orphaned by a mid-generation crash would lock the chapter out FOREVER (reject semantics + no reaper) → **staleness window** `chapter_inflight_stale_secs=1800` (a job `running` past the window is presumed dead, no longer blocks). **LOW #2** — `_persist_chapter_draft` claimed "NEVER raises" but only caught `BookClientError` → broadened to `except Exception` (a raw timeout would have left the job `running`, feeding #1). **LOW #4** — partial expression index `idx_generation_job_chapter_inflight` on `(input->>'chapter_id') WHERE outline_node_id IS NULL`. **#3** (same key across chapters replays the first job) = existing global idempotency-key contract, documented not changed. **Verify:** 324 composition unit (322 + 2 new 409 tests) + py_compile + **LIVE-SMOKE 6/6 vs real Postgres** (A cross-op guard→409 · B independent-chapter · C replay-bypass · **D concurrent `asyncio.gather` race → exactly 1 win + 1 block** [advisory lock serializes] · E stale-running→no-block · F fresh-running→blocks) + index DDL validated on real PG. Single-service (no cross-service token needed; live-smoke present anyway). New deferral **D-COMP-CHAPTER-INFLIGHT-REAPER** (LOW — a proper reaper marking orphaned `running`→failed; the staleness window already prevents lockout).

## ✅ PROD-DEBT cycle 4 DONE (LOOM-45, 2026-06-08, M, v2.2 + /review-impl) — verification + drift locks
Closes the campaign. **chapter_sort double-fetch FIXED** ([pack.py](../../../services/composition-service/app/packer/pack.py) `PackRequest.chapter_sort_hint`; [engine.py](../../../services/composition-service/app/routers/engine.py) chapter endpoint passes the sort it already fetched → `pack()` uses the hint instead of re-calling `book.get_chapter_sort_orders`; opt-in, per-scene/stitch paths untouched). **/review-impl: 1 LOW fixed** — locked BOTH that `pack` uses the hint (`sort_calls==0`, test_pack) AND that the endpoint passes it (`pack_req.chapter_sort_hint==3`, chap_ctx) — the wiring boundary, not just the helper. **D-COMP-TIPTAP-SHAPE-DRIFT ACCEPTED** (composition-side-locked: `test_prose_doc` golden pins the exact shape + B4 persist round-trip catches gross drift; no clean Python↔Go cross-lang automation). **D-A2S1B2-LIVE-SMOKE ATTEMPTED → INCONCLUSIVE → still DEFERRED:** the bounded knowledge `/internal/extraction/extract-item` ran the real pass-2 extractor on a clear death-chapter (Kael/Bryn/Mira → 3 entities, 3 events, 1 fact) but wrote **no `:EntityStatus`** for the project + no status logs — the standalone path doesn't thread the chapter ordinal so `event_order=None`, and A2-S1b-1 **skips status_effects when event_order is None** by design. A faithful smoke needs the full **publish→worker-ai** chain (P3 threads chapter_index→event_order) + the fresh-book extraction bootstrap. Existing `:EntityStatus{gone}` nodes (prior runs) confirm the WRITE path works *given* event_order; whether a real LLM (vs synthetic seed) produced them remains unconfirmed. Honest outcome — NOT claimed passed. **Verify:** 322 composition unit (chapter_sort_hint use + wiring + per-scene regression-safe) + py_compile. Single-service. Dev smoke scripts (`_smoke_a2s1b2.py`, `_smoke_commit.py`) untracked.

## ✅ PROD-DEBT cycle 3 DONE (LOOM-44, 2026-06-08, M, v2.2 + /review-impl) — long-form token hardening
Production-readiness: long chapters must not silently truncate. **Deterministic anti-truncation** (the load-bearing fix): chapter-gen + stitch `max_out = body.override or min(scene/draft_count × `chapter_gen_per_scene_tokens`=700, ceiling)`; raised `chapter_gen_max_tokens`/`stitch_max_tokens` ceilings 4096→**8192** ([config.py](../../../services/composition-service/app/config.py), [engine.py](../../../services/composition-service/app/routers/engine.py)) — a multi-scene chapter now gets proportional room instead of a flat cap (language-agnostic). **Compress input cap** (D-COMP-COMPRESS-INPUT-CAP): `compress.cap_recent_prose` bounds the story-so-far fed to `compress()` to the most-recent `compress_max_input_chars`=24000 (keeps ≥1; both `_compress_fn`s pass it). **/review-impl MED-1 (fixed):** the planned **estimate truncation flag was DROPPED** — `char_estimate(/3) ≈ 1.33× real EN tokens`, so `char_estimate(text) >= max_out` false-flags a normal English chapter at ~75% real budget AND never fires for CJK (`/3` under-counts); a misleading both-ways signal is worse than none. Accurate surfacing deferred → **D-COMP-TRUNCATION-SURFACING** (needs the gateway `finish_reason`); `max_output_tokens` still returned (informative). **Verify:** 321 composition unit (compress-cap ×2 + chapter/stitch `max_out`=1400 sizing asserts) + py_compile. Single-service deterministic — no migration/cross-service/concurrency → no live rebuild. LOW accepted: `cap_recent_prose` keeps ≥1 even if that paragraph alone exceeds the cap.

## ✅ PROD-DEBT cycle 1 DONE (LOOM-43, 2026-06-08, M, v2.2 + /review-impl) — decompose-commit idempotency + true-replace
First slice of the production-readiness debt-payoff campaign (PO: branch must be production-ready before merge). Clears **D-A3-COMMIT-IDEMPOTENCY** + **D-A3-COMMIT-TRUE-REPLACE**. **NEW `decompose_commit` ledger** ([migrate.py](../../../services/composition-service/app/db/migrate.py), additive idempotent DDL; unique `(user_id, project_id, idempotency_key)`). **`outline.commit_decomposed_tree`** (replaces the router's pre-Tx guard): in ONE Tx — **per-project `pg_advisory_xact_lock`** (serializes concurrent commits) → replay (keyed exactly-once) → in-Tx existing-scenes guard → **`replace=true` archives ONLY the target chapters' scenes** → insert → ledger; UniqueViolation backstop for cross-project key reuse. `_insert_decomposed_tree` extracted (shared by create/commit, no dup). [plan.py](../../../services/composition-service/app/routers/plan.py): `CommitRequest +replace +idempotency_key` (`force` = deprecated alias → replace), `AlreadyPlannedError`→409, returns `replay`. **/review-impl (2 MED fixed, no HIGH):** MED-1 — the in-Tx guard alone did NOT close the no-key concurrent TOCTOU (a plain SELECT takes no lock); added the per-project advisory lock. MED-2 — idempotency was scoped `(user,key)` globally but the endpoint is per-project → a reused key replayed another project's result (**the live-smoke caught it** replaying a deleted book's arc across runs); scoped to `(user,project,key)`. LOW: D-A3-REPLACE-ORPHAN-ARC-NODES (replace leaves childless prior arc/chapter outline nodes — invisible until a tree-FE; the planner-FE should filter/archive) + ledger-no-GC documented. **Verify:** 319 composition unit (317 + 2 plan tests: replace/force-alias/idempotency-replay) + py_compile + **LIVE-SMOKE PASS 5/5** (rebuilt composition w/ migration, gateway→composition→book): keyed replay→same arc no dup · 409 re-plan · replace→2-active-not-4 · **concurrent no-key→[201,409] 2-not-4** (advisory lock proven). Single-service.

## ✅ FE assembly-mode DONE (LOOM-42, 2026-06-08, L, [FE], v2.2 + /review-impl) — human-feedback surface for chapter/stitch
Closes the data-collection gap: the chapter/stitch modes had no FE, so no human corrections accrued to learning-service for them. **NEW "Assemble" sub-tab** in [CompositionPanel](../../../frontend/src/features/composition/components/CompositionPanel.tsx) (chapter-scoped). **[ChapterAssembleView](../../../frontend/src/features/composition/components/ChapterAssembleView.tsx)** (view) + **[useChapterAssembly](../../../frontend/src/features/composition/hooks/useChapterAssembly.ts)** (controller): `assembly_mode` toggle (per_scene|chapter) that **merge-patches** `work.settings` (preserves critic_model_*/reasoning_engine — server REPLACES the blob); **Generate chapter** (B2) + **Stitch chapter** (B3, button gated on all-scenes-done) buttons; **editable preview = inline edit-capture**; Accept (kind=edit only on real change — H2)/Regenerate/Reject → `submitCorrection(jobId)` → `composition.generation_corrected` → relay → learning. **persist=false** (no draft clobber; Accept inserts via `onAccept`). `api.ts` +`generateChapter`/`stitchChapter`/`patchWork` (bodies cross-checked vs B2/B3 models); `types.ts` +`ChapterGeneration`/`AssemblyMode`; i18n ×4 (en/vi/ja/zh-TW). **/review-impl: MED-1 fixed** — the settings-MERGE was hook-internal and the component test mocked the hook → added [useChapterAssembly.test.tsx](../../../frontend/src/features/composition/hooks/__tests__/useChapterAssembly.test.tsx) asserting `patchWork` gets the MERGED blob + the work-query invalidation; LOW-1 (FE trim-compare vs BE block-compare on the edit gate — harmless, fire-and-forget) + LOW-2 (api body unverified by a live FE call — cross-checked vs the BE models, no mismatch) documented. **Verify:** 67 composition vitest (9 files, +12 new: 10 ChapterAssembleView + 2 hook) + `tsc --noEmit` clean. FE-only (BE chapter/stitch/correction endpoints already live-smoked at B4). **Known consistency item:** the sub-tab branch-render loses the in-progress preview on tab-switch — matches the existing compose/grounding/canon/quality tabs (ComposeView too); a holistic CSS-hidden refactor is its own task.

## 🎉 CHAPTER-ASSEMBLY-MODES ARC COMPLETE (B1·B2·B3·B4, LOOM-38→41) — granularity thesis validated live.

## ✅ B4 DONE (LOOM-41, 2026-06-08, M, v2.2) — eval `--assembly` 3-arm + cross-service live-smoke
Final slice of [chapter-assembly-modes](../../plans/2026-06-07-comp-chapter-assembly-modes.md). PO (CLARIFY): all-in-one-book + rebuild & full live-smoke. **Dev-only** ([`scripts/eval_a_grounded.py`](../../../services/composition-service/scripts/eval_a_grounded.py)): `+--assembly={per_scene|stitch|chapter|all}` (default per_scene, back-compat) builds the 3 arms on ONE shared book + decompose plan (apples-to-apples) and pairwise-judges each vs V0; helpers `mark_scene_done` (PATCH status=done for the stitch gate) / `gen_chapter_text` / `stitch_chapter_text` / `prose_plain_text`; `persist_round_trip_smoke` (`--persist-smoke`) = chapter-generate persist=True → GET prose → assert the assembled prose round-tripped as valid Tiptap. **★ LIVE-SMOKE (rebuilt composition→HEAD 55d65642 via build-stack.sh, recreated, SHA-label-verified healthy; ran across composition+book+glossary+knowledge+provider-registry):** **PERSIST ROUND-TRIP PASS** (draft v2, 4714 chars back, head-match — clears D-COMP-ASSEMBLY-LIVE-SMOKE + validates D-COMP-TIPTAP-SHAPE-DRIFT: composition's `text_to_tiptap_doc` matches book's `tiptap.go` end-to-end, no drift). **n=1 directional result:** per_scene **0-1** V0 (10 vs 0 defects — granularity loss, as predicted), stitch **1-0** V0 (1 vs 6 defects), chapter **1-0** V0 (0 vs 6 defects). **GRANULARITY THESIS CONFIRMED apples-to-apples** — stitch + chapter both beat V0; per-scene loses on GRANULARITY (fragmented concat), not state — matches eval_a_fair's 3-0. Honest scope: n=1 directional probe (the harness flags it), not a powered study → **n≥3 powered run is the recommended NEXT before claiming a lift** (ceiling-tie/n=1 ≠ validation, per the eval-saturation lesson). **Verify:** 317 composition unit unaffected + py_compile clean + the live run. No /review-impl (dev-only harness; the B2/B3 production code it exercises was already /review-impl'd; the live run end-to-end-validated the script).

## ✅ B3 DONE (LOOM-40, 2026-06-07, XL, v2.2 + /review-impl) — per_scene+stitch path + MED-2 persistence
Third BUILD slice of [chapter-assembly-modes](../../plans/2026-06-07-comp-chapter-assembly-modes.md). PO (CLARIFY): Full B3 in one pass, no /amaw, explicit stitch endpoint, char-cap head+tail. **NEW `POST /v1/composition/works/{project_id}/chapters/{chapter_id}/stitch`** ([engine.py](../../../services/composition-service/app/routers/engine.py) `stitch_chapter_endpoint`): gated on **all scenes done** (`outline.chapter_scene_gate`; else 409 `SCENES_NOT_DONE`) → **`jobs.chapter_scene_drafts`** (NEW — all the chapter's latest completed scene winners in story_order; 0→400 `NO_SCENE_DRAFTS`) → **`engine/stitch.py`** (NEW) `stitch_chapter` = ONE LLM pass merging consecutive scene drafts into a seamless chapter (dedup intros/echoes, smooth transitions, change-NO-facts; temp 0.3) with **`cap_scene_drafts`** char-cap head+tail (MED-3, `stitch_max_input_chars=24000`, elided-middle logged); **degrade→raw concat** (`final_text = stitched or "\n\n".join(drafts)`, flagged `degraded`/`stitched`) → post-stitch **`run_canon_reflect`** over the union cast at the chapter position (a rewrite can re-introduce a gone character; never blocks) → best-effort persist. **MED-2 DONE (BOTH modes):** shared **`_persist_chapter_draft`** writes the assembled chapter to the book-service **DRAFT** via `book.patch_draft` (read draft_version → write with `expected_draft_version`, 409-safe; **NEVER discards the generated text** — it's durable in `generation_job.result`, cross-store best-effort). **Contract finding (verified):** book `patchDraft` stores the body verbatim — NO server-side text→tiptap — so **NEW `engine/prose_doc.py`** `text_to_tiptap_doc` replicates book-service [tiptap.go](../../../services/book-service/internal/api/tiptap.go) exactly (paragraphs split on blank line, **top-level `_text`** snapshot that the chapter_blocks extraction trigger reads). B2's chapter endpoint now persists too; both endpoints gained a `persist` flag (eval sets False for pure-text arms). **/review-impl (no HIGH/MED):** LOW-1 fixed — added a regression-lock proving the **post-canon-revise** text is what gets persisted (persist runs after reflect; the fixture's no-op reflect would have hidden a reorder); LOW-2 documented — `prose_doc` can drift from book's `tiptap.go` across the Python↔Go boundary (no automated cross-lang lock → **D-COMP-TIPTAP-SHAPE-DRIFT**; B4 live-smoke catches gross drift). **Verify:** 317 composition unit (296 B2 baseline + 21: 4 prose_doc + 9 stitch + 8 stitch-endpoint/persist/revise). Single-service code diff; **cross-service persist live-smoke deferred to B4** (`D-COMP-ASSEMBLY-LIVE-SMOKE`). **Deferrals (LOW):** chapter_sort double-fetch (perf), no auto-idempotency for chapter/stitch jobs, long-chapter revise output ceiling, stitch-revise `packed_prompt=""`.

## ✅ B2 DONE (LOOM-39, 2026-06-07, L, v2.2 + /review-impl) — chapter single-pass path
Second BUILD slice of [chapter-assembly-modes](../../plans/2026-06-07-comp-chapter-assembly-modes.md). **PO (CLARIFY): separate endpoint** (not a `target_chapter_id` body field). **NEW `POST /v1/composition/works/{project_id}/chapters/{chapter_id}/generate`** ([engine.py](../../../services/composition-service/app/routers/engine.py) `generate_chapter`): loads the A3 decompose plan (`outline.scenes_for_chapter` — NEW repo method, reading order; **0 scenes → 400 `NO_CHAPTER_PLAN`** before any job) → chapter intent/title from the scenes' shared `kind='chapter'` parent → builds a **synthetic in-memory pack node** (NEVER persisted — MED-1; `chapter_id`, `story_order = sort×STRIDE` = chapter opening, **union cast** = scenes' pov+present deduped, **combined synopsis** = intent + ordered scene beats) → `pack()` at the chapter reading position → single **`diverge(k=1)`** draft (no diverge/rerank; `max_tokens = chapter_gen_max_tokens`) → **chapter-level `run_canon_reflect`** over the union cast at `pc.scene_sort_order` (equivalent-granularity to the per-scene guard — plan CANON-SAFETY; degrade-safe, never blocks) → non-stream JSON (op `draft_chapter`, `outline_node_id=None`, `assembly_mode=chapter`). **NEW `engine/chapter_gen.py`**: pure `union_cast`/`build_combined_synopsis`/`build_chapter_pack_node` + shared **`STORY_ORDER_CHAPTER_STRIDE`** (deduped `plan.py`'s local `_STRIDE=1000` → import, drift-lesson). **B1 `/generate` guard 501 → 409 `USE_CHAPTER_ENDPOINT`** (explicit `assembly_mode=chapter` override only; a work whose DEFAULT is chapter no longer blocks per-scene co-write — the mode selects the autonomous entrypoint, not whether co-write is allowed). **/review-impl (1 MED fixed, no HIGH):** `draft_chapter` was missing from `_OPERATION_INSTRUCTIONS` → the whole-chapter pass got the fallback per-scene instruction ("write the next passage of the scene"); registered a chapter-level instruction + a regression-lock (`diverge` rebuilds messages internally, so the happy-path mocks hid it). Boundaries verified: `gather_structural`/`gather_recent` `id=None`-safe; M5 isolation holds (a foreign `chapter_id` → 0 scenes → 400, no leak); no route collision with `…/chapters/{id}/prose`. **Verify:** 296 composition unit (283 B1 baseline + 13: 8 chapter_gen pure incl the instruction lock + 5 chapter-endpoint incl never-persisted; B1 tests updated 501→409). Single-service; **3-mode live-smoke deferred to B4**. **Deferrals (LOW):** no auto-idempotency / in-flight cancel for chapter jobs (design LOW-1); chapter_sort double-fetch (perf); long-chapter output ceiling (design LOW-2); MED-2 chapter-draft persistence is B3.

## ✅ B1 DONE (LOOM-38, 2026-06-07, M, v2.2 + /review-impl) — chapter-assembly-mode plumbing
First BUILD slice of [chapter-assembly-modes](../../plans/2026-06-07-comp-chapter-assembly-modes.md) (XL). **Plumbing only — no engine path** (B2/B3). **`config.py`**: `composition_assembly_mode_default='per_scene'` + declare-ahead `chapter_gen_max_tokens=4096`/`stitch_max_tokens=4096` (unused until B2/B3, codebase declare-ahead pattern). **`app/engine/assembly.py`** (NEW): `AssemblyMode` Literal + `ASSEMBLY_MODES=('per_scene','chapter')` closed set + pure `resolve_assembly_mode(override, work_settings, default)` (precedence request→setting→default; invalid candidates skipped → `per_scene` safety net so a legacy/hand-edited row never crashes generation). **`routers/engine.py`**: `GenerateBody.assembly_mode: Literal[…]|None` override (bad value 422s at request); resolve right after `_load_work_node`; **501 `ASSEMBLY_MODE_NOT_IMPLEMENTED`** when resolved to `chapter` (BEFORE any job/pack/stream — never silently runs per-scene; B2 replaces the guard); resolved mode echoed in the auto JSON, the auto replay JSON, and the cowrite SSE job event. **`routers/works.py`**: `WorkPatch` `field_validator` rejects a bad `settings.assembly_mode` (422 at the PATCH boundary — the only settings write-path; create uses `{}`). **PO (CLARIFY):** 501 guard for the unimplemented mode (not silent fallback); declare token knobs now. **/review-impl (4 LOW/COSMETIC folded, no HIGH/MED):** auto-replay echo consistency, `GenerateBody`-Literal↔`ASSEMBLY_MODES` drift-lock test, auto-echo coverage test, 501-test strengthened to assert `jobs.create` never ran (not just no status-update). **Verify:** 283 composition unit (269 baseline + 14 new) in-process; single-service (no cross-service live-smoke — all 3 modes live-exercised at B4 eval `--assembly`).

## ✅ A2-S4a DONE (LOOM-21, 2026-06-06, L, v2.2 + /review-impl) — co-write canon panel + Revise (FE)
Surfaces the A2-S3b auto-`/generate` `canon` verdict the FE was silently dropping. [plan](../../plans/2026-06-06-a2-s4a-canon-panel.md). **FE-only**, consumes already-live backend fields. **`types.ts`**: `CanonViolation` + `CanonResult`; `AutoGeneration.canon?`. **`CanonGatePanel.tsx`** (new, pure view): distinguishes **HARD** (`confirmed===true`, red, `resolved=false`) / **ADVISORY** (`confirmed==null`, amber, symbolic-only/judge-down — flag & override) / **UNCHECKED** (`status ∈ skipped_no_cast|skipped_no_position|degraded` + reason) / **clear** (gated on the authoritative `resolved`, + "auto-revised ×N"). **`ComposeView.tsx`**: renders the panel above the candidate cards (guarded by `auto.data?.canon` — replay/cowrite omit it); per-violation **Revise** appends the violation context to the guide textarea + focuses it (**PO: author steers**, not a one-click re-run). i18n ×4 (en/ja/vi/zh-TW composition namespace). **/review-impl: 3 fixed** — (1) MED false-green: `clear` now trusts `resolved` not just an empty filtered list (the arc's "no silent false-green" leaking into FE); (2) append-preserve regression-lock (author guidance survives Revise); (3) row-key→index. 2 accepted (unknown-status→degraded copy; stale-panel-on-next-error mirrors CandidatesView). **Verify:** **45 composition vitest** (10 CanonGatePanel + 17 ComposeView incl 5 canon), `tsc --noEmit` clean. FE-only → no cross-service live-smoke; canon fields live-proven the prior session. PO: split A2-S4 into a (this) + b (publish gate); Revise = pre-fill+focus.

## ✅ A2-S4b DONE (LOOM-21, 2026-06-06, L, v2.2 + /review-impl) — publish-gate canon distinction in editor toolbar
Surfaces the 3 canon gate fields the FE was silently dropping. [plan](../../plans/2026-06-06-a2-s4b-publish-gate-canon.md). **FE-only**, consumes already-live backend gate fields. **`types.ts`**: `PublishGate` += `canon_blocked` / `canon_unresolved_scenes` / `canon_unchecked_scenes`. **`usePublishGate.ts`**: `ChapterPublishGate` += 3 canon fields (degrade-open `false/0/0` when no Work / loading / error); new exported **pure** `publishGateMessages(gate, t) → {blockedReason?, uncheckedWarning?}` — `canonBlocked` is a DISTINCT blocked reason (combined with pending-scenes via `'; '` when both apply — PO decision); `canonUncheckedScenes>0` is a **non-blocking warning** (independent of `blocked`). **`ChapterEditorPage.tsx`**: uses `publishGateMessages`; renders an amber **unchecked chip** (`AlertTriangle` + count + tooltip `gate_unchecked_hint`, `aria-hidden` on icon) next to `PublishControl`. Contract to `PublishControl` unchanged. 4× `editor.json` locales: `gate_canon_blocked`, `gate_unchecked`, `gate_unchecked_hint`. **/review-impl: 4 fixed** — (1) MED: new `ChapterEditorPage.test.tsx` (2 tests lock chip wire-up, vi.hoisted + partial module mock to preserve real `publishGateMessages`); (2) LOW: "blocked AND unchecked simultaneously" regression-lock; (3) LOW: degrade-open `|| undefined` fallback regression-lock; (4) LOW: `aria-hidden="true"` on decorative `AlertTriangle`. **Verify:** vitest **68 pass** (64 → +4 from /review-impl), `tsc --noEmit` clean · `LIVE-SMOKE deferred to D-A2S3B-LIVE-SMOKE`.

## ✅ S2 compress + S1 story_order FIX (LOOM-31, 2026-06-07, L, v2.2 + /review-impl) — assembly machinery validated HONESTLY
[plan](../../plans/2026-06-06-comp-longform-state-reinjection.md) S2. **The live-smoke caught that S1 was SPURIOUS:** decompose-committed scenes had `story_order=None` (`create_decomposed_tree` never set it), so S1's `story_order`-gated fallback never fired — the LOOM-30 "38→12" was variance, not state-reinjection. **S1-FIX:** `decompose_commit` assigns each scene a reading-order `story_order` = `chapter.sort_order*1000 + idx` (chapter-major, stable, collision-free ≤1000 scenes/ch); `create_decomposed_tree` persists it. This also un-degrades the scenes' canon/spoiler/timeline lenses (they fail-closed on `story_order=None`). **S2:** `engine/compress.py` `compress(state)→summary` (DOME KG quadruples + RecurrentGPT NL-memory; degrade→"" keeps raw; spoiler-safe — fed the packer's ALREADY position-filtered `tl_kept` + strictly-prior prose). `pack()` compresses the older story-so-far instead of tail-trimming when it exceeds `PACK_COMPRESS_RECENT_THRESHOLD_CHARS` (compose env, default 6000); summary renders first in the `recent` block, protected. **Verify: 262 unit** + **live (composition+book+glossary+provider-registry):** compress engaged **5×** (positive proof S1 now populates `recent` + S2 fires), **A3 BEATS V0 1-0-1, defects A3:2 vs V0:13 — first GENUINE A3 win** on the concat metric (supersedes the spurious LOOM-30 runs). **/review-impl: no HIGH** (verified compress spoiler-safe via tl_kept + degrade-safe + budget-protected + story_order flows); 2 MED documented (D-COMP-COMPRESS-INPUT-CAP; force story_order collision → D-A3-COMMIT-TRUE-REPLACE), 2 LOW. Compress log line + compose knob are keepers.

## ✅ S1 packer dual-source state-reinjection SHIPPED (LOOM-30, 2026-06-07, L, v2.2 + /review-impl) — SUPERSEDED: was SPURIOUS (story_order=None no-op), corrected in LOOM-31
First slice of D-COMP-LONGFORM-STATE-REINJECTION. [plan](../../plans/2026-06-06-comp-longform-state-reinjection.md) S1. **`GenerationJobsRepo.prior_scene_drafts`** — latest-completed winner text per prior scene, STRICTLY position-bound (`story_order<current`; intra-chapter; M5-isolated on both job + node). **`gather_recent` extended** (the existing L3 `recent` lens): accepted chapter draft (primary) → **generated-scene fallback** when no draft yet (the autonomous/not-yet-accepted gap the A-EVAL/B concat eval exposed); inherits PRIO_RECENT (below PRIO_CANON — never evicts canon/spoiler-safety). Wired `pack()`→engine+grounding (parity); reverted B's manual guide-threading (S1 auto-injects). **Verify: 255 unit pass** + **live (composition+book+glossary+provider-registry):** eval_a_validate AUTOMATIC reinjection → **A3 defects 38(baseline)→17(B-manual)→12(S1, best); A3 0-2→1-1 (won P0 clean, 2 vs 9 defects).** State-reinjection lever confirmed in production. **/review-impl: 1 MED fixed** (prior_scene_drafts filters o.user_id/project_id too — M5 defense-in-depth), **1 MED documented** (SQL position-bound = live-smoke/integration obligation, no DB in test stage), 2 LOW (intra-chapter scope documented; unused guide param). **Residual (both SEPARATE from S1):** P1 decompose-plan-content "ledger" drift (D-COMP-DECOMPOSE-PLAN-LEDGER-DRIFT) + granularity axis (D-COMP-CHAPTER-STITCH-PASS). NEXT = S2 compress.

## ✅ D-COMP-LONGFORM-STATE-REINJECTION PLAN checkpoint DONE (LOOM-29, 2026-06-06, XL, v2.2 + /review-impl — plan only, BUILD deferred)
The first §10.2 build (state-reinjection — the eval-validated long-form lever). [plan](../../plans/2026-06-06-comp-longform-state-reinjection.md) (impl plan; §10.2 design already LOCKED in the reasoning-engine spec). **PO scope = FULL §10.2 + dual-source state**, reconciled with validate-first by **slicing**: **S1** packer dual-source state-reinjection (the validated lever, build first + measure) → **S2** `compress` primitive (scale long chapters) → **S3** `ReasoningState` threading (F2) → **S4** narrative_thread ledger (advisory, D4). Each eval-gated vs `eval_a_validate.py` (the realistic concat metric); human checkpoint after S1. **/review-impl (2 HIGH design + 1 MED + 1 LOW folded):** state-reinjection is a NEW prompt path the spoiler system (`spoiler.py`) doesn't guard → **(H1)** S1 generated-scene gather MUST position-bound `story_order<current` + spoiler regression test; **(H2)** S2 compress MUST summarize from the spoiler-filtered KG not raw; **(M)** draft-source = "current chapter context" (whole draft) vs generated-source = strictly-prior — semantics pinned; **(L)** dual-source is fallback-not-merge. Also folded: story_so_far ranks below canon-safety in the budget; eval must revert B's guide-threading when S1 lands. **Granularity residual flagged D-COMP-CHAPTER-STITCH-PASS (separate, not this milestone).** **No code — plan artifact only; BUILD S1 next.**

## ✅ A-EVAL (C) fair plan-isolation eval DONE — decompose plan VALIDATED, arc RESOLVED (LOOM-28, 2026-06-06, S, v2.2)
[`scripts/eval_a_fair.py`](../../../services/composition-service/scripts/eval_a_fair.py) (eval-only, reuses eval_a_validate helpers). Removes the scene-granularity confound: BOTH arms generate ONE single-pass cowrite draft per chapter at EQUAL granularity, differing only in the node synopsis — **A3-planned** = decompose chapter intent + scene beats (DOC/F5 detailed outline), **V0** = bare premise. Pairwise-judged (critic, order-swapped). **Result: n=2 TIE (1-1, defects 6v9) → n=3 A3-planned 3-0 (defects 0 vs 27); aggregate n=5 A3 4 / V0 1, defects 6 vs 36.** **DECISIVE:** the decompose PLAN's value is REAL (DOC/F5 holds at fair granularity), and the earlier A3 long-form losses (A-EVAL 0-2, B 0-2) were a **SCENE-GRANULARITY artifact** — equalizing granularity flipped A3 **0-2 → 3-0**. **Phase A is validated** (A1 rerank + A2 canon + A3 plan[3-0] + adaptive-K). The long-form gap is **ASSEMBLY** (state-reinjection[B -55%] + granularity), separable + tracked as **D-COMP-LONGFORM-STATE-REINJECTION** → the next build. Honest scope: directional probe (n=5/3 premises), not a powered study, but a strong consistent signal.

## ✅ A-EVAL DONE (LOOM-26, 2026-06-06, M, v2.2) — longer-form pairwise eval-metric + MAJOR finding
The coherence-median saturated (A1+A3), so this builds the discriminating replacement + uses it to actually test the Phase-A thesis. **`engine/eval_judge.py`** — `pairwise_judge` (two full chapter drafts → `{better:1|2|tie, why, defects_{1,2}:{continuity_breaks,dropped_threads,contradictions,repetition}}`; tolerant parse, degrade→tie+error, server-controlled prompt). **`routers/internal_eval.py`** — `POST /internal/composition/eval/pairwise-judge` (X-Internal-Token; user_id in body for BYOK model resolution; a bounded pairwise comparator, NOT a generic LLM proxy — the host-only LLM path is the chat-service's stateful AG-UI SSE, so this keeps the harness one POST→verdict). **`scripts/eval_a_validate.py`** — A3-full (decompose→commit→per-scene auto-gen→concat) vs V0 (bare cowrite chapter-drafts); pairwise judge = the critic (disjoint), blind labels, order-swapped by parity. **Verify: 250 unit pass** (8 new judge/endpoint) + production rebuilt+recreated + **live run n=2**.
**🔴 MAJOR FINDING — the validate-first gate FIRED (V0 beat A3 2-0, defects A3:38 vs V0:2).** The metric WORKS (discriminates — no saturation). **Root cause:** `/generate` packs canon+outline but **NOT prior generated scenes**, so A3's per-scene drafts are blind to each other → concatenation is incoherent (continuity breaks/repetition/contradictions); V0's whole-chapter drafts read as "a single logical arc." **This is NOT a metric artifact + NOT "Phase A is worthless"** (the per-scene mechanisms — A1 rerank, A2 canon-reflect, A3 adaptive-K — all work). **It means long-form coherence needs CROSS-SCENE STATE-REINJECTION (Re3 F2 / §10.2 `narrative_thread`), which the gate redirects as the real next lever — NOT Phase B's recipe library.** Honest confound: A3 many-isolated-scenes vs V0 few-coherent-chapters; the fair A3 long-form test is currently unbuildable (engine can't thread prior scenes) → the confound IS the finding. **NEXT (B): probe via `guide`-reinjection** (see ▶NEXT) before committing to the production state-reinjection build.
**B probe DONE (LOOM-27):** threaded prior intra-chapter scenes into each A3 scene's `guide` (eval-only), re-ran same 2 premises → **A3 defects 38→17 (-55%; P0 20→14, P1 18→3) — state-reinjection VALIDATED as a real lever — but A3 STILL loses V0 2-0.** Residual cause = **scene-granularity** (judge: "collage of disjointed scenes"): 9 stitched scene-drafts < 3 single-pass chapter-drafts even threaded. The harness can't isolate plan-value from granularity (engine generates per-scene only). → **NEXT (C): fair eval at equal single-pass granularity** (planned-synopsis vs bare-premise). **D-COMP-LONGFORM-STATE-REINJECTION** raised (thread prior-scene state into the packer + a chapter stitch pass — the validated lever).

## ✅ A3 B5 DONE → A3 COMPLETE → PHASE A COMPLETE (LOOM-25, 2026-06-06, M, v2.2) — eval-gate + live-smoke
[`scripts/eval_a3_decompose.py`](../../../services/composition-service/scripts/eval_a3_decompose.py) — host-orchestrated, no production code. Composition image rebuilt + recreated (B1-B4 live). **Live run (composition·book·glossary·provider-registry·knowledge):** decompose preview (3 chapters mapped, all beat_role, 9 scenes, 0 unmapped) → atomic commit (arc+3+9) → **persisted scenes 9/9 beat_role + 9/9 tension** (`create_decomposed_tree` + `existing_scene_chapter_ids` LIVE-PROVEN — first real exercise) → auto-generate per scene. **Adaptive K varied 1/3/3** by structural weight (tension 30/setup→1, 45/confrontation→3 [mid+high-weight-beat bump], 85/resolution→3 [≥70 ceiling]) — **spend 7 vs A1 fixed 9** (efficiency win). **GATE PASS A3≥A1**, BUT **coherence saturated 5/5 both arms** → the DOC coherence-**lift** thesis is UNPROVEN on this metric (the A1 saturation lesson, confirmed) — no lift claimed; the real A3 value is grounded scene-intent + adaptive-K placement. **Phase A (A1 diverge→converge · A2 canon check→revise · A3 decompose+adaptive-K) is COMPLETE + live-proven.** **D-EVAL-METRIC-SATURATION** raised (longer-form metric needed before Phase B). FE for the planner deferred (D-A3-PLANNER-FE).

## ✅ A3 BUILD B4 DONE (LOOM-24, 2026-06-06, L, v2.2 + /review-impl) — decompose + commit endpoints
The preview→commit surface that wires the B3 planner. [spec](../../specs/2026-06-06-a3-decompose-planner.md) · [plan](../../plans/2026-06-06-a3-decompose-planner.md). Composition-only. **`routers/plan.py`**: `POST …/outline/decompose` (runs the planner, returns the proposed arc→chapter→scene tree UNPERSISTED — `suggested_k`/`unmapped_beats`/`present_entity_names_unresolved`; guards: template ownership 404, NO_CHAPTERS 400, **plan_max_chapters guard BEFORE the planner**) + `POST …/outline/decompose/commit` (persists the accepted/edited tree ATOMICALLY; guards: **IDOR** chapter_id ∈ JWT-scoped book chapters [502 if book unreachable — never skips], present_entity ∈ glossary cast [skips on glossary outage, non-FK], `force` already-planned guard 409, tension 0..100 bound 422). **`StructureTemplatesRepo.get`** (owner-scoped, no cross-user leak) + **`OutlineRepo.existing_scene_chapter_ids`** + **`create_decomposed_tree`** (one-Tx arc→chapter→scene; beat_role on SCENES per DB CHECK; reuses existing book chapter_id). Registered in main.py. **Planner now WIRED.**
**/review-impl (1 HIGH + 1 LOW fixed, 2 deferred, 1 accept):** **HIGH** — the guard flag `replace` was a MISNOMER: `replace=true` ADDED a duplicate tree, never replaced → renamed to **`force`** + honest message ("scenes added IN ADDITION, existing NOT removed"). **LOW** — commit now maps `CheckViolationError`/`ReferenceViolationError`→400 (parity with create_node, was 500) + regression test. **Deferred:** D-A3-COMMIT-TRUE-REPLACE (archive prior subtree — own design), D-A3-COMMIT-IDEMPOTENCY (no key → double-submit dupes; accept for B4). **Accept:** scene goal/pov_entity_id unset (intent→synopsis). **Verify:** **242 composition unit pass** in a fresh `--target test` image. **`create_decomposed_tree`/`existing_scene_chapter_ids` are router-stub-tested only → B5 live-smoke is their first real exercise. LIVE-SMOKE deferred to B5 (D-A3-EVAL-GATE).**

## ✅ A3 BUILD foundation B1-B3 DONE (LOOM-23, 2026-06-06, XL slice, v2.2 + /review-impl) — list_chapters + adaptive-K + planner
The composition-only foundation for the decompose planner; **B4 endpoints + B5 eval-gate remain** (see ▶NEXT). [spec](../../specs/2026-06-06-a3-decompose-planner.md) · [plan](../../plans/2026-06-06-a3-decompose-planner.md). **B1** — `BookClient.list_chapters` (active chapters in reading order, the enumerate-then-map source; 404→[]) + config knobs (`plan_max_chapters=40`, `plan_{min,max}_scenes_per_chapter`, `plan_high_tension_threshold=70`). **B2** — `engine/adaptive_k.py` (pure `f(tension primary 0..100, beat_role secondary)`, clamp `[1, ceiling]`, both-None→ceiling fallback) **wired into the auto `/generate` path** (co-write untouched; hand-authored nodes unchanged). **B3** — `engine/plan.py` decompose L1 (template beats→existing chapters, index-keyed, every chapter kept, surplus→`unmapped_beats`, C>B share-a-beat) + L2 (chapter→scenes `{title,intent,tension,present_entities}`, tolerant parse, cast name→glossary-id resolve w/ unmatched SURFACED, bounded fan-out, per-level degrade). **`plan.decompose` is built+tested but UNWIRED** (no endpoint until B4 — dead code, intentional).
**/review-impl (1 HIGH + 1 MED fixed, 2 LOW documented):** **HIGH** — tension SCALE mismatch: `adaptive_k`/planner assumed 1-5 but `outline_node.tension` is **0..100** (reasoning/policy gates `>=70`) → a mid-tension hand-authored node (50) would always hit ceiling K. Aligned everything to 0..100, threshold 70. **MED** — the auto-path test asserted winner text but NOT the K value (let the scale bug slip) → added `test_generate_auto_uses_adaptive_k_from_node_tension` (low-tension node → K=1). **LOW** — B4 must enforce `plan_max_chapters` at the endpoint (decompose has no guard); cast resolution ignores aliases (roster-constrained, accept). **Also:** design HIGH#1 ("compute K before the budget reservation") was MOOT vs code — no K-multiplied reservation exists (only the K-independent prompt-size 413). **Verify:** **231 composition unit pass** in a fresh `--target test` image build (no stale code). Single-service; **eval-gate (B5) blocks MERGE** — adaptive-K is live on-branch but its quality thesis (A3≥A1) is unproven.

## ✅ A3 DESIGN+PLAN checkpoint DONE (LOOM-22, 2026-06-06, XL, v2.2 — design only, BUILD deferred) — decompose planner + adaptive K
The last V1 Phase-A slice (F5/DOC — push complexity upstream into the plan). [spec](../../specs/2026-06-06-a3-decompose-planner.md) · [plan](../../plans/2026-06-06-a3-decompose-planner.md). **Design committed as a discrete artifact (no code) so the BUILD session inherits locked interfaces.** **CLARIFY (6 PO decisions):** single milestone (planner+adaptive-K together) · multi-level **arc→chapter→scene** · **beats=chapters mapped onto EXISTING book chapters** (composition-only, BookClient read-only, NO book writes → no /amaw) · planner LLM emits **per-scene tension (1-5)** · **preview→commit** (decompose returns unpersisted tree; separate commit persists). **Design:** `decompose` = 2 LLM levels (L1 map template beats→existing chapters w/ beat_role+intent, surplus beats surfaced as `unmapped_beats`, C>B = chapters-share-a-beat; L2 chapter→scenes w/ intent+tension+present_entities). **Schema-honest tree** `arc→chapter(chapter_id, beat in title/goal)→scene(beat_role,tension,intent,present_entity_ids)` — `beat_role` is scene-only per DB CHECK; chapter/scene reuse EXISTING book `chapter_id` (never mint). **Adaptive K** = f(tension primary, beat_role secondary), clamp [1, compose_diverge_k], None→ceiling fallback. **Eval-gate** A3-vs-A1 coherence+outline-relevance, validate-first. **Design self-review (4 folded):** HIGH#1 compute adaptive-K **before** the /generate budget pre-check (K varies per scene now) + reserved-K==diverge-K test; MED#1 chapter-nodes are navigational-only (packer keys on chapter_id+story_order not parent chain) + dedupe guard; LOW C>B share-beat; LOW glossary auth-mode confirm at BUILD. **PO-approved.** BUILD = ~11 files, plan B1-B5, mid-BUILD seam B3/B4. **FE out of scope** (BE+eval only, mirrors A1/A2→A2-S4). **No VERIFY/eval yet — design artifact only.**

## ✅ D-A2S3B-EVAL-HARNESS + D-A2S3B-LIVE-SMOKE DONE (LOOM-21, 2026-06-06, S, v2.2) — the A2 arc fires end-to-end, live
[`services/composition-service/scripts/eval_a2_canon.py`](../../../services/composition-service/scripts/eval_a2_canon.py) — n=3 seeded-contradiction harness; dev-only, **no production code changed**. Per scenario: fresh throwaway book + 2 chapters + composition Work → mint a character as a glossary entity (linked to BOTH chapters) → seed a chapter-1 death via knowledge `persist-pass2` (writes `:EntityStatus{gone, from_order=1M}`) → outline a chapter-2 scene (`at_order=2M`) whose cast = that character → `/generate mode=auto` → inspect the returned `canon` verdict.
**Live result (real cross-service: composition · knowledge · glossary · book · provider-registry):** `3/3 FIRED + repaired` — `status=checked, iters=1, resolved=True, violations=0` each. Full chain proven: seed `gone` → `fact_for_check` returns gone at the ch2 position → SCORE symbolic guard matches the name → distinct LLM-judge confirms → reflect loop runs ONE revise pass that **repairs** the draft. Serves as BOTH the eval harness AND the contradiction-FIRE+repair live-smoke.
**Detection criterion = `status=="checked" AND iterations>=1`, NOT the CLARIFY-guessed `resolved=false`.** Reading `reflect_revise` showed `resolved` is the *repair outcome* (a successful auto-repair → `resolved=true`), so gating on `resolved==false` would FAIL the eval precisely when the loop works best. `iterations>=1` is the true FIRE signal (a revise pass only runs after the judge confirms a HARD violation); `resolved` is reported as `repaired`/`flagged-unrepaired`.
**Seeding gotcha (memory):** the knowledge anchor loader fetches anchors via glossary `known-entities`, which `HAVING COUNT(chapter_entity_links) >= 2`. A bulk-extract entity with **0 chapter links is invisible to the loader** → no anchored `:Entity{glossary_entity_id}` → status_effect unresolved → `statuses=0` (first live run was a clean 0/3 NO-FIRE from exactly this). Fix: the seed links the entity to ch1+ch2 (frequency=2). This is a real property of the production anchor path. **Known limit:** detection depends on the drafter literally naming the character (pronoun-only prose → NO-FIRE; re-run on flake). **Ports (live stack):** gateway `:3123`, glossary internal `:8211` (NOT 8213 = video-gen), knowledge internal `:8216`; `X-Internal-Token: dev_internal_token`.

## What LOOM is
The name = LoreWeave's **loom**. The **Canon Model** is the **warp** (the fixed structural threads: *published* canon, in-world/reading order, provenance); the AI **co-writer** weaves the **weft** (new prose) through it. Spoiler-safety = you can only weave with threads already laid down (no future-canon leak). LOOM turns a book into living canon, then co-writes grounded in it.

Two parts, built in order:
1. **Canon Model (Cycle 0, prerequisite)** — fixes pre-existing platform bugs B1–B7 that composition surfaced (NOT composition scope). 4 primitives: editorial lifecycle · canon=published · dual-order populated · provenance (aligned with enrichment H0, no enrichment edits).
2. **Composition (V0)** — lore-grounded co-writer + visual planning, built on the Canon Model.

## Design SSOT (locked 2026-06-03, commit `bad7acda`, after 4 adversarial review rounds)
- Canon Model: [spec](../../specs/2026-06-03-canon-model.md) · [Cycle-0 plan](../../plans/2026-06-03-canon-model-cycle0.md) (§8 = corrected/authoritative)
- Composition: [design](../../specs/2026-06-02-composition-design.md) (§12 contract-review, §13 ops-benchmark) · [V0 plan](../../plans/2026-06-02-composition-service-v0.md)

## Locked decisions
- Full **event-driven** extraction rework (coalescing per-project drainer over `extraction_pending`, respects the one-active-job/project index; retract-before-reextract; pinned-revision).
- **canon = published** (extract on `chapter.published` at the pinned revision; `chapter.saved` no longer canonizes).
- **CM-FE (publish UI) in Cycle 0** (else KG/passages stale platform-wide).
- **chapter-gate publish** — composition publishes a chapter only when ALL its scenes are `status='done'`.
- **dual-order:** reading-order (`event_order` from `sort_order`) is the dense V0 spoiler axis; `chronological_order` (from `event_date_iso`) is best-effort/sparse for CJK corpus.
- **enriched-lens deferred** to composition's final phase (glossary has no structured enrichment-content read yet).
- Boundary: touches book/worker-infra/knowledge/worker-ai/SDK + FE; **lore-enrichment never**.

## Build order
**Cycle 0 (Canon Model):** CM1 (book lifecycle + `/publish` + migration) → CM3a (revision_id event + internal revision-text endpoint) → CM2 (relay confirm — no-op) → CM3b (knowledge queue + worker-ai coalescing drainer + pinned-revision + retract-before-reextract + B7 fix) → CM3c (passage-ingest + manual-rebuild gating) → CM4 (dual-order + backfills) → CM-FE (publish affordance) → CM5 (provenance).
**Then Composition V0:** M0 skeleton → M1 schema → M2 repos → M3 clients/prose-source → M4 packer → M5 isolation → M6 engine+critic → M7 contract+gateway → M8 FE tab → M9 OI-1 publish wiring.

## ✅ CM1 DONE (LOOM-01, 2026-06-04) — book-service editorial lifecycle
Shipped (book-service only, additive): `chapters.editorial_status` (draft|published) + `published_revision_id` (FK ON DELETE SET NULL) + `canon_model_migration` marker table + marker-gated one-time `backfillSQL`; `publishChapter`/`unpublishChapter` handlers + routes; `chapter.published{book_id,chapter_id,revision_id}` / `chapter.unpublished` events; import → published + pinned revision; editorial fields on `getChapterByID` + `getInternalBookChapter`. 12-phase v2.2 + `/amaw`: design adversary R1 BLOCK→R2 APPROVED_WITH_WARNINGS, code adversary R1 APPROVED_WITH_WARNINGS — all folded (down-migration gate, publish RETURNING+FOR UPDATE, import NULL-pointer + error-check, marker-gated backfill, stable tiebreak). `go build` + `go test` (migrate/api/config) green. CLARIFY PO: import=published · /unpublish ships · edit-after-publish stays published · always-snapshot · publish-empty→404 · no in_review.

## ✅ CM3a DONE (LOOM-02, 2026-06-04) — internal revision-text endpoint
Shipped (book-service only): `GET /internal/books/{book_id}/chapters/{chapter_id}/revisions/{revision_id}/text` under `requireInternalToken`; IDOR-guarded (`rv.id+rv.chapter_id+c.book_id+lifecycle=active` → cross 404); plain-text projection (`->>'_text'`, ordered, null-safe). 12-phase v2.2 (S, self-review) + **/review-impl** (0 HIGH; folded MED-2 caller-gate contract comment, LOW-2 trim `body` from response, MED-1 tightened the live-smoke to require the IDOR-negative case; accepted LOW-1 body_format='json' / LOW-3 projection-swallow). `go build`+`go vet` green. **Contract:** serves ANY revision by id — canon=published depends on CM3b passing `chapters.published_revision_id`.

## ✅ CM2 DONE (LOOM-02, 2026-06-04) — relay confirm, ZERO code change
Verified in-code (`worker-infra/internal/tasks/outbox_relay.go:149-205`): `processSource` SELECTs all unpublished outbox rows (no event_type filter); `streamKey = "loreweave:events:" + aggregate_type` (`:179`); event_type only carried in the values map (`:184`), never routes/filters. `insertOutboxEvent` hardcodes `aggregate_type='chapter'` and book-service is already a relay source (`chapter.saved` flows today) → `chapter.published` + `chapter.unpublished` auto-flow to `loreweave:events:chapter`. No allowlist; statistics-service (separate consumer group) ignores unknown types; knowledge consumes in CM3b. No code, no commit needed beyond this note.

## ✅ CM3b DONE (LOOM-03, 2026-06-04) — graph-extraction cutover (default v2.2 + /review-impl)
knowledge: `extraction_pending +revision_id +aggregate_type filter +upsert_chapter_pending` (keep-LATEST/re-arm); `handle_chapter_published` (queue at pinned rev) / `handle_chapter_unpublished` (`remove_evidence_for_source` retract — **closes D-CM1-UNPUBLISH-RETRACT**) / `chapter_saved` drops graph-queue; main register; persist-pass2 **retract-then-write** (B6, all paths); `JobScope +chapters_pending`. worker-ai: `_enumerate_pending_chapters` + `process_job` chapters_pending branch (shared loop, revision-text, **mark-by-revision**) + `_ensure_chapters_pending_jobs` poll (reuse last-job models+cap, swallow-409, fail-stop guard) + `clients.get_chapter_revision_text` (CM3a) + **B7** chat-drain filter. **/review-impl: 0 HIGH, fixed MED-1 (re-publish-during-drain race → mark-by-revision) + MED-2 (stale-model recreate-fail loop → 1h fail-stop).** Verify: knowledge 2008 pass/1 pre-existing (lifecycle/budget MagicMock, not CM3b); worker-ai 101 pass; py_compile all. **Closes B6, B7, D-CM1-UNPUBLISH-RETRACT.**

## ✅ CM3c DONE (LOOM-04, 2026-06-04) — passage-ingest cutover + manual-rebuild draft-gating (AMAW)
Completes canon=published for the two CM3b-uncovered paths, across **3 services** (the §8.3 "(knowledge)" label under-counted — worker-ai + book-service list-endpoint gap, per §8.2). **book-service:** `getInternalBookChapters` optional `?editorial_status=published` filter (items **+** COUNT, `+published_revision_id IS NOT NULL`) + exposes `editorial_status`/`published_revision_id` on list items + COUNT error→500. **worker-ai:** `ChapterInfo.editorial_status`; `list_chapters(editorial_status=)` maps `published_revision_id`→`revision_id`; `_enumerate_chapters` published-gate (server-side) + null-revision **WARN**-skip; `p3_is_last` scope-guard (the coalescing `chapters_pending` drain never asserts whole-book is_last — closes a latent CM3b spurious-roll-up). **knowledge:** `BookClient.get_chapter_revision_text` (CM3a); `ingest_chapter_passages(+revision_id, +delete_stale_on_missing)`; `handle_chapter_published` now ALSO ingests L3 passages at the pinned revision (`_ingest_published_passages`, graph-queue first); `handle_chapter_unpublished` retracts passages (independent best-effort try); **`handle_chapter_saved` DELETED** + unregistered (main.py); estimate gates to published.
**AMAW 12-phase:** design adversary R1 BLOCK→R2 BLOCK→R3 APPROVED_WITH_WARNINGS (4 BLOCK + 3 WARN folded: server-side filter, null-ptr WARN, is_last scope-guard, positional-arg wiring, delete_stale_on_missing, independent unpublish-retract, parity lock); code adversary R1 APPROVED_WITH_WARNINGS (Go placeholder-arithmetic + dual-retract verified correct; folded published-filter NOT-NULL + COUNT-err→500); Scope Guard **CLEAR**. **Verify:** book go build/vet/test green; worker-ai 104 pass (+3 CM3c); knowledge 1956 unit pass. **LIVE-SMOKE deferred → D-CANON-CYCLE0-LIVE-SMOKE** (running stack is pre-CM1: live `loreweave_book` has no `editorial_status` col → CM3c can't smoke in isolation; whole CM1→CM3c chain tested together at a future stack-up). **Closes LOW-3** (manual rebuild no longer extracts drafts). PO: drop chapter.saved · passages publish-only.

## ✅ CM4 DONE (LOOM-04, 2026-06-04) — dual-order population + backfills (v2.2 + /review-impl)
Populates the reading + chronology axes the timeline already filtered on but B1 left perpetually NULL. **Knowledge-internal.** **write-path** (`pass2_writer`): `event_order = HierarchyPaths.chapter_index × EVENT_ORDER_CHAPTER_STRIDE + within-chapter idx` (chapter ordinal already threaded via P3; legacy/chat → None → timeline null-sinks); `merge_event` ON MATCH coalesce→**MIN-keep** (monotone-earliest; **⚠️ event_id is chapter-keyed so this is re-extraction stability, NOT cross-chapter — the PO premise was off**); `rerank_chronological_order` (sequential date-rank, undated→NULL, stable id tiebreak) called **debounced** (only when ≥1 dated event written) in the same Tx. **passage** `chapter_index` ← `get_chapter_sort_orders` (the CM3c-kept client). **backfill** `backfill_orders.py` + internal `POST /internal/projects/{id}/backfill-orders` (one-time legacy migration, mirrors C18). **PO:** event_order min · backfill = internal endpoint.
**/review-impl (3 MED folded):** INT32→**INT64 null-sentinel** (`9223372036854775807`) in the 3 timeline ORDER BY — books >2147 chapters breached INT32_MAX once event_order was populated; backfill gap-fill **collision** (re-run-after-growth dup order) → reverted to unconditional one-time SET; **shared `EVENT_ORDER_CHAPTER_STRIDE`** (was duplicated pass2_writer+backfill → drift). LOW: stride = composition cutoff contract (`before_order = N × stride`). **Verify:** knowledge 1969 unit pass (+CM4); single-service. **Neo4j Cypher ordering correctness deferred to integration/D-CANON-CYCLE0-LIVE-SMOKE** (units cover orchestration via fakes only).

## ✅ CM-FE DONE (LOOM-04, 2026-06-04) — Publish affordance (v2.2 + /review-impl)
FE-only (gateway `pathFilter '/v1/books'` proxies CM1's publish/unpublish transparently; `getChapter` exposes `editorial_status` per CM1). **`books/api.ts`**: Chapter editorial fields + `publishChapter`(sends `expected_draft_version`)/`unpublishChapter`. **`usePublishChapter`** hook (publish/unpublish, 409 `CHAPTER_DRAFT_CONFLICT` toast, confirm state). **`PublishControl`** component: status badge + Publish/Re-publish + Unpublish behind a **destructive confirm** (explains KG+passage retract). **`ChapterEditorPage`**: `editorialStatus` state + `refreshEditorialStatus` (meta-only refetch, no editor clobber) + control by Save; **publish disabled while dirty** (publish snapshots the SERVER draft → save first; `save()→load()→setVersion` gives a fresh draft_version). i18n ×4. **PO:** badge+Re-publish (no divergence detect) · confirm on unpublish. **/review-impl (3 LOW folded):** render-null when `editorial_status` undefined (pre-load flicker + pre-CM1-BE graceful degrade); bare-`status:409` test; i18n key regression-lock. **Verify:** tsc clean; **9** PublishControl tests pass. Browser smoke → D-CANON-CYCLE0-LIVE-SMOKE (stack pre-CM1).

## ✅ CM5 DONE (LOOM-04, 2026-06-04) — provenance (minimal) (v2.2 + /review-impl)
Extraction stamps an authorship-origin tag on every node it writes. **Knowledge-internal · NO scoring change · lore-enrichment untouched.** `PersistPass2Request.provenance` = closed `Literal{human_authored, ai_assisted, enrichment}` (default `human_authored`) → threaded `persist-pass2 → write_pass2_extraction → resolve_or_merge_entity/merge_entity/merge_entity_at_id/merge_event/merge_fact`. **PO: ACCUMULATE** — node property `provenances` is a deduped **array** via the existing `source_types` CASE pattern (ON CREATE `[hint]` / ON MATCH `coalesce(x.provenances,[]) + hint`; coalesce guards pre-CM5 nodes). Relations excluded (V0=nodes). Worker + 3 orchestrator call-sites inherit the `human_authored` default; composition sends `ai_assisted` later. **/review-impl (0 HIGH/MED):** verified models are plain BaseModel (`extra='ignore'`) so `provenances` is dropped on read (no 500/etag drift) + added converter-tolerance regression-locks. **Verify:** knowledge 1974+6 unit pass; boundary clean. `provenances` array-accumulate Cypher → D-CANON-CYCLE0-LIVE-SMOKE.

## 🎉 CANON MODEL CYCLE 0 BACKEND COMPLETE — CM1·CM2·CM3a·CM3b·CM3c·CM4·CM-FE·CM5 all DONE.

## 🔧 CM3b-RETRACT-FIX DONE (LOOM-20, 2026-06-06, L, v2.2 + /review-impl) — canon retract was a silent no-op
**Pre-existing HIGH bug** surfaced at A2-S1b-1 VERIFY (re-POST same chapter w/ new job_id → evidence_count climbed 1→2 on Entity/Event). **Root cause:** `:ExtractionSource` is keyed by a **hashed** id (`extraction_source_id(user,project,source_type,source_id)`); `remove_evidence_for_source` matches that hashed id, but the **re-publish persist** ([internal_extraction.py](../../../services/knowledge-service/app/routers/internal_extraction.py)) AND the **chapter.unpublished** retract ([handlers.py](../../../services/knowledge-service/app/events/handlers.py)) both passed the **raw** `source_id` (worker sends `source_id=ch.chapter_id`) → MATCH found nothing → **0 edges removed**. So re-publish never dropped disappeared canon (drift) + unpublish never retracted. Unit tests masked it by calling the helper with the correct hashed `src.id`. **Fix:** new `remove_evidence_for_natural_key` (hashes internally, symmetric w/ `upsert_extraction_source`) at both sites; persist adds `cleanup_zero_evidence_nodes` gated on `removed>0` (under the one-active-job lock); unpublish is **retract-only** (no sweep — /review-impl MED: would race a concurrent same-project extraction outside the job lock). **Verify:** unit 208 + 20 persist/unpublish; **8 live-Neo4j integ** incl a negative control reproducing the bug (`removed_buggy==0` vs `removed_fixed==1`); **live drop-case smoke** on rebuilt image — persist Alpha+Beta → re-persist Alpha → **Beta swept, Alpha stays evidence_count=1** (not inflated). **/review-impl:** 1 MED fixed (unpublish-cleanup race removed), hash-identity verified, 2 LOW deferred (below).
**New deferred:** **D-CM3B-ATOMIC-RETRACT** (Perf/robustness) — `run_write` auto-commits per statement so retract→write→cleanup aren't atomic; a mid-write failure now leaves a transient canon gap (self-heals via worker retry + pending re-arm + K11.9 reconciler). Wrap in `execute_write` tx if it bites. **D-CM3B-CHAPTER-DELETED-COUNTERS** (Naturally-next) — `handle_chapter_deleted` (K14.7) DETACH-deletes the source by property but leaves stale target `evidence_count` + orphan nodes; separate feature, reconciler GCs.

## ✅ A2-S1b-1 DONE (LOOM-20, 2026-06-06, L, v2.2) — entity-status persist plumbing (eval-SAFE)
Wires the A2-S1a `:EntityStatus` primitive into the live persist path, **plumbing only, no prompt change** (eval-neutral; b2 activates it). [spec](../../specs/2026-06-06-kg-entity-status-model.md) · [plan](../../plans/2026-06-06-a2-s1b-1-status-persist.md). **SDK** ([event.py](../../../sdks/python/loreweave_extraction/extractors/event.py)): `StatusEffect{entity_ref,status}` + `status_effects` on `_LLMEvent` (tolerant `@field_validator` drops malformed, keeps the event) + `LLMEventCandidate`, threaded through `_postprocess`/`_tolerant_parse`. Reserved-plumbing pattern → b2 = pure prompt edit. **worker-ai unchanged** (dumps the full candidate via `model_dump` → field flows free). **knowledge** `provenance.py`: `EntityStatus` = 4th `TARGET_LABELS` (so `add_evidence` accepts it; retract is label-generic) + `cleanup_zero_evidence_nodes` parity. `pass2_writer.py`: consume `status_effects` in the event loop at the event's `event_order` (reading axis); resolve `entity_ref` via chapter-map (Tier A.1) → glossary anchor (Tier A.2) → **skip+log** (NO autocreate); **skip+log when `event_order is None`** (M2 legacy/chat); `statuses_merged` counter. **Canon-safe ON the CM3b-fixed retract:** re-extract retracts the old status edge + re-adds → evidence stays 1; a MOVED death orphans the old status (new id) → persist `cleanup_zero_evidence_nodes` (now incl `delete_entity_status_with_zero_evidence`) sweeps it.
**Verify:** unit SDK 25 (+3 status) + knowledge 85 (pass2-status 4 + provenance EntityStatus 4 incl live retract-idempotency); **live smoke** synthetic `persist-pass2` → `:EntityStatus{gone, from_order=3M, ev=1, provenances=[human_authored]}`; re-extract new job → **1 node, evidence=1** (not 2). PO: resolve chapter-map→anchor→skip (no autocreate) · dormant-until-b2 synthetic-proof.

## ✅ A2-S1b-2 DONE (LOOM-20, 2026-06-06, L, v2.2 + /review-impl) — status prompt activation + backfill (eval-GATED)
Activates the dormant b1 field. [plan](../../plans/2026-06-06-a2-s1b-2-status-prompt-backfill.md). **Prompt** ([event_extraction_system.md](../../../sdks/python/loreweave_extraction/prompts/event_extraction_system.md), the live `event_system`): `status_effects` in the output schema + abstract **Rule 10** (coarse active/gone; explicit "do NOT change the language of any other field" guard per the CJK-bias lessons) + extended example (`[]` + a death `gone`). **Backfill** ([backfill_status.py](../../../services/knowledge-service/app/db/migrations/backfill_status.py) + `POST /internal/projects/{id}/backfill-status`): LLM-classify existing events → coarse status; **non-null `event_order` only** (MED#2 skip+count), **rides each event's own ExtractionSource** (provisional — re-publish/unpublish retract + the persist `:EntityStatus` cleanup supersede it), **enforces entity_ref ∈ participants** (/review-impl #1), idempotent for a fixed classification (re-runs accumulate under LLM variance — run once). Reuses the `chat` op (no new JobOperation enum).
**Eval-gate = relative A/B** (PO: F1=0.869 baseline model qwen3.6-35b not loaded → isolate the prompt delta with the loaded qwen2.5-32b): real LM Studio, NEW vs OLD event prompt on CJK/VN/EN fixtures → **no event-recall regression**, **CJK/VN summaries stay in-script** (NEW improved CJK in-script — the language guard worked), **status_effects=gone activated in all 3 languages** with `entity_ref` in the correct script (Castor / 卡斯托 / lãnh chúa Castor). **Verify:** unit SDK prompt 4 + backfill parser/stub 5 + integ 6 + existing backfill 18 + SDK event 26 — green. **/review-impl:** 1 MED fixed (participant-membership), 2 documented (idempotent-claim softened, multi-source), 1 LOW (total-failure==no-deaths), 1 COSMETIC fixed (classify stub tests).
**Deferred D-A2S1B2-LIVE-SMOKE** — full publish→worker→persist→status-from-prompt cycle (needs worker-ai rebuild + a death-chapter publish + model registration); each link proven separately. **Caveat:** absolute F1=0.869 not reproduced (relative-A/B agreed); alias-only participant refs skip+count. **LOW#5 moot** (dedicated `:EntityStatus` node → never in fact RAG selectors).

## ✅ A2-S3b-CANON-STATUS DONE (LOOM-20, 2026-06-06, M, v2.2) — dirty-data: make the canon skip explicit (no silent false-green)
Production-hardening from the PO insight "dirty data is normal in a real DB — can we work with it?". The canon guard already degrades safely (a dangling chapter ref / knowledge outage → skip, generate still succeeds), but the skip was SILENT (`canon.resolved=true` whether checked-clean OR couldn't-check) → a scene could silently lose canon protection while looking green. **Fix:** `ReflectResult.status` ∈ `checked | skipped_no_cast | skipped_no_position | degraded`, set by `run_canon_reflect` (no cast → skipped_no_cast; cast but no resolved position → skipped_no_position; knowledge outage → degraded; else checked) → threaded into the engine `canon` dict (default `degraded` so the except path isn't a false-green) → `chapter_scene_gate` surfaces `canon_unchecked_scenes` (latest job status ∈ skipped_no_position/degraded) — **SURFACED not hard-blocked** (false-blocking every un-positioned scene is worse; the FE warns + the author acts). **Verify:** 24 canon units (+3 status) + 4 gate integ (+1 unchecked-surfacing) + 220 unit; **LIVE** — `/generate` mode=auto on a DANGLING-chapter Work (real qwen2.5-32b drafter, provider-registry brought up) → `status=completed` + `canon.status=skipped_no_position` (dirty-data degrade now EXPLICIT, not a silent green). This live-proves the A2-S3b /generate-auto wiring + the degrade path end-to-end (the contradiction-FIRE+repair path — a gone+positioned cast — still wants a clean scenario, but the env now works).

## ✅ A2-S3b DONE (LOOM-20, 2026-06-06, XL, v2.2 + /review-impl) — LLM-judge + reflect(check→revise) in the auto path
The canon gate's load-bearing payoff (PO override of the checkpoint rec). **composition** [canon_check.py](../../../services/composition-service/app/engine/canon_check.py): `judge_canon` (LLM-judge confirms acting-vs-mentioned on the SCORE candidates — distinct model anti-self-reinforcement; CC4 → advisory on failure, never blocks) + `check_canon` (symbolic→judge orchestration) + `reflect_revise` (the §8.3 `check→revise ≤N` loop). [cowrite.py](../../../services/composition-service/app/engine/cowrite.py): `build_revise_messages` + `revise_draft` (non-stream revise via `stream_draft`). [canon_reflect.py](../../../services/composition-service/app/engine/canon_reflect.py): `run_canon_reflect` glue (snapshot@`scene_at_order` → check → revise → meter). [engine.py](../../../services/composition-service/app/routers/engine.py): **auto path** after `sel.winner` — degrade-wrapped (try/except never fails a generate); folds `canon` {violations, resolved, iterations} + revise tokens into the job + response. [pack.py](../../../services/composition-service/app/packer/pack.py) exposes `scene_sort_order`. **/review-impl: 2 MED + 1 LOW fixed** — advisory(confirmed=None) candidates were dropped → now surfaced (hard+advisory, judge-cleared excluded); `reflect_max_iters` clamped to 3 (config-DoS); judge `why` surfaced. **3 LOW documented:** name-based re-check evadable by epithet-rename (V1.5 NLI); revise input tokens uncounted (bounded under-count); gone-POV not checked. **Verify:** 22 canon units (judge confirm/clear/degrade, reflect repair/escalate/giveup/advisory, **glue repairs a seeded gone-entity contradiction w/ fake LLM**) + 123 composition no-regression; degrade-safe; cross-service `fact_for_check` live-proven (A2-S3a). **Deferred:** D-A2S3B-LIVE-SMOKE, D-A2S3B-EVAL-HARNESS, D-A2S3B-PUBLISH-GATE (see ▶NEXT). **Scope:** auto path only (co-write has the advisory critique; D1).

## ✅ A2-S3a DONE (LOOM-20, 2026-06-06, L, v2.2) — SCORE symbolic canon guard (foundation slice)
The deterministic foundation of A2-S3 (PO sliced: judge/reflect/hard-gate/eval → A2-S3b, after grounding revealed the full build needs a glossary-id mapping + a reflect loop in the load-bearing auto-generate path + a contradiction-injection eval harness). **knowledge** [fact_for_check.py](../../../services/knowledge-service/app/db/neo4j_repos/fact_for_check.py): `get_fact_for_check` now also accepts **glossary_entity_ids**, resolving composition's cast (glossary ids) → `:Entity` via the `glossary_entity_id` FK (+ surfaces `glossary_entity_id` for correlation); [internal_canon.py](../../../services/knowledge-service/app/routers/internal_canon.py) request accepts either id source (model_validator: ≥1 non-empty). **composition** [knowledge_client.py](../../../services/composition-service/app/clients/knowledge_client.py) `fact_for_check` (internal-token, graceful-degrade F1); [canon_check.py](../../../services/composition-service/app/engine/canon_check.py) — the SCORE symbolic guard `gone_cast_in_draft(draft, snapshot)` flags `gone` cast members PRESENT in the draft (ASCII word-boundary avoids `Al`-in-`Always`; CJK substring; **over-inclusive by design** — the A2-S3b judge confirms acting-vs-mentioned) + `scene_at_order(sort_order)=sort_order×stride` (the reading-axis contract; a death in a strictly-earlier chapter ⇒ gone for this scene). **Verify:** 10 SCORE-guard units + 11 composition client + 5 knowledge integ (incl glossary-resolve) green; **live cross-service smoke** — POST fact-for-check by glossary id `g-kai` → resolved to `:Entity`, `glossary_entity_id=g-kai`, `status=gone`@5M over HTTP. Guard NOT yet wired into `/generate` (A2-S3b).

## ✅ A2-S2 DONE (LOOM-20, 2026-06-06, M, v2.2) — fact-for-check canon read
The read A2-S3's SCORE guard checks against. **knowledge-internal, read-only.** [fact_for_check.py](../../../services/knowledge-service/app/db/neo4j_repos/fact_for_check.py) — `get_fact_for_check(user, project, entity_ids, at_order)` composes `status_at_order` (position-aware active/gone — the SCORE signal) + batch entity metadata + per-entity current-valid relations + events `event_order ≤ P` (participant-name match). K11.4 user+project scoped. **Design:** status + events are **position-windowed** (`event_order` axis); **relations are current-valid** (datetime `valid_until` is a DIFFERENT axis — not position-windowed, documented). [internal_canon.py](../../../services/knowledge-service/app/routers/internal_canon.py) — `POST /internal/projects/{id}/fact-for-check` (X-Internal-Token; composition already holds the knowledge internal token), resolves user from knowledge_projects (404 unknown), empty snapshot if no graph. **Verify:** unit 128 router/main/internal (no regression) + 4 integ (windowing@P · user-isolation no-leak · empty-ids); **live HTTP smoke** — seeded project: `@4M` Kai **active** events `[Kai walks]`; `@5M` (after death) Kai **gone** events `[Kai dies, Kai walks]` newest-first. Single-service. Known limits: per-entity relation queries (bounded by ids≤200; guard checks few); honorific-variant participant-match miss (coarse V1).

## ✅ D-CANON-CYCLE0-LIVE-SMOKE — LARGELY PROVEN (2026-06-04, stack rebuilt to HEAD 59626bdd)
Rebuilt book+knowledge+worker-ai via `build-stack.sh` (all 3 now git-SHA-stamped at HEAD) + `compose up -d` (migrations applied). LM Studio up (73 models), provider-registry healthy. Smoke on the 封神演义 demo book (owned by test acct `019d5e3c`) + a throwaway book (trashed after):
- **Migrations ✅** — CM1 `chapters.editorial_status`+`published_revision_id`; CM3b `extraction_pending.revision_id`. CM1 backfill: demo's 5 chapters all `published` + pinned pointer.
- **CM1 publish/unpublish/re-publish ✅** (gateway, JWT) — new chapter starts `draft`; publish→`published`+pointer; `chapter.published` payload `revision_id == pinned pointer`; unpublish→`draft`+NULL+`chapter.unpublished`; re-publish→pointer ADVANCES + new event. Sequence created→published→unpublished→published. (The canon spine that had NO DB-backed Go test.)
- **CM3a ✅** — internal revision-text returns pinned text; cross-chapter revision→**404** (IDOR).
- **CM3c ✅** — book list `?editorial_status=published`→5 items w/ fields; `=draft`→0; invalid→**400**.
- **CM3b END-TO-END ✅** (the load-bearing cutover) — re-publish → `extraction_pending` armed with the **pinned** `revision_id` → worker poll → "created chapters_pending drain job" → fetched **PINNED revision text** via CM3a (`GET …/revisions/{pinned}/text`→200, NOT live draft) → LLM extract → `persist-pass2`→200 (entities=4) → pending row **drained** (mark-by-revision).
- **CM4 ✅ (endpoint)** — backfill endpoint→200, correctly skips NULL-`chapter_id` legacy events (events_ordered=0 is right). [event_order at WRITE not live-checked: demo chapters are legacy/no-P3-hierarchy → event_order=None by design; unit-tested.]
- **CM5 ✅** — the 4 freshly-extracted entities carry `provenances=["human_authored"]`; 30 old entities keep NULL provenances and coexist (no error) → proves the `coalesce` write-guard + `extra='ignore'` read-tolerance LIVE.
- **Gateway ✅** — transparently proxies publish/unpublish (CM-FE FE-only assumption confirmed).
**Residual (low risk):** CM-FE browser click-through (API path proven via the publish/unpublish flow + 9 unit tests; only the React render is unsmoked); CM4 write-time `event_order` on a P3-hierarchical chapter; unpublish graph+passage retract on a project-backed chapter (unit-tested; unpublish event proven). **Pre-existing (NOT a CM bug):** "pass2 filter batch failed cat=relation: model not found" → relations=0 on this stack (relation-filter model unconfigured — eval-config gap).

## ✅ Composition V0 — M0 DONE (skeleton, 2026-06-04, v2.2)
New `services/composition-service/` mirroring knowledge-service house style (single-DB `loreweave_composition`). main.py lifespan (create_pool→run_migrations no-op→routers) + config/pool/migrate(no-op, M1 fills §1.2 DDL)/logging/metrics + 3 middleware copies + health/ping/metrics routers + 3-stage Dockerfile + requirements + test_skeleton/conftest. Infra: db-init `loreweave_composition` line + compose block **host 8217→container 8093** (python-urllib healthcheck). **Verify gate MET LIVE:** built + `compose up` → Up(healthy) → `GET /health` 200 `{status:ok,db:ok}` + `/v1/composition/ping` `{pong:true}` + `/metrics`; host pytest 3 pass. Boundary clean (additive infra only; no lore-enrichment/other-service edits). DB created manually on running stack (db-init only auto-runs on fresh volume).

## ✅ Composition V0 — M1 DONE (schema + migrate, 2026-06-04, v2.2)
§1.2 DDL in `migrate.py` `_SCHEMA_SQL` — **7 tables** (`composition_work`, `structure_template`, `outline_node`, `scene_link`, `canon_rule`, `generation_job` incl. **`base_revision_id`** OI-2 guard, `outbox_events`) + indexes + CHECK constraints (all `IF NOT EXISTS`). **6 built-in `structure_template`** seeded (owner NULL, full canonical beats — save_the_cat 15 / hero_journey 12 / story_circle 8 / kishotenketsu 4 / web_novel 6 / generic 3) via deterministic UUIDs + `ON CONFLICT DO NOTHING` (idempotent). `app/db/models.py` = 7 Pydantic row models + StringConstraints caps. **Verify (gate MET + LIVE):** integration `test_migrate` 3 pass on real PG (migrate ×2 idempotent + 6 seeded once; insert all 7 tables; scene-no-chapter → CheckViolation); host unit 3 pass; composition-service rebuilt+restarted → Up(healthy), dev `loreweave_composition` has 7 tables + 6 templates with correct beat counts. Cross-DB ids = no-FK (§1.4, app-validated); in-DB FKs OK. Test DB throwaway dropped.

## ✅ Composition V0 — M2 DONE (repos + Work resolution, 2026-06-04, v2.2 + /review-impl)
6 repos in `app/db/repositories/`: **works** (CRUD + `resolve_by_book` prefers-marked §6.2 + If-Match), **outline** (tree CRUD, fractional `rank` via new `rank.py`, If-Match, **recursive soft-archive** PO-decision), **scene_links** (CRUD, hard-delete edges), **canon_rules** (CRUD + `list_active` + soft-archive), **generation_jobs** (lifecycle + idempotency replay), **outbox** (txn-local `emit(conn,…)`). `__init__` exports `VersionMismatchError`+`rows_changed`. **PO pulled the knowledge client forward into M2** → `clients/knowledge_client.py` (**JWT-forward** — contract-verified `GET /v1/knowledge/projects?book_id` is JWT-only, no internal variant) + `work_resolution.py` (full §6.2: found/candidates/unmarked_single/unmarked_candidates/none/unavailable; takes `bearer` param, router-free). `deps.py` factories; `main.py` closes the client. **M5 isolation folded in:** every method user_id-first, every query (incl recursive CTE) user_id-filtered.
**/review-impl (1 HIGH + 2 MED FIXED, 3 regression-locks):** HIGH — resolve filtered `project_type=='book'` but the platform binds a book's grounding project by **book_id ONLY** (editor panel `listProjects({book_id,limit:1})`, FE create defaults 'general') → would miss the real project → POST /work makes a **duplicate empty project** → empty-graph grounding; fixed to book_id-only match. MED — `rank` ordering assumed byte-order but the DB is **en_US.utf8** (confirmed via pg_database; PG18 dropped `SHOW lc_collate`) → added `COLLATE "C"` to list_tree ORDER BY + `_next_rank` max(). MED — `archive_node` used `UNION ALL` → a parent cycle (reachable via reparent, no guard) recurses forever/hangs → switched to `UNION`.
**Verify:** **44 pass** (22 unit: rank algorithm, §6.2 6-branch resolution, JWT-forward via respx; 16 integration on **REAL infra-postgres :5555**: If-Match 412-vs-404, recursive archive, **cycle-terminates**, **collation-order**, idempotency replay, **txn-local outbox commit+rollback atomic**; 6 M0/M1 carryover). Integration FK caught a test bug (random outline_node_id→fixed). py_compile + import smoke clean; throwaway DB dropped. **NO rebuild needed** (M2 is library code; no new endpoints/schema — M1 schema unchanged, running container behavior identical until M3 routers wire it).

## ✅ M2-closure batch DONE (2026-06-04, v2.2) — all 3 M2 defers solved
**D-COMP-M0-LOG-SERVICE-LABEL** → `logging_config` labels logs `composition-service` (unit-locked). **D-COMP-M2-XREF-OWNERSHIP** → closed at the **repo layer** (defense-in-depth, no router needed): new `ReferenceViolationError`; `outline.create_node`/`update_node` reject cross-user/cross-project parents + reparent self/descendant **cycles** (`_descendant_ids` CTE) — malformed cycle now blocked at source, `archive_node` UNION stays backstop; `scene_link.create` requires both endpoints owned-in-project; `generation_job.create` requires its node owned-in-project. **D-COMP-M2-RESOLVE-SMOKE** → **live** cross-service smoke PASSED (minted test JWT → live knowledge-service `localhost:8216` JWT-forward returned demo book's project; `resolve_work`→`unmarked_single` w/ correct `book_project_id` vs live `loreweave_composition`; cross-user JWT→`[]` = ownership server-side; also live-confirms the HIGH book_id-only fix). **Verify: 49 pass** (44 +5 guard/label locks; forge-cycle test rewired to raw-SQL bypass so UNION backstop still proven). Accepted nuance: reparent validate-then-write not in one txn (TOCTOU; V0 single-user + UNION backstop). No schema/API/migration change.

## ✅ Composition V0 — M3 DONE (clients + prose-source, 2026-06-04, XL, v2.2 + /review-impl)
**PO: FULL client scope + works router in M3.** **5 clients** (all contract-verified vs real book/glossary/knowledge code): `book_client`(JWT-forward prose — `owns_book` SEC2 primitive, `get_draft`, `patch_draft` mandatory-version, `list_revisions`; typed `BookClientError` for proxy status-map); `glossary_client`(X-Internal `select_for_context`+`list_entities`, graceful-degrade); `knowledge_client` EXTENDED(`build_context` internal; `timeline`/`get_entity`/`search_drawers` JWT — `project_id` always sent per A1, drawers `project_id` REQUIRED, `raw_score`/`chapter_index` int|null); `llm_client`(loreweave_llm SDK wrapper + retry loop + `.sdk` stream escape-hatch); `eval_client`(reuses llm — JudgeLLMClient Protocol structural — + `messages[0].content` extractor). **Routers:** `works.py`(GET /books/{id}/work → `resolve_work` wired + GET/PATCH /works If-Match 412) + `prose.py`(GET/PUT proxy, **mandatory `expected_draft_version`** body field→422-if-absent, draft-only, 409 CHAPTER_DRAFT_CONFLICT map). `get_bearer_token` dep; deps factories; main register+client-close; metrics counters.
**Contract facts locked (verify-vs-real-code):** prose draft GET/PATCH are book-service PUBLIC JWT-only → composition FORWARDS JWT (book enforces ownership 404); `expected_draft_version` optional server-side→composition makes MANDATORY (client-supplied from GET, presence-enforced, NOT re-read — corrects §11 wording); `base_revision_id` from `GET …/revisions` items[0]; SEC2 composition-side chokepoint (`owns_book`) applies to INTERNAL glossary/knowledge reads (M4), NOT the JWT-forwarded prose path.
**/review-impl (1 MED fixed + 2 LOW test-adds):** MED — `ProsePutBody.body` `Any`→`dict[str,Any]` (explicit `null` body would forward + risk clobbering the draft to null; TipTap root is always an object). LOW — added router tests for `candidates` serialization + `body_format`/`commit_message` forwarding. Accepted LOW: prose GET `(draft_version, base_revision_id)` from 2 non-atomic book calls (version guard still protects writes; note for M4).
**Verify: 79 pass** (58 unit: respx contract-shapes incl JWT-vs-internal-token assertions, llm retry-loop via fake SDK, eval gotcha, 12 router tests; 16 integ M2; 7 M0/M1) + **LIVE SMOKE** on rebuilt+restarted composition image (:8217): resolve→found; prose GET proxies REAL book draft+base_revision_id (JWT-forward); PUT no-version→422; **PUT stale-v999→409 CHAPTER_DRAFT_CONFLICT** (OI-2 no-blind-clobber LIVE through composition→book); cross-user→404. Throwaway DB+work-row+smoke script cleaned. **Composition image rebuilt** (git-SHA stamped d7c25dc9; contains uncommitted M3 tree at smoke time → re-stamp after this commit if re-deploying).

## ✅ Composition V0 — M4 DONE (RAG packer, 2026-06-04, XL, v2.2 + /review-impl)
**6 packer modules + sanitize** (`app/packer/`): `profile`(BookProfile NEUTRAL default from `composition_work.settings`; `resolve_source_language` auto→fallback); `lenses`(`_safe_*` gatherers — L0 canon[story_order-windowed] / L1a present=glossary bios+knowledge relations / L1b timeline / L2+L2′ structural / L3 recent-prose[chapter-tail approximation, M8→SceneAnchor] / L4 lore); `spoiler`(**two-axis**: L1b in-world `<story_order` + L4 reading-order `<scene sort_order`, **conservative-drop + LOG `l4_dropped_no_position`**, both FAIL-CLOSED on unknown scene position); `budget`(priority ladder, protected tiers never-drop, `over_budget` flag, tiktoken); `sanitize`(§13 SEC3 tag-not-delete + escapes `<block>` delimiters, bounds guide); `assemble`(**A1 `assert_project_scoped`** + §2.4 blocks + sanitize); `pack`(orchestrator: A1→**SEC2 `owns_book` chokepoint**→parallel gather→spoiler→budget→assemble; **C3a `grounding_available`** signal, **DI3** soft-absent skip caching glossary `entity_id`). **+ `routers/grounding.py`** GET preview, **`book_client.get_chapter_sort_orders`**(internal-token, batch≤200, gated by SEC2), main wiring.
**Contract-verified (real code):** `POST /internal/chapters/sort-orders`(X-Internal-Token) for chapter→sort_order; `hit.chapter_index` populated post-CM4 (best-effort, nullable → resolver fallback). sort_order = dense 1-based INT.
**/review-impl (1 MED fixed + 1 LOW test + earlier 502 fix):** MED — `_applies_at` was fail-OPEN on `story_order=None` → a `reveal_gate`'s spoiler text leaked into `<canon>` for unplaced scenes; fixed to FAIL-CLOSED (ungated world rules only, consistent with L1b). LOW — added L4 fallback-resolver test (`chapter_index=None`→`source_id`→`sort_order`→filtered). review-code earlier fixed book-service-down→grounding 502 (was generic 500).
**Verify: 110 pass** (89 unit incl 31 packer: profile NEUTRAL, sanitize tag-not-delete+escape, budget trim-lowest/protected/over_budget, spoiler L1b-future+None-fail-closed / L4-at-after+conservative-no-position+scene-None-fail-closed, pack A1-ValueError/SEC2-OwnershipError/C3a/L4-spoiler+resolver-fallback/DI3-soft-absent/canon-window+None-fail-closed, book_client sort-orders, grounding 404s+happy+ownership-404+book-down-502; 16 integ M2; M0/M1) + **LIVE SMOKE** on rebuilt composition image: GET grounding on a demo Dracula scene 200 — full packer ran (SEC2 owns_book + A1 + all lenses + budget + assemble), blocks=present(glossary)/beat(COMP)/recent(book L3 tail), token_count=1721, **C3a grounding_available=false+warning** (honest no-graph signal), profile NEUTRAL, cross-user→404. Demo rows+smoke+test-DB cleaned.
**M5 isolation: FOLDED** (per plan — every lens project_id+user_id scoped; A1 chokepoint asserts non-null project_id; SEC2 owns_book before internal reads; cross-user→404 throughout). No separate M5 cycle needed.

## ✅ Composition V0 — M6 DONE (co-write engine + critic, 2026-06-04, XL, v2.2 + /review-impl)
`engine/cowrite.py`: `build_messages`(de-bias — language/voice threaded, NO English-only phrases) + `stream_draft`(via `llm.sdk.stream`, relays TokenEvent/ReasoningEvent, **real UsageEvent metering** — absent/zero-OUTPUT frame→char-estimate fallback clamp≥0 never-0, mid-stream output cap→partial). `engine/critic.py` `judge_prose`: 4 dims + per-violation verdicts; **tolerant parse** (`parse_critique_json` fence-strip+balanced; `normalize_critique` score-coerce + filter malformed violations); **CC4** LLMError/non-completed→degrade never-raise; de-bias judge-in-source_language. `routers/engine.py`: `POST /generate`(SSE: pack→budget pre-check[413]→**S2 create-then-cancel-others-if-created**+idempotency→stream→meter→job-update) · `suggest-cast`(glossary select-for-context) · `GET /jobs/{id}` · `/jobs/{id}/critique`(**CC2 fresh list_active** + **critic≠drafter enforced**, skip+warn if equal/unset) · `/jobs/{id}/dismiss-violation`. **PO model-selection:** drafter (model_source,model_ref) in /generate request; critic in `composition_work.settings` (critic_model_source/_ref).
**Contract-verified (real code):** `sdk.stream(StreamRequest, user_id=)`→TokenEvent.delta/UsageEvent.input+output_tokens/DoneEvent; chat `_run_composer` reference; judge mirrors `judge_precision` tolerant parse; provider-registry:8085 container.
**/review-impl (3 MED/LOW fixed + 3 regression-locks):** MED#1 idempotent retry was cancelling the original in-flight stream → create-first, cancel-others-only-if-created. MED#2 `model_source`/`mode` plain str → invalid value caused mid-stream StreamRequest ValidationError (orphan running job) / DB CheckViolation 500 → `Literal` types→422-pre-stream. LOW/MED#3 zero-OUTPUT usage frame (input>0,output=0) metered output=0 despite prose → gate on `out_tok>0`→char-fallback.
**Verify: 141 pass** (118 unit incl 29 M6: critic parse-tolerance/4-dims/CC4-degrade/malformed-filter, cowrite real+empty+zero-frame+input-only+mid-cap+LLMError metering, engine generate-SSE/S2-cancel/idempotency-no-cancel/invalid-enum-422/critique-CC2/critic≠drafter-skip/dismiss/get-job; 16 integ M2; M0/M1) + **LIVE STREAM SMOKE** real LM Studio via gateway (coder drafter, non-thinking): /generate streamed **136 prose tokens** (text_len=623), job completed, output_tokens=137 **measured=True** (real frame); GET job→completed; POST critique→**DISTINCT critic** (qwen3.5-9b≠drafter) judge_prose coherence=5 canon_consistency=5. Demo rows+smoke cleaned.

## ✅ Composition V0 — M7 DONE (contract + gateway + §5 routers, 2026-06-04, XL, v2.2 + /review-impl)
**Additive infra only (lore-enrichment untouched).** `contracts/api/composition/v1/openapi.yaml` — full §5, additive, frozen before M8. **Gateway:** `compositionProxy`(pathFilter `/v1/composition`, **`selfHandleResponse:false` SSE passthrough**, 503-on-down handler mirroring knowledgeProxy) + `compositionUrl` in the configureGatewayApp iface + `compositionProxyFn` + dispatch branch (beside learning, before lore-enrichment); `main.ts` `requireEnv('COMPOSITION_SERVICE_URL')` + log; `infra/docker-compose.yml` `COMPOSITION_SERVICE_URL=http://composition-service:8093` on api-gateway-bff env + `depends_on(service_started)`. **PO: built the missing §5 routers in M7** (closed D-COMP-OUTLINE-CANON-ROUTERS): `repositories/structure_templates.py`(built-in+user list) + `routers/outline.py`(GET tree+links, POST/PATCH[If-Match 412]/DELETE[soft-archive] nodes, POST/DELETE scene-links; ReferenceViolationError→400 reusing M2-closure guards; CheckViolation→400; UniqueViolation→409; SceneLinkCreate.kind=LinkKind Literal) + `routers/canon.py`(CRUD canon-rules If-Match, **CR4** from≤until validate on create + effective-window-on-patch→400, GET /templates). deps `get_structure_templates_repo`; main registers outline+canon.
**/review-impl (1 finding, contract-marked):** `POST /books/{id}/work` (confirm-create) documented but NOT built → marked PLANNED in the contract + tracked **D-COMP-POST-WORK-CREATE** (M8 FE create-Work prerequisite; needs a knowledge ProjectCreate client). Verified clean: requireEnv breaks no other deploy (single compose env); no /internal exposure through the proxy; routers reuse tested M2 guards.
**Verify: 155 pass** (131 unit incl 13 M7 outline/canon/templates router tests + structure-templates integration[6 built-ins, user-scoped]; 17 integ; M0/M1) + **gateway TS nest-build clean** + **LIVE SSE-THROUGH-GATEWAY smoke** (:3123): GET /v1/composition/templates→6 built-ins (routing+JWT passthrough), GET /works/{}/outline→200 (new M7 router), **POST /generate SSE→streamed 107 prose tokens UN-BUFFERED through the gateway** (selfHandleResponse:false PROVEN) + job completed measured=True. Demo rows+smoke cleaned.

## ✅ Composition V0 — M8 DONE (FE Power panel + POST /work, 2026-06-04, XL [FS], v2.2 + /review-impl)
**Backend prereq (closed D-COMP-POST-WORK-CREATE):** `POST /books/{book_id}/work` — `knowledge_client.create_project`(JWT-forward POST /v1/knowledge/projects {name,project_type:book,book_id}) + `book_client.get_book`(title+ownership) + works router **idempotent get-or-create** (resolve existing book project→bind, else ProjectCreate→create composition_work; UniqueViolation→re-get). `CritiqueBody.target_revision_id` made optional (advisory critique before a revision exists).
**FE `frontend/src/features/composition/`** (mirrors features/knowledge MVC): `api.ts`(BASE=/v1/composition apiJson + generateUrl for SSE), `types.ts`. **hooks**(controllers): `useCompositionStream`(SSE via fetch+ReadableStream+AbortController, **FE-local ghost buffer**, unmount-abort, controller-identity finally-guard), `useWork`(resolve/create/scenes/grounding react-query), `useCanonRules`(CRUD If-Match), `useCritique`. **components**(render-only): `CompositionPanel`(Work resolve/create + scene & model pickers + Compose/Grounding/Canon sub-tabs), `ComposeView`(compose-bar + ghost stream + Accept/Regenerate/Discard + inline `CriticFlags`), `GroundingPanel`(blocks + C3a signal), `CanonRulesPanel`(add/list/archive). Drafter picker reuses `aiModelsApi.listUserModels({capability:chat})`. i18n `composition` ns ×4 (en populated; vi/ja/zh-TW fall back→D-COMP-M8-I18N). Mounted new **rightTab='compose'** in `ChapterEditorPage` (Classic untouched). **§13 SC4:** ghost is FE-local React state; only **Accept→`tiptapEditorRef.insertAtCursor`** touches the editor doc (which then autosaves) — the ghost is NEVER autosaved until accept.
**/review-impl (2 MED fixed + regression-locks):** MED#1 `useCompositionStream` finally clobbered a newer stream on rapid re-generate → controller-identity guard. MED#2 POST /work get-then-create non-atomic → catch UniqueViolation→re-get (deeper dup-knowledge-project race→D-COMP-POST-WORK-RACE). 
**Verify: 1031 unit pass** (BE 159 incl 5 POST/work[create/idempotent/bind/conflict] + critique-optional; FE 872 incl 8 composition: SSE ghost-accumulate/error/clear/not-ok/**supersede-race**, ComposeView ghost-not-accepted-while-streaming/accept→onAccept+critique+clear/generate-gated) + **tsc --noEmit clean** + **POST /work LIVE smoke through gateway** (:3123: create→binds existing project, idempotent re-POST, GET→found). SSE generate/grounding/critique live-proven M6/M7. **LIVE-SMOKE FE browser click-through deferred → D-COMP-M8-PLAYWRIGHT** (FE type-checked + unit-tested + all endpoints live-proven; full Playwright needs vite-dev+login+nav setup). Demo work row+smoke cleaned. **Composition rebuilt** (carries POST /work; FE not yet built into the nginx image — runs via vite dev).

## ✅ Composition V0 — M9 DONE (OI-1 publish wiring + chapter-gate, 2026-06-04, L [FS], v2.2 + /review-impl)
OI-1 was ALREADY structural (accept→draft→autosave; publish→`chapter.published`→CM3 extraction, live-proven in D-CANON-CYCLE0-LIVE-SMOKE). M9 added the **affordance gate** + **commit telemetry** on top. **BE:** `GET /works/{pid}/chapters/{cid}/publish-gate`→`{scenes_total,scenes_done,can_publish}` (`can_publish = total>0 AND done==total` — PO: block until ≥1 done; zero-scenes blocked; scoped to books with a real composition_work) via new `OutlineRepo.chapter_scene_gate`; **first production outbox emit** — `patch_node` detects a scene `status`→`done` and emits `composition.scene_committed` **txn-locally** via new `update_node_commit_aware` (threaded optional `conn` through `get_node`/`update_node`; pure `_is_scene_commit` predicate gates the emit). **FE:** `useChapterPublishGate` (composes Work-resolution + gate; gates ONLY when status `found`/`candidates` → Classic-only books keep CM-FE ungated; degrades-open on no-Work/loading/error) → gates the existing CM-FE `PublishControl` via new `blockedReason` prop (Publish disabled+tooltip; Unpublish unaffected); wired in `ChapterEditorPage`; gate i18n ×4.
**PO:** BE gate endpoint · block-until-≥1-done · gate existing PublishControl · scene_committed on status→done.
**/review-impl (HIGH+MED fixed + regression-locks):** **HIGH#1** — the gate was **dead-on-arrival in the UI**: no FE affordance existed to mark a scene `done` (the live smoke PATCHed via curl, masking it) → every composition book got a permanently-disabled Publish. Fixed: `compositionApi.patchNode` + `useSetSceneStatus` mutation + a **Mark done / Reopen** control per selected scene in `CompositionPanel` (✓ markers in the scene picker). **MED#2** — `useSetSceneStatus` must invalidate `['composition','publish-gate',pid]` too (else the Publish affordance stays stale after a commit) → both keys invalidated. **LOW accepted:** create_node(status='done') emits no telemetry (D-COMP-M9-CREATE-DONE-TELEMETRY); useChapterPublishGate resolves Work per chapter-editor open (D-COMP-M9-GATE-RESOLVE-PERLOAD).
**Verify:** 141 BE unit + **26 BE integration** (real PG: atomic emit, no-op/non-scene no-emit, version-conflict rollback, gate counts incl archived drop-out) + **FE green** (gate hook, PublishControl blockedReason, useSetSceneStatus dual-invalidate) + tsc clean + **LIVE SMOKE through gateway** (:3123): gate `0→blocked / 1of2→blocked / 2of2→can_publish=true`; **2 `scene_committed` outbox rows** w/ correct scene+chapter payload; **done→done re-patch added no 3rd row** (`_is_scene_commit` guard live). Smoke rows + throwaway DB cleaned. **Composition image rebuilt** (carries gate + emit; FE runs via vite dev). publish→extraction (CM3) unchanged & already proven.

## 🎉🎉 COMPOSITION V0 COMPLETE — M0·M1·M2·M3·M4·M6·M7·M8·M9 (M5 folded) + Canon Model Cycle 0 ALL DONE.

## ✅ Auto Reasoning Mode — DONE (2026-06-05, XL [FS], v2.2) — design + SDK foundation + integration + live
Research-grounded auto-switch of LLM "thinking" for the co-writer (reasoning ≈ coding: some passages boilerplate, some architecture). Spec: [docs/specs/2026-06-05-auto-reasoning-mode.md](../../specs/2026-06-05-auto-reasoning-mode.md). **Research verdict:** Anthropic extended-thinking = model-internal CoT (agentic only at the tool-loop/interleaved/think-tool/Agent-SDK layer); OpenAI GPT-5 = trained router (code decides); Gemini = budget=-1 dynamic; **Qwen3 local has NO native auto → we must classify in code**. **PO:** build all 3 engines, **auto-select by registered model** + user override; **don't out-think a self-orchestrating model** (Anthropic/Gemini → pass-through; Qwen3 → our rule-based scorer; llm-judge = V1 seam). **Foundation shipped — SDK-first (PO: reusable module belongs in the SDK).** Generic policy in **`loreweave_llm.reasoning`** (reusable by translation/extraction/chat): `infer_reasoning_control` (adaptive/effort/none from provider+name, registry override wins) · `bucket_effort` (monotone score→effort) · `resolve_reasoning` (user-override beats auto; adaptive→passthrough; effort→caller's auto_effort; none→noop) + `ReasoningDirective`. Composition keeps only the **domain scorer** `app/reasoning/policy.score_effort` (creative-writing signals from the packer: operation, n_canon_rules, reveal_gate, present-entities, tension, guidance-markers → SDK `bucket_effort`). **Benchmark** (`tests/unit/test_reasoning_benchmark.py`): 15 labeled co-write scenarios → 100% to intended tiers, non-degenerate distribution (none4/low1/med5/high5) + regression-lock. **Integration DONE:** `routers/engine.py` `GenerateBody.reasoning` (off/auto/low/med/high, default auto) + model_kind/model_name hints → pack → signals (operation + node tension/present-entities + `canon.list_active` count/reveal_gate) → SDK `resolve_reasoning` → thread `directive.effort` (omit on passthrough) into `stream_draft`; `job` SSE frame carries `reasoning_source`/`reasoning_effort`. FE `ComposeView` Off/Auto/Low/Med/High (default Auto) + model hints from `CompositionPanel`'s selected user-model + resolved badge from the job frame; i18n. **Verify:** SDK 8 + composition reasoning 7 + benchmark 1 + engine-router 13 (incl 3 reasoning) + full composition unit 154 + FE 14 + tsc clean + **LIVE SMOKE through gateway**: `/generate` job frame — auto+qwen3.6→`rule_based/medium`, auto+anthropic-hint→`adaptive/passthrough`, off→`user/none`, high→`user/high`; **e2e composition-generate (U3/U4) green with the new control** (drafts prose→critic). Composition image rebuilt. (Work-settings `reasoning_default` persistence + per-locale i18n = small follow-ups; SDK/openapi unchanged.)

## ✅ Chapter Revision Compare (1-vs-1 diff) DONE (2026-06-04, XL [FS], v2.2 + /review-impl) — NOT LOOM-scoped
Standalone diff page comparing two chapter revisions. **BE (book-service, additive):** new `internal/textdiff/` LCS line-diff pkg (perf-guard + **common prefix/suffix trim** so large-chapter/small-edit never degrades) + `GET /v1/books/{id}/chapters/{cid}/revisions/compare?left=&right=` (`compareRevisions`, JWT + ownership join reusing getRevision SQL; `text_content` via `#>> '{}'` = quote-free; route before `/{revision_id}` — chi static-beats-param) → `{left,right,diff,truncated}`. **FE (React-MVC, route `/books/:bookId/chapters/:chapterId/compare`):** `compareRevisions` api + types; `useRevisionCompare` (controller, defaults to 2 newest, no useEffect); `RevisionDiff` (inline git-style + side-by-side word-level); `wordDiff`+`alignSideBySide` lib (**CJK per-char tokenize**); `RevisionCompareView` (pickers + toggle, Picker hoisted out of render); `ChapterComparePage`; "Compare" entry from `RevisionHistory` (≥2 revs); i18n ×4.
**PO:** two-revisions · standalone route · BE endpoint+Go diff · side-by-side(word)+inline toggle.
**/review-impl (2 MED fixed + 2 LOW documented; 0 HIGH):** verified real revision bodies carry `_text` on every block (feature alive for real chapters, not a dead-gate). **MED#1** perf-guard degraded large-chapter-small-edit to full-replace → **prefix/suffix trim** before DP. **MED#2** wordDiff `\S+` useless for CJK (platform's primary corpus) → **CJK per-char tokenize**. LOW: picker capped at 100 revs (D-COMPARE-PICKER-PAGINATION); full `body` shipped unused by FE (accept).
**Verify:** Go 9 compare tests (textdiff incl trim+CJK-adjacent + handler validation + routing) + full book-service suite + vet clean; FE 11 tests (wordDiff/align/CJK, RevisionDiff inline+sxs, useRevisionCompare) + books suite + tsc clean; **LIVE SMOKE through gateway** (throwaway book+chapter, 2 revisions → A-vs-B diff correct, same-rev all-equal, bogus-rev 404, missing-param 400 COMPARE_BAD_PARAM proving routing≠getRevision, quote-free text). book-service image rebuilt.

## ✅ Composition V1 — correction-flywheel SLICE 1 DONE (LOOM-14, 2026-06-05, L, v2.2 + /review-impl)
BE capture + relay wiring (spec [§7.1](../../specs/2026-06-05-composition-controlled-auto-correction-flywheel.md)). **composition-service:** `generation_correction` table (idempotent DDL; in-DB FK on `job_id`; CHECK `pick_different ⇒ chosen_candidate_index`) + `GenerationCorrection` model; `repositories/generation_corrections.py` — `create` inserts **+ emits `composition.generation_corrected` in ONE txn** (mirrors M9 `update_node_commit_aware`), `count_changed_blocks` pure helper, `list_for_job`; `POST /jobs/{id}/correction` (kinds `edit/pick_different/regenerate/reject` — **NO `accept`, H2**) + per-work `capture_correction_prose` §5 opt-in (raw prose gated; structural always); deps factory. **H1 FIX (D-COMP-OUTBOX-NOT-RELAYED CLEARED):** added `composition:postgres://…loreweave_composition` to worker-infra `OUTBOX_SOURCES` — zero relay code change (CM2's generic relay; `outbox_events` schema already compatible).
**/review-impl (0 HIGH, 3 MED fixed + regression-locks):** MED#1 oversized `guidance` committed row+event then 500 on read-back (capped row model) → request-field cap → 422 pre-write; MED#2 `regenerated_to_job_id` skipped the ownership check the primary `job_id` gets → validated in-txn (D-COMP-M2-XREF pattern); MED#3 zero-change `edit` (edited==winner) = disguised accept → H2 self-reinforcement leak → 422 `EDIT_NO_CHANGE`. **LOW#4 resolved (§8.2):** event payload carries `winner_index`+`chosen_candidate_index`+`candidate_count` so slice-2 learning reconstructs `j ≻ i` WITHOUT reading composition's DB (prose stays redacted §5).
**Verify: 191 composition unit (+19) + 36 integration (+7, real PG)** (atomic insert+emit, emit-failure rollback, foreign-job + foreign-chain reject, CHECK constraint, winner/k payload, opt-in raw, list). **★ LIVE-SMOKE (the load-bearing proof):** injected a `composition.generation_corrected` outbox row → **reached `loreweave:events:composition`** with `outbox_id` on the wire + `published_at` set + durable `event_log` copy; **the H1 fix also drained the backlogged M9 `scene_committed` events** (emitted-but-unrelayed for sessions — stream was `XLEN 0` before). Synthetic row cleaned. **Composition + worker-infra images rebuilt + redeployed:** composition health ok, `generation_correction` table LIVE; worker-infra Go suite green.
**Also resolved in-session (PO-directed):** **D-OUTBOX-CLEANUP-ENCODE** (NEW→CLEARED) — worker-infra `outbox-cleanup` bound `retain_days=30` as TEXT via `($1||' days')::interval`, pgx v5 refused the int→text encode → cleanup errored every run on ALL sources + **never purged a row since Phase B**. Fixed to `make_interval(days => $1)`; live-proven (purged 344 stuck book rows + translation/chat on first clean run).

## ✅ Composition V1 — correction-flywheel SLICE 2 DONE (LOOM-15, 2026-06-05, L, v2.2 + /review-impl)
learning consumes the co-write human-gate (spec [§7.2](../../specs/2026-06-05-composition-controlled-auto-correction-flywheel.md)). **learning-service:** `loreweave:events:composition` → consumer `STREAMS`; `handle_generation_corrected` → `corrections` row (`target_type='generation'`, `op=kind`, `origin_service='composition'`, dedup on `outbox_id`) via the shared `_persist_correction`; `_composition_snapshots` encodes the H2-safe preference (`before={role:winner,index:i}` / `after={role:chosen,index:j,candidate_count:k}` for pick_different; `changed_blocks`+flags for edit; `regenerated_to_job_id` for regenerate; `after=None` for reject). **Only edit/pick_different/regenerate/reject persist — `accept`/unknown are ACKed without a row (H2).** Registered in `build_dispatcher`. **composition (slice-1 amend):** `user_id`+`book_id` added to the `generation_corrected` payload (the store is owner-scoped, `user_id NOT NULL`; slice 1 had omitted it).
**/review-impl (1 HIGH fixed + 2 LOW):** **HIGH#1** — `mining.get_outcome_recompute` joined `corrections` into extraction-outcome WITHOUT a `target_type` filter; composition `generation` corrections share the book's knowledge `project_id` + `source_extraction_run_id IS NULL` → would be miscounted as extraction corrections, falsely degrading clean extraction runs (a live Q2 metric, corrupted the moment someone co-writes in an extracted book). Fixed (`AND c.target_type IN ('entity','relation','event')`), live-validated, regression-locked. LOW: `get_gold_labels` default now returns generation rows mixed (extraction consumers must pass `target_type`); gold's generic `change_magnitude` is noise for generation rows (slice 5 reads `changed_blocks`).
**Verify: 191 composition unit + 36 integration + 117 learning** green. **★ BOTH-DB-HALVES LIVE-SMOKE:** injected `generation_corrected` into live `loreweave_composition` → relay → `loreweave:events:composition` → learning consumer → **`loreweave_learning.corrections` row** (`op=pick_different`, `before={role:winner,index:0}`/`after={role:chosen,index:2}` = the `j≻i` preference, gold-projection reads it); **dedup proven** (same `outbox_id` re-delivered → still 1 row). composition+learning redeployed. Synthetic data cleaned.

## ✅ D-CM3B-DEAD-REVISION-LOOP fixed (LOOM-15, found during slice-2 smoke) — worker-ai runaway drain
**Symptom (caught reading worker-infra relay logs during the smoke):** `outbox-relay relayed source=knowledge count=12` every 30s — NOT a relay bug; worker-ai was creating a fresh `chapters_pending` drain job every poll for **2 chapters whose pinned revision 404s** (deleted), each emitting a `skipped` extraction_run → **2937 junk runs** in `learning.extraction_runs` (99% of the table) + stream/outbox flood. **Root cause:** the skip-on-text-unavailable path ([runner.py](../../../services/worker-ai/app/runner.py)) never marked the `extraction_pending` row processed → it re-armed forever. **Fix:** (1) `clients.get_chapter_revision_text` now returns `None` ONLY on a permanent **404** and **RAISES on transient** (5xx/network) so a blip retries instead of dropping canon; (2) the drain marks the pending row processed (revision-guarded) on a permanent-gone `None`. **Tests:** 4 client (404→None / 5xx→raise / network→raise / 200→text) + 1 runner (dead-revision → marks pending) ; **109 worker-ai green**. **Live-proven:** redeployed worker-ai → the 2 orphaned pendings self-drained, **0 new run events** (loop dead); 2937 junk runs purged. (The 10 remaining unprocessed pendings are the separate dormant "no-prior-job bootstrap" gap, not looping.)

## ✅ Composition V1 — correction-flywheel SLICE 3 DONE (LOOM-16, 2026-06-06, L [FS], v2.2 + /review-impl)
The FE controlled-auto human gate (spec [§4/§7.3](../../specs/2026-06-05-composition-controlled-auto-correction-flywheel.md)). **PO decisions:** mode TOGGLE (keep both — V0 live stream OR the K-cards gate); add the K candidate texts to the auto response (vs a GET round-trip). **composition (BE):** `candidates` (the K texts) added to the auto `/generate` JSON — fresh + replay paths (already computed/persisted; saves a GET). **FE `features/composition/`:** `api.ts` `generateAuto`(POST mode=auto)+`submitCorrection`(POST /jobs/{id}/correction); `types.ts` `AutoGeneration`+`CorrectionBody`; `hooks/useAutoGenerate.ts` `useAutoGenerate`+`useCorrection`; `ComposeView` **Diverge (K options)** toggle branching stream-vs-cards; `CandidatesView`+`CandidateCard` — all K shown, winner badged, per-card Accept/Use-this/Edit-then-accept + Regenerate-with-guidance + Reject all. **Correction mapping (H2-correct):** accept WINNER as-is → insert, NO correction; use a NON-winner → `pick_different`; edit-then-accept → `edit`; regenerate → `regenerate`+re-run; reject → `reject`. Capture is **fire-and-forget** (never blocks insert; the mutation lives in ComposeView so it survives the card unmount); cards are **FE-local until accept** (§13 SC4). i18n 13 keys ×4 (parity ✓).
**/review-impl (0 HIGH):** LOW#1 closed — regression-lock for the regenerate handler (captures on the OLD job_id BEFORE re-mutate). Accepted: LOW#2 non-winner-edit records `edit` only (§8.4 open — final text is the richest drafter signal); LOW#3 no cancel during the ~40s auto generate; COSMETIC#4 dead `busy` flag.
**Verify: 16 composition unit + 25 FE + tsc clean + i18n parity.** **★ FULL controlled-auto loop live-proven end-to-end** (real qwen3.6-35b): auto-generate K=3 → `candidates` on the wire → FE `pick_different` POST 201 → relay → `loreweave_learning.corrections` (`pick_different`, winner_i=1, chosen_j=0, k=3). **+ Playwright browser click-through GREEN (44.5s, model-gated, realizes D-COMP-SLICE3-PLAYWRIGHT):** Diverge toggle → real K-generate → K cards + 1 winner badge → use a non-winner → gate resolves → **DB-asserts the pick_different correction reached learning** (browser→relay→store). Smoke + test rows cleaned. composition rebuilt+redeployed.

## ✅ Composition V1 — correction-flywheel SLICE 5 DONE (LOOM-17, 2026-06-06, L [FS], v2.2 + /review-impl) — FLYWHEEL COMPLETE
The eval-gate (spec [§6](../../specs/2026-06-05-composition-controlled-auto-correction-flywheel.md)). **PO:** composition-local (it owns BOTH `generation_job` + `generation_correction` → no cross-DB, and an auto-vs-cowrite split within one Work IS the within-author A/B) + a FE dashboard. **composition (BE):** `ModeCorrectionStats`/`CorrectionStats` models; `GenerationCorrectionsRepo.correction_stats` (per-mode rates over `generation_job` × `generation_correction`); `GET /works/{id}/correction-stats`. **Rates:** denominator = COMPLETED generations; `accept_rate = (gen − corrected_jobs)/gen` is **derived not mined** (H2-safe); `avg_edit_magnitude` reads `changed_blocks`; both modes always present (zero-filled); rates null at cold-start. **FE:** `api.getCorrectionStats` + `useCorrectionStats` + `QualityPanel` (Quality sub-tab — auto-vs-cowrite table: accept↑/edit/pick/regen/reject↓ + avg edit size + cold-start hint); **cowrite capture wired** — V0 stream regenerate→`regenerate`, discard→`reject` (accept-as-is still emits NO correction, H2) so the A/B has a real cowrite baseline. i18n 16 keys ×4 (parity). `eval_a1_diverge.py` reframed: correction-rate is the gate, the saturating auto-judge is the documented cold-start proxy.
**/review-impl (0 HIGH, 2 MED fixed + regression-locks):** MED#1 per-kind rates counted correction ROWS → a rate could exceed 1.0 → `count(DISTINCT job)`. MED#2 correction counts didn't gate `status='completed'` → a reject on a RUNNING job (API-creatable) could make `accept_rate` negative → every correction aggregate now FILTERed to completed jobs (numerator ⊆ denominator). LOW accepted: A/B temporal confound (cowrite-early vs auto-late — author-style cancels, skill-drift doesn't; §6).
**Verify: 193 composition unit (+6) + 31 FE (+5) + tsc + i18n parity.** **★ Live-smoke through gateway:** seeded 2 auto + 1 cowrite completed jobs + edit + reject → `GET correction-stats` returned the exact A/B (auto edit_rate=0.5 mag=3.0 / cowrite reject_rate=1.0). Seed cleaned. composition rebuilt + redeployed + re-stamped FRESH.

## 🎉🎉🎉 V1 CONTROLLED-AUTO + CORRECTION FLYWHEEL COMPLETE — slices 1 (capture+relay) · 2 (learn) · 3 (FE gate) · 5 (eval) all SHIPPED + live (4 = gateway no-op). The full loop runs end-to-end: diverge→converge → human gate (accept/edit/pick/regenerate/reject) → capture → relay → learning corrections store → per-Work correction-rate dashboard.

## A2 — check→revise-vs-KG, SCOPED into S1–S4 (PO, 2026-06-06)
A2 (Phase-A plan [`2026-06-05-composition-v1-phase-a.md`](../../plans/2026-06-05-composition-v1-phase-a.md); reasoning spec [`…v1-reasoning-engine.md`](../../specs/2026-06-05-composition-v1-reasoning-engine.md) §5) = after diverge→converge picks a winner, **check the draft vs the KG + canon and revise**. **PO chose the FULL §5.1** (SCORE symbolic guard + LLM-judge) **+ auto AND co-write gate** → XL, cross-service. **The load-bearing blocker: the KG has NO entity-status model** (`:Entity` has no status; the SCORE guard needs "is E `gone` at position P?"). So A2 is sliced, **status-model first**:
- **A2-S1 — KG entity-status model** (knowledge-service + extraction SDK + worker-ai + backfill) ← **NEXT (design locked)**
- A2-S2 — knowledge-service `fact-for-check` read (entities + relations + timeline + **status** by id set, project-scoped)
- A2-S3 — composition `canon_check` (symbolic guard incl. status + LLM-judge) + `reflect(check→revise ≤N)` + auto-path wiring + eval-gate
- A2-S4 — co-write FE gate (revise affordance)

## ✅ A2-S1 DESIGN LOCKED + /review-impl-hardened (LOOM-18, 2026-06-06) — design checkpoint, NO CODE
Doc: [`docs/specs/2026-06-06-kg-entity-status-model.md`](../../specs/2026-06-06-kg-entity-status-model.md). **PO decisions:** population = **extraction-time `status_effects:[{entity_ref,status}]` on `LLMEventCandidate`** (SDK prompt + worker-ai passthrough + knowledge persist); vocab = **coarse `active`/`gone`** (V1); **+ a one-time backfill** over existing events. Status stored as an **evidence-backed, `event_order`-keyed record** reusing the `:Fact`/evidence/retract infra. **/review-impl folded 4 MED + 2 LOW:** (M1) order-validity is NET-NEW not free — design the `valid_from_order` model + "latest ≤ P" query + retract interplay explicitly; (M2) legacy/chat events have `event_order=None` → the backfill's blind spot → gate status to non-null order / skip+log; (M3) coarse active/gone **false-positives on revivals** (`gone→active` is legit) → symbolic flags are **ADVISORY (LLM-judge confirms), not a hard gate** — refines D4; (M4) the Pass-2 prompt change is **eval-gated vs the 0.869 extraction baseline** (blocking); (L5) exclude status-Facts from RAG fact selectors; (L6) retract status by the event's `source_id`.

## ✅ A2-S1a DONE (LOOM-19, 2026-06-06, L, v2.2) — the `:EntityStatus` storage primitive (eval-safe foundation)
**Build-time finding:** `:Fact` does NOT fit status (closed `FACT_TYPES`, **content-keyed idempotency** → would collapse per-entity transitions, no order axis, no entity link — confirmed against `facts.py`) → status got its **own `:EntityStatus` node** (a correction to the frozen design's Fact-reuse). **Shipped (knowledge-service only, NO prompt change → no eval risk):** `app/db/neo4j_repos/entity_status.py` — `:EntityStatus` = one `(entity_id, status, from_order)` transition; `merge_entity_status` (idempotent, deterministic id), `status_at_order` (latest EVIDENCED ≤ P per entity, **default `active`**, OPTIONAL-MATCH+collect so no entity drops — memory: CALL-subquery drops outer row), `delete_…_with_zero_evidence` (retract cleanup). Coarse `active/gone`; `from_order` on the `event_order`/`EVENT_ORDER_CHAPTER_STRIDE` scale. Neo4j schema: `entity_status_id_unique` constraint + 3 indexes (+ test EXPECTED sets). **Verify: 4 pure id + 12 LIVE-Neo4j integ + 25 schema-unit green** — live against `infra-neo4j-1` :7688: merge/idempotent, the **revival case** (gone@N→active@M: status-at-P = active<N / gone in [N,M) / active≥M), un-evidenced ignored, retract cleanup + cross-user isolation, schema applied+verified. **2-stage review** (design was /review-impl-hardened up front): M2 gate lives at the repo (`from_order:int` rejects `event_order=None`) → **A2-S1b persist must skip+log such events, not crash**.

## ▶ NEXT SESSION — A2-S1b (persist plumbing → then eval-gated prompt + backfill)
**Start here:** A2-S1a (`:EntityStatus` primitive) is SHIPPED + live. **A2-S1b splits eval-safe-first:**
- **A2-S1b-1 (eval-SAFE plumbing, no prompt change):** (1) `loreweave_extraction` SDK — add the `status_effects:[{entity_ref,status}]` FIELD to the event candidate (schema only, NO prompt change yet); (2) worker-ai — passthrough in the persist-pass2 body; (3) knowledge `pass2_writer` — consume `status_effects` → `merge_entity_status` + `add_evidence` **inside the per-chapter retract-then-write Tx** (status retracts by the event's `source_id`); **skip+LOG events with `event_order=None`** (M2). Proven with a SYNTHETIC `status_effects` in the persist body — no eval risk. + confirm `:EntityStatus` doesn't leak into the RAG/context selectors (it's its own label, not a Fact — verify the context builder).
- **A2-S1b-2 (eval-GATED population):** add `status_effects` to the Pass-2 PROMPT (coarse active/gone, multilingual-safe — prompt-bias lessons) + the one-time backfill job (non-null `event_order` only, idempotent, project+user scoped). **★ BLOCKING eval-gate:** re-run the knowledge extraction eval (entity/relation/event F1) vs the **0.869 baseline** — ship the prompt only if F1 holds.
**`/amaw` recommended** (cross-service + extraction path). Then A2-S2 (fact-for-check read incl. status) → A2-S3 (composition symbolic guard + LLM-judge + reflect/revise + eval-gate) → A2-S4 (co-write FE gate).
**Deploy note:** composition is FRESH; learning/worker-ai/worker-infra carry this arc's code at stale SHA labels (build-before-commit) — re-stamp via `build-stack.sh` only if a future session needs the freshness guard fully green.

<details><summary>(V1 design + A1 detail — read for context)</summary>

**V1 orchestrated-reasoning DESIGN (locked + research-grounded + /review-impl-hardened):** [`docs/specs/2026-06-05-composition-v1-reasoning-engine.md`](../../specs/2026-06-05-composition-v1-reasoning-engine.md). Deepens the existing §8.3 autonomous-loop into a real reasoning architecture: **7 atomic primitives + 5 combinators** (§3) · **craft-method recipe library** as data on the structure_template pattern (§4) · **constraint engine grounded in the external KG + canon_rules + a `narrative_thread` ledger** (§5/§10.2 — the LoreWeave extension beyond Re3/DOC/Dramatron). Empirically backed (deep-research: Re3/DOC/Dramatron beat free-running; FactTrack/SCORE/ConStory canon-check; CFPG foreshadow; DOME memory). **PO decisions D1–D6 resolved (§9):** validate-first eval-gated library · adaptive diverge-K from structural weight · HARD gates = canon-fact+spoiler only (rest advisory) · LLM-judge+SCORE canon-check (no NLI dep) · static per-Work recipe selection. **/review-impl caught 3 HIGH** (split-softmax inapplicable on black-box API → prompt-reinjection only; per-scene check cost → symbolic-primary + judge-on-winner; YAGNI → §8 Phase-A/B validate-first) + 6 MED, all folded.

**▶ NEXT = A1 EVAL-GATE REDESIGN, then A2.** Phase A plan: [`docs/plans/2026-06-05-composition-v1-phase-a.md`](../../plans/2026-06-05-composition-v1-phase-a.md) (A1 diverge→converge ✓ built + committed; A2 check→revise-vs-KG; A3 decompose+adaptive-K).

**NEW DIRECTION (PO, 2026-06-05) — controlled-auto + correction flywheel.** Design: [`docs/specs/2026-06-05-composition-controlled-auto-correction-flywheel.md`](../../specs/2026-06-05-composition-controlled-auto-correction-flywheel.md). Early V1 = **controlled auto, not autonomous**: `diverge→converge` → **human gate** (author accepts / edits / picks-different-candidate / regenerates-with-guidance / rejects) → **capture the correction** → emit `composition.generation_corrected` → learning-service preference store (reuse the eval-track Q2/Q3 corrections flywheel). The human gate guarantees quality + collects the signal to tune the drafter + the A1 reranker (`pick-different` directly corrects the reranker). **Solves the A1 eval-gate problem:** correction-rate metrics (edit/regenerate/pick-different rate) are the discriminating quality ground-truth the saturating auto-judge lacked. **PO decisions:** raw-prose OPT-IN (mirror `save_raw_extraction`); FE shows ALL K candidates in parallel (takes-style); build the FULL loop (BE capture + learning consume + FE surface). Build slices in spec §7. **▶ this supersedes the bare A2/A3 sequencing — controlled-auto first, autonomous §8.3 later (corrections bridge).**

**A1 DONE (built + committed) + eval-gate run (honest finding):** `engine/select.py` (diverge K parallel + LLM rerank converge; 15 unit + 3 router-auto tests; 172 composition unit green) + `engine.py` `mode=auto` branch (non-stream winner JSON; rerank judge prefers the distinct critic). **Live-smoke ✓** (K=3, 43s/scene; rerank correctly rejected 2 mid-sentence-truncated candidates — real coherence selection). **Eval-gate (`scripts/eval_a1_diverge.py`, 4 scenes): AUTO median coherence 5 = COWRITE median 5 → no regression, BUT the metric SATURATES.** Honest validate-first finding: coherence-median on 400-token single passages **can't discriminate** — rerank's benefit is tail-risk (avoiding the occasional truncated/bad draft), not median lift, and the critic's coherence dim doesn't penalize truncation. **The eval-gate metric is WRONG for A1** (Re3/DOC measured on 2000–3000-word stories where incoherence accumulates). **Before trusting any A-slice gate, redesign the metric:** (a) longer multi-scene generation, (b) a completeness/truncation-aware rubric, or (c) worst-case/variance not median. **NOTE:** A2's canon-check likely has a *better* gate signal (inject a canon rule → measure violation RATE, which is countable + discriminating, vs ceiling-coherence). **A1 mechanism is sound + ships; the gate needs the metric fix.**

Schema delta (§10.3, additive, NOT yet built): `craft_recipe` + `narrative_thread` tables + `generation_run.state` JSONB.

</details>

**V0 is COMPLETE + regression-netted.** **Coverage map (V0-SCENARIO-TESTS.md):** U1–U9 · B1.1–B1.5 · B3.1/B3.3 · B4.1/B4.2/B4.4 · B6.1/B6.3/B6.4 · B7.1/B7.2/B7.4/B7.5 · B8.1–B8.10 · B9.1 — all automated (incl. **U8 flywheel** + **B8.6 truncated** added Batch 8). **Genuinely unit-only (justified, NOT convenience):** **B6.2 spoiler** — the cutoff is *pure algorithm* with 31 packer unit tests + fail-closed cases; an e2e needs 2 slow extractions (~10 min) and asserts entity-presence that depends on which entities the LLM happens to extract + Neo4j-sync timing → adds fragility, not confidence. **B5.1/B5.2/B5.3 critic** — distinct-enforcement is pure backend logic (engine.py:272, unit-tested); degrade needs a forced provider-down (kills the drafter too) / malformed JSON (un-forceable externally); dismiss needs the LLM to *produce* a violation (nondeterministic). **B4.3 supersede** — **UI-unreachable**: while streaming the button is **Stop**, not Generate, so two overlapping streams can't be triggered from the UI; the race is hook-level → 29 M6 unit tests are the right place. **B1.6 pre-CM degrade** — schema-impossible (every current-stack book has `editorial_status`; the render-null path can't be produced) → unit-covered.

**Ops:** stack up (`docker compose up -d`); **if worker-ai started before redis it leaves its poller degraded → `docker restart infra-worker-ai-1`**. vite dev :5174 (`cd frontend && npm run dev`). Test accts: `claude-test@` (A) + `claude-test2@` (B, auto-created by `ensureUserB`); drafter `qwen/qwen3.6-35b-a3b`, critic `qwen/qwen3.5-9b`. Run `cd frontend && npx playwright test`. DB-assert specs need the `infra-postgres-1` container (skip otherwise). Reset Bash cwd to repo root after any `cd` (guardrail hook).

**DONE (live-green):** `composition-gate` (U1/U2/B3.1/B7) · `composition-generate` (U3/U4 — drafter qwen3.6-35b reasoning-off + distinct critic qwen3.5-9b) · `revision-compare` ×4 (B8.1/B8.4-CJK/B8.2/B8.8) · **`composition-publish-lifecycle` ×6 (B1.1-B1.5, B7.2) — Batch 1** · **`composition-grounding-canon` ×2 (U5/B6.1, U6) + B6.4 live one-shot — Batch 2** · **`composition-engine` ×3 (B4.1 gated-generate model-free, B4.4 discard+reasoning-badge, B4.2 stop-mid-stream — both model-gated) — Batch 3, 2026-06-05.** (B4.3 supersede + B5.* critic skip/degrade/dismiss stay unit-only — 29 M6 tests; not deterministic in-browser.) · **`composition-journey` ×1 (U1→U7 continuous; model-gated; self-validating via empty-publish guard) — Batch 4** · **`composition-telemetry` ×1 (B3.3 scene_committed — DB-assert) — Batch 5a** · **grounding-empty actionable hint (UX fix) — Batch 5b** · **`composition-isolation` ×1 (B9.1/B2.4/B6.3/B8.10 cross-user A1/SEC2 — API-boundary, `ensureUserB`) — Batch 6** · **`revision-compare-remainder` ×3 (B8.5 trim-not-full-replace, B8.7 pagination >100 revs, B8.9 bogus/cross-book id→4xx) — Batch 7, 2026-06-05.** (B8.3 word-level covered by B8.1.) · **Batch 8 (2026-06-05): `composition-flywheel` ×1 (U8 — auto-drain DECISION at DB layer, db-assert ~39s deterministic; the slow real-extraction version flaked under suite load → redesigned load-insensitive) + `revision-compare-remainder` +B8.6 (2100-line revs → 4M-cell perf-guard truncated, 1.7s). Deferred-item analysis: 2 automated, 5 justified-unit-only.**

**⚠ FINDING (Batch 5, diagnosed live) — composition→canon flywheel needs a manual extraction bootstrap.** Tracing why U8 grounding never flips available on a fresh book: knowledge-service DOES arm `extraction_pending` on `chapter.published`, but **worker-ai `_ensure_chapters_pending_jobs` SKIPS projects with no prior extraction job** (runner.py:1765 `if last is None: continue`) — the auto-drain reuses the project's last job's `llm_model`, and a never-extracted project has none (BY DESIGN — a placeholder model would break extraction; multi-user BYOK can't hardcode one). So a user who only co-writes + publishes (never ran a manual knowledge extraction) silently gets NO canon. **NOT a streaming hang** (no job ever starts; pendings pile unprocessed). **PO decision: surface it in UI, let the user self-bootstrap** (don't auto-resolve a model server-side). **Fix shipped (Batch 5b):** `GroundingPanel` now shows an actionable `groundingEmptyHint` (×4 i18n) when `grounding_available=false` instead of the raw C3a warning-ese — "run a knowledge extraction once; publishing then auto-updates canon". Deterministic e2e asserts it (U5/B6.1). **Also:** worker-ai had a redis-timeout at the session's stack-up (started before redis ready) that left its poller degraded — `docker restart infra-worker-ai-1` cleared it (transient infra, not code; related to D-REDIS8-CONSUMERS).
- **D-COMP-CANON-BOOTSTRAP-HINT (NEW):** the hint is text-only; a future affordance could deep-link to / trigger the knowledge extraction from the composition panel.
- **U8 flywheel — AUTOMATED, fast + deterministic (Batch 8, `composition-flywheel` ×1, db-assert ~39 s, NO models).** First built as a real-extraction test (~4.4 min) — it **flaked in the full suite** (LM-Studio contention pushed the ch2 auto-drain past its 180 s window). Redesigned to lock the actual fix — the auto-drain DECISION — at the DB layer, load-insensitive (pure SQL poll, no LLM): publish on a fresh project → assert NO `chapters_pending` drain job after ≥4 worker polls (the gap/skip); then `seedPriorExtractionJob` (gives the project an extraction history) → assert a `chapters_pending` job IS created (the flywheel engages). Exercises the real book→knowledge→worker-ai pipeline. **Precondition chain discovered while building the slow version (kept for reference):** (1) worker-ai skip-no-prior-job (the fix); (2) `benchmark_missing` — `extraction/start` needs a passing `project_embedding_benchmark_runs` row per (project, embedding_model); (3) extraction ~150 s/job; (4) grounding_available depends on scene-position vs canon window (a scene in ch1 correctly shows no canon — spoiler cutoff, not a bug). **B6.2 spoiler stays unit-only** (coverage-map rationale).

**E2E sweep — Batch 1 DONE (2026-06-05):** publish-lifecycle spec (6/6 live) + **2 PO-approved fixes surfaced by the e2e**: (1) `save()` now matches the structured `CHAPTER_DRAFT_CONFLICT`/409 like the publish path (was brittle message-substring); (2) **empty-publish guard** — book-service rejects publishing a chapter with no `_text` prose → **422 `CHAPTER_EMPTY_PUBLISH`** (canon never includes empty prose / extraction never runs on nothing); FE maps it to a localized `empty_toast` ×4. Added testids `chapter-title-input`/`chapter-save-button` + `data-status` on `editorial-badge`. book-service image rebuilt; live-smoke empty→422 / content→200. Known edge: alt-less image-only chapter blocked too (V0 prose-first; commented).

**Deferred carry:** D-COMP-COWRITE-EDIT-CAPTURE (NEW low — the V0 cowrite stream ghost isn't inline-editable, so an "edit" isn't captured for cowrite [post-accept editor edits blend with normal writing, §8.4]; only regenerate/reject are captured for cowrite, so the auto-vs-cowrite A/B compares those + accept-rate, not edit-rate) · D-COMP-EVALGATE-TEMPORAL-CONFOUND (NEW low — the within-Work auto-vs-cowrite A/B cancels author style but not temporal/skill drift if the author used the modes in different periods; §6) · D-COMP-SLICE3-AUTO-CANCEL (NEW low — no cancel during the ~40s auto-mode generate; the button shows disabled "Generating…"; abort not wired for the non-streaming POST) · D-COMP-SLICE3-NONWINNER-EDIT (NEW low — editing a non-winner card records `edit` only, losing the "started from candidate C" nuance; §8.4 open — the edited final text is the richest drafter signal) · D-LEARN-GOLD-TARGET-TYPE-FILTER (NEW low — `get_gold_labels` default returns `generation` rows mixed with extraction gold; extraction-gold consumers must pass `target_type`; the param exists) · D-LEARN-GEN-CHANGE-MAGNITUDE (NEW low — gold's generic `change_magnitude` is noise for `generation` rows; slice 5 reads `changed_blocks` from `after_structural`) · D-COMP-CANON-BOOTSTRAP-HINT (NEW low — grounding-empty hint is text-only; a future affordance could deep-link / trigger knowledge-extraction from the composition panel) · D-AUTO-REASONING-SCENE-SIGNALS (NEW low — canon signals are project-level not scene-windowed; conservative over-count) · D-AUTO-REASONING-HINT-FALLBACK (NEW low — auto + absent model hint → treated non-reasoning/no-op; FE always sends the hint so only a hand-crafted API call degrades) · D-COMP-REASONING-WORK-DEFAULT (NEW low — persist reasoning_default per Work) · D-COMP-M9-CREATE-DONE-TELEMETRY · D-COMP-M9-GATE-RESOLVE-PERLOAD · D-COMP-POST-WORK-RACE · D-COMP-M8-CANON-EDIT · D-COMP-M6-STREAM-SPEND-GUARDRAIL · D-COMP-M6-CRITIQUE-PRESENT-FACTS · D-COMP-M3-PROSE-GET-NONATOMIC · D-COMP-M4-L4-SAME-CHAPTER · D-CM4-RERANK-PERF · D-CM4-NEO4J-ORDERING-SMOKE · D-CM3B-* accepted races · D-CM3C-PASSAGE-INLINE-BULK · D-EXTRACTION-JOBS-TEST-ROT · relation-filter-model config.
**Recently cleared:** **D-COMP-SLICE3-PLAYWRIGHT** (LOOM-16 — `composition-correction-gate.spec.ts` model-gated browser click-through: Diverge → K cards → use-non-winner → gate resolves + DB-asserts the pick_different correction in learning; green 44.5s). · **D-CM3B-DEAD-REVISION-LOOP** (LOOM-15 — worker-ai re-armed a drain job every poll for chapters whose pinned revision 404s; client now 404→None/transient→raise + drain marks pending on permanent-gone; live-proven loop dead, 2937 junk runs purged). · **D-COMP-OUTBOX-NOT-RELAYED** (LOOM-14 slice 1 — composition added to `OUTBOX_SOURCES`; `generation_corrected` + backlogged `scene_committed` now relay to `loreweave:events:composition`, live-proven). · **D-OUTBOX-CLEANUP-ENCODE** (LOOM-14 — `make_interval(days=>$1)` fix; cleanup had errored every run since Phase B, purged 344+ stuck rows live). · **V0 E2E SWEEP (2026-06-05)** — 23 Playwright specs live-green (full suite 1.5 min) covering U1–U7/U9, B1.*, B3.1/B3.3, B4.1/B4.2/B4.4, B6.1/B6.3/B6.4, B7.*, B8.*, B9.1; **realizes D-COMP-M8-PLAYWRIGHT**; surfaced+fixed 3 prod issues (empty-publish guard 422, save() structured-conflict-code, composition→canon bootstrap UX hint). 7 batches d55bd880→ab542b89. · D-COMP-M8-I18N (**RESOLVED** — composition ns translated to vi/ja/zh-TW; i18n-parity OK 20 ns × 3 locales; commit `a66fe587`) · merged `origin/main` into `feat/composition-service` (0 conflicts — lore-enrichment track isolated; regression check green: composition 154 unit + 29 integ, FE 814 unit + tsc clean, i18n parity OK) · D-COMP-M6-STREAM-THINKING-DISABLE (**RESOLVED** — `reasoning_effort` standardized into SDK `StreamRequest` + Go gateway stream-path + openapi; composition exposes the knob on `/generate` (model-default) + critic uses the working knob; live-proven on qwen3.6-35b-a3b: `reasoning_effort="none"` streams prose, baseline streams reasoning-only) · D-COMPARE-PICKER-PAGINATION (compare picker now load-more paginated via useInfiniteQuery — reaches any revision; offset proven live) · Chapter Revision Compare (BE textdiff+endpoint + FE diff page, /review-impl 2 MED) · M9 OI-1 publish-gate + scene_committed + dead-gate fix → **Composition V0 COMPLETE**. (Earlier: M8 Power panel+POST/work · M7 contract+gateway · M6 engine+critic · M4 packer · M3 prose-source.)

## Deferred / watch
- ~~D-CM1-UNPUBLISH-RETRACT~~ ✅ **CLOSED by CM3b** (unpublish retracts evidence). CM3c adds the **passage** half (unpublish also `delete_passages_for_source`).
- ~~LOW-3 (manual rebuild extracts drafts)~~ ✅ **CLOSED by CM3c** (`_enumerate_chapters` gates `editorial_status='published'` server-side).
- **D-CM3C-PASSAGE-INLINE-BULK (NEW, knowledge owner):** `handle_chapter_published` runs L3 passage embed/upsert INLINE in the event consumer (CM3c dropped the old C12a save-time skip-gate → every publish embeds). A composition **bulk-publish** (all `done` scenes → N sequential `chapter.published`) serializes the consumer behind N embed round-trips, back-pressuring chat/glossary events. Canon never lost (graph-queue written FIRST; passage half best-effort/non-fatal). **Fix at CM-FE:** move passage-ingest behind the `extraction_pending` drain, OR fire-and-forget the best-effort passage half (mind consumer task-lifecycle). Cycle-0 single-publish cadence makes inline acceptable now.
- ~~D-CM3C-BOOK-LIST-GO-TEST~~ ✅ **mostly CLOSED by /review-impl** — extracted `appendEditorialStatusFilter` pure helper + `TestAppendEditorialStatusFilter` locks the placeholder arithmetic (R2-BLOCK#2), the published `IS NOT NULL` gate, and draft/empty variants. Residual (DB-only, → D-CANON-CYCLE0-LIVE-SMOKE): the live count+items parity, invalid→400, COUNT-err→500 paths.
- **D-CM4-RERANK-PERF (NEW, knowledge owner):** `rerank_chronological_order` is O(project events) and fires per dated-chapter persist → O(chapters × events) on a fully-dated whole-book build. Book-scale OK; watch very large/dated projects. Future option: rerank once at end-of-job (worker drain completion) instead of per-chapter — but per-chapter is needed for the single-publish incremental case. Debounce already skips undated chapters/chat.
- **D-CM4-NEO4J-ORDERING-SMOKE (NEW, folded into D-CANON-CYCLE0-LIVE-SMOKE):** CM4's Cypher (min-keep ON MATCH, sequential date-rank ORDER, INT64 null-sentinel ordering past chapter 2147, backfill stamping + idempotence) is unit-tested only via fake-session orchestration. Real ranking/ordering needs a Neo4j integration run: assert `timeline?before_chronological=` + reading-order return correct NON-EMPTY ordered sets; a >2147-chapter event sorts BEFORE null-order events; backfill re-run is deterministic.
- **D-CM3C-DEPLOY-ORDER (NEW, /review-impl #3, ops note):** worker-ai/knowledge CM3c must deploy **AFTER** book-service CM3c. Against a pre-CM3c book-service (which ignores the unknown `?editorial_status=` param and omits the new fields), worker `list_chapters` maps every `revision_id`→None → `_enumerate_chapters` skips ALL chapters (manual rebuild extracts nothing); the estimate over-counts (drafts included). The direction is SAFE (never extracts drafts) but SILENT. **Deploy book-service first.** This is the current running-stack reality (pre-CM1).
- **/review-impl accepted (LOW, documented):** (#4) `ChapterInfo.editorial_status` is populated but unread by the worker (gate keys on `revision_id`) — kept as wire-contract documentation/observability. (#6) cursor-resume restarts the whole rebuild from scratch if the cursor chapter is unpublished/revision-purged mid-run (now filtered out → "not found" → restart); idempotent (retract-then-write) but wasteful — pre-existing trigger widened by the published filter.
- **D-CANON-CYCLE0-LIVE-SMOKE** (tightened; **Postgres/repo half PROVEN 2026-06-04**): ran the knowledge Postgres integration suite against a throwaway `loreweave_knowledge_test` DB on infra-postgres (`:5555`, creds loreweave/loreweave_dev; set `TEST_KNOWLEDGE_DB_URL`, leave `TEST_NEO4J_URI` unset). **CM-relevant tests PASS (42: extraction_pending_repo + migrations) → CM1 schema + CM3b revision_id/aggregate_type/upsert_chapter_pending validated at a real DB.** ⚠️ **Running infra stack is PRE-CM1** (live `loreweave_book` has no `editorial_status` column) → the CM1→CM3c chain is NOT deployed; a real cross-service smoke needs rebuilding book+knowledge+worker-ai images (`scripts/build-stack.sh`) so CM1's migration applies. Still needed: **Neo4j half** (entities/relations/provenance — needs a *dedicated* test Neo4j; community single-DB can't isolate from the demo graph) + **cross-service flow** (publish→queue→drain→extract at pinned rev; re-publish re-arm [MED-1]; unpublish retract; unpublish-during-drain [MED-3]; stale-model fail-stop [MED-2]; **CM3c: manual rebuild skips drafts + passages from pinned revision; unpublish drops passages**) on a stack-up. ⚠️ The Postgres fixture TRUNCATEs `knowledge_projects CASCADE` — NEVER point `TEST_KNOWLEDGE_DB_URL` at live `loreweave_knowledge`.
- **D-EXTRACTION-JOBS-TEST-ROT (NEW, pre-existing, knowledge owner):** `tests/integration/db/test_extraction_jobs_repo.py` uses stale status strings `'complete'/'cancelled'/'paused'` (lines 154/156/181/195…) but the schema enum is `pending/running/summarizing/completed/failed` → 16 CheckViolation failures when run against a real DB. Rotted because it skips by default (no dev-host run). NOT CM3b. Fix: update the test to the current status vocabulary (and add a regression-lock matching the migrate CHECK). Surfaces the broader risk: the heavy integration-skip suite needs a periodic real-DB run (CI Gate-2) to stop rot.
- **CM3b accepted races/edges (review-impl):** MED-3 unpublish-during-drain re-canonize (narrow; re-unpublish fixes) · LOW-1 paused-drain-on-cap blocks until resumed (visible) · LOW-2 per-persist retract assumes per-chapter persist (commented; future per-chunk would break).
- L5 long-term-summary lens (no HTTP read endpoint) — future knowledge surface.
- `chronological_order` quality for non-ISO in-world dates (CM4) — reading-order fallback.

## Deferred / watch
- **D-CM1-UNPUBLISH-RETRACT** — `/unpublish` flips status but does NOT retract already-extracted KG facts; **CM3b** wires `remove_evidence_for_source` on `chapter.unpublished`. Until then: temporary canon drift on unpublish.
- **D-CANON-CYCLE0-LIVE-SMOKE** — book-service has no DB-backed Go test harness; CM1 publish/unpublish tx + backfill one-time property + NULL-pointer scan + **CM3a revision-text endpoint** are string/build-tested only → need a live-smoke at stack-up, OR keep deferred. **Required assertions:** publish→revision pinned→event; re-publish; backfill double-run; unpublish; **CM3a: revision-text returns the published revision's text AND a cross-book/cross-chapter revision_id → 404 (the IDOR-negative, review-impl MED-1 — not just happy path).** (Also CM3b proves publish→extraction end-to-end.)
- L5 long-term-summary lens (no HTTP read endpoint) — future knowledge surface.
- `chronological_order` quality for non-ISO in-world dates — extraction-quality follow-up; reading-order is the fallback.
- Composition V1/V2 (branches/takes, autonomous loop, consistency sweep, 同人) — post-V0.
