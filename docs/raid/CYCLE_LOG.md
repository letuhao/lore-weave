# RAID Cycle Log — creation-unblock

> Task `creation-unblock` (slug `2026-06-13-creation-unblock`). One row per cycle as RAID executes. See [cycle decomposition](../plans/2026-06-13-creation-unblock/CYCLE_DECOMPOSITION.md).
>
> **Schema note:** column 1 is the BARE cycle number (0–28) and column 3 is the single-word Status — this is the contract `scripts/raid/coordinator-helper.py` parses (`| <num> | <title> | <status> |`) and that `done-cycle` flips PENDING→DONE. Do not prefix the number with `C` or reorder these three columns.
>
> **C0** is the shared-FE bootstrap; per the /raid contract C0 is built via the default+AMAW workflow (NOT dispatched by the Coordinator) and must be DONE before the loop runs C1+.

| Cycle | Title | Status | BE/FE | Commit | Verify / notes |
|---|---|---|---|---|---|
| 0 | Bootstrap — shared FE foundation | DONE | FE | (this commit) | FormDialog max-h/scroll/pinned-footer + reusable AddModelCta (deep-link+return, ProvidersTab honors ?return=) + rerank/reranker reconcile (canonical RERANK_CAPABILITY, spy-injection wiring test). tsc+eslint clean; 17 vitest green; verify-cycle-0.sh exit 0. Live screenshot deferred to first consumer (C5/C6/C7/C15) — FormDialog/AddModelCta have no live surface yet (D-C0-FOUNDATION-LIVE-SMOKE). Default workflow, not /raid. |
| 1 | Rerank registration (FE) | DONE | FE | (this commit) | rerank added to KNOWN_FLAGS via canonical RERANK_CAPABILITY; labels in all 4 locales; RerankModelPicker empty-state offers in-flow AddModelCta register link (BL-1). tsc+eslint clean; 10 vitest green (PowerShell runner; bash-spawned vitest hangs in this env). verify-cycle-1.sh added; stale lore-enrichment verify-cycle-{1..18}.sh removed. Coordinator-rescued (sub-agent stalled pre-commit on a hung bg vitest + a never-resolving-promise test). Minor: dead capability.flag.reranker i18n key left (harmless). |
| 2 | Rerank discovery (BE+FE) | DONE | BE+FE | (this commit) | provider-registry inventory sync detects rerank across OpenAI/Cohere-shape /v1/models (new parseCohereModels via endpoints+name) + LM Studio parser; tags canonical rerank (capability_flags.rerank=true + _capability) matching the picker filter; fixed BE reranker→rerank drift. FE discovery setup-guidance hint. Go tests 6/6 (parsers + Cohere httptest); build+vet+provider-gate green; FE tsc/eslint clean, picker 7 vitest. Adversary CLEAR (added multi-endpoint test). LIVE-SMOKE RESOLVED 2026-06-15: `live smoke: rerank model discovered + tagged from :28417 backend (LM-Studio-native id shape)` — the real local-rerank backend /v1/models is `{"models":[{"id":"bge-reranker-v2-m3","state":...}]}` (id-keyed, no `name`/`endpoints`); the original C2 dispatch routed any `models` array to parseCohereModels (keys on `name`) and the OpenAI parser only fired on top-level `data` → 0 items discovered. FIX: openaiAdapter.ListModels now disambiguates the `models`-keyed payload by ENTRY KEYS (isLMStudioNativeIDShape: first entry has `id`, no `name`) → routes the id shape to the new parseLMStudioNativeIDModels (classifies each id via classifyOpenAIModel, `*rerank*`→rerank + canonical boolean flag); the Cohere `name`+`endpoints` shape still routes to parseCohereModels; OpenAI `data` + LM Studio native `/api/v1/models` shapes unchanged. Live re-run vs :28417 (BYOK credential 019eb5a9-…, refresh=true through gateway :3123) now returns 1 item: bge-reranker-v2-m3 tagged `_capability='rerank'` + `rerank:true` (synced_at set). Go 8/8 in adapters_rerank_discovery_test.go (2 new: id-parser-only-reranker + ListModels-id-shape httptest); full provider-registry suite + build + vet + provider-gate green. Evidence: docs/raid/evidence/cycle-2/discovery-rerank-sync.json. |
| 3 | Rerank connection test (BE+FE) | DONE | BE+FE | (this commit) | verify path now rerank-aware: detectPrimaryCapability recognizes rerank (bool + _capability), new verifyRerank does a REAL /v1/rerank round-trip via the canonical provider.Rerank gateway using the BYOK-resolved credential (no direct call/env/hardcoded model). EditModelModal Test renders ranked-docs + top score. Go 3 tests (httptest round-trip + detect + error), build+vet+provider-gate green; FE tsc/eslint clean, 2 EditModelModal vitest. Adversary inline CLEAR. LIVE-SMOKE RESOLVED 2026-06-15: live smoke — real /v1/rerank round-trip via provider.Rerank returns ranked docs + top score (verified=true, top_index=1, top_score=1; cold 2644ms→warm 45ms against :28417 bge-reranker-v2-m3, BYOK credential 019eb5a9-…, no direct call/env/hardcoded model). Evidence: docs/raid/evidence/cycle-3/verify-rerank-roundtrip.json. Rerank trio C1–C3 complete. |
| 4 | Book picker (FE) | DONE | FE | (this commit) | reusable BookPicker (debounced title search over booksApi.listBooks, emits book_id, empty=valid, internal-branch not unmount). Swapped BOTH call sites: ProjectFormModal raw-UUID input + campaign BookProjectStep 200-cap <select> (also dropped its orphaned books query). tsc+eslint clean; BookPicker 4 vitest + ProjectFormModal 5 (regression: added BookPicker stub) green; verify-cycle-4.sh exit 0. Adversary inline CLEAR. Minor: ProjectFormModal bookIdValid now always-true (dead, harmless). |
| 5 | Build-graph gates unblock (FE) | DONE | FE | (this commit) | BuildGraphDialog: in-flow AddModelCta on empty LLM + EmbeddingModelPicker empty-state CTA (deep-link+return, no dead-end); benchmark promoted to a strict visible gate — closed the enable-too-early window (benchmarkLoading) so Confirm stays disabled until a passing benchmark; disabled-reason names the missing precondition. Gates ONLY embedding+benchmark (NOT rerank — LOCKED diagnosis, gate-checked). tsc+eslint clean; 41 vitest (3 new C5 gates: empty→CTA+disabled, unbenchmarked→disabled, pass→enabled); verify-cycle-5.sh exit 0. Adversary inline CLEAR. |
| 6 | Project detail SHELL (FE — IA backbone G6) | DONE | FE | (this commit) | nested route `/knowledge/projects/:projectId/:section` → ProjectDetailShell (useParams projectId+section; route-driven sub-tab `<Link>`s, no stateful-unmount). Overview = reused ProjectRow state-card + config block; Explore-graph CTA + clickable stats deep-link into the shell (CompleteCard onExploreGraph → navigate /entities). Scope-threaded `scopedProjectId` into Entities/Timeline/Raw(Evidence): effectiveProjectId = route param, per-tab project `<select>` hidden when scoped; cross-project flat-tabs (KnowledgePage default) unchanged = secondary. Insights=MiningInsightsTab; Proposals/Gap/graph = stubs (C10/C11/C5+C19). i18n shell+exploreGraph keys in all 4 locales. tsc+eslint clean; 63 vitest green (5 new shell + 2 new scoped-hide); verify-cycle-6.sh exit 0. Adversary CLEAR (0 blk/0 maj/2 min acknowledged-acceptable). LIVE-SMOKE: live infra unavailable — frontend :5174 + gateway :3123 both refused (stack down at dev time); unit tests cover route-scoping + dropdown-hide + Explore-graph nav. |
| 7 | Projects browser = HOME + build polish (FE) | DONE | FE | (this commit) | `/knowledge/projects` HOME browser: search-by-name + sort + filter-by-state + REAL cursor pagination (useProjects → useInfiniteQuery, accumulates pages, Load-more advances next_cursor — no 100-cap). CURSOR FINDING: BE cursor pagination ALREADY EXISTS (listProjects accepts `cursor`, resp has `next_cursor`) → wired it (no offset fallback, no new BE). BE list endpoint has NO search/sort/status params (ProjectListParams = limit/cursor/include_archived/book_id) → search/sort/filter run client-side over loaded rows (narrowProjects, pure+tested) — documented, scope-OUT to add. Rows route INTO C6 shell via onOpen → navigate(/knowledge/projects/:id/overview). Polish: post-submit toast (KN-5 startSuccess) + running-build elapsed label (KN-9, reused unused i18n key). DEFER: raise-cap inline (KN-7) needs a BE job-cap PATCH — no new BE allowed → finding. tsc+eslint clean; 80 targeted vitest green + i18n parity 78 + shell/page regressions 91; verify-cycle-7.sh exit 0; adversary CLEAR (0/0/0/3notes). LIVE-SMOKE: live infra unavailable — :5174 + :3123 refused (stack down); unit tests carry M1 evidence. M1 milestone. |
| 8 | Entities semantic layer (BE+FE) | DONE | BE+FE | (this commit) | BE: derived `status` computed field (archived>canonical>discovered, NO new column) + `status` filter CASE + `sort_by=anchor_score` (closed-allowlist interpolation) + `semantic_query` vector path embedding via provider-registry (`project.embedding_model` ref, no hardcoded model — same BYOK pattern as drawers/search). FE: ⭐/💭/📦 StatusGlyph + ⚓ anchor badge + status legend + semantic-search box (scoped-only) in the C6 shell; project `<select>` stays hidden when route-scoped (G6); i18n in all 4 locales. provider-gate green; pytest 48 (16 new C8) + vitest 12 (new C8) green; verify-cycle-8.sh exit 0. LIVE-SMOKE: `live smoke: semantic_query + status filter on a built knowledge graph` — project 019eb683 (万古神帝, 106 entities): semantic '张若尘'→张若尘#1/林妃/神武印记 vector-ranked (embed via provider-registry 019e7f71), status=discovered→106 / canonical→0 / archived→0. Playwright screenshot docs/raid/evidence/cycle-8/entities-semantic-layer.png. Adversary 0 blk/0 maj/2 min(1 fixed: semantic total count; 1 ack)/2 notes(addressed); Scope Guard QC + POST-REVIEW both CLEAR. Live-smoke caught a real async-await bug (get_embedding_client not awaited) — mock was sync, fixed both. |
| 9 | Promote + entity detail (BE+FE) | DONE | BE+FE | (this commit) | BE: new `POST /v1/knowledge/entities/{id}/promote` orchestrates the two-call flow server-side (FE can't reach glossary `/internal`): (1) glossary DRAFT via `GlossaryClient.propose_entities(default_tags=['ai-suggested'], park_unknown_kinds=False)` → `extract-entities` INSERTs `status='draft'` (NEVER active), (2) anchor via existing `link_to_glossary` (`glossary_entity_id`+`anchor_score=1.0`). Guards: 404 / 409 already_anchored (no double-draft) / 422 no_book / 502 glossary_draft_failed (no anchor) / 502 anchor_failed (draft persists, retry-safe — propose name-dedups, link idempotent). `_draft_glossary_id` accepts created/updated/skipped-with-id (existing-entry anchor) but refuses tombstoned. FE: `promoteEntity`+`setGlossaryEntityPinned` api, `usePromoteEntity`+`useToggleGlossaryPin`+`useEntityFacts` hooks, EntityDetailPanel gains promote (discovered-only), unpin (`is_pinned_for_context` toggle, canonical+book-scoped — NOT delete/archive), facts list + `source_chapter` (provenance MVP; passage-trail deferred); bookId threaded from route-scoped project (G6). i18n 4 locales. provider-gate green; tsc+eslint clean; pytest 10 (new C9) + vitest 25 (10 panel C9 + 11 mutations incl 3 C9 + i18n) green; verify-cycle-9.sh exit 0. LIVE-SMOKE: `live smoke: promote discovered entity -> glossary draft created + entity anchored` — project 019eb683 (万古神帝): promoted 九天明帝经 → glossary draft (status=draft, tags=[ai-suggested], kind=item) + KG entity canonical (glossary_entity_id set, anchor_score=1.0). Playwright: docs/raid/evidence/cycle-9/{entity-detail-discovered-promote,entity-detail-canonical-unpin}.png. Adversary 0 blk/0 maj/2 min(1 fixed: alias canonical-fold dedup; 1 ack live-smoke-covered)/3 notes; Scope Guard QC + POST-REVIEW both CLEAR. Live-smoke caught a real cross-service shape bug: `_draft_glossary_id` first rejected ALL skipped rows → 502 on an entity whose name aliased an existing glossary entry; fixed to anchor to it. |
| 10 | Glossary Gap Report (BE+FE) | DONE | BE+FE | (this commit) | BE (thin): new `GET /v1/knowledge/projects/{id}/gaps?min_mentions=&limit=` — pure pass-through over the existing `find_gap_candidates()` (ENTITY gaps: high-mention DISCOVERED entity, no glossary entry); `min_mentions`(ge=0)/`limit`(ge=1,le=MAX) flow straight to the repo + echoed back; JWT user-scoped, no new gap engine. LOCKED: kept SEPARATE from lore-enrichment attribute-dimension detect-gaps. FE: `getProjectGaps` api + `useGaps` hook; `useBulkPromote` = SEQUENTIAL reuse of the C9 single-promote (`knowledgeApi.promoteEntity`, one await at a time, per-item try/catch → progress indicator + partial-failure survival, NO batch endpoint); `GapReportTab` = summary cards + min_mentions threshold (debounced) + limit control + select-all/bulk-promote, rendered route-scoped in the C6 shell gap section (G6, no project select-box). i18n `gap.*` in all 4 locales (key parity verified). provider-gate green; tsc+eslint clean; pytest 6 (new C10) + vitest 7 (4 GapReportTab + 3 useBulkPromote) green; verify-cycle-10.sh exit 0. SMOKE: rebuilt+restarted knowledge-service (stale image → 404), Playwright on project 019eb683 (万古神帝): threshold 50→5 surfaced 24 gaps (top 张若尘 18), selected 紫怡偏殿+玉漱宫 → bulk-promote "2/2 promoted", count 24→22, both became glossary drafts (status=draft, tags=[ai-suggested], confirmed via glossary API). Screenshots docs/raid/evidence/cycle-10/{gap-report,post-bulk-promote}.png. Adversary 0 blk/0 maj/5 min(3 fixed: debounce threshold, retain-failed-selection on partial fail, per-row aria-label; 2 ack)/4 notes; Scope Guard QC + POST-REVIEW both CLEAR. |
| 11 | Pending Proposals inbox (FE) | DONE | FE | (this commit) | Resumed partial run. Pending Proposals inbox aggregates EXACTLY 3 existing review queues read-only + deep-link (integrate, don't duplicate): (1) glossary AI-suggested drafts (listAiSuggestions → status=draft&tags=ai-suggested) → /books/{id}/glossary; (2) wiki suggestions review queue (listSuggestions → status=pending — CORRECTED the prior partial's nonexistent article status=stub) → /books/{id}/wiki; (3) lore-enrichment proposals (listProposals review_status proposed + author_reviewing as TWO exact-match calls, merged — BE filters review_status by exact equality) → /books/{id}/enrichment. Per-source graceful degrade (each source own try/catch; one erroring/empty never blanks the inbox). ProposalsInboxTab = unified rows grouped by origin + per-origin counts + per-source error chip + empty/no-book/loading states, rendered route-scoped in the C6 shell Proposals section via project.book_id (G6, no project select-box). i18n proposals.* in all 4 locales (12 keys each, parity). tsc+eslint clean; 11 vitest green (6 lib: 3-source merge + per-source exact filters incl wiki status=pending + enrichment two-call + graceful-degrade + empty; 5 tab: counts/deeplinks/degrade/empty/no-book/loading) via PowerShell runner; verify-cycle-11.sh exit 0 (grep-asserts EXACTLY-3-sources + exact filters + deep-links + read-only + G6). LIVE-SMOKE: `live smoke: proposals inbox aggregates 3 review queues read-only + deep-link navigates` — D-C11-INBOX-LIVE-SMOKE RESOLVED 2026-06-15 (stack up, Playwright, claude-test). C6 shell Proposals sub-tab on book 019eb60e (万古神帝, derivative project 019ec734): unified inbox rendered all 3 source groups with per-origin labels + counts — Glossary AI drafts=3 (紫怡偏殿/玉漱宫/九天明帝经 → /books/019eb60e/glossary) + Wiki suggestions=1 (池瑶 → /books/019eb60e/wiki) + Lore-enrichment=0 graceful empty-state (review_status proposed|author_reviewing both 200/0-rows → "No pending proposals", inbox NOT blanked). 2 populated + 1 gracefully-empty = populated-rows AND graceful-degrade in one run. Clicked 紫怡偏殿 row → navigated to the book Glossary review surface (AI-suggestions=3 badge); read-only Link rows, no inbox accept/reject. Glossary drafts had been promoted active by C9/C10/C24 → restored draft via the existing bulk-status endpoint for the capture then flipped back to active (data left as found, NO feature-code change). Console clean of inbox/deep-link errors. Evidence docs/raid/evidence/cycle-11/{SMOKE_NOTE.md,inbox-live.png,inbox-deeplink-landing.png}. Adversary 0 blk/0 maj/0 min/3 notes (NOTE1 wiki-deeplink-granularity ack: tab is the only addressable review route; NOTE2 stale-comment fixed; NOTE3 single-page-count ack); Scope Guard QC + POST-REVIEW both CLEAR. |
| 12 | Build wizard — target-typed extraction + concurrency (BE+FE) | DONE | BE+FE | (this commit) | Target-typed extraction across SDK+orchestrator+BE+worker-ai+FE. SDK `extract_pass2(targets=, concurrency_level=)`: conditional R/E/F gather task-list + `normalize_targets` dependent auto-include + recovery/filter disable when entities∉targets + Semaphore concurrency cap. Orchestrator threads targets (gather + summaries gate + recovery/filter gate). BE: StartJobRequest `targets`+`concurrency_level` (validator dedupes, does NOT bake entities — runtime adds it so the LOCK keys off explicit intent), migration `extraction_jobs ADD targets TEXT[] NOT NULL DEFAULT all-five + concurrency_level INT`, repo+inline INSERT threading (None⇒explicit all-five). worker-ai: JobRow reads j.targets/j.concurrency_level → extract_pass2(sdk subset)+persist-pass2(summaries gate); decoupled trio honours `trio_targets` subset (new_extract_state/apply_entity_result/assemble_trio_submits/fold/trio_complete via `_requested_trio_ops`; entities-only→recovery/filter dispatched not persist-empty). FE: 3-step BuildWizardSteps shell + Step-1 TargetPicker + concurrency input; posts RAW selection via `canonicalTargets` (no entities bake). i18n 4 locales (parity 774 keys). provider-gate green; tsc+eslint clean; tests: SDK 21 + KS 99 (orch 36/billing/validation/persist) + worker-ai decoupled 36 + run_telemetry + FE vitest 52; verify-cycle-12.sh exit 0. LIVE-SMOKE: `live smoke: targets=["events"] build → only the event pass runs` — project 019eb683 (万古神帝): posted ["events"]→stored ["events"] (explicit intent), decoupled trio_targets=["event"], ONLY event_extraction provider job (zero relation/fact). Playwright: docs/raid/evidence/cycle-12/build-wizard-step1-target-picker.png (3-step shell + target picker; events toggle auto-checks+disables entities + recovery-disabled hint). Adversary 0 blk/3 maj(ALL fixed+reverified: (1) entities no longer baked into stored array→recovery/filter LOCK now keys off explicit intent; (2) entities-only+recovery/filter dispatched in decoupled consumer not persist-empty; (3) concurrency_level wired into SDK gather Semaphore)/2 min(ack: legacy /extract-item + orchestrator gate mostly-dead for live worker-ai path)/3 notes; Scope Guard QC + POST-REVIEW both CLEAR. Pre-existing unrelated FAIL: worker-ai test_malformed_extraction_config (env-dependent _load_precision_filter_config, fails on clean HEAD too). DEFER D-C12-CONCURRENCY-DECOUPLED: concurrency_level caps the SDK sync gather; decoupled trio is fire-and-forget submit (no gather to cap) — honoured on the sync path, not the decoupled path. |
| 13 | Build wizard — glossary pinning (BE+FE) | DONE | BE+FE | (this commit) | Glossary pinning across knowledge+worker-ai+glossary+FE. Knowledge: StartJobRequest.pinned_glossary_entity_ids + `pinned_entity_ids JSONB` migration (default null) + `_merge_pinned` name-prefix prepend into known_entities at BOTH `_run_pipeline` call sites (chat_turn + chapter) + pinned-injection cost line (`_TOKENS_PER_PINNED_ENTITY=50`, `estimated_pinned_tokens = pinned_count×50×num_windows` folded into estimated_tokens) + thin `GET /projects/{id}/glossary-entity-stats` proxy. worker-ai: NEW `GlossaryClient.fetch_entities_by_ids` (POST entities/by-ids, REUSES X-Internal-Token, NO new secret) + JobRow.pinned_entity_ids + `_decode_pinned` JSONB + fetch-once-per-job → injected into ALL THREE window types (decoupled `_start_decoupled_chunk`, sync `_extract_and_persist` chapter, chat-turn) replacing the hardcoded `known_entities=[]`. glossary: NEW `GET /internal/books/{id}/entities/stats` bounded GROUP-BY over chapter_entity_links → {entity_id,name,kind,mention_count,first/last_chapter_index,coverage_pct} + book-service-outage denominator fallback. FE: Step-2 dual-list (available↔pinned + search/kind/freq filters + auto-pin banner [coverage≤0.15 AND span≥0.5×chapters] + per-window budget) posting pinned_glossary_entity_ids; pinned-injection estimate line. infra: added worker-ai GLOSSARY_SERVICE_URL=:8088 (fixed stale :8082 default that silently broke all worker glossary calls — service-internal URL, not a model backend). provider-gate green; tests: knowledge 61 (orch incl _merge_pinned + estimate pinned + stats proxy) + worker-ai 106 (fetch_by_ids + _decode_pinned + runner threading; fixed 3 pre-existing C12-broken _get_running_jobs fixture rows) + glossary go (computeEntityStats/maxChapterDenominator + DB GROUP-BY on real PG) + FE vitest 18 (12 pinning lib + 6 PinningStep) + i18n parity 791; verify-cycle-13.sh exit 0. LIVE-SMOKE: `live smoke: 2 pinned entities absent from chapter N → both appear in chapter N extraction prompt` — project 019eb683 (万古神帝): pinned 张若尘+池瑶 (linked only to ch idx=0) → worker log "C13 pinning — 2 pinned id(s) resolved to 2 name(s)" + "C13 pinned-injection: chapter (idx=1) known_entities <- ['张若尘','池瑶']" AND (idx=2) — both ABSENT chapters carry the pins; cross-service knowledge→worker-ai→glossary by-ids verified (rebuilt 3 images, migration applied). Playwright: docs/raid/evidence/cycle-13/{build-wizard-step1-check,build-wizard-step2-pinning,build-wizard-step2-pinned}.png (3-step shell + dual-list pinning 张若尘/池瑶 + "+100 tokens per window" budget). Adversary 0 blk/1 maj(FIXED: chat-turn window omitted pinned_names while estimate charged for chat_turns → injected pinned_names at the chat-turn call site)/1 min(gofmt fixed)/7 notes(1 fixed: inverted coverage-fallback comment; m2/m3 ack safe); Scope Guard QC + POST-REVIEW both CLEAR. |
| 14 | Timeline narrative-order + importance (BE+FE) | DONE | BE+FE | (this commit) | **▶ M2 milestone (knowledge design-complete).** BE thin+additive: DERIVED `Event.importance` `@computed_field` (major/pivotal/None — NO new column, NO re-extraction; blends ≥participants+confidence with mention_count so it survives a degenerate mention_count=1 corpus; long tail stays None) + closed `EVENT_IMPORTANCE`; `sort_by` on the timeline endpoint via closed `TIMELINE_SORT_KEYS` allowlist (`narrative`=default=byte-identical prior `event_order ASC` ordering=back-compat; `chronological`=`chronological_order` in-story axis) — triple-validated (router `Literal`→422, repo `ValueError`, dict-lookup before Cypher assembly; injection-proof). FE: importance badges (pivotal=filled/primary, major=amber outline; null=unbadged) in the timeline rail + narrative/chronological segmented sort toggle, route-scoped in the C6 shell — project `<select>` stays `{!scoped}`-hidden (G6, no re-intro), no stateful-tab unmount; `sort_by` in useTimeline cache key; i18n `timeline.sort.*`+`timeline.importance.*` in all 4 locales (parity 796 keys). provider-gate green; tsc+eslint clean; pytest 38 (12 new C14: derivation branches+enum-closure+wire+back-compat default+422+repo ORDER-BY-key asserts+`"; DROP"` raises) + vitest 15 (4 new C14: default-narrative, chrono-toggle refetch, badge-count exactly-2-ordinary-unbadged, G6 no-select) green; verify-cycle-14.sh exit 0. LIVE-SMOKE: `live smoke: timeline importance derived (5 pivotal + 45 major + 91 unbadged of 141 events) + narrative≠chronological sort` — project 019eb683 (万古神帝): rebuilt+restarted knowledge-service, derived importance over the existing event store (no re-extraction), narrative vs chronological return genuinely different orderings. **playwright: timeline rail renders narrative order with importance badges** — Playwright on the C6 shell Timeline sub-tab (pivotal badge on 云武郡王疏远张若尘和林妃 [3 participants], major badges on multi-party events). M2 screenshot set docs/raid/evidence/cycle-14/m2-*.png (8 shots: timeline rail+chronological C14, entities C8, entity-detail/promote C9, gap C10, proposals C11, build-wizard step1-targets C12 + step2-pinning C13 — all surfaces reachable in the shell). Adversary 0 blk/0 maj/1 min(FIXED: added min-w-0 truncation hardening on title span)/8 notes; Scope Guard QC + POST-REVIEW both CLEAR. |
| 15 | Writer unblock (FE) | DONE | FE | (this commit) | Writer-unblock (WG-1/2/6 LOCKED). DPS1 CompositionPanel: empty active-chat-model list → in-flow `AddModelCta` (capability=chat, deep-link /settings/providers + `return=` round-trip) replacing the dead `<select>`; chat-model-present → positive "Ready to draft" cue framing knowledge/grounding as OPTIONAL (derived `hasChatModel`/`noChatModel`, no useEffect). DPS2 new `CowriteBridgeButton` wired into ChapterEditorPage toolbar = plain-editor→AI bridge via DIRECT handler (`setRightTab('compose')` + open right panel; no useEffect-for-events). Chat model resolved via registry (`listUserModels{capability:'chat'}`) — NO hardcoded model (provider-gate green). ComposeView `canGenerate` unchanged: gates ONLY on scene+model+busy — knowledge empty = advisory, never a wall (no stateful-unmount on empty↔ready; AddModelCta swaps only the select, ComposeView stays CSS-hidden-mounted). i18n 5 keys × 4 locales. tsc+eslint clean; 8 vitest (3 readiness: empty→CTA / present→ready-to-draft / no-hard-block; 2 bridge: direct-handler + aria-current active; 3 CSS-hidden regression) + ChapterEditorPage regression 2 green; verify-cycle-15.sh exit 0. LIVE-SMOKE (Playwright, claude-test): 万古神帝 book ch1 → "Co-write with AI" bridge opens Compose (button aria-current active) → Set up co-writer → "Ready to draft" + optional-knowledge advisory shown (Qwen models via registry) → +Scene + pick Qwen2.5-7B → Generate ENABLED with EMPTY grounding → real prose streamed (full 紫怡偏殿 scene, 本章完) — proves writer-not-blocked + grounding-optional. Screenshot docs/raid/evidence/cycle-15/generate-empty-grounding.png. Adversary 0 blk/0 maj/1 min(FIXED: aria-pressed→aria-current, action-button not dead-toggle)/4 notes(pre-existing rightTab-conditional CompositionPanel ack out-of-scope); Scope Guard QC + POST-REVIEW both CLEAR. |
| 16 | Work-setup resilience (BE composition) | DONE | BE | (this commit) | WG-3: `POST /work` survives a knowledge-service OUTAGE — both `resolve unavailable` (knowledge DOWN) AND `none`+create_project-outage branches degrade for GREENFIELD → persist a lazy Work with `project_id=NULL` + `pending_project_backfill` (was 502). A 4xx CONTRACT error STILL surfaces (new `KnowledgeContractError`: 4xx→raise→502, 5xx/transport→None→degrade — no silent swallow). DERIVATIVE (`source_work_id`) NEVER takes the null path (C23 guard, both branches, tested). Schema re-key (a null PK is impossible): surrogate `id` PK + nullable `project_id` + `pending_project_backfill`; partial-unique `(project_id) WHERE NOT NULL` (1:1 backed) + `(user,book) WHERE pending` (one lazy/book); idempotent + upgrade-safe (×2 apply on a seeded pre-C16 row preserved it, re-pointed PK; live DB 163 rows preserved). Packer `_pack_null_project` → empty-but-valid grounding, calls NO knowledge lens (no cross-project widen), grant still enforced. Backfill seam: `resolve_by_book` excludes pending → a retry on recovery stamps the project onto the SAME row (no dup). DPS1 router/client/repo/model/migration + DPS2 packer. provider-gate green; py_compile clean; pytest 441 unit (67 C16: routers+knowledge_client+pack) + 7 real-PG works integration; verify-cycle-16.sh exit 0. **LIVE-SMOKE: `live smoke: knowledge down → POST /work 2xx → Generate returns prose`** — knowledge-service STOPPED (:8216 refused): POST /work greenfield → 201 `project_id:null` + marker; deployed `_pack_null_project` in-container → grounding_available=False, NO lens called (spy); cowrite Generate on a real-project Work (knowledge still down) → streamed `grounding_available:false` + 95-token prose (紫怡偏殿…), done/completed/stop; **knowledge-service RESTORED :8216/health=200**; retry POST /work → BACKFILL (same id, project stamped, marker cleared). Evidence docs/raid/evidence/cycle-16/LIVE_SMOKE.md. Adversary (cold-start) 1 blk/0 maj/0 min/5 notes — the BLOCKER (null Work not `{project_id}`-route-addressable until backfill) is a SCOPE BOUNDARY → tracked D-C16-NULL-WORK-ROUTE→C17 (FE+routing, OUT of this BE-only cycle); evidence overclaim corrected; derivative-guard/4xx-discrimination/backfill/migration all reviewed CLEAN. Scope Guard QC + POST-REVIEW both CLEAR. knowledge-service restored. |
| 17 | Writer flow polish (FE) | DONE | FE | (this commit) | **▶ M3 milestone (write-from-scratch / continue-writing works).** WG-4/WG-5 (LOCKED writer-not-hard-blocked). DPS1 guided first-run: new `useGuidedFirstRun` HOOK (auto-pick the SOLE chat model ONLY when exactly 1 registered — never 0/≥2, read from registry, NO hardcoded model; derived needsFirstScene + guidedCue; explicit runGuided(), no useEffect-for-events) → CompositionPanel chains the first scene off the "Set up co-writer" success (≤2-click: setup→Generate) + renders a contextual cue + a primed "Start writing" action. DPS2 "Continue from cursor" first-class: InlineAiLayer's ✦ button un-buried (was gated behind the AI-mode toggle which defaults Classic → invisible) → ALWAYS visible/prominent, wired to the existing caret-anchored `useInlineGhost` streaming continue (empty-doc/caret-start/caret-mid + stale-pos already safe); ChapterEditorPage adds a sole-model fallback (composeDefaultModel) + first-scene fallback (effectiveSceneId) so Continue works straight after the guided setup, plus model-kind/name strategy-hint parity. i18n 7 keys × 4 locales (parity 164). provider-gate green; tsc+eslint clean; 27 vitest (10 useGuidedFirstRun unit incl auto-pick-exactly-one/0/≥2 + double-create-guard + chapter-reset; 4 CompositionPanelGuided; 7 InlineAiLayer; + readiness/regression) green; verify-cycle-17.sh grep-asserts PASS. **LIVE-SMOKE (Playwright, claude-test, FRESH book "C17 Writer Flow M3"):** Co-write→Set up co-writer auto-created "Opening scene" + "Ready to draft" + guided cue; picked Qwen2.5-7B → Generate streamed full prose with EMPTY grounding (writer-not-blocked); caret mid-text → first-class "Continue from cursor" streamed a caret-anchored continuation. M3 screenshots docs/raid/evidence/cycle-17/{m3-guided-first-run,m3-guided-generate-streamed,m3-continue-from-cursor}.png. Adversary (cold-start) 0 blk/1 maj(FIXED+reverified: double "Opening scene" race — setup-chain + cue button shared no dedup; unified via a single one-shot guard claimed by markFired() across both origins + chapter-keyed reset)/3 min(fixed/addressed by the same unification: cross-chapter guard reset, invalidation-key, button gate)/2 notes(1 fixed: inline model-strategy-hint parity; 1 ack: always-visible disabled-state UX intentional); Scope Guard QC + POST-REVIEW both CLEAR. D-C16-NULL-WORK-ROUTE left tracked (FE guards null project_id; BE id-route out of FE-only scope). Pre-existing unrelated working-tree break in TranslateModal.tsx (dup pageCount) NOT staged — flagged next session. |
| 18 | Graph subgraph endpoint (BE knowledge) | DONE | BE | (this commit) | NEW `GET /v1/knowledge/projects/{id}/subgraph` (G5) — `get_project_subgraph` repo fn returns `{nodes, edges, node_cap_hit}` for the `(user_id, project_id)` partition. Project-wide: deterministic top-N seeds (anchor_score/mention_count/id) capped IN the Cypher BEFORE edge traversal; ego (`center`): bounded-frontier BFS (per-hop LIMIT, NO `*1..N` enumeration — hub-OOM safe). Both keys bound on every node + both edge endpoints (no cross-project/cross-user bleed). Params hops(≤3)/limit(≤500)/center. Read-only; raw nodes+edges (no server layout). verify-cycle-18.sh exit 0 — 97 pytest green (23 new C18 unit + 4 live-Neo4j integration: partition isolation, cap+determinism, ego hop-bound, inactive-edge exclude). `live smoke: query built project subgraph → capped nodes+edges` (proj 019eb683: 50-node cap/145 edges, full=55 no-cap, ego hops=2=38 nodes, foreign proj=0). Adversary cold-start: 0 blockers, 3 majors (hub-OOM ego, project_id edge belt, active-edge consistency) ALL fixed + re-verified, 2 minors noted. provider-gate green. |
| 19 | Graph canvas (FE) | DONE | FE | (this commit) | **▶ M4 milestone (graph is an explorable visual network).** G5: new `ProjectGraphView` GENERALIZES the T2.2 RelationshipMap ego-network into a project-subgraph renderer — REUSES the shared GraphCanvas/GraphEntityNode/RelationEdge/radialLayout (no fork, NO new graph library), sources data from C18's `GET /projects/{id}/subgraph`. Added opt-in `zoomable` pan/zoom to GraphCanvas (wheel-zoom-toward-cursor + empty-space-drag pan + Reset; node-drag intact; T2.2 byte-compatible, `zoomable` defaults false). `useProjectSubgraph` HOOK owns fetch/cache/MERGE (expand-hop re-queries C18 with center/hops/limit + folds in, no full reload; FE node cap SUBGRAPH_VIEW_NODE_CAP=250 bounds the accreted union + honours BE node_cap_hit). Read-only: click node → reused EntityDetailPanel (edits via its own dialogs); expand fired from the click handler (NOT useEffect-for-events). Wired into the C6 shell graph section (replaced ShellPlaceholder). i18n graph.* in all 4 locales (parity OK). tsc+eslint clean; 29 vitest green (16 new C19: 4 merge + 2 reset/idempotent wiring + 6 canvas-zoom + 6 graph-view; 11 RelationshipMap regression) + ProjectDetailShell 5; verify-cycle-19.sh exit 0 (grep-asserts GraphCanvas reuse + radialLayout generalize + pan/zoom + click→detail + expand-hop center + node-cap + read-only + NO-NEW-GRAPH-LIB package.json guard). provider-gate green (FE-only). **LIVE-SMOKE (Playwright, claude-test): `live smoke: project subgraph rendered as a navigable network (55 nodes · 146 relations) + click→detail + expand-hop re-query + live zoom`** — project 019eb683 (万古神帝) graph view rendered the real network; clicked 张若尘 → EntityDetailPanel (person, 100% conf); ⊞ fired `subgraph?hops=1&limit=60&center=b338ec…` (NO base re-fetch — confirmed in network log); wheel zoom 1.0→1.36 live; graph intact after expand (55n/146e, no reset). M4 screenshots docs/raid/evidence/cycle-19/{m4-graph-network,m4-graph-node-detail,m4-graph-zoomed-expanded}.png. Adversary (cold-start) 0 blk/1 maj(FIXED+wiring-tested: accreted expand-state bled across project nav — shell route re-renders not remounts; added user+project identity reset via store-prev-key-in-ref render pattern, NOT useEffect; comment had falsely claimed a baseKey mechanism)/2 min(FIXED: misleading ⊟ collapse glyph → expand now idempotent no-op on already-expanded; dead expandingId → surfaced an "expanding…" cue)/1 note(radial re-layout jitter on expand — ack acceptable for read-only explorer); Scope Guard QC + POST-REVIEW both CLEAR. |
| 20 | World container — model + API (book-service) | DONE | BE | (this commit) | Additive world container in book-service (Go/Chi). Migration (inline schemaSQL, idempotent): `worlds` table (id/owner_user_id/name/description/timestamps) + nullable `books.world_id` FK ON DELETE SET NULL (default NULL, NO backfill) + `books.is_bible`/`chapters.is_bible` hidden flags; `WorldsDownSQL` drops column-before-table (reversible). Handlers (`worlds.go`): world CRUD (owner-scoped by owner_user_id — cross-user=404, no-token=401) + move-book (`POST /v1/worlds/{id}/books`, edit-grant on book, refuses re-parenting a bible book) + clear (`DELETE .../books/{book_id}`) + `GET /v1/worlds/{id}/books`. World creation auto-provisions a HIDDEN bible book + hidden `sort_order 0` bible chapter (`provisionBibleChapter`, idempotent WHERE-NOT-EXISTS — no double sort_order-0). ZERO lore/sharing/composition/knowledge edits — lore stays book_id/chapter_id-keyed (additivity held). **Migration round-trip on real PG: up→down→re-up clean/reversible/idempotent** (worlds gone then restored; world_id nullable confirmed). **live smoke: create world → bible chapter exists (sort_order 0) → attach book → world lists books; null-world books unaffected** (proj direct on :8205 rebuilt image — 2 books world-grouped vs 275 standalone untouched, library total 72 stable; delete world SET-NULLs 万古神帝 to standalone, survives active; owner-scope 404; idempotent). evidence docs/raid/evidence/cycle-20/LIVE_SMOKE.md. go test ./... green (migrate string-asserts incl down-ordering + 8 worlds handler tests: payload/bible-filename/bookGrantError route→need/auth-deny/owner-scope) + go vet + provider-gate green; verify-cycle-20.sh exit 0. Adversary (cold-start) 0 blk/1 maj(FIXED+re-smoked: bible CONTAINER book leaked into library/world-list/count — only chapter was hidden → added books.is_bible + excluded everywhere)/1 min(FIXED: guard bible-book re-parent)/2 min(ack cosmetic error-code)/3 notes(ack: pool-free units by house convention, down clean, additivity/owner-scope verified). Scope Guard QC + POST-REVIEW both CLEAR. NOTE: gateway BFF does not yet proxy /v1/worlds (FE wiring is C21) — live-smoke hit book-service directly per brief. |
| 21 | World container — FE | DONE | FE | (this commit) | **▶ M5 milestone (prose-less world container exists).** New `features/world/` (React-MVC): `worldsApi` over C20 `/v1/worlds` + lore authoring via the EXISTING glossary endpoints (create entity in the bible BOOK → chapter-link it to the bible CHAPTER = the NOT-NULL anchor). WorldsBrowser (HOME + create→land-in-workspace) + WorldWorkspacePage (resolves `bible_chapter_id`, NO manuscript/editor surface) + WorldLorePanel (kind-picker authoring + extraction-OPTIONAL messaging) + WorldGraphSection (embeds C19 read-only `ProjectGraphView` resolved by `listProjects({book_id})`). Sidebar `nav.worlds` entry + `/worlds`+`/worlds/:worldId` routes; i18n `world.*` + `nav.worlds` in all 4 locales (parity). **Step A reference:** built on the C20 follow-up commit `5fe9eb33` (exposed `bible_book_id`+`bible_chapter_id` on `worldResponse` — its OWN commit, no BE here). **Gateway passthrough:** added a THIN `/v1/worlds*` proxy → bookUrl (pure mirror of bookProxy; gateway-invariant enabling, no world logic) — rebuilt+restarted api-gateway-bff. tsc+eslint clean; provider-gate green; 9 vitest green (6 worldLore: 2-step ordering + anchor==bible_chapter_id + refuse-on-missing-anchor + endpoint targets + 3 worldsApi; 3 workspace: renders lore+graph / no-manuscript-surface / extraction-optional+anchored); verify-cycle-21.sh exit 0. **REQUIRED Playwright smoke (claude-test): `playwright smoke: worldbuilder creates a world with no manuscript and authors lore against the bible chapter`** — created "C21 Worldbuilder Smoke" via UI → landed in no-manuscript workspace → authored a Character → network: POST /v1/worlds 201, POST /v1/glossary/books/{bibleBook}/entities 201, POST .../entities/{id}/chapter-links 201; DB-confirmed `chapter_entity_links.chapter_id` == world's `bible_chapter_id` (anchor correctness). **live smoke: `/v1/worlds` gateway passthrough returns the world + bible handle (201)**. M5 screenshots docs/raid/evidence/cycle-21/{m5-world-workspace-lore-authored,m5-worlds-browser}.png. Adversary (cold-start) 0 blk/0 maj/1 min(cosmetic: hook exposes more than the panel consumes — clarified)/2 notes; Scope Guard QC + POST-REVIEW both CLEAR. M5 reached. |
| 22 | Intent-branching onboarding (FE) | DONE | FE | (this commit) | New `features/onboarding/` (React-MVC): first-run "What do you want to do?" fork — BL-15 four intents each route to a tailored surface + the RIGHT container (NEVER a generic shell): Write→`/books` · Build-a-world→`/worlds` (C20/C21 container, no new world model) · Translate→`/books?intent=translate` (BooksPage consumes the param → translate hint; ROUTE-ONLY, no new translator flow) · Explore→`/knowledge/projects` (C7/C19 read-only browse). `useOnboarding` HOOK owns ALL logic (first-run gating + choice); `IntentScreen` renders only; routing via EXPLICIT `chooseIntent` handler (NOT useEffect-for-events). First-run gated via HomePage→`/onboarding` (server-side seen-flag through `/v1/me/preferences`, NOT localStorage-only — multi-device); choice does a DURABLE awaited write (`savePrefToServer`) before navigating (best-effort: navigates even on write-fail). Re-entry = `/onboarding/new` (forceShow) + sidebar "Start something new" — not forced every session. i18n new `onboarding` ns + `nav.startNew` + `books.translateIntent.hint` in all 4 locales. ZERO BE. tsc+eslint clean; provider-gate green (FE-only); 30 vitest green (7 intentRoutes + 5 IntentScreen + 9 OnboardingPage gating/routing + 7 onboarding parity + 2 BooksPage translate-intent) + verify-cycle-22.sh exit 0. **REQUIRED Playwright smoke (claude-test): `playwright smoke: each of 4 intents (Write/Build-a-world/Translate/Explore) lands in the correct surface`** — reset seen-flag server-side → `/` redirected to the fork (first-run gating) → each of the 4 cards landed on its correct surface+container (Translate showed the tailored translate hint) → re-visit `/onboarding` after seen redirected to `/books` (not re-onboarded). Screenshots docs/raid/evidence/cycle-22/{intent-fork-first-run,intent-write-books,intent-world-worlds,intent-translate-books,intent-explore-knowledge}.png + LIVE_SMOKE.md. Adversary (cold-start) 0 blk/2 maj(BOTH FIXED+reverified live: (1) Translate landed on the generic /books shell ignoring the param → BooksPage now consumes ?intent=translate + renders a tailored translate hint; (2) seen-flag write was fire-and-forget → added durable awaited `savePrefToServer`, navigate in `.finally`)/2 min(addressed: nav.startNew parity added to all 4 locales; loading-flash + stale-guard confirmed correct)/4 notes; Scope Guard QC + POST-REVIEW both CLEAR. |
| 23 | Derivative schema + API (composition) | DONE | BE | (this commit) | dị bản M0 COW substrate in composition-service. Migration (idempotent in-DDL): `composition_work` gains `source_work_id` (self-ref FK ON DELETE SET NULL) + chapter-level `branch_point` (G3); new tables `divergence_spec` (taxonomy ∈ pov_shift/character_transform/au + nullable `pov_anchor` + `canon_rule[]`) + `entity_override` (target_entity_id + `overridden_fields` JSONB, per-(work,target) unique). **GUARD reconciled with C16:** NOT a blanket SET NOT NULL — a CONDITIONAL CHECK `chk_derivative_project_required = (source_work_id IS NULL OR project_id IS NOT NULL)` so a DERIVATIVE must carry project_id while C16's greenfield null/pending_project_backfill path survives. `C23_DOWN_SQL` drops the 2 tables + constraint + source index + 2 cols in dep order. API: `POST /works/{id}/derive` validates source ownership (404 miss; 409 if source is a lazy/un-backed Work), ALWAYS provisions a FRESH knowledge project_id (G2 — never the source's), REJECTS on outage (503 PROJECT_CREATE_UNAVAILABLE) / 4xx contract (502 PROJECT_CREATE_FAILED) — never a null-project derivative; persists spec + entity_override[] atomically (one txn); NO chapter clone (COW, reference spine read-only; `book.patched_with is None` asserted). M0 override scope = entity-field + canon-rule only (no relationship/event). provider-gate green (no AI imports); py_compile clean; pytest 447 unit (31 router incl 6 new C23 derive) + 66 real-PG integration (5 new C23: guard-rejects-null-derivative, greenfield-null-still-allowed, create_derivative-links-source+branch, spec/override roundtrip, migration up→down→up clean). verify-cycle-23.sh exit 0. **LIVE migration round-trip on real composition PG (165 rows): `migration round-trip clean + null project_id rejected`** — up adds substrate (165 rows preserved), DB CHECK rejects a null-project derivative (CheckViolationError), C16 greenfield null insert still ACCEPTED (rolled back), down→up no residue, 165 rows preserved. Evidence docs/raid/evidence/cycle-23/LIVE_MIGRATION_ROUNDTRIP.md. Adversary (cold-start) 0 blk/0 maj/1 min(FIXED: added positive `book.patched_with is None` no-clone assertion)/3 notes(ON DELETE SET NULL on source FK intentional; canon_rule TEXT[] vs JSONB decode asymmetry correct; route keys on source project_id — all benign). Scope Guard QC + POST-REVIEW both CLEAR. 2 DPS. |
| 24 | Divergence wizard + derivative studio (FE) | DONE | FE | (this commit) | dị bản M1 FE in `features/composition/` (React-MVC). **4-step divergence wizard** (`useDivergenceWizard` controller — NO useEffect; explicit goNext/goBack/submit callbacks; no localStorage): Step1 source+chapter-level `branch_point` (G3) · Step2 type (pov_shift/character_transform/au, UX §7.1) · Step3 entity-field overrides + canon rules (real source-project entities) · Step4 name → `compositionApi.deriveWork` → `POST /works/{src_project}/derive`. All 4 step bodies stay MOUNTED (CSS `hidden`, no unmounting ternary). **Derivative studio**: `DerivativeBanner` (you-are-in-a-dị-bản + branch_point + read-only-reference reminder) + `DerivativeGroundingLayers` 2-layer badges (`GroundingLayerBadge`: INHERITED ⛓ base / OVERRIDDEN ✎ delta, from the REAL submitted override set via `useDerivativeContext.classify` — never a guess) + legend + **read-only reference spine** (source chapters ≤ branch as Open links, NO auto-insert — LOCKED). i18n `derive.*` ×43 keys parity across en/ja/vi/zh-TW. **Step-A reference (own prior commit `f05b851d`):** unblocked the C23 derive 500 (knowledge `is_derivative`/`force_new` so a derivative gets its OWN fresh distinct project — G2); C24 is FE-only on top. tsc+eslint clean (C24 files; pre-existing CandidatesView rules-of-hooks lint is out-of-scope/untouched); provider-gate green; **19 vitest** (5 banner + 4 useDerivativeContext + 2 grounding-layers + 5 useDivergenceWizard + 3 wizard) + verify-cycle-24.sh exit 0. **REQUIRED Playwright smoke (claude-test): `playwright: genderbend dị bản spawned + studio badges shown`** — drove all 4 wizard steps on 万古神帝 (branch 第3章, type=character_transform, overrode 张若尘→"now a woman (genderbend)", named "Genderbend AU"), `POST /v1/composition/works/019eb683-…/derive` → **201** (was 500 pre-fix), studio switched to the derivative + rendered the banner ("branches at chapter 4 · read-only reference") + 张若尘=OVERRIDDEN / all others=INHERITED + legend + read-only spine (ch 1-3 Open links). DB: UI derivative `019ec734-…` is_derivative=t, DISTINCT from source `019eb683-…`. 0 console errors. Screenshot `docs/raid/evidence/cycle-24/genderbend-dibang-studio-badges.png` + SMOKE_NOTE.md. Adversary (cold-start) 0 blk/0 maj/2 min (cosmetic, no fix)/3 notes (1 addressed: comment at `activeWorkOverride` documenting the `key={bookId}` remount dependency). Scope Guard QC + POST-REVIEW both CLEAR (only composition FE + verify-cycle-24.sh; wizard 4 steps→derive; banner+badges+legend; reference spine read-only; Playwright filed; NO BE in this commit). 2 DPS. |
| 25 | Packer override-merge (composition) | DONE | BE | (this commit) | dị bản M0 two-project base+delta merge in the packer (G2). For a DERIVATIVE Work, `pack()` now grounds on TWO knowledge partitions: BASE (source project, branch-FILTERED ≤ branch_point via min(scene,branch) cutoff + `_cap_events`/`_cap_lore` belt) + DELTA (derivative's own project, full), merged with DELTA precedence (`merge_present`/`merge_timeline`/`merge_lore`, reconciled by glossary-anchor / casefold-normalized name/title — the cross-service normalization seam). Override mutation (`apply_entity_overrides`) applied AFTER the merge (the glossary present lens is BOOK-scoped → base+delta surface the same bio; the derivative's override is its divergence truth, wins over both): entity-field (name/summary, `description`↔`summary` alias) + added canon-rule scope → `<canon>`. Re-read+re-applied EVERY pack via `build_derivative_context`→`list_overrides_for_work` (self-syncing, NO cache). GUARD `assert_derivative_scoped` refuses a derivative with null base OR delta project (runs BEFORE the C16 null short-circuit so a null delta can't mask as greenfield); `is_derivative` = source OR branch_point OR overrides. NEW `WorksRepo.get_by_id` (READ-only, no schema) resolves the BASE project from `source_work_id` — the source's `id` is NOT its project_id (live data: id=019ec5f4 ≠ project=019eb683), so reusing source_work_id as a project_id was a real bug the live-smoke caught. Cross-space reconcile: an override target may be a KNOWLEDGE node id while present keys on the GLOSSARY anchor → `_resolve_override_anchors` (concurrent `get_entity`) maps knowledge-id→glossary anchor, match on both. Wired into all 3 engine pack call sites + grounding. provider-gate green (composition has NO AI imports); py_compile clean; pytest 467 unit (18 new C25: base-cap/no-post-branch-leak/delta-precedence×3/GUARD×2/normalization×2/override-field×5/self-syncing-no-cache/targets-only/build_derivative_context-resolves-base-via-id/non-derivative-unchanged) + 1 real-PG integration (get_by_id source resolution + cross-user isolation); verify-cycle-25.sh exit 0. **LIVE-SMOKE: `live smoke: derivative generate → overridden entity stays overridden across chapters`** — genderbend dị bản (019ec734, branch=3) on a stacked-up composition+knowledge (rebuilt images): grounding pack on 2 scenes AFTER the branch (ch4+ch5) → 张若尘 shows "now a woman (genderbend) — 张若尘 is female in this dị bản" in BOTH (override re-applied every pack; base+delta two-project reads confirmed in logs: drawers/search hit project_id=019eb683 BASE + 019ec734 DELTA same pack); other entities keep inherited base descriptions (override targets only the matched entity). Self-syncing verified live: edited override → next pack reflected "EDITED: now a powerful sorceress queen", stale v1 gone (no cache). Evidence docs/raid/evidence/cycle-25/LIVE_SMOKE.md. Adversary (cold-start) 0 blk/0 maj/3 min(ALL addressed: get_entity project-agnostic rationale comment; glossary book-scoped stub-vs-prod note in test; anchored-present-item invariant comment)/2 notes(1 addressed: serial N-await → asyncio.gather concurrent; 1 commit-hygiene: test staged). Scope Guard QC + POST-REVIEW both CLEAR. FINDING (tracked → C26/FE): the C24 divergence wizard persists `target_entity_id` as a KNOWLEDGE node id (b338ec39, an unanchored duplicate node) rather than the GLOSSARY anchor the present lens keys on — C25 reconciles knowledge-id→glossary, but the FE should write the glossary anchor. 2 DPS. |
| 26 | Critic override enforcement (composition) | DONE | BE | (this commit) | dị bản M3 derivative critic dimension in composition-service. NEW `app/engine/critic_override.py`: `detect_override_findings` (pure, AI-free deterministic detector) + `critique_overrides` (wired orchestrator). ACTIVATES ONLY for a derivative Work — gated on `deriv.source_project_id is None or not deriv.overrides` BEFORE any base read (canon Work → [] without querying the base lens, spy-asserted). REUSES C25's resolution (no re-merge): loads active `entity_override[]` via `build_derivative_context` + reconciles knowledge-id→glossary-anchor via `_resolve_override_anchors` (grep-asserted it does NOT re-implement `apply_entity_overrides`/`merge_present`). Flags an **override slip** (overridden bio field reverts to canon/base value in the passage → structured finding: entity_id+name+field+expected-vs-found) + **delta internal consistency** (a reverted field that also added a `canon_rule` → `delta_inconsistency` finding). Scope = entity-field + added canon-rule only (no relationship/event). WIRED into the `/critique` call site (`engine.py`): runs INDEPENDENTLY of the LLM critic-model gate (a derivative with no distinct critic still gets enforcement) + folds `derivative_findings` into the critic contract + persists. **WIRING PROOF (anti-no-op):** unit spy-injection `test_wiring_dimension_fires_for_derivative` (build+base-lens+detector all ran; base hit the SOURCE project) AND router end-to-end `test_critique_derivative_dimension_FIRES_at_call_site` (real `/critique` handler surfaces+persists the slip with NO critic model). provider-gate green (composition stays AI-free); py_compile clean; pytest: 11 critic_override + 40 engine_router (incl wiring) + 15 critic = 51 targeted green, full composition unit suite **478 passed**; verify-cycle-26.sh exit 0. **VERIFY token: `unit: derivative critic flags override slip + passes compliant scene`** (BE-unit is the verify per CYCLE_DECOMPOSITION C26; no live cross-service token required). Evidence docs/raid/evidence/cycle-26/VERIFY.md. Adversary (cold-start) 0 blk/1 maj(FIXED+reverified: bio-field precedence INVERTED C25 — `description` won vs C25's `summary`-wins → would flag against a value the drafter never saw [two-sources-of-truth drift]; unified `_override_bio` to mirror `apply_entity_overrides` precedence + added `test_bio_precedence_matches_c25_summary_wins`)/3 min(tracked Deferred D-C26-CRITIC-FN: broad-query base lens may not match the packer's grounded set; override-value-as-substring-of-base false-negative; delta_inconsistency rides on the bio-slip — all advisory false-NEGATIVE edges, M0-acceptable, dimension is advisory never blocks accept)/3 notes(wiring genuinely end-to-end; canon-Work correctly gated + no base read; degrade-safety+isolation hold). Scope Guard QC + POST-REVIEW both CLEAR. 1 DPS. |
| 27 | Flywheel on delta + what-if→derivative promotion | DONE | BE+FE | (this commit) | dị bản M4. **DPS1 delta flywheel (composition BE):** new `POST /works/{id}/chapters/{chapter_id}/approve` → on a DERIVATIVE chapter approval, dispatches the EXISTING knowledge extraction trigger (`/internal/extraction/extract-item`, REUSED — no new engine) into the derivative's OWN `project_id` (delta partition, G2) — NEVER source/null. Pure `app/engine/delta_flywheel.py`: `assert_delta_extraction_scoped` GUARD (refuse null delta → 409 DELTA_PROJECT_UNSCOPED), `is_forward_of_branch` write-order (pre-branch = THINNER delta skip, not an error — check runs BEFORE the guard), `plan_flywheel_dispatch`. New `KnowledgeClient.extract_item` (180s timeout — LLM extraction is slow) + `tiptap_doc_to_text` flatten. AI-FREE (caller-supplied model_ref). **DPS2 what-if→promotion (FE):** `useWhatIfPromotion` + `PromoteWhatIfButton` materialize an EPHEMERAL what-if into a PERSISTENT derivative via the C23 `deriveWork` path (`whatIfToDeriveBody` carries branch+taxonomy+pov_anchor+canon_rule+overrides, NONE dropped; fresh-project_id guard throws if BE reuses source — G2). Wired into CompositionPanel (non-derivative work only); i18n `promote.*` ×4 locales (parity). provider-gate green; py_compile clean; tsc+eslint clean; pytest **506** composition unit (27 new: flywheel 12 + approve-router 8 + extract_item 2 + prose_doc 3 + flywheel-closing 2) + FE **12** vitest (promotion hook 5 + button 4 + panel 3); verify-cycle-27.sh exit 0. **LIVE-SMOKE: `live smoke: approve dị bản chapter → delta enriches next-scene grounding`** — genderbend deriv 019ec734 (branch=3), rebuilt+restarted composition+knowledge: approved ch4 (sort 4>branch 3) → extracted 30 entities/46 events into the DELTA project 019ec734 (delta 0→30; COW held — source 019eb683 untouched), next scene's pack surfaced the new delta facts (云儿, 九天明帝经) via C25's OWN delta-read path (reconcile-by-truth: logs show `timeline?project_id=019ec734 200` + `drawers/search?project_id=019ec734 200`). Live-smoke caught a real bug: the read-lens 5s timeout silently aborted the slow LLM extract-item → fixed to 180s. Evidence docs/raid/evidence/cycle-27/LIVE_SMOKE.md + Playwright docs/raid/evidence/cycle-27/whatif-promote-action.png (what-if→derivative promotion surface live). Adversary (cold-start) 0 blk/0 maj/1 min (FIXED+tested: a derivative whose source Work was deleted was silently mislabeled `not_a_derivative` → now surfaces `source_unresolved` + refuses dispatch)/3 notes (extract_item swallows 4xx as outage — Deferred candidate; unused source_type flexibility; tiptap round-trip lockstep — all ack). Scope Guard QC + POST-REVIEW both CLEAR. 2 DPS. |
| 28 | Living-world view (FE) | DONE | FE | (this commit) | **▶ M6 milestone (living world) — creation-unblock RAID COMPLETE.** New `features/world/` living-world timeline tree: a world surfaces its canon Work as the TRUNK + each dị bản as a BRANCH, composed FE-side from EXISTING endpoints (NO BE) — `worldsApi.listWorldBooks` (C20 `GET /v1/worlds/{id}/books`) enumerates the world's books, `compositionApi.resolveWork` per book collects canon + derivatives (COW keeps a derivative on the source's book_id → resolves as `candidates`), and `buildWorldTree` joins them via C23's `source_work_id → id` chain (2nd-degree derivatives included; orphan-guard roots a foreign-source derivative so NO other world's branches bleed in), anchored at chapter-level `branch_point` (G3). REUSES the shared `GraphCanvas` SVG layer (LOCKED G5 — NO new graph library; package.json unchanged, verify grep-guarded) with a hand-rolled trunk+branch `layoutWorldTree`. Read-only; click a node → navigate into THAT Work via an EXPLICIT handler (`navigate(/books/{bookId}?work={surrogate id})`, NOT useEffect); the `?work=` selector disambiguates which Work the book's CompositionPanel opens (canon + dị bản share a book_id) — derived inline (no useEffect), backward-compatible URL-param read, NO C24/25/26/27 derivation/packer/critic/flywheel/wizard logic touched. i18n `living.*` ×16 keys parity across en/ja/vi/zh-TW. tsc+eslint clean; provider-gate green (FE-only); **30 vitest** (8 tree-builder + 4 hook + 8 LivingWorldTree + 4 CompositionPanel deep-link + WorldWorkspace 3 + CompositionPanel regression 3) + verify-cycle-28.sh exit 0 (grep-asserts GraphCanvas reuse + source_work_id chain + branch_point anchor + orphan-guard + explicit-nav + read-only + NO-NEW-GRAPH-LIB package.json guard). **REQUIRED Playwright smoke (claude-test): `playwright: world timeline tree shows canon + ≥2 derivative branches + branch click navigates`** — set up world `019ec7b1…` with the 万古神帝 canon book (canon trunk + **4 dị bản branches** at ch 4/2/3/4); the living-world view rendered "1 canon · 4 dị bản branches" with branch_point labels; clicked the "branches at ch. 2" dị bản → URL `?work=019ec72b…` → the derivative STUDIO opened (banner "You are writing a dị bản · Adapting from canon · branches at chapter 2") — proving the deep-link lands in THAT specific dị bản, not the canon. M6 screenshots docs/raid/evidence/cycle-28/{m6-living-world-tree,m6-branch-click-lands-in-work}.png. Adversary (cold-start) 0 blk/2 maj(BOTH FIXED+reverified: (1) navigation dropped the Work identity → all branches landed on the same /books/{id} showing the canon → added `?work=<id>` selector + wired CompositionPanel to seed activeWorkOverride from it [4 deep-link tests + live dị bản-studio proof]; (2) worksByBook memo keyed on `[object Object]`-stringified query data → stale on re-fetch → content-digest key)/2 min(BOTH FIXED: multi-trunk layout row-overlap → per-node row band; weak counts-substring test → full interpolated-string assert)/11 notes; Scope Guard QC + POST-REVIEW both CLEAR. |
