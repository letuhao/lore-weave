# RAID Cycle Log — lore-enrichment

> Task `lore-enrichment` (slug `2026-05-30-lore-enrichment`). One row per cycle as RAID executes. See [cycle decomposition](../plans/2026-05-30-lore-enrichment/CYCLE_DECOMPOSITION.md).
>
> **Schema note:** column 1 is the BARE cycle number (0–18) and column 3 is the single-word Status — this is the contract `scripts/raid/coordinator-helper.py` parses (`| <num> | <title> | <status> |`) and that `done-cycle` flips PENDING→DONE. Do not prefix the number with `C` or reorder these three columns.

| Cycle | Title | Status | DPS | Commit | Verify / notes |
|---|---|---|---|---|---|
| 0 | Bootstrap service skeleton | DONE | 1 | 9272ffbc | live smoke /health 200 ok (RestartCount=0); pytest 3/3; gateway nest build + jest 9/9. default+AMAW, not /raid. |
| 1 | KG-read port + verifies | DONE | 1 | 399f6711 | verify-cycle-1.sh exit 0; live smoke: read graph-stats from running knowledge-service (HTTP 401, reachability+scoping confirmed); 27 pytest pass; adversary 0blk/0maj/1min/3note; scope-guard CLEAR. In-place fallback (worktree base-branch mismatch; Agent tool unavailable). H1/H2/M4 recorded in docs/raid/findings/C1-verifies.md |
| 2 | Data model + H0 | DONE | 2 | f7b5b38a | verify-cycle-2.sh exit 0; 5 tables in loreweave_lore_enrichment (bare-SQL run_migrations, platform convention not alembic); H0 enforced: confidence CHECK(>0 AND <1.0), origin NOT NULL+immutable trigger, lifecycle DAG trigger (proposed→author_reviewing→approved→promoted\|rejected), promote-only invariant, permanent origin markers (promoted_from_proposal_id/original_technique). 11 real-DB tests (H0 round-trip + illegal-jump + up/down idempotency) + 27 unit pass. Live: container lifespan applied migrations on restart, /health 200, RestartCount=0. adversary 0blk/0maj/2min; scope-guard CLEAR. In-place (worktrees/Agent-spawn unavailable). |
| 3 | API contract freeze | DONE | 2 | 39069541 | verify-cycle-3.sh exit 0; OpenAPI 3.1 spec (4 families jobs/proposals/sources/templates + H0 author promote) spectral-lint clean; 17 stub routes mounted, lists 200 spec-valid empty shapes + actions 501 (never 404/500); H0 in contract: confidence exclusiveMaximum 1.0, required origin/review_status, dedicated promote endpoint as ONLY canon path + PromoteResult permanent origin markers (promoted_from_proposal_id/by/at/original_technique), promote carries Principal (not anonymous), 403 author-only documented; no hardcoded model names/URLs; Q1 approve/reject/edit mirror pending_facts; Q3 project_id scope params. 7 new contract tests + 27 prior = 34 unit pass (DB tests need compose). Live: rebuilt+restarted container, /health 200 RestartCount=0 healthy, curled list 200 + promote 501 via host :8221. adversary 0blk/0maj/0min; scope-guard CLEAR. In-place (worktrees/Agent-spawn unavailable). |
| 4 | PLATFORM K14 event pipeline | DONE | 2 | e4a0e867 | verify-cycle-4.sh exit 0; **live smoke: glossary entity written via bulk extract-entities auto-propagated glossary→outbox→relay→Redis stream loreweave:events:glossary→knowledge-service consumer→glossary_sync→Neo4j node (H1 confirmed; relay relayed count=1, consumer `C4: glossary.entity_updated synced action=created`, `K15.11 glossary entity created in Neo4j`)**. ADDITIVE/backward-compat: glossary-service new outbox_events table (mirrors book-service, aggregate_type='glossary') + emit on single create (in-tx transactional outbox), patch (best-effort post-commit), and bulk extract-entities (one event PER created/updated entity, skipped excluded); knowledge-service new handle_glossary_entity_updated triggers EXISTING sync_glossary_entity_to_neo4j (Q2: never writes Neo4j directly; MERGE keyed on (user_id,glossary_entity_id) → idempotent on replay). worker-infra OUTBOX_SOURCES += glossary (env-only; relay+stream MAXLEN glossary already supported, no worker-infra code change). 4 new glossary emit tests + 6 new knowledge consumer tests; glossary go test ./... green; knowledge 1830 unit pass (no regression). adversary 0blk/0maj/1min (pre-existing worker-infra outbox-cleanup `$1::int||' days'` encode bug, NOT introduced here, daily cleanup only — relay unaffected); scope-guard CLEAR. In-place (worktrees/Agent-spawn unavailable). |
| 5 | PLATFORM D4-03 wiki-from-KG | DONE | 2 | | verify-cycle-5.sh exit 0; **live smoke: entity → generated wiki body persisted (canon+enriched)** — seeded glossary entity + book + tiny synthetic Neo4j KG fixture (1 canon RELATES_TO conf=1.0 validated, 1 enriched RELATES_TO conf=0.6 pending), real cross-service flow glossary `/wiki/generate`→knowledge-service `POST /internal/knowledge/wiki-neighborhood`→Neo4j→deterministic renderer→`wiki_articles` body persisted; H0 confirmed end-to-end (canon edge under 关系 plain; enriched edge under 补充关系（增补·待校验）with `source_type='enriched'` attrs + 【增补】prefix + unverified disclaimer). ADDITIVE/backward-compat: replaced empty `generateWikiStubs` `{}` body with KG→prose renderer (kept owner-auth/verifyBookOwner, per-book scoping, kind_codes/limit filter, skip-existing). knowledge-service: new read-only `get_neighborhood_by_glossary_id` (run_read; Q2 — no Neo4j write) + `internal_wiki.py` router (source_type derived server-side: pending OR conf<1.0 → enriched; any enriched marker on entity → enriched, fail-safe). glossary-service: new `knowledge_client.go` (graceful degrade to nil/minimal body if KG down/unconfigured — Q6) + `wiki_render.go` deterministic TipTap renderer (no LLM, no hardcoded model). 9 new go renderer tests + 10 new py source_type tests; glossary `go test ./...` green; knowledge unit 1840 pass (+10, no regression); gofmt/vet/ruff clean. adversary 0blk/0maj/0min; scope-guard CLEAR. Deferral D-044: wire `KNOWLEDGE_SERVICE_URL` in compose at merge-time (live-smoke used a temp override). In-place (worktrees/Agent-spawn unavailable). |
| 6 | Gap MODEL spec (M1a) | DONE | 2 | 8bd4834d | verify-cycle-6.sh exit 0; SPEC + typed MODEL + Fengshen fixtures (no engine — that is C7). app/gaps/model.py: EntityKind (extensible, only LOCATION='location' modeled — matches C2 entity_kind vocab; CHARACTER/ITEM/FACTION reserved, no speculative dims), Dimension set 历史/地理/文化/features/inhabitants (3 required Chinese-labelled + 2 optional English per lock), DimensionSpec frozen weight table (required=3.0, optional=2.0), Gap (PURE DATA — describes ABSENCE only, NO content/source_type/confidence/proposal per H0), deterministic rank_score = (REQUIRED_BONUS*missing_required + Σ weight(missing)) * log-damped salience, round(6); rank_gaps total order (-score, canonical_name). docs/gap_model.md spec (5 dims + gap def + ranking formula + pinned table). tests/fixtures/gaps_fengshen.json: 4 locked LOCATIONs (玉虛宮/碧遊宮·金鰲島/蓬萊/陳塘關) classified present/missing from canon — pinned golden order 蓬萊>玉虛宮>碧遊宮>陳塘關 (蓬萊 #1: missing all 5 > 玉虛宮 missing 4-of-5 despite higher salience). 24 new gap-model tests (schema/determinism/pinned-ordering+scores/H0-purity/no-IO-imports/no-model-names) + 58 full suite pass, 11 DB-skip; ruff clean. adversary 0blk/0maj/0min/2note (both fixed in-place: dead weight_by_dim dict removed; fixture-meta order corrected to deterministic output pre-commit); scope-guard CLEAR. Single-service pure-data → no cross-service live-smoke. In-place (worktrees/Agent-spawn unavailable). |
| 7 | Gap-detection engine (M1b) | DONE | 2 | | verify-cycle-7.sh exit 0; ENGINE on top of the C6 data model (no live KG). app/gaps/engine.py: `EntityCoverage` (per-entity KG coverage input: kind/name/target_ref/mention_count + present_dimensions tuple) → `GapDetectionEngine` derives missing = dimension_set − present via C6 `dimensions_for` (iterated in frozen enum-declaration order so derived tuples are deterministic), skips fully-described entities (None, no Gap-validator raise), builds typed `Gap`, ranks via C6 `rank_gaps` (engine adds ZERO ranking logic). `detect()→list[Gap]`, `detect_ranked()→list[GapRanking]`, async `detect_ranked_for_project(port,…)` Q6 entrypoint: reads graph-stats through C1 `KnowledgeReadPort`; empty/zero graph (Null port, empty-stats, or HTTP port degraded to empties) → `[]`, never raises. LLM-free + DB-write-free (reads only through the C1 port). Golden NOT reverse-engineered: tests feed ONLY each fixture entry's present_dimensions; engine reproduces the C6 fixture `_meta.expected_ranking_order` 蓬萊>玉虛宮>碧遊宮>陳塘關 + exact `expected_scores` (computed by the C6 model in cycle 6). 18 new engine tests (derivation/no-gap-for-complete/pinned-ranked-order+scores/determinism+input-order-independence/tie-break/Q6 null+empty+nonempty port/LLM-free+write-free boundary) + 58 prior = 76 unit pass, 11 DB-skip; ruff clean on app/gaps/. adversary 0blk/0maj/0min/2note (brief path `app/gap`→kept C6's `app/gaps`; corrected a latent CJK-collation assumption in own tie-break test to assert the genuine code-point ordering — both resolved in-place pre-commit); scope-guard CLEAR. Not cross-service (C1 port exercised via Null/mock impl) → no live-smoke token. In-place (worktrees/Agent-spawn unavailable). |
| 8 | Strategy core | PENDING | 2 | | dep C2,C3; interface+registry+cost guardrail |
| 9 | Strategy (a) template | PENDING | 2 | | dep C7,C8; P1 template scaffolding |
| 10 | Strategy (b) retrieval | PENDING | 2 | | dep C8,C9; reuse knowledge-service embed; cross-service live-smoke |
| 11 | Schema-gov gen + H0 tag | PENDING | 2 | | dep C9,C10; origin/provenance tagging |
| 12 | Canon-verify | PENDING | 2 | | dep C11; contradiction+anachronism+injection-defense |
| 13 | Review gate + write-back (H0) | PENDING | 2 | | dep C4,C5,C12; promote→canon; cross-service live-smoke |
| 14 | Job orchestration (DEMO) | PENDING | 2 | | dep C13; full P1 e2e on Fengshen; cross-service live-smoke |
| 15 | Eval + gate | PENDING | 2 | | dep C14; judge-ensemble; gate blocks below threshold |
| 16 | Strategy (c) fabrication | PENDING | 2 | | dep C15; P2 behind gate |
| 17 | Strategy (d) re-cook | PENDING | 2 | | dep C15,C16; P3 behind gate + licensing |
| 18 | Productionize | PENDING | 2 | | dep C14; observability + runbook + secret-scan |
