# RAID Cycle Log — lore-enrichment

> Task `lore-enrichment` (slug `2026-05-30-lore-enrichment`). One row per cycle as RAID executes. See [cycle decomposition](../plans/2026-05-30-lore-enrichment/CYCLE_DECOMPOSITION.md).
>
> **Schema note:** column 1 is the BARE cycle number (0–18) and column 3 is the single-word Status — this is the contract `scripts/raid/coordinator-helper.py` parses (`| <num> | <title> | <status> |`) and that `done-cycle` flips PENDING→DONE. Do not prefix the number with `C` or reorder these three columns.

| Cycle | Title | Status | DPS | Commit | Verify / notes |
|---|---|---|---|---|---|
| 0 | Bootstrap service skeleton | DONE | 1 | 9272ffbc | live smoke /health 200 ok (RestartCount=0); pytest 3/3; gateway nest build + jest 9/9. default+AMAW, not /raid. |
| 1 | KG-read port + verifies | DONE | 1 | 399f6711 | verify-cycle-1.sh exit 0; live smoke: read graph-stats from running knowledge-service (HTTP 401, reachability+scoping confirmed); 27 pytest pass; adversary 0blk/0maj/1min/3note; scope-guard CLEAR. In-place fallback (worktree base-branch mismatch; Agent tool unavailable). H1/H2/M4 recorded in docs/raid/findings/C1-verifies.md |
| 2 | Data model + H0 | DONE | 2 | f7b5b38a | verify-cycle-2.sh exit 0; 5 tables in loreweave_lore_enrichment (bare-SQL run_migrations, platform convention not alembic); H0 enforced: confidence CHECK(>0 AND <1.0), origin NOT NULL+immutable trigger, lifecycle DAG trigger (proposed→author_reviewing→approved→promoted\|rejected), promote-only invariant, permanent origin markers (promoted_from_proposal_id/original_technique). 11 real-DB tests (H0 round-trip + illegal-jump + up/down idempotency) + 27 unit pass. Live: container lifespan applied migrations on restart, /health 200, RestartCount=0. adversary 0blk/0maj/2min; scope-guard CLEAR. In-place (worktrees/Agent-spawn unavailable). |
| 3 | API contract freeze | DONE | 2 | 39069541 | verify-cycle-3.sh exit 0; OpenAPI 3.1 spec (4 families jobs/proposals/sources/templates + H0 author promote) spectral-lint clean; 17 stub routes mounted, lists 200 spec-valid empty shapes + actions 501 (never 404/500); H0 in contract: confidence exclusiveMaximum 1.0, required origin/review_status, dedicated promote endpoint as ONLY canon path + PromoteResult permanent origin markers (promoted_from_proposal_id/by/at/original_technique), promote carries Principal (not anonymous), 403 author-only documented; no hardcoded model names/URLs; Q1 approve/reject/edit mirror pending_facts; Q3 project_id scope params. 7 new contract tests + 27 prior = 34 unit pass (DB tests need compose). Live: rebuilt+restarted container, /health 200 RestartCount=0 healthy, curled list 200 + promote 501 via host :8221. adversary 0blk/0maj/0min; scope-guard CLEAR. In-place (worktrees/Agent-spawn unavailable). |
| 4 | PLATFORM K14 event pipeline | DONE | 2 | e4a0e867 | verify-cycle-4.sh exit 0; **live smoke: glossary entity written via bulk extract-entities auto-propagated glossary→outbox→relay→Redis stream loreweave:events:glossary→knowledge-service consumer→glossary_sync→Neo4j node (H1 confirmed; relay relayed count=1, consumer `C4: glossary.entity_updated synced action=created`, `K15.11 glossary entity created in Neo4j`)**. ADDITIVE/backward-compat: glossary-service new outbox_events table (mirrors book-service, aggregate_type='glossary') + emit on single create (in-tx transactional outbox), patch (best-effort post-commit), and bulk extract-entities (one event PER created/updated entity, skipped excluded); knowledge-service new handle_glossary_entity_updated triggers EXISTING sync_glossary_entity_to_neo4j (Q2: never writes Neo4j directly; MERGE keyed on (user_id,glossary_entity_id) → idempotent on replay). worker-infra OUTBOX_SOURCES += glossary (env-only; relay+stream MAXLEN glossary already supported, no worker-infra code change). 4 new glossary emit tests + 6 new knowledge consumer tests; glossary go test ./... green; knowledge 1830 unit pass (no regression). adversary 0blk/0maj/1min (pre-existing worker-infra outbox-cleanup `$1::int||' days'` encode bug, NOT introduced here, daily cleanup only — relay unaffected); scope-guard CLEAR. In-place (worktrees/Agent-spawn unavailable). |
| 5 | PLATFORM D4-03 wiki-from-KG | DONE | 2 | | verify-cycle-5.sh exit 0; **live smoke: entity → generated wiki body persisted (canon+enriched)** — seeded glossary entity + book + tiny synthetic Neo4j KG fixture (1 canon RELATES_TO conf=1.0 validated, 1 enriched RELATES_TO conf=0.6 pending), real cross-service flow glossary `/wiki/generate`→knowledge-service `POST /internal/knowledge/wiki-neighborhood`→Neo4j→deterministic renderer→`wiki_articles` body persisted; H0 confirmed end-to-end (canon edge under 关系 plain; enriched edge under 补充关系（增补·待校验）with `source_type='enriched'` attrs + 【增补】prefix + unverified disclaimer). ADDITIVE/backward-compat: replaced empty `generateWikiStubs` `{}` body with KG→prose renderer (kept owner-auth/verifyBookOwner, per-book scoping, kind_codes/limit filter, skip-existing). knowledge-service: new read-only `get_neighborhood_by_glossary_id` (run_read; Q2 — no Neo4j write) + `internal_wiki.py` router (source_type derived server-side: pending OR conf<1.0 → enriched; any enriched marker on entity → enriched, fail-safe). glossary-service: new `knowledge_client.go` (graceful degrade to nil/minimal body if KG down/unconfigured — Q6) + `wiki_render.go` deterministic TipTap renderer (no LLM, no hardcoded model). 9 new go renderer tests + 10 new py source_type tests; glossary `go test ./...` green; knowledge unit 1840 pass (+10, no regression); gofmt/vet/ruff clean. adversary 0blk/0maj/0min; scope-guard CLEAR. Deferral D-044: wire `KNOWLEDGE_SERVICE_URL` in compose at merge-time (live-smoke used a temp override). In-place (worktrees/Agent-spawn unavailable). |
| 6 | Gap MODEL spec (M1a) | DONE | 2 | 8bd4834d | verify-cycle-6.sh exit 0; SPEC + typed MODEL + Fengshen fixtures (no engine — that is C7). app/gaps/model.py: EntityKind (extensible, only LOCATION='location' modeled — matches C2 entity_kind vocab; CHARACTER/ITEM/FACTION reserved, no speculative dims), Dimension set 历史/地理/文化/features/inhabitants (3 required Chinese-labelled + 2 optional English per lock), DimensionSpec frozen weight table (required=3.0, optional=2.0), Gap (PURE DATA — describes ABSENCE only, NO content/source_type/confidence/proposal per H0), deterministic rank_score = (REQUIRED_BONUS*missing_required + Σ weight(missing)) * log-damped salience, round(6); rank_gaps total order (-score, canonical_name). docs/gap_model.md spec (5 dims + gap def + ranking formula + pinned table). tests/fixtures/gaps_fengshen.json: 4 locked LOCATIONs (玉虛宮/碧遊宮·金鰲島/蓬萊/陳塘關) classified present/missing from canon — pinned golden order 蓬萊>玉虛宮>碧遊宮>陳塘關 (蓬萊 #1: missing all 5 > 玉虛宮 missing 4-of-5 despite higher salience). 24 new gap-model tests (schema/determinism/pinned-ordering+scores/H0-purity/no-IO-imports/no-model-names) + 58 full suite pass, 11 DB-skip; ruff clean. adversary 0blk/0maj/0min/2note (both fixed in-place: dead weight_by_dim dict removed; fixture-meta order corrected to deterministic output pre-commit); scope-guard CLEAR. Single-service pure-data → no cross-service live-smoke. In-place (worktrees/Agent-spawn unavailable). |
| 7 | Gap-detection engine (M1b) | DONE | 2 | | verify-cycle-7.sh exit 0; ENGINE on top of the C6 data model (no live KG). app/gaps/engine.py: `EntityCoverage` (per-entity KG coverage input: kind/name/target_ref/mention_count + present_dimensions tuple) → `GapDetectionEngine` derives missing = dimension_set − present via C6 `dimensions_for` (iterated in frozen enum-declaration order so derived tuples are deterministic), skips fully-described entities (None, no Gap-validator raise), builds typed `Gap`, ranks via C6 `rank_gaps` (engine adds ZERO ranking logic). `detect()→list[Gap]`, `detect_ranked()→list[GapRanking]`, async `detect_ranked_for_project(port,…)` Q6 entrypoint: reads graph-stats through C1 `KnowledgeReadPort`; empty/zero graph (Null port, empty-stats, or HTTP port degraded to empties) → `[]`, never raises. LLM-free + DB-write-free (reads only through the C1 port). Golden NOT reverse-engineered: tests feed ONLY each fixture entry's present_dimensions; engine reproduces the C6 fixture `_meta.expected_ranking_order` 蓬萊>玉虛宮>碧遊宮>陳塘關 + exact `expected_scores` (computed by the C6 model in cycle 6). 18 new engine tests (derivation/no-gap-for-complete/pinned-ranked-order+scores/determinism+input-order-independence/tie-break/Q6 null+empty+nonempty port/LLM-free+write-free boundary) + 58 prior = 76 unit pass, 11 DB-skip; ruff clean on app/gaps/. adversary 0blk/0maj/0min/2note (brief path `app/gap`→kept C6's `app/gaps`; corrected a latent CJK-collation assumption in own tie-break test to assert the genuine code-point ordering — both resolved in-place pre-commit); scope-guard CLEAR. Not cross-service (C1 port exercised via Null/mock impl) → no live-smoke token. In-place (worktrees/Agent-spawn unavailable). |
| 8 | Strategy core | DONE | 2 | | verify-cycle-8.sh exit 0; the plugin framework all techniques register into — `EnrichmentStrategy` ABC (key/tier derive from `Technique`; `estimate_cost`+`run` signature-only, no body, H0: `run` returns proposals later, never canon) + `StrategyRegistry` (register-by-key; `select`/`list_active` flag-gated — registered-but-inactive raises `InactiveStrategyError` DISTINCT from `UnknownStrategyError`, so P2/P3 can't leak; `list_registered` is ops-only, non-selectable) + config-driven `FeatureFlags` (Q-R2: DEFAULT_ACTIVE derived from tier table = P1 `template`+`retrieval` ON; P2 `fabrication`/P3 `recook` OFF until C15 gate; env override fails CLOSED on garbage token) + per-job `CostGuardrail` (`would_exceed`=`spent+next>cap` STRICT, ==cap allowed, checked BEFORE charge; `charge_or_pause` pauses job(cost_cap) BEFORE overrun, never retroactive; unit-opaque, no currency) + `JobStateMachine` (states == C2 `enrichment_job.status` vocab; legal DAG pending→estimating→running⇄paused→completed\|cancelled\|failed; illegal transitions RAISE no silent no-op; persisted==in-memory via persist-then-advance + rollback on persist failure). 43 new tests (registry active/inactive/unknown + flags + state legal/illegal + cost-cap boundary/pause) + 76 prior = 119 pass, 11 db-skip; ruff clean; NO model names, NO migrations, NO LLM/HTTP imports, NO `source_type='glossary'`/confidence=1.0. adversary 0blk/0maj/0min/1note (unreachable `float('inf')` cost branch → invariant assert, fixed in-place pre-commit); scope-guard CLEAR. Single-service in-process → no cross-service live-smoke. In-place (worktrees/Agent-spawn unavailable). |
| 9 | Strategy (a) template | DONE | 2 | | verify-cycle-9.sh exit 0; P1 template scaffolding — the first concrete `EnrichmentStrategy`. app/strategies/template.py: `TemplateStrategy(EnrichmentStrategy)` (technique=TEMPLATE, tier P1) + `ScaffoldedProposal` (frozen pydantic). `run(gaps,ctx)` → one EMPTY proposal SKELETON per gap: one slot per MISSING dimension, KEYS = C6 `DimensionSpec.label` (历史/地理/文化 source-faithful Chinese; features/inhabitants English per the LOCKED C6 set) derived LIVE from `dimensions_for(kind)` (single source of truth, no drift), VALUES empty placeholder `""` (NOT English stubs — `is_empty_scaffold()` asserts). NO LLM, NO retrieval, NO model names, NO migrations — pure deterministic in-memory scaffolding (estimate_cost=0, units=gap_count). H0 by construction (model defaults, caller can't flip to canon): origin='enrichment'(≠'glossary'), technique='template', review_status='proposed', 0<confidence<1.0 (=0.01 SCAFFOLD_CONFIDENCE — DB CHECK is `>0 AND <1.0`, cannot be exactly 0, so near-zero positive denotes empty-shell low confidence), pending_validation=True, provenance_json records technique+source_gap. Q3 scope (user_id/project_id) carried gap→proposal. Registered into C8 registry under 'template' (P1 flag ON → selectable; ENRICH_STRATEGY_TEMPLATE_ENABLED=0 → InactiveStrategyError). 19 new C9 tests (shape/one-slot-per-missing/Chinese-keys/empty-not-stub/H0-negative-assert/scope-preserved/registry+flag/determinism/no-LLM-no-model) + 119 prior = 138 pass, 11 db-skip; ruff clean. adversary 0blk/0maj/0min/1note (confidence-floor 0.01 vs schema `>0`: documented near-zero compromise, not exact 0); scope-guard CLEAR. Single-service in-process scaffolding → no cross-service live-smoke. In-place (worktrees/Agent-spawn unavailable). |
| 10 | Strategy (b) retrieval | DONE | 2 | | verify-cycle-10.sh exit 0; **live smoke: real bge-m3 embed+retrieve round-tripped** — ingested 2 山海经 chunks via REAL provider-registry `/internal/embed` (model_ref resolved by NAME at runtime; JIT load-race tolerated w/ retry), retrieved 蓬萊 chunk top-1 by cosine (score 0.7992). REUSES C1 `KnowledgeClient.embed` (no new RAG framework, no langchain/llamaindex, no heavy dep). New `app/retrieval/`: chunker.py (deterministic CJK-aware sentence-window chunker, byte-safe, stable 0-based chunk index + sha256), store.py (`SourceCorpusStore` idempotent ingest: ON CONFLICT(corpus_id,chunk_index) DO NOTHING → re-ingest 0 inserts/0 re-embeds; embeddings as DOUBLE PRECISION[]; in-process cosine `top_k` — no pgvector; project-scoped Q3), embedding.py (the ONE seam binding C1 client → model_ref, model NAME never in code), strategy.py (`RetrievalStrategy` technique=retrieval P1: embeds gap query → top-K → populates `cultural_grounding_ref` (corpus_id+chunk_ids+scores) on H0 `GroundedProposal`). C2 migration += `source_corpus_chunk` (embedding_model_ref + embedding_dim drift guards; CASCADE on corpus). H0 by construction (origin=enrichment, technique=retrieval, review_status=proposed, 0<confidence<1.0=0.05, pending_validation; NO glossary/KG write, NO canon). 27 new C10 unit + 3 new DB tests; 179 unit pass + 14 DB pass (real Postgres). ruff clean. adversary 0blk/0maj/0min/2note (registry-wiring deferred to C14; live-smoke harness env-default model NAME is test-only/configurable); scope-guard CLEAR (only lore-enrichment-service + verify-cycle-10.sh; knowledge-service READ-only). In-place (worktrees/Agent-spawn unavailable). |
| 11 | Schema-gov gen + H0 tag | DONE | 2 | | verify-cycle-11.sh exit 0; THE H0 enforcement point — schema-governed GENERATION of the C9 scaffold's empty Chinese dimensions from C10 retrieved grounding, normalized-repaired to a game-ready schema, every fact origin-tagged. New `app/generation/`: provenance.py (the H0 CHOKEPOINT — `EnrichedFact` + `make_enriched_fact` factory: origin∈{enriched, enriched:<technique>} (validator rejects 'glossary'/blank/arbitrary), provenance NON-empty (records technique + provider-registry model_ref [NEVER a model name] + C10 grounding_ref_ids + timestamp), confidence `gt=0,lt=1.0` (default 0.30 > C10 0.05 floor, never 1.0), source_refs NON-empty, pending_validation==True, review_status=='proposed' — all REQUIRED+validated so an untagged/canon-looking fact is impossible to construct), repair.py (normalization-repair: strips fences/prose, tolerates trailing commas, coerces scalars/lists, drops extra keys BUT RECORDS them in RepairReport [no silent loss], catches English-leakage in Chinese dims via Latin-letter+cjk_ratio guard → repaired-or-typed-`RepairError`, NEVER silent drop / None / []), generate.py (`SchemaGovernedGenerator`: prompt[Chinese, cites grounding, no model name] → injected `CompleteFn` LLM seam [model_ref via provider-registry, real binding deferred to C14 like C10] → repair → one H0 `EnrichedFact` per missing dim via the factory; refuses to generate when grounding empty [unprovenanced=H0 violation]). H0 by construction (mirrors C2 enrichment_proposal columns origin/technique/provenance_json/confidence/source_refs_json). 88 new C11 tests (test_provenance_h0 + test_generation_repair malformed-corpus + test_generation mocked-LLM pipeline) + 165 prior = 253 pass, 14 db-skip; ruff clean. **self-adversary 0blk/2maj-FIXED/0min**: A1 `model_construct()` and A6 `model_copy(update=)` both bypassed pydantic validators → could mint origin='glossary'/confidence=1.0 → CLOSED (model_construct forbidden + model_copy revalidates on update); A2-A8 (factory rejects conf>=1.0/empty refs, repair no-silent-drop, boundary, origin-spoof) clean. scope-guard CLEAR (only lore-enrichment-service + verify-cycle-11.sh + docs/raid; NO write-back/C12/C13/orchestration; LLM seam mocked → no live LM Studio hit). Cross-service=NO → no live-smoke token required (unit + mocked sufficient per brief). In-place (worktrees/Agent-spawn unavailable). | dep C9,C10; origin/provenance tagging |
| 12 | Canon-verify | DONE | 2 | | verify-cycle-12.sh exit 0; M2 CONSISTENCY annotator at proposal creation (NOT correctness — H0: human PROMOTE gate owns correctness). New `app/verify/`: sanitize.py (injection-defense MIRRORING knowledge-service pending_facts/Q1: tag-not-delete `[FICTIONAL]`, idempotent fixed-width lookbehind, scan-then-tag, CJK-safe; multilingual zh/ja/ko/vi + chat-template/role markers; PRE-normalizes NFKC+invisible-strip so 全角/zero-width/bidi evasions fold before the phrase scan — extends C1 app/clients/sanitize.py), canon_verify.py (`CanonVerifier` → typed `VerifyResult{flags[],verify_degraded,neutralized}` + `VerifyFlag{kind,dimension,evidence,severity}`: (a) CONTRADICTION — canon read via C1 KnowledgeReadPort reachability + injected CanonLookupFn over glossary/KG SSOT; canon-term + negation-marker (zh 不是/并非/实为… + en is-not/never) heuristic; **Q6 graceful-degradation: KG empty/down OR malformed project_id → verify_degraded=True, NEVER auto-pass (no false-green)**; (b) ANACHRONISM — operates on CHINESE content vs locked 商周/封神 frame via curated out-of-era CONCEPT marker table (later dynasties/modern-tech/foreign-faiths; data not English wordlist; flags carry evidence+reason); (c) INJECTION — neutralize multi-field: canonical_name + every dimension label + every content value + every grounding excerpt; hit → flag(HIGH)+record SAFE text), wiring.py (`verify_and_annotate` proposal-creation seam → `AnnotatedVerify` w/ provenance_patch under `provenance_json['canon_verify']` + `verify_status`∈{verified_clean,needs_review,quarantined,degraded} — **EVERY status keeps proposal quarantined; H0: no status admits canon, review_status stays 'proposed', no source_type/confidence/pending_validation touched, no write-back**). 24 C12 unit tests (a/b/c/d + KG-unavailable-degraded + malformed-id-degraded + canon-lookup-failure + multi-field injection + 全角/zero-width/idempotent/CJK-safe neutralize + H0 no-mutate/no-canon-marker/every-status-quarantined + no-model-name/no-http-import guards) + 251 prior = 275 pass, 14 db-skip; ruff clean. **self-adversary 0blk/0maj/1min-FIXED**: bare `电` anachronism marker false-positived on era-appropriate 雷电/电光 (Classical thunder/lightning) → removed, replaced w/ compound 电灯/电力 + regression test (over-reach guard); injection evasions (newline-split, role-spoof, bidi, full-width-English) all caught; contradiction is intentionally CONSERVATIVE (M2 = flag-for-human, not auto-reject — FP only raises review priority, never harms H0). scope-guard CLEAR (only lore-enrichment-service/app/verify + test + verify-cycle-12.sh + docs/raid; NO promote/write-back C13, NO glossary/KG/world-service/climate-geo edits, NO new RAG dep, NO hardcoded model). Cross-service=NO (KG reads mocked at C1 port boundary; live KG verify is C13/C14) → no live-smoke token required per brief. In-place (worktrees/Agent-spawn unavailable). | dep C11; contradiction+anachronism+injection-defense |
| 13 | Review gate + write-back (H0) | DONE | 1 | 62b84bfe | verify-cycle-13.sh exit 0; **live smoke: propose → enriched-in-KG (quarantined, source_type=enriched:template, pending=true, confidence<1.0) → author promote → canon (source_type=glossary, conf=1.0, pending=false) WITH permanent origin marker intact (origin=enrichment, promoted_by, original_technique) → retract → soft (valid_until)** on seeded demo 蓬萊 through the live glossary→KG pipeline. Review API replaces C3 stubs: list/approve/reject/edit (mirror pending_facts Q1) + author-only `promote` + `write-back` + `retract`(M6). H0 AIRTIGHT: write-back enters KG QUARANTINED via NEW knowledge-service `/internal/knowledge/enriched-{writeback,promote,retract}` (enriched `:Fact`+`:RELATES_TO` tagged source_type=enriched:<technique>, pending, conf<1.0 — distinct from canon 蓬萊 node); ONLY author promote (verified vs book-service `/projection` owner_user_id — NOT a client claim) flips to source_type=glossary/conf=1.0/pending=false while RETAINING origin marker on both proposal row + KG facts; retract = glossary recycle-bin soft-delete + KG valid_until (reversible). Lifecycle state machine (proposed→author_reviewing→approved→promoted\|rejected) enforced in-app + DB trigger (defence-in-depth); illegal jumps→409; idempotent promote (re-call same entity=no-op, no dup canon). Q2: entity ANCHOR through glossary SSOT extract-entities; **self-adversary A1-FIXED: write-back writes IDENTITY-ONLY (no enriched content on canon attr pre-promote) — content flows to glossary canon ONLY on promote**; defense-in-depth (deferred 050): all proposal/LLM text injection-neutralized before crossing a service boundary. lore-enrichment 311 pass (real DB +6 new C13 DB tests: scoping/promote-retains-origin/illegal-transition/idempotent/edit-non-canon) + 16 new unit (H0 a/b/c/d/e boundary); knowledge-service 1853 unit pass (+11 C13) no regression; ruff clean; OpenAPI spectral clean (+write-back/retract/WriteBackRequest/Result schemas). adversary 0blk/1maj-FIXED/1note; scope-guard CLEAR (lore-enrichment primary + ADDITIVE knowledge internal router; NO C14/eval/world-service/prod). In-place (worktrees/Agent-spawn unavailable). |
| 14 | Job orchestration (DEMO) | DONE | 1 | | verify-cycle-14.sh exit 0; **live smoke: full P1 job on Fengshen 蓬萊 → quarantined enriched proposal → review → author promote → write-back to glossary observed** (REAL Qwen 3.6 generation via provider-registry). End-to-end job RUNNER orchestrates C7–C13 into one pipeline (NO new pipeline logic): estimate→start→per-gap[cost-cap charge_or_pause→retrieval(C10)→schema-gov gen(C11, real Qwen by model_ref)→canon-verify(C12)→persist quarantined H0 proposal]→complete, with Redis Streams lifecycle events + cost-cap incl. reserved eval-cost line (M5). **DEMO round-trip live-verified on seeded 蓬萊**: gap (5 dims missing) → 山海经 grounding → real Qwen generated source-faithful Chinese for all 5 dims (历史/地理/文化/features/inhabitants; e.g. 文化「宫室皆以金玉为之，具仙家琼阁之制」) → H0 proposal (origin=enrichment, conf=0.30<1.0, review_status=proposed, pending_validation, 山海经 provenance) → approve → author PROMOTE → write-back to glossary SSOT (5 facts promoted, glossary entity created) WITH permanent origin marker intact (origin=enrichment, original_technique=retrieval, promoted_by=demo user); 4 Redis events emitted, 0 failures. New `app/jobs/`: runner.py (JobRunner: C8 state machine pending→estimating→running→completed\|paused\|failed; pause-on-cap RESUMABLE never crash; ungroundable gap SKIPPED not failed [H0: no unprovenanced fact]; unexpected error→failed), stages.py (GapPipeline: per-gap C10→C11→C12 wiring), events.py (JobEventEmitter: idempotent producer dedupe per job+stage+gap, consumer-group-safe XADD field-map, best-effort — Redis down NEVER fails the job), cost.py (JobCostBudget: wraps C8 CostGuardrail + reserved eval-line M5 held out of working cap; breach pauses, eval reserve protected), proposal_store.py (Pg + InMemory ProposalStore: the missing INSERT path; build_proposal_fields H0-stamps origin=enrichment/conf<1.0/proposed, folds C11 dims + C12 verify into provenance_json), assembly.py (live wiring root). New `app/generation/complete.py` (real C11 CompleteFn → provider-registry /internal/llm/stream by model_ref, collect token deltas; deferred from C11 like C10's embed). API: jobs router now REAL — POST /jobs runs P1 e2e + list/get + start/pause/resume/cancel (C8 transitions); CreateJobRequest spec extended additively (embedding/generation_model_ref + targets + eval_reserve_fraction + top_k); spectral clean. NO hardcoded model names (Qwen/bge-m3 resolved via provider-registry model_ref; demo Qwen owned by demo user); NO P2/P3 (registry flags untouched); NO eval scoring (only RESERVE the line); write-back via glossary SSOT only (Q2). 25 new tests (runner stage-chaining/pause-on-cap/skip/fail/H0-every-proposal + events idempotent/best-effort/CJK + cost eval-reserve/off-by-one/misconfig + SSE collector + persist H0-guard); lore-enrichment 322 pass / 22 db-skip (no C0–C13 regression); ruff clean; secret/model-name scan clean. self-adversary 0blk/0maj/2min: (1) cross-run crash-resume relies on consumer dedupe_key (producer idempotent within a run; seen_keys resume-seed seam exists, unused by runner) → Track-2 D-C14-RESUME-SEED; (2) partial-resume re-run would re-INSERT proposals, but C14 has no partial-resume entry (single-shot run_job; lifecycle resume doesn't re-run pipeline) → not reachable in C14 scope. scope-guard CLEAR. In-place (worktrees/Agent-spawn unavailable). **C14-FIX (adversary BLOCK1/WARN1, cold-start):** BLOCK1 — the per-job cost-cap was INERT in production: `assembly.py` wired `cost_strategy=TemplateStrategy()` whose estimate is the FREE scaffold (cost 0.0), so every gap charged 0.0 and a real Qwen/embed job ran with NO ceiling (the cap only "fired" in tests via a fabricated `_CostlyStrategy` the real wiring never used — mock-only false-green). FIXED: new `GapCostModel` in cost.py charges a NON-ZERO per-gap cost = retrieval embed (1.0) + LLM completion (4.0) = 5.0 (opaque unit, not currency); assembly now wires it, so a runaway job PAUSES before unbounded overshoot (cap checked BEFORE each gap, kept `max_spend_usd` semantics). New cap-pause test uses the SAME `GapCostModel` the assembly wires (not `_CostlyStrategy`); `_CostlyStrategy` kept only as a unit adjunct. WARN1 — resume was unsafe: a re-run would re-charge from gap 0 (budget always reset to spent=0) and DUPLICATE proposals (no row dedupe). FIXED (safe + honest): migration adds `gap_ref` + partial `UNIQUE(job_id, gap_ref)`; `persist_proposal` does `ON CONFLICT DO NOTHING` + reloads the existing row (`deduped=True`) so a re-run can NEVER duplicate (live-validated on real Postgres: 2nd persist of same gap → same proposal_id, row count=1); `build_live_runner(spent_so_far=)` seeds the budget from `actual_cost_usd` so a resumed run does not reset + double-spend (`load_spent_so_far` helper). Docstrings downgraded from "resumable from where it paused" to truthful "re-runnable safely (no double-charge, no duplicate) but NOT yet skip-prior-work"; full skip-prior-work resume tracked D-C14-FULL-RESUME; NIT (C10 query-embed only pre-charged not post-metered) tracked D-C14-EMBED-METER. lore-enrichment 330 unit pass + 22 real-DB pass = 352 (was 322; +8 new: GapCostModel non-zero + assembly-wiring guard + cap-pause-on-real-path + tiny-cap-runaway-stop + rerun-no-duplicate + resume-no-budget-reset); ruff clean; live: container rebuilt+restarted, /health 200, migration applied gap_ref column + uq_enrichment_proposal_job_gap index. scope-guard CLEAR (only app/jobs/* + app/api/jobs.py + app/db/migrate.py + tests; H0 tagging / P1-gate / other services untouched). In-place (cold-start). |
| 15 | Eval + gate | DONE | 2 | | verify-cycle-15.sh exit 0; **live smoke: real judge-ENSEMBLE eval on the demo output (4 locations 玉虛宮/碧遊宮·金鰲島/蓬萊/陳塘關) → composite=96.88 (schema=100/canon=100/anachronism=100/provenance=100/usefulness=87.5), κ moderate-to-below-chance across runs, 2-of-4 judges voting (qwen-30b + qwen-35b; gemma/claude 402 model-pricing-not-configured → D11 unjudged, never faked); GATE=P2/P3 CLEARED (unlocked); persisted to enrichment_eval_runs + queryable live via /internal/eval/{project}/gate-status (has_run=true, p2_p3_unlocked=true)**. EXTENDS the platform eval ADDITIVELY in a SEPARATE namespace — ZERO diff to climate/geo eval (scripts/climate_eval*.py, eval/climate-eval-suite.toml, eval/baselines/v*.json) AND knowledge-service tests/quality (reused by FILE-PATH import via app/eval/_ensemble_shim, never copied/edited). New: scripts/enrichment_eval.py (CLI: load suite TOML → score → baseline-diff → scorecard JSON → exit non-zero on gate-fail), eval/enrichment-eval-suite.toml (weighted sub-scores schema 0.20/canon 0.15/anachronism 0.15/provenance 0.25/usefulness 0.25 + regression thresholds + [gate] min_composite 70), eval/baselines/enrichment-v1.json (frozen DETERMINISTIC baseline; live judge baseline → gitignored enrichment-v1-live.json + DB), eval/fixtures/{enrichment_demo,enrichment_bad}.json. app/eval/: scorers.py (4 DETERMINISTIC sub-scores — schema/canon/anachronism/provenance; H0 leak (origin=glossary or conf>=1.0) → provenance 0, never a high score), judge_usefulness.py (judge-ENSEMBLE majority + Fleiss κ + partial-credit; **050: proposal text NEUTRALIZED via C12 sanitizer + DATA-FENCED + strict-JSON-only verdict → injection-in-content cannot subvert the judge (3 tests prove prose-coaxing → unjudged, never forced-high)**; judges via provider-registry model_ref, NO names), suite.py (weighted composite + baseline-diff regression), runner.py (load→run→persist), gate.py (the LOAD-BEARING gate — gate_decision conservative: composite>=min AND ensemble-acceptable AND provenance>=90/anachronism>=75 floors AND no regression; gated_feature_flags FORCES P2/P3 OFF on fail so registry.select(fabrication) raises InactiveStrategyError — proven NOT advisory). enrichment_eval_runs table ADDITIVE in migrate.py (mirrors knowledge-service benchmark_runs; UNIQUE(project,suite,run_id) idempotent; clean down) + EvalRunsRepo (persist/get_latest, cross-user isolation) + /internal/eval gate-status route (fail-closed: has_run=false → p2_p3_unlocked=false). 53 new tests (18 scorer/aggregation/baseline-diff + 11 judge-ensemble incl 050 + 8 gate incl actually-gates-the-registry + 4 DB repo round-trip/idempotent/isolation + migration roundtrip extended); 397 pass with real DB (was 334; +53 + 4 DB; 0 regression). ruff/prod-isolation/secret-scan clean. self-adversary 0blk/0maj/2note: (1) gate clears at below-chance κ — κ is INFORMATIONAL not a hard gate condition (deterministic four carry composite); a κ-floor is a future tightening, tracked not a bug; (2) C14 demo proposals not persisted in this DB instance → live smoke scores the committed demo-output REPLAY fixture (same 4 locations, representative source-faithful Chinese) with the REAL judges + persists a real scorecard — labelled [demo-fixture-replay] honestly. scope-guard CLEAR (only lore-enrichment-service eval + scripts/enrichment_eval.py + eval/enrichment-* + verify-cycle-15.sh; NO climate/geo edit, NO knowledge-service edit, NO C16/C17 strategy, NO world-service/prod). In-place (worktrees/Agent-spawn unavailable). |
| 16 | Strategy (c) fabrication | DONE | 2 | | verify-cycle-16.sh exit 0; **live smoke (BOTH gate states observed): gate CLEARED (live enrichment_eval_runs composite=96.88, p2_p3_unlocked=true) → FabricationStrategy SELECTED via the gate-aware factory → REAL Qwen 3.6 fabricated 5 source-faithful Chinese dims for 蓬萊 (商周·封神-framed: 「商周之际…封神榜出…商纣无道」, era-appropriate, NO anachronism; echoes 山海经 anchor 宫室皆以金玉/鸟兽尽白) → 5 H0 facts (origin=enriched:fabrication, conf<1.0, review_status=proposed, pending_validation, grounding-cited, provenance fabricated=True), canon-verify ran (status=degraded — demo KG has no canon to contradict, conservative no-false-green), NOT promoted; AND gate LOCKED (no row) → fabrication REFUSED with InactiveStrategyError (exit 3) — the gate is ENFORCED, not advisory.** The C14-shape trap (DEFERRED-054) is CLOSED: new `app/strategies/factory.py` `GateAwareStrategyFactory` is the SOLE P2 selection path — it reads the LIVE gate (latest enrichment_eval_runs via `gate_reader.py` make_eval_runs_gate_reader, the SAME read as `/internal/eval/{project}/gate-status`) and applies the EXISTING C15 `gated_feature_flags` so a locked gate FORCES every non-P1 technique OFF, OVERRIDING any caller base_override (no `load_feature_flags(overrides={FABRICATION:True})` escape hatch); read-error fails CLOSED. `app/strategies/fabrication.py` `FabricationStrategy(EnrichmentStrategy)` (technique=fabrication, tier P2): canon-GROUNDED fabrication — REUSES C10 retrieval (grounding basis) + the C11 H0 chokepoint `make_enriched_fact` + `repair_generation` (unchanged) + C12 `verify_and_annotate` (unchanged), with a DISTINCT Chinese prompt that PERMITS plausible extrapolation but BOUNDS it hard (consistent w/ corpus + KG neighbourhood, never contradict, 商周/封神 era only); optional read-only KG-neighbour lookup (Q6 degrade to empty). Cost HIGHER than P1 (FABRICATION_GAP_COST=8.0 > retrieval 1.0) so the C8 cap pauses/escalates a runaway P2 batch sooner. NO write-back / proposal store / KG writer (C13 owns canon; H0 quarantine non-negotiable). NO hardcoded model name (model via context.model_ref / injected CompleteFn). 19 new C16 tests (P2 tier/cost; every fact origin=enriched:fabrication+conf<1.0+quarantined+grounding-basis provenance; KG-neighbour basis recorded; grounding required→refuse free invention; unrepairable→refuse; canon-verify runs; anachronistic+contradictory fabrication FLAGGED; Chinese era-bounded prompt; no-model-name grep; gate LOCKED→unselectable; CLEARED→selectable; override-cannot-bypass; read-error fails-closed; P1 unaffected; unknown-vs-inactive distinct) + 397 prior = 416 pass with real DB (0 regression). ruff/secret/prod-isolation clean. self-adversary 0blk/0maj/0min/2note: (1) decision_from_gate_status min_composite=0.0 is cosmetic-only (gated_feature_flags reads only decision.passed — threshold already applied at C15 persist); (2) FabricationStrategy not yet wired into the autonomous job runner/assembly — that is C18 productionize scope, correctly OUT (it is ONLY constructible/selectable via the gate-aware factory). scope-guard CLEAR (only app/strategies/{fabrication,factory,gate_reader,__init__} + 2 tests + verify-cycle-16.sh + docs/raid; NO new write path, NO C15 eval/climate/geo/world-service/prod edits). Circular-import fix: factory imports app.eval.gate LAZILY (the gate imports app.strategies.base → triggers the package __init__). In-place (worktrees/Agent-spawn unavailable). |
| 17 | Strategy (d) re-cook | PENDING | 2 | | dep C15,C16; P3 behind gate + licensing |
| 18 | Productionize | PENDING | 2 | | dep C14; observability + runbook + secret-scan |
