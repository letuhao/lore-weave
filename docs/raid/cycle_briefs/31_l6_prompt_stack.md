# Cycle 31: L6.H + L6.I + L6.J + L6.K + L6.L Prompt composer + Wrap + Routing + Templates + Stubs

> Auto-generated by `scripts/raid/brief-generator.py` from CYCLE_DECOMPOSITION.md §2.
> Last generated: 2026-05-28T20:36:05Z.
> last_synced_with_RAID_WORKFLOW_version: .

## 🎯 TL;DR (30 seconds — TOP critical info)
- **Scope:** L6.H + L6.I + L6.J + L6.K + L6.L Prompt composer + Wrap + Routing + Templates + Stubs. See parent layer plan for sub-component details.
- **Acceptance gate:** `scripts/raid/verify-cycle-31.sh` exits 0
- **Top 3 LOCKED decisions consumed:** Q-CYCLE-31-A-1 (placeholder; populated by layer-plan reader follow-up), Q-CYCLE-31-A-2 (placeholder; populated by layer-plan reader follow-up), Q-CYCLE-31-A-3 (placeholder; populated by layer-plan reader follow-up)
- **DPS count:** 5
- **Estimated wall time:** 3-5h (per §14.11 baseline)

## Dependencies (must show DONE in CYCLE_LOG.md)
- Cycles: C21
- Files expected to exist (grep-able paths): see parent layer plan

## Scope (IN)
- All sub-components named in title: "L6.H + L6.I + L6.J + L6.K + L6.L Prompt composer + Wrap + Routing + Templates + Stubs"
- DPS parallel breakdown per CYCLE_DECOMPOSITION.md §2 row 31 (`5`)
- Per-component tests + lints + integration smoke for cross-service touches

## Scope (OUT — explicitly)
- Cycles other than 31
- Layer sub-components not named in this title
- Anything in `infra/existing-prod/` (B5 prod-isolation enforced)

## Acceptance criteria (CI gates — exit code 0 = pass)
- Tests pass: per-cycle test suites (populated by per-cycle verify-cycle-31.sh)
- Lints pass: language-specific (go vet, cargo clippy, ruff, etc.)
- Integration smoke: cross-service if ≥2 services touched (per CLAUDE.md VERIFY rule)
- B5 prod-isolation-lint exit 0
- B6 secret-scan-cycle exit 0

## DPS parallelism plan
- DPS count from §2 table: 5
- Per-DPS slice mapping: see parent layer plan §X (Raid Leader assigns at Phase 4 PLAN)
- Each DPS isolated test stack via `test-infra-up-dps.sh 31 <I>` (B2)
- Each DPS return budget: 1500 tokens summary (P4)

## Adversary review focus (cold-start sub-agent — return budget 2000 tokens)
- Verify all named sub-components actually implemented (not stubbed)
- Cross-DPS data races (per §13.2 — per-DPS infra prevents)
- LOCKED Q-ID resolutions honored (no re-litigation)
- Cycle-specific known pitfalls: see parent layer plan REVIEW section

## Scope Guard CLEAR criteria (cold-start sub-agent — return budget 500 tokens)
- All IN-scope items present in diff
- No OUT-scope items touched
- All acceptance criteria met
- Cross-cycle invariants not violated (I1-I19 amended)

## Cross-references (for deep-read IF Raid Leader needs FOCUS mode)
- Layer plan: see PLANS_DIR/L<X>_*.md (X derived from cycle number)
- Kernel chunks: per CYCLE_DECOMPOSITION.md §3 dependency graph
- LOCKED decisions consumed (full list): Q-CYCLE-31-A-1 (placeholder; populated by layer-plan reader follow-up); Q-CYCLE-31-A-2 (placeholder; populated by layer-plan reader follow-up); Q-CYCLE-31-A-3 (placeholder; populated by layer-plan reader follow-up)

## ⚠️ REMINDERS (BOTTOM — re-stated critical info, anti-lost-in-middle)
- 🔴 **Top LOCKED 1:** Q-CYCLE-31-A-1 (placeholder; populated by layer-plan reader follow-up) → see parent layer plan
- 🔴 **Top LOCKED 2:** Q-CYCLE-31-A-2 (placeholder; populated by layer-plan reader follow-up) → see parent layer plan
- 🔴 **Top LOCKED 3:** Q-CYCLE-31-A-3 (placeholder; populated by layer-plan reader follow-up) → see parent layer plan
- 🔴 **Acceptance MUST include:** verify-cycle-31.sh exit 0 + secret-scan + prod-isolation-lint
- 🔴 **Do NOT touch:** infra/existing-prod/ OR services not in scope of this cycle
- 🔴 **Fresh session reminder:** this is a new `/raid 31` invocation; no carry-over from prior cycles. Read CYCLE_LOG.md + this brief + LOCKED file ONLY.
