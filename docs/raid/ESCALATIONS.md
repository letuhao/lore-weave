# RAID Escalations — creation-unblock

> Append a row whenever a cycle escalates to the user (blocker, ambiguity, 3+ failed fix attempts, scope change). Empty = healthy.

| When | Cycle | Type | Summary | Resolution |
|---|---|---|---|---|
| 2026-06-13 | 8 | infra_blocker | Dev stack down/unstable after a mid-run restart — knowledge-service(8216), book(8205), glossary(8211), provider-registry(8208) all DOWN after 100s; composition/translation-worker/lore-enrichment-worker crash-looping. C8–C14 (knowledge BE cluster) require a stable stack + the built 万古神帝 graph for their cross-service live-smoke; deferring all of them would stack unvalidated cross-service work. Halting before C8. | OPEN — user restores the stack (`docker compose -f infra/docker-compose.yml up -d`, confirm 8216/8205/8211/8208 `/health`=200 + the 万古神帝 project still `ready`), then re-invoke `/raid` to resume from C8. |
| 2026-06-14 | 21 | design_gap | C20 provisions a hidden world-bible book + sort_order-0 bible chapter, but exposes NO FE-reachable handle to resolve them. `worldResponse` (create/get/list world) returns only `book_count` — never `bible_book_id` or `bible_chapter_id`. The bible book carries `is_bible=true` and is excluded from BOTH `GET /v1/books` (`listBooksByLifecycle`: `AND b.is_bible=false`) AND `GET /v1/worlds/{id}/books` (`listWorldBooks`: `AND b.is_bible=false`) — added in C20's own adversary fix to stop the container book leaking into the library. C21's core acceptance ("author entity/canon/timeline lore against the world's bible chapter") is therefore unbuildable FE-only: glossary `chapter_entity_links.chapter_id` is NOT NULL (ARCH-REVIEW lock) so lore MUST anchor to the bible chapter id, and the FE has no endpoint to obtain it. Resolving it requires a book-service handler edit (surface `bible_book_id`/`bible_chapter_id` on the worlds response, OR an `?include_bible=true` on `listWorldBooks`), which the C21 brief + coordinator grounding explicitly FORBID ("ZERO book-service world-model/API edits; missing endpoint = record a finding, do NOT add BE"). The allowed thin gateway `/v1/worlds*` passthrough does not help — it only forwards; it cannot invent the missing resolution. Investigated exhaustively: no `?include_bible` param, no bible id in any worlds/books response, glossary/knowledge expose no world→bible mapping. | RESOLVED 2026-06-14 (C20 follow-up commit) — added `bible_book_id` + `bible_chapter_id` to `worldResponse`, populated on create + get + list. Create returns them from the provisioning txn; get/list resolve via two correlated subselects (world's `is_bible` book → that book's active `sort_order=0 is_bible` chapter), NULL-graceful for legacy worlds. Purely additive read field, no schema/migration. Live-smoke confirmed the create response carries a `bible_chapter_id` resolving to a real `sort_order=0 is_bible active` chapter. C21 FE now anchors lore via `worldResponse.bible_chapter_id`. |
| 2026-06-14 | 24 | design_gap | C24 FE is COMPLETE + green (19 vitest, tsc/eslint/provider-gate clean, verify-cycle-24.sh exit 0) and proven end-to-end through the gateway: the genderbend Playwright smoke drove all 4 wizard steps on 万古神帝 (branch 第3章, type=character_transform, overrode 张若尘→"now a woman", named the dị bản) and `POST /v1/composition/works/019eb683-…/derive` reached the real composition-service endpoint with the correct body — FE→BFF→BE wiring confirmed. BLOCKED by an UPSTREAM **C23 BE defect**: derive 500s with `UniqueViolationError uq_composition_work_project Key(project_id)=019eb683 already exists`. Root cause — C23's `KnowledgeClient.create_project` POSTs `{project_type:"book",book_id}`; knowledge-service `create_or_get` DEDUPES book projects per (user,book) (the D-COMP-POST-WORK-RACE guard) and returns the SOURCE's existing project (created=False) instead of a fresh one, so `create_derivative` inserts a 2nd composition_work row with the source's project_id → unique violation. G2 ("derivative gets its OWN fresh project_id / Neo4j delta partition") is structurally unsatisfiable via the current create_project path for any book that already has a Work. C23's migration round-trip never exercised the real knowledge round-trip, so this surfaced only under C24's live UI smoke. C24 is FE-only and FORBIDDEN from editing C23 schema/API; the fix belongs upstream. (First saw a stale pre-C23 composition image → 404 no-route; rebuilt+`up -d composition-service` so the derive route is live; retry surfaced the real 500.) Evidence: docs/raid/evidence/cycle-24/SMOKE_NOTE.md + wizard-step{1..4}*.png + derive-blocked-c23-dedup-500.png. | RESOLVED 2026-06-15 (Step-A BE fix, own commit) — additive + backward-compatible. knowledge-service: new `is_derivative BOOLEAN NOT NULL DEFAULT false` column on `knowledge_projects` (idempotent `ADD COLUMN IF NOT EXISTS`) + a `force_new` field on `ProjectCreate`; `create_or_get` short-circuits to a plain insert when `force_new` (skips the per-(user,book) advisory-lock/dedup) and `_insert` stamps `is_derivative=true`; the dedup SELECT + `get_by_book` add `AND NOT is_derivative` so a derivative is NEVER returned for the source book. composition-service: `KnowledgeClient.create_project(..., force_new=True)` (adds `force_new` to the POST body) and C23's `derive_work` passes it. Default false ⇒ greenfield POST /work stays idempotent (unchanged). TDD: knowledge repo (2 derives → DISTINCT ids; back-compat dedup intact; create_or_get/get_by_book never return a derivative), composition (derive sends force_new=true; client payload). knowledge 2494 unit + 25 projects-repo integration green; composition 449 unit green; provider-gate green. **live smoke: derive creates derivative with fresh distinct project_id (G2)** — rebuilt+restarted knowledge+composition, migration applied live (`is_derivative` column present), POST /works/{src}/derive on 万古神帝 (book already had a Work) → 201 with derivative project `019ec72a…` ≠ source `019eb683…`, flagged is_derivative=t; two derives → two distinct projects. Re-invoke `/raid 24` lands the FE + re-runs the genderbend smoke. |
