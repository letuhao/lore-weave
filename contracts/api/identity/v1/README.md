# Identity API contract (v1)

Machine-readable contract for Module 01. Human-readable baseline: `docs/03_planning/12_MODULE01_API_CONTRACT_DRAFT.md`.

- `openapi.yaml` — OpenAPI 3.0.3
- Versioning: path prefix `/v1` (breaking changes require `/v2` or negotiated bump).

Lint locally (optional):

```bash
npx @stoplight/spectral-cli lint openapi.yaml
```
