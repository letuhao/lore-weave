# Model Registry Contract

OpenAPI source:

- `v1/openapi.yaml`

Lint command:

```bash
npx @stoplight/spectral-cli lint --ruleset "contracts/.spectral.yaml" "contracts/api/model-registry/v1/openapi.yaml"
```
