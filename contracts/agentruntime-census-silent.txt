# Refusal sites the suite does not notice being removed ON THEIR OWN.
#
# 🔴 The header used to say 'a refusal nothing checks', and two verifiers measured
# that false for 2 of these rows: a site can be SILENT because a SAME-CLASS SIBLING
# reds first, or because it is UNREACHABLE. This gate neuters one site at a time, so
# what it observes is exactly 'alone' — and claiming more than the experiment supports is
# the failure this whole effort exists to end. Deciding WHY a row is here still needs a
# person and a verdict id.
#
# 🔴 THAT PERSON IS `CP-1-round25-v-code-b`, WHICH DID THE WORK THIS HEADER HAS BEEN
# ASKING FOR SINCE THE FILE WAS WRITTEN. Every row below now carries its classification
# and the observation behind it, so the count is no longer three different questions
# wearing one label. Two rows LEFT as a result: `manifest::build::UntrustedRow::4` and
# `manifest::validate_document::UntrustedRow::6` were provably unreachable `except`
# clauses — `check_row` raises exactly one class, proved by an AST closure and 25 executed
# malformed rows — and an `except` that cannot fire is not a refusal, so they were deleted
# from the source rather than carried here as debt.
#
# Generated by scripts/agentruntime-census.py --write, then annotated by hand from the
# verdict named above. Every line is a claim that nothing checks; adding one is a
# decision, and removing one is a closed finding.

# SIBLING — neutered alone, `digest(1.5)` is still refused, by ::4's fall-through with a
# different message. Guarding this means asserting WHICH refusal fires, not that one does.
canon.py::_norm::NotCanonicalisable::1::417da3eb

# UNCHECKED — neutered, `digest({1: "a"})` returns a digest. A non-string key has no stable
# ordering across runs, so that digest depends on insertion order and nothing says so.
canon.py::_norm::NotCanonicalisable::2::94328cec

# UNCHECKED — neutered, `digest(object())` returns a digest for a value with no canonical form.
canon.py::_norm::NotCanonicalisable::4::15c0d4e9

# UNCHECKED — fires when `manifest_path()` returns None, reproduced by anchoring
# `ambient.module_anchor` outside any marker. This is the flattened-image case that
# `manifest_path`'s own docstring exists for, and nothing drives it.
manifest.py::generate::UntrustedRow::1::0265f69f

# UNREACHABLE deterministically — needs a concurrent `unlink` between `exists()` and `load()`.
# The refusal is correct and the race is real; a test for it would be a test for a scheduler.
manifest.py::generate::UntrustedRow::2::ec0cf983

# SIBLING (partial) — neutered, `declarations="nope"` is still refused, because `list("nope")`
# yields characters and `check_row` refuses them. Residual, recorded rather than hidden:
# `declarations=None` and `declarations=5` become an uncaught `TypeError` in that state,
# converting a documented refusal into a crash.
manifest.py::validate_document::UntrustedRow::1::843af89b

# UNCHECKED — neutered, `TakeWhileBudget(budget=-1)` constructs.
surface.py::TakeWhileBudget.__post_init__::ValueError::1::3130a968

# UNREACHABLE while P1 holds, and KEPT rather than deleted — CP-2.2, 2026-08-08.
#
# The widening rule looks up the `Narrowing` it is about to overrule, so it can record WHAT it
# overruled. `cut is None` means a declaration is admitted, absent from the surface, and has no
# narrowing record: a P1 violation. Executed rather than argued — the only stage that can shrink
# `kept` without registering is `OrderBy.sort`, and (a) the real one returns every row, (b) a
# subclass overriding it is refused by `validate_pipeline`'s `type(s) is k`, measured:
#   `pipeline[0] is a Rogue, which is not one of the six stage kinds`.
#
# 🔴 This file's own precedent is to DELETE an unreachable refusal (two `except` clauses went that
# way at R25), and the precedent does not apply here. Those could not fire because the try-body
# raised exactly one other class — dead by construction of that statement. This one is dead only
# because a DIFFERENT invariant holds elsewhere, and deleting it turns a future P1 regression from
# a named failure into a bare `StopIteration` three frames up. Kept, and the cost is this row.
#
# ✅ CLOSED 2026-08-10 (tool-v2 loop #2). The census reported it NOW GUARDED, and the row is
# removed rather than left as stale debt. The id is UNCHANGED (`6b19409d`), which is what
# distinguishes this from the failure mode the header above warns about: a row can also leave
# because its digest CHURNED, and that case shows up as a NEW id arriving NEWLY SILENT. No
# surface.py id did. The raise itself stays in the source for the reason argued above.
