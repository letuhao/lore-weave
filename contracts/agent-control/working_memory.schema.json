{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://loreweave.dev/contracts/agent-control/working_memory.schema.json",
  "title": "WorkingMemory",
  "description": "The pinned goal-state block for a roleplay/interview session. Two tiers: an immutable `charter` (written ONLY by the goal authority — a static template here, the world model in full roleplay) and a mutable `state` (the progress estimate the executive rewrites). The summarizing executive may NEVER write `charter`. See docs/specs/2026-06-23-interview-roleplay.md.",
  "type": "object",
  "required": ["version", "charter", "state"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "integer", "minimum": 1 },
    "charter": {
      "description": "Committed goal/intention. Written once by the goal authority (template) ⇒ frozen for interview. Executive can never write this.",
      "type": "object",
      "required": ["goal", "phases", "checklist", "language"],
      "additionalProperties": false,
      "properties": {
        "goal": { "type": "string", "minLength": 1 },
        "phases": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
        "checklist": { "type": "array", "items": { "type": "string" } },
        "time_budget_min": { "type": ["integer", "null"], "minimum": 1 },
        "language": { "type": "string", "minLength": 2 }
      }
    },
    "state": {
      "description": "Progress estimate. Mutable; the executive rewrites it (safe-when-wrong). `covered` is monotonic/append-only; `remaining` is derived (charter.checklist - state.covered), never stored.",
      "type": "object",
      "required": ["phase", "covered"],
      "additionalProperties": false,
      "properties": {
        "phase": { "type": "string" },
        "covered": { "type": "array", "items": { "type": "string" } },
        "elapsed_min": { "type": ["integer", "null"], "minimum": 0 },
        "drift_note": { "type": ["string", "null"] },
        "redirect_hint": { "type": ["string", "null"] }
      }
    }
  }
}
