{
  "ledger_version": 1,
  "runbook": "docs/plans/2026-08-12-frontend-journey-loop-RUNBOOK.md",
  "denominator": {
    "derived_at": "2026-08-12",
    "source": "loreweave_agent_registry",
    "query": "select 'workflow',status,tier,array_to_string(surfaces,',') from workflows group by ... union all select 'skill',... from skills group by ...",
    "published_workflows": 12,
    "published_skills": 27,
    "total_declared_journeys": 39,
    "note": "Re-derived by live query at entry, never read from the RUNBOOK table. Ordering: workflows before skills; within workflows the surface with the most declared members first -- {book,editor}=5, then {book,editor,studio}=4, unrestricted=2, {book}=1. Within a surface group, by declared step count descending (a longer state machine fails in more ways), tie-broken by slug."
  },
  "preflight": {
    "state": "hardened",
    "note": "The RUNBOOK's gate 1 ('every running container is on its CURRENT image') was measured GREEN -- 35/35 infra containers matched their :latest image id -- and was STILL INSUFFICIENT. container==image does not imply image==source. infra-composition-service:latest was built 2026-08-12T04:54Z from a stale build context: its /app/app/services/plan_forge_service.py was 1990 lines with 0 occurrences of _compile_blocked_message where HEAD has 2058 lines and 2, and the image carried db/repositories/epub_import_hierarchy.py, a file absent from HEAD and absent from disk. The first defect this loop 'found' (plan_compile raising the bare 'validation failed -- compile blocked') was therefore an artifact of code deleted from the repo on 2026-08-11, and that evidence was discarded. AMENDMENT, now applied every entry: compare SOURCE to IMAGE by content -- per-file md5 of every *.py under services/<svc>/app against /app/app in the running container (exact, all 10 Python services), and for compiled services grep the running binary for a string literal introduced by that service's most recent commit. Measured after the fix: 10/10 Python services difflines=0; book-service carries 'FROM worlds old' (#319, 2026-08-12), glossary-service 'CANNOT re-add the same code' (02beee08c), catalog-service 'titleMatchesQuery' (fdb8a8b49), agent-registry-service 'surfaces where this workflow is advertised' (14e8804ce). Timestamps are NOT evidence: the stale COPY layer was dated 11:54 local while the file it copied was last modified at 05:15 local."
  },
  "journeys": {
    "vision-to-book": {
      "state": "proven",
      "surface": "{book,editor}",
      "declared_steps": 9,
      "modules_crossed": 3,
      "note": "PROVEN end to end through the browser at localhost:5174, driven by google/gemma-4-26b-a4b-qat via lm_studio (this account's active `chat` default, confirmed in user_default_models, not set for the run). NO ARGUMENT WAS TYPED BY THE OPERATOR: the only text supplied was the user's prose story idea; the model derived every tool argument from it. SELECTION worked -- it called workflow_load unprompted and entered the rail. DERIVATION worked -- glossary_extract_entities_from_doc built a document from the prose and recovered the six entities the prose names. CHAINING worked across a module boundary -- kg_add_nodes was gated with {entity_ids:[6 uuids], mode:'from_glossary', project_id} where the ids came from glossary_propose_entities and project_id from kg_project_create, none of them handed to it. COMPOSITION worked -- the confirm gate on apply-categories and the Tier-A gate on kg_add_nodes both surfaced and both resumed correctly. SURFACING was verified against SSOT rather than trusted: the FE renders a FAILED tool call with border-destructive/text-destructive against border-accent for a success, which is why plan_compile's failure was visible at all -- checked as a control before it was filed as a defect, and it refuted the claim. FINAL MEASURED STATE (book 019ff497-dff3-7f26-9565-7e284f7ca71c): 6 categories, 6 glossary entities, plan_run status='compiled', 3 structure_node (arcs), 6 outline_node (the chapter plan), 0 chapters -- correct, since drafting is the model's offered next step, not this workflow's. The journey surfaced defect D-FJ-1 (its own row, its own falsifier, its own commit)."
    },
    "draw-a-map": {
      "state": "proven",
      "surface": "{book,editor}",
      "declared_steps": 4,
      "modules_crossed": 2,
      "note": "PROVEN end to end through the browser, driven by google/gemma-4-26b-a4b-qat, no operator-typed tool argument -- book 019ff4cf-2143-7a5f-9cc4-0cf62e286432, all four declared steps in order and green: workflow_load, world_list (x2), world_map_create, world_map_add_marker, world_map_add_region, zero failed calls in the turn. The model derived the world to draw in, the map, the marker for Ashmere and the salt-flat region from the prose alone. THE FIRST ATTEMPT FAILED and is what surfaced D-FJ-2: on book 019ff497 the model skipped world_list, called world_map_create with only {name}, was refused for a missing world_id, stopped calling tools and reported 'I have initialized the map for The Drowned World' over a map that was never created (world_maps holds no row for it). Recorded honestly: the passing run did NOT exercise D-FJ-2's fix -- it never omitted world_id, so no refusal was produced. The journey and the defect are therefore proven by different evidence, which is why they are separate rows."
    },
    "kg-build": {
      "state": "proven",
      "surface": "{book,editor}",
      "declared_steps": 4,
      "modules_crossed": 2,
      "note": "PROVEN end to end through the browser on book 019ff542-1ad0-70c0-8c7d-b6066560d3aa, driven by google/gemma-4-26b-a4b-qat, with no operator-typed tool argument — the only text supplied was the user's prose. The rail's terminal step kg_build is async_job/class-C, so its correct terminal is a minted confirm card ('proposed: true … nothing is spent until confirmed'), which is what it returned with a real embedding_model and llm_model in the token params. The journey surfaced TWO defects, each with its own row, falsifier and commit: D-FJ-3 (a refusal that called one precondition the ONLY one on the call it had just refused for another) and D-FJ-4 (that refusal naming recovery tools that were not on the surface). Neither passing run was accepted as proof of the other: D-FJ-3 is proven by the refusal now naming both gates in ONE message on the first call, D-FJ-4 by the arming log plus the model actually calling the two tools it had only narrated before. Two turns in this journey died with LLM_UPSTREAM_ERROR from the local model and were retried; LM Studio answered a direct probe normally, so those were transient upstream failures, not product defects (carried as OBS-11)."
    },
    "populate-from-notes": {
      "state": "proven",
      "surface": "{book,editor}",
      "declared_steps": 3,
      "modules_crossed": 2,
      "note": "PROVEN end to end through the browser on book 019ff4cf-2143-7a5f-9cc4-0cf62e286432, driven by google/gemma-4-26b-a4b-qat, no operator-typed tool argument — the only input was a block of pasted notes in the shape a person actually writes them (four ALL-CAPS headings, em-dash bullets, a quoted term). All three declared steps succeeded: glossary_book_ontology_read, glossary_extract_entities_from_doc, glossary_propose_entities. MEASURED: 11 of 11 note items became glossary entries, every one correctly kinded — 3 character (Iseul Vantar, Rho Delkanen, Emet Sooll), 3 location (Ashmere, The Weeping Stair, Cauldron Bay), 2 technique (Saltreading, The Long Sight), 2 terminology (true chart, Drowning debt) and 1 organization (Tidewright Guild). All status='draft', i.e. reviewable rather than committed, which is what the workflow's description promises. Two details worth recording as CORRECT behaviour rather than defects, both checked before being filed: 'Warden Emet Sooll' was stored as 'Emet Sooll' (the canonicaliser strips honorifics by design), and 'Tidewright Guild' was filed as organization even though MY notes listed it under TERMS — the ontology defines organization as 'a body of PEOPLE acting together … NOT a doctrine', so the extractor was right and the heading was wrong. NO DEFECT SURFACED. The model then went beyond the rail into kg_project_create + kg_add_nodes, which is currently parked on its Tier-A approval card; that is the gate working, not a failure."
    },
    "translation-pass": {
      "state": "in_progress",
      "surface": "{book,editor}",
      "declared_steps": 3,
      "note": "NOT CONCLUDED. Run on book 019f8027-294f-7106-b697-a68b7e8f1c66 (en, 2 chapters — a book with real text, since translation-pass needs source prose and every book this loop created has 0 chapters). Attempt 1 died before any assistant row: LLM_UPSTREAM_ERROR, CP-0.4 stamped 'failed' on the user message. Attempt 2 produced one tool call (composition_package_tree) and ended outcome='crashed' with 0 chars. NOT a context problem — provider-registry's own preflight logged input_tokens=12,344 against context_length=200,000, 6% used — and LM Studio answered a direct /v1/chat/completions probe normally in between. Neither declared step (translation_coverage, translation_start_job, translation_retranslate_dirty) was ever reached. Two findings recorded from the attempts (OBS-12, OBS-13). This journey is the loop's resume point."
    }
  },
  "defects": {
    "D-FJ-1": {
      "state": "proven",
      "found_by": "vision-to-book",
      "lives_in": "services/composition-service/app/engine/plan_forge/ingest.py::_section_level",
      "title": "A plan with TWO OR MORE arcs parsed to zero arcs -- and the guidance told the caller to send the shape it had just sent",
      "note": "MECHANISM. _section_level descends past a lone top heading whenever the level below it holds >=2 siblings, which is how it tells a document TITLE ('# Dracula's Legacy' + 4 sections) from a lone real section. A story plan is written '# 1. Arc Overview' then one '## ' per arc. With ONE arc the level below holds 1 and the `len(children) < 2` guard stops the descent BY LUCK; with the ordinary THREE arcs it holds 3, so the rule descended, read '# 1. Arc Overview' as the document's title, and promoted each ARC to a top-level section. The kind matcher cannot place an arc's name, so all three landed in `unclassified` and the spec parsed ZERO arcs. Both '# 1. Arc Overview' documents in the corpus behind the original rule happened to hold a single arc, so the shape was never actually protected -- the docstring's own table records the case as 'L2 has 1 -> STAY'. WHY IT IS UNRECOVERABLE, not merely wrong: plan_propose_spec is honest -- it returned problem='no_arcs_parsed' and guidance saying 'do NOT tell the author the plan is ready ... source_markdown must open with the line \"# 1. Arc Overview\" ... then one \"## \" heading per ARC, and under each arc one \"### \" heading per beat'. The model had ALREADY sent exactly that. An instruction the caller cannot satisfy is a loop, and the turn ended with the author told a chapter plan was 'ready to see and adjust' over a book with 0 structure_node, 0 outline_node and 0 chapters. FIX. In _section_level, stop descending when the lone heading at the current level is one the author NUMBERED (`_NUMBERED_SECTION = ^#{1,6}\\s+\\d+[.)]\\s`). This is the parent-level twin of the `_DOTTED_SUB` rule already trusted one level down: `N.M` is the author saying 'this is part of section N', a bare `N.` is the author saying 'this IS section N'. It is the AUTHOR's signal, not the vocabulary's -- descending on 'is this heading a known kind?' was tried first by the original author and misread '# Story Premise: ...' as a section because 'premise' is vocabulary. The space after the dot is required, so '# 1.1 Ho So' remains a dotted sub-heading. All five unnumbered corpus titles descend exactly as before. CODE. Falsifier test_a_numbered_section_with_SEVERAL_arcs_is_still_not_a_title proven RED on the ORIGINAL defect, failing with the live symptom verbatim ('3 section(s) were read but matched no known kind: The Forged Path, The Salted Truth, The Great Unmapping'), then GREEN. Full suite 3739 passed / 403 skipped; a clean-HEAD worktree control ran 3738 passed, i.e. exactly the one test added and no regression. (One parallel-scheduling flake seen once in test_pack_budget_language_parity::test_vietnamese_is_not_starved_by_an_over_counting_encoder -- passed in isolation, passed in the clean-HEAD control, passed on re-run; pre-existing order dependence, carried below, not caused by this change.) LIVE. A/B through the browser, same prose, same model, same journey, only the fix differing -- book 019ff482 (before): problem='no_arcs_parsed', arcs=[], empty_read=true, unclassified=[3 arcs], sections_read=3, plan_run='proposed', structure_node=0, outline_node=0. Book 019ff497 (after): problem=null, arcs=3 ['The Forged Path','The Salt's Secret','The Great Unmapping'], events=6, unclassified=[], sections_read=1, plan_run='compiled', structure_node=3, outline_node=6. DATA. The 6 outline nodes are the chapter plan the author was promised and are grounded in the submitted prose ('Iseul discovers the first discrepancy in a most critical map.', 'The truth threatens the survival of Ashmere.', ...). Deployed by content, not by 'Built': image rebuilt and containers force-recreated, then per-file md5 of all 242 *.py verified difflines=0 against source on both composition-service and composition-worker."
    },
    "D-FJ-2": {
      "state": "proven",
      "found_by": "draw-a-map",
      "lives_in": "services/chat-service/app/services/stream_service.py::_missing_args_message",
      "title": "The refusal for a missing argument asserted it was NOT an id, when the missing argument was an id",
      "note": "MECHANISM. CP-5.4 split the missing-required-argument refusal into two arms keyed off the tool's DECLARED argument_supplier: 'context|plan' gets \"NOT yours to invent ... establish that context first (e.g. list or open the book you mean, then call this with the id it returns)\", and everything else gets \"These carry the actual CONTENT (not ids the system already fills) ... Do not call it with only ids\". There is a THIRD case folded into the second: the tool declares NO supplier. declared_supplier returns None, None is not in ('context','plan'), so an undeclared argument silently inherits the model-supplied sentence -- and that sentence asserts a FACT about the missing arguments that nothing computed. Only 12 of the 315 federated tools carry a contract, so undeclared is the COMMON path, not the exotic one. MEASURED LIVE 2026-08-12: world_map_create refused for ['world_id'] with \"These carry the actual CONTENT (not ids the system already fills) ... Do not call it with only ids or empty arguments.\" The one missing argument IS an id. The model was told the thing it lacked was not the thing it lacked, and told not to do the thing it needed to do; it stopped calling tools and claimed the map existed. FIX. A third arm: when any missing argument has no declared supplier, assert nothing that cannot be known -- name the arguments, forbid guessing, give the id move FIRST (obtain it from the tool that LISTS or SEARCHES those records) and the content move second. Not a reword: the old sentence makes a false claim about this call's data; the new one makes no claim it cannot support, which is the standard the context|plan arm already met. The three arms moved into one helper so the undeclared case cannot silently re-inherit the model-supplied sentence, and the call site was rewired to it. CODE. 5 new guards + 1 rewritten, ALL SIX proven RED by the repo's own harness (scripts/agentruntime-falsification.py run over just these rows: PROVEN 6, FAILED 0, each reddening its own named test, no bystanders). The pre-existing CP-5.4 guard had a falsifier anchored on the inline snippet this refactor removed; it was re-anchored and rewritten from a source-grep to a BEHAVIOURAL assertion, because a grep for the old expression would have gone red on a pure refactor while staying green on a message that consulted nothing -- the wrong way round for a guard. Full chat-service suite serial: 2875 passed, 3 skipped, 0 failed. (Under -n auto 18 tests fail and the clean-HEAD control itself crashed with an xdist INTERNALERROR; the CP suites spawn subprocesses and are parallelism-sensitive. Serial is the trustworthy signal here.) DATA. Before/after on the SAME input, with 'before' being the refusal actually recorded in loreweave_chat from the live failure and 'after' produced by the DEPLOYED image (verified difflines=0 against source): the phrase 'not ids the system already fills' and the instruction 'Do not call it with only ids' are gone, replaced by 'obtain it from the tool that LISTS or SEARCHES those records'. 🔴 WHAT THIS DOES NOT SHOW: the model did not re-make the identical mistake after the fix -- the passing draw-a-map run called world_list first and produced no refusal, and a retry in the original session made zero tool calls -- so RECOVERY FROM THE NEW MESSAGE WAS NOT OBSERVED LIVE. The refusal text is proven changed on the deployed image for the exact failing input; the model's response to it is not claimed."
    },
    "D-FJ-3": {
      "state": "proven",
      "found_by": "kg-build",
      "lives_in": "services/knowledge-service/app/tools/build_tools.py::_handle_kg_build / _handle_kg_build_graph",
      "title": "kg_build refused 'That is the ONLY precondition' on the call it had just refused for a different precondition",
      "note": "MECHANISM. target=graph has TWO gates: llm_model, checked in _handle_kg_build, and the project's embedding model, checked in _handle_kg_build_graph. They were enforced one per round trip, and the second one's message ended 'That is the ONLY precondition'. MEASURED LIVE 2026-08-12 (book 019ff497, project 019ff497-e068-77db-89f7-9d8c298fe8cd): call 1 kg_build{target:graph,project_id} -> refused, needs llm_model. Call 2 added llm_model='gpt-4o' -> refused for the embedding model WITH the exclusivity claim, i.e. a sentence denying the gate the same call had just failed. Call 3 repeated call 2 verbatim. The model then told the author 'I've started building the connection map' over a build that never started. The clause was only ever meant to say that kg_run_benchmark is advisory. FIX. Say exactly that and claim no exclusivity ('kg_run_benchmark ... does NOT gate this build'), and report EVERY unmet precondition of the requested target in ONE refusal rather than one per round trip. The project read happens only on the failure path, and errors it raises are the same ones the graph handler would raise a moment later, so they propagate rather than being swallowed into a second incomplete answer. CODE. Three guards, each proven RED by injecting the ORIGINAL code: restoring the exclusivity sentence reds test_a_refusal_never_claims_to_be_the_ONLY_precondition and nothing else; restoring the single-gate raise reds both test_BOTH_unmet_preconditions_are_named_in_ONE_refusal and test_a_SATISFIED_precondition_is_not_reported_as_unmet. The third guard is the control: with the embedding model configured the refusal names ONLY llm_model and says '1 precondition(s)', so listing a satisfied gate would be the same dishonesty pointed the other way. Full knowledge-service suite serial: 4204 passed, 3 failed -- all three reproduce IDENTICALLY on a clean-HEAD worktree control (test_kg_triage_list_default_is_bounded_and_summary, test_build_timeline_orders_and_maps, test_response_shapes_match_committed_snapshot), so they are pre-existing. (Under -n auto the run died with execnet MemoryError spawning workers -- a host resource limit, not a code failure; serial is the trustworthy signal.) DATA. Deployed by content: knowledge-service rebuilt and force-recreated, per-file md5 over all 301 *.py difflines=0 against source, and the deployed build_tools.py carries the new wording. THE JOURNEY IS STILL NOT COMPLETABLE, for a DIFFERENT defect recorded as D-FJ-4."
    },
    "D-FJ-4": {
      "state": "proven",
      "found_by": "kg-build",
      "lives_in": "services/chat-service/app/services/stream_service.py::_tools_named_in_refusal + the backend-dispatch failure path",
      "title": "A refusal named the tools that unblock it, and none of them were on the model's surface",
      "note": "MECHANISM. kg_build's refusal says 'call kg_project_set_embedding_model first (pick one of your embedding models with settings_list_models)'. Both are federated -- present in the gateway's tools/list -- but MEASURED 2026-08-12 (session 019ff498-7603…) the turn's advertised surface carried kg_build and NEITHER of them; they were reachable only by discovering them through tool_list/tool_load. The model understood the requirement, wrote 'I'll get to work on setting that up now' three times, never called either tool, retried kg_build unchanged, and ended by asking the author whether to keep trying. An instruction naming an unreachable tool cannot be followed. FIX. Arm the catalogue tools a REFUSAL names, exactly as the dispatch chokepoint already does for an off-surface tool the model DID call ('making it round-trip through tool_list/tool_load to be told yes, that one is ceremony a weak model fails') and as D-NARRATED-WRITE does for a tool the model merely NAMED. A tool the RUNTIME named, in the sentence explaining how to proceed, was the strongest of the three cases and the only one uncovered. Whole-word matching, capped at 3, never re-arming what is already on-surface. CODE. 4 guards, all proven RED by the repo's harness (PROVEN 4, FAILED 0). One of them caught a real defect in ITS OWN first draft: the call-site guard grepped for '_tools_named_in_refusal(', which the function's own `def` line also contains, so it would have stayed green with the call site deleted — it now asserts the assignment. The harness also rejected the first falsifier for that guard as a BYSTANDER, because substituting an undefined name broke the import and a different test failed first. Full chat-service suite serial: 2879 passed, 0 failed. LIVE. The journey completed end to end through the browser on book 019ff542-1ad0-70c0-8c7d-b6066560d3aa, driven by google/gemma-4-26b-a4b-qat, no operator-typed tool argument. The log records `armed recovery tool(s) ['kg_project_set_embedding_model', 'kg_run_benchmark', 'settings_list_models'] named in kg_build's refusal`, and the model then CALLED settings_list_models and kg_project_set_embedding_model — the two tools it had previously only ever narrated. After the Tier-A approval the project gained embedding_model=019eeb08-8bff-75cb-8e86-700efd4033b5 and kg_build succeeded, minting its class-C confirm card. DATA. The decisive before/after is in the ARGUMENTS, not the prose: pre-fix the model invented llm_model='gpt-4o', a model this account does not own; post-fix the confirm token carries llm_model=019eeb08-8be3-78fb-86c0-3b1eda7e0457 and embedding_model=019eeb08-8bff-75cb-8e86-700efd4033b5, both real ids obtained from settings_list_models. Reaching the tool turned a hallucinated argument into a derived one."
    }
  },
  "carried_observations": [
    {
      "id": "OBS-1",
      "note": "FE agent-status reads 'Dang chay cong cu - <tool>' (running tool) while chat-service has already persisted outcome=awaiting_input for the turn. Observed at both gates, on the verified stack, in every run. Turn-outcome and call-outcome are two vocabularies; the chip is showing the last call and calling it the turn's state."
    },
    {
      "id": "OBS-2",
      "note": "chat-service terminal-persist wrote the SAME assistant message id twice with conflicting outcomes: first outcome=crashed, then outcome=awaiting_input (session 019ff466-c571-7843-a67f-a0083b40b982, msg d6393215-c9c5-4ef5-b60c-f76d2012d06f, 0 chars both times). A gate-suspended turn appears to transit through 'crashed'."
    },
    {
      "id": "OBS-3",
      "note": "Tier-A permission card renders three buttons localized ('Cho phep mot lan', 'Luon cho phep', 'Tu choi') and the fourth raw English ('Never allow') -- one missing key in one component. Same class: the confirm card title 'Set up your book's world', the 'Did <tool_name>' lines under 'Da lam giup ban', and the book-ceiling error 'book limit reached (200) - delete or purge a book first', all English inside a Vietnamese UI."
    },
    {
      "id": "OBS-4",
      "note": "stream_service logged \"intent pinned workflow(s) ['kg-build'] from the user's request\" for prose that is almost a paraphrase of vision-to-book's own description. Both rails were then tracked and the correct one ran, so the run recovered -- but the pin chose the wrong workflow."
    },
    {
      "id": "OBS-5",
      "note": "book-service: countActiveBooks reads then INSERT writes outside one transaction, so concurrent creates can both pass the ceiling. Measured: 201 active novels against maxBooksPerUser=200, unreachable through either gated path alone. The HTTP and MCP create paths share the pattern."
    },
    {
      "id": "OBS-6",
      "note": "GET /v1/notifications/stream returned 500 on cold boot, then ERR_INCOMPLETE_CHUNKED_ENCODING on later loads. Did not affect the journey."
    },
    {
      "id": "OBS-7",
      "note": "composition-service test_pack_budget_language_parity::test_vietnamese_is_not_starved_by_an_over_counting_encoder failed once under `-n auto --dist load` and passed in isolation, in a clean-HEAD control, and on re-run. Pre-existing order dependence, not caused by D-FJ-1."
    },
    {
      "id": "OBS-8",
      "note": "Only 12 of 315 federated tools declare an `argument_supplier` contract, so CP-5.4's precise 'the runtime owes you this' arm can never fire for the other ~303 -- including every world_* tool, where world_id is a plan value emitted by world_list (the rail's own first step). D-FJ-2 makes the undeclared case honest rather than wrong; DECLARING these contracts is a separate cycle."
    },
    {
      "id": "OBS-9",
      "note": "The `draw-a-map` rail has no world-create step, and 74 of this account's 76 active novels have world_id NULL. It is satisfiable only because the account already owns 28 worlds for world_list to return -- from a genuinely fresh account the declared journey has no first move. MEASURED CONSEQUENCE in the passing run: asked to draw 'a map of a world for this story', the model picked an unrelated pre-existing world named 'S10 World probe' and created the map there. The map, its Ashmere marker and its Ring of Salt Flats region are all real and correct, but the book they were drawn for still has world_id NULL, so nothing binds them to it. The four declared steps all completed, which is why the journey is proven; that the rail cannot bind a world to the book is a gap in the DECLARED journey, for its own cycle."
    },
    {
      "id": "OBS-10",
      "note": "The `kg-build` rail is glossary_book_ontology_read -> kg_project_create -> kg_add_nodes -> kg_build, with no step that configures the project's embedding model -- yet kg_project_create creates the project with embedding_model NULL and kg_build hard-requires it. Same shape as OBS-9: the declared journey omits a step its own last step demands."
    },
    {
      "id": "OBS-11",
      "note": "A long book-studio session died with LLM_UPSTREAM_ERROR and chat-service stamped 'CP-0.4 orphaned turn: no assistant row, outcome failed' on the user message. The session had accumulated 467,340 input tokens over 9 messages against a 195K-usable context. The UI's own context meter read 10%."
    },
    {
      "id": "OBS-12",
      "note": "The intent pinner engaged the WRONG rail again, and this time it was not recoverable. For 'Check what in this book still needs translating into Vietnamese, and bring the translation up to date' — a near-paraphrase of translation-pass's own description — stream_service logged 'rail vision-to-book: 3/9 steps done, next=glossary_book_ontology_read'. translation-pass was never pinned. Same class as OBS-4 (kg-build pinned for a vision-to-book request), but there the correct rail still ran; here the model called composition_package_tree and no translation tool at all."
    },
    {
      "id": "OBS-13",
      "note": "Turns are dying as LLM_UPSTREAM_ERROR / outcome='crashed' with 0 chars, and NOTHING logs the upstream reason. provider-registry-service logs only its 'chat context preflight' line; chat-service re-raises an opaque {\"event\":\"error\",\"code\":\"LLM_UPSTREAM_ERROR\"}; worker-ai logs nothing. Observed 3 times across 3 books on 2026-08-12, at 6% context use, while a direct LM Studio probe succeeded. The operator cannot tell a model crash from a bad payload from a transport fault — a silent seam on the one path every journey depends on."
    }
  ]
}