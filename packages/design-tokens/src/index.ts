// @loreweave/design-tokens — skeleton.
//
// Filled in Session C (frontend-game scaffold). Planned exports:
//   - tailwindPreset: Tailwind plugin/preset matching the game palette
//   - cssVars: CSS custom-property declarations (HUD, modal, panel)
//   - phaserPalette: typed color constants Phaser scenes import directly
//     (so HUD bar tint matches React HUD bar bg exactly)
//
// Per spec §1 #3 (hybrid React + Phaser), shared color tokens are
// load-bearing — divergence between DOM HUD colors and canvas HUD
// elements would look broken at first glance.

export {};
