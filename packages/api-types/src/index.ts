// @loreweave/api-types — skeleton.
//
// Filled in Session D when the first tilemap-service GET call from
// frontend-game (TanStack Query) needs typed response shapes. Planned
// modules:
//   - tilemap.ts: mirrors of WorldZoneSnapshot, TilemapTemplate
//   - health.ts: HealthBody from services/tilemap-service/src/http/health.rs
//   - auth.ts: session/JWT shapes (cross-imported by @loreweave/auth-client)
//
// Generation strategy (deferred to Session D): manual hand-typed mirrors
// for V0 (small surface). If the contract surface grows past ~20 types,
// consider auto-gen from OpenAPI or schemars JSON Schema export.

export {};
