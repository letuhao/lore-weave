// @loreweave/auth-client — skeleton.
//
// Filled in Session E (WebSocket echo demo) when auth handshake is
// validated end-to-end against api-gateway-bff. Exports planned:
//   - login(credentials) → JWT/cookie
//   - logout()
//   - me() → current session
//   - HTTP interceptor that injects Authorization header
//
// Per spec §1 #5, this package is consumed by frontend-game only.
// frontend/ keeps its existing auth code (out of workspace).

export {};
