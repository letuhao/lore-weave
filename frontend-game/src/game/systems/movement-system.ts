// Tile-by-tile movement for the Player + any AI-driven entity.
// Session D fills with: lerp-to-target + collision against tilemap
// traversable flag + animation state transition.

export const MovementSystem = {
  update: (_delta: number): void => {
    // TODO Session D: tween entities toward their queued target tile
  },
};
